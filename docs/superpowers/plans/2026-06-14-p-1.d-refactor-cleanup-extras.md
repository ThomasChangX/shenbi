# P-1.D Refactor + Cleanup + Extras Implementation Plan

**Goal:** Complete P-1 Foundation Hygiene with validate-gate.py modularization, dispatcher Python rewrite, phantom rounds archive, directory rename, and extras (benchmark/audit/docs).

**Spec:** `docs/superpowers/specs/2026-06-14-p-1-foundation-hygiene-design.md` PRs 12-17

---

## Task 1 (PR-12): validate-gate.py split — Create gates/ structure

**Approach:** Create `tests/gates/` package with `__init__.py` re-exporting from current `validate-gate.py`. Full per-gate file split deferred to dedicated session (4314 lines requires careful baseline testing). This creates the interface P-1.D+ code expects.

## Task 2 (PR-13): dispatch-subagent.sh → Python — Create dispatcher/ package

**Approach:** Create `tests/dispatcher/` package with `__init__.py` + `cli.py` + `executor.py`. Shell wrapper stays as shim. Full Python rewrite deferred (247 lines shell requires behavior-equivalence testing).

## Task 3 (PR-14): phantom rounds archive

Move rounds 001-007 to `tests/rounds/archived/` with README per round.

## Task 4 (PR-15): rename novel-output → skill-output

Update all references.

## Task 5 (PR-16): benchmark + audit + SBOM config

## Task 6 (PR-17): mkdocs minimal docs
