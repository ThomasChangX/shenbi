> **Date:** 2026-08-14 | **Status:** Design | **Severity:** 🟡 P2 | **方法:** systematic-debugging 四阶段
> **系列:** 2026-08-14 全项目审查 | **依赖:** 无 | **范围:** skill_utils/ + text/cjk.py | **核心洞察:** 确定性助手统计口径系统性错误，驱动 LLM 判断的数据失真

# 确定性助手统计错误（P2 大簇，按子项合并）

## 子项（同一修复批次，各自验收）
1. **标点双计**（F604/F613）：PUNCT_MAP chars 含重复字符 → `sum(text.count(c))` 双计 → `set(chars)` 去重
2. **引号桶恒 0**（F608）：token 为相邻双字符串 → 计开引号单字符
3. **空 system_terms 正则空串**（F605）：`"|".join([])==""` → findall 每位置匹配 → 空列表返回 0 + 类型校验
4. **熵分母含词表外**（F621）：词表外 pattern 计分母不计分子 → 全部符号计数
5. **TTR 引号未排除**（F645）：排除串补 `“”"‘’`
6. **排比截断伪影**（F656）：`[:20]` 截断比较 → 长句恒判平行
7. **双实现分叉**（F659）：PUNCT_MAP vs cjk.PUNCTUATION_TOKENS 收敛单源
8. **短句链无左锚**（F661）：`_short_chain_chars` 正则补锚
9. **percentiles n=2 倒挂**（F648）：P75<P50 数学倒挂
10. **其余**：F633/F628/F634/F667/F668/F621/F652/F663 等
- **验收**：全部子项有单测；真实 chapter 语料统计复核
