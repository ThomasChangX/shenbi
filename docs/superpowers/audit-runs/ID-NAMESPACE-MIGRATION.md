# Ledger ID 命名空间迁移注记（spec #49 R2 · C35 · F978/F956）

> **裁决（2026-09-07）**：新轮生效 `<轮日期>-<前缀><编号>` 形态（如 `2026-08-16-F117`、`2026-08-16-T3`），
> 消除跨轮/跨审计代 ID 碰撞（F978：跨轮 F 编号重叠 72/123 同 ID不同 finding；F956：F8/F9/F10 跨代复用）。
>
> **旧轮零改动**：2026-08-14、2026-08-15 两轮 ledger 与既有 spec/plan/archive 的旧 ID 引用一律保持原样
> （append-only，不重编号不复用）；本注记即迁移记录，无历史行重写。
>
> 工具配合：`tools/lint_audit_run.py` 的 `id_unique` 检查在新形态下天然全局唯一（前缀含轮日期）；
> `tools/generate_carryover.py` 产出的承接清单保留旧 ID 原文（与上轮 ledger 对照用），新轮立案时
> 映射为带轮前缀新 ID 并在 ledger 行注记来源 ID。
