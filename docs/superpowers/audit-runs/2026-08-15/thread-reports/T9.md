# T9 | 枚举/状态字符串单一信源审计（fresh context）

- 仓库：`/Users/xiaotiac/Documents/GitHub/shenbi`（只读审计；脚本与中间产物仅写 `/tmp/t9x/`）
- 审计线：contracts/enums.py 唯一定义清点；lint_status_strings 覆盖洞机械化测量；生产值域三源对账
- 方法：AST 全量提取 src/shenbi 的 enum/Literal 域（36 域）→ novel-output 234 个 JSON 采集实际值（含 83 个不可解析文件的 regex 侧采集）→ md 正文 token 计数 → lint 检测集三方对照
- 编号：T901–T911（台账 `^| T9` 现状核对：全部空闲）

---

## 1. enums 全量 vs 生产值域对账矩阵

### 1.1 "唯一信源"现状：两个自我宣告的单源 + 32 个散落域

`src/shenbi/contracts/enums.py:1` 自称"全框架单一词表……所有 Literal 必须从此处 import"，但只定义 **4 域**；
`src/shenbi/status.py:1-8` 又自称 "THE single definition of every status string in the framework"，定义 **5 域**；
其余 **27 个声明域**散落在 12 个文件（AST 提取，`/tmp/t9x/extract_enums.py` 实跑：`domains: 36`）。

### 1.2 对账矩阵（36 声明域 + 5 无主生产域）

| # | 域 | 定义处 | 值 | 生产实值（三源） | 判定 |
|---|---|---|---|---|---|
| 1 | GateStatus | status.py:17 | PASS/FAIL/SKIP/WARN/UNIMPLEMENTED | gate-marker status=PASS(23)；check `s`=PASS(79)/SKIP(5)/WARN(2) | ✓（lint 拦不住 HARD_FAIL，见 §2） |
| 2 | GateOutcome.status | contracts/base.py:29 | PASS/FAIL/SKIP/WARN | — | **断裂：重复定义 #1 且缺 UNIMPLEMENTED** |
| 3 | PhaseState | status.py:30 | created/started/skills_done/scored/finalized | novel-output 无 phase-state 文件（tests-only） | 未触生产 |
| 4 | CommandStatus | status.py:40 | ok/blocked/error/exists | truth_embed 发 `ok` ✓ 但 fallback 发 `"degraded"` 越表（truth_embed.py:247） | **断裂：degraded 越表** |
| 5 | ScoringStatus | status.py:49 | ok/REJECT/MARKER_MISSING/UNIMPLEMENTED | 未持久化 | — |
| 6 | ScoreClassification | status.py:58 | PASS (excellent)/PASS (acceptable)/CONDITIONAL/FAIL | 生产 0 次（novel-output 无 classification 键值） | 死域（未持久化） |
| 7 | enums.Severity | enums.py:6 | BLOCKING/CRITICAL/MINOR | revision-decisions: BLOCKING:2/CRITICAL:2/MINOR:4 ✓；**src 零 import 消费**（消费侧是 revision_router.py:75-86 regex + route.py:44 裸比较） | **断裂：单源成摆设，词表活在 regex 里** |
| 8 | enums.Verdict | enums.py:7 | 通过/有瑕疵/不通过 | md token：通过 9391/有瑕疵 514/不通过 455（review-* 族活） | 半活（见 #9 双词表） |
| 9 | resonance 判定词表 | g4/review_resonance.py:25 `_VERDICTS` | 通过/阻断/待人机复核 | md token：阻断 117/待人机复核 16（score/resonance 族活） | **断裂：第二活词表不在 enums.py，裸 tuple 硬编码** |
| 10 | decisions.Severity | schemas/decisions.py:16 | low/medium/high | chapter-N-decisions：low 202/high 254/medium 129（585/585 ✓）；revision-decisions：146 在表 + **39 越表** | **断裂（跨文件混多词表，见 T903）** |
| 11 | revision-decisions severity 生产域 | **无定义** | — | blocking 2/critical 13/minor 6/info 5/none 1/warning 2/observation 1/critical_per_audit 1（39 处） | **无主词表** |
| 12 | G4Severity | chapter_loop.py:409 | hard/soft/warn | G4_CHECK_MAP 内部自洽 | ✓（域内） |
| 13 | linguistic_drift severity | linguistic_drift.py:56,221 | NONE/WARN/HARD/ESCALATE | 未持久化 | —（severity 概念第 4 词表） |
| 14 | drift-guidance SKILL severity | skills/shenbi-drift-guidance/SKILL.md:103 | warning | — | severity 概念第 5 词表（正文示例） |
| 15 | progress.json skill status | **无定义**（codex.py:44 / trace/materialize.py:28,59 裸写） | pending/done/skip | g_reconcile 读 "DONE"（F449 已立案） | **无主词表 + 大小写断裂（T906）** |
| 16 | HookState | schemas/hooks.py:19 | PLANTED/RELEVANT/TRIGGERED/RESOLVED/ARCHIVED/EXPIRED（+TRIGGER 非规范映射） | review_checklist.py:357 用 ACTIVE/PENDING；lifecycle skill 用 DORMANT/ACTIVE | 已立案（F815/F820/Z3-r3），不重立 |
| 17 | CPZone | enums.py:8 | GREEN/ORANGE/RED | foreshadowing_resolve.py:37-39 消费 ✓ | ✓ |
| 18 | ActorRole | enums.py:9 | GENERATOR/SCORER/GATE/SKILL/HUMAN/SYSTEM | trace.jsonl 仅 GATE(4) | ✓ |
| 19 | OutputKind | contracts/legacy.py:39 | artifact/report/ephemeral | staging plans decisions.json kind=artifact ✓ | ✓ |
| 20 | RegistryKind | schemas/registry.py:26 | 16 值 | truth-files.yaml | F445/F440 族已立案，不重立 |
| 21 | Producer | schemas/registry.py:45 | skill/pipeline/external/shared | — | — |
| 22 | ownership status | contracts/ownership.py:22 | added/deleted/modified | — | —（status 概念又一域） |
| 23 | ownership level | contracts/ownership.py:31 | field/record_create/record_field | — | — |
| 24 | Basis/Handling/Trim | schemas/decisions.py:15-18 | 4+4+4 值 | F237 已立案 schema 断裂，不重立 | 已立案 |
| 25 | RevisionMode | revision_routing/route.py:28 | spot-fix/regenerate/constrained-regenerate | revision-decisions mode：spot-fix 2 ✓ + **reconstruction 1 / no_op 1 越表** | **断裂（T910）** |
| 26 | RevisionRoute | revision_router.py:33 | spot-fix/regenerate/constrained-regenerate/no-revision | — | 与 #25 双域（+no-revision vs 生产 no_op 异形） |
| 27 | RevisionDecision | revision_router.py:42 | pass/revision/escalation | — | — |
| 28 | PipelinePhase | pipeline/state.py:18 | genesis/chapter-loop/closure/completed/failed | pipeline-state phase=genesis/chapter-loop ✓ | ✓ |
| 29 | GenesisState/ClosureState | pipeline/state.py:26,33 | pending/in-progress/checkpoint-pending/completed | genesis.state=completed/in-progress ✓；chapter_states.status=complete 55/pending 1 | ✓（但见 #30） |
| 30 | ChapterState.status | pipeline/state.py:98 | **`status: str = "pending"` 自由字符串** | complete(55)/pending(1)——**"complete" vs GenesisState 的 "completed" 同族异形** | **断裂（T907）** |
| 31 | CheckpointType | pipeline/state.py:40 | 8 值 | pending_checkpoint.type=none ✓ | ✓ |
| 32 | ReviewDecision | pipeline/state.py:51 | approve/modify/reject | checkpoint_history.decision=approve ✓；genre-config approval.decision=**approved**（另一词表，见 #33） | ✓（域内） |
| 33 | genre-config approval.decision | contracts/skills/genre_config.py:41-44 | approved/rejected | 两个生产 novel.json 均 approved | **同概念双词表（T909）** |
| 34 | WriteSafety / RecoveryStrategy / DriftKind / Routing / cjk mode | 各处 | … | 未持久化 | ✓（内部） |
| 35 | CLI not_implemented | pipeline/cli.py:904 | not_implemented | — | **"未实现"概念第 2 形态（vs UNIMPLEMENTED）（T908）** |
| 36 | trace materialize status | trace/materialize.py:28,59 | pending/done/skip | — | 归 #15 无主域 |
| 37 | novel.json status | **无定义** | — | worldbuilding / worldbuilding_complete | **无主域（T911）** |
| 38 | 修订 content_preservation status | **无定义** | — | preserved(16)/skipped(1)/delegated(1)/reconstructed_from_cross_source_evidence(1) | **无主域（并入 T909）** |
| 39 | 修订 verdict.status | **无定义** | — | skipped 等 | 并入 T909 |
| 40 | escalation severity regex 域 | revision_router.py:75-86 / parallel_dispatch.py:218-220 / audit_layer.py:157 | BLOCKING/CRITICAL（大小写不敏感） | 生产 blocking/critical 小写并存 | severity 概念第 6 词表（regex 形态） |

**汇总：36 个声明域中 15 域与本审计无关（内部/未持久化），21 域进入生产对账，其中 9 域断裂/无主**（#2,#4,#7+9,#10+11,#15,#25,#30,#33,#37+38）。

### 1.3 同概念多词表对（任务 4 全量清点）

| 概念 | 词表数 | 各词表 |
|---|---|---|
| **severity** | **6** | ① enums.Severity {BLOCKING,CRITICAL,MINOR}（零消费者）② decisions.Severity {low,medium,high} ③ G4Severity {hard,soft,warn} ④ linguistic_drift {NONE,WARN,HARD,ESCALATE} ⑤ SKILL 正文 "warning" ⑥ regex/生产自由域 {blocking,critical,minor,info,none,warning,observation,critical_per_audit} |
| **status（结果信封）** | 5+ | ① GateStatus ② GateOutcome.status（缺 1 值）③ progress skill status {pending,done,skip}（无主）④ CommandStatus+degraded ⑤ not_implemented ⑥ ChapterState.status（自由串）⑦ novel.json {worldbuilding*}（无主）⑧ content_preservation {preserved,…}（无主） |
| **verdict（判定）** | 2 | ① enums.Verdict {通过,有瑕疵,不通过}（零 src 消费者）② review_resonance `_VERDICTS` {通过,阻断,待人机复核}（硬编码，两族并存于同一"判定："行） |
| **未实现** | 3 形态 | UNIMPLEMENTED（status.py）/ not_implemented（cli.py:904）/ unimplemented()（gates/shared.py:210 的函数名，输出 UNIMPLEMENTED） |
| **完成** | 2 | completed（GenesisState/ClosureState/GenesisState）/ complete（ChapterState.status 生产 55 处） |
| **审批决定** | 2 | ReviewDecision {approve,modify,reject} / genre-config {approved,rejected} |
| **修订模式** | 3 | RevisionMode（3 值）/ RevisionRoute（4 值，+no-revision）/ 生产 {reconstruction,no_op}（双双越表） |
| **done** | 2 | done（codex.py/materialize 写）/ DONE（g_reconcile 读）——F449 已立案 |

---

## 2. lint 覆盖测量（机械化，`/tmp/t9x/measure_lint.py`）

### 2.1 检测集解剖

- 词表：`STATUS_STRING_LITERALS` = 5 个 StrEnum 共 **20 值**——不含 enums.py 任何域、不含 HookState、不含 severity/verdict/kind/mode 任何域
- 键集：`STATUS_KEYS = {status, state, classification}`——门禁 check 项实际用的 **`s` 键不在内**（docstring 自认是"避免误报"的设计取舍）
- 扫描面：`TARGET_GLOBS = ("src/shenbi/**/*.py",)`——**不含 tools/、tests/、skills/**
- 站点形态：dict 字面量 + 下标赋值——不含 return、比较、Literal 注解、f-string

### 2.2 三方对照实测（实跑 `uv run python tools/lint_status_strings.py` → 0 违规 exit=0）

| 测量项 | 数值 | 含义 |
|---|---|---|
| 面内（src/shenbi）"词表内"裸字面量站点 | **0** | lint 在其扫描面内的拦截目标集**为空**（合法发射已走 status.py 枚举）——lint 现状是纯空转守卫 |
| 面内 lint 键上的**越表**值站点 | **10** | 全部逃逸 → **面内越表逃逸率 10/10 = 100%**（F447 定性的"越表逃逸"在此首次量化） |
| 面内 `s` 键上的状态词表站点 | **308** | PASS 165/FAIL 51/SKIP 57/WARN 23/UNIMPLEMENTED 12——lint 键集外，零设防 |
| 面外（tests/）站点 | 141 词表内 + 15 越表 + 7 `s` 键 | 扫描面外，含 test_g_reconcile 的 `status: "DONE"`（F449 的测试侧镜像） |
| CWD 依赖 | `Path().glob` 相对 CWD | 从非 repo 根运行 → 0 文件扫描 → **exit 0 静默通过**（实测 `/tmp` 下 0 匹配） |

**10 个面内越表逃逸点清单**（lint 的三个键上、不在 20 值词表内的字面量）：

| 站点 | key: value | 域归属 | 已立案? |
|---|---|---|---|
| dispatcher/modes/codex.py:44 | status: `done` | progress 无主域 #15 | F449（大小写侧） |
| gates/g4/chapter_revision.py:97 | status: `HARD_FAIL` | GateStatus 域 #1 | **F402/F447** |
| pipeline/cli.py:904 | status: `not_implemented` | "未实现"第 2 形态 | **新（T908）** |
| pipeline/hook_planting.py:188 | state: `PLANTED` | HookState 域 #16（lint 词表不含它） | 新（跨域键复用，T905 附注） |
| pipeline/truth_embed.py:247 | status: `degraded` | CommandStatus 域 #4 | **新（T908）** |
| pipeline/truth_index.py:199 | state: `state`/`""` | 读方探针（非真发射） | 误报，不计 |
| trace/materialize.py:28,59 | status: `pending`/`done` | progress 无主域 #15 | **新（T906）** |

### 2.3 结论

lint 的设计目标是"防第二信源落地"，但它只能拦截"在 20 值词表内、在 3 个键上、在 src/shenbi 内、以 dict 形态"的发射——四个与门同时满足才有拦截力。实测扫描面内该目标集为空（0），而 10 个真实越表发射 100% 逃逸；门禁 check 项的主战场（`s` 键，308 站点）与全部面外代码不设防；CWD 非根时整条 lint 静默通过。**词表内拦截率无从谈起（目标空集），越表逃逸率 100%。**

---

## 3. Findings

### T901 | 双"唯一信源"并立，36 域中仅 9 域在两个"单源"文件内 | 架构 | P2
- **证据**：`grep -n "唯一\|single" src/shenbi/contracts/enums.py src/shenbi/status.py` → enums.py:1 "全框架单一词表……所有 Literal 必须从此处 import"；status.py:3 "THE single definition of every status string"。AST 提取（`/tmp/t9x/extract_enums.py` 实跑输出 `domains: 36`）：enums.py 4 域 + status.py 5 域 + **27 域散落 12 文件**（contracts/base.py、ownership.py、schemas/decisions.py、schemas/registry.py、schemas/hooks.py、legacy.py、pipeline/{chapter_loop,revision_router,write_safety,state}.py、recovery.py、skill_utils/{drift_detection/linguistic_drift,revision_routing/route,review_resonance/routing}.py、text/cjk.py）
- **根因**：单源化改造只收编了 1980 年代的 severity 分裂（enums.py）与 D3 审计的 status 信封（status.py）两个局部，未建立"新增域必须落单源"的门禁；后续 pipeline/contracts 扩张各处自建 StrEnum/Literal
- **验证**：矩阵 §1.2（36 行全列）
- **影响面**：任何"以 enums.py 为查询入口"的消费者（人 or 工具）看不到 75% 的域；F211 的"必须从此 import"契约结构性不可满足
- **建议方向**：不搬文件——在 G0 或 lint 加"新 StrEnum/Literal 域必须注册进 ALL_ENUMS 或 status.py 并声明消费键"的注册表检查；enums.py 文档字符串改口"severity/verdict/CPZone/ActorRole 单源"而非"全框架"

### T902 | Verdict 双活词表：enums.Verdict 零 src 消费者，真实第二词表硬编码于 checker | 词表断裂 | P2
- **证据**：`grep -rn "Verdict" src/shenbi --include="*.py" | grep -v contracts` → 除 contracts/__init__ 再导出外零消费；g4/review_resonance.py:25 `_VERDICTS = ("通过", "阻断", "待人机复核")`（裸 tuple）。生产 md token 计数（grep -ro 实跑）：通过 9391 / 有瑕疵 514 / 不通过 455（review-* 族）+ 阻断 117 / 待人机复核 16（score/resonance 族）——两族写同一形态的"判定："行
- **根因**：enums.Verdict 收编 review-* 族的词表后，resonance 校准门族引入第二判定词表未回写 enums.py，checker 也不 import 单源
- **验证**：`grep -rl "有瑕疵" novel-output | wc -l` → 191 文件；`grep -rn "import.*Verdict" src/shenbi` → 仅 contracts/__init__.py
- **影响面**：判定聚合/统计若按 enums.Verdict 过滤会漏掉 resonance 族全部"阻断"判定（117 处生产）；两族判定语义不同构（有瑕疵=可过，阻断=硬拦）
- **建议方向**：二选一——enums.py 增 `ResonanceVerdict = Literal["通过","阻断","待人机复核"]` 且 review_resonance.py import 之；或合并为统一五值判定词表并加迁移映射

### T903 | severity 概念六词表并立；revision-decisions 生产 39/185（21.1%）severity 值越一切词表 | 词表断裂 | P2（F211 的量化扩展）
- **证据**：regex 全量采集（含 83 个不可解析文件）`chapter-N-revision-decisions.json` severity：low 95/high 36/medium 15（decisions.Severity ✓，146 处）+ critical 13/minor 6/info 5/blocking 2/none 1/warning 2/observation 1/critical_per_audit 1（**39 处不在任何词表**）+ BLOCKING 2/CRITICAL 2/MINOR 4（enums.Severity ✓但与同文件小写形态并存——chapter-6-revision-decisions.json 同时含 `delegation_decisions[].severity="BLOCKING"` 与 `adjustments[].severity="blocking"`）。对照组 `chapter-N-decisions.json`：low 202/high 254/medium 129，**585/585 全在表**
- **根因**：chapter-revision skill 的输出示例/审计引用（SKILL.md:64,85,95-96 用小写 blocking/critical）引导 LLM 混用三词表+自由扩展；decisions-validator 的 Literal low/medium/high 从未对该文件族生效（叠加 F434：pipeline 不传 decisions 给 G4）
- **验证**：`/tmp/t9x/collect_production.py` + regex 侧采集实跑输出（§证据行）；drift-guidance SKILL.md:103 `severity: warning` 为第 5 词表实例
- **影响面**：revision_router 的 BLOCKING/CRITICAL regex（大小写不敏感）恰能兜住 blocking/critical，但 info/minor/none/warning/observation/critical_per_audit 7 个值无任何消费语义；若 F434 修复后 decisions-validator 上线，39 处将批量 schema FAIL
- **建议方向**：生产侧收敛——chapter-revision SKILL.md 的输出模板把 severity 枚举显式化为 low/medium/high（与 decisions-sidecar 契约对齐），G4/chapter_revision checker 增加 severity 词表检查；enums.Severity 与 decisions.Severity 合并或显式声明互斥适用面（escalation 域 vs sidecar 域）
- **边界**：F211 立案"同名双域"，F237 立案"schema 全景断裂（P1 verified）"；本条是**词表层**的六词表清单+生产量化，不重复立案 schema 失败

### T904 | GateOutcome.status 重复定义 GateStatus 域且缺 UNIMPLEMENTED | 双域不一致 | P2
- **证据**：contracts/base.py:29 `status: Literal["PASS","FAIL","SKIP","WARN"]` vs status.py:17 GateStatus 五值（含 UNIMPLEMENTED）；base.py 注释自认"后续支柱二门改造时统一迁移"——迁移未发生
- **根因**：v2 契约层先行定义，status.py 收编时未同步；Literal 注解形态恰好不在 lint 检测形态内（dict 值才查）
- **验证**：mypy/basedpyright 对 `GateOutcome(status=GateStatus.UNIMPLEMENTED)` 会报 Literal 不匹配（类型层已暴露不一致）
- **影响面**：若任何门以 unimplemented() 输出喂 GateOutcome 聚合器，类型错误或静默降级 str
- **建议方向**：base.py 改 `status: GateStatus`（import from status.py），删本地 Literal——一行改动

### T905 | lint_status_strings 三重覆盖洞：面内拦截目标空集+越表逃逸率 100%+`s` 键/面外/CWD 三盲区 | lint 覆盖洞 | P2
- **证据**：§2.2 全表（实跑）。要点：面内 lint 键上"词表内"发射 0 处、越表发射 10 处全逃逸；`s` 键 308 站点（面内）零设防；tests/ 156 站点、tools/ 不在 TARGET_GLOBS；`Path().glob` 相对 CWD，非 repo 根运行 0 文件 exit 0（实测 `/tmp/t9x` 下 `len(files)==0`）
- **根因**：lint 用"词表内字面量"当违规信号（白名单式），词表外的新词表（正是漂移本体）定义上不可能被拦——检测器与被测物互为空集
- **验证**：`uv run python tools/lint_status_strings.py` → 无输出 exit 0（当前全绿）vs 本审计同面测得 10 越表点
- **影响面**：F402 的 HARD_FAIL、T906 的 done/pending、T908 的 degraded/not_implemented 均以此洞存活；`s` 键是门禁 check 结果的主键，308 个发射点无单源约束
- **建议方向**：①反向检测——status/state/classification 键上的**任何**字符串字面量（除 status.py）都违规（值必须来自枚举成员），白名单改为"枚举成员表达式"；②`s` 键纳入检测（check 项构造改用 GateStatus 成员）；③TARGET_GLOBS 锚定 `Path(__file__).parents[1]`；④tests/ 纳入扫描面
- **边界**：F447 已立案"越表字面量逃逸"定性；本条是机械化量化+三盲区清单，修复设计归此

### T906 | progress.json 的 skill status 域（pending/done/skip）无任何枚举定义 | 词表断裂 | P2
- **证据**：dispatcher/modes/codex.py:44 `skill_entry[test_type] = {"score": score, "status": "done"}`；trace/materialize.py:28 `{"status": "pending"}`、:59 `payload.get("status", "done")`、:64 `if sd[tt]["status"] in ("done", "skip")`——三处裸写三值词表 {pending, done, skip}，src 无任何 StrEnum/Literal 定义该域；g_reconcile 读 "DONE"（F449 大小写断裂已 verified）
- **根因**：progress.json 的 per-skill 状态是 dispatcher 时代的自由字符串，status.py 收编时只收了 phase-state 域（PhaseState），漏了 skills 嵌套域
- **验证**：`grep -rn '"status": "done"\|"status": "pending"' src/shenbi/dispatcher src/shenbi/trace` → 上述站点
- **影响面**：F449 修复若只改大小写不立域，"skip" 值仍在词表外（g_reconcile 的 done 判定集不含 skip → GR.1/GR.2 对 skip 技能行为未定义）
- **建议方向**：status.py 增 `SkillProgressStatus(StrEnum)` {pending, done, skip}，三写方+g_reconcile 读方全部 import；F449 修复时一并落地
- **边界**：F449 立案 done/DONE + '-scores' 双源；本条立案**域无主+skip 值**，是 F449 的补全面

### T907 | ChapterState.status 是无词表自由串，且 "complete" 与 GenesisState/ClosureState 的 "completed" 同族异形 | 词表漂移 | P2
- **证据**：pipeline/state.py:98 `status: str = "pending"`（无 Literal/枚举）；生产 pipeline-state.json chapter_states：`{'complete': 55, 'pending': 1}`（实跑 Counter）vs genesis.state="completed"、closure="pending"（GenesisState/ClosureState 词表值 completed）
- **根因**：ChapterState 漏收编；"完成"拼写随手写
- **验证**：`python3 -c "json.load(pipeline-state.json)...Counter"` → complete 55/pending 1
- **影响面**：任何按 "completed" 过滤章节状态的消费方漏掉全部 55 个已完成章；未来 in-progress 章若写入，拼写无约束
- **建议方向**：`ChapterStatus(StrEnum)` {pending, in-progress, complete} 或统一 "completed" 拼写（一次性迁移生产 state 文件），state.py:98 收型

### T908 | "未实现"三形态（UNIMPLEMENTED/not_implemented/unimplemented）+ CommandStatus 域外值 degraded | 词表漂移 | P2
- **证据**：status.py:27 `UNIMPLEMENTED="UNIMPLEMENTED"`（Gate/Scoring 两域共用）；pipeline/cli.py:904 `"status": "not_implemented"`（rollback 命令 JSON）；gates/shared.py:210 `def unimplemented()`（函数名）。pipeline/truth_embed.py:247 `CommandStatus.OK if ok else "degraded"`——**注解声称 CommandStatus，fallback 值在域外**（{ok,blocked,error,exists} 无 degraded）
- **根因**：CLI 层 JSON 信封与框架 status 域两套体系；truth_embed 的 degraded 是 embed 降级语义，写时未扩 CommandStatus
- **验证**：`grep -rn "not_implemented" src/shenbi` → cli.py:904 唯一；`grep -n "degraded" src/shenbi/pipeline/truth_embed.py` → :247
- **影响面**：CLI 消费方按 CommandStatus 解析 rollback 输出得未定义值；emit_json 不校验域
- **建议方向**：CommandStatus 增 DEGRADED（或 truth_embed 用独立 EmbedStatus）；cli.py:904 改 `CommandStatus.ERROR`+message，或 CommandStatus 增 NOT_IMPLEMENTED 一处收编

### T909 | 审批决定双词表（approve/modify/reject vs approved/rejected）+ 修订 content_preservation/verdict status 无主域 | 词表漂移 | P2
- **证据**：ReviewDecision（pipeline/state.py:51）{approve,modify,reject} 由 pipeline-review CLI 消费（cli.py:587 `ReviewDecision(args.decision)`）；genre-config approval.decision {approved,rejected} 由 contracts/skills/genre_config.py:41-44 独立校验——生产 novel-output 两域并存（checkpoint_history.decision=approve；genre-config.json approval.decision=approved）。另：chapter-6-revision-decisions.json `content_preservation_verification.items[].status` = preserved(16)/skipped(1)；`revision_verdict.status`=skipped；`delegation_decisions[].status`=delegated 等——**整族无主词表**
- **根因**：genre-config 的审批是 LLM 写 JSON、schema 层自校验小词表；pipeline 审批是人机命令行域；修订验证 status 由 chapter-revision SKILL 输出模板自由生成
- **验证**：`/tmp/t9x/collect_production.py` 输出 decision/status 键全值（§1.2 #33/#38/#39 行）
- **影响面**：低（各自消费方自洽），但"决定/状态"概念词表增殖无门禁——下一个 sidecar 还会再造
- **建议方向**：genre_config.py 若可改用 ReviewDecision 值域则收敛（approved→approve 迁移一次）；修订验证 status 族在 chapter-revision SKILL.md 输出模板中枚举化并进 decisions schema

### T910 | 修订 mode 生产越表：reconstruction/no_op 不在 RevisionMode、RevisionRoute、SKILL.md 任一词表，G4 不校验 | 词表断裂 | P2
- **证据**：生产 revision-decisions mode（regex 全量）：spot-fix 2 ✓ / **reconstruction 1 / no_op 1**；词表三处：route.py:28 RevisionMode {spot-fix,regenerate,constrained-regenerate}、revision_router.py:33 RevisionRoute（+no-revision）、chapter-revision SKILL.md:119-121（仅三模式）；g4/chapter_revision.py grep "mode" → 无值校验（仅 "revision mode must be documented" 的存在性检查）
- **根因**：LLM 产 mode 无枚举约束、checker 只查"有无"不查"值域"；RevisionMode/RevisionRoute 本身又是同概念双域（RevisionRoute 多 no-revision，生产却写 no_op——第三个"不修"拼写）
- **验证**：§证据 regex 计数实跑
- **影响面**：下游若按 RevisionRoute 分流（spot-fix→PATCHES，regenerate→重生），reconstruction/no_op 落默认分支；"no_op vs no-revision vs skip" 三异形无映射
- **建议方向**：G4/chapter_revision 增加 mode∈RevisionRoute 检查；或 decisions schema 给 mode 加 Literal；"不修"拼写统一 no-revision

### T911 | novel.json status（worldbuilding/worldbuilding_complete）无主域、src 无读写方 | 文案/无主域 | M
- **证据**：生产 novel.json `"status": "worldbuilding"`（xinghuo-ranqiong）/ `"worldbuilding_complete"`（test-validation）；`grep -rn "novel.json" src/shenbi` → 仅 total_chapters 读写与 G4/G5 文件清单，无 status 读写——纯 LLM 产物字段
- **根因**：worldbuilding skill 输出模板带 status 字段，无消费无校验
- **影响面**：极低（无读取方）；仅作词表增殖样本
- **建议方向**：novel.json schema 若立（G4/worldbuilding.py 已读该文件），顺带声明或删除该字段

---

## 4. 与已有台账的边界划分

| 已立案 | 内容 | T9 边界 |
|---|---|---|
| F402/F447 | chapter_revision 返回 HARD_FAIL；lint 只拦词表内字面量的定性 | T905 把"越表逃逸"量化为 10/10=100% 并给出三盲区+修复设计；HARD_FAIL 不重立 |
| F449 | g_reconcile 读 DONE vs 写方 done（+ '-scores' 后缀） | T906 立案**域无主+skip 值**——F449 的补全面；大小写断裂本体不重立 |
| F211 | enums.Severity 与 decisions.Severity 同名不同域、"必须 import"被违反 | T903 扩展为六词表全清单+生产 39/184 量化；T901 立案双单源并立的架构面 |
| F237 | DecisionsDoc schema vs 生产全面断裂（P1 verified） | T903 只做词表层；schema 验证失败归 F237 |
| F815/F820/Z3-r3 | HookState 缺 DORMANT/ACTIVE；review_checklist 用 ACTIVE/PENDING | HookState 域不再立案，矩阵 #16 仅引用 |
| F445/F440/F414 | SHORT_MAP/注册表缺项 | RegistryKind/技能注册表族不重立 |
| F434 | pipeline 不传 decisions 给 G4 | T903 影响面引用（词表检查上线的前提） |

## 5. 总结

- **枚举域数**：36 个声明域（AST 全量）+ 5 个无主生产域 ≈ 41；仅 9/36 在 enums.py+status.py 两个"单源"文件内
- **断裂域**：9 个（GateOutcome-status 重复、CommandStatus-degraded、Verdict 双活词表、severity 六词表、progress-skill-status 无主、ChapterState complete/completed、审批 approve/approved、RevisionMode 生产越表、novel.json/content_preservation 无主族）
- **lint 逃逸率**：扫描面内越表逃逸 **100%（10/10）**，词表内拦截目标集为空（0 站点）；`s` 键 308 站点、tests/ 156 站点、非根 CWD 三盲区
- **新增 findings**：T901–T911 共 **11 条**——**P2×10，M×1，无 P0/P1**（P1 级的解析失败已由 F237/F449 立案，本线增量均为词表漂移/覆盖洞层）
- **单源化最小改动面**（每域一方向）：base.py 一行改 `status: GateStatus`（T904）；status.py 增 `SkillProgressStatus`（T906）；ChapterStatus 收型 state.py:98（T907）；CommandStatus 增 DEGRADED/NOT_IMPLEMENTED 或 cli 改用 ERROR（T908）；enums.py 增 ResonanceVerdict 且 review_resonance import（T902）；chapter-revision SKILL severity/mode 枚举化+G4 值域检查（T903/T910 生产侧收敛）；lint 白名单反转为"仅枚举成员表达式合法"+锚定 `__file__`+纳入 `s` 键与 tests/（T905）；新域注册门禁（T901 架构面）
