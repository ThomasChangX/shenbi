# T10 · 历史修复回归核验（fresh context，2026-08-15 轮）

> **线程**: T10 历史修复回归核验
> **核验基线**: HEAD `f019259`（main；工作树干净）
> **输入清单**: zones/Z9-c.files §3"上轮修复声称清单"22 条（权威输入）+ 上轮 thread-reports/T10.md（19 条）+ findings-ledger.md（F508/F514/F860/F340/F341/F304）+ final-report.md §6.5 + plans/archive/2026-08-15-pipeline-never-completes.md（PR #42 spec）
> **只读声明**: 除本报告文件外未创建/修改/删除任何仓库文件；未运行 pytest/dispatch/pipeline；未执行任何 git 写操作（log/show/diff 只读）；一切命令非交互（`</dev/null` 包裹）；临时产物仅写 /tmp/t10/
> **方法**: 对每条修复声称提取修复签名（函数/常量/配置值/工件），在当前 main 代码逐项 grep + read 精确定位；消失 = 回归 finding

---

## 0. 前置结论

**回归率 0/22**：没有任何一条已声称的修复签名从当前 main 消失。最高优先级第 22 条（PR #42 pipeline-never-completes）R1-R6 + F340/F341/F304 全部 9 个修复面在当前代码逐项命中。唯一 partial 为第 9 条（归档 spec 行号订正再漂移，T1001 持续且恶化）。上轮 T1002（torch INDEX 注记过期）已随 PR #42 的 INDEX 重建顺带解决。

**后继覆盖排查**：`git log ac46663..HEAD -- src/ skills/ tests/` = **0 提交**（其后 19 个提交全部为 `docs(audit)`），故 PR #42 合并后不存在任何可覆盖修复的代码提交；`git merge-base --is-ancestor ac46663 HEAD` 确认 #42 已在 main。

---

## 一、22 条逐条核验表

| # | 声称（出处） | 核验命令（关键签名） | 当前命中（行号） | 判定 |
|---|---|---|---|---|
| 1 | TokenLedger 接线（PR #39；T10.md:14） | `grep -n "TokenLedger(\|def _record_token_usage\|def _log_token_usage" src/shenbi/pipeline/dispatch_helper.py` | `TokenLedger(project_dir).record(` **:1346**（`_record_token_usage` :1314 内）；`_log_token_usage` :1278，`if state:` :1310 分支真实调用 | ✅ 修复确认 |
| 2 | finish_reason=length 检测 + cap-raise（PR #40；T10.md:15） | `grep -n "finish_reason\|_MODEL_OUTPUT_CEILING\|content_filter\|raised_cap" dispatch_helper.py` | 捕获 :1425-1431；检测 :1446；`_MODEL_OUTPUT_CEILING=65536` :82；cap 公式 `min(cap*2, ceiling*0.9)` :1593-1594；persistent-length fail-fast :1639-1653；content_filter hard-fail :1586-1589 与 :1655-1661 | ✅ 修复确认（仍仅 API 路径，T505 口径不变） |
| 3 | truth_io upsert 调用方全覆盖（T10.md:16） | `grep -rn "upsert_markdown_row" src/shenbi/pipeline/` | dispatch_helper.py **:1218-1219**（emotional_arcs/chapter_summaries）；chapter_loop.py **:3055**（resonance_trend）；truth_io.py（pipeline/ 下）per-path Lock :54-69 | ✅ 修复确认（与 F397/F1104 的 append_dedup 路径不同面，无涉） |
| 4 | torch-bump follow-up（PR #28；T10.md:17） | `sed -n '3610,3612p' uv.lock`；`grep -n "dependency-type" .github/dependabot.yml`；`ls .github/workflows/embeddings-smoke.yml` | torch **2.13.0**（uv.lock:3610-3611）；`src/` 零 `import torch`；dependabot.yml:19 `dependency-type: "direct"`；embeddings-smoke.yml:37-44（instantiate + `m.encode`，非 import-only） | ✅ 修复确认（T1002 注记已另行解决，见下） |
| 5 | cyclic-import 重构（PR #26；T10.md:18） | `grep -rn "py/cyclic-import" src/` | **0 命中**；audit/_shared.py 叶模块（`__all__ = ["AuditResult", "derive_output_files"]` :27，仅依赖 contracts.*+stdlib） | ✅ 修复确认 |
| 6 | silent CWD fallback 移除（Task 15b；T10.md:19） | `grep -n "silent CWD fallback removed" src/shenbi/gates/shared.py` | shared.py:58 `def resolve_input_path`，:73 `"silent CWD fallback removed (Task 15b)"` raise | ✅ 修复确认 |
| 7 | G0.16 skill 契约验证 + P2.5（T10.md:20） | `grep -n "check_skill_contracts" g0.py`；`grep -n "P2.5" AGENTS.md`；`grep -n "rationale" gates/g4/decisions_validator.py`；`grep -n "decisions" gates/g2.py` | g0.py:627-646 G0.16 块调 `check_skill_contracts()`（g0_skill_contract.py:93）；AGENTS.md:70 P2.5 原文；decisions_validator.py:108；g2.py:76 `file_type == "decisions"` 跳 word count | ✅ 修复确认 |
| 8 | 决策 JSON 恢复（raw_decode；T10.md:21） | `grep -n "raw_decode\|DecisionsDoc.model_validate" dispatch_helper.py` | :839 `decoder.raw_decode(content)`；:847-851 `shenbi-decisions-v1` 必过 `DecisionsDoc.model_validate` | ✅ 修复确认 |
| 9 | 文档行号漂移修复（spec 文本订正；T10.md:22） | 见 T1001 详情表 | 订正动作存在但行号已再漂移 **+19~+43 行**（上轮 +6~+22，恶化） | ⚠️ partial（T1001 持续） |
| 10 | mkdocs 14 死链 + pre-push 拦截（PR #23；T10.md:28） | `grep -n "mkdocs\|--strict" tools/pre-push-check.sh` | :44-48 docs 变更触发 `mkdocs build --strict` + 死链特征串判定 | ✅ 修复确认 |
| 11 | G0.11 fixture 漂移 + MIRROR_MAP 单一源 + pre-commit（PR #23；T10.md:29） | `grep -n "MIRROR_MAP" g0.py tools/check_fixture_mirror.py`；`shasum -a 256 outline-example.md tests/fixtures/outline-example.md` | g0.py:13 模块级 MIRROR_MAP；check_fixture_mirror.py:12 从 g0 import；**sha256 双方一致** `12c31001…b851` | ✅ 修复确认 |
| 12 | pre-push pip-audit 锁 dev 组（PR #25；T10.md:30） | `grep -n "pip-audit\|--group dev" tools/pre-push-check.sh` | :37-39 `uv sync --frozen --group dev` → `uv run pip-audit` | ✅ 修复确认 |
| 13 | pro→flash 文档漂移订正（PR #40 audit-T5；T10.md:31） | `grep -rn "deepseek-v4-pro" src/ executor_config.toml`；`grep -rln "deepseek-v4-flash" …` | pro **0 命中**；flash 命中 4 文件（dispatch_helper/cost/estimate/cost/pricing/executor_config.toml） | ✅ 修复确认 |
| 14 | RETRY_JITTER 加宽 2.0（PR #40 audit-T6；T10.md:32） | `grep -n "RETRY_BACKOFF_BASE\|RETRY_JITTER" src/shenbi/pipeline/parallel_dispatch.py` | :31 `RETRY_BACKOFF_BASE = 2.0`；:36 `RETRY_JITTER = 2.0`（注释引 spec §5.3/§2.8）；消费 :118 | ✅ 修复确认 |
| 15 | 定价 fail-loud + ledger 守卫（PR #40 audit-T7；T10.md:33） | `grep -n "raise ValueError" cost/pricing.py`；`grep -n "_safe_estimate_cost" cost/ledger.py` | pricing.py:40,52 未知模型 raise；ledger.py:23 `_safe_estimate_cost` 包裹（消费 :76） | ✅ 修复确认 |
| 16 | renovate.json 移除 + ci step 移除（PR #28；T10.md:34） | `ls renovate.json`；`grep -rn renovate .github/workflows/*.yml` | 文件不存在；workflows 0 引用 | ✅ 修复确认 |
| 17 | drafting max_tokens 32768（PR #40 audit-T3；T10.md:35） | `grep -n "max_tokens" executor_config.toml` | :12 `max_tokens = 32768`（`[overrides."shenbi-chapter-drafting"]` 下，注释记载 16384→32768 决策；:3 的 16384 为 `[default]` 节，非 drafting） | ✅ 修复确认 |
| 18 | 字段级 reads dict-form 落地（PR #16/#17；T10.md:36） | `grep -n "fields:" skills/shenbi-chapter-drafting/SKILL.md` | :9,17,22 `fields:` 三处 dict-form reads | ✅ 修复确认（F201/F836 语义缺陷为独立 finding，非回归） |
| 19 | F508/F514 coverage.xml 污染重生成（findings-ledger.md:24,169） | `head -c 250 …/2026-08-14/d1/coverage.xml`；`wc -l d1-06-coverage-gaps.txt` | line-rate=**0.8728**；d1-06 = **1448 行** | ✅ 修复确认（final-report 85.16% 未同步为本轮 F971，非本线程范围） |
| 20 | F860 .hypothesis 17 个失败样本已修复（findings-ledger.md:635） | 见 §三 专项 | 17 个 `via('discovered failure')` 计数吻合；4 族修复签名全部在场（PR #8 引入后未移除） | ✅ 修复确认（1 子例不可静态归因，见 §五；计数勘误 T1003） |
| 21 | 共享 coverage.xml 写竞争修复（final-report.md:66 §6.5） | `grep -n "COVERAGE_FILE" d1/d1-baseline.md` | :69 修复记录：`COVERAGE_FILE=/tmp/audit-exclusive.coverage` + 独占 xml 输出重跑（2814 passed）+ d1-06 重提取 1448 行；工件 coverage.xml（timestamp 1786641780561）在场 | ✅ 修复确认（审计过程性修复；D1-02 addopts 根因仍 specced 开放，从未声称已修，非回归） |
| 22 | **PR #42 pipeline-never-completes（R1-R6 + F340/F341/F304）** | 见 §二 专项 | R1/R2/R3/R4a/R4b/R5/R6 + F340/F341/F304 **9 面全部命中** | ✅ 修复确认（全项） |

---

## 二、第 22 条专项：PR #42（commit `ac46663`，squash-merged，T1-T9）逐 R 核验

**合并状态**: `git merge-base --is-ancestor ac46663 HEAD` ✅；plans/INDEX.md:12 登记"最近完成：#6 pipeline-never-completes（PR #42，2026-08-15）"；spec（specs/archive/2026-08-14-pipeline-never-completes-design.md:1）Status = "Done (PR #42)——post-merge just check EXIT=0（2887+4 passed，85.34%）；T1-T9 全落地"；.superpowers/sdd/audit-T1.md~T9.md 在场（每 task fresh-context 重审产物）。

| R 项 | 修复签名 | 当前命中 | 判定 |
|---|---|---|---|
| **R1** 中文卷界解析（F324） | `_read_cn_volume_boundaries`/`_CN_VOL_HEAD_RE`/`_CN_VOL_RANGE_LINE_RE`；fixture + MIRROR_MAP；测试 | _shared.py:58,62,**:68**（定义）、:121（英文空集回落）；tests/fixtures/volume-map-xinghuo.md（26334 字节）；g0.py:15 MIRROR_MAP；tests/pipeline/test_volume_map_cn.py（4 test） | ✅ 真实存在 |
| **R2** total_chapters 统一（F353） | `update_total_chapters`（单一写点）；genesis step-6 钩子；mid-book heal；cli/triggers 委托；`_count_total_chapters` 删除 | _shared.py:**:139**；genesis.py:271（钩子定义）+:372-373（`if step.skill == "shenbi-volume-outlining": genesis_finalize_volume_map(...)` 在 `_INDEX_UPDATE_SKILLS` 成功块内）；cli.py:217-222 heal（`_orchestrate_to_checkpoint` :165 内、守卫前）；cli.py:158-162 与 triggers.py:360-364 双委托；`grep -rn "_count_total_chapters" src/ tests/` = **0 命中**（旧求和口径已删）；tests/pipeline/test_total_chapters.py（7 test） | ✅ 真实存在 |
| **R3** G4 目录参数化 + closure step 10 契约对齐（F371） | generic.py `is_dir()` 分支 + `manifest_missing`/`dir_empty`；closure step 10 `snapshots/chapter-NNN/`；`_closure_snapshot_dir`；SKILL manifest 钉契约 | generic.py:36,47,52,54（全分支）；closure.py:115（step 10 路径，注释 "was final-snapshot/"）+:166-178（helper）+:190,334（两接线点）；skills/shenbi-snapshot-manage/SKILL.md:18,33（manifest.json 契约）；tests/pipeline/test_g4_directory.py（5 test）；tests/fixtures/snapshot-dir/ 两真实快照 + g0.py:16-19 MIRROR_MAP | ✅ 真实存在 |
| **R4a** PathContext + per-family N 表（F245） | `PATH_CONTEXT_PREFIX`/`PathContext`/`format_path_context`/`parse_path_context`/`build_trigger_context`/`resolve_contract_path`/`resolve_or_skip_ctx`/`_FAMILY_N`/`_AC_ANCHOR` | contracts/paths.py:24,27,28,33,50,59,84,97,124（九签名全部）；tests/pipeline/test_path_context.py（15 test） | ✅ 真实存在 |
| **R4b** 四消费者接线（F373） | triggers ctx 计算+[path-context] prompt 行+G4 解析；`_build_skill_prompt(path_context=)`；reads/writes 经 ctx；executor 双派发点 parse+derive ctx；audit/_shared derive ctx | triggers.py:523-530（ctx 计算）、:535-541（`prompt = f"{prompt}\n{ctx_line}"` 注入）、:565（g4_file 经 resolve）；dispatch_helper.py:530（签名）、:594（reads 经 resolve_or_skip_ctx）、:669-671（writes/updates）、:1517/:1729（API/IDE 路由 parse+透传）、:1891（子进程路由）；executor.py:169-177,209,257（parse+derive+audit_watch 三接点）+:98,110,234-238（derive 签名）；audit/_shared.py:25,42,55；tests/pipeline/test_trigger_context.py（5 test） | ✅ 真实存在 |
| **R5** closure 显式上下文 + F3B5/F380 哨兵 | `_closure_step_context`（per-step）；`dispatch_skill(path_context=)`；escalation genesis 哨兵；anchor-curate anchor ctx（条件注入） | closure.py:148-162（builder：2→arc、4/5→volume、6/10→chapter）+:191,288（两消费点）；dispatch_helper.py:1820+（dispatch_skill 签名）+:1859-1861（行注入）；revision_router.py:161-164（`chapter is None` → `PathContext(escalation="genesis")`）+:166（`dispatch_skill(..., path_context=path_ctx)`）；genesis.py:319（`PathContext(anchor=1) if step.skill == "shenbi-anchor-curate" else None`——条件注入非全局拼接）；tests/pipeline/test_closure_context.py（8 test） | ✅ 真实存在 |
| **R6** 中文节点/桥接/卷上下文共享提取器 | `read_chapter_node`/`BridgeRow`/`read_bridges`/`bridges_for_chapter`/`_volume_display_name` + 4 正则；三消费方委托；`_resolve_volume_at_runtime` 真实卷名 | _shared.py:196-208（4 正则）+:211,226,234,261,271（5 接口）；chapter_loop.py:2211-2214、plan_skeleton.py:196-206、context_assemble.py:246,252（三消费方全部委托 `_shared`）；_shared.py:186（`_volume_display_name(...) or f"Volume {i}"` 回落链）；tests/pipeline/test_cn_extract.py（10 test）+ tests/unit/pipeline/test_shared_cn_extractors.py（11 test） | ✅ 真实存在 |
| **F340** REJECT 重做语义 | `_apply_reject_redo`/`_reset_retry_budget`；DERIVED_TRUTH_MAP PER_CHAPTER 条目；feedback 条件式入队；REJECT 唯一调用点 | cli.py:544（`_apply_reject_redo`）+:524（`_reset_retry_budget`）+:66-72（PER_CHAPTER→chapter-revision 条目）+:563,565（分支内 `_queue_re_dispatches(feedback=)`/`_reset_retry_budget`）+:138（`if fb:` 条件拼接，无字面 None 渲染）+:604（cmd_review REJECT 内唯一调用，clear_checkpoint 前）；tests/pipeline/test_review_reject.py（10 test） | ✅ 真实存在 |
| **F341** --auto 并行 staging 提交 | `_auto_settle_parallel`（全守卫体镜像：提交+清 staging、无 checkpoint）+ 并行分支调用点替换 | chapter_loop.py:896-919（函数：review_required→False / staging 提交+clear_staging / 返回 bool）+:2727-2735（并行分支调用：auto 已提交→完成判定镜像；review_required→保留原 STATE_SETTLE checkpoint 路径 :2736-2744） | ✅ 真实存在 |
| **F304** RetryExhaustedError 捕获 | `_orchestrate_to_checkpoint` 整体 try 包裹 → ESCALATION checkpoint（带章号供 REJECT 重置） | cli.py:165（函数）+:172-176（F304 docstring+import）+:313-325（`except RetryExhaustedError` → `log.error("retry_budget_exhausted_escalation")` → `set_checkpoint(ESCALATION, context=…)`，esc_chapter 取 current_chapter 防 `_reset_retry_budget` no-op） | ✅ 真实存在 |

**PR #42 判定：修复真实存在（9/9 面，无部分、无消失）。** 附注：spec Status 行记录"1 处超 plan 修复（F304↔F340 交互）"即上述 esc_chapter 增量，与代码一致。

---

## 三、第 20 条专项：F860 .hypothesis 17 失败样本静态核验

**声称**（findings-ledger.md:635）: ".hypothesis 实际 11 个 0 字节文件 + 12 patch 共 17 个已发现失败（任务称 44 样本不符），17 个失败全部已修复"；Z7-d.md:13 给出 4 族修法：①`\r`/`\ud800` → blacklist；②`''`/`'主'` → 空串守卫；③`[1,2]` → P50 改 `values[n//2]`；④`['引入','升级','转折']` → 熵上界 +1e-4 容差。

**静态核验（禁 pytest，重放替代）**:

1. **失败样本计数**: `grep -h "via('discovered failure')" .hypothesis/patches/*.patch | wc -l` = **17** ✅ 与声称精确吻合。逐例枚举：`content='\r'`×6、`content='\ud800'`×2（capability_fs roundtrip）、`vs=[1,2]`×4（percentile）、`text=''`×2、`text='主'`×2（tokenize）、`patterns=['引入','升级','转折']`×1（entropy）= 17。patch 目标文件恰为 4 个（cjk/test_tokenize_frozen.py、gates/test_capability_fs_properties.py、stats/test_percentile_properties.py、stats/test_entropy_properties.py）。
2. **修复签名在场（HEAD）**:
   - ①blacklist：tests/property/gates/test_capability_fs_properties.py:**16** `st.characters(blacklist_characters="\r\n", blacklist_categories=("Cs",))` ✅
   - ③P50：tests/property/stats/test_percentile_properties.py:19-21（docstring 明记"旧 bug：P50 用 int(n*0.50)-1，median 用 n//2…修复后二者一致"）✅
   - ④熵容差：tests/property/stats/test_entropy_properties.py:46,51 `assert entropy <= math.log2(k) + 1e-4` ✅
   - ②空串守卫：tests/property/cjk/test_tokenize_frozen.py:87-88 `if not text.strip(): return` ✅（覆盖 `text=''`）
3. **修复引入与存续**: `git log -S` 四族签名全部由 `76662a5`（PR #8）引入；其后（含 PR #42）无任何提交移除（ac46663..HEAD 对 tests/ 0 提交）。
4. **计数勘误**: patches 目录现为 **13 个** patch（全部 mtime 2026-06-30，即审计前已存在），F860 文本称"12 patch"——勘误见 T1003。

**判定**: ✅ 修复确认（静态可证 4 族签名 + 17 计数闭合）。`text='主'` 单例的守卫归属不可静态归因（见 §五）。

---

## 四、新 Findings 详情

### T1001 | D1 行号订正在归档 spec 中再次漂移（持续，且较上轮恶化） | 部分回归（修复动作在、修复效果失效） | P2

- **声称内容**（第 9 条）: sdd spec-deviations D1 "✅ 已修（spec 文本订正）"，归档 spec `2026-08-01-inference-control-audit-design.md` 含订正行号。
- **核验命令与输出**: `grep -n "1387-1393\|1522-1524\|1410-1411\|1532-1533" docs/superpowers/specs/archive/2026-08-01-inference-control-audit-design.md` → :64,:127 仍载订正值；对照当前 `grep -n "chat.completions.create\|temperature=_get_skill_temperature\|streaming_early_stop\|api_dispatch_early_stop" src/shenbi/pipeline/dispatch_helper.py`。
- **当前状态**（订正值 → 实际，偏离）:

| 归档 spec 引用 | 订正值 | 当前实际 | 偏离 | 上轮偏离 |
|---|---|---|---|---|
| §2.3 `client.chat.completions.create`（spec:64） | :1387-1393 | **:1412** | +19~+25 | +6 |
| §2.3 kwargs 注入 temperature/max_tokens/timeout（spec:64） | :1522-1524 | **:1565-1567** | +43 | +22 |
| §2.9 streaming stop_reason log（spec:127） | :1410-1411 | **:1444-1445** | +34 | +21 |
| §2.9 `api_dispatch_early_stop` log（spec:127） | :1532-1533 | **:1575-1576** | +43 | +22 |

- **判定依据**: PR #42 对 dispatch_helper.py +61 行（R4b/R5 接线）使行号第三次移位；归档 spec 未随 PR #42 再订正。语义仍可按函数名定位（4 处同在 `_call_llm_streaming`/`_dispatch_via_api` 内），非功能回归；但该修复的初衷即行号准确，修复效果已两轮失效且漂移幅度扩大（+22 → +43）。维持 P2，建议改符号引用或加"基于 commit 快照"声明（与上轮建议一致，未被执行）。

### T1002 | INDEX:91 torch "待 #3 follow-up" 注记过期 | 修复确认（已解决） | （原 M，闭环）

- **核验**: `grep -n "torch" docs/superpowers/specs/INDEX.md` = **0 命中**；`git log -S "待 #3 follow-up" -- specs/INDEX.md` 显示该注记由 `b4e8696`（#27）写入、由 **`ac46663`（PR #42）** 移除（PR #42 重建 INDEX 时删除整个 #6 条目及过期注记）。当前 #3 条目（INDEX.md:131-140）依赖字段无 torch/embeddings 残留。
- **判定**: 上轮 finding 已解决（顺带修复，非专门修复）。归档 spec `2026-08-02-pr20-followup-…-design.md` 仍为 follow-up 交付的权威记录（第 4 条本体 ✅）。

### T1003 | F860 台账文本"12 patch"与磁盘 13 patch 不符（17 失败计数无误） | 文档漂移（台账勘误） | M

- **声称内容**（第 20 条）: findings-ledger.md:635 F860 "12 patch 共 17 个已发现失败"。
- **核验命令与输出**: `ls .hypothesis/patches/*.patch | wc -l` = **13**；`grep -h "via('discovered failure')" .hypothesis/patches/*.patch | wc -l` = **17**；`ls -la .hypothesis/patches/` 全部 mtime 2026-06-30（审计前状态，排除"审计后新增"解释）。
- **判定依据**: 核心量（17 失败、全部已修复）经静态核验准确（§三）；仅 patch 文件计数 12 vs 13 为台账笔误/清点误差（Z7-d.md:13 同文亦称 12）。修复本体不受影响。建议台账勘误为"13 patch（含 17 个 discovered failure）"。另注：F860/F861 所称"11 个 0 字节 example 文件"为当时快照，现 examples/ 为 11 个非空 hash 目录（.gitignore 内运行时 DB，随 dev 运行演化，非回归信号）。

---

## 五、不可静态核验清单

1. **`text='主'` 失败样本的守卫归属（第 20 条子例）**: tokenize 两属性测试中，`if not text.strip(): return`（test_tokenize_frozen.py:87）仅解释 `text=''`；`'主'` 的历史失败需 jieba 初始化时序（tokenize 现有 `jieba.initialize()` 幂等调用 + `if w.strip()` 过滤，cjk.py:93-104）或 strip 守卫的组合解释，无法由静态读码唯一归因。**需要的运行时步骤**（本轮禁令外）: `uv run pytest tests/property/cjk/test_tokenize_frozen.py -p no:cacheprovider`（G3.4 独立子代理口径）重放确认 2 个 `@example('主')` 均通过。其余 15 例（`\r`×6、`\ud800`×2、`[1,2]`×4、`''`×2、熵×1）修复签名静态可证。
2. **PR #42 行为级验收（H1 边界集等）**: spec Status 行声称"post-merge just check EXIT=0（2887+4 passed，85.34%）"与"H1 `{15,35,55,75,100}`"——本轮只核验了修复签名结构性在场（§二）与测试文件存在性（9 个新测试文件 72 个 test 函数），未运行测试重放。**需要的运行时步骤**: `uv run pytest tests/pipeline/ -v`（覆盖率污染禁令解除后的 CI 轮）。签名层面无任何消失迹象（ac46663..HEAD 零代码提交），回归风险极低。

---

## 六、修复确认简表（复核仍成立，不计新 finding）

| # | 修复 | 本轮证据锚点 |
|---|---|---|
| 1 | TokenLedger 接线 | dispatch_helper.py:1278/1310/1346 |
| 2 | finish_reason + cap-raise | dispatch_helper.py:82/1425-1431/1446/1586-1589/1593-1594/1639-1661 |
| 3 | truth_io upsert | dispatch_helper.py:1218-1219；chapter_loop.py:3055；pipeline/truth_io.py:54-69 |
| 4 | torch follow-up | uv.lock:3610-3611（2.13.0）；dependabot.yml:19；embeddings-smoke.yml:37-44 |
| 5 | cyclic-import | src/ `py/cyclic-import` 0 命中；audit/_shared.py:27 |
| 6 | silent CWD | gates/shared.py:58,73 |
| 7 | G0.16 + P2.5 | g0.py:627-646；AGENTS.md:70；decisions_validator.py:108；g2.py:76 |
| 8 | JSON 恢复 | dispatch_helper.py:839,847-851 |
| 10 | mkdocs 死链拦截 | pre-push-check.sh:44-48 |
| 11 | MIRROR_MAP 单一源 | g0.py:13；check_fixture_mirror.py:12；sha256 一致 |
| 12 | pip-audit dev 组 | pre-push-check.sh:37-39 |
| 13 | pro→flash | pro 0 命中；flash 4 文件 |
| 14 | RETRY_JITTER | pipeline/parallel_dispatch.py:31,36 |
| 15 | 定价 fail-loud | cost/pricing.py:40,52；cost/ledger.py:23,76 |
| 16 | renovate 移除 | renovate.json 不存在；workflows 0 引用 |
| 17 | max_tokens 32768 | executor_config.toml:12 |
| 18 | dict-form reads | chapter-drafting SKILL.md:9,17,22 |
| 19 | coverage.xml 重生成 | d1/coverage.xml line-rate=0.8728；d1-06 1448 行 |
| 20 | .hypothesis 17 失败已修 | 17 计数吻合 + 4 族签名（§三） |
| 21 | 独占 COVERAGE_FILE | d1-baseline.md:69 + coverage.xml 工件 |
| 22 | PR #42 全项 | §二 9/9 面 |
| — | T1002（上轮 M） | 注记已由 ac46663 移除（闭环） |

---

## 七、总结

- **核验声称总数**: 22/22（Z9-c §3 清单全覆盖；上轮 19 条 + 台账/自述 3 条）
- **回归（签名消失）**: **0**
- **部分回归**: 1（第 9 条 → T1001 持续且漂移扩大 +22→+43；非功能影响，纯归档文档行号）
- **修复确认**: 21（含第 22 条 PR #42 全项 R1-R6+F340/F341/F304 = 修复真实存在 9/9）；另有上轮 T1002 顺带闭环
- **新 findings**: 2 条 —— T1001（P2，持续/恶化）、T1003（M，F860 台账 patch 计数勘误 12→13）
- **不可静态核验**: 2 项（'主' 子例守卫归属；PR #42 行为级重放——均给出所需运行时步骤，签名层面无消失迹象）
- **PR #42 回归风险评级**: 无。`ac46663..HEAD` 19 个提交全部为 docs(audit)，src/skills/tests 零变更；全部修复签名在 HEAD 直接命中。
- **severity 分布（本轮新立）**: P2×1（T1001）、M×1（T1003）；无 P0/P1 回归。
