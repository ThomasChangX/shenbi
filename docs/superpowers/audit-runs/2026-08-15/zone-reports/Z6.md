# Z6 分区初审报告（records / trace / text / config / plugins / skill_utils）

- 轮次: 2026-08-15 全项目深度审查
- 分区: Z6（只读深读）
- 文件清单: docs/superpowers/audit-runs/2026-08-15/zones/Z6.files（49 文件，全部覆盖，无缺漏）
- findings 编号段: F601-F699（实际使用 F601-F628，共 28 条：P1×4，P2×13，M×11，P0×0）
- 覆盖缺口参照: docs/superpowers/audit-runs/2026-08-15/d1/d1-06-coverage-gaps.log

## 汇总

| 严重度 | 数量 | 编号 |
|---|---|---|
| P0 | 0 | — |
| P1 | 4 | F601, F602, F603, F604 |
| P2 | 13 | F605-F610, F612, F613, F615, F617-F619, F625 |
| M | 11 | F611, F614, F616, F620-F624, F626-F628 |

关键结论：
1. **trace 签名链健康**：`canonical_payload` 双侧均走 `datetime.isoformat()`，pydantic 序列化差异（`Z` 后缀）不影响重放校验——实证 round-trip 稳定（见 event.py 条目）。二次 compaction 链（pcs=None→1）与 writer 接续（next_seq=2）均实证正确。
2. **四个 P1 全部实证复现**：CJK 引号计数恒 0；dialogue 崩塌触发条件数学上永不命中；config 治理 float 绕过；linguistic drift 安全网生产死线（baseline 文件无人写）。
3. **print 裁决（F616）**：escalation/check.py:149 与 foreshadowing_recall/recall.py:61 的 `print()` 位于 CLI `main()`，stdout JSON 即产品输出，非框架运行时日志——判定不违反 AGENTS.md "No print() in framework code" 条款；但同仓 8 个兄弟 CLI 均用 `sys.stdout.write`，风格不一致记 M。

---

### src/shenbi/config/__init__.py
- 处置: deep-read
- 声称检查的不变量: [re-export 与 thresholds.py 一致；__all__ 完整]
- findings: 无
- 验证命令: 读文件比对 thresholds.py 导出（AUDIT_SAFETY_MATRIX / DEFAULT_THRESHOLDS / QualityThresholds 三者一致）；d1-06 覆盖 100%
- 置信度: high

### src/shenbi/config/config_coherence.py
- 处置: deep-read
- 声称检查的不变量: [关键审计维度禁用需 ≥50 字 rationale；floor 不得低于 revision trigger 60；每次变更写 JSONL 审计轨迹；并发 append 有锁]
- findings:
  - F603 | resonance_global_floor float 值绕过 Rule2 下限校验 | error | P1 | src/shenbi/config/config_coherence.py:121-125 | 根因: Rule2 用 `isinstance(new_value, int)` 判定，float（如 55.0）不命中 → 低于 60 的地板静默写入 genre-config.json，治理契约被绕过（G0 只校验 PipelineState 的 floor，不读 genre-config 的该键）。 | 验证: `uv run python -c "update_genre_config(d, {'resonance_global_floor': 55.0}, ...)"` → 无 ConfigError，写入 floor=55.0，audit trail 记录 new=55.0。 | 建议: `isinstance(new_value, (int, float)) and not isinstance(new_value, bool)`，或先 `float(new_value)` 归一再比较。
  - F605 | 审计轨迹先写后校验，失败时 trail 与配置文件不一致 | error | P2 | src/shenbi/config/config_coherence.py:132-133 | 根因: `_append_audit_trail` 在 changes 循环内逐条追加，后续 key 抛 ConfigError 时 genre-config.json 未写但 trail 已含前序条目 → trail 声称发生过从未落盘的变更。 | 验证: `changes={'auditDimensions.dialogue': False, 'resonance_global_floor': 55}` → ConfigError 抛出、genre-config.json 不存在、trail 已有 1 行 dialogue 条目。 | 建议: 两阶段——先全量校验所有 changes，再统一 `_set_nested` + 落盘 + 补 trail。
- 附注: `_load_config` 用 `assert isinstance(...)`（-O 下剥离）；`_set_nested` 遇非 dict 中间节点抛裸 TypeError 而非 ConfigError（M 级）。
- 覆盖缺口处置: 51（配置文件缺失 → `{}`）must-test；61（非 dict 中间节点）must-test（配合 F603 补 float 用例）。
- 验证命令: 见 F603/F605 内嵌命令（均已实际运行）；`grep -rn update_genre_config` → 仅 tests/unit/config/test_config_coherence.py 调用（生产无调用方，治理工具为人工/技能入口，可接受）
- 置信度: high

### src/shenbi/config/thresholds.py
- 处置: deep-read
- 声称检查的不变量: [阈值单一信源；dataclass frozen；AUDIT_SAFETY_MATRIX 与 G0 消费一致]
- findings: 无
- 验证命令: `grep -rn DEFAULT_THRESHOLDS src/` → state.py:72（`resonance_global_floor: int = DEFAULT_THRESHOLDS.resonance_global_floor`）、g0_config_coherence.py:27 等均引用同一常量，E11 单一信源契约成立；引用的 spec `2026-07-19-04-configuration-coherence-and-threshold-governance-design.md` 存在于 docs/superpowers/specs/archive/（无漂移）；d1-06 覆盖 100%
- 置信度: high

### src/shenbi/plugins/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包标记]
- findings: 无
- 验证命令: 读文件（1 行 docstring）；覆盖 100%
- 置信度: high

### src/shenbi/plugins/generate.py
- 处置: deep-read
- 声称检查的不变量: [master.json 前置结构校验（必填字段）；输出经 safe_write 原子写；幂等（同输入同输出）；未知 format 报错退出 1]
- findings:
  - F627 | gen_codex 对 platform fields 缺 marketplace/type 时抛裸 KeyError | error | M | src/shenbi/plugins/generate.py:50-51 | 根因: `load_master` 只校验顶层字段，`fields["marketplace"]`/`fields["type"]` 无校验，坏 master.json → KeyError 而非带上下文的 ValueError。 | 验证: 读代码（未运行——运行会写仓库文件，违反只读禁令）。 | 建议: load_master 增加 platforms.*.fields 结构校验。
- 附注: `REPO_ROOT = parents[3]` 仅在源码树/可编辑安装下成立（wheel 安装会 FileNotFoundError，对 repo-local 生成器可接受）；`output_path.relative_to(REPO_ROOT)` 在 output 逃逸根时抛 ValueError（M 级）。
- 覆盖缺口处置: 80（非 json 输出分支 `safe_write(output_path, content)`）dead-code——当前仅注册 json 生成器，为扩展点，acceptable。
- 验证命令: `grep -rln gen_codex tests/` → tests/unit/test_plugins_generate.py 存在（97% 覆盖）；读 plugins/master.json 确认 platforms.codex.fields 含 marketplace/type；`.codex-plugin/` 目录不存在（产物未入库，幂等由 safe_write+确定性内容保证）
- 置信度: high

### src/shenbi/records/__init__.py
- 处置: deep-read
- 声称检查的不变量: [re-export 与 parser.py 一致]
- findings: 无
- 验证命令: 读文件比对；覆盖 100%
- 置信度: high

### src/shenbi/records/drift.py
- 处置: deep-read
- 声称检查的不变量: [YAML 权威，md 表为派生视图；数值感知比较（0.80 vs 0.8）；无表/空表 → {}]
- findings:
  - F606 | 单向检测：YAML 有而 markdown 表缺行不报 drift | error | P2 | src/shenbi/records/drift.py:82-92 | 根因: `detect_cross_section_drift` 只遍历 `md_rows.items()`；YAML 记录缺失于派生表时（活跃伏笔丢失一行）不产生 issue，与模块 docstring「派生表必须与 YAML 一致；不一致即 drift」矛盾——丢失行恰是最常见的真实漂移形态。 | 验证: 读代码 + tests/unit/records/test_drift.py 无 YAML-only-missing 用例；生产消费方 audit/write_audit.py:56 直接透传。 | 建议: 补 `by_id.keys() - md_rows.keys()` 差集报告（状态过滤后）。
  - F607 | 重复 id 静默覆盖（键唯一性无保障） | error | P2 | src/shenbi/records/drift.py:80 | 根因: `{str(r.get("id")): r for r in yaml_records}` 后者覆盖前者；parser.py 也不去重 → 重复 id 的记录漂移检测基于被覆盖视图静默进行。 | 验证: 读代码（未运行独立用例，字典覆盖语义确定）。 | 建议: 解析或检测层对重复 id 报 issue。
- 附注: `_values_equal(None, "")` 会报 drift（YAML 缺键 vs md 空单元格）——真实 fixture 未触发，M 级边界。
- 覆盖缺口处置: 34->54 / 48->47 / 52->34 分支 partial——空表、无表头路径已有测试（test_drift.py:21 用真实 fixture），acceptable；建议补 F606/F607 用例。
- 验证命令: `grep -rn detect_cross_section_drift` → src/shenbi/audit/write_audit.py:15,56 + tests/unit/records/test_drift.py；fixture tests/fixtures/truth-pending_hooks.md 表头 8 列与 `_MD_HEADER_TO_KEY` 逐列核对一致
- 置信度: high

### src/shenbi/records/parser.py
- 处置: deep-read
- 声称检查的不变量: [YAML 块截取停在下一个 ## 标题；空块 → []；语义 round-trip：parse(serialize(parse(x)))==parse(x)；sort_keys 规范序列化]
- findings:
  - F623 | `_parse_body` 静默丢弃非 dict 元素 | error | M | src/shenbi/records/parser.py:45 | 根因: `[r for r in data if isinstance(r, dict)]` 无告警过滤——坏记录（如纯字符串项）被静默剔除，N 计数缩水无痕迹。 | 验证: 读代码。 | 建议: 至少计数或 warn。
- 附注: `extract_yaml_block` 的 `m.end() + 1` 在 `\s*$` MULTILINE 回溯语义下对 0/1/2 空行、CRLF 均正确跳到正文（逐案推演 + 真实 fixture 通过）；`yaml.safe_load` 异常正常上抛不吞。
- 覆盖缺口处置: 42（空文档 → None → []）、44（非 list → ValueError）must-test——两条防御路径均未测。
- 验证命令: tests/unit/records/test_parser.py + test_golden_parse.py 存在（90% 覆盖）；fixture round-trip 由 golden 测试保障
- 置信度: high

### src/shenbi/skill_utils/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包标记]
- findings: 无
- 验证命令: 读文件（空包）；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/calibration/__init__.py
- 处置: deep-read
- 声称检查的不变量: [re-export 完整（HitRate/calibrate_confidence/main）]
- findings: 无
- 验证命令: 读文件比对 confidence.py；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/calibration/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口转发 main]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable——两行约定式 runner，与 7 个兄弟包同构；CLI 本体由 confidence.main 覆盖测试缺口（confidence.py 100%，但 main() 的 argparse 路径实际由 test_calibration.py 覆盖）。
- 验证命令: 读文件
- 置信度: high

### src/shenbi/skill_utils/calibration/confidence.py
- 处置: deep-read
- 声称检查的不变量: [只降级不升级（high→mid when hit-rate<threshold）；纯函数无副作用；CLI 输出 JSON]
- findings: 无
- 验证命令: 读代码（分支完整：reported!="high" 或 hr>=threshold 原样返回）；tests/unit/skill_utils/test_calibration.py + test_confidence_routing_integration.py 存在；skills/shenbi-review-resonance/SKILL.md:64 引用该 CLI（文档↔代码一致）；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/chapter_pattern/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包标记]
- findings: 无
- 验证命令: 读文件（空包）；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/chapter_pattern/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable（同上约定式 runner）。
- 验证命令: 读文件
- 置信度: high

### src/shenbi/skill_utils/chapter_pattern/compute_pattern.py
- 处置: deep-read
- 声称检查的不变量: [熵 H=-Σp·log₂p 且窗口取最近 N 章；连续 run 计算正确（含末尾 flush）；分布覆盖窗口阈值；全部确定性无状态]
- findings:
  - F621 | 章节列表混入非 dict 元素时裸崩 AttributeError | error | M | src/shenbi/skill_utils/chapter_pattern/compute_pattern.py:170 | 根因: `c.get("pattern", ...)` 假设全为 dict；`'[{"num":1,"pattern":"引入"},"引入"]'` → `'str' object has no attribute 'get'`。 | 验证: `echo '[{"num":1,"pattern":"引入"},"引入"]' | uv run python -m shenbi.skill_utils.chapter_pattern` → AttributeError traceback。 | 建议: isinstance 过滤或错误信息含索引。
- 正确性核验: `compute_consecutive` 末尾 flush 正确；`compute_entropy` 空列表 → (0.0,[])；`classify_entropy` 边界（h==阈值落入低档、h==0.0 → 严重单调）逐条推演正确；`check_distribution` `len(patterns) < recent_n` → None；主流程 `chapters[-w]` 由 `n >= w` 守护；空输入 `[]` 实测输出正常（ratio 0 守护 `n > 0`）。
- 覆盖缺口处置: 165（文件路径参数）must-test——docstring 记载的主用法之一未测；169（dict 包装 `data.get("chapters")` 分支）must-test；178-188/192（窗口分支 partial）acceptable（核心路径已测）。
- 验证命令: 上述 echo 实测（空列表 + 坏元素两例）；tests/unit/skill_utils/test_compute_pattern.py 存在（93%）
- 置信度: high

### src/shenbi/skill_utils/drift_detection/__init__.py
- 处置: deep-read
- 声称检查的不变量: [re-export 与子模块签名一致]
- findings: 无
- 验证命令: 读文件比对（10 个导出与 baseline/compute_drift 定义一致）；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/drift_detection/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable；注意 main() 本体（compute_drift.py:236-286）大部分由 test_drift_detection.py 覆盖。
- 验证命令: 读文件；skills/shenbi-drift-guidance/SKILL.md:51-58 引用该 CLI
- 置信度: high

### src/shenbi/skill_utils/drift_detection/baseline.py
- 处置: deep-read
- 声称检查的不变量: [基线 = 前 N 章五指标平均；total_chars 求和；经 safe_write 落盘 style/linguistic_baseline.json；无章节文件 → FileNotFoundError]
- findings:
  - F604 | establish_baseline 零生产调用者 → 语言学漂移安全网整体死线 | error | P1 | src/shenbi/skill_utils/drift_detection/baseline.py:78（唯一 writer）vs src/shenbi/pipeline/chapter_loop.py:2042（唯一 reader） | 根因: docstring 声称「Wire-in point: called from _run_context_assembly or the chapter loop after chapter 3 completes」，但全仓库 grep 无任何调用（仅 __init__ re-export 与 docs 归档）；chapter_loop `_check_linguistic_drift` 读 `style/linguistic_baseline.json`，文件不存在时 log warning 后 `return None` → spec §3.4 的 WARN/HARD/ESCALATE 三层干预在生产永不激活，安全网静默失效。 | 验证: `grep -rn establish_baseline --include=*.py --include=*.md --include=justfile /repo` → 仅包内 + docs/superpowers/{plans,specs,audit-runs} 归档；`grep -rn linguistic_baseline src/` → writer 仅 baseline.py:78，reader 仅 chapter_loop.py:2042（路径一致但无写入方）。 | 建议: 在章 3 完成钩子接线 `establish_baseline(project_dir, [1,2,3])`，或明确弃用并删除。
- 正确性附注: 五指标平均、round(4)、total_chars 求和逻辑本身正确；空 chapters 列表 → FileNotFoundError 分支正确。
- 覆盖缺口处置: 44-85（整个函数体，19% 覆盖）must-test——若按 F604 建议接线，必须先补基线计算/落盘/FileNotFoundError 三类用例；若判 dead-code 删除则随 F604 一并处置。
- 验证命令: 见 F604 内嵌 grep（已运行）
- 置信度: high

### src/shenbi/skill_utils/drift_detection/compute_drift.py
- 处置: deep-read
- 声称检查的不变量: [3 点平滑（边界 2 点）；触发 (a) ≥3 连续非排除章且平滑累计降 ≥3；触发 (b) 低于均值-2σ ≥2 连续且样本 ≥6；卷级连续下滑；human_overridden 排除；CLI findings 非空 exit 1]
- findings:
  - F628 | main --write-audit-drift 写死相对路径 truth/audit_drift.md（依赖 CWD） | error | M | src/shenbi/skill_utils/drift_detection/compute_drift.py:284 | 根因: `_append_audit(findings, Path("truth/audit_drift.md"))` 相对于进程 CWD 而非 --resonance 所在项目根；skill 指令在项目根运行时成立，但 --resonance 传绝对/子目录路径时 audit 写错位置。 | 验证: 读代码（未运行写文件命令——违反只读禁令）。 | 建议: 由 resonance 路径推导父目录。
- 正确性核验（实测）: `smooth([1,3])` → [2.0,2.0]（n=2 边界正确）；`detect_chapter_drift([90,89,88,86])` 平滑后累计降 2.5<3 → 不触发（符合公式：平滑压缩端点降幅，spec-conformant）；`detect_chapter_drift([90,89,88,88,86,85])` → 正确触发 "chapters 1-5"；`detect_volume_drift([80,75])→1 / [75,80]→0 / [80,80]→0`（末两卷一步下滑即触发，与 spec §8.3「卷级连续下滑」的实现解读一致——若 spec 意为两次连续下滑则过敏感，见 positive-quality-gates-design §8.3 表述，无反例，不另立 finding）；排除索引重置（run/start/prev 归零）逻辑推演正确。
- 覆盖缺口处置: 156-158（check_linguistic_drift_trigger 的 None/非漂移分支）must-test；174->182/183（parse_trend 无匹配 header → 空 dict）must-test；176/178/191/194/197（_split_row/_try_float 分支）acceptable；204（CLI stdout 循环）acceptable；262->272/267/273->280（main 文件存在性分支）acceptable。
- 验证命令: 上述 uv run python 实测（smooth/chapter/volume 三组）；tests/unit/skill_utils/test_drift_detection.py + test_volume_drift_macro.py + test_drift_triggers_integration.py 存在
- 置信度: high

### src/shenbi/skill_utils/drift_detection/linguistic_drift.py
- 处置: deep-read
- 声称检查的不变量: [五指标 per-mille 归一；drift 阈值 >500% 偏差或 dialogue 崩塌 <20%；severity 由绝对 system_term_density 驱动（30/50/100）；config 驱动词表（无 config 用 bootstrap）；纯确定性]
- findings:
  - F602 | dialogue 崩塌触发永不命中（5.0 不大于 5.0） | error | P1 | src/shenbi/skill_utils/drift_detection/linguistic_drift.py:214-218 | 根因: dialogue_ratio<0.2 时 `max_deviation_ratio = max(max_deviation_ratio, 5.0)`，而 `is_drift = max_deviation_ratio > 5.0` 严格大于 → dialogue 崩塌路径产出的哨值 5.0 恒不满足；density 类真实偏差需 ratio>5.0 不受影响，但文档承诺的「dialogue density collapses to <20% of baseline」触发是死代码。 | 验证: `detect_drift(curr={'dialogue_density':1.0,...}, base={'dialogue_density':10.0,...})` → `is_drift=False, severity=NONE, deviations['dialogue_density']=0.1`。 | 建议: 哨值改 5.1，或 is_drift 条件对 dialogue 单独判 `dialogue_ratio < 0.2`。
  - F617 | 双基线体系三重不一致（路径/量纲/词表） | error | P2 | src/shenbi/skill_utils/drift_detection/linguistic_drift.py:276-311 vs baseline.py:62-83 | 根因: `_load_baseline` 写 `context/linguistic_baseline.json`（0-1 分数制、指标名 dialogue_ratio、硬编码词表 `系统|面板|等级|技能|属性|经验`），而 establish_baseline 写 `style/linguistic_baseline.json`（per-mille、指标名 dialogue_density、config 驱动词表）——同一概念两套互不兼容实现；且 `check_linguistic_drift`（消费前者）仅 tests/pipeline/test_linguistic_drift.py 调用，生产走 chapter_loop 包装（消费后者）。 | 验证: grep 两个路径的读写方（见 F604 命令）；读代码比对量纲。 | 建议: 删除 `_load_baseline`/`check_linguistic_drift` 旧体系或统一到 config 驱动版本。
  - F618 | 零基线单次出现即触发 drift（ratio 强制 6.0） | error | P2 | src/shenbi/skill_utils/drift_detection/linguistic_drift.py:202 | 根因: `base_val==0 且 curr>0` → ratio=6.0>5.0 → is_drift；基线无破折号而当前章出现 1 个 "——"（0.33‰）即 WARN 注入纠偏指令——零基线的最小信号无降噪。 | 验证: 读代码分支；detect_drift 实测确认 ratio 语义。 | 建议: 加最小绝对密度或最小计数门槛。
  - F625 | check_linguistic_drift 对话 regex 只匹配 ASCII 直引号，CJK 弯引号恒 0 | error | P2 | src/shenbi/skill_utils/drift_detection/linguistic_drift.py:298,354 | 根因: `re.finditer(r'["].+?["»]', text)` 开闭均为 ASCII `"`——中文小说标准引号 “”（U+201C/D）完全不计入 dialogue_chars → 章>10 且全文弯引号对话时 dialogue_ratio≈0，恒触发 "Dialogue density near zero -- possible character disappearance" 误报。同文件 compute_linguistic_metrics:120-122 用 \u201c/\u201d 正确统计——同模块两套引号语义。 | 验证: 读代码比对 120-122 vs 298/354；该函数仅测试调用（生产影响受 F617 限制）。 | 建议: 复用 \u201c/\u201d 计数。
- 正确性附注: `frequency_divergence_alarms` 的 sigma 语义实际是倍率（`baseline_rate*(1+sigma)`）而非标准差——docstring 自认是近似且标注 WARN-only（CJK 长尾告诫在 docstring:148-153），acceptable；`check_window_redundancy` O(w²·SequenceMatcher) 窗口 4 → 可接受。
- 覆盖缺口处置: 74,77-78（config 读取 JSONDecodeError/OSError 容错）must-test；168,178（detect_drift 分支）acceptable；225,227（WARN/ESCALATE severity 分支）must-test——F602 的 dialogue 死分支恰在 225 附近，正是无测试掩盖了 P1；261,280-281（_load_baseline 新建路径）结合 F617/F604 处置；304->308（chapters_read>0 分支）must-test；354-357（dialogue 近零告警）must-test（F625 用例）。
- 验证命令: detect_drift 实测两组（见 F602/F618 验证）；tests/unit/skill_utils/drift_detection/test_linguistic_drift.py + tests/pipeline/test_linguistic_drift.py + tests/unit/pipeline/test_drift_intervention.py 存在
- 置信度: high

### src/shenbi/skill_utils/escalation/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包标记]
- findings: 无
- 验证命令: 读文件；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/escalation/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable。
- 验证命令: 读文件
- 置信度: high

### src/shenbi/skill_utils/escalation/check.py
- 处置: deep-read
- 声称检查的不变量: [斜率 = 线性回归最小二乘（点/章）；样本 <window 不触发；六触发器确定性；库函数纯]
- findings:
  - F616 | main() 使用 print()（CLI 输出 vs AGENTS.md 框架禁令的边界裁决） | 文案 | M | src/shenbi/skill_utils/escalation/check.py:149 | 裁决与理由: `print()` 位于 CLI `main()`，输出的 JSON 数组即工具产品（供 skill 指令/人捕获），不是框架运行时日志——AGENTS.md「No print() in framework code; use structlog」针对框架代码的日志通道，CLI 入口的 stdout 是数据通道，判**不违规**；但同仓 calibration/routing/compute_drift/match_tropes/compute_stats/revision_routing 六个兄弟 CLI 均用 `sys.stdout.write`，风格不一致（ruff select 无 "T" 规则，CI 不拦）。 | 验证: `grep -n '"T"' pyproject.toml` → select 列表无 T 族；grep 兄弟 CLI 确认 sys.stdout.write 风格。 | 建议: 统一 sys.stdout.write。
- 正确性核验: 回归斜率公式（x_mean=(n-1)/2、分母 Σ(i-x̄)²）标准无误；边界 slope 恰为 -2.0 时不触发（严格 <，spec §6.2 语义）；库函数 check_escalation 无状态；生产消费方 chapter_loop.py:80,1029 按真实签名调用（与 2026-07-19-13 plan 固定的签名一致，无漂移）。
- 覆盖缺口处置: 48（denominator==0 → False，window=1 理论分支）acceptable；125-149（main() CLI 整体）must-test——argparse/布尔解析路径零覆盖。
- 验证命令: grep 调用方（chapter_loop.py:1029）；tests/unit/skill_utils/test_escalation.py 存在（库函数 80%）
- 置信度: high

### src/shenbi/skill_utils/foreshadowing_recall/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包标记]
- findings: 无
- 验证命令: 读文件；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/foreshadowing_recall/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable。
- 验证命令: 读文件
- 置信度: high

### src/shenbi/skill_utils/foreshadowing_recall/recall.py
- 处置: deep-read
- 声称检查的不变量: [过期判定纯算术 (current-last_reinforced)>max_distance；排除 RESOLVED；缺 last_reinforced/max_distance 跳过；确定性]
- findings:
  - F616（共用） | main() 使用 print() | M | recall.py:61 | 裁决同上：CLI stdout 是产品输出，不违反 AGENTS.md 框架禁令；风格与兄弟 CLI 不一致。 | 建议: sys.stdout.write。
  - F622 | escalation/foreshadowing_recall CLI 无任何 skill/生产引用 | dead-wire | M | src/shenbi/skill_utils/foreshadowing_recall/recall.py:17-20 + escalation/check.py:9-14 | 根因: 两个模块 docstring 声称 CLI 用法，但 grep skills/ 无 `python -m shenbi.skill_utils.escalation|foreshadowing_recall` 引用（escalation 库函数由 chapter_loop 消费；recall 库函数仅测试消费）——CLI 是文档化接口无消费者。 | 验证: grep 命令见验证段。 | 建议: 确认保留（人工调试入口）或接入 SKILL.md 指令。
- 正确性附注: `hook["id"]` 在有 last_reinforced/max_distance 但缺 id 时抛 KeyError（schema 声明 id 必填，M 级）；空列表/全 RESOLVED/缺字段实测语义正确。
- 覆盖缺口处置: 53-61（main() 整体）must-test——JSON 解析与输出路径零覆盖。
- 验证命令: `grep -rn "skill_utils.escalation\|skill_utils.foreshadowing_recall" skills/ docs/` → 仅 docs/plans 归档提及，无 skill 指令引用；tests/unit/skill_utils/test_foreshadowing_recall.py 覆盖库函数（含空输入/边界）
- 置信度: high

### src/shenbi/skill_utils/review_resonance/__init__.py
- 处置: deep-read
- 声称检查的不变量: [re-export 完整（BORDERLINE_BAND/MAX_AUTO_REVISIONS/RevisionLoop/Routing/route_block/main）]
- findings: 无
- 验证命令: 读文件比对 routing.py；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/review_resonance/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable；skills/shenbi-review-resonance/SKILL.md:63 引用该 CLI（文档一致）。
- 验证命令: 读文件 + grep SKILL.md
- 置信度: high

### src/shenbi/skill_utils/review_resonance/routing.py
- 处置: deep-read
- 声称检查的不变量: [PASS 需 overall≥threshold 且全地板过；±5 边界带 → 人工；非 high 校准置信 → 人工；2 次自动修订上限；纯函数]
- findings: 无
- 正确性核验: `_decide` 分支全枚举推演——(overall≥T, floor 破, |Δ|≤5)→HUMAN（边界带）；(overall≥T+5, floor 破, high, 未达上限)→AUTO_REVISE（地板破坏按失败处理，与 docstring「breached floor near threshold 才人工」一致）；confidence=="low" 与 "非 high 非 low"（mid/异常值）均 HUMAN；prior≥2 → HUMAN。`route_block` 的 floors 空 Mapping → floor_ok=True（vacuous pass，合理边界）。near_threshold 在 _decide 与 route_block 双算一致。
- 覆盖缺口处置: 181-183（CLI --floor 解析 `spec.split("=")`）must-test——`DIM=SCORE:FLOOR` 解析路径未测（缺 "=" 或 ":" 时 ValueError 裸抛，M 级）。
- 验证命令: 读代码分支枚举；tests/unit/skill_utils/test_routing.py 存在（94%）；与 calibration 集成由 test_confidence_routing_integration.py 覆盖
- 置信度: high

### src/shenbi/skill_utils/revision_routing/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包标记]
- findings: 无
- 验证命令: 读文件；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/revision_routing/__main__.py
- 处置: deep-read
- 声称检查的不变量: [--diagnosis JSON → {"mode": ...} stdout]
- findings:
  - F613 | main() 15 行整体 0% 覆盖 | test | P2 | 覆盖日志 d1-06: src/shenbi/skill_utils/revision_routing/__main__.py 0% lines 3-17 | 根因: CLI 入口无任何测试（库函数 route.py 100%，但 JSON 解析→路由→序列化链无冒烟）——与其他 __main__ 的两行 runner 不同，此文件含实质 main() 逻辑。 | 验证: d1-06 覆盖行 + ls tests/unit/skill_utils/（test_revision_routing.py 测库函数）。 | 建议: 补 CLI 冒烟（含坏 JSON 报错路径）。
- 附注: 函数内 `import argparse` 与兄弟模块顶层 import 风格不一（M）。
- 验证命令: 读文件；生产消费方 revision_router.py:96-99 调库函数（签名一致）
- 置信度: high

### src/shenbi/skill_utils/revision_routing/preserve_check.py
- 处置: deep-read
- 声称检查的不变量: [再生章节必须保留原章节全部 hooks_advanced/changes_realized/state_changes；set 差集方向正确（original-regen）；纯函数]
- findings:
  - F612 | docstring 声称的 CLI 用法不存在 | error/docs | P2 | src/shenbi/skill_utils/revision_routing/preserve_check.py:16-19 | 根因: docstring 写 `python -m shenbi.skill_utils.revision_routing.preserve_check --original ... --regenerated ...`，但模块无 main()/argparse/__main__ 守护——`python -m` 该模块仅完成 import 即退出，文档↔代码漂移误导调用方。 | 验证: 读文件全文确认无 CLI 入口；包 __main__.py 只有 --diagnosis。 | 建议: 删除文档用法或补 argparse 入口。
- 正确性核验: 差集方向正确（原有一再生缺 = 丢失）；changes_realized/state_changes 用 list 遍历 + set 成员检查（保序报告）；空输入 → (True, [])。
- 覆盖缺口处置: 100% 无缺口。
- 验证命令: tests/unit/skill_utils/test_revision_routing.py:96-153 四个用例覆盖
- 置信度: high

### src/shenbi/skill_utils/revision_routing/route.py
- 处置: deep-read
- 声称检查的不变量: [unmet_goal+BLOCKING → regenerate；叠加 craft → constrained-regenerate；仅 craft/空 → spot-fix；确定性]
- findings: 无
- 验证命令: 分支枚举（2×2 真值表与实现一致：TT→CONSTRAINED, T F→REGENERATE, F*→SPOT_FIX）；生产消费方 pipeline/revision_router.py:96-99；tests/unit/skill_utils/test_revision_routing.py 存在；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/style_learning/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包标记]
- findings: 无
- 验证命令: 读文件（空包）；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/style_learning/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable；skills/shenbi-style-learning/SKILL.md:51 引用该 CLI（文档一致，且 skill 声明禁 LLM 估算强制运行）。
- 验证命令: 读文件 + grep SKILL.md
- 置信度: high

### src/shenbi/skill_utils/style_learning/compute_stats.py
- 处置: deep-read
- 声称检查的不变量: [零 LLM 零随机；P50 与 median 同源；滑窗 TTR；n-gram 频次；输出经 safe_write]
- findings:
  - F609 | compute_punctuation 复刻 cjk.py 已修复的逐字计数 bug（——/…… 双倍）且引号缺 CJK 弯引号 | error | P2 | src/shenbi/skill_utils/style_learning/compute_stats.py:27,226 | 根因: `sum(text.count(c) for c in chars)` 逐字迭代多字符标点——一个 "——" 计 2、一个 "……" 计 2；「「」『』」逐字计（一对=2）；引号串为 ASCII 直引号（`"\"\"''「」『』"`），弯引号 “” 不计。text/cjk.py:58-70 的修复注释明确记载这正是被修掉的旧 bug——两个标点统计实现冲突，style-learning 的密度基线系统性失真。 | 验证: 读代码 + `uv run python -c` 确认两文件均无 U+201C/U+201D 字节；与 cjk.py 修复注释比对。 | 建议: compute_punctuation 改用 `shenbi.text.count_punctuation`（token 整体计数）。
  - F610 | read_chapters 裸 except Exception 静默吞读错误 | error | P2 | src/shenbi/skill_utils/style_learning/compute_stats.py:320-326 | 根因: 目录/文件两处 `except Exception: continue`——编码错误或权限问题的章节被静默剔出样本，统计基于缩水样本无任何 stderr 痕迹（违反 AGENTS.md「无裸 except 吞错」精神，虽然该条针对框架代码，此处为 skill CLI，但后果相同：静默数据丢失）。 | 验证: 读代码。 | 建议: 至少 stderr 计数告警。
  - F611 | RHETORICAL 正则死代码 + TRANSITION_WORDS 重复项 | error/dead-code | M | src/shenbi/skill_utils/style_learning/compute_stats.py:37-41,54 | 根因: RHETORICAL 三条编译正则无任何引用（detect_rhetoric 自带独立实现）；TRANSITION_WORDS 含两个 "与此同时"（dict 覆盖，不双计，仅冗余）。 | 验证: grep RHETORICAL 仅定义处。 | 建议: 删除或接线。
- 正确性核验: `compute_percentiles` P50 与 median 同源（n//2，含修复注释）；空值守护（{} 返回）；`compute_ttr` total<1000 → 无窗口回落 global；`compute_ngrams` O(N)；`segment_sentences` 末尾 flush 正确；--output 经 safe_write。复杂度：detect_rhetoric 反复检测 3 个 phrase_len × O(N) dict 累积，对 67 章×3000+ 字可接受（线性主导）。
- 覆盖缺口处置: 148（compute_paragraph_stats 空输入分支附近）acceptable；320-321/325-326（read_chapters except 容错）must-test——配合 F610；380-381（main stderr 分支）must-test（no input/no readable files 两条退出路径）。
- 验证命令: tests/unit/skill_utils/test_compute_stats.py 存在（96%）；grep RHETORICAL/TRANSITION_WORDS 引用计数
- 置信度: high

### src/shenbi/skill_utils/trope_detection/__init__.py
- 处置: deep-read
- 声称检查的不变量: [re-export 完整（Trope/count_trope_hits/load_trope_inventory/main/trope_overuse）]
- findings: 无
- 验证命令: 读文件比对 match_tropes.py；覆盖 100%
- 置信度: high

### src/shenbi/skill_utils/trope_detection/__main__.py
- 处置: deep-read
- 声称检查的不变量: [python -m 入口]
- findings: 无
- 覆盖缺口处置: 3-5（0%）acceptable（runner；main 本体 100%）。
- 验证命令: 读文件
- 置信度: high

### src/shenbi/skill_utils/trope_detection/match_tropes.py
- 处置: deep-read
- 声称检查的不变量: [子串匹配；一 beat 多签名只计一次；overuse 严格 >；inventory 缺 rewrite_hint 默认 ""；确定性]
- findings: 无
- 附注: 模块 docstring 自认是测试/fixture 代理（生产用 LLM 语义匹配）——与 tests/fixtures 引用一致（AGENTS.md G0.9 fixture 契约）。`match_all` 未入 __init__ 导出但被 main 使用（M 级不一致）。
- 覆盖缺口处置: 无（100%）。
- 验证命令: tests/unit/skill_utils/test_trope_detection.py 存在
- 置信度: high

### src/shenbi/text/__init__.py
- 处置: deep-read
- 声称检查的不变量: [re-export 与 cjk.py 一致；__all__ 完整]
- findings: 无
- 验证命令: 读文件比对；覆盖 100%
- 置信度: high

### src/shenbi/text/cjk.py
- 处置: deep-read
- 声称检查的不变量: [find_terms 精确子串匹配（CJK 每字符位置为合法边界）；count_punctuation 按整 token 计数；count_words 两模式；TermHit/Token frozen]
- findings:
  - F601 | 引号 token 只能匹配空引号对，真实引号恒计数 0 | error | P1 | src/shenbi/text/cjk.py:54 | 根因: 引号桶 tokens 为两字符序列 `'""'`/`"''"`/`'「」'`/`'『』'`——真实引用「你好」不含子串「」（开闭引号间有内容），ASCII 直引号对 `"你好"` 同理不含 `""`；且中文标准弯引号 “”（U+201C/D）完全不在表内。结果：任何真实文本的 引号 计数恒 0（只有空引号 `` 才能命中）。count_punctuation 是本模块对外的规范标点计数器，正常路径功能性错误。 | 验证: `uv run python -c "from shenbi.text.cjk import count_punctuation; print(count_punctuation('他说：“你好。”然后离开了。')['引号'])"` → 0；`'他说:"你好。"'` → 0；`count_punctuation('你好——世界')['破折号'] == 1`（破折号/省略号等其余桶正确）。 | 建议: 引号改为单字符计数（“ ” 「 」 『 』 ' " 各自计，或只计开引号），弯引号入表。
  - F615 | tokenize 污染 jieba 全局词典（隐藏跨调用状态） | error/纯函数性 | P2 | src/shenbi/text/cjk.py:100-104 | 根因: `jieba.add_word(term)` 修改进程级全局分词词典并持久到后续所有 tokenize 调用——同进程内结果依赖调用历史，破坏确定性助手纯函数契约（Z6 重点维度）。docstring 自认 known limitation（skeleton 债）。 | 验证: 读代码（jieba.add_word 全局语义确定）。 | 建议: 每次构造 `jieba.Tokenizer()` 实例隔离。
- 正确性核验: find_terms 空 term 跳过、空文本 → []、重叠匹配 start=idx+1（实证测试齐全 test_cjk.py:8-39）；count_words mixed 排除标点只计 CJK+拉丁词；生产消费方 truth_index.py:325-326 与 g6.py:421 按签名调用一致。
- 覆盖缺口处置: 32（`if not term: continue` 空 term 跳过）must-test——一行用例即覆盖。
- 验证命令: 见 F601 实测；grep find_terms 消费方（truth_index/g6）
- 置信度: high

### src/shenbi/trace/__init__.py
- 处置: deep-read
- 声称检查的不变量: [12 个导出与子模块一致]
- findings: 无
- 验证命令: 读文件逐项比对（compact/verify_chain/GENESIS_PREV/TraceEvent/canonical_payload/sign/materialize_progress/migrate_from_progress/replay/CURRENT_VERSION/assert_monotonic/migrate_to_current/TraceWriter 全部存在）；覆盖 100%
- 置信度: high

### src/shenbi/trace/compaction.py
- 处置: deep-read
- 声称检查的不变量: [COMPACTION 头 seq=1/prev=GENESIS；payload 含 prev_compaction_seq/snapshot/truncated_at_seq；crash-safe（temp+fsync+replace+dir-fsync）；verify_chain 首条 None 合法、后续 pcs 链单调无缺口]
- findings:
  - F619 | compact 绕过 safe_write 的 flock 锁协议 | error/并发 | P2 | src/shenbi/trace/compaction.py:50-56 | 根因: safe_write 对同目录写用 flock+O_EXCL 互斥，compact 的 temp+os.replace 直接执行不取锁——并发 safe_write（其 trace 追加经 TraceWriter append 模式写同文件）与 compact 的整文件替换可交错丢事件。当前调度为顺序（spec 拓扑）故不可复现，但锁协议半覆盖是竞态温床。 | 验证: 读代码比对 safe_write.py:40-91 锁实现。 | 建议: compact 复用 _acquire_lock 或文档化独占运行前提。
- 正确性核验（实测）: 二次 compact 幂等链正确——compact1 后 events=[COMPACTION]，pcs=None，trunc=2；追加 POST(seq=2) 后 compact2 → pcs=1（前 COMPACTION 的 seq），trunc=2，verify_chain=[]，后续 TraceWriter next_seq=2 接续正确。verify_chain 首条 pcs 为 int 合法（第二次 compact 后的常态）。BaseException 清理 tmp 正确。
- 覆盖缺口处置: 30（二次 compact 的 prev_compaction_seq 赋值）must-test——我已手测通过但仓库无测试，这是长流水线的主路径；64-71（Windows dir-fsync OSError 容错）acceptable（平台分支）；85/87（pcs 非法类型）must-test（verify_chain 防御分支）。
- 验证命令: `uv run python` 双 compact 实验脚本（/tmp 临时目录，未触仓库）；tests/unit/trace/test_compaction.py 存在（79%）
- 置信度: high

### src/shenbi/trace/event.py
- 处置: deep-read
- 声称检查的不变量: [canonical_payload 排序键去顺序差异；signature 不入签名域；frozen 模型；sign_and_new 保证链前驱；序列化 round-trip 后签名校验仍通过（重放正确性根基）]
- findings: 无
- 正确性核验（实测，本分区最高风险项）: pydantic v2 将 ts 序列化为 `2026-08-15T05:46:21.546351Z`，但 canonical_payload 双侧（签名时与重放校验时）都经 `_json_default` 的 `datetime.isoformat()`（`+00:00` 后缀）→ round-trip 签名稳定：两链实测 `sign(prev, canonical_payload(reloaded), v)==reloaded.signature` 均 True。`_SIGNED_FIELDS` 不含 signature（无自引用）；schema_version 入签名域（版本混链可见）。extra="ignore" 容忍前向字段。
- 覆盖缺口处置: 44（`_json_default` 的 `str(obj)` 非 datetime 回退）acceptable——datetime 主路径已测，回退为防御。
- 验证命令: `uv run python -c`（sign→dump→reload→verify 两链实验，输出 True/True）；tests/unit/trace/test_event.py 存在（95%）
- 置信度: high

### src/shenbi/trace/materialize.py
- 处置: deep-read
- 声称检查的不变量: [progress.json 为 trace 派生视图；INIT/MARK_DONE 重放语义对齐 cmd_init；未标记技能三 phase pending；per-phase 队列（非 total-done）；经 safe_write 落盘并追加 MATERIALIZE trace 事件]
- findings:
  - F614 | "round" 字段在 project_dir 输入下恒为 "???"，日期名截断 | error | M | src/shenbi/trace/materialize.py:81 | 根因: `Path(round_dir).name.split("-")[1] if "round-" in str(round_dir) else "???"`——生产调用方（chapter_loop.py:697）传 state.project_dir（不含 "round-"）→ 恒 "???"；即便 round-2026-08-15 也截成 "2026"。 | 验证: `uv run python -c`（Path('/x/round-2026-08-15') → '2026'；'/x/my-novel-2026' → '???')。 | 建议: 直接用目录名或删字段。
  - F624 | subagent_completion_count 对重复 MARK_DONE 双计数 | error | M | src/shenbi/trace/materialize.py:53-63 | 根因: 每个 MARK_DONE 事件 done_counter+1，同一 (skill, test_type) 的重复事件（重试/重发）使计数虚高于真实完成数——与"completion"语义偏离。 | 验证: 读代码（计数无去重）。 | 建议: 以 (skill,tt) 集合基数计数。
- 正确性核验: `_pending` 与 genuinely_done 的三 phase 全完成语义正确；MARK_DONE 未知 test_type 仅扩 dict 不崩；INIT 缺字段回落参数默认；safe_write 传 round_dir+trace_action 追加 MATERIALIZE 事件（重放时被忽略，无递归）。
- 覆盖缺口处置: 96% 无行缺口；53->48/62->48 分支 partial acceptable。
- 验证命令: tests/unit/trace/test_materialize.py 存在；生产消费 chapter_loop.py:694-741 三处调用签名一致
- 置信度: high

### src/shenbi/trace/migrate.py
- 处置: deep-read
- 声称检查的不变量: [幂等（已有 LEGACY_MIGRATION 直接返回）；progress.json 缺失/坏 JSON → 空快照不崩；事件经 TraceWriter 链式追加；sha256 锚定原文件]
- findings: 无
- 附注: 若 trace 已有非 LEGACY 事件（如 INIT）仍会追加 LEGACY_MIGRATION（合理 bootstrap）；actor_role="GATE" 为 contracts/enums.py Literal 合法值（已核）。
- 覆盖缺口处置: 20->23/21->20 分支 partial acceptable；27-28（JSONDecodeError → snapshot={}）must-test——坏 progress.json 容错路径未测。
- 验证命令: tests/unit/trace/test_migrate.py 存在（85%）
- 置信度: high

### src/shenbi/trace/replay.py
- 处置: deep-read
- 声称检查的不变量: [撕裂/签名断链边界截断其后全部；keepends 精确累计字符（CRLF 安全）；空行保留计入 keep_chars；截断经 safe_write（无 round_dir → 不追加 trace 事件，不污染链）]
- findings:
  - F620 | 截断静默无日志 | error/可观测性 | M | src/shenbi/trace/replay.py:38-48 | 根因: JSON 解析失败/签名不匹配 break 后直接 safe_write 截断——设计上有意为之（torn-line 恢复），但无 structlog 记录：生产中事件被丢弃时排障无线索（丢多少、从哪条起）。 | 验证: 读代码（无任何 log 调用）。 | 建议: 截断发生时 log.warning（丢弃行数/字符数/首坏行预览）。
- 正确性核验: safe_write 无 trace_action 不写 trace（读 safe_write.py:126 确认）→ 无链自毁；CRLF keepends 计数注释与实现一致。
- 覆盖缺口处置: 36-37（空行 keep_chars 分支）must-test——空行/纯换行 trace 文件路径未测。
- 验证命令: tests/unit/trace/test_replay.py 存在（93%）；/tmp 撕裂实验（见 F608）间接验证 replay 行为
- 置信度: high

### src/shenbi/trace/versioning.py
- 处置: deep-read
- 声称检查的不变量: [schema_version 单调非递减；未知更高版本 fail；MIGRATIONS 注册表；迁移到 CURRENT]
- findings:
  - F626 | migrate_to_current 对 schema_version<CURRENT 且无注册迁移时死循环（理论路径） | error | M | src/shenbi/trace/versioning.py:33-38 | 根因: while 循环内 `_identity` 不提升版本 → 若事件版本 <1（仅 model_construct 可绕过 Field(ge=1) 校验）则无限循环。当前 CURRENT=1 且校验保证 ≥1，不可达。 | 验证: 读代码（未运行——会挂起）。 | 建议: 迁移后版本不变即 raise。
- 覆盖缺口处置: 15（`_identity` 定义）与 36-37（循环体）dead-code——CURRENT=1 下恒等迁移不可达，acceptable（文档自认结构扩展点）。
- 验证命令: grep assert_monotonic → gates/g7_trace.py:50 消费（G7 校验接线确认）；tests/unit/trace/test_versioning.py 存在（87%）
- 置信度: high

### src/shenbi/trace/writer.py
- 处置: deep-read
- 声称检查的不变量: [seq 从现有 trace 接续；每条链前一条签名；append 后 fsync；首建 fsync 父目录；非线程/进程安全（已文档化）]
- findings:
  - F608 | torn-tail 时 TraceWriter 构造抛裸 JSONDecodeError | error | P2 | src/shenbi/trace/writer.py:57 | 根因: `_last_sig_existing` 对最后一行直接 `json.loads`——崩溃残留的撕裂行（截断 JSON）使下一次 TraceWriter 构造抛未捕获 `json.decoder.JSONDecodeError`（不在框架异常层级），与 replay 的优雅截断设计不一致。safe_write.py:129-140 已预期此失败并吞掉（"trace append can crash if trace.jsonl has a torn tail"）→ 后果是 torn 后所有 safe_write 的 trace 追加静默失败（仅 WARN 日志），直到某次 replay/materialize 截断修复；而直接构造方（如 migrate）则裸崩。 | 验证: /tmp 实验——写 2 事件、截去尾部 12 字节、`TraceWriter(d)` → `json.decoder.JSONDecodeError: Unterminated string`。 | 建议: `_last_sig_existing` 复用 replay 容错（撕裂则回落 GENESIS/触发截断），或抛框架异常。
- 正确性核验: compact 后 next_seq=2 接续正确（实测）；`_count_existing` 非空行计数与 head seq=1 对齐；并发限制已在模块 docstring 声明（顺序调度下安全）。
- 覆盖缺口处置: 30-31/34-35（_fsync_dir 双层 OSError 容错，Windows/网络 FS）acceptable（平台分支）；56（空文件存在 → GENESIS_PREV）must-test。
- 验证命令: /tmp 撕裂实验（F608）；tests/unit/trace/test_writer.py 存在（90%）
- 置信度: high

---

## 覆盖统计

- 清单文件 49 / 报告条目 49（100%，无缺漏）
- findings：28 条（P0=0，P1=4，P2=13，M=11）
- 实际运行的验证命令：约 20 组（含 3 组 /tmp 临时目录实验——trace 撕裂/双 compact/签名 round-trip，均未触仓库文件；2 组 CLI echo 实测 chapter_pattern；多组 grep/读文件交叉验证）
- 未运行（只读禁令）：pytest、shenbi-validate、shenbi-dispatch、pipeline、plugins 生成器（会写 .codex-plugin/）

## 低置信度文件列表

无（全部 high）。个别行号级覆盖缺口的归属（如 compute_pattern 178-188 的具体分支）基于读码推断 + 覆盖日志行号交叉，不影响 finding 本身。

## 未覆盖文件列表

无（49/49 全覆盖）。
