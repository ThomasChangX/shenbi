# Z3 区初审报告 — src/shenbi/pipeline/（34 文件，2026-08-15 轮）

审查人：Z3 分区初审 agent（只读深读）。编号段 F301–F399。
覆盖缺口基线：docs/superpowers/audit-runs/2026-08-15/d1/d1-06-coverage-gaps.log。
重要背景：2026-08-14 的 5 份 live design specs（cost-ledger / gate-effectiveness / drift-chain / decisions-chain / p2-batch）已记录本区多个缺陷并提出了修复设计，但截至本轮（代码 mtime 2026-08-15 10:00）代码尚未实现——这些是"已立案未结案"的开放缺陷，本轮复核确认其仍在代码中，并补充了若干新发现（F303/F304/F306/F310/F325 等在本区首次系统记录）。

---

## findings 总表

| 编号 | 标题 | 类别 | 严重度 | 证据 |
|---|---|---|---|---|
| F301 | TokenLedger 系统性少计：并行审计波/并行 post-draft/genesis/closure/triggers 派发均不传 state | error | P0 | parallel_dispatch.py:86-91; chapter_loop.py:2432-2445; genesis.py:321; closure.py:294; triggers.py:542; dispatch_helper.py:1310-1311 |
| F302 | TokenLedger chapter 维度恒 0（state 无 chapter 属性） | error | P1 | dispatch_helper.py:1347-1348 |
| F303 | audit 级联跳过 dead-wire：_get_audit_history 扁平格式与 _should_skip_audit 分组格式不兼容 + 章号字符串排序 | error | P1 | chapter_loop.py:367-390, 2567-2571 |
| F304 | 默认（并行）流中共振分永不解析：resonance floor 检查失效、resonance_trend.md 永不更新 | error | P1 | chapter_loop.py:3030-3058（死分支）vs 2541-2656（默认并行路径）; revision_router.py:109-117 |
| F305 | 并行审计波零 G3/G4：requires_independent 技能（review-resonance）在默认流不跑 G3，审计输出无结构校验 | error | P1 | chapter_loop.py:2541-2656; write_safety.py:44-46 |
| F306 | 快照 ring-buffer 全文备份永不命中：零填充模式与非填充章节文件名不匹配，修订回滚无法恢复章节 | error | P1 | snapshot_diff.py:112-115 |
| F307 | DriftEscalationError 被吞：ESCALATE 级漂移的 pause-for-review 被 except Exception 降级为 warning | error | P1 | chapter_loop.py:2060-2066, 2757-2765 |
| F308 | `<` 转义 no-op：`replace("<", "\u003c")` 恒等替换，防 `</document>` 注入防御为死代码 | security | P1 | dispatch_helper.py:747 |
| F309 | 并行审计失败静默放行：重试耗尽后无 escalation，blocking_found 基于 stdout 行首扫描（API 路径 stdout 为 JSON → 恒 False），未审计章节被标记 complete | error | P1 | parallel_dispatch.py:210-221; chapter_loop.py:2636-2642 |
| F310 | SCR 缓存无失效：章节修订后 extract_scr 返回修订前缓存（注释声称 "fresh"） | error | P1 | scr_extractor.py:428-433; chapter_loop.py:3072-3075 |
| F311 | curated 文档章节备忘错位到 P7 且 P1 显示"未产出"；且 curated 文件无任何 skill 消费者（step-3 写入为死输出） | error | P2 | context_curation.py:143-154, 453-464; skills/shenbi-chapter-drafting/SKILL.md:14-15 |
| F312 | audit_context_cache 双路径错误：零填充章节名 + truth/volume_map.md（正确为 outline/） | error | P2 | audit_context_cache.py:49, 71-74 |
| F313 | _maybe_rebuild_truth_index 重建后不持久化：truth-index.json 停留在 genesis 版本 | optimization | P2 | chapter_loop.py:836-844 vs genesis.py:126-129 |
| F314 | volume_align.py 整模块死代码（step-1 仅标记完成不调用） | deps | P2 | volume_align.py:52-74; chapter_loop.py:2755-2769 |
| F315 | CONDITIONAL_STEPS 死表；_should_run_recall/_should_run_drift/_check_conditional_resolve 生产调用点全部死亡 | deps | P2 | chapter_loop.py:266-290, 2957-2958, 3085-3088 |
| F316 | state.py compact_pipeline_state/_archive_chapter_state 无任何调用者 | deps | P2 | state.py:383-428 |
| F317 | state_heal._heal_last_snapshot 只识别 legacy 平面快照，不识别默认差分快照目录 | error | P2 | state_heal.py:80-95 |
| F318 | atexit 累积注册 + 紧急清 staging 可丢弃未 commit 的 staged 输出 | error | P2 | crash_recovery.py:66, 143-151; chapter_loop.py:2534-2536 |
| F319 | review_checklist 静态/动态 split 段死代码 + 硬编码 stub + 零填充 plan 路径 | deps | P2 | review_checklist.py:464-568 |
| F320 | run_gate_g3 缺 progress.json 时伪造 scorer 身份使 G3 可通过（gate 完整性洞） | security | P2 | dispatch_helper.py:1977-1990 |
| F321 | 串行审计/共振分支默认流不可达（死代码漂移风险，含 F304 根因） | deps | P2 | chapter_loop.py:2957-3058 |
| F322 | chapter-drafting 声明读 context-decisions.json 但生产者（context-composing）已被移除 | deps | P2 | skills/shenbi-chapter-drafting/SKILL.md:15; chapter_loop.py:1228-1255 |
| F323 | cli MODIFY 语义冲突：commit staging 后又回退 step_index 重派发，LLM 重生成覆盖人工编辑 | error | P2 | cli.py:592-622 |
| F324 | cli backfill-context 使用 print() 违反 AGENTS.md no-print / stdout-JSON-only 约定 | error | P2 | cli.py:945-947 |
| F325 | _verify_truth_integrity 返回值被忽略（fail-fast 未接线） | deps | P2 | cli.py:788-791 |
| F326 | cmd_chapters / _get_audit_history 章号字符串排序（1,10,11,2） | error | P2 | cli.py:879; chapter_loop.py:375 |
| F327 | cmd_init TOCTOU：state_file.exists() 在锁外检查后才取写锁 | error | P2 | cli.py:400-479 |
| F328 | truth_embed.embed_and_store 每条目重载 SentenceTransformer + EmbeddingStore 不 close | optimization | P2 | truth_embed.py:119-127; genesis.py:157-176 |
| F329 | _write_parsed_outputs literal 路径回退：整段 stdout 写入目标文件（模型路径漂移时垃圾写入） | error | P2 | dispatch_helper.py:1172-1175 |
| F330 | _budgeted_truncate 预算不再分配（短文件余量不给被截断文件） | optimization | P2 | dispatch_helper.py:294-317 |
| F331 | live specs（2026-08-14）file:line 引用漂移 >5 行（8-15 代码改动后未回填） | deps | P2 | docs/superpowers/specs/2026-08-14-*.md vs 当前行号 |
| F332 | genesis auto-mode G4-continue 路径完全未测试 | error | P2 | genesis.py:346-360（coverage 349-359） |
| F333 | scr_extractor 事件行号恒 1、POV 行号伪造（i*2） | error | M | scr_extractor.py:168-183, 265-286 |
| F334 | dispatch timeout docstring "base=300s" vs 代码 900 | docs | M | dispatch_helper.py:101-107 |
| F335 | _AUDIT_MIN_BYTES=200 vs 注释 ">500 bytes" | docs | M | llm_output_integrity.py:53-55 |
| F336 | closure 失败 escalation 传 chapter=0（"chapter 0"）而非 None（genesis 语义） | docs | M | cli.py:298-302 |
| F337 | truth_index main() CLI 零测试；run_gate_g4 files 逗号 join 对含逗号文件名有歧义 | docs | M | truth_index.py:335-370; dispatch_helper.py:1946 |
| F338 | machine.clear_checkpoint 无 NONE 防御：空 checkpoint 也记入 history | error | M | machine.py:76-84 |

---

### src/shenbi/pipeline/__init__.py
- 处置: deep-read
- 声称检查的不变量: 包 docstring-only，无逻辑；不引入循环导入副作用
- findings: 无
- 验证命令: `wc -l`（1 行）；coverage 100%
- 置信度: high

### src/shenbi/pipeline/_shared.py
- 处置: deep-read
- 声称检查的不变量: 中文卷界解析正确性（空文件/缺标题/重复卷号/英文形态优先级）；read_volume_boundaries 三级 fallback；update_total_chapters 单写点；bridges 聚合排除续作行；read_chapter_node 不匹配桥接表行
- findings:
  - F339 | _resolve_volume_at_runtime 双重读盘：read_volume_boundaries 内部已读 volume_map.md，随后又 read_text 一次（每章 context 装配调用） | optimization | P2 | _shared.py:176-180 | 热路径每章 2 次全文件读 | 未验证（静态读码） | 合并单次读取返回 (text, boundaries)
- 正确性核验：`_CN_VOL_HEAD_RE` 要求冒号；section 切至下一 `##`；每卷只取首条卷级范围行（KR 级 `- **章节范围**` 被行首锚排除）；重复卷号 set 语义安全；英文 `_END_RE` 优先、`_RANGE_RE` 次之、中文最后——与 tests/pipeline/test_volume_map_cn.py 的真实 fixture 测试一致。`_CN_NODE_ROW_RE_TMPL` 的 `第\s*{ch}\s*章` 不存在 12→2 误匹配（`第` 后必须紧跟目标数字）。`bridges_for_chapter` 窗口语义 `chapter >= activation - 3` 正确。`read_total_chapters` 对 bool/float total 宽容（int(bool)），可接受。
- 覆盖缺口处置（83->77, 101, 133-134, 152, 252-255, 279）: 101=project_dir 空 ValueError → must-test（一行）；133-134/152=JSON 解码失败路径 → acceptable（与 252-255 bridge WARN 同类防御日志）；279=`_volume_display_name` 越界 None → must-test；252-255=非数字 activation WARN → acceptable
- 验证命令: `uv run python -c` 验证 `resolve_chapter_path('chapters/chapter-N.md',5)` → 'chapters/chapter-5.md'（确认非填充约定）
- 置信度: high

### src/shenbi/pipeline/audit_context_cache.py
- 处置: deep-read
- 声称检查的不变量: 每章构建一次共享审计上下文；字段截断上限；estimated_tokens 估算
- findings:
  - F312 | 双路径错误：`chapters/chapter-{chapter:03d}.md`（零填充，实际为非填充 chapter-N.md）→ chapter_text 恒空；`truth/volume_map.md`（实际在 outline/）→ volume_context 恒空 | error | P2 | audit_context_cache.py:49, 71-74 | 章节文件命名约定漂移；volume_map 路径写错目录 | `uv run python -c "from shenbi.contracts.paths import resolve_chapter_path; print(resolve_chapter_path('chapters/chapter-N.md',5))"` → `chapters/chapter-5.md`（非零填充） | 改为 `chapter-{chapter}.md` 与 `outline/volume_map.md`；注意 chapter_text 目前仅用于 estimated_tokens（dispatch_helper 只注入 4 个缓存字段，chapter_text 不注入），影响为 token 估算偏低 + 死字段
- 覆盖缺口处置（55-103 大部分缺失）: 现有测试只覆盖空目录路径 → 这正是 F312 未被发现的原因；修复时 must-test（带真实文件的路径断言）
- 验证命令: 静态读码 + 上条 resolve_chapter_path 输出
- 置信度: high

### src/shenbi/pipeline/audit_layer.py
- 处置: deep-read
- 声称检查的不变量: genre 圈激活矩阵 camelCase 键；boundary 触发器周期；dispatch/G4 失败保守置 blocking_found=True
- findings: 无新发现（其保守失败处理与并行波 F309 形成对照，佐证并行路径缺陷）。注意 run_audit_layer 仅在串行分支被调（见 F321），默认流不可达。
- 覆盖缺口处置（131）: `_issue` detail 参数分支 → acceptable
- 验证命令: 未运行（纯读码；签名与 chapter_loop.py:2969 调用一致）
- 置信度: high

### src/shenbi/pipeline/chapter_loop.py
- 处置: deep-read（全 3090 行）
- 声称检查的不变量: 16 步状态机推进/检查点/重试预算；并行波与串行路径等价性；staging 生命周期；REJECT 重做游标；SoftFail 升级接线；快照/清单维护
- findings:
  - F303 | audit 级联（Spec 8 Fix 8）dead-wire：`_get_audit_history` 返回扁平记录 `{"skill":..., "chapter":..., ...}`，而 `_should_skip_audit` 按 `chapter_results.get(skill)` 消费按章分组字典——生产接线（2567-2571）下 `.get(skill)` 恒 None → 永不跳过。叠加 375 行 `sorted(...items())` 对字符串键排序（"10"<"2"） | error | P1 | chapter_loop.py:355-358, 375, 2567-2571 | 两函数数据模型不一致；测试各自单测（test_audit_cascading.py 用分组格式直测 _should_skip_audit）掩盖了集成断裂 | `uv run python -c`（构造 3 章干净 streak 的 state）：`_should_skip_audit('dialogue', _get_audit_history(state,4))` → **False**；同数据手工分组 → **True**；history 章序输出 `[10, 11, 2]` | 统一数据模型（在 _get_audit_history 内按章分组），排序改 `key=lambda kv: int(kv[0])`，并补 wiring 集成测试
  - F304 | 默认（并行）流中共振分永不解析：`cs.resonance_score = _parse_resonance_score(...)` 仅存在于串行 `"review-resonance" in step.skill` 分支（3030-3058），而并行波在 `_FIRST_AUDIT_IDX` 拦截并直接跳过全部审计步；`_route_revision_after_resonance` 读到的 resonance_score 恒 None → `check_resonance(None)=True` 恒过（revision_router.py:115-117）→ §6.3 resonance floor 从不触发；resonance_trend.md 永不更新 → escalation_bridge.parse_resonance_scores 数据源枯竭 | error | P1 | chapter_loop.py:2541-2656, 3030-3058; revision_router.py:109-117 | 并行重构（Plan 18）未迁移共振解析 | 静态读码 + grep 确认 `_parse_resonance_score` 赋值仅 3034 一处 | 在并行波 consolidate 后解析 audits/chapter-N-resonance.md 并执行 trend upsert
  - F305 | 并行审计波零 G3/G4：波内 6 个核心审计 + genre 审计的输出不做 run_gate_g4 / run_gate_g3；`requires_independent("shenbi-review-resonance")=True` 但并行路径从不调用 G3 → 违反 AGENTS.md "Scoring MUST use an independent subagent (G3.4)" | error | P1 | chapter_loop.py:2541-2656（无任何 gate 调用）vs 串行 2875/2946 | 并行重构遗漏 gate 段 | `uv run python -c "requires_independent('shenbi-review-resonance')"` → **True**；tests/unit/pipeline/test_chapter_loop_full.py:234 注释自认 "G3 is not called in the parallel-dispatch audit path" | 波结束后对全部审计输出批量跑 G4，对 requires_independent 技能跑 G3
  - F307 | DriftEscalationError 被 2757-2765 的 `except Exception` 捕获降级为 `linguistic_drift_check_failed` warning——2060-2066 的 ESCALATE "Pipeline paused for human review" 语义失效 | error | P1 | chapter_loop.py:2757-2765, 2060-2066 | 非阻塞注释与函数内 raise 矛盾（drift-chain-design.md:19 已立案） | `uv run python -c`：`_run_chapter_step_impl` 源码含 `except Exception` + `linguistic_drift_check_failed` → **True** | except 子句排除 DriftEscalationError（`except DriftEscalationError: raise` 前置）或让其转为 ESCALATION checkpoint
  - F309 | 并行审计失败静默放行：`_dispatch_with_retry` 耗尽后 consolidate 仅统计 `Failed: N`，chapter_loop 无 escalation/无步骤重试；`cs.audit_results["blocking_found"] = "## BLOCKING Issues" in consolidated` 而 consolidate 扫描 `result.stdout` 行首 BLOCKING——API 路径 stdout 是原始 JSON（json_mode），行首 BLOCKING 不可能出现 → 恒 False；审计文件缺失时 collect_audit_issues 也无 issue → NO_REVISION → 未审计章节被标记 complete | error | P1 | parallel_dispatch.py:99-105, 210-221; chapter_loop.py:2623-2647 | 失败处理未对齐串行路径（串行 _handle_failure 会 escalation） | 静态读码（对照 _dispatch_via_api 返回 DispatchResult(True,0,output_text,"")，output_text 为 JSON） | 波结果含失败任务时置 blocking_found=True 并 raise ESCALATION（对齐 audit_layer.run_audit_layer:171-174 的保守语义）
  - F315 | CONDITIONAL_STEPS（intent-management/drift-guidance/snapshot-manage）死表：定义后无任何消费者；`_should_run_step` 对这些 skill 的门控永不触发；`"foreshadowing-track" in step.skill`（2957）与 `shenbi-foreshadowing-recall`（3085）匹配的 skill 已不在 CHAPTER_STEPS → `_check_conditional_resolve`/`_update_last_recall_manifest` 生产调用点死亡（仅测试直调） | deps | P2 | chapter_loop.py:266-290, 2957-2958, 3085-3088 | 步骤表重构（20→16 步）后遗留 | `grep -rn CONDITIONAL_STEPS src/ tests/` → 仅定义处 1 条命中 | 删除或重新接线（TRIGGERED hook 解析目前完全依赖 lifecycle skill 自身）
  - F321 | 串行审计圈/audit-layer 阻塞重试/共振解析段（2957-3058）默认流不可达：并行波在 `_FIRST_AUDIT_IDX` 拦截后 step_index 直跳 `_LAST_AUDIT_IDX+1`，中间值从不持久化 | deps | P2 | chapter_loop.py:2541-2656 vs 2961-3058 | 同 F304/F305 根因 | 静态控制流分析 | 串行分支要么删除要么作为 resume 兜底显式测试
  - F313 | `_maybe_rebuild_truth_index` 只 build_index 不写 truth-index.json（genesis._update_indexes 有 safe_write）→ 周期重建结果被丢弃，索引停留 genesis 版本 | optimization | P2 | chapter_loop.py:836-844 vs genesis.py:126-129 | 写盘调用遗漏 | 未验证（静态读码） | 补 safe_write(project_dir/"truth-index.json", index.to_json())
  - F318 | `register_emergency_handlers` 在每个 chapter step_idx==0 重复 atexit.register → 句柄随章数线性累积；且 `_emergency_cleanup` 第 4 步 clear_staging 会丢弃已 staged 未 checkpoint 的输出（2724-2726 注释自认 data-loss trap，仅在 auto-settle 路径规避） | error | P2 | chapter_loop.py:2534-2536; crash_recovery.py:66, 143-151 | 幂等注册缺失；清理顺序未区分 staging 状态 | 未验证（静态读码） | 注册前置 flag 防重；cleanup 仅清理确认无 pending staging 的目录
- 覆盖缺口处置（275 行缺失，重点）: 693-699（progress 物化）→ acceptable（best-effort）；718-743（auto rebuild）→ acceptable；865-889（world freshness + auto settle）→ must-test（F341 auto-settle 分支有数据丢失风险历史）；1117-1132（serial STATE_SETTLE auto commit）→ acceptable（与 865-889 同构）；1673-1739（legacy snapshot 路径）→ acceptable（migration 安全网）；2039-2086（drift ESCALATE/HARD/WARN）→ must-test（F307 修复后）；2523-2531（shutdown checkpoint）→ must-test；2672-2679（settling 失败 pause）→ must-test；2889-2911（soft-fail tracker）→ acceptable（有 test_soft_fail_escalation）；2919-2941（auto-mode G4 硬失败 continue）→ must-test；3013-3027（audit 100 硬帽）→ acceptable（不可达路径，见 F321）；3032-3058（串行共振）→ 与 F304 一并处理
- 验证命令: 见 F303/F305 各条内嵌输出；另 `grep -rn "_check_conditional_resolve\|_should_run_recall" src/` → 仅测试命中
- 置信度: high

### src/shenbi/pipeline/checkpoint.py
- 处置: deep-read
- 声称检查的不变量: staging→final 提交原子性；reject 清空；缺失文件报 FileNotFoundError
- findings: 无（路径参数 target_path 均来自内部常量（cli.py:356/363、chapter_loop resolve_chapter_path），LLM 不可直接注入；`_resolve_all_wildcards` 的 `[^/]*` 模式阻断 `../` 穿越——安全面核验通过）
- 覆盖缺口处置（50-52）: OSError 重抛分支 → acceptable（有日志）
- 验证命令: 未运行（静态读码）
- 置信度: high

### src/shenbi/pipeline/cli.py
- 处置: deep-read（全 1007 行）
- 声称检查的不变量: 读写锁包裹全部状态变更；REJECT 重做游标正确性；MODIFY 派生 truth 再同步；closure 失败 escalation；RetryExhaustedError→checkpoint
- findings:
  - F323 | MODIFY 语义冲突：`_commit_staging_for_checkpoint` 先把 staged 计划提交到最终路径，随后又回退 step_index=1 重新派发 chapter-planning——LLM 重生成 + approve 后 commit 会覆盖人工对 plans/ 的直接编辑；与 `_queue_re_dispatches` docstring（"re-dispatch derived truth … reflect the human edit"，spec §9.2）意图相悖 | error | P2 | cli.py:592-622 | "modify=带反馈重做"与"modify=保留人工编辑、只同步派生"两种语义同时实现 | 未验证（静态读码；行为推演） | 二选一：保留人工编辑则不回退主 skill 游标（仅队列派生同步），或文档化 modify=regenerate-with-feedback
  - F324 | cmd_backfill_context 使用 print() 到 stdout，违反 AGENTS.md "No print() in framework code" 与本文件头 "machine-readable output goes to stdout via emit_json" | error | P2 | cli.py:945, 947 | 规约违反 | `grep -n "print(" src/shenbi/pipeline/cli.py` → 945,947 两条 | 改 structlog 或 emit_json
  - F325 | `_verify_truth_integrity` 返回 missing 列表但 cmd_resume 丢弃返回值——"fail fast" 设计未接线，仅日志 | deps | P2 | cli.py:788-791 | 接线遗漏 | 未验证（静态读码） | 至少将 missing 并入 resume 输出或 BLOCKED 状态
  - F326 | cmd_chapters 对字符串章号排序 → 章列表顺序 1,10,11,…,2 | error | P2 | cli.py:879 | 与 F303 同根 | 未验证（静态读码） | `key=lambda kv: int(kv[0])`
  - F327 | cmd_init TOCTOU：state_file.exists() 在 ReadLock 内但 save 在另一个 WriteLock 中，两个并发 init 可双写 | error | P2 | cli.py:400-479 | 检查与提交不同锁 | 未验证 | init 全程持 WriteLock
  - F336 | closure 失败 escalation 传 chapter=0 → dispatch_escalation 生成 "chapter 0" 上下文而非 genesis 语义（None） | docs | M | cli.py:298-302 | 参数语义混淆 | 未验证 | 传 None
- 正确性核验：`_reset_retry_budget`（REJECT-ESCALATION 时清 retry_counts+retry_budget_consumed，带章前缀）正确补上了 machine.clear_checkpoint 不清预算的缺口；RetryExhaustedError 捕获带章（313-328）正确；closure 失败→escalation（291-308）路径完整；VOLUME_BOUNDARY approve 后延迟快照 + 越界转 closure（808-833）逻辑自洽。
- 覆盖缺口处置（105 行缺失）: 112-148（re-dispatch 执行）→ must-test（MODIFY 传播核心）；231-270（触发器失败 escalation）→ must-test；296-311（closure 失败 escalation）→ must-test；775-786/812-833（resume 边界快照）→ must-test；922-949（backfill）→ acceptable（CLI 工具）
- 验证命令: `grep -n "print("` → 945,947
- 置信度: high

### src/shenbi/pipeline/closure.py
- 处置: deep-read
- 声称检查的不变量: 10 步序列；step-9 后 BOOK_CLOSURE checkpoint 先于 step-10；G3 for requires_g3；重试→escalation 由 cli 兜底
- findings: 无新发现（cli.py:291-308 已兜底 `_handle_closure_failure` 耗尽后的 escalation；`_gate_passed` 含 SKIP 宽容与 genesis 不一致但为有意设计——目录型输出）。`while True` 重试循环上限由 handle_dispatch_failure(max_revision_retries) 保证，无失控风险。
- 覆盖缺口处置（133->135, 266, 312-314）: 266=project_dir 空 ValueError → acceptable；312-314=G3 失败重试分支 → must-test
- 验证命令: 未运行（静态读码；与 cli/triggers 签名核对一致）
- 置信度: high

### src/shenbi/pipeline/context_assemble.py
- 处置: deep-read
- 声称检查的不变量: 三路检索 + 权重 rerank + 内容哈希去重 + 角色预算；Route B 降级标记
- findings: 无（`_route_a` 条目为指针型文本属设计使然；预算 first-fit 停止可接受）。`_load_volume_context` 依赖 `_resolve_volume_at_runtime` 运行时解析，无硬编码卷界——核验通过。
- 覆盖缺口处置（150, 159-165, 302, 320, 362-390）: 362-390=main() CLI → acceptable（无测试 CLI 工具，但低于 truth_index 的 F337 因为非注册入口……实际同为注册入口，同标 must-test-lite）；159-165=Route B DB 缺失分支 → acceptable
- 验证命令: 未运行（静态读码）
- 置信度: high

### src/shenbi/pipeline/context_curation.py
- 处置: deep-read
- 声称检查的不变量: 9 段结构化；P1-P7 分层映射；结尾多样性 3 章窗；hook 债务双层表
- findings:
  - F311 | `_reorder_to_layered_format` 插入的章节备忘 Section(source="P1 章节备忘") 在 `_render_context_document` 的前缀匹配中无任何 `_PRIORITY_ORDER` 键命中 → 计划全文落入 P7 桶，P1 渲染"(未产出)"。且 grep 确认无任何 SKILL.md 消费 chapter-N-curated.md（drafting 读的是 chapter-N-context.md）→ 该步产出为死输出 | error | P2 | context_curation.py:143-154, 453-464; skills/shenbi-chapter-drafting/SKILL.md:14-15 | 桶键（"chapter-plan"）与插入源（"P1 章节备忘"）不匹配；死输出未被任何测试对消费链断言 | `uv run python -c`（构造带计划的 chapter-5）：输出 `P1 contains plan: False / P7 contains plan: True / P1 shows 未产出: True` | 修桶键映射（P1 source → bucket 1）；并决策 curated 文件的去留（接入消费或删步骤）
- 覆盖缺口处置（176, 284, 381-384, 404-432）: 381-384=spine YAML 异常 → acceptable；404-432=表行回退解析 → acceptable（防御）
- 验证命令: 见 F311 内嵌输出
- 置信度: high

### src/shenbi/pipeline/crash_recovery.py
- 处置: deep-read
- 声称检查的不变量: 信号 handler 仅置 flag；atexit 兜底；二次信号立即终止；测试隔离 reset
- findings:
  - F318（共享，见 chapter_loop 条目）: atexit 累积 + emergency 清 staging 的丢失面
- 覆盖缺口处置（88-89, 118-120, 149-151）: cleanup 各 try 段 → acceptable（best-effort 设计，test_crash_recovery 覆盖主干）
- 验证命令: 未运行（静态读码；tests/pipeline/test_crash_recovery.py 覆盖 emergency snapshot 路径）
- 置信度: high

### src/shenbi/pipeline/dispatch_helper.py
- 处置: deep-read（全 2031 行）
- 声称检查的不变量: 三路派发路由；JSON/markdown 双格式解析；写失败检测与预算截断；finish_reason 截断处理；token 记录；门子进程调用
- findings:
  - F301 | TokenLedger 系统性少计：`dispatch_skill(state=)` 仅 chapter_loop.py:2831 一处传入；审计波（parallel_dispatch.py:86-91 不传 state）、并行 post-draft（chapter_loop.py:2432-2445 不传）、genesis（genesis.py:321）、closure（closure.py:294）、triggers（triggers.py:542）、re-dispatch（cli.py:140）全部不传 → `_log_token_usage` 的 `if state:`（1310-1311）跳过 `_record_token_usage` → cost/token-ledger.jsonl 与 state.token_usage 漏记全书中占大头的审计/周期/创世调用 | error | P0 | dispatch_helper.py:1310-1311, 1344-1354; parallel_dispatch.py:86-91; chapter_loop.py:2432-2445; genesis.py:321; closure.py:294; triggers.py:542 | ReviewTask 不携带 state；各调用点未接线（2026-08-14-cost-ledger-design.md:7 已立案未实现） | `grep -n "state=" src/shenbi/pipeline/parallel_dispatch.py` → 无 state 参数；test_dispatch_helper_ledger.py 只单测 `_record_token_usage` 本身（SimpleNamespace state），未覆盖并行波 | ReviewTask 增加 state 字段并透传；各 dispatch 调用点补 state
  - F302 | chapter 维度恒 0：`getattr(state, "chapter", 0)`——PipelineState 无 chapter 字段（有 chapter_loop.current_chapter）→ 所有 ledger 行 chapter=0，by_chapter 聚合全坍缩 | error | P1 | dispatch_helper.py:1347-1348; state.py:162-183（无 chapter 字段） | 属性名臆断 | `uv run python -c`：`hasattr(PipelineState(...),'chapter')` → **False**；模拟记录后 ledger 行 `chapter: 0` | 改 `state.chapter_loop.current_chapter`
  - F308 | `<` 转义 no-op：`content.replace("<", "\u003c")` 中 `"\u003c"` 即字符 `<`，恒等替换——防 `</document>` 标签注入的防御完全失效（输入文件内容可闭合注入包裹标签） | security | P1 | dispatch_helper.py:744-748 | 转义应输出字面 `\u003c` 六字符（需 `"\\u003c"`） | `uv run python -c "print('hello <document> world'.replace('<','\u003c') == 'hello <document> world')"` → **True** | 改 `content.replace("<", "\\<u003c")` 等字面转义（cost-ledger-design.md:11 已立案）
  - F320 | run_gate_g3 缺 progress.json 时伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history → G3.4 独立性检查在 pipeline 模式下恒可通过（gate 自我满足） | security | P2 | dispatch_helper.py:1977-1990 | 为绕过缺文件而合成独立性证据（gate-effectiveness-design.md:7 已立案） | 未验证（静态读码） | pipeline 模式显式携带真实 scorer 身份或 G3 fail-closed
  - F329 | literal 路径回退垃圾写入：`parsed.get(rel_path, parsed.get("__stdout__", ""))` —— 模型输出的 path 与契约路径不完全一致时，把整段响应（JSON 全文）写入每个缺失的契约路径 | error | P2 | dispatch_helper.py:1172-1175 | 回退过宽 | 未验证 | 仅在无任何 parsed 命中时回退 __stdout__，否则记 error 跳过
  - F330 | `_budgeted_truncate` 按权重分配后短文件余量不再分配给被截断文件（总输出可远低于预算同时仍有文件被截） | optimization | P2 | dispatch_helper.py:307-317 | 单轮分配无再平衡 | 未验证 | 两轮分配（先按需，再分余量）
  - F334 | `_compute_dispatch_timeout` docstring "base = 300s (5 min)" vs 代码 `base = 900` | docs | M | dispatch_helper.py:101-107 | 漂移 | 未验证 | 更新 docstring
  - F337b | run_gate_g4 以 `",".join(files)` 传子进程——文件名含逗号时解析歧义（当前内部文件名无逗号，防御记录） | docs | M | dispatch_helper.py:1940-1948 | 未验证
- 安全核验：`_dispatch_via_ide`/legacy 均列表参数 subprocess（无 shell=True）；prompt 经 stdin/argv（legacy argv 传全文 prompt——进程列表可见性面，M 级）；wildcard 正则 `[^/]*` 阻断路径穿越；`_find_ide_cli` zcode 复用 codex 旗标（注释自知）。
- 覆盖缺口处置（135 行缺失，重点）: 615-634（共享上下文注入）→ must-test（与 audit_context_cache F312 联动）；978/988/1031/1035（输出解析边界）→ acceptable；1362-1366（cap-raise 400 分支）→ acceptable；1436-1442（early_stop）→ acceptable；1951-1956（G4 超时）→ acceptable
- 验证命令: 见 F302/F308 内嵌输出；`grep -rn "generate_static_template" ` 等
- 置信度: high

### src/shenbi/pipeline/error_handler.py
- 处置: deep-read
- 声称检查的不变量: 纯谓词；max_revision_retries=3 → 3 次总尝试；max_audit_retries=3 → 3 轮修订
- findings: 无（MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES 常量为文档性死常量，enforcement 实际读 state.config——注释已声明，acceptable）。语义核验：attempt<limit 重试 → 1,2 重试 / 3 升级 = 3 总尝试 ✓；revision_count 0,1,2 重做 / 3 升级 = 3 轮 ✓。
- 覆盖缺口处置: 100% 覆盖，无
- 验证命令: 未运行（100% 覆盖 + 纯函数读码）
- 置信度: high

### src/shenbi/pipeline/filelock_utils.py
- 处置: deep-read
- 声称检查的不变量: POSIX flock 读写分离；超时 TimeoutError；fd 泄漏防护；Windows 回退
- findings: 无（`_flock_acquire` 把非阻塞 OSError 一律当"未获锁"重试至超时——EBADF 等真错误会被拖延到超时，M 级防御缺失，acceptable）
- 覆盖缺口处置（67-72 = Windows 分支）: acceptable（pragma no cover）
- 验证命令: 未运行（tests/unit/pipeline/test_filelock_utils.py 存在）
- 置信度: high

### src/shenbi/pipeline/genesis.py
- 处置: deep-read
- 声称检查的不变量: 17 步序列；optional 步失败跳过；重试→escalation；step-6 total_chapters 固化；索引刷新失败不阻塞
- findings:
  - F332 | auto-mode（per_chapter_review_enabled=False）G4 失败"重试一次后续行"路径（346-360）零测试（coverage 349-359 缺失）——该路径会让 G4 失败输出继续进入索引/推进，属静默降级关键分支 | error | P2 | genesis.py:346-360 | 测试缺口 | d1-06 coverage 349-359 missing | must-test
  - F328（部分，见 truth_embed 条目）: `_update_route_b` 内 `embed_and_store` 每条 hook/rule 重新加载模型 + EmbeddingStore 未 close
- 语义注记：genesis 以 `per_chapter_review_enabled`（章循环配置）作为自身 auto 模式开关——命名跨域复用，M 级可读性问题。
- 覆盖缺口处置（131-133, 176, 349-359）: 131-133=索引失败 WARN → acceptable；176=route_b_embeds_updated 日志 → acceptable（依赖 embeddings extra）
- 验证命令: 未运行（tests/unit/pipeline/test_genesis.py + test_genesis_to_loop.py 覆盖主路径）
- 置信度: high

### src/shenbi/pipeline/hook_planting.py
- 处置: deep-read
- 声称检查的不变量: section-7 解析双格式；仅 plant/open 入账；按 ID 去重；原子写
- findings: 无（写回仅 frontmatter 会丢弃 body（263-267），但 frontmatter 为权威源且 truth_index 有 body 双源兜底——acceptable 设计取舍）
- 覆盖缺口处置（50-51, 117, 129, 140, 145, 230->247 等）: 均为解析防御分支 → acceptable（tests/unit/pipeline/test_hook_planting.py 覆盖主干）
- 验证命令: 未运行
- 置信度: high

### src/shenbi/pipeline/llm_output_integrity.py
- 处置: deep-read
- 声称检查的不变量: 写失败 dominator 规则；泄漏/围栏/审计完整性/行号引用检查
- findings:
  - F335 | `_AUDIT_MIN_BYTES = 200` 与注释 "real audits are > 500 bytes" 不一致 | docs | M | llm_output_integrity.py:53-55 | 未验证 | 对齐注释或常量
- 覆盖缺口处置: 100% 覆盖，无
- 验证命令: 未运行（100% 覆盖）
- 置信度: high

### src/shenbi/pipeline/machine.py
- 处置: deep-read
- 声称检查的不变量: load/save 原子；set/clear checkpoint；ESCALATION 解决时重置三组 retry_counts
- findings:
  - F338 | clear_checkpoint 无 NONE 防御：pending_checkpoint 为 NONE 时仍追加 `{"type":"none",...}` 历史 | error | M | machine.py:76-84 | 未验证 | 前置 `if cp.type is CheckpointType.NONE: return`
- 覆盖缺口处置（50-52）: checkpoint 写盘失败 OSError → acceptable
- 验证命令: 未运行（tests/unit/pipeline/test_checkpoint.py 存在）
- 置信度: high

### src/shenbi/pipeline/parallel_dispatch.py
- 处置: deep-read
- 声称检查的不变量: Semaphore=4 限流；指数退避+同量级 jitter；assert_parallelizable 拒绝非只读技能；结果按序返回
- findings:
  - F301（主证据，见 dispatch_helper 条目）: `_dispatch_with_retry` 调 dispatch_skill 不传 state → 审计波 token 不入账（本区最重的 token 消耗段）
  - F309（主证据，见 chapter_loop 条目）: 失败结果仅在 consolidate 计数，无消费方升级；consolidate 的 stdout 行首扫描对 API 路径失效
- 并发核验：results dict 全 future 填充（异常兜底 DispatchResult），按 idx 重排 ✓；Semaphore 在重试间释放 ✓；`assert_parallelizable` 在线程spawn前执行 ✓（write_safety 默认 WRITE_SHARED 保守正确）。无共享可变状态（ReviewTask immutable-ish）✓。无 pickle 边界（线程池）✓。
- 覆盖缺口处置（77-128 重试主体, 165, 168-188, 216）: 77-128=`_dispatch_with_retry` 重试/退避主体 → must-test（tests/pipeline/test_parallel_dispatch_backoff.py 存在但 coverage 显示主体未覆盖——测试 mock 掉了 dispatch，退避路径未走真）；168-188=future 异常兜底 → acceptable
- 验证命令: `grep -n "def test" tests/unit/pipeline/test_parallel_dispatch.py` → 7 个测试均为构造/consolidate 级，无 dispatch_skill state 断言
- 置信度: high

### src/shenbi/pipeline/plan_skeleton.py
- 处置: deep-read
- 声称检查的不变量: 8 段骨架；1/4/6/7 预填；5 完全 LLM；卷界运行时解析
- findings: 无（`_resolve_volume_at_runtime` None → "Unknown" 兜底合理）
- 覆盖缺口处置（170-176）: resolved None 分支 → acceptable（test_plan_skeleton.py 7 测试覆盖主干）
- 验证命令: 未运行
- 置信度: high

### src/shenbi/pipeline/review_checklist.py
- 处置: deep-read
- 声称检查的不变量: mtime 缓存失效；注入位置；各提取器缺文件容错
- findings:
  - F319 | 静态/动态 split 段（464-568）死代码 + 硬编码 stub：`generate_static_template`/`generate_chapter_delta`/`get_checklist` 无生产调用者；`_load_voice_constraints` 硬编码 "third_person_limited"、`_load_pov_mode` 硬编码 "multi_pov"；`_extract_hooks_from_plan` 用零填充 `chapter-{chapter:03d}-plan.md`（实际非填充）→ 永远读不到 | deps | P2 | review_checklist.py:464-568 | Plan 11 Task 3 半成品未接线 | `grep -rn "generate_static_template\|get_checklist" src/ tests/` → 仅定义处 | 删除或完成接线
- 覆盖缺口处置（98-99, 217-223, 260, 296-297, 330-331, 334-346, 400-402, 428-434, 449-456, 487, 514-568）: 514-568=死代码段 → dead-code（F319）；其余提取器防御分支 → acceptable（test_review_checklist.py 两套测试覆盖主干）
- 验证命令: 见 F319 grep 输出
- 置信度: high

### src/shenbi/pipeline/revision_router.py
- 处置: deep-read
- 声称检查的不变量: §6.3 决策树；severity 正则防误报（"No BLOCKING issues" 不误判）；escalation 派发
- findings:
  - F304 关联 | `check_resonance(None) = True`（115-117）——None 被视为通过。在共振分未被解析的默认流（F304）下，这是 floor 失效的直接放大器。函数本身语义（缺数据不阻塞）合理，但与 F304 组合后 §6.3 的 resonance 分支整体失效 | error | P1（并入 F304 计） | revision_router.py:109-117 | 见 F304
- 核验：`_SEVERITY_BLOCKING_RE` 对 `**严重度**: BLOCKING`/`severity: BLOCKING`/`**BLOCKING**` 三形态覆盖 ✓；裸 "BLOCKING" 出现在句子中不误判 ✓（与 audit_layer 的朴素 `"BLOCKING" in content` 不一致——audit_layer:188 更宽，可接受差异但建议统一）。`collect_audit_issues` glob 前缀 `chapter-{N}-` 无跨章误匹配（chapter-3- 不匹配 chapter-30-）✓。
- 覆盖缺口处置（106）: route NO_REVISION 兜底 return → acceptable（98% 覆盖）
- 验证命令: 未运行（98% 覆盖 + tests/unit/pipeline/test_revision_router.py）
- 置信度: high

### src/shenbi/pipeline/scr_extractor.py
- 处置: deep-read
- 声称检查的不变量: 一次抽取全章缓存；facts-only 确定性；段落统计
- findings:
  - F310 | 缓存无失效：`extract_scr` 命中 `context/chapter-N-scr.json` 即返回，不比对章节文件 mtime——注释声称 "available and fresh" 但无 freshness 检查。章节在 step-16 修订后 SCR 停留在修订前版本；`_check_world_file_freshness`（读近 10 章 SCR）与后续消费全部用旧数据 | error | P1 | scr_extractor.py:428-433; chapter_loop.py:3072-3075（抽取在修订前） | 缓存键无版本/时间戳 | 未验证（静态控制流：extract 于 post-draft，revision 于其后，无失效钩子） | 缓存记录 source mtime，不匹配则重抽；或 revision 成功后删除对应 SCR
  - F333 | `_extract_event_timeline` 的 line_num 恒 1（168-171 初始化后不随句推进）；`_extract_pov_shifts` 行号伪造为段落序×2（282） | error | M | scr_extractor.py:168-183, 265-286 | 未验证 | 修正或移除伪行号
- 覆盖缺口处置（74-84, 144, 274, 331-335, 374-376, 412）: 74-84=名字 BAD_END 剥离 → acceptable；其余解析防御 → acceptable（test_scr_extractor.py 覆盖主干）
- 验证命令: 未运行
- 置信度: high

### src/shenbi/pipeline/seed_parser.py
- 处置: deep-read
- 声称检查的不变量: 双语节抽取；冒号最早分割；total_chapters 不在此设置
- findings: 无（`_split_key_value` min-position 分割正确处理值内冒号；`_extract_section` 层级感知正确；golden_opening/language 注入合理）
- 覆盖缺口处置（76->74, 112, 124->109, 140, 146->137, 167, 173->164）: 均为分支覆盖局部未走 → acceptable（92%，test_seed_parser.py 存在）
- 验证命令: 未运行
- 置信度: high

### src/shenbi/pipeline/snapshot_diff.py
- 处置: deep-read
- 声称检查的不变量: truth 全量 + 不可变哈希 + ring-buffer N=3 全文；restore 恢复 truth 与近章全文
- findings:
  - F306 | ring-buffer 全文条件 `f"chapter-{chapter:03d}" in f.name`——实际章节文件为非零填充 `chapter-3.md`，`chapter-003` 永不匹配 → 所有快照对 chapters/plans 仅存哈希，修订回滚（restore 的核心卖点）无法恢复章节正文 | error | P1 | snapshot_diff.py:112-115 | 零填充/非填充命名约定漂移（与 F312/F319 同族） | `uv run python -c`（构造 chapter-1/2/3.md 后 create_differential_snapshot(d,3,snap)）：日志 `ring_buffer_full=True` 但 `ring-buffer full-content files: []`，章节条目全部无 content | 改 `f.name.startswith(f"chapter-{chapter}-")` 或统一命名约定；补真实命名的回归测试
- 覆盖缺口处置（53, 61-62, 149-150, 171-184）: 149-150/171-184=restore 分支 → must-test（回滚能力核心，当前 tests/pipeline/test_snapshot_diff.py 未覆盖 restore 的哈希失配路径）
- 验证命令: 见 F306 内嵌输出
- 置信度: high

### src/shenbi/pipeline/state.py
- 处置: deep-read
- 声称检查的不变量: 线程安全 mutator；round-trip 序列化；SoftFailTracker 滑窗；instance-level lock；状态自愈
- findings:
  - F316 | `compact_pipeline_state`/`_archive_chapter_state`（383-428）无任何调用者（src+tests grep 仅定义）——宣称的 ~236KB→~80KB 压缩从未生效 | deps | P2 | state.py:383-428 | 未接线 | `grep -rn "compact_pipeline_state" src/ tests/` → 仅 state.py 定义 | 接线到 _complete_chapter 或删除
  - F318 前置注记 | `step_timings` 字段不参与 to_dict/from_dict（有意的 ephemeral observability，可接受，M 级缺注释）
  - 核验通过项：`_lock` 实例级 ✓（无 multiprocessing/pickle 边界——ThreadPoolExecutor 线程共享对象）；`add_step_done` 幂等 ✓；SoftFailTracker 窗口过滤 `chapter - ch <= window` ✓；from_dict 的 config 键过滤防 TypeError ✓；`_validate_state_consistency` 愈合与 clamp 正确（tests/pipeline/test_state_machine_heal.py 4 测试）
- 覆盖缺口处置（122, 132, 202-208, 319, 370-374, 387-401, 412-428, 522）: 387-428=死代码（F316）；202-208=from_json 解码失败 → acceptable；370-374=soft-fail 序列化边界 → acceptable
- 验证命令: `grep -rn "compact_pipeline_state" src/ tests/` → 仅定义
- 置信度: high

### src/shenbi/pipeline/state_heal.py
- 处置: deep-read
- 声称检查的不变量: resume 时预算/修订数/快照指针按盘面保守修复
- findings:
  - F317 | `_heal_last_snapshot` 仅 glob 平面 `snapshots/chapter-*.md`（legacy 格式）；默认差分快照为 `snapshots/chapter-NNN/` 目录 → 差分项目 crash 后 last_snapshot 无法愈复 | error | P2 | state_heal.py:80-95 | 快照格式迁移遗漏 | 未验证（静态读码 + chapter_loop.py:1747 差分目录命名） | glob 目录并选最新 mtime
- 覆盖缺口处置（56-57, 79, 85）: 79=last_snapshot 已有 → acceptable；85=无快照 → acceptable
- 验证命令: 未运行
- 置信度: high

### src/shenbi/pipeline/transitions.py
- 处置: deep-read
- 声称检查的不变量: 五迁移纯状态操作；REJECT closure→chapter_loop 全量重置
- findings: 无（`transition_closure_to_chapter_loop` 重置 closure_step/skills_done/retry_counts + step_index=0，语义完备；与 cli.py 的调用方一致）
- 覆盖缺口处置: 100% 覆盖，无
- 验证命令: 未运行（tests/unit/pipeline/test_transitions.py）
- 置信度: high

### src/shenbi/pipeline/triggers.py
- 处置: deep-read
- 声称检查的不变量: 触发周期（12/36/卷界/结尾）；[I14] 写序；volume-boundary 检查点后置快照；genre 漂移阈值
- findings:
  - F301 关联 | run_triggered_skills 派发不传 state（542）——周期技能 token 不入账（并入 F301 证据链）
  - M | `_style_profile_is_stale` 的 `glob("chapter-*.md")` 把 `chapter-N-pre-rev.md` 备份计入章数（104） | docs | M | triggers.py:100-104 | 未验证 | 过滤 `-pre-rev`
  - 核验通过项：get_trigger_steps 的 volume_boundary 抑制独立 style_learning 条目避免重复执行 ✓；check_escalation 签名（resonance_scores/sensitivity_blocking/volume_objective_met/regeneration_attempts）与 chapter_loop.py:1029 调用一致 ✓；book_closure `chapter >= total_chapters` 在 cli 仅 total>0 时调用 ✓。
- 覆盖缺口处置（337, 349, 399->406, 419-420, 506, 537-556, 588-593）: 588-593=G3 失败分支 → must-test；337/349=漂移解析边界 → acceptable
- 验证命令: `grep -n "def check_escalation" -A 12 src/shenbi/skill_utils/escalation/check.py`（签名核对）
- 置信度: high

### src/shenbi/pipeline/truth_embed.py
- 处置: deep-read
- 声称检查的不变量: §7.3 降级路径（模型缺失返回 False）；SQLite upsert 主键去重；余弦维度过滤
- findings:
  - F328 | `embed_and_store` 每次调用 `st.SentenceTransformer("bge-large-zh")` 全量加载模型（数秒-数十秒 + 数百 MB RAM）——genesis `_update_route_b` 对每条 hook/rule 调用一次：一个 14 步索引刷新 × 几十条目 = 数十次模型加载；且 genesis 的 EmbeddingStore 从不 close（连接泄漏） | optimization | P2（条件性：仅安装 embeddings extra 时触发；默认环境 is_embed_available=False 短路） | truth_embed.py:119-127; genesis.py:157-176 | 模型无模块级缓存 | 未验证（需模型环境） | 模块级单例 + store 复用/with 语义
- 覆盖缺口处置（95, 117-127, 227-252）: 117-127=真实 embed 路径 → acceptable（测试环境无模型，降级路径已测）；227-252=main() CLI → dead-code 候选（无测试；与 F337 truth_index main 同批处置）
- 验证命令: 未运行
- 置信度: high（medium for 生产影响估计——依赖 embeddings extra 安装率）

### src/shenbi/pipeline/truth_index.py
- 处置: deep-read
- 声称检查的不变量: 双源 hook 索引；中文序数规则标题；空索引 WARN（静默失败信号）；canonical 名匹配替代 CJK 子串
- findings:
  - F337 | main() CLI（335-370）零测试（coverage 342-370 全缺）——注册入口的 rebuild/query 无回归网 | docs | M | truth_index.py:335-370 | 未验证 | 补 CLI smoke 测试
  - M | `_HOOK_ID_RE` body 源无条件入索引：散文中 "M16"（枪械型号）等 `[HM]\d+` 形态词会被索引为 hook（208-218）——索引污染低风险 | docs | M | truth_index.py:32, 207-218 | 未验证 | body 源要求邻近"钩子/Hook"词再入索引
- 核验通过项：`_parse_rules` seen_spans 去重防双模式重复 ✓；`_warn_if_empty` >100 字节阈值合理 ✓；`extract_entities_from_plan` 用 find_terms canonical 匹配 ✓。
- 覆盖缺口处置（56, 129, 142, 145, 189->188, 192, 277-279, 342-370）: 342-370=main（F337）；277-278=OSError 防御 → acceptable
- 验证命令: 未运行（tests/unit/pipeline/test_truth_index*.py 存在覆盖核心）
- 置信度: high

### src/shenbi/pipeline/truth_io.py
- 处置: deep-read
- 声称检查的不变量: 路径键控锁序列化 read-merge-write；自然键去重（章号/hook id）；三模式
- findings: 无 P 级。核验：`_upsert_by_key` 无键记录保序前置 ✓；`_upsert_markdown_table_row` 首列键删除后追加 ✓（resonance_trend 行格式与 escalation_bridge cells[6] 契约一致——读码比对 escalation_bridge.py:10-25 ✓）；`_path_lock` 注册表锁防双创建 ✓；replace(dict) 的 Python repr 渲染（111）为 M 级格式瑕疵。
- 覆盖缺口处置（128, 230, 235-245, 257-265）: 235-245=YAML 读失败 WARN → acceptable；其余分支局部 → acceptable（93%）
- 验证命令: 未运行（tests/unit/pipeline/test_truth_io.py）
- 置信度: high

### src/shenbi/pipeline/volume_align.py
- 处置: deep-read
- 声称检查的不变量: 从 volume_map 抽章节点；关键词匹配率告警
- findings:
  - F314 | 整模块死代码：`check_volume_alignance/extract_chapter_node/extract_key_terms` 无生产调用者——CHAPTER_STEPS step-1 "pipeline-volume-align" 走通用 `pipeline-` 分支仅标记完成（chapter_loop.py:2755-2769 无任何调用）；且本模块仅支持英文 "## Chapter N" 形态，对生产中文卷图（## 第N卷 / | 第N章 | 行）恒返回 None | deps | P2 | volume_align.py:13-23, 52-74; chapter_loop.py:138-141, 2755-2769 | 步骤表重构时未接线 + 格式不支持生产中文卷图 | `grep -rn "check_volume_alignment" src/` → 仅本模块定义（tests 直调） | 删除模块或将 step-1 接到 _shared.read_chapter_node 的中文实现
- 覆盖缺口处置（10, 18-19, 23, 59, 63）: 59/63=node None 早退 → 与 F314 一并处置（dead-code）
- 验证命令: `grep -rn "check_volume_alignment\|volume-align" src/ tests/` → step-1 无调用
- 置信度: high

### src/shenbi/pipeline/write_safety.py
- 处置: deep-read
- 声称检查的不变量: shenbi-review-* → READ_ONLY_AUDIT；已知写共享集；未知保守 WRITE_SHARED
- findings: 无（保守默认正确；核心波 6 技能均 review- 前缀通过分类 ✓）。注记：READ_ONLY_AUDIT 实际写审计报告文件——语义为"只写自有报告"，docstring "read-only" 略有误导（M）。
- 覆盖缺口处置: 100% 覆盖，无
- 验证命令: 未运行
- 置信度: high

---

## docs 引用漂移（F331 明细）

live（非 archive）specs 引用 vs 当前行号（chapter_loop.py / dispatch_helper.py 今日 10:00 有改动）：
- 2026-08-14-drift-chain-design.md:19 引 chapter_loop.py:2722-2727 → 实际 2757-2765（漂移 ~35 行）
- 2026-08-14-gate-effectiveness-design.md:12 引 chapter_loop.py:2907-2917 → 实际 2945-2954（漂移 ~38 行）
- 2026-08-14-cost-ledger-design.md:11 引 dispatch_helper.py:734 → 实际 747（漂移 13 行）
- 2026-08-14-p2-batch-design.md:288 引 dispatch_helper.py:1030-1037 → 实际 append_dedup 注释在 1075-1081（漂移 ~45 行）
- 2026-08-14-tooling-gate-chain-design.md:63 引 chapter_loop.py:680 → 实际 684（5 行内，可接受）
处置：P2——specs 是立案依据，行号失准会误导后续实施定位；建议结案时统一回填。

## 测试对应性总评

真实测试存在且有效的：state machine/transitions/checkpoint（100%）、error_handler（100%）、llm_output_integrity（100%）、truth_io/truth_index/seed_parser/plan_skeleton/hook_planting/filelock_utils/crash_recovery/scr_extractor/revision_router（>88%，直测真实函数）。
测试失效掩盖真实缺陷的：test_audit_cascading.py（分组格式直测 _should_skip_audit + 扁平格式单测 _get_audit_history——两半各自通过、集成断裂无人测，F303）；test_parallel_dispatch.py（仅构造/consolidate 级，无 state 透传与失败升级断言，F301/F309）；test_chapter_loop_full.py:234 把"G3 不调用"写成注释断言（把缺陷固化为预期，F305）；test_volume_align.py 只测死模块本身（F314）。
零覆盖注册入口：truth_index.main、truth_embed.main（F337/F328）。

## 安全初查汇总

- 路径穿越：wildcard 正则 `[^/]*` 阻断 `../`（dispatch_helper.py:417-468）✓；checkpoint/staging 路径全部内部常量 ✓
- shell 注入：全部 subprocess 列表参数、无 shell=True ✓；legacy 路径 prompt 走 argv（ps 可见性面，M）
- 凭证：API key 仅经环境变量读取（1540），无落盘 ✓
- Prompt 注入防御失效：F308（`<` 转义 no-op）——唯一实际击穿点
- Gate 完整性：F320（G3 progress.json 伪造）

## 复杂度与资源效率汇总

- 每章循环主导成本：并行审计波 6+ LLM 调用（共享上下文已缓存 ✓）+ 生命周期/结算并行 2 调用 + 触发器周期扇出；无每章超线性增长点 ✓（truth 重读已由 audit_context_cache/合同读取过滤缓解，但 F312 使缓存部分失效）
- 全量重算点：assemble_context 每章 build_index 全扫（可接受）；_maybe_rebuild_truth_index 白算（F313）；truth_embed 模型重载（F328，条件性）
- 状态体积：chapter_states 无压缩（F316 的 compact 未接线）——100 章 ~236KB state JSON，可接受但设计已预案
