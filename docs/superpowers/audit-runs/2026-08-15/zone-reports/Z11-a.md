# Z11-a 段报告 — novel-output/ 磁盘执行产物（2026-08-15 轮）

- **范围**: docs/superpowers/audit-runs/2026-08-15/zones/Z11-a.files 全部 1260 文件（test-validation/ 27、validation-results/ 4、xinghuo-ranqiong/ 1229）
- **处置**: 机械全量核验（/tmp/z11a_verify.py + /tmp/z11a_verify2.py，只读）+ 语义抽读 28 文件
- **覆盖**: 机械核验 1260/1260（100%）；未覆盖文件列表为空
- **编号段**: F1101–F1119
- **总体**: xinghuo-ranqiong 是一次真实 56 章 pipeline 运行（快照时间戳 2026-07-15T23:22 → 2026-07-17T13:00，整树 mtime 一致为 2026-07-20 10:29:49，系一次性拷贝入库，mtime 不可作运行时间线证据）。运行在中断态被快照：closure=pending、ch56 未完成。历史 findings（chapter_summaries 2/56、token-ledger 空、finish_reason 盲点）在当前产物中**全部复现且加重**。

---

## 汇总

| 严重度 | 数量 | 编号 |
|---|---|---|
| P0 | 3 | F1101, F1102, F1104 |
| P1 | 5 | F1107, F1108, F1109, F1111, F1115 |
| P2 | 11 | F1103, F1105, F1106, F1110, F1112, F1113, F1114, F1116, F1117, F1118, F1119 |

---

### novel-output/xinghuo-ranqiong/chapters/（145 文件）
- 处置: deep-read（机械核验 145 + 抽读 8: ch1/2/9/12/22/30/44/55）
- 声称检查的不变量: 章号 1–56 连续无缺无重；每章为 ≥3000 字正文；decisions.json 遵循 shenbi-decisions-v1；每章伴随 plan/audits/decisions
- findings: F1101, F1102, F1103（另 F1104 的后果落点在此）
- 验证命令: `python3 /tmp/z11a_verify.py`（[a]/[d] 节）+ 逐文件 head/tail 抽读
- 置信度: high

**F1101 | 5 章正文被修订技能摘要覆写丢失（不可恢复） | error | P0**
- 证据: `novel-output/xinghuo-ranqiong/chapters/chapter-55.md:1` 全文 104 字节 `"Chapter content unchanged — no revision needed. Use the existing chapters/chapter-55.md from input."`（自引用占位符）；`chapters/chapter-9.md:1`（642B 修订结论元叙述）；`chapters/chapter-2.md:1`（1035B 英文修订摘要表）；`chapters/chapter-44.md:1`（1229B）；`chapters/chapter-12.md:1`（1506B）。正文实测非空白字符：ch55=89、ch9=321、ch2=742、ch44=1032、ch12=1154（其余 51 章均值 8761）。
- 不可恢复性: snapshots 仅覆盖 ch5–55；ch2 无快照；ch9/12/44/55 的快照（`snapshots/chapter-055-20260717T130039.md` 等）"## Chapter N" 段落的起点即占位符/摘要文本（快照=章节文件+审计拼接），真实正文未存活于任何仓库产物。ch22 曾同样被覆写（`audits/chapter-22-anti-ai.md` 记录 "字数: 54（仅剩G4修复摘要，正文已丢失）"、931B）后被重写恢复，证明该失败模式反复发生。
- 根因: shenbi-chapter-revision 将"修订说明/未变更声明"直接写入 `chapters/chapter-N.md`，而非仅写 revision-decisions；修订后无字数下限校验拦截。
- 验证命令+输出: `python3 -c "import re;print(len(re.sub(r'\s','',''.join(open('novel-output/xinghuo-ranqiong/chapters/chapter-55.md').read()))))"` → `89`
- 建议方向: revision 技能输出契约强制"正文文件只含正文"；修订后 G4.word_count 复验；快照改为正文字节级复制而非拼接审计。

**F1102 | decisions.json 契约被静默违反：145 个中 88 个（61%）不合规 | error | P0**
- 证据: 故障模式分解（`/tmp/z11a_verify.py` [d] 节 + 分解脚本）：concatenated_extra_data=67（合法 JSON 后追加 Markdown，如 `chapters/chapter-1-decisions.json` raw_decode 至 char 3007 后出现 ``` ``` `` 与 "两项 G4 失败修复摘要"）；not_json_at_all=5（如 `chapters/chapter-12-revision-decisions.json:1` 以 "The revision is complete..." 开头）；missing_delimiter=9；invalid_control_char=2；`$schema` 键名漂移=3（如 ch30 用 `"schema"`/`"timestamp"`/`"decisions"` 替代 `$schema`/`produced_at`/`selections`）；缺必填字段=2。fully_valid 仅 57/145（39%）。真实数据 P2.5 违规 93 条（rationale 缺失/禁止项存在/超 100 字）。
- 根因: G4 decisions 门禁未拦截——`gate-markers/G4-shenbi-chapter-revision-generative.json` 为 `{"gate":"G4-decisions","status":"PASS","checks":[],"files_checked":["chapters/chapter-55.md"]}`（空 checks 直接 PASS）；`G4-shenbi-chapter-drafting-generative.json` 中 `"G4.dec":{"s":"SKIP","r":"no files"}`。下游按 `reads:` 消费 decisions 的技能全部静默失败。
- 验证命令+输出: 见上文模式分布 `{'concatenated_extra_data': 67, 'fully_valid': 57, 'not_json_at_all': 5, ...}`；总 145
- 建议方向: 写入时 JSON 解析硬校验（fail-fast）；G4-decisions 空检查数组应判 FAIL 而非 PASS。

**F1103 | state 死字段：revision_count 与 resonance_score 全程未写入 | error | P2**
- 证据: `pipeline-state.json` 全 56 章 `revision_count: 0`、`audit_retry_count: 0`、`resonance_score: null`；而磁盘 34 个 `*-revision-decisions.json` 存在且 revision_route 分布 no-revision:21 / spot-fix:23 / constrained-regenerate:5 / regenerate:6。34 章"有修订产物但计数为 0"。
- 根因: 状态机只写 route 不递增计数；resonance 评分落盘到 `truth/resonance_trend.md` 但未回填 state。
- 验证命令+输出: `revision_count dist: {0: 56}`；`resonance_score null: 56/56`
- 建议方向: 修订完成后回写计数；或删除死字段避免误读。

---

### novel-output/xinghuo-ranqiong/truth/（13 文件）
- 处置: deep-read（机械核验 13 + 抽读 6: chapter_summaries/current_state/character_matrix/audit_drift/resonance_trend/pending_hooks）
- 声称检查的不变量: 文件集 ⊆ docs/framework/truth-files.yaml 词表；chapter_summaries 覆盖全部已完成章；趋势类 truth 持续追加
- findings: F1104, F1105, F1106
- 验证命令: `python3 /tmp/z11a_verify.py` [b]/[l] 节
- 置信度: high

**F1104 | chapter_summaries.md 仅覆盖 2/56 章（历史 finding 原样复现） | error | P0**
- 证据: `truth/chapter_summaries.md`（2671 字）仅含 `## 第55章`、`## 第56章` 两个条目；`第N章` 正则全集 = {55, 56}。当前 novel-output 不变式要求已完成 55 章的摘要逐章沉淀，53 章上下文丢失（下游 context-composing 的 `adjacent_to_target_chapter` 选择无料可选）。正面对照：`current_state.md` 与 `character_matrix.md`（frontmatter `last_chapter: 56`）均为最新。
- 根因: state-settling 的章节摘要写入路径在 ch1–54 期间未生效（与 F1112 context 断档、F1105 趋势断档同簇：ch55–56 系后来用新版代码续跑，仅末 2 章吃到修复）。
- 验证命令+输出: `'第N章' mentions found: [55, 56] -> coverage 2/56`
- 建议方向: 为 1–54 章离线回填摘要；将章覆盖率纳入 G4 state-settling 检查。

**F1105 | 趋势类 truth 同样覆盖坍缩：resonance_trend 1 行、audit_drift 1 章 | error | P2**
- 证据: `truth/resonance_trend.md`（316B）仅 ch55 一行（overall=70）；`truth/audit_drift.md`（850B）仅 Ch55 条目，而其自述为"记录每章审稿后的共鸣短板条目"。与 F1104 同根因。
- 验证命令+输出: 机械核验 [l] 节文件尺寸 + cat 全文
- 建议方向: 同 F1104。

**F1106 | truth/state_snapshot-pre-rev.md 不在词表——静默同义词 | error | P2**
- 证据: `docs/framework/truth-files.yaml` 词表无 `truth/state_snapshot-pre-rev.md`；磁盘存在（4921B）。truth-files.yaml 头注声称"添加新文件 = ONE edit here，防止静默同义词"，该文件即绕过实例。同型历史证据：test-validation write-audit 拦截过未声明写入 `truth/bridge_tracker.md`（亦不在词表）。
- 验证命令+输出: `on disk but NOT in vocab: ['truth/state_snapshot-pre-rev.md']`
- 建议方向: 词表补录或改用既有概念名；G2 对词表外 truth 路径告警。

---

### novel-output/xinghuo-ranqiong/audits/（722 文件）
- 处置: deep-read（机械核验 722 + 抽读 7: ch1/22/48/55 的 anti-ai/pacing/pov/review-summary）
- 声称检查的不变量: review-summary 的 BLOCKING/CRITICAL 计数 = 各单项审计判定之聚合；审计对象与磁盘文件一致
- findings: F1107, F1108
- 验证命令: `python3 /tmp/z11a_verify.py` [f] 节 + `/tmp/z11a_verify2.py` [f2] 节
- 置信度: high

**F1107 | BLOCKING 汇总抑制：单项审计判 BLOCKING，review-summary 全部报 0 | error | P1**
- 证据: 128/722 审计文件含 BLOCKING 判定（Top: ch22-anti-ai×12、ch48-pacing×8、ch11-pacing×5、ch35-continuity×5）。`audits/chapter-48-pacing.md` 明文 `判定: BLOCKING（累积周期）`、`BLOCKING — 远超maxChaptersPerCycle(15)`；`audits/chapter-22-anti-ai.md` `结果: 不通过（BLOCKING）`。但 55/55 份 `chapter-N-review-summary.md` 均为 `BLOCKING Issues: 0` + "No BLOCKING or CRITICAL issues found across all reviews"，state `blocking_found: false`。ch22 案例无歧义：审计输入即被毁文件、结论为不通过，汇总却报 0（语义抽取核验 55 份一致，非正则误报；ch44 revision-decisions 佐证管线知情："Pacing BLOCKING … Book-level structural … Cannot fix"——按书级延期处理但未如实入汇总）。
- 根因: review-summary 聚合只统计"本章可修订"级问题，书级/结构级 BLOCKING 被静默丢弃且汇总文案未声明该口径。
- 验证命令+输出: `BLOCKING Issues distribution across summaries: Counter({0: 55})` vs `files containing BLOCKING: 128`
- 建议方向: 汇总区分 `chapter_actionable` 与 `deferred_book_level` 两栏；或聚合器解析单项审计终判。

**F1108 | 审计波冗余：6.2MB 审计对 1.3MB 正文（4.64x），并在快照内二次嵌埋 | optimization | P1**
- 证据: audits/=6.2MB、chapters/=1.3MB（机械核验 [bytes] 节）；snapshots/ 12.9MB/51 文件（均 ~250KB）系"章节+全套审计"拼接（F1109），同一审计文本在仓库存 ≥2 份（~6MB 重复）；staging/ 另存 1.2MB plans/truth 副本。全树 23.7MB 中约 8MB 为纯重复（~34%）。13 种审计 × 56 章的固定波次即使 no-revision 章也全量执行（21 章 no-revision 仍产 11 份单项审计）。
- 根因: 审计波无条件全跑 + 快照拼接设计 + staging 不清理。
- 验证命令+输出: `audits=6.2MB chapters=1.3MB ratio=4.64x`
- 建议方向: 依据 review-checklist 风险分采样审计；快照去审计化（F1109）。

---

### novel-output/xinghuo-ranqiong/snapshots/（52 文件）
- 处置: deep-read（机械核验 52 + 抽读 6: manifest + ch9/12/44/55/30 快照头）
- 声称检查的不变量: 快照可恢复章节正文；manifest 与文件一致；覆盖全部已完成章
- findings: F1109
- 验证命令: `/tmp/z11a_verify.py` [a]/[snapshots] 节 + `head snapshots/chapter-055-*.md`
- 置信度: high

**F1109 | 快照机制失能：拼接审计而非正文副本，且未覆盖 ch1–4/ch56 | error | P1**
- 证据: `snapshots/chapter-055-20260717T130039.md` 的 "## Chapter 55" 段即 104B 占位符原文，随后 84,247 字符中 85 个 `##` 头几乎全为追加的审计报告——快照在章节已毁之后才拍摄，恢复价值为零（ch9/12/44 同构）。`snapshots/manifest.json` 仅列 ch5–55；ch1–4 无快照而 state `shenbi-snapshot-manage` 步骤标记完成；ch56 未拍。`pipeline-state.json` `last_snapshot: {}` 为空。
- 根因: 快照内容 = 章节文件现值 + 审计拼接，拍摄点在修订/审计之后；无"快照与上一版差异"校验。
- 验证命令+输出: ch55 快照 `han_chars=43343, ##-headers=85`，首头 `## Chapter 55  Chapter content unchanged`
- 建议方向: 快照=修订前正文字节副本（state_snapshot-pre-rev.md 的 md 版本即此意图，但只落了 1 份）；manifest 全章覆盖。

---

### novel-output/xinghuo-ranqiong/staging/（119 文件）
- 处置: deep-read（机械核验 119 + 抽读 2: staging/truth/pending_hooks.md、staging/plans 清单）
- 声称检查的不变量: staging→live 晋升完整（plans、decisions sidecar、truth 同步）；暂存区不滞留过期数据
- findings: F1110
- 验证命令: `/tmp/z11a_verify2.py` [i] 节
- 置信度: high

**F1110 | staging 晋升半途而废：55 个 plan-decisions 仅存暂存，truth 暂存过期 | error | P2**
- 证据: staging/plans 56 个 plan.md 与 plans/ 字节一致（纯冗余）；55 个 `chapter-N-plan-decisions.json` 无 live 对应（词表定义 `plans/chapter-N-plan-decisions.json` 为合法概念，磁盘 live 缺失），其中 38 个 JSON 不可解析、仅 15 个 `$schema` 合规（同 F1102 疾病）。`staging/truth/pending_hooks.md`（4171B）≠ live `truth/pending_hooks.md`（9886B）过期；`staging/truth/state-settling-decisions.json`（6025B）从未晋升（词表要求 `truth/state-settling-decisions.json`）。
- 根因: 晋升逻辑只搬 .md 不搬 decisions sidecar；staging 无清理步骤。
- 验证命令+输出: `no live counterpart: 55 e.g. ['chapter-1-plan-decisions.json', ...]`；`pending_hooks.md: identical=False (4171B vs 9886B)`
- 建议方向: 晋升按词表概念整体搬移；晋升后清空 staging。

---

### novel-output/xinghuo-ranqiong/{gate-markers,pipeline-state,progress,config-change-log,顶层}/（31 文件）
- 处置: deep-read（机械核验 31 + 抽读 6: pipeline-state 全文、2 个 G4 marker、progress.json、config-change-log、DEBUG 文档）
- 声称检查的不变量: gate marker 如实证明其所名技能的输出；state 声称的步骤有对应磁盘产物；checkpoint_history 与 gate-markers 对应
- findings: F1111, F1112, F1113, F1114, F1118
- 验证命令: `/tmp/z11a_verify.py` [c] 节 + `/tmp/z11a_verify2.py` [k] 节
- 置信度: high（F1112 medium）

**F1111 | gate marker 空转/被覆写：无法证明其所名内容，且与复验矛盾 | error | P1**
- 证据: `gate-markers/G4-shenbi-chapter-drafting-generative.json` `files_checked: ["chapters/chapter-56.md"]`——名为 chapter-drafting 技能级标记，实为最后一次调用（ch56）单章记录，前 55 次被覆写；`G4-shenbi-chapter-revision-generative.json` `checks: []` + `status: PASS`（零检查盖章）；drafting marker 内 `"G4.dec": {"s":"SKIP","r":"no files"}`。交叉矛盾：`validation-results/validation-report.md` Step 3 对 ch1 复验 G4 = FAIL（word_count 2813<3000、no_hook），而 marker 存根为 PASS。`pipeline-manifest.json` 全仓不存在（validation-report Step 4 亦证实）。
- 根因: marker 文件名按技能命名但按调用覆写；G4-decisions 实现空跑。
- 验证命令+输出: 见上文 JSON 原文（机械核验 [k] 节后追加 cat）
- 建议方向: marker 按 `<skill>-ch<N>` 增量落盘或汇总；空 checks 判 FAIL。

**F1112 | state 声称 context-composing 完成但 41 章无 context 产物 | error | P2**
- 证据: `pipeline-state.json` ch13–54 全部 `status: complete` 且 steps_done 含 `pipeline-context-assemble` + `shenbi-context-composing`；磁盘 `context/chapter-N-context.md` 仅 13 份（ch1–12, 55, 56）。DEBUG 文档将 context 文件归于 `shenbi-context-compression`（技能名与 steps_done 记录不一致，且产出为条件性）。
- 根因: 老版 loop 未落 context 文件 / 条件性产物未在 state 中区分"执行且产出"与"执行且跳过"。
- 验证命令+输出: `ch13/30/54: has_context_assemble=True has_context_composing=True` vs `context=13/56`
- 建议方向: steps_done 记录产物哈希或区分 skip 分支。
- 备注: 因产出方技能名与条件性不确定，置信度 medium。

**F1113 | progress.json 为 171B 空壳（仅 scorer 字段） | error | P2**
- 证据: `progress.json` 全文仅 `current_scorer_agent` + 1 条 `scoring_history`；无章级进度/计时。validation-report "Timing Baseline: Not available" 同证。此为上轮 F1309 已登记问题在当前产物的实存。
- 验证命令+输出: 机械核验 [e] 节 progress.json 原文
- 建议方向: 上轮 z11-output-contracts-design 的 materialize 方案落地后复验。

**F1114 | 运行中断态与 state 尾部失真 | error | P2**
- 证据: `closure: "pending"`、`closure_step: 0`；ch56 `status: pending`（9/20 步），磁盘缺 5 份审计（dialogue/motivation/resonance/sensitivity/world-rules）+ review-summary + 快照 + decisions——本属一致的未完成态；但 `last_snapshot: {}`（实际有 51 快照）、`pending_checkpoint: {"type":"none"}`、`checkpoint_history` 仅 2 条（genesis-complete、ch35 escalation approve），与 22 个 G4 marker 无对应机制（两套记账互不引用）。
- 根因: 中断属运行事实；last_snapshot 空值属状态回写缺失。
- 验证命令+输出: `/tmp/z11a_verify2.py` [k] 节
- 建议方向: 快照后回写 last_snapshot；checkpoint 与 marker 统一账本。

**F1118 | DEBUG 文档自证已知缺陷与 state 漂移；正文与 genesis 大纲完全脱轨 | error | P2**
- 证据: `DEBUG_USE_MANUAL_CREATE.md` 自述 "最小章 Ch55: 101 字（生成中断）"、"plans 12章开始结构发生变化，后续plan情节出现巨大脱节和问题"、"retry_feedback 记录: 54 条"——当前 state 中 genesis `retry_counts: {}`、全章 `audit_retry_count: 0`（重试史被压缩丢失或文档失实，二者必居其一）。叙事脱轨：`plans/chapter-30-plan.md` §1 明言 "volume_map.md 将 Ch30 定位为 KR3 收官（楚云岚决定信任林烽）…当前叙事已完全偏离该大纲——以 current_focus 指令为准覆盖"；`foundation/review_report.md`（93 分通过）评审的是梅德兰/梵天金属片/铁炉堡设定，而实际 56 章为参数化实验叙事，ch30 尾部检查甚至以 "无…迷雾山脉/楚云岚/龙傲天突破" 为禁忌项。
- 根因: intent-management/current_focus 逐章覆盖大纲而无书级对账；运行方知情但仅记于 DEBUG 文档。
- 验证命令+输出: 上述文件原文引述（语义抽读）
- 建议方向: 书级 drift 对账门（volume_map vs 已写章节）；DEBUG 文档中的运维结论应进 truth/audit_drift。

---

### novel-output/xinghuo-ranqiong/{plans,context,genesis-context,world,characters,outline,style,foundation}/（147 文件）
- 处置: deep-read（机械核验 147 + 抽读 8: plan-30、context-10、checklist-30、story_bible、volume_map、protagonist、style_profile、foundation/review_report 头部）
- 声称检查的不变量: plans 56/56；checklist 56/56；genesis 词表文件齐全；产物与 truth 内容一致
- findings: 无独立 finding（缺陷分别归 F1108/F1112/F1118；结构完整：plans=56/56、checklist=56/56、world 5/5、outline 4/4、characters 2/2）
- 验证命令: `/tmp/z11a_verify.py` [a]/[h] 节
- 置信度: high

---

### novel-output/test-validation/（27 文件）
- 处置: deep-read（机械核验 27 + 抽读 7: pipeline-state、trace.jsonl、write-audit.jsonl、truth-index.json、gate marker、truth/4 文件、genesis-context）
- 声称检查的不变量: write-audit 契约执行；trace 完整性；genesis 产物词表合规
- findings: F1116, F1119
- 验证命令: `/tmp/z11a_verify.py` [e]/[g]/[h] 节
- 置信度: high（中止运行的不可判定项见低置信度说明）

**F1116 | 计量链盲点：trace 无 finish_reason 字段、重试无反馈变化 | error | P2**
- 证据: `test-validation/trace.jsonl` 4 行，schema 键集 `{seq,ts,actor,actor_role,action,target,skill,gate,payload,schema_version,signature}`——无 finish_reason（历史 finding"finish_reason 盲点"实锤：全产物零捕获，`finish_reason` 分布 `{None: 4}` 实为键缺失）。行 1–3 为三次完全相同的 GATE_FAIL（shenbi-worldbuilding 未声明写入 `truth/bridge_tracker.md`/`truth/character_matrix.md`），0.7s 间隔盲重试后第 4 行 escalation-review AUDIT_PASS。无 *.log 文件、无 ERROR/traceback（唯一 "429" 命中系 signature 十六进制子串，非限流）。
- 根因: trace 事件模型未设计模型层信号；重试循环不消费 gate 反馈。
- 验证命令+输出: `trace keys: {...无 finish_reason}`；三行 violations 逐字相同
- 建议方向: dispatcher 将 response 元数据（finish_reason/usage）追加进 trace 事件。

**F1119 | 三子项目结构漂移：xinghuo 缺 instrumentation，test-validation 词表不全 | error | P2**
- 证据: xinghuo 无 `trace.jsonl`/`write-audit.jsonl`（test-validation 有）；test-validation gate-marker 仅 1 个 vs xinghuo 22 个；test-validation `truth-index.json` 为空壳 `{"characters":{},"hooks":{},"rules":{}}`、`truth-embeddings.db` 为 0 有效索引；genesis-context 缺 forces/personal_conflict/plot_lines 3 文件（xinghuo 有）；truth 仅 4 文件（中止于 genesis step 2，state `phase: genesis, in-progress`，与 validation-report "no LLM backend" 一致）。
- 根因: 两轮运行跨框架版本（2026-07-16 vs 07-19），instrumentation 仅新版有；无运行清单（manifest）固化结构。
- 验证命令+输出: `test-validation-only: ['trace.jsonl','write-audit.jsonl']`；truth 4 vs 13 文件对比
- 建议方向: pipeline-init 按词表生成目录骨架并以 manifest 校验。

---

### novel-output/validation-results/（4 文件）
- 处置: deep-read（4/4 全读）
- 声称检查的不变量: 报告与产物实况一致
- findings: F1117
- 验证命令: 全文阅读 + 与磁盘交叉（G2 substitute、marker 计数）
- 置信度: high

**F1117 | 验证报告如实但记录了未修复的门禁失败集 | error | P2**
- 证据: `validation-report.md` 记录 Stage1 67 单测失败/coverage 18.41%、G0.16 FAIL（`G0.sc.missing_write_semantics:shenbi-foreshadowing-lifecycle:truth/pending_hooks.md`）阻断 round_creation、G2 以 xinghuo 章节替身数据 10/10 PASS（ch1–10 中 8 章 G2.12 "may be truncated" 告警未深究）、`pipeline-manifest.json` 两处均缺。报告诚实标注替身与阻塞（质量正面），但所列 G0.16/manifest/单测失败截至本轮仍以产物形态留存。
- 根因: 验证轮（2026-07-19）发现的问题未回灌修复即入库。
- 验证命令+输出: 报告原文 + `find -name pipeline-manifest.json` → 无
- 建议方向: G0.16 修复项移交框架区跟踪；G2.12 告警与 F1101 的截断/覆写模式或同源，应关联分析。

---

## 已知问题核验表（上轮 findings → 本轮产物表现）

| 上轮 finding | 本轮状态 | 证据 |
|---|---|---|
| chapter_summaries 仅 2/56 | **复现，未改善** | F1104：仍 {55,56} |
| token-ledger 恒空 | **加重：全仓 0 个 ledger 文件（非空文件，是不存在）** | F1115：spec 2026-08-14-cost-ledger-design.md:8 验收要求"真实 pipeline 运行后覆盖全部 dispatch"，本次 40 小时运行零计量 |
| finish_reason 盲点 | **复现：trace 事件模型无该字段** | F1116 |

**F1115 | token-ledger 全仓不存在：40 小时/56 章运行零 token 计量 | error | P1**
- 证据: `find . -name "*ledger*"` 仅命中 `truth/particle_ledger.md`（叙事道具账本，非计量）；无 `cost/` 目录。框架契约 `docs/superpowers/specs/2026-08-14-cost-ledger-design.md:8`（"验收：真实 pipeline 运行后 token-ledger.jsonl 覆盖全部 dispatch"）、`docs/superpowers/plans/archive/2026-07-19-03-pipeline-cost-and-token-accounting-plan.md:15`（`project_dir/cost/token-ledger.jsonl`，spec §3.2）。本运行（07-15→07-17）早于修复，为"死接线"期产物——但该产物恰是此后修复的验收基线，其零计量使 token 浪费量化（如 F1108）只能用字节代理。
- 根因: 运行早于 ledger 修复；无追溯性计量。
- 验证命令+输出: `token-ledger.jsonl anywhere in repo: 0`
- 建议方向: 修复后重跑最小 pipeline 验收；字节代理证据保留供浪费率对照。

## 覆盖与置信度声明
- 机械核验 1260/1260 = 100%（清单文件 0 缺失、磁盘 0 未列文件）；抽读 28 文件。
- 低置信度簇: ① F1112（context 产物条件性不确定）；② test-validation 整簇的"运行中止 vs 缺陷"边界（state 诚实标 in-progress，未深究 4 个 genesis skill 的内容质量）。
- 未覆盖文件列表: **空**。
