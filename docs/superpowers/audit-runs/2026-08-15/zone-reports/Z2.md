# Z2 分区初审报告（contracts/ + dispatcher/，38 文件）

- 轮次: 2026-08-15 全项目深度审查
- 分区: Z2（只读深读）
- 编号段: F201–F299（实际使用 F201–F223）
- 审查方式: 全部 38 文件逐文件语义深读 + 调用方 grep 验证 + 只读 `uv run python -c` 行为验证（未运行 pytest / 未执行任何写入命令）

## findings 总表

| 编号 | 标题 | 严重度 | 证据 |
|---|---|---|---|
| F201 | 字段级 reads 部分匹配时静默丢弃缺失声明字段（违反 AGENTS.md escape-hatch 契约） | P1 | contracts/fields.py:59-64; pipeline/dispatch_helper.py:605 |
| F202 | genre-config 禁用维度 + 空 customRules 绕过规则 7 校验（G4 洞） | P1 | contracts/skills/genre_config.py:93-94; skills/shenbi-genre-config/SKILL.md:300 |
| F203 | codex 输出 JSON 提取正则取最内层扁平对象，嵌套 scores 解析错误 | P1 | dispatcher/modes/codex.py:75 |
| F204 | run_g1/run_g2 对子进程 stdout 无守卫 json.loads，崩溃时丢失 stderr 上下文 | P2 | dispatcher/executor.py:127,147 |
| F205 | derive_file_type 单一类型 join + G2 decisions 分支跳过 .md → 混合输出技能的 markdown 在 T1 派发路由无 G2 校验 | P2 | dispatcher/executor.py:86-91; gates/g2.py:83-84 |
| F206 | _record_completion 对 progress.json 无锁 read-modify-write（并发 CLI 派发丢完成记录） | P2 | dispatcher/modes/codex.py:27-48 |
| F207 | family 占位符在 ctx 缺该 family 值时回退 chapter 语义（arc-N ← 章号，错文件） | P2 | contracts/paths.py:104-121 |
| F208 | _FAMILY_N.sub(count=1)：同一路径第二个 family 占位符取 chapter 值 | P2 | contracts/paths.py:112 |
| F209 | NNN 用无界 str.replace，与"有界替换"文档声明矛盾 | M | contracts/paths.py:143 |
| F210 | pydantic 契约层并非单一信源：4 个 schema + 7 个 skill 模型未接线，docstring 声称的消费者不存在，门内并行规则重复 | P2 | schemas/novel.py:1-2; schemas/scores.py:1-2; gates/g6.py:51; gates/g5.py:68; gates/g4/chapter_planning.py:97 |
| F211 | enums.py"所有 Literal 必须从此 import"被多处违反；decisions.Severity 与 enums.Severity 同名不同域 | P2 | contracts/enums.py:1; schemas/decisions.py:16; legacy.py:39; schemas/registry.py:26 |
| F212 | rationale 矩阵三处漂移：code 对 medium 强制 rationale，schema 文档枚举/P2.5 表与 AGENTS.md 均无 medium 行 | P2 | schemas/decisions.py:36-44; docs/framework/decisions-schema.md:66-68,84-90 |
| F213 | CONSTELLATION 校验区间 [15,35] 宽于 docstring [20,30] 与 SKILL.md 权威规则（31-35% 漏检） | P2 | contracts/skills/pacing_design.py:4,53-57; skills/shenbi-pacing-design/SKILL.md:176,187 |
| F214 | _GENRE_KEYS 声称"真实 9 键"含 tropeInventory；实际 genre-config.json 为 8 键；键集三处平行定义不一致 | P2 | contracts/ownership.py:37-50; novel-output/*/genre-config.json（实测 8 键） |
| F215 | load_registry 每次 load_contract 全量重读重解析 truth-files.yaml，实测 8.5ms/次，每派发 ≥3 次 | P2 | contracts/legacy.py:93-113,200-207 |
| F216 | dispatcher/cli.py 0% 覆盖；executor 异常路径与 SHENBI_G1_SKIP_READS 块完全未测 | P2 | d1-06 覆盖日志: dispatcher/cli.py 6-29; dispatcher/executor.py 182-201,271-282 |
| F217 | executor 对 ContractError 的静默回退（file_type→"chapter"、inputs→[]）绕过契约错误 | P2 | dispatcher/executor.py:77-80,107-116 |
| F218 | codex 模式杂项：空 prompt 抛未捕获异常、.raw 残留、stdout 双解析 | M | dispatcher/modes/codex.py:53-54,74,114-116; dispatcher/cli.py:28 |
| F219 | internal 模式报错指引误导（SHENBI_LLM_API_KEY 不会启用 dispatcher 的 API 模式） | M | dispatcher/modes/internal.py:22-24; modes/codex_api.py:1-8 |
| F220 | VALID_BASIS/VALID_SEVERITY 无消费者，与 Literal 平行双定义 | M | schemas/decisions.py:11-12 |
| F221 | ScoreReport 类名在 contracts 包内两处不同定义（skills/_scoring_base vs schemas/scores） | M | contracts/skills/_scoring_base.py:37; schemas/scores.py:31 |
| F222 | RegistryConcept.glob 字段无任何读取方 | M | schemas/registry.py:55 |
| F223 | from_markdown 首次匹配正则 + 子串场景类型识别，散文数字会污染 G4 输入 | P2 | contracts/skills/pacing_design.py:84-123; gates/g4/pacing_design.py:47 |

---

## src/shenbi/contracts/__init__.py
- 处置: deep-read
- 声称检查的不变量: [统一导入面（criterion 3）; legacy re-export 不破坏旧导入; __all__ 完整]
- findings: 无
- 验证命令: `grep -rn "from shenbi.contracts " src/shenbi --include="*.py"` — g0/g5 等经包根导入 ContractError/load_contract 正常；`src/shenbi/contract.py` 已不存在（legacy re-export 注释准确）。注意 g1.py:17/g5.py:17 直接从 `shenbi.contracts.legacy` 子模块导入，绕过统一面——风格问题不立案。
- 覆盖缺口处置: d1-06 显示 100%，无未覆盖行。
- 置信度: high

## src/shenbi/contracts/base.py
- 处置: deep-read
- 声称检查的不变量: [PureInput 无写能力; GateOutcome 纯数据 + passed/fail 工厂; frozen 不可变]
- findings: 无
- 验证命令: 读 tests/unit/contracts/test_base.py（3 tests，覆盖工厂与 frozen）。`passed()`/`fail()` 签名与 AGENTS.md "Gate functions return passed()/fail() helpers" 一致。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/enums.py
- 处置: deep-read
- 声称检查的不变量: [全框架单一词表；所有 Literal 从此 import；ALL_ENUMS 可迭代]
- findings:
  - F211 | enums.py 单一词表声明被同包违反 | error | P2 | enums.py:1 "所有 Literal 必须从此处 import"；但 schemas/decisions.py:16 定义 `Severity = Literal["low","medium","high"]` 与本文件 `Severity = Literal["BLOCKING","CRITICAL","MINOR"]`（enums.py:6）同名不同域；legacy.py:39 `OutputKind`、schemas/registry.py:26 `RegistryKind`、schemas/decisions.py:15-18 Basis/Handling/Trim 均为本地 Literal | 根因: enums.py 只收录了审计词汇域，未随 schema 层扩张更新声明或收编 | 验证: `grep -n "Severity" src/shenbi/contracts/enums.py src/shenbi/contracts/schemas/decisions.py` 输出两组不同定义 | 建议: 要么把 decisions 域词汇改名（如 DecisionSeverity）并入 enums.py，要么修订 enums.py docstring 明确管辖范围
- 验证命令: 上述 grep（输出两组 Severity 定义）；tests/unit/contracts/test_enums.py 5 tests 通过设计。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/fields.py
- 处置: deep-read（Z2 重点：字段级 reads 过滤）
- 声称检查的不变量: [规范化规则 = strip+fold U+3000/零宽+NFKC、不 lowercase; H2 提取; 缺失匹配→全文件+WARN（escape hatch）; JSON 投影]
- findings:
  - F201 | 部分匹配时静默丢弃缺失的声明字段，escape hatch 只在"全部未匹配"时触发 | error | P1 | fields.py:58-64（`if not matched` 才回退全文件；任一匹配即只返回匹配节）+ dispatch_helper.py:605（`content, _matched = filter_to_fields(...)` 丢弃 matched 标志） | 根因: escape hatch 判定用"零匹配"而非"任一声明字段缺失"，调用方又无法感知部分缺失 | 验证命令（实际运行）: `uv run python -c "...filter_to_fields(md, ['A','MISSING'], 'truth/x.md')"` 输出 `partial-with-missing: matched=True`、`MISSING section content present? False` — 无 WARN、无全文件回退；对照 AGENTS.md "Field-Level Reads" 节: "If a declared field is missing from the file, the escape hatch returns the full file + logs WARN" | 建议: 逐字段匹配，缺任一声明字段即回退全文件+WARN（或至少对缺失字段逐个 log.warning），并把 matched 信息交还调用方
- 验证命令: 上例；tests/unit/contracts/test_fields.py 9 tests 只测了全匹配/零匹配两端，未测部分匹配（测试缺口与 bug 同位）。fixture 抽查: tests/fixtures/style-profile-example.md 与 chapter-drafting 声明的 3 字段当前全匹配（bug 未被现实触发，但字段改名即触发）。
- 覆盖缺口处置: 50->52（fields 为空直通）、74-75（JSON 非对象回退）、78-79（JSON 零匹配回退）— 74-79 属 escape-hatch 错误路径，**must-test**（正是 F201 相关面）；50->52 分支 acceptable（空 fields 直通是显式契约）。
- 置信度: high

## src/shenbi/contracts/graph.py
- 处置: deep-read
- 声称检查的不变量: [parametric 精确匹配优先于 glob; dag_key glob 优先再 parametric; 与 sync_contracts/G5.2/lint 同一匹配语义]
- findings: 无
- 验证命令: 读 tests/unit/contracts/test_graph.py（3 tests）。`normalize_to_glob`/`dag_key` 签名与 sync_contracts.py:20 导入一致（grep 验证）。docstring 自述的 over-connect trade-off 是已知可接受噪声。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/legacy.py
- 处置: deep-read（实际是现行契约加载器，非"legacy"）
- 声称检查的不变量: [契约唯一加载器（loader-uniqueness）; reads dict-form {file,fields} 归一化; writes dict-form 语义元数据; 注册表解析三通道; frontmatter 解析容错]
- findings:
  - F215(共) | load_registry 无缓存，每次 load_contract 全量重读 truth-files.yaml + pydantic 校验 | optimization | P2 | legacy.py:93-113（每次调用 `REGISTRY_PATH.read_text`+`yaml.safe_load`+`model_validate`），200-207 load_contract 每次先 load_registry；executor 一次派发调 derive_file_type+derive_input_files+derive_output_files = 3 次全量加载；g0/g1/g5/dispatch_helper/phase_runner/sync_contracts 各自重复 | 根因: 无 lru_cache；truth-files.yaml 运行期不变 | 验证命令（实际运行）: `uv run python -c "50×load_contract('shenbi-chapter-drafting')"` 输出 `423.5 ms total, 8.470 ms/call` | 建议: `@lru_cache(maxsize=1)` 包 load_registry（或模块级缓存 + 显式失效入口）；requires_independent_agent（210-229）与 load_contract 重复读同一 SKILL.md，可合并
- 附注（不立案）: read_frontmatter_contract 用 `text.split("---", 2)`，frontmatter 值内含 "---" 会截断——YAML 本身也会先失败，实际不可达。`resolves()`（116-127）每次调用重建 concepts/globs/parametrics 集合，O(paths×entries) 小规模可接受。
- 验证命令: 上述基准；`grep -rn "read_frontmatter_contract" src tools` — g1.py:48 复用同一读取函数，loader-uniqueness 声明成立。
- 覆盖缺口处置: 71（fields 非 list[str] raise）、73（reads[] 非法形态 raise）— **must-test**（Layer B dict-form 契约的守卫，正是 F201 所在链路）；88（writes/updates 非法形态 raise）— must-test；103/106-109/112-113（registry 缺失/坏 YAML/坏 schema）— acceptable（环境错误，bootstrap_registry 有 FrameworkError 回退分支被 property 测试覆盖）；133/136/139-142（frontmatter 缺失/未闭合/坏 YAML）— acceptable（错误消息自描述）；151（kind 缺失）— acceptable；221/224/227-228（requires_independent_agent 的缺文件/坏 frontmatter 回退 False）— acceptable（宽松回退是有意设计）。
- 置信度: high

## src/shenbi/contracts/ownership.py
- 处置: deep-read
- 声称检查的不变量: [field/record_create/record_field 三级越权判定; 无条目返回 [] 交由 file-level 检查; fixture 亲手核对的参考键集]
- findings:
  - F214 | genre-config 键集"9 键含 tropeInventory"与现实 8 键不符，键集在 ownership/model/skill 三处平行 | error | P2 | ownership.py:37-50（`_GENRE_KEYS` 9 键 + 注释"真实 9 顶层键…fixture 亲手核对"）；contracts/skills/genre_config.py:5（"verified against real fixture (8 camelCase keys)"，模型无 tropeInventory 字段）；实测 `novel-output/xinghuo-ranqiong/genre-config.json` 与 `novel-output/test-validation/genre-config.json` 均为 8 键无 tropeInventory；而 skills/shenbi-foundation-review/SKILL.md:213,217 仍指示读 genre-config 的 tropeInventory | 根因: tropeInventory 从 genre-config 产出移除（或从未落地），ownership 矩阵与 foundation-review 指引未同步 | 验证命令（实际运行）: `uv run python -c "json.load(...)→sorted(keys)"` 两文件均输出 8 键 | 建议: 核实 tropeInventory 的真实产地；从 _GENRE_KEYS 与 foundation-review SKILL.md 移除或迁移该键；统一"genre-config 顶层键集"到一处（GenreConfig 模型字段派生）
- 验证命令: 上例；tests/unit/contracts/test_ownership.py 9 tests（含 record_create/record_field 正反路径，gates/g4 消费方 write_audit.py:57,62 已接线）。
- 覆盖缺口处置: 122（record_create 删除记录违规）、125->136、129（record_field 新增/删除违规）— 122/129 为错误分支 **must-test**（写越权检测的负路径是本模块存在意义）；125->136 partial acceptable。
- 置信度: high

## src/shenbi/contracts/paths.py
- 处置: deep-read（Z2 重点：路径解析单一源 + 信任边界）
- 声称检查的不变量: [有界 N 替换（mid-token N 不腐蚀）; per-family 语义; AC-NNN anchor; 载体行解析防穿越; isdecimal 防上标数字]
- findings:
  - F207 | ctx 缺 family 值时 family 占位符回退 chapter 语义 → 错文件 | error | P2 | paths.py:104-117（`_FAMILY_N` 命中但 `val is None` 时落入 `resolve_chapter_path(path, chapter)`） | 根因: 回退保留 legacy 行为，但 arc/stratum/volume 与章号语义不同（arc=chapter//12）| 验证命令（实际运行）: `resolve_contract_path('truth/arcs/arc-N.md', 25, PathContext(chapter=25))` → `truth/arcs/arc-25.md`（正确应为 arc-2，25//12=2），无任何 WARN | 现实触达: closure.py:162 `PathContext(chapter=total)` 仅 chapter（步骤 6/10 技能只读 chapter 族，未触雷）；但跨路由载体行（51f5458 "[path-context] line crosses all three routes"）允许只带 chapter= 的行，context-composing 读 truth/arcs/arc-N.md（SKILL.md:29）时即错读 | 建议: family 命中但值缺失时 raise UnresolvedPathError（走 resolve_or_skip 过滤）而非章号顶替
  - F208 | `count=1` 只替换首个 family 占位符，同路径第二个 family 取章号 | error | P2 | paths.py:112（`_FAMILY_N.sub(..., count=1)` 后委托 resolve_chapter_path，其 _BOUND_N 会用章号替换第二个 family 占位符） | 验证命令（实际运行）: `resolve_contract_path('audits/arc-N/volume-N.md', 25, PathContext(chapter=25, arc=2, volume=3))` → `audits/arc-2/volume-25.md`（volume 应为 3）| 现状: grep skills/*/SKILL.md 与 truth-files.yaml 未发现双 family 同路径声明，属潜伏缺陷 | 建议: 用回调替换按 group(1) 逐个查 ctx，全部 family 命中后才委托 bare-N 语义
  - F209 | NNN 替换无界，与模块 docstring"bounded regex only"表述不一致 | error | M | paths.py:143（`path.replace(_NNN, ...)` 全串替换）vs paths.py:8-10（声称只有界处替换） | 验证: 读代码对照；mid-token "NNN"（如 "BANNNE"）会被替换，现实中无此路径 | 建议: NNN 也改有界正则或修 docstring
- 验证命令: 上述三个 `uv run python -c` 实跑；tests/pipeline/test_path_context.py（13 tests）覆盖 per-family 正路径、载体行 roundtrip、防穿越、防反引用——但 F207/F208 场景（部分 ctx、双 family）无测试。
- 覆盖缺口处置: 74->71（parse 循环 continue）、151（resolve_volume_path 无占位符直通）— acceptable（test_paths.py 已覆盖主干，151 属平凡分支）。
- 置信度: high

## src/shenbi/contracts/registry.py
- 处置: deep-read
- 声称检查的不变量: [REGISTRY 自动发现约定（Report(BaseModel)，_ 前缀跳过）; bootstrap 与 load_registry 不漂移; known_skill_names 为权威技能名集]
- findings: 无独立 finding（REGISTRY 消费面问题归入 F210）
- 验证命令: `grep -rn "load_skill_contract|REGISTRY" src tools tests` — 生产消费仅 tools/generate_autocheck_docs.py:136（文档生成器），无运行时门消费（F210 证据之一）；property/contracts/test_registry_consistency.py 验证三源一致（54==54==54）；tests/unit/gates/test_g0.py:313 消费 known_skill_names（G0 接线成立）。
- 覆盖缺口处置: 32-36（bootstrap 的 FrameworkError→{} 回退）— acceptable（property 测试注释说明 pre-bootstrap 语义，错误路径难以无文件环境复现，属环境错误）；53->48、74（跳过 pkg/`_` 模块、SKILLS 不存在返回空）— acceptable（平凡守卫）。
- 置信度: high

## src/shenbi/contracts/schemas/__init__.py
- 处置: deep-read
- 声称检查的不变量: [子包 docstring 声明]
- findings: 无
- 验证命令: 读文件（2 行 docstring，无代码）。
- 覆盖缺口处置: 0 stmts，无。
- 置信度: high

## src/shenbi/contracts/schemas/adapt.py
- 处置: deep-read
- 声称检查的不变量: [ValidationError→{id,file,s,r} 微失败字典; G2.dec.2/3 旧 ID 保留; schema 故障分类（loc=='$schema' 或 msg 含 schema）]
- findings: 无
- 验证命令: 消费方 grep — g2.py:18 + g4/decisions_validator.py:20 接线确认；tests/unit/contracts/test_canaries.py Sentinel 7 静态+动态证明 g2/g4 共享同一 DecisionsDoc（含 lockstep mutation 测试）。`decisions_err_to_g2_failures` 对 loc==() + "$schema must be..." 消息（decisions.py:81-83 的 root validator）经 `'schema' in msg.lower()` 正确归类 dec.2。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/schemas/decisions.py
- 处置: deep-read（Z2 重点：单一信源）
- 声称检查的不变量: [$schema 版本锁定; P2.5 rationale 规则; extra=forbid; rationale ≤100 字符; adjustment 必带 rationale]
- findings:
  - F212(共) | rationale 矩阵 code/文档漂移：medium 未在 schema 文档与 AGENTS.md 声明，code 却强制 medium 带 rationale | error | P2 | decisions.py:36（`requires = severity in ("medium","high") or basis=="manual_override"`）；docs/framework/decisions-schema.md:66-68（selections[].severity 枚举仅 low/high）、84-90（P2.5 表无 medium 行）；AGENTS.md（"REQUIRED on manual_override + high-severity"） | 验证命令（实际运行）: `Selection(target='x', selected=['a'], basis='arc_relevance', severity='medium', omitted=[])` → REJECTED "rationale REQUIRED"（文档示例 decisions-schema.md:33-35 的 medium 调整项带 rationale，与 code 一致，但枚举节自相矛盾）| 根因: medium 语义在文档两节内部就分裂 | 建议: schema 文档补 medium 行并统一三处（或删 medium）
  - F220 | VALID_BASIS/VALID_SEVERITY 无消费者死常量 | error | M | decisions.py:11-12 | 验证: `grep -rn "VALID_BASIS|VALID_SEVERITY" src tests tools docs`（除归档 plan 文档外零引用），与 15-16 行 Literal 平行双定义 | 建议: 删除或由 Literal 派生（`get_args(Basis)`）
- 验证命令: 上例；tests/unit/contracts/schemas/test_decisions.py（9 tests，含 medium allowed、manual_override、routine-low 禁 rationale）。
- 覆盖缺口处置: 35（selection rationale 超长 raise）、58（adjustment rationale 超长 raise）— **must-test**（P2.5 长度上限是显式契约且 cheap to test）。
- 置信度: high

## src/shenbi/contracts/schemas/deps.py
- 处置: deep-read
- 声称检查的不变量: [deps.json 真实 shape（hyphenated alias + 下划线 alias）; prerequisites=成员名册; phase_of 线性查找]
- findings: 无独立 finding（无生产消费者归入 F210）
- 验证命令: `grep -rn "DepsDoc|phase_of" src` — 仅测试引用（tests/unit/contracts/schemas/test_deps.py）；docstring 自述 G3.1 已删除，phase_of 为遗留 helper。alias 方案（`Field(alias="_g4_note")` 规避 pydantic 私有属性吞键）正确。
- 覆盖缺口处置: 100%，无。
- 置信度: high（接线缺失即其定位，docstring 未虚报消费者）

## src/shenbi/contracts/schemas/hooks.py
- 处置: deep-read
- 声称检查的不变量: [6 canonical 状态; TRIGGER→TRIGGERED 折叠; 大小写不敏感; 未知/空返回 None 不抛]
- findings: 无
- 验证命令（实际运行）: `parse_hook_state('TRIGGER'/'trigger'/'planted'/'活跃'/'')` → TRIGGERED/TRIGGERED/PLANTED/None/None；fixture 快照 pending_hooks.md state=PLANTED 兼容。消费方 pipeline/chapter_loop.py:45 接线确认。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/schemas/novel.py
- 处置: deep-read
- 声称检查的不变量: [producer-authoritative target_word_count; extra=forbid 暴露漂移]
- findings:
  - F210(共) | docstring 声称 "g6 consumer / g6.py now reads it" 为假——g6 直接 dict 访问，模型零生产消费者 | error | P2 | novel.py:1-7 vs gates/g6.py:51（`_nj.get("target_word_count") or _nj.get("target_words", 100000)` 手写兜底） | 验证: `grep -rn "NovelConfig" src/shenbi/gates/` 零命中 | 根因: D26 修复只改了 g6 的键名，未真正接入模型 | 建议: g6 改用 NovelConfig（保留 legacy 键 fallback 于模型 validator），或修正 docstring 并移入待接线清单
- 验证命令: 上例；tests/unit/contracts/schemas/test_novel_scores_state.py 覆盖模型本身。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/schemas/registry.py
- 处置: deep-read
- 声称检查的不变量: [16 kind 值; version 仅 1; concepts 非空; extra=forbid; producer 默认 skill]
- findings:
  - F222 | RegistryConcept.glob 字段无读取方 | error | M | schemas/registry.py:55 | 验证: `grep -rn "\.glob\b" src`（非 Path.glob 调用）仅 patterns[].glob（graph.py:35、audit/snapshot.py:29）被读，concepts[].glob 零消费 | 建议: 删除或注明预留
- 验证命令: 上例；tests/unit/contracts/schemas/test_registry.py（15 tests）；property 三源一致性测试。
- 覆盖缺口处置: 87（version!=1 raise）— **must-test**（schema 版本防线从未验证）。
- 置信度: high

## src/shenbi/contracts/schemas/scores.py
- 处置: deep-read
- 声称检查的不变量: [_provenance alias; extra=forbid; ScoreReport 形状]
- findings:
  - F210(共) | docstring 声称 "g5 consumer" 为假——g5 用裸 dict | error | P2 | scores.py:1-2 vs gates/g5.py:68（`rdata.get("final_score", rdata.get("score", 0))`） | 验证: `grep -rn "ScoreReport" src/shenbi/gates/` 零命中 | 建议: g5 接入模型或修正 docstring
  - F221 | ScoreReport 类名双定义 | error | M | schemas/scores.py:31 vs contracts/skills/_scoring_base.py:37（字段集完全不同：dimensions/final_score vs route_c_hard_binary/route_a_score） | 验证: 读两文件类定义 | 建议: 其中一方改名（如 ScoringSheet vs T1ScoreReport）
- 验证命令: 上例；tests/unit/contracts/schemas/test_novel_scores_state.py。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/schemas/state.py
- 处置: deep-read
- 声称检查的不变量: [extra=ignore（C3 明确要求）; producer-uncontrolled 容错]
- findings: 无独立 finding（无消费者归入 F210，但其 docstring 如实说明理由与升级条件）
- 验证命令: `grep -rn "ProgressDoc|SummaryDoc" src` — 零生产消费；round-exec.sh 用 python3 直读 summary.json 佐证"shell heredoc 产出"描述。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/skills/__init__.py
- 处置: deep-read
- 声称检查的不变量: [每模块导出 Report; _ 前缀跳过发现]
- findings: 无
- 验证命令: registry.py:48-54 发现逻辑与该约定一致（`mod_info.name.startswith("_")` 跳过 _scoring_base）。
- 覆盖缺口处置: 0 stmts，无。
- 置信度: high

## src/shenbi/contracts/skills/_scoring_base.py
- 处置: deep-read
- 声称检查的不变量: [阈值从 thresholds.py 单一源 import; 0.6/0.4 权重; hard_binary 失败阻断 passed/tier 但不清零分; pass<=total]
- findings:
  - F221(共) | 见 schemas/scores.py 节 | M
- 验证命令: tests/unit/contracts/test_scoring_contracts.py 显式断言 PASS_THRESHOLD==90、TIER_ADVANCE_THRESHOLD==94、权重 0.6/0.4（与 AGENTS.md 阈值一致）；边界 90 通过、hard_binary 阻断均有测试。三个 score_*.py 均为 2 行 re-export。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/skills/chapter_planning.py
- 处置: deep-read
- 声称检查的不变量: [chapter>3 必有 typed change; hook op ∈ 4 值; defer silent>=4 需 activation]
- findings:
  - F210(共) | 模型无任何门消费，规则集与 g4/chapter_planning.py 平行且互不覆盖 | error | P2 | 本文件 15 行 `_VALID_OPS` 与 gates/g4/chapter_planning.py:97 `hook_ops = ["open","advance","resolve","defer"]` 重复定义；G4 检查 8 节/chapter_role/golden-3（g4/chapter_planning.py:36-100），模型检查 typed-change/defer-silent（本文件 26-57），两套规则无交集互不调用 | 验证: `grep -rn "from shenbi.contracts" src/shenbi/gates/g4/chapter_planning.py` 零命中 | 根因: 契约层"门从此派生"未落地，defer-silent>=4 规则实际无生产执行点 | 建议: g4/chapter_planning 接入模型（如 genre_config 先例），消除 _VALID_OPS 双定义
- 验证命令: 上例；覆盖 34%（28-36,40-44,48-57 未覆盖 = 三个 validator 全部从未在测试中触发错误路径之外的运行）。
- 覆盖缺口处置: 28-36/40-44/48-57 — **must-test**（若保留模型）或 dead-code 处置（若决定 G4 为唯一源则删除模型）——二选一，现状最差。
- 置信度: high

## src/shenbi/contracts/skills/context_composing.py
- 处置: deep-read
- 声称检查的不变量: [9 节; hook debt 带 source_file; ≤2 连续同型结尾]
- findings:
  - F210(共) | 无门消费；section_count 由调用方自报，模型无法验证真实节数 | error | P2 | gates/g4/context_composing.py 未 import 本模型（grep 验证）；`section_count: int = 0`（19 行）信任外部计数 | 验证: `grep -rn "ContextComposing" src/shenbi/gates/` 零命中 | 建议: 接入或删除；若接入改为从 sections 列表计数
- 验证命令: 上例；覆盖 38%（三个 validator 未覆盖）。
- 覆盖缺口处置: 25-27/31-34/38-43 — 同 chapter_planning：must-test 或 dead-code，二选一。
- 置信度: high

## src/shenbi/contracts/skills/foreshadowing_resolve.py
- 处置: deep-read
- 声称检查的不变量: [zone 由 cp 单源推导（GREEN<50≤ORANGE<100≤RED）; must_resolve>200; debt 与 max cp 一致; 同 hook 单 cp]
- findings:
  - F210(共) | docstring "门做 model_validate()" 为假——无门消费 | error | P2 | foreshadowing_resolve.py:49 vs `grep -rn "foreshadowing_resolve" src/shenbi/gates/` 零命中（g4/ 无该 checker import） | 验证: grep 输出空 | 附注: 模型本身质量高（tests/unit/contracts/test_foreshadowing_resolve_contract.py 含 zone 边界、hand-filled zone 忽略、三 cp 一致性），`_debt_consistent_with_hooks` 用构造 HookCP 复用 zone 排导是正确的单一源手法 | 建议: 接线到 G4 或修正 docstring
- 验证命令: 上例。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/skills/genre_config.py
- 处置: deep-read（Z2 重点：该模型是唯二接入 G4 的 skill 模型之一）
- 声称检查的不变量: [7 条可自动检查规则; 8 camelCase 字段; extra=ignore]
- findings:
  - F202 | 规则 7 在 customRules 为空时整体跳过——禁用维度无任何理由也 PASS | error | P1 | genre_config.py:93-94（`if disabled and self.custom_rules:` 空 custom_rules 直接免检） | 根因: 条件把"有规则文本才检查"写成"有规则才检查"，语义反了 | 验证命令（实际运行）: 构造 `auditDimensions:{dim0:False,...}`+`customRules:[]` → 输出 `BUG CONFIRMED: disabled dim + empty customRules PASSES validation`；权威规则 skills/shenbi-genre-config/SKILL.md:300 "每个设为 false 的维度必须在 customRules 中有对应的禁用理由规则" | 影响: g4_genre_config（gates/g4/genre_config.py:36 经 model_validate 接线）对此输入返回 PASS，G4 校验洞 | 建议: `if disabled:` 后在 custom_rules 为空时直接报每个 disabled 维度缺理由
  - F214(共) | 模型 8 字段 vs ownership 9 键（tropeInventory 缺席） | P2 | 见 ownership.py 节
- 验证命令: 上例；tests/unit/contracts/test_genre_config_contract.py（9 tests，无 disabled-dimension 用例——与 d1-06 缺口 95-100 吻合）。
- 覆盖缺口处置: 59->64、70->75（禁用/慎用词 replacements 非 list/dict 时的 isinstance 跳过）— acceptable（防御性跳过，坏 shape 由 extra=ignore+后续 jload 捕获）；95-100（规则 7 本体）— **must-test**（F202 直接证据，测试缺失掩盖 bug）。
- 置信度: high

## src/shenbi/contracts/skills/pacing_design.py
- 处置: deep-read（接入 G4 的另一个模型）
- 声称检查的不变量: [四拍齐全; 拍和=100; 三线齐全; CONSTELLATION 区间; 6-12 场景类型; 无 3 连同型]
- findings:
  - F213 | CONSTELLATION 校验区间与三处权威表述不一致且更宽 | error | P2 | pacing_design.py:55 `not 15 <= const <= 35`；docstring:4 称 "[20, 30]"；skills/shenbi-pacing-design/SKILL.md:176 "低于 20% 或高于 30% 触发警告"、:187 "PASS: … CONSTELLATION 15-30%" | 验证: 读三处对照；31-35% 区间的输出将通过校验而 SKILL.md 判 FAIL 边界（>30） | 根因: 区间数值三次手抄无单一源 | 建议: 以 SKILL.md:187 PASS 行为准（[15,30]），常量固化并让 docstring/SKILL.md 引用它
  - F223 | from_markdown 首次匹配正则 + 子串识别，散文数字污染输入 | error | P2 | pacing_design.py:86,92（`re.search(rf"{beat}.*?(\d+...)?%")` 取文档中第一个出现的百分数）、97-123（场景类型靠 20 个已知词的子串命中） | 验证: 读代码；gates/g4/pacing_design.py:47 将真实 rhythm_principles.md 全文喂入——若文档前部散文含"铺垫…40%"或列出超过 12 个已知类型词（97-123 的列表本身 20 个，正文提到 ≥13 个即触发 6-12 上限误报）即误判 | 建议: 限定在表格/标题行内匹配，或要求行首锚定
- 验证命令: 读代码 + SKILL.md 对照；tests/unit/contracts/test_pacing_design_contract.py（9 tests，from_markdown 仅单行理想输入）。
- 覆盖缺口处置: 38->42（beats 为空跳过和校验）、68->74、70->68（chapter_sequence 窗口边界）— acceptable（test_pacing_design_contract 已覆盖三连与空序列主干，分支残余为边界组合）。
- 置信度: high

## src/shenbi/contracts/skills/score_arc.py
- 处置: deep-read
- 声称检查的不变量: [Report = ScoreReport re-export; REGISTRY 发现]
- findings: 无（未接线问题归 F210 总项）
- 验证命令: `grep -rn "score_arc" src/shenbi/gates/` — gates/g4/score_arc.py 未 import 本模块（平行实现，F210 证据）。tests/unit/contracts/test_scoring_contracts.py 经 REGISTRY 覆盖。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/skills/score_stratum.py
- 处置: deep-read
- 声称检查的不变量: [同 score_arc]
- findings: 无（归 F210）
- 验证命令: 同上（gates/g4/score_stratum.py 平行实现）。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/skills/score_volume.py
- 处置: deep-read
- 声称检查的不变量: [同 score_arc]
- findings: 无（归 F210）
- 验证命令: 同上（gates/g4/score_volume.py 平行实现）。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/skills/volume_outlining.py
- 处置: deep-read
- 声称检查的不变量: [KR 3-5; 张力曲线和=100; entity hooks>=3 且类型>=2]
- findings:
  - F210(共) | 无门消费（gates/g4/volume_outlining.py 未 import） | error | P2 | 验证: `grep -rn "VolumeOutlining" src/shenbi/gates/` 零命中 | 附注: `entity_hook_types: set[str]`（22 行）JSON 往返经 pydantic 强转可用，但调用方需知道集合语义 | 建议: 接线或删除
- 验证命令: 上例；覆盖 40%（26-29,33-37,41-45 未覆盖 = 三个 validator 全未跑）。
- 覆盖缺口处置: 26-29/33-37/41-45 — must-test 或 dead-code 二选一（同 chapter_planning）。
- 置信度: high

## src/shenbi/contracts/skills/worldbuilding.py
- 处置: deep-read
- 声称检查的不变量: [最小 stub 契约]
- findings:
  - F210(共) | stub 模型（`kind: str = "artifact"` 默认 + extra=ignore）对任意 dict 都 PASS，进入 REGISTRY 造成"已有契约覆盖"的假象 | error | P2 | worldbuilding.py:8-13 | 验证: 读模型；`load_skill_contract("shenbi-worldbuilding") is not None`（tests/unit/contracts/test_registry.py:31 断言存在）而 gates/g4/worldbuilding.py（99 stmts）自有全套检查未用模型 | 建议: 补真实字段或从 REGISTRY 剔除
- 验证命令: 上例。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/contracts/thresholds.py
- 处置: deep-read
- 声称检查的不变量: [94/94/94/90/100 五常量单一源]
- findings: 无
- 验证命令: `grep -rn "from shenbi.contracts.thresholds import" src` — g3/g5/_scoring_base 消费；tests/unit/contracts/test_thresholds.py + test_scoring_contracts 断言数值与 AGENTS.md 一致。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/dispatcher/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包 docstring 指向 cli/executor]
- findings: 无
- 验证命令: 读文件（5 行 docstring）。
- 覆盖缺口处置: 0 stmts，无。
- 置信度: high

## src/shenbi/dispatcher/cli.py
- 处置: deep-read
- 声称检查的不变量: [argv>=4 用法守卫; 转发 dispatch_with_write_audit; 返回码透传]
- findings:
  - F216(共) | 整模块 0% 覆盖（d1-06: 6-29 全缺） | error | P2 | cli.py:6-29 | 验证: `grep -rln "dispatcher.cli|dispatcher/cli" tests/` 无对应测试文件（仅 executor 测试） | 根因: CLI 包装层无冒烟测试，pyproject.toml:59 入口 `shenbi-dispatch = "shenbi.dispatcher.cli:main"` 的参数解析从未被测 | 建议: 补 usage-error（argc<4→1）与转发 monkeypatch 冒烟各一
- 验证命令: 上例。
- 覆盖缺口处置: 6-29 全部 — **must-test**（见 F216）。
- 置信度: high

## src/shenbi/dispatcher/executor.py
- 处置: deep-read（Z2 重点：派发协议）
- 声称检查的不变量: [G1→G2→子代理顺序（G2 前置是设计：技能输出先于派发产生，与原 shell 脚本 6bab764:tests/dispatch-subagent.sh "the skill execution happened earlier" 注释一致）; pipeline 模式跳 G2; chapter 优先级 kwarg>载体行>正则; 崩溃后仍跑 write-audit]
- findings:
  - F204 | run_g1/run_g2 对子进程 stdout 无守卫 json.loads | error | P2 | executor.py:127,147 | 根因: 未检查 returncode/stderr；shenbi-validate 崩溃（空 stdout + traceback 在 stderr）时抛 `JSONDecodeError: Expecting value`（已实测 `json.loads('')` 抛错），丢失 stderr 上下文；shell 前身会输出 UNKNOWN+原始输出 | 验证命令（实际运行）: `uv run python -c "json.loads('')"` → `Expecting value: line 1 column 1 (char 0)`；tests/unit/test_dispatcher_executor.py:197-223 仅 mock 正常 stdout | 建议: 先查 returncode，非 0 时 log.error(stderr) 并返回 {"status":"ERROR",...}
  - F205 | 混合输出技能的 .md 在 T1 派发路由无 G2 校验 | error | P2 | executor.py:86-91（单 file_type join：truth 优先→decisions→chapter）+ gates/g2.py:83-84（decisions 分支 `if not fp.endswith(".json"): continue`） | 验证命令（实际运行）: `derive_file_type('shenbi-chapter-drafting')` → `decisions`、`derive_file_type('shenbi-context-composing')` → `decisions`；tests/round-exec.sh 无 G4 调用（grep 验证），pipeline 模式则整体跳 G2（executor.py:218-219）→ chapters/chapter-N.md 的字数/结构校验在该路由不存在 | 根因: 单技能异构输出被压成单 file_type | 建议: 按 outputs 后缀分组分别调 G2（.json→decisions、.md→kind 对应类型）
  - F217 | ContractError 静默回退 | error | P2 | executor.py:77-80（file_type→"chapter"）、107-116（inputs→[]）、_shared.py:60-61（outputs→[]） | 验证: 读代码；test_dispatcher_executor.py:109-119 明确测试并接受该行为（"meta skill" 场景），但坏契约的正式技能同样静默降级为全默认 | 建议: 至少 log.warning 区分"元技能无契约"与"契约损坏"
  - F216(共) | 182-201（SHENBI_G1_SKIP_READS 整块）与 271-282（dispatch_exception 记录路径）零覆盖 | error | P2 | d1-06 覆盖日志 | 验证: grep tests 无 SHENBI_G1_SKIP_READS 引用；271-282 是注释自称 "CRITICAL: log every exception" 的关键韧性路径 | 建议: must-test 两条（env 过滤 + dispatch 抛异常仍产 write-audit.jsonl）
- 序说明（审查中排除的疑点）: G2 在子代理派发前运行曾疑为顺序错误；经 git 考古（6bab764 的 shell 原型 G2 同样先于 scoring 派发，注释 "the skill execution happened earlier"）确认是设计——dispatch 派发的是评分子代理，技能本体输出已存在。tests/unit/dispatcher/test_executor_no_codex_api.py:47-49 亦以 pipeline-state.json 跳 G2 佐证该语义。
- 验证命令: 上列实跑 + `git show 6bab764:tests/dispatch-subagent.sh`（G2 段 81-94 先于 "Subagent Dispatch" 段）。
- 覆盖缺口处置: 44->48/57->61（缓存判空分支）acceptable；159（detect_mode internal 分支）acceptable（test_dispatcher_executor.py:168-193 已 mock 覆盖同逻辑）；170->176/174->176 acceptable；182-201 **must-test**（F216）；205-206 acceptable；212->221/215-216 acceptable（g2_failed 分支被 audit 测试间接覆盖）；225-227 acceptable（codex 分支由 detect_mode 测试+集成路径覆盖）；239-240 acceptable（ContractError 回退，建议补 warning 后转 must-test）；261->263 acceptable；271-282 **must-test**（F216）；290 acceptable（re-raise 直通）。
- 置信度: high

## src/shenbi/dispatcher/modes/__init__.py
- 处置: deep-read
- 声称检查的不变量: [包 docstring]
- findings: 无
- 验证命令: 读文件（1 行 docstring）。
- 覆盖缺口处置: 0 stmts，无。
- 置信度: high

## src/shenbi/dispatcher/modes/codex.py
- 处置: deep-read（Z2 重点：派发协议与重试参数）
- 声称检查的不变量: [空 prompt 拒绝; 600s 超时; returncode 传播; JSON 提取; safe_write 落盘; 完成记录写 progress.json]
- findings:
  - F203 | JSON 提取正则取最内层扁平对象，嵌套对象被误取 | error | P1 | codex.py:75 `re.search(r"\{[^{}]*\}", raw_text, re.DOTALL)` | 根因: `[^{}]*` 不能跨嵌套大括号；首个成功匹配是嵌套内层对象而非外层 | 验证命令（实际运行）: 对 `Here is the score: {"final_score": 90, "dimensions": {"d1": 5, "d2": 4}} done` 运行同正则 → extracted `{"d1": 5, "d2": 4}`，final_score 丢失 | 影响: 评分子代理输出含任何嵌套对象（dimensions/子项）时，scores 文件写入错误对象 → shenbi-score 拿错输入；该 bug 自 shell 原型（6bab764 tests/dispatch-subagent.sh 内嵌同正则）原样翻译继承 | 建议: 用 json.JSONDecoder().raw_decode 从每个 `{` 尝试解码取首个合法完整对象（g2.py:104-109 已有同型手法可复用）
  - F206 | progress.json 无锁 read-modify-write | error | P2 | codex.py:27-48 | 验证: 读代码；repo 已有 pipeline/filelock_utils.py 锁惯例；parallel_dispatch.py 走 dispatch_helper（API 路由）不经此路径，故需同 round_dir 并发 `shenbi-dispatch` CLI 才触发——丢 completed_skill_names 会让 g_dispatch 的完成集静默缺项 | 建议: 复用 filelock 或原子 rename 写
  - F218 | 杂项 | error | M | codex.py:53-54（空 prompt 抛 SubAgentProtocolError，而 cli.py:28 允许省略 argv[4] 默认 "" → CLI 用户得到 traceback 而非 usage 提示）；74（raw_out 读失败未捕获 FileNotFoundError）；原 shell 版有 `rm -f RAW_OUT` 而 Python 版残留 .raw；114/116 对同一 stdout 双重 json.loads | 建议: usage 级错误返回码+log；读文件 try；解析一次复用
- 重试参数说明（rubric Z2 重点）: 本文件无 tenacity/退避——429/finish_reason 处理全部位于 pipeline/dispatch_helper._dispatch_via_api（Z1/Z3 范围，本区只作边界记录：codex CLI 路由超时仅一次 600s 硬失败，无重试，SubAgentTimeoutError 语义在 exceptions 层）。
- 验证命令: 上列实跑；tests 中无 codex.py 单测（覆盖 75%，缺口即 29-30/54/67-68/71-72/77-78/81-85/112 错误分支）。
- 覆盖缺口处置: 29-30（progress.json 不存在新建）acceptable；36->38 acceptable；54（空 prompt raise）**must-test**（F218）；67-68（TimeoutExpired→SubAgentTimeoutError）must-test；71-72（codex 非零返回）must-test；77-78/81-85（无 JSON/坏 JSON 协议错误）must-test（F203 修复时应一并覆盖嵌套用例）；112（shenbi-score 非零返回）acceptable。
- 置信度: high

## src/shenbi/dispatcher/modes/codex_api.py
- 处置: deep-read
- 声称检查的不变量: [deprecated 占位符总抛 DispatcherError; 不可达性由 test_executor_no_codex_api 行为测试锁定]
- findings: 无（自述"Safe to delete entirely in the next minor release"与现状一致，保留无害）
- 验证命令: tests/unit/dispatcher/test_executor_no_codex_api.py（强制 detect_mode="codex-api" 验证 fall-through internal 且 spy 未被调）。
- 覆盖缺口处置: 100%，无。
- 置信度: high

## src/shenbi/dispatcher/modes/internal.py
- 处置: deep-read
- 声称检查的不变量: [硬拒绝、无 LLM 后端、不伪造评分]
- findings:
  - F219 | 错误指引误导 | error | M | internal.py:22-24 "Set SHENBI_LLM_API_KEY to use API mode" — dispatcher 执行器根本没有 API 路由（codex_api.py:1-8 明示 API 派发在 pipeline/dispatch_helper._dispatch_via_api，不经 shenbi-dispatch CLI） | 验证: 读 executor.py:221-230（仅 codex/internal 两分支）| 建议: 改为"install codex CLI or use the pipeline (SHENBI_LLM_API_KEY applies to pipeline dispatch only)"
- 验证命令: 上例；tests/unit/test_dispatcher_executor.py:179-193 覆盖 detect_mode 回退 internal。
- 覆盖缺口处置: 100%，无。
- 置信度: high

---

## 汇总统计

- 覆盖文件: 38/38（Z2.files 全部，无遗漏）
- findings: 23 条 — P1×3（F201 字段过滤部分匹配静默丢字段、F202 genre-config 禁用维度绕过、F203 codex 嵌套 JSON 误取）、P2×14、M×6
- 实际运行过的验证命令: 均在各节"验证命令"注明（`uv run python -c` 行为验证 ×7 组、git 考古 ×3、grep 调用方 ×15+、fixture 比对 ×4）；未运行 pytest / shenbi-dispatch / pipeline 任何子命令
- 低置信度文件: 无
- 未覆盖文件: 无

### F210 汇总明细（契约单一信源——Z2 重点维度结论）
"门/helpers/文档从此派生"（contracts/__init__.py:1-3）目前仅部分成立:
- 已接线: DecisionsDoc（g2+g4，Sentinel 7 锁定）、GenreConfig（g4）、PacingDesign（g4）、TruthFilesRegistry（legacy/graph/sync_contracts）、HookState（chapter_loop）、thresholds/enums/ownership/paths/fields（多消费方）。
- 未接线（docstring 声称的消费者不存在或无任何消费者）: schemas/novel.py（称 g6 读、实为裸 dict）、schemas/scores.py（称 g5 读、实为裸 dict）、schemas/state.py、schemas/deps.py、contracts/skills/{chapter_planning, context_composing, volume_outlining, foreshadowing_resolve, worldbuilding(stub), score_arc, score_stratum, score_volume}——REGISTRY 的唯一生产消费是 tools/generate_autocheck_docs.py:136 文档生成器。
- 平行定义实例: _VALID_OPS（模型 15 行 vs g4/chapter_planning.py:97）、genre-config 键集（ownership 9 键 vs 模型 8 字段 vs 真实文件 8 键）、CONSTELLATION 区间四处（F213）、rationale 矩阵三处（F212）。
