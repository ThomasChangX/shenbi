# Z10 分区初审报告（2026-08-15 全项目深度审查）

- 分区: Z10 — CI/workflows、tools/ 脚本、benchmarks/、根级配置（pyproject/justfile/uv.lock/executor_config/run_pipeline.sh/mkdocs/cliff/gitignore/pre-commit/yamllint/LICENSE）、scripts/、plugins/
- 审查 agent: Z10 只读初审（编号段 F1001–F1099）
- 文件清单: `docs/superpowers/audit-runs/2026-08-15/zones/Z10.files`（53 个文件，全部覆盖，见文末统计）
- 审查日期: 2026-08-15
- 只读约束遵守: 未创建/修改/删除除本报告外的任何仓库文件；未 git add/commit；未执行 pytest；未执行 shenbi-dispatch/pipeline。

## 运行门面对照矩阵（rubric 1/3 核心证据）

四个执行面（justfile `check` / ci.yml quality+codegen / .pre-commit-config.yaml / tools/pre-push-check.sh）逐步对照：

| 检查 | just check | ci.yml | pre-commit | pre-push |
|---|---|---|---|---|
| uv lock --check | ✗ | ✓ (ci.yml:45) | ✗ | ✓ (:10) |
| ruff check / format --check | ✓ (:18-19) | ✓ (:47-48) | ✓ | ✓ |
| mypy src/shenbi/ | ✓ (:20) | ✓ (:49) | ✓ (mirrors) | ✓ |
| basedpyright | ✓ (:21) | ✓ (:50) | ✓ | ✓ |
| lint_status_strings | ✓ (:15) | ✓ (:52) | ✓ | ✓ |
| lint_contract_graph（ORPHAN_READ 阻断） | ✓ (:53) | **✗** | ✗ | ✗ |
| scripts/lint_contract_fields | ✓ (:54) | **✗** | ✗ | ✗ |
| lint_contracts | ✓ (:55) | ✓ (:55) | ✓ | ✓ |
| lint_repo_consistency | ✓ (:16) | ✓ (:56) | ✓ | ✓ |
| lint_no_forbid_with_computed_field | **✗** | ✓ (:58) | ✓ | ✓ (:32) |
| lint_no_fs_mutation | **✗** | ✓ (:60) | ✓ | ✓ (:34) |
| check_fixture_mirror | ✗ | **✗** | ✓ (:81) | ✗ |
| sync-contracts 幂等 diff | ✓ (:22) | ✓ (:81,88) | ✓ | ✓ (:89-90) |
| autocheck-docs 幂等 diff | **✗** | ✓ (:83,88) | ✓ (:86) | ✓ (:94-95) |
| generate-plugins 幂等 diff | ✗ | ✓(:85,89)（恒空，见 F1003） | ✗ | ✗ |
| pytest not last | ✓ (:23) | ✓ (:62-64) | ✗ | ✓ (:70) |
| pytest last（--no-cov） | ✓ (:24) | ✓ (:66) | ✗ | ✓ (:85) |
| pip-audit | ✗ | security.yml | ✗ | ✓ (:39) |
| yamllint workflows --strict | ✗ | ✓ (:108) | ✓（全部 yaml） | ✗ |
| mkdocs build --strict | ✗ | docs.yml | ✗ | 条件 ✓ (:48) |

pytest flag 级漂移：ci.yml:63-64 用 `--dist loadscope --timeout=120`；justfile:23 无 `--dist`/`--timeout` 覆盖（落 addopts 的 60s）；pre-push:70 有 loadscope+120 但无 `--hypothesis-profile=ci`。均不改变通过性语义，记 M 级（并入 F1002 备注）。

---

## Per-file 报告

### .github/workflows/ci.yml
- 处置: deep-read
- 声称检查的不变量: [quality job 步骤与 justfile check 一致；codegen-idempotency 的 git diff 能捕获全部三个生成器漂移；continue-on-error 只影响 3.13/macos；依赖检查最小权限]
- findings: [F1001, F1002, F1003]
  - **F1001** | CI 缺 just lint-contracts 的两个阻断 lint（graph closure + field drift） | error | P1 | 证据 `.github/workflows/ci.yml:53-56`（只跑 lint_contracts.py + lint_repo_consistency.py）vs `justfile:52-55`（lint_contract_graph.py + scripts/lint_contract_fields.py + lint_contracts.py）；lint_contracts.py 本体（tools/lint_contracts.py:75-84）不内部调用另两个 | 根因: lint-contracts 拆分为三脚本后 CI 步骤未同步；lint_contract_graph.py:7 自述 "the marquee CI mechanism" 但 CI 从不运行它 | 验证: `grep -rn "lint_contract_graph" .github/ .pre-commit-config.yaml` → 0 命中；`uv run python tools/lint_contract_graph.py` → RC=0（仅 DANGLING_WRITE warn）；单元测试 tests/unit/tools/test_lint_contract_graph.py 用合成契约集，不对真实仓库跑闭包 | 建议: ci.yml quality 增加 `uv run python tools/lint_contract_graph.py` 与 `uv run python scripts/lint_contract_fields.py`（或直接 `just lint-contracts`）
  - **F1002** | just check 缺 CI 强制步骤 → 本地绿 CI 红 | error | P1 | 证据 `justfile:14-24`（check recipe）缺 ci.yml:44-45 `uv lock --check`、ci.yml:57-60 两个 lint、ci.yml:83 `generate_autocheck_docs.py` 幂等 | 根因: 检查加入 CI/pre-commit/pre-push 时未回填 justfile；AGENTS.md:PR 协议称 "just check — Run all CI checks locally"，实际覆盖 ~70% | 验证: 上文矩阵；`uv run python tools/lint_no_fs_mutation.py src/shenbi` 单独运行 RC=0（当前无实际红灯，但门面缺口成立） | 建议: just check 回填 4 项缺失步骤
  - **F1003** | codegen-idempotency 对 `.codex-plugin/` 的 diff 恒空 | error | P1 | 证据 ci.yml:84-89 运行 `shenbi-generate-plugins` 后 `git diff --exit-code -- ... .codex-plugin/`；`.gitignore:20` 忽略 `.codex-plugin/` | 根因: gitignored 目录中的生成文件是 untracked，永不出现在 `git diff` 中 → "Plugin manifests (.codex)" 新鲜度检查不可能失败（仅生成器 crash 会被捕获）；与 F1004 复合后，清单漂移完全不可见。P0 候选（"CI 门形同虚设"），因该 job 其余 diff 路径仍真实生效且发布产物不含 plugin.json，定 P1 | 验证: `git ls-files .codex-plugin/` → 空；`git check-ignore -v .codex-plugin/` → `.gitignore:20:.codex-plugin/`；`ls .codex-plugin/` → 目录不存在 | 建议: 提交生成产物，或改为「生成后与已存产物哈希比对」的显式断言
- 验证命令: [矩阵逐项 grep 对照；`uv lock --check` → Resolved 180 packages（绿）；`uv run yamllint --strict .github/workflows/` → RC=0]
- 置信度: high

### .github/workflows/codeql.yml
- 处置: deep-read
- 声称检查的不变量: [触发面 = push+weekly+manual 且有注释依据；permissions 最小化；security-and-quality 套件]
- findings: [F1006]
  - **F1006** | `on: push` 无 branches 过滤 → 任意分支每次 push 触发全量 CodeQL 分析 | optimization | P2 | 证据 codeql.yml:12-15（`on: push` 无过滤；对照 ci.yml:3-4 有 `branches: [main]`） | 根因: 为绕开 PR-diff 300 文件上限选择 push 扫描（注释 :9-11 说明动机），但未限制分支 → 每个特性分支的每次 push 都跑全量 python 分析 | 验证: 未验证（静态读；分支过滤属 YAML 语义，无需运行） | 建议: `branches: [main]` + 保留 schedule/manual，或接受成本并注明
- 权限核验: permissions 块（:21-24）= actions:read + contents:read + security-events:write，最小化正确。
- 验证命令: [diff 对照 ci.yml 触发面]
- 置信度: high

### .github/workflows/docs.yml
- 处置: deep-read
- 声称检查的不变量: [PR 只 build 不 deploy；deploy 仅 main push；libcairo 依赖显式安装；docs 组与 pyproject [dependency-groups].docs 一致]
- findings: [F1026]
  - **F1026** | 无 concurrency 取消组 → 同 PR 连续 push 堆积 docs build | optimization | M | 证据 docs.yml:1-8（无 concurrency 块；对照 ci.yml:7-9 有） | 根因: 遗漏 | 验证: 未验证（静态） | 建议: 复制 ci.yml 的 concurrency 组
- 其余: deploy 条件 `github.ref == 'refs/heads/main' && github.event_name == 'push'`（:23）正确防 PR 误部署；permissions contents:write 仅 deploy job（:25-26）最小化正确。
- 验证命令: [uv.lock docs 组核验：mkdocs==1.6.1 / mkdocs-material==9.7.6 / mkdocstrings==1.0.4 / pymdown-extensions==11.0 全部在锁内]
- 置信度: high

### .github/workflows/embeddings-smoke.yml
- 处置: deep-read
- 声称检查的不变量: [触发仅 schedule+manual（不跑 PR）；dev 组含 sentence-transformers+torch；模型缓存 key 正确；assert shape (2,384) 与 all-MiniLM-L6-v2 维度一致]
- findings: 无
- 备注: dev 组依赖声明与 pyproject.toml:47 一致（已核）；daily cron 07:00 UTC 与 nightly 06:00、autoupdate 周一 02:00 错峰合理。
- 验证命令: [uv.lock 核验 sentence-transformers==5.6.0, torch==2.13.0 在锁内]
- 置信度: high

### .github/workflows/nightly.yml
- 处置: deep-read
- 声称检查的不变量: [禁用状态与注释一致（schedule 注释掉，仅 manual）；三个 job 的依赖安装正确；3.13 job 无 continue-on-error]
- findings: 无
- 备注: `windows-smoke` 用 `--cov-fail-under=85`（:39）与 pyproject [tool.coverage.report] fail_under=85 一致；禁用理由（:2-17）自洽；触发面=workflow_dispatch 仅手动，无"不该触发却触发"。
- 验证命令: [静态读 + 对照 pyproject coverage 配置]
- 置信度: high

### .github/workflows/pre-commit-autoupdate.yml
- 处置: deep-read
- 声称检查的不变量: [周更 + manual；permissions contents+pull-requests write 最小够用；rc>1 才阻断 PR]
- findings: [F1007]
  - **F1007** | exit-code 语义解释错误：rc==1 被当 "autofix 预期" 放行 | error | P2 | 证据 pre-commit-autoupdate.yml:26-34（注释断言 "1 = hooks autofixed files；>1 = real failure"） | 根因: pre-commit 实际退出码语义为 0=全过、1=任意 hook 失败（含 autofix 修改文件与真实失败两种情形）、2+=内部错误；新 rev 不兼容导致的真实 lint 失败同样以 1 退出 → 会被误判为 autofix 而创建带坏 hook 的周更 PR | 验证: 未验证（基于 pre-commit 公开退出码语义推断；本机未实际复现 rc==1 分支） | 建议: 用 `pre-commit run --all-files` 后 `git diff --exit-code` 判定 autofix 之外，再以输出中 "Failed" 行判定真实失败
- 验证命令: [静态读]
- 置信度: medium（退出码语义高置信，具体失败复现未做）

### .github/workflows/release.yml
- 处置: deep-read
- 声称检查的不变量: [仅 tag v* 触发；HEAD^ 不存在时兜底；SBOM+wheel+sdist 进 release；permissions contents:write 最小]
- findings: [F1021]
  - **F1021** | release notes 用裸 `git log --format='- %s (%h)'` 生成，绕过 cliff.toml 分组/过滤 | error | P2 | 证据 release.yml:23-38 vs cliff.toml:29-36（commit_parsers 分组）及 justfile:98-99（`just changelog` 走 git-cliff） | 根因: 两套 changelog 机制并存未统一 → GitHub Release notes 与 CHANGELOG.md 分组、`filter_unconventional=false` 语义不一致（release notes 混入 merge commit/非规范消息原文） | 验证: `ls CHANGELOG.md` 存在；两文件对照读 | 建议: release 步骤改用 `git-cliff --latest` 或 `--unreleased` 生成 body
- 其余: `git describe --tags --abbrev=0 HEAD^`（:28）从父提交找前 tag，避免匹配刚推的 tag，正确；首提交/无前 tag 两级兜底（:25-34）完整。
- 验证命令: [静态读]
- 置信度: high

### .github/workflows/security.yml
- 处置: deep-read
- 声称检查的不变量: [push main + PR 触发；pip-audit 跑 dev 组环境；SBOM 上传 artifact]
- findings: 无
- 备注: 无 permissions 块（默认 token 权限），upload-artifact 无需显式写权限，可接受；无 schedule 定期审计（CodeQL 有 weekly，pip-audit 仅事件驱动）——P2 以下观察，不计 finding。
- 验证命令: [uv.lock 含 pip-audit==2.10.1, cyclonedx-bom==7.3.0]
- 置信度: high

### .github/CODEOWNERS
- 处置: deep-read
- 声称检查的不变量: [默认 owner 覆盖全仓；分区 owner 与实际维护结构一致]
- findings: [F1028]
  - **F1028** | 所有分区条目与默认 `*` 同一 owner，冗余 | optimization | M | 证据 .github/CODEOWNERS:2（`* @ThomasChangX`）与 :5-12（src/tests/docs/skills 同 owner） | 根因: 单人仓库占位结构 | 验证: 未验证（静态） | 建议: 保留 `*` 一行即可，分区条目留作扩展位可加注释
- 置信度: high

### .github/dependabot.yml
- 处置: deep-read
- 声称检查的不变量: [uv + github-actions 两生态；direct-only 过滤生效；commit-message 符合 conventional（chore(deps):）]
- findings: 无
- 备注: `allow: dependency-type: "direct"`（:18-19）与注释中 PR#20 僵尸 torch PR 的根因陈述自洽；labels/prefix 配置符合 AGENTS.md 提交约定（chore）。
- 验证命令: [静态读]
- 置信度: high

### .github/SECURITY.md
- 处置: deep-read
- 声称检查的不变量: [指向仓库根 SECURITY.md 且目标存在]
- findings: 无
- 验证命令: [`ls SECURITY.md` → 存在于仓库根]
- 置信度: high

### .github/ISSUE_TEMPLATE/config.yml
- 处置: deep-read
- 声称检查的不变量: [blank issues 关闭；contact link URL 与 repo 一致]
- findings: 无
- 备注: discussions URL `ThomasChangX/shenbi` 与 mkdocs.yml repo_url 一致。
- 置信度: high

### .github/ISSUE_TEMPLATE/bug.md
- 处置: deep-read
- 声称检查的不变量: [模板命令可执行：`import shenbi; print(shenbi.__version__)`]
- findings: 无
- 验证命令: [grep `src/shenbi/__init__.py:12` → `__version__ = "0.1.0"` 存在]
- 置信度: high

### .github/ISSUE_TEMPLATE/feature.md
- 处置: deep-read
- 声称检查的不变量: [结构完整（问题/方案/备选）]
- findings: 无
- 置信度: high

### .github/ISSUE_TEMPLATE/skill-proposal.md
- 处置: deep-read
- 声称检查的不变量: [字段与 AGENTS.md 技能契约对齐（reads/writes、phase 枚举）]
- findings: 无
- 备注: phase 列表（genesis, architecture, planning, drafting, audit, foundation, management）与 pipeline 阶段命名一致（spot-check src/shenbi/pipeline/genesis.py 步骤技能均可归入）。
- 置信度: medium

### .github/PULL_REQUEST_TEMPLATE.md
- 处置: deep-read
- 声称检查的不变量: [checklist 与仓库强制项一致（no print / no ignore_errors / CHANGELOG / ADR / 三件套 lint）]
- findings: 无
- 备注: checklist 引用的 `ruff check, mypy, basedpyright` 与实际门一致；"pytest passes locally" 措辞弱于 CI 实际双段（not last + last）执行，M 级不计。
- 置信度: high

### .gitignore
- 处置: deep-read
- 声称检查的不变量: [round 产物忽略+模板保留；coverage/site/SBOM/mutants 忽略；无必要文件被误忽略]
- findings: [F1019, F1020]
  - **F1019** | `novel-output/` 先忽略后双重取反，第一行为死配置 | error | P2 | 证据 .gitignore:91（`novel-output/`）+ :96-97（`!novel-output/` + `!novel-output/**`） | 根因: 从"忽略"翻转为"跟踪"时保留旧行 → 净效果为全跟踪（`git ls-files novel-output/` 有 3+ 文件），:91 是死行且误导读者/工具（ruff exclude 也保留 novel-output/，pyproject.toml:108-110） | 验证: `git ls-files novel-output/` → test-validation/ 下多文件已跟踪；`git check-ignore novel-output` → 无命中 | 建议: 删除 :91 死行与注释矛盾处
  - **F1020** | 缺 run_pipeline.sh 产物忽略模式（`novel-<user>-<ts>/` 目录与 `pipeline.log`） | error | P2 | 证据 run_pipeline.sh:6（默认 `PROJECT_DIR=novel-${USER}-$(date ...)`）与 :13-14（在仓库根 mkdir + tee pipeline.log）vs .gitignore 全文无对应模式 | 根因: 脚本产物落在仓库根（cd "$SCRIPT_DIR"）而非 project-dir 下 | 验证: `git check-ignore -v novel-xiaotiac-test-20260815 pipeline.log` → 均 RC=1（未忽略） | 建议: 增加 `novel-*/`（排除 novel-output）与 `pipeline.log`，或让 run_pipeline.sh 产物统一入 staging/
- 备注: `.venv/` 未列入但 uv 自动生成 `.venv/.gitignore`（含 `*`）兜底，实测 `git check-ignore --no-index .venv/` 命中，不计 finding；`!tests/coverage/.gitkeep`（:74）对已跟踪文件有效（git ls-files 确认 .gitkeep 被跟踪），非 bug。
- 验证命令: [git check-ignore / git ls-foods 系列，见上]
- 置信度: high

### .pre-commit-config.yaml
- 处置: deep-read
- 声称检查的不变量: [hook 集合与 CI 覆盖互补不冲突；rev 与锁定版本一致性；files/exclude 过滤正确]
- findings: [F1008]
  - **F1008** | yamllint hook rev v1.33.0 落后锁定版本 1.38.0 两个 minor | deps | P2 | 证据 .pre-commit-config.yaml:42（`rev: v1.33.0`）vs uv.lock yamllint==1.38.0（CI action-validation 用 `uv run yamllint`，justfile 无） | 根因: autoupdate 周更 workflow 存在但 rev 未随之更新（或 workflow 未跑过） | 验证: `grep 'name = "yamllint"' -A1 uv.lock` → 1.38.0；pyproject.toml:44 `yamllint>=1.33.0` 下限即 hook rev，属巧合性一致 | 建议: bump rev 至 v1.38.0 或依赖 autoupdate PR 真正落地
- 其余核验: ruff-pre-commit rev v0.15.17 == uv.lock ruff==0.15.17 ✓；mirrors-mypy v2.1.0 == uv.lock mypy==2.1.0 ✓；check-yaml 排除 mkdocs.yml（!!python/name 标签）理由正确；contract-sync-idempotency hook（:75-76）diff 范围与 justfile:22 一致（都不含 .codex-plugin/，与 ci.yml 差一个恒空路径——见 F1003）。
- 验证命令: [rev vs uv.lock 三组对照]
- 置信度: high

### .yamllint
- 处置: deep-read
- 声称检查的不变量: [workflows 在 --strict 下可过（line-length 100 warning 级 + strict = 失败级）；truthy check-keys off 容纳 `on:`]
- findings: 无
- 验证命令: [`uv run yamllint --strict .github/workflows/` → RC=0；先用 awk 字节长度误报（nightly.yml:2/17 185 bytes），实为 CJK/box-drawing 多字节——yamllint 按码点计，复核通过]
- 置信度: high

### LICENSE
- 处置: deep-read
- 声称检查的不变量: [标准 MIT 文本；版权人/年份；与 pyproject license 文本一致]
- findings: 无
- 验证命令: [head/tail 通读 21 行，MIT 2026 ThomasChangX；pyproject.toml:6 `license = { text = "MIT" }` 一致；plugins/master.json:7 license MIT 一致]
- 置信度: high

### benchmarks/anchors/AC-001.md
- 处置: deep-read
- 声称检查的不变量: [frontmatter schema（id/category/source_work/source_ref/calibrates）完整；评分带与 anchor 语料统一（88-97/75-87/<75）；calibrates 维度名存在于技能 rubric 词汇]
- findings: 无
- 备注: AC-001 是 shenbi-anchor-curate SKILL.md:54 明确引用的格式模板（已核）；"禁止默认 95" 对应 scoring 反通胀政策；genesis.py:79 步骤 16 的 output_path 解析为 project-dir 相对（不覆盖本 tracked 文件）。
- 验证命令: [grep skills/shenbi-anchor-curate/SKILL.md 引用；grep src/ 维度消费面（review-resonance / review-arc-payoff / review-highpoint SKILL.md 命中 情感落地/爽点 等）]
- 置信度: high

### benchmarks/anchors/AC-002.md … AC-009.md（8 个）
- 处置: deep-read（frontmatter + 正文全读）
- 声称检查的不变量: [同 AC-001：schema 完整、评分带统一、维度可追溯到技能 rubric]
- findings: [F1029]
  - **F1029** | AC-003 正文维度标签与 frontmatter calibrates 记法不一致 | error | M | 证据 benchmarks/anchors/AC-003.md:6（`calibrates: [弧段/卷级.高光, 卷级.伏笔纪律]`）vs 正文 "卷级高光/伏笔纪律 88-97"（斜杠合写、缺 弧段 层级） | 根因: 手写正文未回贴 frontmatter 规范记法 | 验证: 两行并读 | 建议: 正文维度带统一用 dotted 记法
- 其余 7 个（AC-002/004/005/006/007/008/009）：schema 完整、评分带统一、无 finding。
- 验证命令: [逐文件 head -8 + 正文 sed 全读]
- 置信度: high

### benchmarks/anchors/AC-010.md
- 处置: deep-read
- 声称检查的不变量: [同上；增强版带描述（括号判据）格式自洽]
- findings: 无
- 备注: 与 AC-011 同为"带判据"富格式，与 AC-001..009 简式并存——格式双轨但字段同构，不计 finding。
- 置信度: high

### benchmarks/anchors/AC-011.md
- 处置: deep-read
- 声称检查的不变量: [同上]
- findings: 无
- 置信度: high
- 备注（rubric 5 总评）: anchors 是 LLM 校准语料（无 src 机器解析 `calibrates`，仅 paths.py:28 解析 `AC-NNN` 路径占位符），基准带与 skills rubric 维度（章级/卷级/弧段/大弧级.*）经抽查可对应；无 CI 校验 anchor frontmatter schema 属可接受的数据文档态。

### cliff.toml
- 处置: deep-read
- 声称检查的不变量: [commit_parsers 覆盖 AGENTS.md 全部 5 类提交前缀；Keep a Changelog header；与 `just changelog` 一致]
- findings: 无（分组缺口由 F1021 关联，不重复计）
- 备注: feat/fix/docs/refactor/test/chore 六组 ⊇ AGENTS.md 五类 ✓；`filter_unconventional = false` 保留非规范提交（会落入无分组段）——M 级观察。
- 验证命令: [对照 AGENTS.md Commit Conventions 五前缀]
- 置信度: high

### executor_config.toml
- 处置: deep-read
- 声称检查的不变量: [[default]/[overrides."skill"] 结构与 dispatch_helper._load_executor_config 消费面一致；18 个 override 技能名全部存在于 skills/；drafting max_tokens=32768 < cap-raise 天花板余量；温度分层合理（评分 0.1、评审 0.1-0.2、修订 0.6、起草 0.85）]
- findings: 无
- 验证命令: [18 个技能名逐一 `[ -d skills/<name> ]` → 无 MISSING；`sed -n 74-84p src/shenbi/pipeline/dispatch_helper.py` → `_MODEL_OUTPUT_CEILING = 65536`，注释明确 32768 < 58982 余量；tests/pipeline/test_executor_config.py 存在守护]
- 置信度: high

### justfile
- 处置: deep-read
- 声称检查的不变量: [check 与 CI 一致（见 F1001/F1002，违反）；recipe 引用路径存在；pipeline/dispatch/gate recipe 与 CLI 参数面一致]
- findings: [F1002（主证之一）, F1025]
  - **F1025** | mutate-check 守卫提示过时 | error | M | 证据 justfile:92-95（提示 "requires Plan 2"，但 `tools/compare_mutation_score.py` 与 `tests/baselines/mutation-score.txt` 均已存在） | 根因: Plan 2 落地后未清理守卫 | 验证: `ls tools/compare_mutation_score.py tests/baselines/mutation-score.txt` → 均存在 | 建议: 移除守卫或改提示
- 其余核验: `test`/`test-all`/`test-file`/`fix`/`gate`/`dispatch`/`pipeline-*` 的参数面与 pyproject [project.scripts]（:57-68）入口一一存在；`pipeline-init` 的 just 表达式拼接 `--project-dir` 未加引号（含空格路径会破）——M 级观察。
- 验证命令: [grep [project.scripts] 对照；矩阵见文首]
- 置信度: high

### mkdocs.yml
- 处置: deep-read
- 声称检查的不变量: [nav 13 个文件全部存在于 docs/；exclude_docs 只排除非发布物；mermaid custom fence 用 !!python/name（pre-commit check-yaml 已豁免）]
- findings: 无
- 验证命令: [13 个 nav 路径逐一 `[ -f ]` → 无 MISSING；docs/superpowers/audit-runs/ 为 exclude 目标（:65-66），本报告不受 strict build 影响（排除理由注释 :60-64 完整）]
- 置信度: high

### plugins/master.json
- 处置: deep-read
- 声称检查的不变量: [skills 列表覆盖 skills/ 全部技能；version 与包版本同步；platforms 生成配置可被 generate.py 消费]
- findings: [F1004, F1005]
  - **F1004** | 技能清单陈旧：列 59，磁盘 74，缺 15 个技能 | error | P1 | 证据 plugins/master.json:8-68（59 条）vs `ls skills/`（74 个含 SKILL.md 目录） | 根因: master.json 为手工维护的 "single source of truth"（src/shenbi/plugins/generate.py:3），无目录比对校验（generate.py 仅做 REQUIRED_FIELDS 结构校验 :21-33）；叠加 F1003（CI 幂等门恒空）后漂移不可见。缺失名单：shenbi-anchor-curate、shenbi-book-spine-init、shenbi-escalation-review、shenbi-foreshadowing-lifecycle、shenbi-foreshadowing-recall、shenbi-memory-distill、shenbi-review-arc-payoff、shenbi-review-group-{character,craft,factual,plan}、shenbi-review-resonance、shenbi-score-{arc,stratum,volume} —— 即整个 score-* 评分子系统和 group-review 家族（占技能库 20%）。若 .codex-plugin 产物对外发布即构成静默契约违反（P0 候选）；当前发布产物（release.yml 仅 wheel/sdist/SBOM）不含 plugin.json，故定 P1 | 验证: `python3 -c`（master 集合 vs 磁盘集合 diff）→ missing 15 / extra 0 | 建议: 清单生成化（从 skills/ 目录派生）或加 lint（master ⊆ 磁盘且磁盘 ⊆ master）
  - **F1005** | version 0.2.0 与 pyproject/__init__ 0.1.0 漂移 | error | P2 | 证据 plugins/master.json:3 vs pyproject.toml:3、src/shenbi/__init__.py:12（双源均 0.1.0） | 根因: 手维护版本第二源 | 验证: 三处并读 | 建议: 生成时读包版本
- 置信度: high

### pyproject.toml
- 处置: deep-read
- 声称检查的不变量: [依赖声明与 uv.lock 一致（uv lock --check 绿）；scripts 入口与 src 模块对应；coverage fail_under=85 与 AGENTS.md 一致；ruff/mypy/basedpyright 配置自洽；mutmut 键名正确（下划线）]
- findings: [F1009, F1024]
  - **F1009** | pytest-ordering 0.6 死依赖且与 pytest-randomly 语义冲突 | deps | P2 | 证据 pyproject.toml:33（`pytest-ordering>=0.6`）+ :38（`pytest-randomly>=3.15.0`） | 根因: ordering 插件自 2016 未维护（后继为 pytest-order）；全仓 grep 无 `pytest.mark.order`/ordering 使用（"last" 语义用双段 `-m` 调用实现，justfile:23-24）；两个 order-类插件并存（randomly 打乱 vs ordering 固定）属隐患 | 验证: `grep -rln "pytest.mark.order|pytest_ordering|pytest-order" tests/ src/` → 0 命中（除 pyproject 声明本身） | 建议: 移除 pytest-ordering
  - **F1024** | mypy `python_version = "3.12"` 高于支持底线 3.11 | optimization | P2 | 证据 pyproject.toml:359 vs :8（`requires-python = ">=3.11"`）；basedpyright 反而用 3.11（:378） | 根因: 类型检查基准与运行底线错位 → 3.12+ 专用 API 通过 mypy 而 3.11 运行时炸（由 CI ubuntu 3.11 门兜底，故 P2 非 P1） | 验证: 两配置并读 | 建议: mypy python_version 改 "3.11" 与 basedpyright 对齐
- 其余核验: sentence-transformers 双声明（optional.embeddings + dev）为 embeddings-smoke.yml:5 注释声明的有意重复 ✓；jieba==0.42.1 唯一精确 pin 有 frozen tokenize property 测试语境支撑 ✓；[tool.mutmut] 下划线键名与注释（:97-103）一致 ✓。
- 验证命令: [`uv lock --check` → Resolved 180 packages 无漂移；锁定版本抽查 jieba 0.42.1 / openai 2.44.0 / mypy 2.1.0 / ruff 0.15.17 / basedpyright 1.39.7 全部满足声明范围]
- 置信度: high

### run_pipeline.sh
- 处置: deep-read
- 声称检查的不变量: [set -euo pipefail 全覆盖；JSON grep 模式与 pipeline CLI 实际输出格式匹配；错误分支不误放行；状态机推进逻辑正确]
- findings: [F1013, F1014]
  - **F1013** | `$PROJECT_DIR` 字符串内插进 python3 -c 源码 | security | P2 | 证据 run_pipeline.sh:31-38 与 :70-79（`open('$PROJECT_DIR/pipeline-state.json')`） | 根因: 路径未走 argv/env 传递 → 含单引号或反斜杠的 PROJECT_DIR 使内嵌 Python 语法错误（或注入任意代码；参数来自本机调用者，威胁模型弱，主要影响是静默 `except: print('unknown')` 吞错后走 `2>/dev/null`，CURRENT_STEP 恒 unknown） | 验证: 未验证（静态边界分析） | 建议: `PROJECT_DIR="$PROJECT_DIR" python3 -c '...os.environ...'`
  - **F1014** | error 分支 `grep -q "escalation\|gate\|dispatch"` 过宽 | error | P2 | 证据 run_pipeline.sh:88-91 | 根因: 本框架几乎所有错误消息含 "gate"/"dispatch" 字样 → 任意 error 被自动 approve，绕过 checkpoint 审查语义（与脚本自述 "Auto-approves checkpoints" 部分一致，但 error≠blocked 也被放行） | 验证: 未验证（静态） | 建议: 收紧为结构化判定（如 status 字段 + checkpoint 存在性）
- 其余核验（正向）: `set -euo pipefail`（:4）✓；`OUTPUT=$(...) || true` + `|| echo ""` 吞 grep 无匹配的 pipefail ✓；grep 模式 `"status": "` 与 `cli_utils.emit_json` 的 `json.dumps` 默认分隔符（`: ` 带空格）匹配（src/shenbi/cli_utils.py:20）——已实读确认非 bug；`exec > >(tee ...)`（:14）日志隔离 ✓。
- 验证命令: [静态读 + cli_utils.py:20 并读]
- 置信度: high

### scripts/lint_contract_fields.py
- 处置: deep-read
- 声称检查的不变量: [declared fields 与样本 H2/JSON key 匹配用统一 matcher（match_field）；无样本路径跳过不误报；frontmatter 解析 3 段约定；REPO 锚定 CWD 无关]
- findings: 无
- 备注: 仅被 justfile 接线（:49,:54）——CI 缺席已记 F1001；EXAMPLE_FIXTURES 引用的 fixture 存在性由 G0.9/G2 测试体系覆盖；`resolve_sample` 第 3 步 parametric glob 首匹配有序（sorted）可复现。
- 验证命令: [`uv run python scripts/lint_contract_fields.py` → "All contract field declarations match truth files." RC=0]
- 置信度: high

### tools/__init__.py
- 处置: deep-read
- 声称检查的不变量: [空文件使 tools/ 成包，支撑 tests 导入与 basedpyright extraPaths（pyproject.toml:379-381 注释）]
- findings: 无
- 验证命令: [`wc -l` → 0 行]
- 置信度: high

### tools/audit-skill-descriptions.py
- 处置: deep-read
- 声称检查的不变量: [≤500 字符与 trigger-only 规则复用 G0 单源（_parse_frontmatter/_desc_has_behavioral_text）；skills-dir 不存在 exit 2；输出报告格式]
- findings: [F1011]
  - **F1011** | 审计工具未接入任何门：真实 skills/ 目录从不被审 | error | P2 | 证据 tools/audit-skill-descriptions.py 全文 + `grep -rn audit-skill-descriptions .github/ justfile .pre-commit-config.yaml` → 0 命中；其单测 tests/unit/tools/test_audit_skill_descriptions.py 仅用 tmp_path 合成目录（:23-33） | 根因: spec §3.4 审计落地为工具+测试但未进 CI/just/pre-commit → AGENTS.md "description ONLY when-to-use ≤500" 契约在仓库层无自动执法（仅 G0 运行时管） | 验证: grep 输出 0 命中；测试文件 sed 通读 | 建议: 挂入 just check 或 CI（幂等、纯读）
- 其余: 复用 G0 私有助手（`_` 前缀导入）有一致性收益亦有 brittleness——M 级观察。
- 验证命令: [grep 接线面；通读测试]
- 置信度: high

### tools/check_fixture_mirror.py
- 处置: deep-read
- 声称检查的不变量: [MIRROR_MAP 单源来自 shenbi.gates.g0；源缺失跳过、哈希不等报 drift 并给 cp 修复提示]
- findings: [F1012]
  - **F1012** | 仅 pre-commit 接线，CI 与 just check 均缺席 | error | P2 | 证据 .pre-commit-config.yaml:79-83 为唯一接线；ci.yml/justfile 无引用（grep 0 命中） | 根因: G0.11 的本地防线未同步 CI → 未装 pre-commit 的贡献者 fixture mirror 漂移可过 CI | 验证: `grep -rn check_fixture_mirror .github/ justfile` → 0 命中 | 建议: 入 just check（与 F1002 一并回填）
- 其余: 相比其他 tools 缺 `sys.path.insert(src)`（:12 直接 import shenbi），依赖 `uv run` 安装态——裸 python3 运行即 ImportError，与其余工具不一致（M 级，并入 F1012 备注）。
- 置信度: high

### tools/compare_mutation_score.py
- 处置: deep-read
- 声称检查的不变量: [baseline 解析正则与 mutmut results 输出列格式匹配；缺失模块按 0.0 记（fail-safe）；subprocess 列表参数无 shell 注入]
- findings: [F1010]
  - **F1010** | docstring 称 "Used by CI in PR-35"，实际无任何 CI 引用 | error | P2 | 证据 tools/compare_mutation_score.py:5 vs `grep -rn "mutmut\|mutate" .github/workflows/` → 0 命中；唯一消费为 justfile:95 `mutate-check`（手动 recipe） | 根因: 计划中的 CI 接线未落地，注释遗留 | 验证: grep 输出 0 命中 | 建议: 修正 docstring 或真正接入 CI（nightly 合适）
- 其余: `subprocess.run([...], check=False, capture_output=True)` 无注入面 ✓；mutmut results 解析失败时全部模块按 0 记 → fail-loud ✓。
- 置信度: high

### tools/generate_autocheck_docs.py
- 处置: deep-read
- 声称检查的不变量: [sentinel 块整块替换幂等；无 frontmatter 文件的安全插入；REGISTRY/SKILLS 消费正确；CI diff 门（真实生效路径）]
- findings: [F1018]
  - **F1018** | `_PATTERN.sub(block, ...)` 的替换串未转义反斜杠 | error | P2 | 证据 tools/generate_autocheck_docs.py:123（`re.sub` 的 repl=block，block 含模块常量渲染值） | 根因: repl 中 `\g`/`\1`/孤立 `\` 会被解释为组引用/坏转义 → 常量值含反斜杠时崩溃或错写（当前常量为数值/无反斜杠，属边界未触发） | 验证: 未验证（静态；当前仓库常量集不含反斜杠故绿） | 建议: `re.sub(..., lambda m: block, ...)`
- 验证命令: [静态读；CI 幂等门在 ci.yml:83,88（该 diff 路径 skills/ 真实生效，与 .codex-plugin/ 不同）]
- 置信度: high

### tools/lint_contract_graph.py
- 处置: deep-read
- 声称检查的不变量: [ORPHAN_READ 阻断 / DANGLING_WRITE 仅 warn；glob-superset 匹配防误报；外部/pipeline 生产者豁免；测试可 monkeypatch（call-time 解析 import）]
- findings: 无（其 CI 缺席归 F1001）
- 验证命令: [`uv run python tools/lint_contract_graph.py` → RC=0，输出 3 条 DANGLING_WRITE warn（score-stratum / short-drafting / short-packaging，非阻断语义正确）]
- 置信度: high

### tools/lint_contracts.py
- 处置: deep-read
- 声称检查的不变量: [load-all 每个管线技能契约可载入；report 技能下游被消费则必须有 persisted write（dag_key glob 归一）；META_SKILLS 排除 2 元技能]
- findings: 无
- 验证命令: [`uv run python tools/lint_contracts.py` → RC=0 无输出]
- 置信度: high

### tools/lint_no_forbid_with_computed_field.py
- 处置: deep-read
- 声称检查的不变量: [同类体内 computed_field + model_config forbid 判定；用法错误 exit 2；继承路径为文档化限制]
- findings: 无
- 验证命令: [`uv run python tools/lint_no_forbid_with_computed_field.py src/shenbi/contracts` → RC=0]
- 置信度: high

### tools/lint_no_fs_mutation.py
- 处置: deep-read
- 声称检查的不变量: [src/shenbi FS 变更原语全禁（allowlist 外）；mode 提取覆盖 method/keyword/builtin 三形；TRANSITIONAL_ALLOWLIST 无死条目（有测试验证）]
- findings: 无
- 验证命令: [`uv run python tools/lint_no_fs_mutation.py src/shenbi` → RC=0；`grep -rn TRANSITIONAL_ALLOWLIST tests/` → tests/unit/test_lint_no_fs_mutation.py 存在（"no dead entries" 声明有测试支撑）]
- 置信度: high

### tools/lint_repo_consistency.py
- 处置: deep-read
- 声称检查的不变量: [body-ban 剥离自动生成块后查手写契约；术语禁表只查 prose（剥反引号）；loader-uniqueness 仅 contract.py/legacy.py 豁免；dead-decisions-sidecar 三条件判定]
- findings: [F1017]
  - **F1017** | `_G4_DECISIONS_SKILLS` 硬编码快照复制 g4/generic.py 状态 | optimization | P2 | 证据 tools/lint_repo_consistency.py:109-119 vs src/shenbi/gates/g4/generic.py:304-334 | 根因: 快照式复用（注释自认 "as of this audit"）→ generic.py 新增 g4_decisions 技能后此表不同步，反向造成 dead-decisions-sidecar 误报（阻断 lint）或漏报；当前两边 7/7 一致（已实测） | 验证: `grep -n "g4_decisions" generic.py` → 7 技能与 frozenset 7 元素逐一相符 | 建议: 运行时 import checkers dict 推导，或加一致性断言测试
- 其余: `find_body_contract_blocks` 的 "tests/rounds/archived" 跳过条件（:53）对 `skills/*/SKILL.md` 输入恒假（死分支，M 级）。
- 验证命令: [`uv run python tools/lint_repo_consistency.py` → RC=0]
- 置信度: high

### tools/lint_status_strings.py
- 处置: deep-read
- 声称检查的不变量: [status/state/classification 三键的裸状态词字面量全禁（status.py 豁免）；递归覆盖三元/布尔表达式；Subscript 赋值形态覆盖]
- findings: [F1015, F1016, F1027]
  - **F1015** | CWD 敏感：非 repo root 运行静默零扫描通过 | error | P2 | 证据 tools/lint_status_strings.py:91（`Path().glob("src/shenbi/**/*.py")` 相对 CWD）与 :100（main 传 TARGET_GLOBS 常量） | 根因: 未像 lint_repo_consistency.py:21 那样 REPO 锚定 → 错误目录运行 = 0 文件 = 0 违规 = exit 0（假绿） | 验证: 未验证（未在异目录实际运行；语义确定） | 建议: `REPO = Path(__file__).resolve().parents[1]` 锚定 + 空扫描集报错
  - **F1016** | 覆盖缺口：`dict(status="PASS")` kwargs 形态与 setattr 不检 | error | P2 | 证据 tools/lint_status_strings.py:51-74（Visitor 仅 visit_Dict/visit_Assign） | 根因: AST 启发式固有边界；dict-kwarg 是等价 emit 位点 | 验证: 未验证（静态） | 建议: visit_Call 补 dict(**) 检测或在 docstring 限制清单中显式登记
  - **F1027** | `scan(roots)` 忽略形参（死参数） | error | M | 证据 tools/lint_status_strings.py:87-95,100 | 根因: API 演进残留 | 验证: 通读 | 建议: 删参或用参
- 验证命令: [`uv run python tools/lint_status_strings.py` → RC=0（repo root 运行）]
- 置信度: high

### tools/migrate_contract_to_frontmatter.py
- 处置: deep-read
- 声称检查的不变量: [幂等守卫（已迁移跳过）；YAML 转义/引号处理；体块剥离只删手写契约；一次性工具不再需要]
- findings: [F1022]
  - **F1022** | 一次性迁移器残留，内嵌第三份契约快照 | optimization | P2 | 证据 tools/migrate_contract_to_frontmatter.py:22-518（CLASSIFICATION 全表）与 :8（"Run once"） | 根因: 迁移完成后未归档 → 518 行快照与 SKILL.md frontmatter / truth-files.yaml 构成第三数据源，且该快照已陈旧（新技能不在表内，安全；但误导维护者） | 验证: 静态读；对照 skills/ 现状（74 技能 vs 表内 ~45） | 建议: 移 docs/ 归档或删除
- 置信度: high

### tools/pre-push-check.sh
- 处置: deep-read
- 声称检查的不变量: [CI 模拟完整（对照 ci.yml 步骤）；--no-cov 保护 coverage.xml（AGENTS.md 记述的历史 bug 已修复）；libcairo-only 容忍逻辑不吞真错误；main...HEAD 范围判定]
- findings: 无（独立发现均已归入 F1001/F1002——它同样缺 graph/fields 两个 lint，且 pytest 缺 --hypothesis-profile=ci）
- 备注: :70 `--cov-fail-under=85` 在第一段、:85 `--no-cov` 在 last 段——AGENTS.md PR 协议第 4 条描述的历史事故（第二段误带 --cov-fail-under 覆盖 coverage.xml）确认已修复；:55-56 `grep -vE ... || true` 的 pipefail 防御正确；:74-79 死代码计数仅 WARNING 非阻断（自述清楚）。
- 验证命令: [静态逐段对照 ci.yml；AGENTS.md 第 4 条对照]
- 置信度: high

### uv.lock
- 处置: dependency-list 核验（按任务指令不逐行深读）
- 声称检查的不变量: [与 pyproject 声明一致（uv lock --check 绿）；无异常 pin；optional vs dev 组语义无意外重复]
- findings: 无（版本偏差见 F1008 属 pre-commit rev，非锁内问题）
- 验证命令: [`uv lock --check` → "Resolved 180 packages in 4ms"（无漂移）；直接依赖锁定版本抽查全部满足声明范围（jieba 0.42.1 精确 pin、openai 2.44.0、pydantic 2.13.4、numpy 2.5.0、mypy 2.1.0、ruff 0.15.17、basedpyright 1.39.7、torch 2.13.0、sentence-transformers 5.6.0、mkdocs-material 9.7.6 等 33 项）；sentence-transformers 在 optional.embeddings 与 dev 双声明为 embeddings-smoke.yml:5 注释声明的有意重复]
- 置信度: high

---

## findings 总表（F1001–F1029）

| 编号 | 严重度 | 类别 | 文件 | 一句话 |
|---|---|---|---|---|
| F1001 | P1 | error | ci.yml | CI 缺 lint_contract_graph + lint_contract_fields 两个阻断 lint |
| F1002 | P1 | error | justfile | just check 缺 CI 强制的 4 步（lock/两 lint/autocheck 幂等）→ 本地绿 CI 红 |
| F1003 | P1 | error | ci.yml + .gitignore | .codex-plugin/ 幂等 diff 恒空（gitignored），插件新鲜度门形同虚设 |
| F1004 | P1 | error | plugins/master.json | 技能清单 59/74，缺 15 技能（含全部 score-* 与 group-review 家族） |
| F1005 | P2 | error | plugins/master.json | version 0.2.0 vs pyproject 0.1.0 |
| F1006 | P2 | optimization | codeql.yml | push 无分支过滤，任意分支全量扫描 |
| F1007 | P2 | error | pre-commit-autoupdate.yml | rc==1 语义误判（1=hook 失败≠仅 autofix） |
| F1008 | P2 | deps | .pre-commit-config.yaml | yamllint rev 1.33.0 落后锁定 1.38.0 |
| F1009 | P2 | deps | pyproject.toml | pytest-ordering 死依赖（且与 pytest-randomly 冲突） |
| F1010 | P2 | error | compare_mutation_score.py | docstring 称 CI 使用，实际未接线 |
| F1011 | P2 | error | audit-skill-descriptions.py | 未接任何门，真实 skills/ 不被审 |
| F1012 | P2 | error | check_fixture_mirror.py | 仅 pre-commit，CI/just 缺席（另缺 sys.path 锚定） |
| F1013 | P2 | security | run_pipeline.sh | $PROJECT_DIR 内插 python3 -c（引号/反斜杠破坏） |
| F1014 | P2 | error | run_pipeline.sh | error 分支 grep "gate\|escalation\|dispatch" 过宽自动放行 |
| F1015 | P2 | error | lint_status_strings.py | CWD 敏感，异目录静默假绿 |
| F1016 | P2 | error | lint_status_strings.py | 漏检 dict(status=...) kwargs 形态 |
| F1017 | P2 | optimization | lint_repo_consistency.py | _G4_DECISIONS_SKILLS 硬编码快照（当前 7/7 一致） |
| F1018 | P2 | error | generate_autocheck_docs.py | re.sub repl 未转义反斜杠（边界未触发） |
| F1019 | P2 | error | .gitignore | novel-output 先忽略后取反，死行误导 |
| F1020 | P2 | error | .gitignore | 缺 run_pipeline.sh 产物（novel-<user>-*/、pipeline.log） |
| F1021 | P2 | error | release.yml | 裸 git log 生成 notes，绕过 cliff 分组 |
| F1022 | P2 | optimization | migrate_contract_to_frontmatter.py | 一次性迁移器 + 第三份契约快照残留 |
| F1023 | P2 | error | AGENTS.md（跨区） | 技能计数 69 vs 磁盘 74 |
| F1024 | P2 | optimization | pyproject.toml | mypy python_version 3.12 > 支持底线 3.11 |
| F1025 | M | error | justfile | mutate-check 守卫提示过时 |
| F1026 | M | optimization | docs.yml | 无 concurrency 取消组 |
| F1027 | M | error | lint_status_strings.py | scan() 死形参 |
| F1028 | M | optimization | CODEOWNERS | 全部条目同 owner 冗余 |
| F1029 | M | error | AC-003.md | 正文维度标签与 frontmatter 记法不一致 |

严重度分布: P0×0、P1×4、P2×19、M×6。

## 覆盖统计

- 清单文件: 53/53 覆盖（deep-read 52；uv.lock 按指令做依赖清单核验）
- 未覆盖文件: 无
- 低置信度文件: .github/ISSUE_TEMPLATE/skill-proposal.md（medium，phase 枚举仅 spot-check）；.github/workflows/pre-commit-autoupdate.yml（medium，F1007 未实际复现 rc==1）
- 实际运行的验证命令: `uv lock --check`；`uv run yamllint --strict .github/workflows/`；6 个只读 lint（lint_status_strings / lint_contracts / lint_repo_consistency / lint_no_fs_mutation / lint_no_forbid_with_computed_field / lint_contract_graph → 全部 RC=0）；scripts/lint_contract_fields.py → RC=0；git ls-files/check-ignore 系列（.codex-plugin、novel-output、tests/coverage/.gitkeep、.venv）；master.json vs skills/ 集合 diff（missing 15）；18 个 executor_config override 技能存在性（无 MISSING）；mkdocs nav 13 文件存在性（无 MISSING）；uv.lock 33 项直接依赖锁定版本抽查
- 明确未验证（报告内已标注 "未验证"）: F1006/F1013/F1014/F1015/F1016/F1018/F1026/F1027/F1028（静态边界分析，无可无副作用的复现路径或属 YAML/设计语义）
- 跨区观察（供汇总 agent）: AGENTS.md 技能计数 69 vs 实际 74（F1023）；AGENTS.md "just check 等价命令" 清单本身缺 lint 项（与 F1002 同根）
