# Z11-b 段报告 — 只读磁盘执行产物（2026-08-15 轮）

- **范围**: docs/superpowers/audit-runs/2026-08-15/zones/Z11-b.files 全部 86 文件
  （根级 truth/ 2 · .superpowers/sdd 22 · sdd-archive-inference-control 10 · sdd-archive-issue24 5 · .hypothesis 45（含 .gitignore + .gitkeep + 43 样本）· .benchmarks 1 · .playwright-mcp console log 1）
- **处置**: deep-read（progress/spec-deviations/audit 全文或定向读 + /tmp/match_hypothesis_keys.py digest 匹配核验，全部只读）
- **覆盖**: 86/86（100%）；未覆盖文件列表为空
- **编号段**: F1151–F1161（11 findings：P1×1 · P2×8 · M×2）
- **pre-seeded findings（源自 SDD 历史已知问题/未决偏差）**: 5 条 — F1153, F1154, F1155, F1156, F1157（另有 F513→spec #10 已有 owner，未立 finding，见 sdd 簇注记）
- **总体**: 当前 SDD #6（PR #42 已合并）工作目录干净、审计链完整；三个 SDD 目录的未决项均为「记录在案、转交/悬置」型——4 项有 owner（spec #26×3、spec #10×1），1 项（温度队列调优）无任何活跃 spec 承接。hypothesis 失败样本体系经 digest 实证**双重死亡**：跨机器不入库（gitignore 压制）+ 本机全 stale（0/10 key 匹配）。根级 truth/ 实为 PR #19 提交的模板（被 2 个单测消费），非孤儿运行产物，但未登记词表且与项目内 truth/ 语义同名冲突。.benchmarks 为单次冒烟 autosave（无真实基准套件）。console log 无项目代码异常。

---

## 汇总

| 严重度 | 数量 | 编号 |
|---|---|---|
| P0 | 0 | — |
| P1 | 1 | F1159 |
| P2 | 8 | F1151, F1152, F1153, F1154, F1155, F1156, F1158, F1160 |
| M | 2 | F1157, F1161 |

---

## 只读禁令遵守声明

除本段文件外未创建/修改/删除任何仓库文件（核验脚本仅写 /tmp/match_hypothesis_keys.py）；未执行 git add/commit；未执行 pytest/shenbi-dispatch/pipeline。digest 匹配脚本仅 importlib 导入测试模块计算 function_digest（无测试执行）；jieba 缓存写入系统临时目录（非仓库）。

---

### truth/（根级，2 文件）
- 处置: deep-read
- 声称检查的不变量: truth 文件有可考来源（哪个 pipeline/何时写入）；结构符合 truth-files.yaml 词表；与 novel-output/ 各项目 truth 的关系明确（非孤儿/非陈旧活跃态）
- findings: F1151, F1152
- 验证命令:
  - `git log --oneline --follow -- truth/` → 仅 1 commit：`dd1fc62`（PR #19，2026-07-20 10:29，feat 段落 "truth/bridge_tracker.md tracks all 16 cross-volume bridges"）
  - `git log --all -S "Lin Feng" -- truth/` → 同一 commit（六角色演示数据无其他来源）
  - `grep -rn '"truth"' src/shenbi/pipeline/*.py` → 全部为 `project_dir / "truth"` 相对路径（真实项目 truth 在 novel-output/xinghuo-ranqiong/truth/ 13 文件、test-validation/truth/ 4 文件）
  - `diff truth/character_matrix.md novel-output/xinghuo-ranqiong/truth/character_matrix.md` → 全文不同（Lin Feng 等六角色 vs 参数化角色 冷/光/安静…，后者 last_chapter 56、version 0.2.2）
  - 消费者：`tests/unit/pipeline/test_bridge_tracker.py:11,21` 与 `tests/unit/gates/g4/test_state_settling.py:164` 均 `Path(__file__).resolve().parents[N] / "truth" / ...` → 仓库根 truth/，作为「模板结构」断言读取（存在即跳过守卫已死代码化，见 Z7-a F712）
  - `src/shenbi/pipeline/genesis.py` → genesis 经技能派发写 project_dir 内 truth/*，不消费根级模板
- 置信度: high（事实链完整）；「模板意图 vs 演示种子」定性 medium（推断自 commit 消息 + 测试消费方式）

**F1151 | 根级 truth/ 以生产 truth 文件名/路径存放模板，与词表的「项目内 truth/」语义同名冲突，仅被 2 个模板结构测试消费 | error | P2**
- 证据: `truth/bridge_tracker.md:3` "Auto-generated from outline/volume_map.md at Genesis time" + `:26` "Last Updated: Genesis"（16 桥全 PENDING、Expected/Actual Activation Ch 全 0/—）；`truth/character_matrix.md:7-12` 六演示角色（Lin Feng/Chen Weimin/Zhao Tiezhu/Chu Yunlan/Koen Whiteman/Gangshan Tieya）；根目录无 novel.json/outline//world/（非完整项目布局）；与 xinghuo-ranqiong、test-validation 两真实项目角色集均不符
- 根因: PR #19 将 genesis 形态的模板直接提交到仓库根 truth/——位置与运行时 truth 契约（project_dir 相对）同名不同物；若 dispatcher/pipeline 以 repo 根为 project_dir（`.`）调用，会读改该模板（无代码默认此行为，纯人误路径）
- 验证命令: `ls truth/`（仅 2 文件，词表 truth 概念 17 项的子集）+ 上述 git log/diff
- 建议方向: 移至 `tests/fixtures/templates/`（或 docs/）并在文件头显式标注 "checked-in template, not live state"；同步修 F712 的死 skip 守卫路径
- 严重度理由: 表中 P0 例「truth 来源不明致误用」——此处来源可考（dd1fc62）且无程序化消费者，误用需显式指错路径 → 取 P2（状态漂移/边界）

**F1152 | truth/bridge_tracker.md 未登记 truth-files.yaml 词表（index.json 有条目、yaml 无 concept；且为 write-only 无读者） | error | P2**
- 证据: `docs/framework/truth-files.yaml:29-48` truth 概念清单无 bridge_tracker.md（头注 `:2-3` "Adding a genuinely new file = ONE edit here…prevents silent synonym creation"）；`docs/framework/truth-files.index.json:631` `truth/bridge_tracker.md → {reads: [], writes: [shenbi-foreshadowing-track], updates: []}`；yaml `globs` 的 `truth/*.md`（:122）可让契约字面合法，但 concept 级登记缺失；reads 为空 = 无技能读者
- 根因: PR #19 新增文件时绕过 yaml 单点登记（sync_contracts.py 从技能契约反推 index.json，掩盖了 yaml 侧缺口）；与 Z11-a 本轮已立的 state_snapshot-pre-rev.md 未登记同型（D1 词表漂移家族）
- 验证命令: `python3` 对比 yaml `name:` 概念集 vs index.json 键集 → bridge_tracker.md 在 index-only 集合
- 建议方向: yaml 补 `{name: truth/bridge_tracker.md, kind: truth}` 或裁决废弃该文件（其唯一写者 shenbi-foreshadowing-track 已标 DEPRECATED，见 Z8-a F819）
- 注: 词表完整性主责在 docs/framework 管辖簇（Z11-a 同型已立）；本条补根级/生产视角证据

---

### .superpowers/sdd/（SDD #6 pipeline-never-completes，22 文件：9 audit + progress + spec-deviations + .gitignore + 10 review diff）
- 处置: deep-read（progress.md/spec-deviations.md 全文；audit-T1/T9 全文，T2-T8 定向扫描未决标记；10 个 review-*.diff 头部+行数核验）
- 声称检查的不变量: audit-T*.md 计数 == plan task 数（9）；每 task 双 verdict 收敛、C/I 全修；spec-deviations 中无未处置 ⛔/⏳；已记录未决项有活跃 spec owner
- findings: F1153, F1154, F1155（pre-seeded，转交 spec #26）
- 验证命令:
  - `ls .superpowers/sdd/audit-T*.md | wc -l` → 9 == plan task 数 ✓（progress.md:98）
  - `grep -n "BLOCKED\|未修\|遗留" .superpowers/sdd/audit-T*.md` → 3 命中全为「已修/转交」处置记录
  - `git status --porcelain -- .superpowers/` → 空（本地工作态与归档一致；全树被根 .gitignore:26 忽略，仅本机存在）
  - owner 核验: `grep -n "#26\|#10" docs/superpowers/specs/INDEX.md` → `:160` #26 快照子系统接线（active）、`:65` #10 审计链失效（active）
- 置信度: high

**F1153 | [pre-seeded] SDD #6 未决偏差：F304 预算耗尽路径只设 ESCALATION checkpoint、不派发 escalation-review 产物，与 closure 失败路径行为不一致 | error | P2**
- 证据: `.superpowers/sdd/spec-deviations.md:12`（T8 M4 偏离注记：「plan 未规定派发；与 closure 失败路径的派发行为不一致，#26/后续 spec 可统一」）；`.superpowers/sdd/audit-T8.md:17`（同，处置=记录为偏离注记）
- 根因: plan 阶段未覆盖 F304×escalation 交互；修复被显式转交 spec #26 统一
- 验证命令: `grep -n "M4" .superpowers/sdd/audit-T8.md` → `:17` 表行
- 建议方向: 将「escalation 派发行为统一」写入 #26 验收清单显式条目，防止转交项在 #26 收尾时再次静默掉队

**F1154 | [pre-seeded] SDD #6 未决偏差：snapshot-manage 真实格式目录 fixture 未建——manifest.json 由测试按钉死契约名构造，非真实 skill 产物 | error | P2**
- 证据: `.superpowers/sdd/spec-deviations.md:7`（T5 I10 G0.9 边界裁定：「目录内容文件=生产 snapshots/ crash_recovery 真实产物精确副本…manifest.json 为门内部测试输入非 skill 场景断言；真实 snapshot-manage 格式 fixture 归 #26 接线验收」）；`.superpowers/sdd/audit-T5.md:29` 同
- 根因: G0.9（fixture 须真实产物、禁手工 mock）边界裁定豁免 + 延后；豁免有据但 #26 落地前该测试的 manifest 输入仍是手工构造
- 验证命令: `grep -n "I10" .superpowers/sdd/spec-deviations.md` → T5 段
- 建议方向: #26 接线 snapshot-manage 后用真实产物替换测试构造的 manifest

**F1155 | [pre-seeded] SDD #6 未决偏差：truth-files.yaml D20 注释与新 snapshot 契约矛盾，仅加 supersession 注记未彻底协调 | error | P2**
- 证据: `.superpowers/sdd/audit-T5.md:17`（M3：「已加 supersession 注记（86e4b32；#26 接线时彻底协调）」）；现状 `docs/framework/truth-files.yaml:72-77` D20 旧注与 2026-08-15 supersession 新注并存，同一段内对 `snapshots/chapter-NNN/` 概念状态给出两代表述
- 根因: spec #6 范围外的词表协调项，挂账 #26
- 验证命令: `sed -n 72,77p docs/framework/truth-files.yaml` → 两段注释并存
- 建议方向: #26 时收敛为单一权威描述（删除被取代的 D20 叙述）

- 注记（未立 finding）: progress.md:82 记录 PR #42 中 1 条 Copilot pushback「F513 归 spec #10」——F513 已有活跃 owner（INDEX:65 #10 审计链失效），属已跟踪转交，非悬置。

---

### .superpowers/sdd-archive-inference-control/（10 文件）
- 处置: deep-read（progress.md/spec-deviations.md 全文；audit-T1..T7 定向扫描）
- 声称检查的不变量: SDD 归档态完整（PR #40/#41 已合并、7 audit 全 PASS、无 BLOCKED）；记录的延后项有后续 owner 或显式裁决
- findings: F1156, F1157
- 验证命令:
  - `grep -n "deferred\|延后" .superpowers/sdd-archive-inference-control/audit-T*.md` → 1 命中（audit-T4.md:8）
  - `grep -rn "温度\|temperature" docs/superpowers/specs/INDEX.md` → 0 命中（无活跃 spec 承接）
  - `docs/superpowers/specs/2026-08-01-output-side-waste-audit-design.md:20` → 「调用参数浪费（温度/模型/截断）❌（推理 spec）不重复」
- 置信度: high

**F1156 | [pre-seeded] inference-control SDD 延后项断链：评估队列 8 skill + 聚合队列 4 skill 温度调优被搁置且无任何活跃 spec 承接 | optimization | P2**
- 证据: `.superpowers/sdd-archive-inference-control/audit-T4.md:8`（「Evaluative (8) + aggregator (4) deferred per spec §2.1」）；`executor_config.toml:30-32` 注释至今称「deferred to P1 pilot / plan-stage decision」；归档 spec `archive/2026-08-01-inference-control-audit-design.md:46,178`（评估队列降 P1 试点、聚合队列 plan 阶段裁决）；INDEX 无温度条目；唯一提及温度的活跃 spec（#4 output-side-waste，Design 态）明确声明不重复该范围 → 12 个 skill 仍跑默认 temperature=0.7（executor_config.toml:2，无 override）
- 根因: 延后决定记录在已归档 spec + 配置注释里，从未转成 INDEX 条目/微 spec——跨 SDD 轮次的「延后≠遗忘」交接机制缺失
- 验证命令: `grep -n "deferred" executor_config.toml` → `:31-32`；`grep -rn "评估队列" docs/superpowers/specs/*.md` → 0
- 建议方向: 开微 spec 承接（试点 1 skill → G4 对比 → 扩展，按原 §2.1 路径），或裁决「维持 0.7 默认」并删配置中 deferred 注释闭环
- 附: max_tokens=32768 PRE-DEPLOYMENT probe 悬置上轮已立（Z10 F1219，M），本轮不重复

**F1157 | [pre-seeded] 归档 progress.md 尾部残留过时 checklist：8 个未勾选 Phase 与上方已完成记录自相矛盾 | error | M**
- 证据: `.superpowers/sdd-archive-inference-control/progress.md:54-62`（`- [ ] Phase 3 Plan` … `- [ ] Phase 10 清理` + `[x] Phase 11 归档`）vs `:10-21` 同名 11 项全 `[x]`（PR #40/#41 合并、just check 全绿）
- 根因: 模板追加式编辑残留未清理；两份 checklist 并存误导后续轮次的状态判读
- 验证命令: `sed -n 54,62p .superpowers/sdd-archive-inference-control/progress.md`
- 建议方向: 删除尾部过时段（.superpowers/ 全树 gitignored 仅本机存在，修复优先级低）

---

### .superpowers/sdd-archive-issue24/（5 文件）
- 处置: deep-read（progress.md/spec-deviations.md 全文；audit-T1..T3 定向扫描）
- 声称检查的不变量: 11 阶段全完成（PR #26 squash-merge 4d228f7）；CodeQL 11 告警归零；spec-deviations 全处置无悬置
- findings: 无
- 验证命令: progress.md:9-21 全勾选 + `:48-50` 「SDD 完成」；spec-deviations.md 仅 3 条（2 条 spec 文本订正 + 1 条 commit 边界偏差，均显式裁定不阻塞）；audit-T1..T3 无未修标记
- 置信度: high
- 备注: 该目录无自己的 .gitignore（另两个 SDD 目录各有 `*`），但父级被根 .gitignore:26 整树忽略，效果等同——无影响

---

### .hypothesis/.gitignore + examples/（43 样本文件 + .gitkeep）
- 处置: deep-read（digest 匹配核验 + git 跟踪状态核验 + metakey 解码）
- 声称检查的不变量: 「Examples ARE committed — replay them on every run, ensuring regression detection」（.hypothesis/.gitignore:4-5 声明）；失败样本对应的测试当前存在且形态未变（重放可达）；样本非永久滞留
- findings: F1158, F1159
- 验证命令:
  - `git ls-files .hypothesis/` → 仅 2 文件（.gitignore + examples/.gitkeep）；`git check-ignore -v .hypothesis/examples/008e903f60ea3c6b/8db630b3036f40e2` → `.gitignore:80:.hypothesis/`（父目录整树忽略使嵌套 `!examples/` 否定失效——git 无法重包含被忽略目录内的文件）
  - metakey 解码: `04e6b3400353b141` = sha384(b".hypothesis-keys")[:16]（hypothesis/database.py:440-441），其下 10 文件 = 10 个测试 key 原文（部分带 `.secondary` 后缀，对应 core.py:2302 `function_digest(test) + b".secondary"`）
  - digest 匹配（只读，无 pytest）: `.venv/bin/python /tmp/match_hypothesis_keys.py` → 扫描 15 个 @given 测试模块（tests/property/** + tests/unit/test_*property* + test_pytest_framework）58 个函数，对每个函数计算 `function_digest(inner_test)` 与 `+b".secondary"` 两种 key 的 sha384[:16]，与磁盘 10 个 key 目录比对 → **matched: 0/10**
  - 样本 mtime: 2026-06-21 ×1 + 2026-06-30 ×42（此后 tests/property 经 #8 76662a5、#11 8d3f5c7、485772b 三次重构，测试函数源/签名/given 参数均已改写）
- 置信度: high（digest 为确定性同环境计算；venv hypothesis 即项目锁定版本；两种 key 形态均覆盖）

**F1158 | 43 个失败样本全部 stale：10 个测试 key 与当前 58 个 @given 函数 digest 0/10 匹配，本地重放价值为零 | error | P2**
- 证据: 上述匹配脚本输出（0/10）；样本对应测试函数在 6-7 月重构（#8/#11/485772b）后 digest 全变；hypothesis 仅 fetch 现存测试的 key → 10 个目录 43 文件成为永不读取、永不清理的死语料
- 根因: 测试演替 + hypothesis 本地 example 库无陈旧 key GC
- 验证命令: `.venv/bin/python /tmp/match_hypothesis_keys.py`（matched: 0/10）
- 建议方向: 清空 examples/（保留 .gitkeep）待 F1159 修复后由新一轮失败重建
- 判定口径: 只读判定（digest 级），未运行重放

**F1159 | 「Examples ARE committed」回归重放机制双重死亡：gitignore 压制使样本从未入库（CI/新克隆零重放），叠加上轮 F1318（M）未修 | error | P1**
- 证据: `.hypothesis/.gitignore:3-6` 声明 committed + `!examples/` 否定；根 `.gitignore:80` `.hypothesis/` 整目录忽略 → 嵌套否定永不生效（git 语义：被忽略目录内的文件无法重包含）；`git ls-files` 仅 2 文件；hypothesis/database.py:428-434 官方推荐模式要求 `.hypothesis/*` + `!.hypothesis/examples/` 组合在**同层**生效
- 根因: 根目录与 .hypothesis/ 两层 gitignore 的忽略/否定语义冲突，声明从未被验证过（首立为上轮 2026-08-14 Z11 F1318=M）
- 验证命令: `git check-ignore -v <任一样本文件>` → 根 .gitignore:80 命中
- 升级理由: 上轮仅见「未入库」；本轮新增 F1158（本机内容也全 stale）——声称的不变量「every run 重放已提交反例、regression detection」在**所有**环境（CI、新克隆、本机）均不成立 = 正常路径功能错误 → P1
- 建议方向: 根 .gitignore 改 `.hypothesis/*` + `!.hypothesis/examples/`（对齐官方模式）后提交重建样本并 pin hypothesis 版本；或删除 .hypothesis/.gitignore 的虚假 committed 声明，明确接受本机库

---

### .benchmarks/（1 文件）
- 处置: deep-read（JSON 全解析 + 配置/目录关系核验）
- 声称检查的不变量: 基准历史有时间跨度（可比较）；与 tests/baselines/ 的关系明确
- findings: F1160
- 验证命令:
  - `find .benchmarks -type f | wc -l` → 1：`Darwin-CPython-3.11-64bit/0001_0d86371207d97ef97b32f5134c1a3de25aae5e3b_20260814_125304.json`（mtime 2026-08-14 20:53 本地，commit 0d86371207d9）
  - JSON benchmarks 数组仅 1 条：`test_benchmark_marker_works`（rounds=198334，mean≈2.9e-08 s）= `tests/unit/test_pytest_framework.py:43` 的 marker 冒烟
  - `ls tests/benchmarks/` → 目录不存在（pyproject.toml:414 coverage omit 引用空目录）；justfile 与 .github/workflows 无 benchmark/autosave 引用 → autosave 来源为 08-14 手动 pytest 运行（未验证具体命令，推断）
  - `ls tests/baselines/` → gate-outputs/、mutation-score.txt、pending_hooks.parse.json（差分测试基线体系，与 pytest-benchmark 存储无交集）
- 置信度: high（事实）；autosave 触发来源 medium（推断）

**F1160 | .benchmarks 基准历史为单次冒烟 autosave：无真实性能基准套件，无跨运行可比性；与 tests/baselines/ 是两套无关体系 | optimization | P2**
- 证据: 上述单文件单条目（29ns 冒烟）；pyproject 已装 pytest-benchmark>=4.0.0 + `benchmark` marker + `--benchmark-min-rounds=5/--benchmark-warmup=on`（:429-430）但 tests/benchmarks/ 从未建立
- 根因: 基准基础设施就位后未写真实用例；存储目录 gitignored（.gitignore:32）无共享/对比机制
- 验证命令: `python3 -c "import json; d=json.load(open('.benchmarks/Darwin-CPython-3.11-64bit/0001_*.json')); print(len(d['benchmarks']), d['benchmarks'][0]['name'])"` → 1, test_benchmark_marker_works
- 建议方向: 二选一——补真实 perf 用例（如 G2 批量校验吞吐、truth-index 重建耗时）建立可比较历史；或删除孤儿 autosave 并接受现状（则 AGENTS.md 三层测试中 benchmark 维度名存实亡，建议同步修订表述）

---

### .playwright-mcp/console-2026-07-04T13-10-06-378Z.log（1 文件）
- 处置: deep-read（35 行全文）
- 声称检查的不变量: 无项目代码 ERROR/traceback；无敏感信息泄漏
- findings: F1161
- 验证命令: `grep -c -i error <log>` → 10，全部为 `[ERROR] Error: Not connected to alive`（`github.githubassets.com/assets/*.js` 栈，GitHub PRs 页面自身 WebSocket/SSE 重连噪音，非项目代码）；`grep -i "traceback\|exception"` → 0；会话时长 ≈63 分钟（3354ms→3800284ms），含一次 Google OAuth 登录跳转（accounts.google.com）；日志被根 .gitignore:86 忽略（未入库）
- 置信度: high

**F1161 | 本地会话日志残留 Google OAuth 流完整 URL（一次性 state/nonce/code_challenge 参数），无清理策略 | security | M**
- 证据: `console-2026-07-04T13-10-06-378Z.log:32`（VERBOSE 行含 `state=90af4…`、`nonce=d58af…`、`code_challenge=-XyX7…`）；参数为单次性且已过期 6 周；文件 gitignored，无外泄渠道
- 根因: .playwright-mcp/ 会话日志无轮转/保留期策略，随时间累积凭据流 URL
- 验证命令: `grep -n "accounts.google.com" .playwright-mcp/console-*.log`
- 建议方向: 定期清理 .playwright-mcp/（或加保留期）；本期无活性泄露、无需应急处置

---

## 覆盖统计

| 簇 | 文件数 | 处置 | findings |
|---|---|---|---|
| truth/（根级） | 2 | deep-read | F1151, F1152 |
| .superpowers/sdd/ | 22 | deep-read（10 review diff 头部+行数核验） | F1153, F1154, F1155 |
| .superpowers/sdd-archive-inference-control/ | 10 | deep-read | F1156, F1157 |
| .superpowers/sdd-archive-issue24/ | 5 | deep-read | 无 |
| .hypothesis/（gitignore+gitkeep+43 样本） | 45 | digest 匹配 + 跟踪状态核验 | F1158, F1159 |
| .benchmarks/ | 1 | JSON 全解析 | F1160 |
| .playwright-mcp/ | 1 | 全文 | F1161 |
| **合计** | **86/86** | | **11** |

## 低置信度簇列表

- truth/（根级）：事实链（来源 commit、测试消费者、项目对比）high；「模板意图 vs 演示种子」的原始意图定性 medium（依据 commit 消息 + 消费方式推断）
- .benchmarks/：文件事实 high；autosave 触发来源（手动 pytest 运行）medium（无命令记录，未验证）

## 未覆盖文件列表

（无 — 86/86 全覆盖）

## 交叉引用

- Z7-a（本轮）：F712 死 skip 守卫（test_bridge_tracker.py/test_state_settling.py 以根 truth/ 文件存在性为条件）——与 F1151 同源，处置应联动
- Z8-a（本轮）：F815④/F819——bridge_tracker 契约声明侧（lifecycle 未声明写、track 已 DEPRECATED）；F1152 的词表登记与其互补
- Z11-a（本轮）：state_snapshot-pre-rev.md 未登记词表——F1152 同型家族
- 2026-08-14 轮 Z11 F1318（=M）→ 本轮 F1159 升级 P1（新增本机全 stale 证据）；Z10 F1219（probe 悬置）→ 本轮 F1156 附注不重复
