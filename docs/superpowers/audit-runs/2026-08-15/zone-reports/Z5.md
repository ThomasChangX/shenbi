# Z5 分区初审报告（audit/ + cost/ + orchestration/）

- 日期: 2026-08-15 | agent: Z5 (只读) | 编号段: F501–F514
- 清单: docs/superpowers/audit-runs/2026-08-15/zones/Z5.files（13 文件，全部 deep-read，0 缺失）
- 发现总数: 14（P0×3, P1×2, P2×6, M×3）
- 覆盖缺口数据勘误: 任务书称 "snapshot.py 有 38 行未覆盖"；d1-06 实际数据为 `Stmts=70 Miss=7 Branch=38 BrPart=5`——"38" 是分支总数，未覆盖为 7 条语句 + 5 个部分分支。下文按实际行号逐条处置。

## 交叉验证结论（Z5 重点维度）

1. **TokenLedger 接线（dead-wire 核查）**: 计量链 `_call_llm_streaming → _log_token_usage(dispatch_helper.py:1583/1639) → _record_token_usage(1311) → TokenLedger.record(1346)` 在 API 路径真实发生，**但被 `if state:`（dispatch_helper.py:1310）门控**。grep 全部 `dispatch_skill(` 调用点（10 处）：仅 chapter_loop 3 处传 `state=`；**8 处未传**（parallel_dispatch.py:86、triggers.py:542、audit_layer.py:170、revision_router.py:166、cli.py:140、cli.py:819、closure.py:294、genesis.py:321）→ 这些路径（含 genesis 大额调用与审计波）的 usage 整体不落账本。与 2026-08-14-cost-ledger-design.md R1/F302(P0) 一致，状态仍为 Design = 未修复。另 IDE 路径无 usage 对象（dispatch_helper.py:1803-1811，诚实记录为 uninstrumented）。
2. **审计波调度**: Z5 文件不含波调度器（audit_layer/audit_context_cache 属他区），但 audit_writes 每次调用的扫描集 = 该技能契约 writes+updates 解析后的精确路径（无 `*` 通配契约，grep 验证为空），**不随章数增长**——性能安全；代价是扫描面之外的越权写结构性不可见（见 F501/F506 讨论）。审计波 dispatch（audit_layer.py:170）未传 state → 成本不落账（并入 F504）。
3. **成本分摊正确性**: 归集键 skill 真实、chapter 恒 0（F505）；汇总口径（ledger.summarize）算术正确；report.py 的 `print` 为 CLI 标准输出而非日志替代（AGENTS.md 禁令针对 framework 日志，gates/g4/chapter_drafting.py、pipeline/cli.py 同样存在 CLI print 先例），**不立案**。

---

### src/shenbi/audit/__init__.py
- 处置: deep-read
- 声称检查的不变量: 导出与子模块符号一一对应；无循环导入
- findings: 无
- 验证命令: `grep -rn "from shenbi.audit" src/ tests/`——6 个导出符号（AuditResult/audit_writes/compute_file_change/parametric_globs/record_audit_outcome/snapshot_tree）与各子模块定义一致；executor.py:252-255、tests/unit/audit/* 均经此包导入，无循环（_shared.py 文档声明的 leaf 地位成立）
- 置信度: high

### src/shenbi/audit/_shared.py
- 处置: deep-read
- 声称检查的不变量: leaf 模块（只依赖 contracts+stdlib）；derive_output_files 语义（chapter 解析 N/NNN、unresolvable 跳过、round_dir 绝对化）
- findings: 无（本文件行为正确；其对 unresolvable parametric 的"跳过"语义被 write_audit.py 误用，见 F501——根因在消费方）
- 验证命令: `uv run python -c "...derive_output_files('shenbi-chapter-drafting', chapter=3)"` → `['chapters/chapter-3.md', 'chapters/chapter-3-decisions.json']`；不带 chapter → `[]`；`resolve_or_skip_ctx('chapters/chapter-N.md', None, None)` → `None`
- 未覆盖行处置: 100% 覆盖，无缺口
- 置信度: high

### src/shenbi/audit/record.py
- 处置: deep-read
- 声称检查的不变量: 账本 append-only 恒写；trace seam 失败不丢审计结果；返回值语义（True=可 ship）
- findings: 无
- 验证命令: 对照 trace/writer.py:65-76 `append(*, actor, actor_role, action, target, skill, payload)` ——record.py:44-51 的关键字调用签名完全匹配，"GATE" 属 enums.ActorRole。测试 tests/unit/audit/test_record.py 覆盖 pass/violation/drift 三态，真实（读写真实 tmp 账本文件）
- 未覆盖行处置 (91%, missing 52-53): `except (OSError, ValueError, TypeError)` 回退分支 → **must-test**（模块文档的核心承诺"绝不静默丢弃"依赖此路径；断言 log.warning 已发且账本行仍存在）
- 置信度: high

### src/shenbi/audit/snapshot.py
- 处置: deep-read
- 声称检查的不变量: pre/post 重展开使新增文件可见；JSON field 级 diff；md record 级 diff；added/deleted 判定
- findings:
  - **F502 | OWNERSHIP truth 文件被整体删除逃过审计** | error | P0 | 证据 src/shenbi/audit/snapshot.py:101-102（deleted 早退，`deleted_record_ids` 恒空）+ src/shenbi/contracts/ownership.py:121-122,128-129（record_create/record_field 级仅查 `deleted_record_ids`，**完全不看 `status=="deleted"`**）+ src/shenbi/audit/write_audit.py:62（有 OWNERSHIP 条目时跳过 file-level 检查）| 根因: FileChange 携带 status="deleted" 信息但 check_write_ownership 不消费；删除整个 truth/pending_hooks.md 时 record 级粒度退化为"无记录变更" | 验证: `uv run python -c` 读代码路径推演 + grep tests/unit/audit/test_write_audit.py 无删除场景测试（未直跑删除场景，判定基于三处代码联动，机制确定）| 建议: check_write_ownership 对 OWNERSHIP 文件 status=="deleted" 直接判越权（1 行修复）
  - **F503 | JSON truth 被非法 JSON 内容替换逃过 field 级审计** | error | P1 | 证据 snapshot.py:66-67（`json.JSONDecodeError → return ()`）+ ownership.py:115（空 changed_top_keys ⊆ 任何 write_keys → 无 violation）| 根因: 解析失败静默降级为"无键变化"而非报错/标记 | 验证: `uv run python -c "compute_file_change('genre-config.json', '{\"version\": \"1.0\"}', 'garbage not json'); check_write_ownership('shenbi-genre-config', ch)"` → `violations: []`（已运行，输出粘贴如上）| 建议: JSONDecodeError 时返回哨兵键（如 `("<unparseable>")`）触发越权报告。注: md 侧同场景会被判为"删除全部记录"而拦截（parse_records→[]），不对称性佐证 json 侧是漏洞
  - **F508 | 未变更/双 None → status="modified" 语义错误** | error | P2 | 证据 snapshot.py:103-104（`if pre == post: return FileChange(status="modified")`）| 根因: 缺 "unchanged" 状态；双 None（声明文件前后都不存在）也标 modified | 验证: `uv run python -c "compute_file_change('a.json', None, None)"` → `FileChange(status='modified', ...)`（已运行）| 当前消费者不看 status 故无功能影响，但 FileChange 是公共契约（contracts/ownership.py:22 Literal 无 unchanged），latent trap
  - **F509 | _diff_records 无 id 记录按 "None" 键碰撞 → 幻影 modified** | error | P2 | 证据 snapshot.py:82-83（`{str(r.get("id")): r ...}`）| 根因: parser 不强制 id，两条无 id 记录坍缩同键 | 验证: `uv run python -c "_diff_records([{'content':'x'},{'content':'y'}], [{'content':'x'},{'content':'z'}])"` → `((), (), (('None', frozenset({'content'})),))`（已运行）| 对 record_field 技能可产生假越权（fail-closed 方向）
- 验证命令: 见上；另 `grep -rn "parametric_globs" src/` 确认 registry 每次 reload（load_registry 无缓存，legacy.py:93），每 dispatch 3-4 次磁盘读 truth-files.yaml——docstring 已声明 freshness 取向，可接受
- 未覆盖行处置 (85%, Miss=7/BrPart=5):
  - 40->39（glob 命中但非文件）: acceptable——目录项防御过滤
  - 43-45（raw `*` 通配模式展开）: acceptable（防御分支）——grep 全部 contracts/skills/*.py 的 writes 无 `*` 模式，生产不可达；但若保留建议补 3 行单测以防将来引入通配契约时无护栏
  - 66-67（JSONDecodeError）: **must-test**——正是 F503 漏洞所在，未覆盖行掩盖语义洞
  - 69（顶层非 dict）: must-test——同族降级路径
  - 92->87（changed 为空不 append）: acceptable——正常分支组合
  - 118（非 json/md 回退）: acceptable——防御（当前契约均为 .md/.json）
- 置信度: high

### src/shenbi/audit/write_audit.py
- 处置: deep-read
- 声称检查的不变量: N1 粒度分派；OWNERSHIP 优先、无条目回落 file-level 声明检查；cross-section drift 检测
- findings:
  - **F501 | parametric 写入技能审计双向失效：合法输出误判"未声明写入"（rc=2 误拦）或审计空转** | error | P0 | 证据 src/shenbi/audit/write_audit.py:24（`derive_output_files(skill)` **不带 chapter** → parametric 全被 resolve_or_skip 丢弃 → declared=[]）vs src/shenbi/dispatcher/executor.py:263（watch 用 `derive_output_files(skill, chapter, ctx)` **带 chapter** → 精确路径）+ write_audit.py:33-35（glob 匹配分支因 declared 永不含 parametric literal 而为死分支）| 根因: 匹配面（declared）与观察面（watch）用不同参数构造；declared 侧应保留原始 parametric literal 以激活 fnmatch glob 分支 | 验证（已运行）:
    ```
    uv run python -c "... derive_output_files('shenbi-chapter-drafting', chapter=3)" → ['chapters/chapter-3.md', 'chapters/chapter-3-decisions.json']
    derive_output_files('shenbi-chapter-drafting') → []
    audit_writes('shenbi-chapter-drafting', {p:None...}, {p:'content'...}) →
    violations: ('未声明写入: chapters/chapter-3-decisions.json（不在 shenbi-chapter-drafting 契约 writes/updates）',
                 '未声明写入: chapters/chapter-3.md（...）')  → blocked=True
    ```
    双失效模式: (a) prompt 可提取 chapter 时（executor.py:257-262）→ 技能**自己的契约产出**被判 GATE_FAIL rc=2（executor.py:287-288），legacy 路由（dispatch_helper.py:1909 spawn shenbi-dispatch → dispatcher/cli.py:11 → dispatch_with_write_audit）正常路径可复现误拦; (b) chapter 提取失败时 watch=[] → snapshot 空 → 审计**空转**，写越权完全不可见。判定表 P0「契约被静默违反导致错误执行」字面命中（方向 b）+ 正常路径功能错误（方向 a），取 P0。测试盲区: tests/unit/audit/test_write_audit.py 全部用非 parametric 技能（genre-config/foreshadowing-track/chapter-drafting 手工 dict）——test_undeclared_file_write_blocked 手工构造 post dict，生产 executor 路径根本不可能产出该输入（watch 只含声明面）
  - **F507 | `_declared_patterns` 裸 `except Exception` 吞错** | error | P2 | 证据 write_audit.py:25-26 | 根因: derive_output_files 内部已捕获 ContractError（_shared.py:60-61），此处 except Exception 只会吞意外 bug（如注册表损坏的 AttributeError）→ declared=[] → 与 F501 叠加成全文件误报洪水，且掩盖根因 | 建议: 收窄为 `except Exception: log.error(...)` 至少留痕，或直接删 try（ContractError 已内吞）
- 验证命令: 见 F501 复现；`grep -rn "audit_writes" src/` → 唯一生产调用方 executor.py:285
- 未覆盖行处置 (78%, missing 25-26, 31-35, 63->48):
  - 25-26: must-test + 修复（F507）
  - 31-35（_matches_declared glob 分支）: **dead-code in production**——与 F501 同根：declared 永不含 parametric literal；修复 F501 后此分支复活，必须连带补测
  - 63->48（OWNERSHIP 无条目且匹配声明 → 通过）: acceptable——正向通过分支
- 置信度: high

### src/shenbi/cost/__init__.py
- 处置: deep-read
- 声称检查的不变量: 包文档字符串与模块职责一致
- findings: 无（空包，仅 docstring）
- 验证命令: 读文件
- 未覆盖行处置: 0 语句，无缺口
- 置信度: high

### src/shenbi/cost/estimate.py
- 处置: deep-read
- 声称检查的不变量: CJK/ASCII 双比率估算；80% 阈值告警；未知模型回退默认上限
- findings: 无（注: line 44 默认 logger 用 stdlib `logging.getLogger` 而非 structlog——与 AGENTS.md "use structlog" 精神略偏，但签名支持注入且 dispatch_helper.py:1555 传入 structlog logger，生产路径不走默认分支；M 级备注，不占编号）
- 验证命令: `grep -n "warn_if_over_budget" src/shenbi/pipeline/dispatch_helper.py` → :1555 唯一调用点，传 `logger=log`（structlog）；MODEL_CONTEXT_LIMITS 默认值与 pricing/dispatch_helper 的 deepseek-v4-flash 三处一致（estimate.py:19, pricing.py:16, dispatch_helper.py:74）
- 未覆盖行处置: 100%，无缺口
- 置信度: high

### src/shenbi/cost/ledger.py
- 处置: deep-read
- 声称检查的不变量: append-only 单行 JSONL；坏行跳过不崩；summarize 三口径（total/by_skill/by_chapter）算术一致；线程安全写
- findings:
  - **F510 | 实例级 threading.Lock 对跨实例并发写无效 + 每次 record 新建实例** | error | P2 | 证据 ledger.py:57（实例锁）+ dispatch_helper.py:1346（`TokenLedger(project_dir).record(...)` 每次调用新建实例 → 锁形同虚设）+ parallel_dispatch.py:12（ThreadPoolExecutor 并发）| 根因: 锁的保护对象应是文件而非对象 | 现实风险: O_APPEND 单行小写（~200B）在 POSIX 常见文件系统上事实原子，且当前 parallel 路径未传 state（F504）根本不触发——前瞻性防御缺陷；每次 mkdir(parents=True) 重复执行为小浪费 | 建议: 模块级按路径锁或进程级 fcntl
- 验证命令: `grep -rn "TokenLedger(" src/` → 生产唯一构造点 dispatch_helper.py:1346（每次新建）；report.py:40 只读。`record(skill, chapter, usage)` 签名与调用点位置参数一致（skill_name, chapter, dict）
- 未覆盖行处置 (96%, missing 32-34): `_safe_estimate_cost` ValueError 回退 → **must-test**（spec §5.2 I3 显式行为：未知模型不崩 dispatch 只告警；tests/unit/cost/test_pricing.py 覆盖了 estimate_cost 的 raise，但 ledger 侧包装未测）
- 置信度: high

### src/shenbi/cost/pricing.py
- 处置: deep-read
- 声称检查的不变量: 默认模型与 dispatch 路径镜像一致（防成本错配）；未知模型 raise
- findings: 无
- 验证命令: `grep -n "_ENV_LLM_MODEL\|_DEFAULT_MODEL" dispatch_helper.py` → :70 `SHENBI_LLM_MODEL`、:74 `deepseek-v4-flash`——与 pricing.py:16,27 完全一致，镜像不变量成立
- 未覆盖行处置: 100%，无缺口
- 置信度: high

### src/shenbi/cost/report.py
- 处置: deep-read
- 声称检查的不变量: 空 ledger 优雅输出；百分比分母为零保护；CLI 入口可用
- findings:
  - **F504 | TokenLedger 接线不全：8 个 dispatch_skill 调用点未传 state → API 模式成本系统性少计** | error | P0 | 证据（本文件为受害消费端，根因在 dispatch_helper.py:1310 `if state:` 门控）: parallel_dispatch.py:86 / triggers.py:542 / audit_layer.py:170 / revision_router.py:166 / cli.py:140 / cli.py:819 / closure.py:294 / genesis.py:321 均无 `state=`（`sed` 逐点验证 8 处 grep state= 计数=0，输出已粘贴）；genesis（世界观/角色/大纲大额调用）与审计波（audit_layer）全不在账 → 本文件 render_report 的 total 系统性偏低，成本数据不可信 | 交叉印证: docs/superpowers/specs/2026-08-14-cost-ledger-design.md R1（F302, P0）同一发现，status=Design 未修复；chapter_loop 3 处（:1274,:2831,:3003）已传 state = 部分修复痕迹 | 判定表 P0 例「TokenLedger 少计」字面命中
  - **F505 | ledger chapter 归集键恒 0 → by_chapter 坍缩单桶，"Per-chapter average cost" 失真** | error | P1 | 证据 dispatch_helper.py:1348（`getattr(state, "chapter", 0) or 0`）+ state.py PipelineState 无 chapter 属性（仅 chapter_loop.current_chapter，state.py:142）→ 所有行 chapter=0 | 验证（已运行）: `uv run python -c "from shenbi.pipeline.state import PipelineState; s=PipelineState(); print(hasattr(s,'chapter'), getattr(s,'chapter',0))"` → `False 0` | 效果: report.py:70-72 的 per-chapter average = 单桶均值 = 总成本，标注为"每章平均"误导；即使 F504 修复，分摊口径仍错
  - **F511 | `_try_avg_g3_score` 抓取任意 0-100 数值 → cost-per-quality-point 可为噪声** | error | P2 | 证据 report.py:22-35（`glob("**/*score*.json")` 后收集 dict 内**任意** 0-100 数值：threshold=94、retry 计数、版本号 1-100 等全计入）| 根因: 无 schema 识别 | docstring 已自认 best-effort，但输出行无低置信标注，读者无从辨别 | 建议: 限定读取 scores.json 的分数字段或输出时标注 n 与来源
  - **F512 | main() 尾部 `return 2` 不可达** | error | M | 证据 report.py:86-97（`required=True` + 唯一子命令 "report" → args.cmd 恒为 "report"，line 97 永不执行）| 建议删除
- 关于 print 与 AGENTS.md: print 仅出现在 main() CLI 输出（:93 stderr 错误、:95 stdout 报告），非 framework 日志替代；src 内 gates/g4/chapter_drafting.py(3 处)、pipeline/cli.py(2 处) 同模式先例。**不构成违规**，不立案
- 验证命令: `grep -n "shenbi-cost" pyproject.toml` → :64 入点存在；`ls tests/unit/cost/` → test_estimate/test_ledger/test_pricing/test_report 四件齐全（真实 tmp 账本，无 mock）
- 未覆盖行处置 (70%, missing 25-32, 35, 69->74, 76-77, 97):
  - 25-32（json 解析失败 continue）: acceptable——best-effort 扫描的容错
  - 35（无分数返回 None）: must-test——空项目不输出 cpq 行的契约
  - 69->74（by_chapter 空分支）: acceptable——空 ledger 早退已覆盖主路径
  - 76-77（cpq 计算）: must-test——输出正确性（注意 F511/F505 使该行当前必错，修后需回归）
  - 97: dead-code（F512）——删除
- 置信度: high

### src/shenbi/orchestration/__init__.py
- 处置: deep-read
- 声称检查的不变量: 包存在性
- findings: 无（空包，仅 docstring "orchestration package."）
- 验证命令: 读文件
- 未覆盖行处置: 0 语句，无缺口
- 置信度: high

### src/shenbi/orchestration/escalation_bridge.py
- 处置: deep-read
- 声称检查的不变量: trend 表第 7 列提取 overall；表头行过滤；malformed 行跳过；参数透传 check_escalation
- findings:
  - **F506 | run_escalation_check 与 parse_resonance_scores 生产零调用（dead-wire bridge）** | error | P2 | 证据: `grep -rn "run_escalation_check\|parse_resonance_scores" src/`（已运行）→ 除本模块外零命中；生产 escalation 路径 chapter_loop.py:1013-1029 自行gather 输入（`_get_recent_resonance_scores` 读 per-chapter audits/chapter-N-resonance.md，:1481-1495）直接调 check_escalation，绕过本 bridge；resonance_trend.md 的行格式契约仅靠 chapter_loop.py:1355,1369 注释 + tests/unit/pipeline/test_resonance_persistence.py 维系 | 根因: wave4 计划（archive/2026-06-28-...-wave4-loop-approval.md:539-575）落地了脚手架但编排器随后另起实现 | 影响: 两套 resonance 来源（trend 文件 vs per-chapter 报告）并存，bridge 侧格式演进无生产反馈；escalation 传播本体（chapter_loop 侧）不属本区
  - **F513 | `if val > 0` 丢弃合法 0 分** | error | M | 证据 escalation_bridge.py:20-21 | resonance 0 分是崩塌极端信号，过滤后不参与 escalation 判定；因 F506（dead-wire）生产影响被包含，降为 M；若 bridge 复活须改为 `>= 0` 并按 spec 跳过 "pending" 行（该职责已由 float() ValueError 承担）
- 验证命令: grep 输出如上；tests/unit/orchestration/test_bridges.py 覆盖 parse 提取/双评分争议/collapse 三例，真实但只测 dead 路径
- 未覆盖行处置 (79%, missing 17->14, 20->14, 22-24, 37-38):
  - 37-38（run_escalation_check 函数体）: **dead-code in production**——处置二选一: 接线（替换 chapter_loop 手工 gather）或删除 bridge 保留纯函数；维持现状 = 测试 100% 保活僵尸
  - 17->14（列数 <7 跳过）/ 20->14（val<=0）/ 22-24（ValueError）: 若保留模块则 must-test（格式脆弱点：硬编码 cells[6]，列序变更即静默漏行——与 chapter_loop 写入侧仅注释耦合）
- 置信度: high

### src/shenbi/orchestration/scoring_bridge.py
- 处置: deep-read
- 声称检查的不变量: 薄包装不改语义；needs_arbitration = not agreed
- findings:
  - 并入 **F506**（生产零调用）: `grep -rn "validate_dual_scorer\|check_single_scorer_collapse\|from shenbi.orchestration" src/`（已运行）→ 唯一命中本模块自身 import（scoring_bridge.py:7,14,23）。check_scorer_agreement / flag_score_collapse 在 src 内**无任何其他生产消费**——双评分一致性与 collapse 检测两个质量机制在 pipeline 未启用（AGENTS.md 强调 G3.4 独立评分，此为其自动化延伸件）。severity 维持 P2（死代码/未接线），但建议 either 接入评分链或显式归档
- 验证命令: grep 如上；tests/unit/orchestration/test_bridges.py:26-36 真实断言包装行为
- 未覆盖行处置: 100%（由 dead-path 测试覆盖——覆盖率高但价值低，佐证 F506）
- 置信度: high

---

## 汇总

| 编号 | 文件 | 标题 | 类别 | 严重度 |
|---|---|---|---|---|
| F501 | write_audit.py | parametric 技能审计双向失效（误拦/空转） | error | P0 |
| F502 | snapshot.py | truth 文件整体删除逃过 OWNERSHIP 审计 | error | P0 |
| F503 | snapshot.py | 非法 JSON 替换逃过 field 级审计（已复现 violations=[]） | error | P1 |
| F504 | report.py(消费端)+dispatch_helper(根因) | 8 调用点未传 state → TokenLedger 系统性少计 | error | P0 |
| F505 | ledger.py(归集)+dispatch_helper:1348 | chapter 键恒 0 → by_chapter 坍缩 | error | P1 |
| F506 | escalation_bridge.py + scoring_bridge.py | 两 bridge 生产零调用（dead-wire） | error | P2 |
| F507 | write_audit.py:25-26 | 裸 except Exception 吞错 | error | P2 |
| F508 | snapshot.py:103-104 | 未变更/双 None 标 "modified" | error | P2 |
| F509 | snapshot.py:82-83 | 无 id 记录 "None" 键碰撞 | error | P2 |
| F510 | ledger.py:57 | 实例锁跨实例无效 | error | P2 |
| F511 | report.py:22-35 | avg G3 分数抓取噪声 | error | P2 |
| F512 | report.py:97 | 不可达 return 2 | error | M |
| F513 | escalation_bridge.py:20-21 | val>0 丢 0 分 | error | M |
| F514 | ownership.py:22+enums.py:1 | FileChange.status 等 Literal 未入 enums 单一词表（enum 声明"所有 Literal 必须从此处 import"与实践漂移） | error | M |

- 覆盖统计: 13/13 清单文件全部 deep-read；每文件均含不变量清单 + 验证命令 + 覆盖缺口逐行处置
- 低置信度文件: 无（三个 P0 均经 `uv run python -c` 实跑复现或三处代码联动确证）
- 未覆盖文件: 无
