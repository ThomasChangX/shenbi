# Z6 分区第四轮独立复核报告（fresh-context 全量重读）

> 复核人：第四轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11 findings（F601-F611）+ 第一轮 8（F612-F619）+ 第二轮 10（F620-F629）+ 第三轮 7（F630-F636）逐条独立验证；新发现编号 F637-F639。
> 方式：49/49 文件全文 read + `PYTHONPATH=src python3` 实际运行验证（F601/F605/F612/F636/F637/F638/F639 均本轮回放）+ grep 全仓调用方验证 + **真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对**（truth/pending_hooks.md、truth/resonance_trend.md、truth/audit_drift.md、genre-config.json、chapters/chapter-1.md、config-change-log.jsonl、tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未达成**（新发现 3 条：P1 × 1、P2 × 2）。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §3）
- 初审 11 findings：**11/11 成立**（无整条误报，无严重度异议）
- 第一轮 8 findings：**8/8 成立**
- 第二轮 10 findings：**10/10 成立**
- 第三轮 7 findings：**7/7 成立**
- 新发现：**3**（F637-F639，P1 × 1，P2 × 2）
- 误报数：**0**；严重度异议数：**0**
- 判定：**非 0 新 finding（3 条），未达收敛目标**
- 关键独立验证结论：
  - 四条 P1（F601/F602/F612/F620）本轮以**直接运行/代码精读/全仓 grep** 三重复核，无反证；
  - 前 36 条（F601-F636）逐条核对无整条误报、无严重度异议；
  - 本轮 3 条新 finding 全部落在前四轮未触及的方向：**真实数据格式验证**（F637，P1）、**治理链键形一致性**（F638，P2）、**CLI 文件级边界**（F639，P2）。

---

## 1. F601-F636 判定表（逐条独立复核）

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（P1 维持，无反证） | 本轮直接运行：baseline dialogue=10 → current=1 → `is_drift: False | severity: NONE`。代码确认 linguistic_drift.py:215（`max(…,5.0)`）与 :218（严格 `>5.0`）：对话塌陷分支把比值置为恰好 5.0，永不满足 `>5.0` → 塌陷单独存在时 is_drift 恒 False。docstring :185-188 明确承诺 "<20% of baseline" 触发 → 契约 vs 实现 off-by-one 成立。P1 维持（当前被 F602 无 baseline 掩盖为潜伏，不影响本体）。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持，无反证） | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py` re-export；`find . -name linguistic_baseline.json` 0 命中（含 novel-output/xinghuo-ranqiong/style/ 仅有 style_profile.md）。chapter_loop.py:2018 `log.warning("no_linguistic_baseline")` + return None，:2722 每章走此分支。计划 07-19-07 Task 5a 显式接线指令未履行。P1 维持。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链确认：write_audit.py:54-56 → record.py:24 `blocked = bool(violations) or bool(drift)` → GATE_FAIL 阻断 ship。test_drift.py 仅覆盖 md-ghost 方向。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'`，:226 `sum(text.count(c) for c in chars)`。前轮已运行复现 count=4（应为 2）。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持；补充一处同根变体） | 本轮运行：`compute_linguistic_metrics('参数系统阈值。'*5)`（空 terms）→ `system_term_density: 1028.5714`。review1 影响修正（被 is_drift 门控、无管道暂停）复核同意；F620 进一步证明即使 severity 成立也不暂停。**补充（非新 finding）**：`load_drift_config` 对 `system_terms: null` 走 `list(None)` → `TypeError: 'NoneType' object is not iterable` 崩溃（本轮实测）——与空串/字符串误配同根（配置驱动 terms 无类型校验），F605 建议的"非 list → 回退默认"一并覆盖。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过（前轮运行复现 ACCEPTED 且落盘）。下游 str 落盘使 `score >= "58"` TypeError（review3 已核）。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs detect_drift 读 `dialogue_density`；:296/:335 硬编码术语 vs :107 config 驱动。本轮补充核实：真实项目 context/ 目录亦无 linguistic_baseline.json（平行实现同样从未产出）。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | cjk.py:54 桶 token 为相邻双字符 `""`/`''`/`「」`/`『』`，正常文本引号包围内容不相邻 → `text.count` 恒 0；:61-62 "whole token" 修复对引号桶反而永久为 0。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py 全文无任何 logging/structlog import；:40-43 两处 `break` 无日志、:47-48 `safe_write` 改写文件。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无 → 单条脏数据整批崩溃。F622 为独立类型失败模式。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持；review1 证据细化正确） | config_coherence.py:109 `key.startswith("auditDimensions.")`；整键对象覆盖绕过（review1 实测 ACCEPTED）。本轮补充：F631 已覆盖 falsy 值向量；F638（本轮新发现）覆盖 snake_case 键形向量——三个绕过向量并存。P2 成立。 |

### 1.2 第一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持，无反证） | 本轮直接运行：baseline stm=60 → current stm=110 → `is_drift: False | severity: ESCALATE`；stm=55 → `is_drift: False | severity: HARD`。chapter_loop.py:2024 `if result.is_drift:` 包裹 log + ESCALATE raise + HARD/WARN 注入（:2024-2043）。计划 07-19-07:749 绝对阈值触发契约。P1 维持。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"破折号": "——"`/`"省略号": "……"` 重复字符 + :226 按字符迭代 → 一个 `——` 计 4。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | config_coherence.py:105-133 循环内逐 key `_set_nested`+`_append_audit_trail`，`safe_write` 在循环外 :142 → 中途 ConfigError 时 trail 已追加、config 未落盘。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | writer.py:57 `json.loads(lines[-1])` 无 try；replay.py:38-43 同文件可自愈 → 不对称确认。三个生产调用点均有外部缓解（safe_write.py:127-135 / audit/record.py:42-46 / migrate.py 先 replay）——writer 自身缺口仍成立。P2 成立。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | 本轮 grep：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` → 仅 :37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。前轮运行复现 IndexError。P2 成立。 |

### 1.3 第二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持，无反证） | 本轮代码精读确认：chapter_loop.py:2722-2728 `try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed")`；函数内 :2032-2038 raise DriftEscalationError（"Pipeline paused for human review"）；:2719 注释自称 "non-blocking"——调用点与函数内契约自相矛盾。grep 唯一生产调用点即 :2722。P1 维持。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | recall.py:41-46 `.get()` 只判 None 不判类型。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持） | compute_stats.py:52 单 U+2026 vs 真实文本双 `……`（与 :23 PUNCT_MAP 一致）。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无 skills 校验 | P2 | **成立**（P2 维持） | 本轮按 master.json 路径规范化重算：master 59 名（`skills/<name>/SKILL.md` 形式）、skills/ 目录 74、缺失恰好 15 个（名单与 review2 一致）、反向 0。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity); e = up(e)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、无生产调用方）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | 本轮 grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export。补充核实：shenbi-drift-guidance skill 走 `python -m shenbi.skill_utils.drift_detection` CLI（LLM 层调用），不消费该函数——函数仍为死导出。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件；对比 records/drift.py:38-39 非 `|` 行即 break。真实 resonance_trend.md 为单表（`grep -c "^## "`=0），潜伏。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | compute_stats.py:16 SENT_ENDS 含 `；`，:63 segment_sentences 只按 `。！？\n`，:87 segment_paragraphs 用 SENT_ENDS。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | 本轮 grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用。P2 成立。 |

### 1.4 第三轮 F630-F636

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | **成立**（P2 维持） | 本轮代码确认 revision_router.py:62-64（注释明示 "config.resonance_global_floor" 后跟 `= 50`）vs thresholds.py:30 `=65`；chapter_loop.py:1897 显式传 `state.config.resonance_global_floor` 覆盖默认值 → 潜伏。P2 成立。 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | **成立**（P2 维持） | config_coherence.py:109、g0_config_coherence.py:94 均 `is False`；audit_layer.py:112-113 `audit_dims.get(dim_key, False)` 任何 falsy 即不激活。本轮补充：G4 侧 contracts/skills/genre_config.py:93 `if v is False` 同族（Z6 外，佐证该家族扩散）。P2 成立。 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | **成立**（P2 维持） | linguistic_drift.py:222-226 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50。P2 成立。 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | **成立**（P2 维持） | compute_stats.py:79 docstring 与 :80 `re.split(r"\n\s*\n")` 矛盾；`\n` ∈ SENT_ENDS :16。P2 成立。 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | **成立**（P2 维持） | 真实章节 chapter-1.md 含 `<!--META-BEGIN-->`~`<!--META-END-->`（约 515/4177 字符）；两处统计入口都无剥离。P2 成立。 |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | **成立**（P2 维持） | 本轮 grep：`update_genre_config` 仅 config_coherence.py 自身 + 测试 + 计划文档；真实 config-change-log.jsonl 仅 1 条 2026-07-19 E34 一次性修复产物；skills/shenbi-genre-config/SKILL.md:204 直写文件。P2 成立。 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | **成立**（P2 维持） | 本轮直接运行：base 全 0 + current em_dash=0.33 → `is_drift: True | severity: WARN`。linguistic_drift.py:202。P2 成立。 |

**严重度异议：0 条。** 四条 P1（F601/F602/F612/F620）本轮全部以直接运行/代码精读复核，属"计划文档显式承诺的干预/安全网被实现静默失效"，与决策表 P1 档（违反显式契约）吻合；F637（本轮新）同档。其余 P2 均属边界/统计口径/死代码/潜伏类，无高判或低判。

---

## 2. 新发现（F637-F639）

### F637 | records 判据 12 漂移检查在真实 pending_hooks.md 上恒为空操作：真实文件无 `## hooks` YAML 与 `## 活跃伏笔` 表，ch-025 快照有 → 格式在 ch25→ch56 间分叉，drift/block-ship 安全网静默失效且单测全部掩盖 | 真实数据验证 | P1
- 证据：
  - 真实文件 `novel-output/xinghuo-ranqiong/truth/pending_hooks.md`（143 行，last_updated 2026-07-17，ch56）标题仅 `## 第56章伏笔呈现`/`## 第56章生命周期状态更新（foreshadowing-track）` 等章节附录结构——**无 `## hooks`、无 `## 活跃伏笔`、无 `## 伏笔时间线`**（`grep -n "^#"` 全清单见上）；
  - 对照 `tests/fixtures/truth-pending_hooks.md`（parser docstring :3 自称"真实 fixture"）与 `tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md`（真实项目 2026-06-11 ch25 快照，project=星火燃穹）——两者均有 `## 活跃伏笔`（:12）与 `## hooks`（:20）；
  - `git log -p --follow -- novel-output/xinghuo-ranqiong/truth/pending_hooks.md | grep -c "## hooks"` → **0**（真实文件从未有过权威结构）；
  - 消费链已接线：`dispatcher/executor.py:240,266`（audit_writes → record_audit_outcome → record.py:24 `blocked = bool(violations) or bool(drift)` → GATE_FAIL）；`write_audit.py:54-56` 对每个 post .md 执行 `parse_records` + `parse_markdown_table` + `detect_cross_section_drift`；`audit/snapshot.py:110` 亦消费 `parse_records`（记录 diff）；
  - skill 契约证据：`skills/shenbi-foreshadowing-track/SKILL.md:8` 声明 `reads: {file: truth/pending_hooks.md, fields: [活跃伏笔, 伏笔时间线]}` —— 真实文件无这两个 section → 字段级过滤回退整文件 + WARN（AGENTS.md Layer B 逃逸口），契约与实际输出同样分叉。
- 根因：shenbi-foreshadowing-track 在 ch25（2026-06-11）后改变了 pending_hooks.md 的书写结构（改为逐章附录），而 parser/drift/snapshot 三件套仍只认 ch-025 快照所代表的权威格式（YAML block + md 派生表）。真实文件对 parser 而言不可解析：`parse_records → []`、`parse_markdown_table → {}`、`detect_cross_section_drift([], {}) → []` → **判据 12 的漂移检查与 block-ship 在唯一真实项目上永不触发**（既无假阳性也无真阳性，权威记录源整体消失）。单测（test_parser/test_drift/golden）全部基于 fixture 格式 → 测试通过反而掩盖真实世界的空转（P1 档"测试失效掩盖真实缺陷"）。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "from shenbi.records.parser import parse_records; from shenbi.records.drift import parse_markdown_table, detect_cross_section_drift
  live = open('novel-output/xinghuo-ranqiong/truth/pending_hooks.md', encoding='utf-8').read()
  print(len(parse_records(live)), parse_markdown_table(live), detect_cross_section_drift(parse_records(live), parse_markdown_table(live)))
  -> 0 {} []
  fixture: parse_records -> 3 | md rows -> 3"
  PYTHONPATH=src python3 -c "from shenbi.audit.write_audit import audit_writes; print(audit_writes('shenbi-foreshadowing-track', {}, {'truth/pending_hooks.md': open('novel-output/xinghuo-ranqiong/truth/pending_hooks.md', encoding='utf-8').read()}).drift)"
  -> []  # 生产接线路径下 drift 恒空
  ```
- 建议方向：二选一并补测试——(a) 将 shenbi-foreshadowing-track 的 pending_hooks.md 输出恢复/迁移回权威格式（`## hooks` YAML block + `## 活跃伏笔` 派生表 + `## 伏笔时间线`），使判据 12 检查与 skill 契约、ch-025 快照一致；(b) 若逐章附录是新的既定格式，则更新 parser/drift/snapshot 与 skill reads 契约并显式废弃旧结构。无论哪条：新增一条"用真实格式样本跑 records 三件套"的回归测试（现测试只喂 fixture），否则该安全网继续空转无人知晓。

### F638 | 治理链 auditDimensions 键形分叉：audit_layer 支持 snake_case 回退，config_coherence Rule 1 与 G0 只认 camelCase → snake_case 配置禁用关键审计维度时绕过写侧 rationale 与读侧 G0，审计却照常被禁用 | 跨文件状态一致性 | P2
- 证据：`src/shenbi/pipeline/audit_layer.py:100-107`（`genre_config.get("auditDimensions")`，缺失则回退 `audit_dimensions`）；`src/shenbi/config/config_coherence.py:109`（`key.startswith("auditDimensions.")`）；`src/shenbi/gates/g0_config_coherence.py:79`（`config.get("auditDimensions", {})`，无 snake_case 分支）。
- 根因：三层治理对同一配置键的键形认知不一致——audit_layer 把 snake_case 视为合法输入（禁用即不激活审计），而写侧治理（Rule 1）与读侧治理（G0）只检查 camelCase → `{"audit_dimensions": {"texture": false}}`（或 update_genre_config 写入 `audit_dimensions.texture: False`）时：Rule 1 不触发（key 不以 "auditDimensions." 开头）、G0 不报 G0.cc.critical_audit_disabled、audit_layer 却真实停用 texture 审计——"禁用关键审计必须带 rationale" 与 "G0 必须拦截关键审计禁用" 两道防线同时失明。与 F631（falsy 值向量）、F611（整键向量）同族但为独立键形向量；F630/F632 类双源分叉的又一实例。当前真实配置为 camelCase（novel-output 实键核实）→ 潜伏。
- 验证命令+输出：
  ```
  update_genre_config(d, {'audit_dimensions.texture': False}, rationale='x') -> ACCEPTED（Rule 1 绕过，且 config 同时出现 auditDimensions.texture=True 与 audit_dimensions.texture=False 的 split-brain）
  check_config_coherence(tmp, resonance_global_floor=65)（配置为 {"audit_dimensions": {"texture": False}}）-> []（G0 不报）
  get_active_genre_audits({"audit_dimensions": {"texture": False}}) -> []（texture 审计实际不激活）
  ```
- 建议方向：统一三层的键形处理——要么 G0/config_coherence 与 audit_layer 同样支持 snake_case 回退，要么 audit_layer 删除回退并全链强制 camelCase（在 G4 genre_config 契约层校验键形）；补单测（snake_case 禁用 texture：Rule 1 必须拒绝 或 G0 必须报 issue）。

### F639 | compute_drift CLI 对缺失趋势文件静默跳过并 exit 0（误判"无漂移"）；真实项目缺 arc_payoff/volume 趋势文件 → CLI 只跑半个检测；`--write-audit-drift` 向 drift-guidance 单一写者拥有的 audit_drift.md 追加无协调 | 边界/CLI | P2
- 证据：`src/shenbi/skill_utils/drift_detection/compute_drift.py:261-278`（`if resonance_path.exists():` / `if arc_path.exists():` 静默跳过，无缺失告警）；`:283-284`（`--write-audit-drift` 追加 `## drift findings` 到 `truth/audit_drift.md`）；真实项目 `novel-output/xinghuo-ranqiong/truth/` 仅有 resonance_trend.md（1 行 ch55 数据），**无 arc_payoff_trend.md、无 volume_score_trend.md**；`skills/shenbi-drift-guidance/SKILL.md` 声明 reads 含后两者且 :32 单一写者（single-writer）拥有 audit_drift.md。
- 根因：CLI 以 `exists()` 守卫而非报错——路径笔误/文件缺失时静默 exit 0，排障者与下游（drift-guidance skill）把 exit 0 当作"无漂移"；真实项目缺两个趋势文件 → arc/volume 半边检测恒不运行，与 F626/F627 叠加使 drift-guidance 的数据输入面进一步收窄。且 CLI 追加 audit_drift.md 与 skill 的单一写者/append_dedup 契约（frontmatter `mode: append_dedup, key: chapter`）无协调——CLI 一旦在有 findings 的项目上运行会写入无 dedup 键的裸 `## drift findings` 段。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -m shenbi.skill_utils.drift_detection --resonance /nonexistent/trend.md --arc-payoff /nonexistent/arc.md; echo $?
  -> （无任何输出）0   # 缺失文件静默通过
  # 构造 5 行下滑数据 + --write-audit-drift -> 生成 truth/audit_drift.md 裸段（见下）
  -> - [monotonic_decline] 情感落地: ... | exit: 1 | truth/audit_drift.md 内容以 "## drift findings" 开头（无协调追加）
  ```
- 建议方向：缺失趋势文件时 CLI 报错退出（stderr + exit 2），或至少 WARN 并注明"半边检测未运行"；`--write-audit-drift` 追加内容带上 chapter/key 元数据并与 drift-guidance 的单一写者契约协调（或改为仅由 skill 统一写）；为真实项目补 arc_payoff/volume 趋势文件或显式声明该方向未启用。

---

## 3. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2054 漂移检查全链、:2716-2728 ESCALATE 调用点、:1897 check_resonance 显式 floor）、revision_router.py（:62-64 DEFAULT_RESONANCE_FLOOR、:108/123）、audit_layer.py（:100-113 激活语义）、g0_config_coherence.py（:35-42 关键维矩阵、:75-95 floor/audit 检查）、audit/write_audit.py（:54-56 records 消费）、audit/record.py（:24 block 语义）、audit/snapshot.py（:110 parse_records 消费）、dispatcher/executor.py（:240,266 生产接线）、contracts/skills/genre_config.py（:93 `is False` 同族佐证）、plugins/master.json（59 skills 规范化对比）、skills/ 目录（74，shenbi-* 73 + using-shenbi 1）、skills/shenbi-foreshadowing-track/SKILL.md（:8 reads 字段契约）、skills/shenbi-drift-guidance/SKILL.md（单一写者 + CLI 调用）、skills/shenbi-genre-config/SKILL.md（:204 直写）、novel-output/xinghuo-ranqiong/（truth/pending_hooks.md 143 行真实结构、truth/resonance_trend.md 单表单行、truth/audit_drift.md 9 行无 CLI marker、genre-config.json 10 维 camelCase、chapters/chapter-1.md META、config-change-log.jsonl 单条、style/ 与 context/ 均无 linguistic_baseline.json）、tests/fixtures/truth-pending_hooks.md + tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照）、git log（真实 pending_hooks.md 从未含 `## hooks`；master.json 2026-08-01）。
- 与前几轮观察的衔接（不重复立项）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）、compute_pattern main 非 dict 章节 `.get` 崩溃、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize round 名解析脆弱性（`name.split("-")[1]`）、compute_drift `_append_audit` 重复追加无幂等——本轮维持观察，未升 finding（输入契约/数据稀疏类，无生产数据路径证据）。

---

## 4. 返回摘要

**复核统计：新 finding 3（F637-F639：P1 × 1、P2 × 2）/ 误报 0 / 严重度异议 0。**

**判定：非 0 新 finding（3 条），未达收敛目标。**

F601-F636 共 36 条全部成立（初审 11 + 第一轮 8 + 第二轮 10 + 第三轮 7，无整条误报）；四条 P1（F601/F602/F612/F620）本轮以直接运行 + 代码精读 + 全仓 grep 三重复核无反证。本轮 3 条新 finding 集中在**前三轮从未触达的"真实数据格式"方向**：

1. **F637（P1，本轮最重要）**：records 判据 12 漂移/block-ship 检查在唯一真实项目上**恒为空操作**——真实 pending_hooks.md（ch56）无 `## hooks`/`## 活跃伏笔` 结构（ch-025 快照有，格式在 ch25→ch56 间分叉且 git 历史从未含 `## hooks`），parse_records/parse_markdown_table/detect_cross_section_drift 全返回空，write_audit 生产接线（dispatcher/executor.py:240,266）但 drift 永不触发；skill 契约 reads 字段 [活跃伏笔, 伏笔时间线] 亦与真实文件分叉；单测全用 fixture → 测试通过掩盖真实空转。建议与 F603/F616 修复绑定：先决定权威格式归属（恢复 canonical 结构 or 更新三件套+契约），再补真实格式回归测试。
2. **F638（P2）**：治理链 auditDimensions 键形分叉——audit_layer 认 snake_case 回退、config_coherence Rule 1 与 G0 只认 camelCase → snake_case 禁用关键审计绕过两道治理且审计实际停用（F611/F631 之外的第三绕过向量，运行复现）。
3. **F639（P2）**：compute_drift CLI 缺失趋势文件静默 exit 0（误判无漂移）+ 真实项目缺 arc_payoff/volume 文件致半边检测恒不运行 + `--write-audit-drift` 与 drift-guidance 单一写者契约无协调（运行复现）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转（格式分叉 + 测试掩盖） | P1 | novel-output/xinghuo-ranqiong/truth/pending_hooks.md; parser.py:18-19; drift.py:23; write_audit.py:54-56; snapshot.py:110; snapshots/chapter-025 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过 Rule 1 与 G0，audit_layer 照常禁用） | P2 | audit_layer.py:100-107; config_coherence.py:109; g0_config_coherence.py:79 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 真实项目 arc/volume 半边不运行 + audit 追加无协调 | P2 | compute_drift.py:261-278,283-284; novel-output truth/ 目录 |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review4.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
