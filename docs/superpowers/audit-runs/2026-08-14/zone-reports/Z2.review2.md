# Z2 分区第二轮独立复核报告（Z2.review2）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）
- 模式: fresh-context 全量重读 38 文件；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；git show 仅读历史）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖），不依赖前两轮转述；另读了跨区关键消费面以验证裁决（g1.py / g2.py / g3.py / g3_independence.py / dispatch_helper.py / write_audit.py / _shared.py / g4/genre_config.py / g4 三私有实现 / round-exec.sh / first-novel.md / docs/framework/dispatcher.md / decisions-schema.md / OPTIONAL_READS）。
2. **独立验证方式**：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前两轮一致）；十余个 `python -c` 运行时探针（G2 端到端、字段过滤 json/md、ownership 越权、decisions rationale、codex progress.json、G1 空集）；`git show 99f91f0:tests/dispatch-subagent.sh`（原 shell 版门序对比，F227 关键证据）。
3. **前两轮判定**：26 条 findings（F201-F226）**全部实质成立，0 条整体误报**；F220 计数修正（5→6）成立；F211 表述修正成立；复核轮 2 条严重度异议（F214→P1、F218→P1）**经独立反证尝试后支持**。
4. **本轮新发现 8 条（F227-F234）**，集中在两类前两轮盲区：
   - **executor G2 编排三缺陷**（F227 门序回归 / F228 glob writes 不展开 / F229 单 file_type 批处理误套与漏套）——前两轮只覆盖了 G2 的 rc 检查（F212）与 fail-open（F219），从未核查"G2 在技能执行前对尚不存在的输出做校验"这一门序问题及其输出形态不兼容；
   - **契约层其余模型/工具边界**（F230 fields json/md 规范化不一致——复核轮"md 与 json 同构"断言为误；F231 genre-config 消费侧 povMode/sensitivityFlags 缺键；F232 decisions P2.5 空串绕过；F233/F234 ownership 两处审计盲区）。

---

## 一、复核结论表（F201-F226 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮独立验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 重读三模型 + g4 私有实现：chapter_planning.py:26-57（typed change/defer activation plan）vs g4/chapter_planning.py:99-102（s7 hook ops 存在性）；context_composing.py:24-27（section_count==9）vs g4/context_composing.py:74-86（P1-P7/route 标题）；volume_outlining.py:24-45（entity_hooks>=3 且 types>=2）vs g4/volume_outlining.py:59-72（KR/张力曲线/跨卷桥）。grep 证实无生产消费者。双实现已发散，契约层"单一信源"被架空——P1 成立。 |
| F202 | P2 | **成立** | grep `load_skill_contract\|REGISTRY[` src/ 无生产消费者（仅 __init__ re-export）；g4/genre_config.py:13 直接 import 模型。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import（全仓 grep 仅 novel.py 自身）；g6.py:51 raw dict 读取属实——死的是模型非行为。 |
| F205 | P2 | **成立** | `from shenbi.contracts.schemas` 生产命中仅 adapt/decisions/hooks/registry（本轮复核 grep 复证）；scores.py 无消费者。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 全仓无生产 import（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点实测（audit_layer.py:174,181,188,191,193 / revision_router.py:204,206 / parallel_dispatch.py:218,220 / route.py:41）+ 复核增补 chapter_loop.py:1797（"BLOCKING"/"FAIL" in text）、:409-438（G4Severity 自建词表）——本轮重读复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 复制 status.py:17-24 GateStatus；status.py:27,55 确有 UNIMPLEMENTED 而 base.py Literal 缺（GateOutcome 无法表达 stub gate）。 |
| F210 | P2 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测行 40/79-80/94，Δ32-40。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 零测试（grep 无测试设置该 env 于 executor 路径；coverage 产物显示 169-188 missing）；g1.py:174-190 有测试（test_g1.py:245-258）、dispatch_helper 设置有测试（test_dispatch_helper.py:172-187）——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；rc!=0 且 stdout 空 → 裸 JSONDecodeError。P2 维持（门二进制失败非正常路径）。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564 对比属实。 |
| F214 | P2 | **成立；支持复核异议→建议 P1** | codex.py:20-48 `_record_completion` 不写 current_scorer_agent；grep 唯一写入点 dispatch_helper.py:1939。**反证尝试**：独立 dispatch 后 G3 是否根本不运行（若如此 fail-closed 无触发面则 P2 合理）——executor 确不跑 G3，但 pipeline 的 run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时**主动写入伪造** `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history → G3.4（fail-closed）与 G3.5 以生产方自写证据静默放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。反证不成立，异议成立（证据主体在 pipeline/，跨区）。 |
| F215 | P2 | **成立** | 实测 `\{[^{}]*\}`：嵌套 JSON 取内层（`{"final_score":90,"dimensions":[{"num":1,"score":88}]}` → 匹配 `{"num":1,"score":88}`）；多对象取第一个。 |
| F216 | P1 | **成立，P1 维持** | **反证尝试**：customRules 为空时是否无真实禁用维度触发面——fixture genre-config-example.json 实测含 `texture: False` 禁用维度 + 28 条 customRules（规则 7 恰为 SKILL.md 可自动检查规则）；`if disabled and self.custom_rules:`（genre_config.py:94）空规则跳过已复现。反证不成立。P1（G4 校验洞，正常路径）成立。 |
| F217 | P2 | **成立；新提低置信严重度异议（P2→P1）见第三节** | 实测 SKILL.md 内 5 处 CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :187 15-30 / :192 15-25 / :193 30-40 开卷 / :194 15-25）+ docstring 20-30 + 代码 15-35；场景类型 4 源矛盾。g4/pacing_design 以模型强制 15-35 → 25-35% 输出过门但违反 SKILL.md:91 声明上限。 |
| F218 | P2 | **成立；支持复核异议→建议 P1** | 实测部分匹配：md 与 json 均只返回命中 section、matched=True、无 WARN；全缺才全文+WARN。**反证尝试**：AGENTS.md:87-89 是否只承诺"全部缺失"场景——原文 "If a declared field is missing"（任一/单数）→ 部分缺失亦应全文+WARN，实现不满足。反证不成立。P1（违反显式契约 + 正常路径 truth 文件随剧情演进缺 section 属常态，下游静默在信息不全状态决策）成立。 |
| F219 | P2 | **成立（增补成立）** | 实测 G1 空输入 → `G1.0 SKIP` + 整体 PASS（真空 PASS 复现）；executor.py:74-75/:107-108 对 ContractError 静默降级。增补点（_audit_watch_paths 空监视面、_truth_file_set 缓存固化空集）本轮重读通过。P2 维持（触发需 registry/契约损坏）。 |
| F220 | P2 | **成立（计数修正维持：6 条）** | ownership.py:73-94 实测 6 条目：genre-config(field) / foundation-review(field read, tropeInventory) / plant(record_create) / track / resolve / state-settling(record_field)。实质结论（矩阵小、file-level 兜底 write_audit.py:62-64）成立。 |
| F221 | P2 | **成立** | 四源实测：模型 8 字段无 tropeInventory（genre_config.py:30-37）vs SKILL.md:286,290 规则 1"顶层字段数=8" vs fixture 9 键（含 tropeInventory）vs OWNERSHIP _GENRE_KEYS 9 键；`extra=ignore` 静默丢弃 tropeInventory；g4 `GenreConfig.model_validate(fixture)` PASS。P2（多源漂移 + 可检查规则未编码；权威方未定故不升 P1）。 |
| F222 | P2 | **成立** | 三处实测：executor.py:167-188（basename fnmatch + **存在性判断**——仅丢不存在的文件，且对 glob 字面路径做 exists() 检查会整条丢弃）、g1.py:174-190（仅丢未匹配 glob）、dispatch_helper.py:382（设置者，:1861 注入 env）。**增补（本轮实测）**：executor 侧对 glob 读路径（如 `characters/major/*.md`）在宽泛 skip 模式（`*.md`）下因字面路径 exists()=False 被整条丢弃，即使真实匹配文件存在——与 g1 的 glob 展开语义分歧的具体表现。P2 维持。 |
| F223 | P2 | **成立** | 实测：progress.json 损坏（`{corrupt json!!`）→ `_record_completion` 裸 JSONDecodeError 未捕获（codex.py:28-29）；:114/:116 对同一 stdout 二次 json.loads；缺 final_score 键静默记 0。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` 唯一命中 g3.py:153（读取）；codex.py:40-46 写 `skills[skill][test_type]={score,status}` 无 output_files → G3.3 永 SKIP（g3.py:187）。P2 维持（pipeline 模式 G2 兜底缺失，但 G4/G6 已覆盖结构校验）。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields 引用；消费点仅 dispatch_helper.py:592,1229-1230。独立路径 codex 子代理直读 round_dir 全量文件，字段过滤不生效（token 浪费 + 信息面超声明）。与 F218→P1 不矛盾：F218 是 pipeline 主路径的**静默部分信息**问题，F225 是 T1/codex 路径**过滤完全不生效**（无 escape hatch 语义可谈）。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise SubAgentProtocolError；cli main 无 try/except → traceback。M 合理。 |

**误报统计**：整体误报 0 条；部分误报 1 条（F220 计数 5→6，实质成立）；表述修正 1 条（F211，实质成立）。前两轮 26 条全部实质成立。

---

## 二、新发现（F227-F234）

### F227 | executor.dispatch G1/G2 在技能执行前校验尚不存在的输出——PR-20 翻译门序回归，文档化 first-novel 流程第一步即失败 | 漏报（功能错误/回归） | P1
- 证据: `src/shenbi/dispatcher/executor.py:190-214`（run_g1 → run_g2 → **然后才** dispatch_codex）；`src/shenbi/dispatcher/modes/codex.py:61-66`（技能执行在 codex exec 内，位于 G2 之后）；git 历史 `git show 99f91f0:tests/dispatch-subagent.sh`（原 shell 版 G2 注释明示 **"G2: Output validation — verifies the skill's output files exist and are well-formed... (the skill execution happened earlier)"**——原语义是校验**已产出**的输出，脚本本身是 G3.4 评分独立性包装）；`docs/getting-started/first-novel.md:41`（官方指南第 1 步 `uv run shenbi-dispatch shenbi-worldbuilding generative <round_dir> ...`，round-exec.sh 只建脚手架不产 novel.json/world/*）
- 根因: PR-20（executor.py:1-5 "Python translation of tests/dispatch-subagent.sh"）把技能执行从"脚本之前已完成"改为"并入 dispatch_codex（G2 之后）"，但保留了执行前 G1/G2 的门序 → 新轮次（无 pipeline-state.json）G1 校验尚不存在的输入（novel.json）、G2 校验尚不存在的输出 → 全部 G2.1 not-found FAIL。
- 验证命令: `PYTHONPATH=src .venv/bin/python`（fresh temp round + derive_input_files/derive_output_files + gate_G1/gate_G2）→ G1 FAIL（novel.json 缺失）、G2 FAIL（6 个输出全 G2.1 not-found，含 `truth/*.md` 字面路径）；`git show 99f91f0:tests/dispatch-subagent.sh | grep -n "skill execution happened earlier"`
- 建议方向: G2 移至 dispatch_codex 执行完成之后（或仅在输出预存在时校验、缺失则跳过并记 WARN）；与 pipeline 侧 run_gate_g2 语义对齐；补一条 fresh-round 集成测试（现 test_dispatch_returns_0_on_success 全 mock，未暴露门序）

### F228 | derive_output_files/G2 不展开 writes/updates 中的 glob → 9 技能 G2.1 恒失败 | 漏报 | P2
- 证据: `src/shenbi/audit/_shared.py:38-56`（derive_output_files 仅 resolve_or_skip N/NNN，无 glob 展开）；`src/shenbi/gates/g2.py:44-49`（fps 按字面路径处理，无 glob 展开）；实测 9 技能 writes/updates 含 `*`：worldbuilding(`truth/*.md`)、character-design(`characters/major/*.md`,`characters/minor/*.md`)、character-extraction、canon-import(`import/canon/*.md`)、import-analysis(`import/analysis/*.md`)、sequel-writing(`truth/*.md`)、short-packaging(`import/packaging/*`)、snapshot-manage(`snapshots/chapter-NNN/*`)、truth-sync(`truth/*.md`)
- 根因: G1 侧 normalize_file_paths/gate_G1 有 glob 展开（g1.py:177-193），G2 侧无对称实现；derive_output_files 把 glob 字面传给 G2 → `p.exists()` 恒 False → G2.1 not-found FAIL。重试轮次（输出已存在）同样失败。
- 验证命令: `PYTHONPATH=src .venv/bin/python`：`derive_output_files("shenbi-character-design", chapter=1, round_dir=tmp)` → 含 `.../characters/major/*.md`；`gate_G2(outs, "chapter", tmp)` → status FAIL，must_fix 含两个 `*.md` 路径 G2.1
- 建议方向: derive_output_files 对 glob 展开（与 G1 一致）或 G2 支持 glob；至少 glob 写路径在 G2 前展开/跳过并 WARN

### F229 | derive_file_type 单一 file_type 批处理与异构输出不兼容：(a) chapter 型技能非散文输出误套章节规则；(b) decisions 型技能 .md 散文静默漏套 G2.6-2.10 | 漏报（校验洞+误拒） | P2
- 证据: `src/shenbi/dispatcher/executor.py:64-86`（derive_file_type 每技能一个 type）、`:196-204`（run_g2 全批单 type）；`src/shenbi/gates/g2.py:76-84`（decisions 分支对非 .json `continue`，注释声称 ".md files are validated by their own file_type gate instead"）、`:218-271`（chapter 分支 G2.6/G2.7 字数、G2.8/G2.9 PRE/POST_WRITE_CHECK 标记）
- 根因: (a) worldbuilding/genre-config/character-design 等输出为 JSON/角色档等非散文却归 "chapter" → G2.6（字数下限 3000）/G2.8/G2.9（要求 `## PRE_WRITE_CHECK` 标记）对 novel.json、genre-config.json、protagonist.md 误拒（输出存在也 FAIL）；(b) chapter-drafting/context-composing/chapter-revision 输出含 decisions 侧车 → file_type="decisions" → .md 散文在 g2.py:83-84 被跳过，executor 只调一次 run_g2，不存在注释声称的"own file_type gate"第二次运行 → 章节散文字数/标记/占位符校验静默缺失。
- 验证命令: `gate_G2([genre-config-example.json], "chapter", tmp)` → FAIL（must_fix G2.8/G2.9）；`gate_G2([novel-example.json], "chapter", tmp)` → FAIL（G2.6/G2.8/G2.9）；`gate_G2([character-profile-example.md], "chapter", tmp)` → FAIL（G2.6/G2.8/G2.9）；read g2.py:76-84+executor.py:196-204
- 建议方向: 按文件粒度路由 G2 type（.json→decisions/config、.md 散文→chapter、world/truth→对应 type）；或 G2 chapter 分支加扩展名守卫；补 chapter-drafting 输出批的 G2 集成测试

### F230 | fields.py `_filter_json` 不做规范化（裸 `k in fields`），与 `_filter_md` 的 canonical rule 不一致——复核轮"md 与 json 同构"断言为误 | 漏报（一致性缺陷） | P2
- 证据: `src/shenbi/contracts/fields.py:1-6`（自称 "Replaces 3 divergent matching semantics... Canonical rule: strip + fold ASCII whitespace AND U+3000... NFKC... do NOT lowercase"）；`:55-64`（`_filter_md` 经 `match_field` 规范化双方）；`:76`（`_filter_json` 裸 `k in fields` 成员判断）
- 根因: json 路径绕过了模块头声明的 canonical normalization——声明字段含 U+3000/全角/多空白变体时，md 路径命中、json 路径落到 escape hatch（全文+WARN，信息过滤失效）；生产可达（genre-config.json 的 dict-form reads：review-group-character `[povMode]`、review-group-factual `[pacing, chapterTypes]`、chapter-drafting/plant 等）。第一轮复核声称"md 与 json 同构，json 仅投影命中键"不成立。
- 验证命令: `PYTHONPATH=src .venv/bin/python`：md 路径 `match_field("当前\u3000世界局势","当前 世界局势")`=True；json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；全角声明 `activeＣlues` 同理
- 建议方向: `_filter_json` 对键做 `_normalize_ws` 后比对（`any(match_field(f, k) for k in data)`），或明确 JSON 键不做规范化的文档声明；补 json 路径规范化测试（现 test_fields.py:42-49 仅 ASCII 键）

### F231 | genre-config 消费侧引用 povMode/sensitivityFlags，但模型/fixture/SKILL.md 规则表/OWNERSHIP 全源无此键——review_checklist 恒空默认、review-group-character 字段过滤恒 escape hatch | 漏报（契约-消费漂移） | P2
- 证据: `src/shenbi/pipeline/review_checklist.py:183`（`genre_config.get("povMode", "")`）、`:186`（`sensitivityFlags`）；`skills/shenbi-review-group-character/SKILL.md:59`（`- {file: genre-config.json, fields: [povMode]}`）；`skills/shenbi-review-pov/SKILL.md:74`（"读取 `genre-config.json` 的 `povMode` 字段"）；对照：`contracts/skills/genre_config.py:30-37`（8 字段无 povMode/sensitivityFlags）、fixture 9 键无两者、SKILL.md:286 顶层字段表无两者、ownership.py:38-50 _GENRE_KEYS 无两者
- 根因: 生产方 schema 从未声明这两个键，消费方（review_checklist 恒 `""`/`[]`；review-group-character 的 read_fields 声明永不命中 → dispatch_helper.py:592 escape hatch 每次全文 + `field_filter_no_match` WARN）按存在假设读取 → review-pov 的 POV 模式逻辑静默退化、字段过滤声明形同虚设。与 F221（tropeInventory/顶层字段数=8 漂移）互补但独立：F221 是生产侧声明冲突，本条是消费侧引用悬空。
- 验证命令: `grep -rn "povMode\|sensitivityFlags" src/ skills/ tests/fixtures/genre-config-example.json` → 仅 review_checklist.py:183,186 / review-group-character SKILL.md:59 / review-pov SKILL.md:49,74 命中；fixture 顶层键 9 个均无
- 建议方向: 判定 povMode/sensitivityFlags 归属（若为规划中字段则入 schema+fixture+模型；否则删除消费侧引用并修正 review-group-character 的 read_fields 声明）

### F232 | decisions.py P2.5 rationale 空串/纯空白绕过 REQUIRED 规则 | 漏报（校验洞） | P2
- 证据: `src/shenbi/contracts/schemas/decisions.py:33`（`has = rationale is not None`——不检查非空）、`:36-44`（high/manual_override 仅要求 has）；`docs/framework/decisions-schema.md:84-91`（P2.5 表 "rationale REQUIRED"）
- 根因: `rationale=""` 或纯空白满足 `is not None` 且长度 ≤100 → Selection(high/manual_override) 与 Adjustment 的 REQUIRED 规则被空串满足，AGENTS.md"rationale REQUIRED on manual_override + high-severity + adjustments"形同虚设；routine+low 分支空串反而被拒（`has=True` → FORBIDDEN）——两侧语义不对称。
- 验证命令: `PYTHONPATH=src .venv/bin/python`：`Selection(..., severity="high", rationale="")` → ACCEPTED；`Selection(..., basis="manual_override", rationale="   ")` → ACCEPTED；`Adjustment(..., rationale="")` → ACCEPTED
- 建议方向: `has = bool(rationale and rationale.strip())`；rationale 加 `min_length=1`（strip 后非空）校验

### F233 | ownership.py record_create 的 write_keys 从不校验新增记录键集——plant 可写任意键，_HOOK_KEYS_NEW_RECORD 声明 dead | 漏报（审计盲区） | P2
- 证据: `src/shenbi/contracts/ownership.py:52-71`（_HOOK_KEYS_NEW_RECORD 16 键声明）、`:120-124`（record_create 分支只查 deleted_record_ids 与 modified_record_keys，**从不检查 new_record_ids 的键集**）；`write_audit.py:57-64`（有 OWNERSHIP 条目则跳过 file-level 检查 → 无兜底）
- 根因: field 级有 write_keys 越权检查，record_create 级的 write_keys 声明后未接线——plant 新建记录带任意键（如拼错的 `statue`）通过审计，pending_hooks.md 结构漂移静默入库，下游 track/resolve 依赖的 `state` 等键可能缺失。
- 验证命令: `PYTHONPATH=src .venv/bin/python`：`check_write_ownership("shenbi-foreshadowing-plant", FileChange(relpath="truth/pending_hooks.md", status="modified", new_record_ids=("h1",)))` → `violations=[]`（键集任意）；对照组 track 改 `content` 键 → 违规（record_field 分支正常）
- 建议方向: record_create 分支校验新增记录键 ⊆ write_keys（与 record_field 对称）；补测试

### F234 | 整文件删除 owned 文件零违规——check_write_ownership 忽略 FileChange.status | 漏报（审计盲区） | P2
- 证据: `src/shenbi/audit/snapshot.py:101-102`（pre 非 None、post None → `FileChange(status="deleted")` 无键/记录信息）；`src/shenbi/contracts/ownership.py:101-136`（三 level 均只查 changed_top_keys/new/deleted_record_ids/modified_record_keys，**从不看 status**）；`write_audit.py:61-64`（owned 文件跳过 file-level 声明检查）
- 根因: 整文件删除的 FileChange 不携带键/记录数据，check_write_ownership 三个分支对 deleted 变更全部返回 []，且 owned 文件不走 file-level 兜底 → genre-config.json / truth/pending_hooks.md 被整文件删除时写所有权审计静默通过（数据丢失路径无告警）。
- 验证命令: `PYTHONPATH=src .venv/bin/python`：`audit_writes("shenbi-genre-config", {"genre-config.json": '{"version":"1.0"}'}, {})` → `violations=()`；`check_write_ownership("shenbi-foreshadowing-plant", FileChange(relpath="truth/pending_hooks.md", status="deleted"))` → `[]`
- 建议方向: check_write_ownership 对 status=="deleted" 直接判越权（owned 文件不可删）；或 write_audit 对 owned 文件删除补 file-level 检查

### M 级观察（随文记录）
- `legacy.py:219-228` `requires_independent_agent` 对 malformed frontmatter / YAML 错误**静默返回 False**（fail-open），与 read_frontmatter_contract 的 loud ContractError（:133-140）不对称——G3.4 独立性标志在文件损坏时静默降级（触发需 frontmatter 损坏，F219 同类链，M）
- `enums.py:1` 声称"所有 Literal 必须从此处 import"，同包 `schemas/decisions.py:15-18` 自建 Basis/Severity/Handling/Trim Literal（决策域 severity low/medium/high 与 enums Severity BLOCKING/CRITICAL/MINOR 属不同词表，统一需谨慎；但声称未注明豁免）(M)
- `modes/internal.py:22-23` 错误消息指引 "Set SHENBI_LLM_API_KEY to use API mode"，而 API mode 不可达（detect_mode 仅 codex/internal，codex_api.py 自述 DEPRECATED）(M)
- `dispatch_helper.py:363` OPTIONAL_READS["shenbi-context-composing"] 含 `"trend"` 模式，context-composing 契约 reads 无 basename 匹配项（跨区观察）(M)
- `codex.py:62` codex exec cwd 由原 shell 的 `-C PROJECT_DIR` 改为 `-C round_dir`（PR-20 翻译差异）——T1 轮次中项目根 truth 文件相对读路径语义变化，未执行验证（M，F227 关联）

---

## 三、严重度异议（不修改前两轮，仅提理由）

1. **支持复核轮异议 F214 P2 → P1**（评分独立性证据被生产方伪造）：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent` 与 `scoring_history` → G3.4 fail-closed 与 G3.5 静默放行，违反 AGENTS.md 评分独立性显式契约。本轮反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持复核轮异议 F218 P2 → P1**（部分匹配静默丢字段）：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足；truth 文件随剧情演进缺 section 是正常路径，下游静默在信息不全状态决策——满足决策表 P1"违反 AGENTS.md 显式契约；正常路径可复现功能错误"。
3. **新提（低置信）F217 P2 → P1**：g4/pacing_design 以模型强制 CONSTELLATION ∈ [15,35]，SKILL.md:91 声明有效区间 15-25%——25-35% 输出过门但违反技能自身契约，属正常路径校验漂移（与 F216 同类逻辑）。但 SKILL.md 内部另有 :176（20-30 警告）/ :187（15-30）/ :193（30-40 开卷）等多处自相矛盾，权威区间未定，故降为低置信异议，供协调者裁决（若裁定 SKILL.md:91 为权威则升 P1）。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前两轮一致）；G1 空输入真空 PASS 复现；G2 端到端探针（genre-config/novel/character-profile/chapter 批）复现；字段过滤 json/md 探针复现；ownership 越权/删除探针复现；decisions rationale 探针复现；codex progress.json 损坏探针复现。
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F227 的 cwd 差异观察标注未执行验证）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show` 仅读历史；探针均在 /tmp 目录与 `python -c`。

---

## 五、统计

- 新 finding：**8 条**（F227-F234：P1×1、P2×7）
- 误报：**0 条整体误报**（前两轮 26 条全部实质成立；F220 部分误报/计数修正维持；复核轮"md 与 json 同构"断言经 F230 证伪——系复核轮陈述错误，非 finding 误报）
- 严重度异议：**3 条**（支持复核轮 2 条：F214→P1、F218→P1；新提 1 条低置信：F217→P1）
- 前两轮 findings 判定：F201-F226 共 26 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持复核轮结论）
- **0 新 finding 判定：否**（F227-F234 共 8 条新发现，其中 F227 为 P1 门序回归，直接影响文档化 first-novel 流程）
