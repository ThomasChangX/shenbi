# Z4.review16 复核报告 — src/shenbi/gates/（含 g4 checkers）第十六轮独立复核

- 复核 agent：第十六轮独立复核（fresh-context，全量重读，不依赖前十六轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`~`Z4.review15.md`（F419-F499+F4A0，共 101 条）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个专用 checker + generic 3 分支 + score 三副本 + decisions_validator/make_composite_checker 逐个通读；`src/shenbi/contracts/schemas/decisions.py`、`src/shenbi/pipeline/dispatch_helper.py`、`src/shenbi/contracts/legacy.py`、`src/shenbi/contracts/skills/pacing_design.py`、`tests/tiers/deps.json`、`tests/tiers/acceptance.json`、`tests/unit/gates/test_g1.py` 作交叉证据核实；真实项目 `novel-output/xinghuo-ranqiong/outline/volume_map.md` 实证）
- 实测基线：`.venv/bin/python -m pytest tests/unit/gates -q -p no:cacheprovider` → **785 passed**（本次实测，与全量基线一致；本轮未修改任何仓库文件，未 git add/commit）；新增只读验证 12+ 组（`.venv/bin/python` /tmp 临时目录构造 + 真实 volume_map.md 格式核对 + ALL_SKILLS/t1-skill 计数 + dispatch_helper/decisions.py 源码核实）；`git status --short` 复查无跟踪文件污染（仅新增本报告与既有 untracked 文件）
- 复核结论：**误报 0**；**新增严重度异议 0**（确认前几轮 F404 P2→P1、F408 P2→P1 两条异议成立）；**新发现 4**（F4A1-F4A4：1×P2 + 3×M，全部实测验证）
- 结论：**0 新 finding 判定 = 否**（本轮发现 4 个前十五轮漏报，未收敛到 0；但均为"check 输出自相矛盾/宽松启发式/路径边界"类低烈度缺陷，无新的 P0/P1，无新的缺陷类别——F4A3 为 F402/F426/F427 路径解析家族新实例，F4A1/F4A2 为 F481 check 输出一致性的新实例，F4A4 为 G6.8 解析启发式缺陷）

---

## 一、复核结论表（F400-F499+F4A0 逐条判定）

本轮判定方法：对 101 条 finding 逐条直接读其证据行所在源码 + 关键项独立实测（P1 六条逐一复测、jload/内层形状家族全站点枚举核对、G1/G4/G6/G7 各子检查通读）。共同依据是本人对 52 个文件的全量重读。按组列出。

**A 组：P1 六条（任务点名，逐一反证核查，全部成立，无推翻证据）**

| 编号 | 本轮判定 | 独立反证（本轮实测/读源码） |
|---|---|---|
| F401 | **成立** | 读 g_reconcile.py:52-62：`stem.rsplit("-", n_split)` 后对 `shenbi-worldbuilding-generative-scores.json`（n_split=2）→ parts[0]=`shenbi-worldbuilding` ∈ ALL_SKILLS、candidate_tt=`generative-scores`；progress 键为 `generative` → `td={}` → `td.get("status")` None ≠ "DONE" → GR.2 恒 FAIL。无推翻理由。附注（本轮补充推演核实）：5 连字符技能名（shenbi-review-group-character 等）在全部 n_split∈[1,5] 下 parts[0] 均 ∉ ALL_SKILLS → 走 GR.2 SKIP 分支而非 -scores FAIL——F401 的"review-group-* 同并入后缀"推演仅对 ≤2 连字符技能名成立，多连字符名实为**静默 SKIP**（同一 GR.2 校验洞的另一种表现，不构成独立缺陷，并入本 finding 注记）。 |
| F404 | **成立，确认 P1 异议** | 本轮用正确 schema 字段复测：`DecisionsDoc.model_validate({$schema, skill, chapter, produced_at, selections:[{target, selected, basis:'manual_override', severity:'high', rationale:''}]})` → **PASS（HOLE）**。decisions.py:30-33 `has = rationale is not None`，空串 has=True → REQUIRED 被绕过。AGENTS.md:70 契约在正常路径被空串静默违反 → P1 恰当。 |
| F408 | **成立，确认 P1 异议** | 本轮直接读 dispatch_helper.py:1927-1935：`if not pp.exists(): safe_write(pp, {... current_scorer_agent: pipeline-g3-scorer-<uuid>, scoring_history: [{agent: pipeline-skill-generator, g2_passed: True}]})` → 伪造 scorer 证据（且该形状无 `skills` 键，进一步破坏 GR.2/G_TRANSITION 语义）→ G3.4 fail-closed 被击穿并制造虚假审计证据。P1 恰当。 |
| F432 | **成立** | 本轮实测：`ALL_SKILLS`=74（skills/ 含 SKILL.md 目录，实测 `ls -d skills/*/SKILL.md | wc -l`=74），`tests/tiers/t1-skill` 非 `_` 目录=69（实测），`comm` 差 5 = foreshadowing-lifecycle + review-group-character/craft/factual/plan（实测名单一致）；g7.py:59 G7.1b 以 74 为全集 → 满分 round 恒 FAIL。P1 无推翻理由。 |
| F444 | **成立** | 读 g3.py:150-153：`skills.get(skill_name, {}).get("output_files", [])` 取 skill 层；全部生产 progress 形状（codex/materialize/round-exec/run_gate_g3 伪造）均无该键 → G3.3 恒 SKIP。P1 无推翻理由。 |
| F458 | **成立，确认 P1** | 本轮读 decisions.py:48-59：`Adjustment.rationale: str` 必填但 `_rationale` validator（:55-58）仅查 `> _RATIONALE_MAX_CHARS`；`"rationale": ""` 为合法 str → PASS（HOLE）。AGENTS.md P2.5 "rationale REQUIRED on adjustments" 被空串绕过 → P1 恰当。 |

**B 组：核心门逻辑（g0-g7/shared/专用门）— 全部成立（读码确认 + 关键项实测）**

- F400（g2.py:315-322 G2.12 / 273-287 G2.10 无 file_type 守卫；JSON 末行 `}` 不在 sentence_enders → 恒 WARN）成立；F402（length_normalizing.py:29 `pf = resolve_input_path(fp, rd)` vs :35 `word_count_md(fp)` 用原始 fp）成立；F403（cli.py:113-121 无 try/except + 11 个 RoundPaths checker 双 None raise ValueError——本轮逐一读到 character_design:28-29 / faction_builder:27-28 / foreshadowing_track:27-28 / location_builder:27-28 / pacing_design:33-34 / plot_thread_weaver:25-26 / power_system:27-28 / relationship_map:27-28 / story_architecture:26-27 / volume_outlining:27-28 / worldbuilding:28-29）成立；F406（g0.py:195/323 用 `PROJECT/skill-output`、g7.py:66 用 `rd/skill-output`；round-exec 测试布局限定维持）成立；F407（g0.py:177-179 无 seed → `return passed("G0", [G0.1 SKIP])`）成立；F409（g1.py:281-282 空输入仅记 G1.0 SKIP → PASS）成立；F410（SHORT_MAP 20 项 vs 11 个缺 shorthand 目录存在）成立；F411（g0.py:220 `missing_dirs` 永不 append、:227 恒假；:532 注释 "20 skills"）成立；F412（g3_independence.py:3 引 g3.py:144-162，实际 G3.4 在 193-206）成立；F413（decisions_validator.py:175 注释 shared.py:113，fail() 在 125-136）成立；F414（chapter_revision.py:97 `"HARD_FAIL"` 不在 GateStatus）成立；F415（SKILL.md:125=`<!--META-END-->`，规则在 :140）成立；F416（decisions-schema.md 缺 medium）成立；F417（无 test_gate_manifest.py / g4 无 test_memory_distill.py；gate_manifest 0% 覆盖）成立；F418（g0.py:665 `check_config_coherence(project_dir)` 未传 floor；g0_config_coherence.py:65 仅 floor 非 None 时执行两类检测）成立；F419（g1.py:210-213 / g2.py:169-172 except 缺 ValueError）成立；F420（g6.py:50/56 novel.json/genre-config.json jload 无保护）成立；F421（g_reconcile.py:33 无 try；g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError）成立；F422（cli.py:83-87 JSON 失败→`[]`）成立；F423（`ls tests/scoring.py` 不存在，实现在 src/shenbi/scoring.py）成立；F428（g3.py:98 循环外 threshold=acceptance.t1=94 vs :113/:125 回退硬编码 90）成立；F429（g1.py:228-232 gate 内 safe_write .bak，FAIL 判定前已落盘）成立；F435（g0.py:521-529 exempt_skills 读后零消费；:530-533 注释声称的校验未实现）成立；F436（g6.py:125/142 planted_chapters 零消费）成立；F437（g5.py:274-296 except Exception → WARN vs G6.3/G7.13 FAIL）成立；F439（`'shenbi-style-polishing' in BACKUP_SKILLS` → False，BACKUP_SKILLS=16 全为 truth-updater；style_polishing.py:42-43 恒无 .bak）成立；F440（g0.py:202-207/524-528 except 缺 ValueError）成立；F441（g0.py:98-99 json.loads 直读 + :107 `.get` 无守卫）成立；F443（g1.py:102-144 check_fields_exist 全仓仅测试引用；chapter_loop 消费 check_hook_fulfillment 的注记属实）成立；F445（shared.py:210-222 unimplemented() 零调用）成立；F449（g_reconcile.py:40/61-62 `"DONE"` vs 生产小写 done）成立；F453（g2.py:237-238 `_is_important_chapter(fp, project_dir or "")`；phase_runner.py:213 与 g3.py:177 无 pd；executor.py:144 传 pd 的限定维持）成立；F454（g_transition.py:47-64/70-85 键语义漂移）成立；F456（g6.py:66 `sorted(ch_dir.glob("chapter-*.md"))` 字典序；真实项目前 12 个 = [1,10,...,19,2]）成立；F457（g6_checks.py:111-113/245 `len(re.findall(...))` 计段数非字符数）成立；F462（g3.py:103 total_score/score vs scoring.py 规范输出顶层 final_score）成立；F463（g3.py:100 遍历全部报告无 skill 过滤 + :107 跨技能 rubric）成立；F464（codex.py:56 `-scores-subagent.json` 与 find_report/G0.10 glob/GR.2 均不匹配）成立；F466（chapter_drafting.py:260-261 `Path(rd)/"project-output"/"chapters"`；G6.3 传 rd=pd → 静默跳过；review7 补充的 G5.5/run_gate_g4 同形状调用方属实）成立；F468（g3.py:98 循环外 threshold + :113/:125 循环内覆写 → 跨报告泄漏）成立；F478（g0.5/g7.2-7.4,7.8/GR.3-4/GT.2,4,5/GD.2-3 12 个 UNIMPLEMENTED stub，门仍 PASS）成立；F484（g3_independence.py:23-27 agent_trace 非 dict 时同源检测静默跳过 → PASS）成立；F498（g5.py:153 num_pat 单捕获组 + :159 `m.group(2)` IndexError 被 :171-172 `except Exception: continue` 吞掉 → G5.3 数值冲突永不报告；test_g5.py:139-163 钉死惰性行为并标注 "source bug"）成立。

**C 组：jload/内层形状家族（F431/F440/F441/F459/F460/F465/F470/F471/F477/F485/F488/F489/F493/F497）— 全部成立（本轮对全仓 30 处 jload + 21 处 json.loads 站点逐一核对 except 元组，确认家族枚举完整）**

- F431（8 处 jload ValueError 调用点：g1:210/262、g2:169、g3:149/196/211、g5:57、g7:35/57/240/246、shared:232）成立；F440（g0.py:203/525）成立；F441（deps.json 顶层非 dict + :107 内层 .get）成立；F459（worldbuilding.py:40/61）成立；F460（g0.py:204 chapter_word / g0_config_coherence:89-92 auditDimensions / g5.py:62-63 t1_scores 条目 / decisions_validator:54-55 相邻文件）成立；F465（g7.py:37/58 t1_scores、g_reconcile.py:36 skills、character_design.py:203 archetype_sources 元素、foreshadowing_plant.py:69 hooks 元素）成立；F470（g3.py:97/102、g5.py:67 完全无 try）成立；F471（gate_manifest.py:64-66 "gates" 为列表 setdefault AttributeError）成立；F477（g_reconcile.py:60 `skills.get(candidate_skill, {}).get(candidate_tt, {})`——GR.1 有 isinstance 守卫、GR.2 遗漏）成立；F485（g3.5 scoring_history / g_dispatch completed_skill_names / g5 t1_scores list / g7 t2/t3_scores / g6 chapter_word 5 处）成立；F488（g_transition.py:48/57 remaining 标量 len() TypeError）成立；F489（g6.py:51/60 novel.json 字符串数值 `-(-x // y)` TypeError）成立；F493（g3.py:152 skills[skill] 值 / g0.py:107 _calibration_hashes / g5.py:40 t2-phases 顶层 / g6.py:39 t3-pipelines 顶层）成立；F497（g5.py:48/g6.py:40 t2-phases/t3-pipelines **条目值**非 dict）成立。
- 本轮完整性核对：全仓 `grep jload` 30 处 + `grep json.loads` 21 处逐一核对 except 元组——**未发现新的未枚举 ValueError/AttributeError/TypeError 逃逸点**（g4_genre_config:34 用 `except Exception`、g4_character_design:42 用 `except Exception`、g4_pacing_design 的 `except ValidationError` 已实测确认 from_markdown 的 ValueError 均出自 pydantic model_validator 会被包成 ValidationError，不逃逸——见覆盖空洞声明"已排除候选 (a)"）。

**D 组：per-skill g4 checker — 全部成立（本轮逐个通读复核）**

- F405（G4_CHECKER_SKILLS 22 vs generic.py checkers 分发表 31，差 9）成立；F424（chapter_drafting.py:219-222 skill-output 爬升 → fatigue 兜底）成立；F425（g7.py:66+86-91 G7.6 嵌 skill-output）成立；F426（genre_config.py:28 `str(Path(rd) / fps[0]) if rd and fps else fps[0]` 未走 resolve_input_path；pacing_design.py:31 fps[0] 启发式）成立；F427（chapter_revision.py:45 `Path(fp)` 原始路径，OSError 被 :51 捕获 → 误报 invalid_json）成立；F430（foreshadowing_track.py:41-46 裸词 `操作` 命中即 PASS）成立；F433（chapter_drafting.py:331-334 条件判 project_dir、取值用 project_root）成立；F434（state_settling.py:14/46-53 单字符子串匹配）成立；F438（chapter_drafting.py:204 `max(5, wc//1000)` vs SKILL.md:85/134 `≤1/3000`）成立；F442（length_normalizing.py:61-64 只查 3000/10000 无 25% 双底线）成立；F446（shared.py:314-331 减法表把契约词"然而"减净、漏减 然后/依然/显然 等）成立；F447（chapter_drafting.py:203/225 transition/fatigue 全文件计数 vs :241-246 G4.meta 剥离）成立；F448（chapter_drafting.py:70 `第\d+章` 漏中文数字）成立；F450（g6.py:118-142 按 `## hooks`+`- id:` 解析 vs 真实 pending_hooks.md 表格格式）成立；F451（g6_checks.py:101-102/234-240 剥离正则前瞻仅 `## `+`\Z` 缺 `# `/`<!--META`）成立；F452（chapter_drafting.py:293/316-328 未剥离元区块即取 `paragraphs[-1]`）成立；F455（generic.py:28 docstring "has frontmatter" 未实现）成立；F461（chapter_drafting.py:334/293-302 protagonist/scene 未剥离元区块）成立；F467（chapter_planning.py:37 sections 接受 `## {i}[\.、：:\s]` vs :82/:97 s5/s7 仅认 `## 5\.`）成立；F469（generic.py:313 `make_composite_checker(g4_decisions, g4_chapter_revision)` 接线错位）成立；F476（chapter_planning.py 仅实现 SKILL 声明 8 条可检查规则中 2 条）成立；F479（worldbuilding.py:68 注释 "prose density < 5%" 与代码方向相反）成立；F480（generic.py:102/152 死条件 `all(...)` 恒假）成立；F482（chapter_drafting.py:58-66 docstring 声称主题化命名未实现）成立；F483（chapter_drafting.py:164 只加"他"不加"她"）成立；F486（plot_thread_weaver.py:39-46 任一标签即 PASS）成立；F487（chapter_drafting.py:146-148 正则 + :150-158 YAML 双源 append 同一主角名）成立；F490（chapter_revision.py:98 checks 槽为 list[str]）成立；F491（worldbuilding.py:129-133 `## 地点[：:]?` 可选冒号吞汇总节 + numbered_loc 顶替计数；本轮实测真实 volume_map.md 为 `## 第一卷：觉醒之火（第1-15章）` 带全角冒号格式，story_architecture/volume_outlining 的卷正则可正常匹配——F491 与 G6.11（F473）所依赖的卷格式语义互不冲突）成立；F492（g0.py:276-288 evidence 正则漏认"证据"列头形态 → 带 seed 的 G0 恒发 3 条 score-* WARN）成立；F495（review_arc_payoff.py:101 / review_resonance.py:118 `:\d+(?!\d)` 被时间/比例形态满足 → file+line 证据检查 fail-open）成立；F496（g6.py:198/208 G6.8 幽灵角色/口头禅 [:5000] 截断）成立；F499（g6.py:259 G6.9 章节 [:3000] 与 g5.py:191 术语 [:3000] 截断；g6_checks.py:52/g5.py:156 因 F456/F498 不可达的注记属实）成立；F4A0（g6.py:238-269 G6.9 约束提取侧失效：`ctx.split(str(val))[0]` 关键词↔约束错配 + 关键词含操作符 + 真实 rules.md 散文格式零约束 + 仅人/个单位 + `constraints[:10]` 上限）成立。

**E 组：M 级文案/优化 — 全部成立**

- F410、F411、F412、F413、F414、F415、F416、F436、F445、F455、F479、F480、F481（g2.py:325-329 G2.meta_ratio 双 WARN）、F482、F486、F490、F492、F495 逐一读码确认成立，无推翻证据。

**误报判定：0 条。** F400-F499+F4A0 全部成立。对任务点名的 6 个 P1（F401/F404/F408/F432/F444/F458）本轮逐一读源码 + 实测反证：F404/F458 空串绕过用正确 schema 字段实测复现（Selection manual_override+high、Adjustment 均 PASS HOLE）、F408 伪造写入读 dispatch_helper.py:1927-1935 确认、F401 GR.2 恒 FAIL 读 g_reconcile.py:52-62 确认、F432 74-vs-69 计数 `comm` 实测确认、F444 G3.3 skill 层查找读 g3.py:150-153 确认——**无任何推翻证据**。不准确处仅限前几轮已注明的范围/限定（F401 masking 按归档 spec Non-Goal #3、F406/F424/F425 "恒 SKIP/永不命中"仅 pipeline 布局、F405 名单、F444 形状、F453 executor 限定、F466 未枚举 G5.5/run_gate_g4、F401 的 review-group-* 推演本轮已限定为 SKIP 分支），本轮无新增修正。

**严重度异议：0 新增。** 确认前几轮 2 条异议成立（F404 P2→P1、F408 P2→P1，均属"AGENTS.md 显式契约在正常路径被静默违反"，按决策表 P1 恰当）；其余严重度按决策表核对无异议（F450/F464/F473/F491 的"真实项目恒 FAIL/误报可争 P1"维持前几轮 P2 判定；F498 的"测试钉死 source bug 可争 P1"维持 P2，测试有显式文档）。

---

## 二、新发现（F4A1-F4A4）

### F4A1 | g4_worldbuilding truth 模板检查：缺 frontmatter 字段时同一文件同时输出 FAIL(must_fix) 与 PASS(check)（c.append(PASS) 无条件执行） | error | M
- 证据: src/shenbi/gates/g4/worldbuilding.py:153-158——`fm = yload(str(tp))` 后 `for field in ["type","category","status"]: if field not in fm: mf.append(...)`（:155-157）与 `c.append({"id": f"G4.truth.{tmpl}", "s": "PASS"})`（:158）**无 else 分支**，PASS 条目无条件追加；同文件其它字段检查（如 novel.json :41-51）均为 if/else 二选一，仅此处双写。
- 根因: 代码笔误/演进残留——truth 模板文件缺失 type/category/status 任一字段时，gate 的 checks 列表同时含 `G4.truth.current_state.md PASS` 与 must_fix `G4.truth.current_state.md.missing_type` 等 FAIL 条目，同一文件被判 PASS 又被判 FAIL（自相矛盾）；gate status 本身仍正确（mf 非空 → FAIL），故为 check 输出一致性问题。
- 验证命令+输出: `.venv/bin/python -c`（构造 `proj/truth/current_state.md` 仅含 `---\nname: x\n---`）→ `json.loads(g4_worldbuilding([], str(proj), str(proj)))` → `must_fix` 含 `G4.truth.current_state.md.missing_type/missing_category/missing_status` **且** `checks` 含 `{'id': 'G4.truth.current_state.md', 's': 'PASS'}`（同文件 PASS+FAIL 并存，实测复现）。
- 建议方向: :158 改为 else 分支（字段齐全才 append PASS）；与 F479/F491 一并处置 worldbuilding check 输出质量；补"缺字段 → 只有 FAIL 无 PASS"断言用例。

### F4A2 | G6.8/G6.9/G6.11 子检查产生 must_fix 违规时仍无条件输出 "PASS" 汇总 check 条目 → G6 FAIL 输出的 checks 带自相矛盾 PASS（F481 同族新实例） | error | M
- 证据（3 处独立实例，均实测复现）:
  - g6.py:221-228（G6.8）：`c.append({"id": "G6.8", "s": "PASS", "chars_with_voice": ..., "chars_total": ...})` 无条件——G6.8 既有 ghost_voice FAIL（:201）又有 WARN（:214-220）时仍输出 PASS 汇总；
  - g6.py:281（G6.9）：`c.append({"id": "G6.9", "s": "PASS", "constraints_extracted": len(constraints)})` 无条件——G6.9:limit_exceeded/below_minimum 违规时仍输出 PASS；
  - g6.py:352-359（G6.11）：`c.append({"id": "G6.11", "s": "PASS", "volumes": ..., "chapters_total": ...})` 无条件——G6.11:no_chapters/no_ending_hook 违规时仍输出 PASS。
- 根因: G6 各子检查的"汇总统计条目"直接标 `"s": "PASS"` 且不参考 mf 状态，与 G2 的 `_check_meta_ratio`（F481）同属 check 输出自相矛盾族；gate status 仍由 mf 决定（正确 FAIL），但任何按 checks 逐条消费的调用方会把该子检查误读为通过（pipeline-manifest 会持久化该形状）。
- 验证命令+输出: 构造 `rules.md='集会人数不超过100人。'` + `chapter-1.md` 含 "集会人数不超过200人" → `gate_G6` → `status: FAIL`、`checks` 含 `{'id': 'G6.9', 's': 'PASS', 'constraints_extracted': 1}`、`must_fix` 含 `G6.9:limit_exceeded:...`（实测复现）；构造 volume_map（卷一 1-3 章无对应 chapter）+ chapter-4/5/6 → `checks` 含 `{'id': 'G6.11', 's': 'PASS', 'volumes': 2}` 且 `must_fix` 含 `G6.11:no_chapters:vol一:range=1-3`（实测复现）。
- 建议方向: 违规时汇总条目改 `"s": "FAIL"/"WARN"` 或标注 `"note": "violations in must_fix"`；与 F4A1 同批修复 check 输出一致性；补"违规 → 无 PASS 条目"断言用例。

### F4A3 | G1 glob 展开双重缺陷：相对 glob 按 CWD 解析忽略 round_dir（与 resolve_input_path rd 语义不一致）+ compute_backup_targets 在展开前计算 → in-place 技能 glob 输入下 .bak 永不创建且误标 "not in-place skill" | error | P2
- 证据: src/shenbi/gates/g1.py:164（`targets = compute_backup_targets(skill_name, fps, ...)` 在 glob 展开**之前**对原始 fps 计算）、:177-193（`_glob.glob(fp, recursive=True)` 无 root 参数，相对 glob 按 **CWD** 解析）、:195-241（循环遍历**展开后** fps → `if fp in target_dict`（:225）——展开后的真实文件不在 target_dict 内 → 走 :240-241 else 分支输出 `G1.4 SKIP: not in-place skill`，.bak 不创建）。
- 根因: (a) glob 展开的基准目录是 CWD 而非 round_dir——与 resolve_input_path（shared.py:58-75）"相对路径按 rd 解析、无 CWD 回退"的仓库契约不一致；CLI 在 rd≠CWD 场景传 `truth/*.md` + rd 时 glob 匹配失败 → 保留字面路径 → G1.1 误报 "not found"（文件实际在 rd 下）。(b) compute_backup_targets 基于展开前路径——对 BACKUP_SKILLS 内技能（truth-updater，如 shenbi-state-settling，本轮实测在 BACKUP_SKILLS 中）传 glob 时，展开后的真实文件永远不匹配 target_dict → G1.4 静默 SKIP（附误导性原因 "not in-place skill"）→ .bak 缺失 → 后续 G2.11 truth-diff 无基线（truth 删除检测静默失效）。
- 验证命令+输出:
  - CWD≠rd：`os.chdir(tmpdir)` 后 `gate_G1('shenbi-state-settling', ['truth/*.md'], str(rd))`（rd/truth/current_state.md 存在）→ `status: FAIL`、`must_fix: ['G1.1:truth/*.md']`（文件存在却被误报 not found；CWD 与 rd 一致时 glob 可命中 → PASS，实测对照翻转）；
  - CWD=rd（glob 命中）：`gate_G1('shenbi-state-settling', ['truth/*.md'], str(rd))` → `status: PASS` 但 `G1.4: {'file': 'truth/current_state.md', 's': 'SKIP', 'r': 'not in-place skill'}` 且 `(rd/'truth'/'current_state.md.bak').exists() == False`（.bak 未创建，尽管该技能 ∈ BACKUP_SKILLS）。
  - test_g1.py:229-260 的 glob 用例全部传绝对路径（`str(tmp_path / "*.md")`）且技能为 shenbi-worldbuilding（非 BACKUP_SKILLS 成员）→ 两个缺陷均被测试掩盖。
- 建议方向: (a) glob 展开基准改为 `rd or Path.cwd()`（与 resolve_input_path 对齐）；(b) targets 在 glob 展开后计算（对展开后的真实文件调用 compute_backup_targets）；补"相对 glob + rd≠CWD → G1.1 通过"与"truth-updater glob 输入 → .bak 创建"用例。严重度说明: 同 F402/F426/F427 路径解析家族取 P2（CLI glob 输入形态可达，生产 literal 路径不受影响；.bak 缺失属 G2.11 基线丢失的静默验证洞）。

### F4A4 | G6.8 口头禅提取 `ct[ct.index("voice_profile:") :]` 至 EOF 抓取全部引号串（正文 + 后续 frontmatter 字段）→ 口头禅列表污染、`[:3]` 检查语义漂移 → 真实口头禅在场仍误发 catchphrases_not_found WARN | error | M
- 证据: src/shenbi/gates/g6.py:187-188（`cp_section = ct[ct.index("voice_profile:") :]` 从 voice_profile 到**文件末尾**（含 `---` 之后正文与其余 frontmatter 字段）；`cps = re.findall(r'"([^"]{2,30})"', cp_section)` 抓取全部双引号串）、:206-213（`for cp in vdata["catchphrases"][:3]` 只查前 3 个捕获串）。
- 根因: 语音档案 YAML 中 speech_patterns/catchphrases/avoid_patterns 与后续字段（core_value 等）及正文引号全部混入 cps；`cps[:3]` 通常取到 speech_patterns（位于 catchphrases 之前）而非真正的口头禅 → "top-3 catchphrases" 检查的语义变成"角色的任意前 3 个引号短语是否出现在章节"——真实口头禅在正文出现、但前 3 个 speech_pattern 未出现时误发 `G6.8:XXX:catchphrases_not_found_in_chapters` WARN（误报方向，与 F486/F495 同类宽松启发式）。
- 验证命令+输出: `.venv/bin/python -c`（角色 frontmatter `speech_patterns: ["沉默寡言","简短应答","冷静分析","直截了当"]`、`catchphrases: ["命运从不免费"]`；chapter-1 正文仅含 `"命运从不免费"`）→ `gate_G6` → `checks` 含 `{'id': 'G6.8', 's': 'WARN', 'r': '张三:catchphrases_not_found_in_chapters'}`（真实口头禅在正文，仍误报 WARN；对照——cps 含 6 个串，[:3] 恰为 speech_patterns）。
- 建议方向: 提取限定在 catchphrases 键值范围内（如 `re.search(r"catchphrases:\s*(\[.*?\])", ct, re.DOTALL)` 后解析该数组），或至少对 cp_section 截断到 frontmatter 结束（`---`）；与 F496 的 [:5000] 截断一并处置 G6.8；补"口头禅在正文、speech_patterns 不在 → 不 WARN"用例。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个专用 checker + generic 3 分支 + score 三副本 + decisions_validator/make_composite_checker 逐个通读）。未发现清单外遗漏文件。
- 实测基线：`.venv/bin/python -m pytest tests/unit/gates -q -p no:cacheprovider` → **785 passed**（本次实测，与全量基线一致；本轮未修改任何文件）；新增只读验证 12+ 组（`.venv/bin/python` /tmp 临时目录构造 + 真实 volume_map.md 格式核对 + ALL_SKILLS/t1-skill `comm` 计数 + dispatch_helper.py:1927-1935/decisions.py:25-60/legacy.py:13-48 源码核实 + G1 glob 对照实验 + G6.8/G6.9/G6.11/worldbuilding check 输出实测）；`git status` 复查无跟踪文件污染（仅新增本报告）。
- 本轮重点复核了任务点名的三处：30 个 per-skill g4 checker 最后一遍（24 专用 + generic 3 分支 + score 三副本，全部通读）、G0 各子检查（G0.1-G0.16/G0.cc 全部路径，含 G0.5b evidence/threshold 正则）、g6_checks/g7/g3_independence 剩余路径（含 G6.8 口头禅提取、check_continuity/check_pacing/check_style_consistency 采样口径、G7.13-G7.16 全部分支、G3.4 独立性函数本体）。
- 已排除的候选（非新 finding，理由如下）：
  (a) g4_pacing_design 仅捕获 `except ValidationError`——经读 contracts/skills/pacing_design.py 全文确认 from_markdown 触发的全部 ValueError（beats 缺失/百分比≠100/CONSTELLATION 越界/scene types 数量）均出自 pydantic `model_validator`，pydantic 会包装为 ValidationError → 被捕获，不逃逸（实测：百分比≠100 的 rhythm_principles.md → 正常 FAIL JSON 而非崩溃）。**非缺陷，排除。**
  (b) foreshadowing_plant.py:87 `re.search(..., notes)` 对非 str notes（list）抛 TypeError——需 dict 元素 + notes 非 str 双畸形，纯防御类；按 review12/15 对 "expected_outputs None"/"jsonl 行形状" 的排除先例，未编号。
  (c) g6.py:61 `expected * min_ratio` 对 min_chapter_ratio 为 str 的 TypeError——deps.json 畸形触发，同 (b) 纯防御类，未编号。
  (d) G6.8 的 `found_any` 跨 catchphrase 短路（首个命中即不再查其余）——WARN 语义为"前 3 个均未出现"，属设计而非缺陷，未编号。
  (e) 真实 volume_map.md 为 `## 第一卷：觉醒之火（第1-15章）`（带全角冒号），story_architecture/volume_outlining 的卷正则 `## 第[一二三四五六七八九十\d]+卷[：:]` 可正常匹配（实测 5/5）——F453 之外无新卷格式漂移，未编号。
  (f) pacing_design 模块 docstring "CONSTELLATION ratio in [20, 30]"（contracts/skills/pacing_design.py:12）vs validator [15, 35]——文件不在 Z4 清单（属 contracts 区），按分区归属不录入 Z4。
- 跨区交叉确认建议（与 prior rounds 一致）：F4A3 的 G1 glob 语义 ↔ Z5/Z6（dispatcher 是否以 glob 调 G1）；F4A2 的 checks 自相矛盾对 pipeline-manifest 记录的影响 ↔ Z5/Z6；F408 的 run_gate_g3 调用语义 ↔ Z5/Z6；F473/F474 的 G6.11/G6.4 真实项目恒定 FAIL 归因 ↔ Z5/Z6。

## 四、最终消息（复核统计）

- **新 finding：4**（F4A1：g4_worldbuilding truth 模板缺字段时同文件 PASS check + FAIL must_fix 并存，实测复现（M）；F4A2：G6.8/G6.9/G6.11 违规时仍输出 PASS 汇总 check，2 组实测复现（M）；F4A3：G1 glob 展开 CWD 解析忽略 rd + .bak targets 展开前计算致 in-place 技能 .bak 缺失，2 组实测对照翻转（P2）；F4A4：G6.8 口头禅提取至 EOF 抓取全部引号串致真实口头禅在场仍误发 WARN，实测复现（M）。全部实测或代码路径验证；F4A3 为 F402/F426/F427 路径解析家族新实例，F4A1/F4A2 为 F481 check 输出一致性家族新实例，F4A4 为 G6.8 解析启发式缺陷——均无新缺陷类别）
- **误报：0**（F400-F499+F4A0 全部成立；对任务点名的 F401/F404/F408/F432/F444/F458 六个 P1 逐一读源码 + 实测反证：F404/F458 空串绕过用正确 schema 字段实测复现、F408 伪造写入读 dispatch_helper.py:1927-1935 确认、F401 GR.2 恒 FAIL 读 g_reconcile.py:52-62 确认（review-group-* 多连字符名实为 GR.2 SKIP 分支，已在 F401 注记限定）、F432 74-vs-69 计数 comm 实测确认、F444 G3.3 skill 层查找读 g3.py:150-153 确认；无任何推翻证据）
- **严重度异议：0 新增**（确认前几轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度按决策表核对无异议）
- **0 新 finding 判定：否**（本轮发现 4 个前十五轮漏报，未完全收敛到 0；相比前几轮（12/9/6/10/6/6/5/11/3/4/2/3/2/2/1 条）处于收敛末端：F4A1/F4A2 需逐文件核对 check 输出与 must_fix 的一致性才可见（前十五轮聚焦 gate status/must_fix，未对 checks 条目做一致性审计）；F4A3 需对 G1 的 glob 展开做 CWD/rd 对照实验 + 对 BACKUP_SKILLS 成员做 .bak 落盘验证才可见（test_g1 全部用绝对路径 + 非 backup 技能掩盖）；F4A4 需对 G6.8 的 voice_profile 解析做"多 speech_pattern 把真实口头禅挤出 [:3]"构造才可见）
