# Z2 分区第十七轮独立复核报告（Z2.review17）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）+ `Z2.review9.md`（F253-F255）+ `Z2.review10.md`（F256-F257）+ `Z2.review11.md`（F258）+ `Z2.review12.md`（F259-F261）+ `Z2.review13.md`（F262）+ `Z2.review14.md`（F263-F266）+ `Z2.review15.md`（F267-F268）+ `Z2.review16.md`（F269）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，逐文件重读，未依赖前十六轮转述）；另读跨区消费面验证裁决（audit/{_shared,snapshot,write_audit,record}.py、gates/{cli,g0,g0_skill_contract,g1,g2,g3,g3_independence,g_reconcile,shared}.py、pipeline/{dispatch_helper,closure,truth_io}.py、phase_runner.py、safe_write.py、scoring.py、tests/round-exec.sh、tests/fixtures/、novel-output/ 真实 truth 文件、skills/*/SKILL.md 契约段、git ls-files）
- 模式: 只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）。git status 确认 Z2 源码自前轮零改动（工作树无跟踪修改）。
2. **独立验证方式**：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**；15+ 个 `python -c`/heredoc/子进程运行时探针（F214 机制复证、F216/F218/F227/F235/F239/F247/F251/F252 复现、新候选 F270/F271 证据、F269 异议反证尝试）。
3. **前十六轮判定**：F201-F269 共 **69 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注、F243 机制修正（exit 3 先于 exit 2）、F247 范围注记均复核通过。6 条 P1（F214/F216/F218/F227/F235/F247）经独立反证尝试**全部维持**。
4. **本轮新发现 2 条（F270-F271，全部 M 级）+ 1 条 F214 机制精度注记 + 1 条新严重度异议（F269 M→P2 低置信）**。集中于十六轮都未触及的两处极细碎面：
   - **F270（M）**：`record_audit_outcome`（audit/record.py:36-39）的 write-audit.jsonl ledger 写入**无 try/except**（与同函数 trace seam 的 try/except 不对称）——round_dir 只读/磁盘故障时在 executor.py finally 内裸抛，**掩盖 dispatch_exc 且审计记录整体丢失**；F238 覆盖的是 `audit_writes` 内容解析崩溃（故障面=LLM 产出），本条是 `record_audit_outcome` 自身 I/O 崩溃（故障面=文件系统），两者独立（PermissionError 实测复现）；
   - **F271（M）**：`fields.py:extract_h2_sections` 不跳过围栏代码块——truth 文件内 ``` 围栏含 `## ` 行时产生**伪 section** 且字段过滤输出携带**悬空围栏标记**（实测 output 含未闭合 ` ```md `）；F264（重复标题折叠）/F218（部分匹配丢弃）/F262（声明漂移）均未覆盖代码围栏干扰面；当前 fixture/novel-output 零触发（latent）。
5. **F214 机制精度注记（非误报、非新根因）**：gate_G3 的 G3.4 在 progress.json **缺失**时返回 **SKIP**（g3.py:205-206），并非 FAIL——run_gate_g3 的伪造（dispatch_helper.py:1934-1945）实际把 **SKIP 转为 PASS** 并**伪造独立性证据**（而非"绕过 fail-closed FAIL"）；fail-closed 的 FAIL 面在 progress.json **存在但无 scorer** 的成功派发路径（scoring_independence_status fail-closed 正常触发）。F214 本体（伪造独立性证据违反 AGENTS.md 评分独立性契约、掩盖失败派发）**不受影响**，P1 维持。

---

## 一、复核结论表（F201-F269 判定）

| 编号 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|
| F201 | **成立，P1 维持** | 三模型（chapter_planning/context_composing/volume_outlining）无生产消费者（grep 仅 test_skill_integration.py:188 类名文本）；g4 私有实现规则不同（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）——本轮 read 复证。双源已发散。 |
| F202 | **成立** | registry.py:40-63 REGISTRY/_discover_skill_models/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | **成立** | deps.py:56-78 DepsDoc/phase_of 无生产消费者；sync_contracts.py:195 仍 json.loads（跨区 read 复证）。 |
| F204 | **成立（附注）** | novel.py NovelConfig 无生产 import；g6.py:51 与 g4/worldbuilding.py 以 raw dict 读 target_word_count/target_words——死的是模型非行为。 |
| F205 | **成立** | schemas/scores.py 无生产消费者（grep 复证）。 |
| F206 | **成立** | schemas/state.py ProgressDoc/SummaryDoc 无加载点（g3.py 用 jload 直读 dict）。 |
| F207 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | **成立（增补成立）** | enums.py Severity/Verdict 无生产 import（生产仅 CPZone→foreshadowing_resolve、ActorRole→trace）；裸字面量散落点 + chapter_loop.py:1797（"BLOCKING"/"FAIL"）、:409-438（G4Severity）本轮 read 复核通过。 |
| F209 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有）；GateOutcome/PureInput 无生产消费者（过渡类型自述 base.py:4-5）。 |
| F210 | **成立** | 本轮 grep 实测 foreshadowing-track/SKILL.md：PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94 vs hooks.py 引用 72-73/87/120，Δ32-40。 |
| F211 | **成立（表述修正维持）** | executor.py:167-188 cov missing（74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0（本轮 read 复证）→ 修复方向按"CLI 崩溃"修正，本体成立。 |
| F213 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实（read 复证）。 |
| F214 | **成立，P1 维持（机制精度注记见第三节）** | 本轮 read + 子进程探针复证：codex.py:20-48 `_record_completion` 不写 current_scorer_agent；run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`；实测 `gate_G3`（无 progress）→ G3.4 **SKIP**，伪造后 → G3.4 **PASS**、G3.5 **PASS**（"0 prior scorers"——伪造条目键 "agent" 非 "agent_id"）。伪造点 pipeline/（跨区），Z2 侧本体 P2，P1 由跨区证据支撑。 |
| F215 | **成立** | codex.py:75 `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（read 复证 + 前轮复现）。 |
| F216 | **成立，P1 维持** | **本轮运行时复现**：空 customRules + texture:False 禁用维度 → `GenreConfig.model_validate` PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。反证（空规则无触发面）不成立——新轮次 stub customRules 可空。 |
| F217 | **成立；支持低置信异议（P2→P1）维持 P2** | SKILL.md 内 7 处 CONSTELLATION 区间 + docstring 20-30 + 模型 [15,35]（本轮 read 复证）；权威区间未定（SKILL.md 自相矛盾），维持低置信供协调者裁决。 |
| F218 | **成立，P1 维持** | **本轮运行时复现**：部分匹配（声明 ["当前任务","不存在的字段"]，文件 3 节）→ matched=True、仅命中 "当前任务" 节（世界设定/活跃线索静默丢弃）、无 WARN；全缺 → 全文 + `field_filter_no_match` WARN（fields.py:55-64）。AGENTS.md "If a declared field is missing ... returns the full file + logs WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 维持。 |
| F219 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py 空 fps 零迭代）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态（"支柱一续"）；F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，维持 P2（不降 M）。 |
| F221 | **成立** | fixture 9 键含 tropeInventory（本轮 json.loads 复证），`GenreConfig.model_validate` PASS 且 tropeInventory 被静默丢弃（extra=ignore）；SKILL.md:286/:292 声称 8 键；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段 + docstring "8 camelCase keys"。四源漂移确认。 |
| F222 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（本轮 probe 复证 `{}.get('final_score', 0)` → 0）。 |
| F224 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:44 写 `{score,status}` → G3.3 永 SKIP。 |
| F225 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise SubAgentProtocolError；cli main 无 try/except（read 复证）。 |
| F227 | **成立，P1 维持** | **本轮复证**：worldbuilding contract reads=`['novel.json']`（load_contract 实测）；round-exec.sh create-mode 只建 t1-reports/t2-reports/t3-reports/skill-output/skill-traces + progress.json/meta.json/summary.json/.token-hashes.json（**无 novel.json**）；dispatch()（executor.py:190-214）在 dispatch_codex（技能执行处）之前跑 G1/G2；原 shell 注释 "the skill execution happened earlier" 证实门序回归。反证（缺 novel.json 是预置问题）不成立。P1 维持。 |
| F228 | **成立；支持低置信异议（P2→P1）维持 P2** | derive_output_files（audit/_shared.py:38-56）只 resolve_or_skip、不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found（本轮 probe：6 输出含字面 `truth/*.md`）。受 F227 掩盖维持低置信。 |
| F229 | **成立（范围低估但严重度维持）** | 本轮全技能枚举复证：无技能同时写 truth+decisions 概念（新增枚举，消除"truth 分支漏 decisions.json 的 G2.dec"潜在面）；decisions 类技能（chapter-drafting 等 5 个）的 .md 散文在 g2.py:83-84 被 continue 跳过 + executor 只调一次 run_g2 → .md 散文 G2.6/G2.8/G2.9 静默缺失。 |
| F230 | **成立** | **运行时复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`（无规范化）。 |
| F231 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者（复证）。 |
| F232 | **成立** | decisions.py:33 `has = rationale is not None`——空串/纯空白满足 REQUIRED；routine+low 空串被拒，两侧不对称（read 复证 + 前轮复现）。 |
| F233 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | **成立，P1 维持** | **本轮复证**：`git ls-files truth/` → bridge_tracker.md + character_matrix.md 均 tracked；worldbuilding watch 含 `truth/*.md`（_audit_watch_paths 实测）→ snapshot_tree(PROJECT_DIR=仓库根) glob 展开命中两文件 → F236 匹配失败 → 零写入派发也 `未声明写入` → rc=2；round_dir 真实写入不被审计。codex.py:62 cwd 已改 round_dir 而审计根未跟随；test_executor_audit.py monkeypatch `ex.PROJECT_DIR = tmp_path` 编码了正确语义。反证（审计面意图为仓库根）不成立。P1 维持。 |
| F236 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs（parametric_globs() 仅 patterns），registry globs（`truth/*.md`）恒判未声明（read 复证 + 前轮复现）。 |
| F237 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | **成立；支持 review6 低置信异议（P2→P1）维持 P2** | audit_writes（write_audit.py:52-56）对每个 post .md 无 try 地 parse_records；records/parser.py:40-44 yaml.safe_load 可抛 ScannerError/非 list 抛 ValueError；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | **成立** | **本轮实测**：derive_file_type worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归；executor.py:81-85 精确字符串交集）；state-settling → "truth"、chapter-drafting → "decisions"。 |
| F240 | **成立** | **本轮实测**：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证）。 |
| F242 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`（get_ownership → None）；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"。生产写者 lifecycle 无条目 → 记录级写保护对生产路径空转。 |
| F243 | **成立（机制修正维持）** | codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator；机制修正（review8）维持：gate-marker 强制 exit 3（scoring.py:364-377）先于分数校验 exit 2（:422-432）。 |
| F244 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak（不存在则静默无检查）；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | **成立** | **本轮复现**：resolve_chapter_path(volume-N/AC-NNN, 7) 全按章节填；resolve_or_skip(volume-N, None) → None；resolve_volume_path(volume-N, None) 抛 UnresolvedPathError；resolve_volume_path 仅 closure.py:158 消费（grep 复证）；`_build_skill_prompt`（dispatch_helper.py:656-658）以 resolve_chapter_path 直接调用 → chapter=None 时抛 UnresolvedPathError。 |
| F246 | **成立** | dispatch_skill（dispatch_helper.py:1864）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径；rd = round_dir or project_dir）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | **成立，P1 维持（范围注记维持）** | **本轮复现**：`_declared_patterns("shenbi-chapter-drafting") == []`（write_audit.py:19-26 无 chapter）vs `_audit_watch_paths("shenbi-chapter-drafting", 7) == ['chapters/chapter-7.md','chapters/chapter-7-decisions.json']`（executor.py:220-225 带 chapter）→ N 写技能成功派发恒判「未声明写入」rc=2。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | **成立** | PacingDesign.from_markdown（pacing_design.py:77-130）chapter_sequence 恒空（:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）致误拒/虚增（read 复证）。 |
| F249 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500（read 复证）。 |
| F250 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | **成立；支持 review9 低置信异议（P2→P1）维持 P2** | **本轮端到端复现**：`find_report(t1-reports, worldbuilding, generative)` 对 `-scores-subagent.json` → None；`gate_G_RECONCILE` → `FAIL`、`GR.2:...-scores-subagent:status=?`（rsplit 解析出 candidate_tt 含 "-scores-subagent" 后缀，与 progress "generative" 键永不相等）；g0.py:440 G0.10 glob `*-generative-scores.json` 不匹配；raw 维度 dict 无 final_score。 |
| F252 | **成立** | **本轮复现**：`python -m shenbi.scoring <rubric> <scores> --test-type generative --round-dir <rd> --subagent` → `{"status":"MARKER_MISSING","missing_markers":["G4-shenbi-worldbuilding-generative"]}`（exit 3）；executor 编排（:190-214）只跑 G1/G2 无 G4；grep write_gate_marker dispatcher/ → 空。 |
| F253 | **成立** | codex.py:27-48 `_record_completion` 读-改-写整体无锁（safe_write.py `_acquire_lock` flock 仅覆盖写入周期，读在锁外）；pipeline legacy 并行（parallel_dispatch.py Semaphore(4) 共享 project_dir）触发面真实。 |
| F254 | **成立** | run_g1/run_g2（executor.py:114-118/:125-137）与 shenbi-score 子进程（codex.py:95-109）均无 timeout；仅 codex exec 有 600s（codex.py:63）；`grep -n timeout src/shenbi/dispatcher/` → 仅 codex.py:63。 |
| F255 | **成立** | derive_file_type 对 4 个 report-kind truth 写者（score-volume/score-arc/score-stratum/review-arc-payoff）全 → "report"（executor.py:77-78 REPORT 分支先于 truth 交集）→ G2.11（g2.py:290 仅 file_type=="truth"）对其静默失效（read 复证）。 |
| F256 | **成立** | **本轮 read 复证**：g2.py:273/:315 注释声明 "chapter files only"，G2.10（:274-287）/G2.12（:316-322）位于 `if file_type == "chapter":` 块（:218-271）**之外**无条件执行——truth/report/json 全部受影响；句末符集合（:317）缺 `}`/`]` → 误归 chapter 的 JSON 恒 G2.12 WARN。 |
| F257 | **成立** | executor.py:156 `def dispatch(..., *, chapter: int | None = None)` 全仓零调用方（grep 仅 cli.py:29、executor.py:250，均不传）；dispatch_with_write_audit（:242）另行独立二次 extract_chapter（read 复证）。 |
| F258 | **成立** | `extract_chapter("Now that chapter 6 is complete, draft chapter 7")` → 6（首匹配）；单引用 → 7。pipeline 生成 prompt 均为单章节引用，仅 standalone 手工 prompt 触发。 |
| F259 | **成立** | `extract_chapter("draft chapter 0")` → 0；`resolve_chapter_path('chapters/chapter-NNN.md', 0)` → `chapters/chapter-000.md`；对照 chapter_planning.py:22 `Field(ge=1)`。 |
| F260 | **成立** | snapshot.py:63-76 `a.get(k) != b.get(k)` None 哨兵不可区分"缺席"与"null 值"→ null 值新增键旁路（read 复证 + 前轮复现）。根因 Z5，被 Z2 executor 审计链消费。 |
| F261 | **成立** | decisions.py:11-12 VALID_BASIS/VALID_SEVERITY 死常量（grep -c = 1 仅定义）；`_p25` 长度检查（:34）先于 routine_low FORBIDDEN（:41-42）→ routine+low 超长 rationale 报长度错误（read 复证 + 前轮复现）。 |
| F262 | **成立** | read_fields 声明与生产者模板系统性漂移（7 文件/5 技能）；book_strata/arc-N 部分匹配静默丢「未解决悬置」（F218 机制实锤）；style_profile/volume_map 等全漏恒 escape hatch。本轮复证所有 18 条 read_fields 声明均为 .md/.json 路径。 |
| F263 | **成立** | legacy.py:134-135 `split("---", 2)` 非行首锚定（frontmatter 内 "---" 子串静默截断或 ScannerError）；g0_skill_contract.py:67 同款孪生（read 复证）。 |
| F264 | **成立** | fields.py:38-52 重复 H2 标题 dict 键覆盖 → 首段静默丢失（read 复证 + 前轮复现）。 |
| F265 | **成立** | snapshot.py:87-91 `_diff_records` None 哨兵 → null 值新增键旁路 record_field 校验（F260 的 record 层孪生；read 复证 + 前轮复现）。根因 Z5。 |
| F266 | **成立** | legacy.py:85-87 write_semantics mode 值无词表校验（拼写错误静默通过）；append_dedup/merge_prose 声明无框架实现（dispatch_helper.py:1058-1067 显式 NOT branched、:1129 mode 仅作日志字段——本轮 read 复证 `log.info("output_written", ..., mode=mode)`）；create_or_overwrite 零消费者；contract `mode:` 与 SKILL.md `update_mode:` 双词表并行。**本轮增补 M 细节**：semantics 以**未解析契约路径**为键（如 `chapters/chapter-N.md`），`_write_one` 的 rel_path 为**已解析路径**（`chapters/chapter-7.md`）→ 对 parametric 写路径 mode 恒取不到（日志 mode=None）——F266 机制面的实例级细节，不新增根因。 |
| F267 | **成立** | cli.py:28 `prompt = sys.argv[4] if len(sys.argv) > 4 else ""`——argv[5:] 静默丢弃（read 复证）。 |
| F268 | **成立** | executor.py:124 `output_files = ",".join(outputs)`（G2 第 1 参逗号拼接）vs gates/cli.py:92 `a0.split(",")`（逗号拆分）——含逗号路径无法还原；truth-files.yaml 路径列实测 0 逗号，零触发面。 |
| F269 | **成立（新提低置信严重度异议 M→P2，见第三节）** | snapshot.py:63-67 `_changed_top_keys` except 分支（JSONDecodeError/TypeError）提前返回空变更集 → owned JSON 被覆写为非法 JSON 时 field 级键集校验静默旁路（read 复证 + 前轮复现）；F237/F260 家族第三个触发面，根因 Z5。 |

**误报统计**：整体误报 **0 条**（F201-F269 共 69 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F270-F271）

### F270 | `record_audit_outcome` 的 write-audit.jsonl ledger 写入无防护：round_dir 只读/磁盘故障时 finally 块裸抛，掩盖 dispatch 异常且审计记录丢失（F238 的 record 层孪生，故障面=文件系统） | 漏报（错误处理缺陷，F238 家族新触发面） | M

- 证据: `src/shenbi/audit/record.py:36-39`（`ledger.parent.mkdir(...)` → `ledger.open("a")` → `fh.write(...)` **无 try/except**——与同函数 trace seam 的 try/except（:52 `except (OSError, ValueError, TypeError)`）不对称）；`src/shenbi/dispatcher/executor.py:263-268`（finally 内 `audit_ok = record_audit_outcome(round_dir, skill, result)` 无捕获）→ 若 record_audit_outcome 抛异常：(a) finally 块提前退出，`if dispatch_exc is not None: raise dispatch_exc`（:269-270）被**掩盖**（调用方看到的是 OSError 而非 dispatch 原始异常）；(b) 审计结果（含 GATE_FAIL 判定）**整体丢失**（ledger 无记录）。
- 根因: F238 覆盖的故障面是 `audit_writes` 对 LLM 产出的 malformed YAML 解析崩溃（内容面）；本条是 `record_audit_outcome` **自身 I/O** 崩溃（文件系统面：round_dir 只读/磁盘满/权限变更）——两条独立路径都使 "崩溃仍审计" 保证（executor.py:245-246）失效，但前十六轮只列了 audit_writes 一条。record.py 的 ledger 写入是框架自有 I/O，未走 safe_write（无 flock/try），异常面完全裸露。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c`（/tmp 只读目录模拟）→ `record_audit_outcome(read_only_rd, 'shenbi-x', AuditResult(...))` → `PermissionError [Errno 13] Permission denied`（实测复现，输出见本报告验证清单）；对照 trace seam（record.py:52）有 try/except 捕获同型 OSError → 不对称确认
- 建议方向: executor.py finally 内对 `record_audit_outcome` 调用包 try/except（记 structlog CRITICAL 后继续 re-raise dispatch_exc）；或 record.py 的 ledger 写入走 safe_write（与 progress.json 同规）；补 round_dir 只读/磁盘故障用例（F238 修复清单联动）

### F271 | `extract_h2_sections` 不跳过围栏代码块：truth 文件内 ``` 围栏含 `## ` 行时产生伪 section，字段过滤输出携带悬空围栏标记（F264/F218/F262 均未覆盖代码围栏干扰面） | 漏报（边界解析缺陷，字段过滤层） | M

- 证据: `src/shenbi/contracts/fields.py:38-52`（`extract_h2_sections` 逐行 `if line.startswith("## ")`，**无围栏状态跟踪**——不跳过 ``` 开闭围栏内的 H2 形行）；`:55-64`（`_filter_md` 以 sections dict 输出）→ 围栏内伪 section 使：(a) 围栏开启标记 ` ``` ` 被并入上一个真 section 的 body，过滤输出含**未闭合围栏**（下游 LLM 看到悬挂 ```md）；(b) 围栏与下一真 H2 之间的正文被归入伪 section，若伪标题未命中声明字段则该段正文**静默丢失**（F218 的未命中丢弃机制在围栏场景的新触发形态）。F264 覆盖重复标题折叠、F218 覆盖部分匹配丢弃、F262 覆盖声明漂移，均未触及代码围栏干扰。
- 根因: section 解析器假设 `## ` 只出现在真实章节标题行；对 markdown 围栏（``` 开闭）与 HTML 注释无感知。LLM 产出的 truth 文档内含 "```markdown\n## 示例\n..." 型示例块属正常概率路径（skills 模板已有 ```dot/```markdown 围栏先例——anchor-curate/anti-detect SKILL.md:36,86）。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c` → `extract_h2_sections('## 当前任务\nbefore\n\n```md\n## 伪标题\ncode\n```\n\n## 世界设定\nafter')` → `{'当前任务': 'before\n\n```md', '伪标题': 'code\n\n```', '世界设定': 'after'}`；`filter_to_fields(text, ['当前任务'], 'x.md')` → `matched=True`、输出 `'## 当前任务\nbefore\n\n```md'`（**悬空围栏**）；对照 grep fixtures/novel-output 真实文件零触发（latent）
- 建议方向: `extract_h2_sections` 增加围栏状态跟踪（```` ``` ```` 开闭切换时跳过内部行；HTML 注释块同理）；与 F264 修复联动（fields.py 重构时统一处理标题边界）；补围栏内 H2 测试

---

## 三、严重度异议（不修改前十六轮，仅提理由）

1. **支持 F214 P2 → P1（已入账）——附机制精度注记（非异议）**：gate_G3 的 G3.4 在 progress.json **缺失**时返回 **SKIP**（g3.py:205-206 `c.append({"id": "G3.4", "s": "SKIP", "r": "no progress.json"})`），并非 FAIL——run_gate_g3 的伪造（dispatch_helper.py:1934-1945）实际把 **SKIP 转 PASS** 并**伪造独立性证据**（本轮子进程实测：无 progress → SKIP；伪造后 → G3.4 PASS、G3.5 PASS "0 prior scorers"）。"G3.4 fail-closed 以生产方自写证据放行"的表述在缺失路径上应修正为"伪造使 G3.4 从 SKIP 转 PASS 并注入虚假证据"；fail-closed 的 FAIL 面在成功派发路径（真实 progress.json 无 scorer）正常触发（review10 已列：伪造恰在失败路径触发、成功路径反而 FAIL）。**F214 本体（pipeline 主动写入伪造评分独立性证据、违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"、且掩盖失败派发）不因该精度注记而动摇，P1 维持。**
2. **支持 F218 P2 → P1（已入账）**：AGENTS.md "If a declared field is missing ... returns the full file + logs WARN" 部分缺失时两处均不满足（fields.py:55-64 本轮复现）；F262 提供 book_strata/arc-N 真实数据形状实例；truth 文件随剧情演进缺 section 属正常路径。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——SKILL.md 内部 7 处自相矛盾，权威区间未定，维持低置信。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found 恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内 → record_audit_outcome 永不执行（审计记录丢失）；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（chapter-drafting/context-composing/chapter-revision 等 5 个）的 .md 散文在 G2 被 continue（g2.py:83-84）整体跳过，G2.6/G2.8/G2.9 对章节正文静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F251 P2 → P1（低置信维持）**：评分产物三重不匹配使 standalone 轮次对 G5.1（no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL，本轮端到端复现）确定性失败；受 F252（exit 3 前置）掩盖维持低置信。
8. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。
9. **新提（低置信）F269 M → P2**：`_changed_top_keys` 的 JSONDecodeError 分支把"无法解析"静默降级为"无变更"（snapshot.py:63-67）——owned JSON（genre-config.json）被覆写为**非法 JSON**（LLM 截断/拼接输出的正常概率路径）时，field 级键集校验零感知、审计记 AUDIT_PASS，**静默**（不崩溃、不告警）；对比同链 F238（.md 解析崩溃）为 **loud**（P2，且已有 P1 异议在审）——静默吞错比 loud 崩溃更接近决策表 P2"错误处理缺陷"。维持低置信的理由：审计职责边界为"所有权"而非"内容有效性"（非法 JSON 属 G2/G4 面），且当前被 F227/F235/F247/F252 链掩盖；供协调者裁决（若裁定"owned 文件内容损坏且审计零感知"即 P2 则升）。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc / 子进程，零仓库写入）：
  - F216 空 customRules + texture:False 禁用维度 → model_validate PASS（bug 复现）
  - F218 部分匹配 → matched=True、仅命中 section、无 WARN；全缺 → 全文 + WARN
  - F227 worldbuilding reads=['novel.json']；fresh round 输入/输出含不存在路径（含字面 `truth/*.md`）；round-exec.sh create-mode 产物清单（无 novel.json）
  - F235 `git ls-files truth/` → bridge_tracker.md + character_matrix.md tracked；watch 含 `truth/*.md` + PROJECT_DIR=仓库根
  - F239 derive_file_type 全技能枚举：worldbuilding→chapter、chapter-drafting→decisions、state-settling→truth、score-volume→report；**新增枚举：无技能同时写 truth+decisions 概念**
  - F247 `_declared_patterns`=[] vs `_audit_watch_paths(chapter-drafting, 7)`=2 项（chapter-7.md/decisions.json）
  - F214 机制：`gate_G3`（无 progress）→ G3.4 SKIP；伪造 progress → G3.4 PASS + G3.5 PASS "0 prior scorers"
  - F251 find_report → None（-subagent.json）；G_RECONCILE → `GR.2:...-scores-subagent:status=?` FAIL
  - F252 shenbi-score 实跑 → `MARKER_MISSING G4-shenbi-worldbuilding-generative`（exit 3）
  - **F270（新）** record_audit_outcome 对只读 round_dir → PermissionError 裸抛（ledger 写入无 try）
  - **F271（新）** extract_h2_sections 围栏内 H2 → 伪 section + 过滤输出悬空 ` ```md `；fixtures/novel-output 真实文件零触发（latent）
  - F210 hooks 行号（PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94）；F266 增补：dispatch_helper.py:1129 mode 仅作日志字段
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75，注：gates 区 test_g0.py:311-316 实有断言，Z2 子集未覆盖）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git ls-files`/`git status` 仅读；探针均在 /tmp 目录与 python -c。

---

## 五、统计

- 新 finding：**2 条**（F270-F271：M×2）；无新增 P1/P2
- 误报：**0 条整体误报**（F201-F269 共 69 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持；F214 机制精度注记 1 条，非误报）
- 严重度异议：**9 条**（支持已有 8 条：F214→P1、F218→P1（均已入账）、F217→P1 低置信、F228→P1 低置信、F238→P1 低置信、F229→P1 低置信、F251→P1 低置信、F220 维持 P2；**新提 1 条低置信：F269 M→P2**）
- 前十六轮 findings 判定：F201-F269 共 69 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否（严格意义上）**——本轮仍有 2 条新发现（F270/F271，均为 **M 级**：record_audit_outcome ledger 写入无防护、extract_h2_sections 围栏干扰），**无新增 P1/P2**；6 条 P1（F214/F216/F218/F227/F235/F247）经独立运行时复现全部维持。**实质性（P1/P2）新 finding 已收敛为 0**，剩余 2 条 M 级边界遗漏（F270 建议随 F238 修复清单一并处理；F271 随 fields.py 重构/F264 修复一并处理）。
