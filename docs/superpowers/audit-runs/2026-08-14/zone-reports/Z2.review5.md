# Z2 分区第五轮独立复核报告（Z2.review5）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）
- 模式: fresh-context 全量重读 38 文件；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖），不依赖前五轮转述；另读跨区消费面（gates/shared.py、gates/cli.py、g1.py、g2.py、g3.py、g3_independence.py、g4/{genre_config,pacing_design,chapter_planning,context_composing,volume_outlining,decisions_validator}.py、audit/{_shared,snapshot,write_audit,record}.py、pipeline/{dispatch_helper,closure,chapter_loop}.py、safe_write.py、cli_utils.py、status.py、scoring.py、round-exec.sh、first-novel.md、truth-files.yaml、tests/unit/dispatcher/test_executor_audit.py、git show 99f91f0:tests/dispatch-subagent.sh）。
2. **独立验证方式**：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前五轮一致）；20+ 个 `python -c`/heredoc 运行时探针（F216/F218/F230/F232/F215/F236/F237/F238/F239/F240/F241/F245/F246、F227 fresh-round 端到端、F235 审计面端到端、N 占位符碰撞）。
3. **前五轮判定**：F201-F244 共 **44 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；已有 4 条严重度异议（F214/F218→P1、F217/F228→P1 低置信）经独立反证尝试后**全部支持**，本轮**不新增异议**。
4. **本轮新发现 2 条（F245-F246）+ M 级观察 4 条**，集中于五轮都未触及的两类盲区：
   - **N 占位符语义碰撞（F245）**——`resolve_chapter_path`/`resolve_or_skip` 把契约中所有 bounded `N` 一律按**章节号**解析：`arc-N`（memory-distill 写 / context-composing 读）、`volume-N`（score-volume/review-arc-payoff 写）、`stratum-N`（score-stratum 写）、`escalation-N`（escalation-review 写）、`AC-NNN`（anchor-curate 写）全部被章节号替换或（chapter=None 时）整条丢弃；`resolve_volume_path` 除 closure.py:158 外零消费 → **pipeline API 路径对 4 个卷/弧技能 dispatch 直接崩 `UnresolvedPathError`（"Prompt build failed"）**，closure 永不完成（与 F371 独立同类的第二个 closure 阻塞根因）。
   - **pipeline legacy 回退继承 standalone 全缺陷（F246）**——`dispatch_skill`（dispatch_helper.py:1864）无 API key / 无 codex CLI 时默认 `uv run shenbi-dispatch` → pipeline 默认路径就是 F227/F235 的故障路径（fresh 轮 G1 失败；worldbuilding G1 过而审计 rc=2 GATE_FAIL）→ pipeline genesis 在默认环境下无法完成。

---

## 一、复核结论表（F201-F244 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 重读三模型 + g4 私有实现：g4/chapter_planning.py:99-102（s7 hook ops 子串存在性）vs 模型（typed change/defer 激活计划）；g4/context_composing.py:74-86（P1-P7/route-a/b/c 标题）vs 模型（section_count==9）；g4/volume_outlining.py:59-72（KR/张力曲线/跨卷桥）vs 模型（entity_hooks>=3 且 types>=2）。grep 无生产消费者；cov 34/38/40% 吻合。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13 直接 import 模型）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g6.py:51 raw dict 读 target_word_count 属实——死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797（"BLOCKING"/"FAIL"）、:409-438（G4Severity 自建词表）复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED；status.py:27 GateStatus 实有（read 确认）。 |
| F210 | P2 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文。**附注**：gates/cli.py:148-150 对已知 gate 恒返回 0（PASS/FAIL 只经 stdout status 传达）→ returncode 检查只能捕获 CLI 自身崩溃（rc=1 + 空 stdout → 裸 JSONDecodeError），"门二进制失败"实为"门 CLI 崩溃"——建议方向需按此修正（检查 rc + 空 stdout 容错），finding 本体成立。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | P2 | **成立；支持异议→建议 P1** | codex.py:20-48 不写 current_scorer_agent；dispatch_helper.py:1939-1940 唯一写入点为伪造 `pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（read 复证；且 review4 增补成立：伪造条目键为 "agent" 非 "agent_id" → G3.5 prior_agents 恒空 → PASS）。反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。 |
| F215 | P2 | **成立** | 运行时复现：`\{[^{}]*\}` 对嵌套 JSON 取内层（`{"final_score":90,"dimensions":[{"num":1,"score":88}]}` → `{"num":1,"score":88}`）。 |
| F216 | P1 | **成立，P1 维持** | **反证尝试失败**：空 customRules + disabled dim（5 维度 1 禁用、customRules=[]）→ `GenreConfig.model_validate` PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；G4 门（g4/genre_config.py:36）以 model_validate 强制 → 校验洞真实可触发。P1 维持。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | 实测 SKILL.md 内 5 处 CONSTELLATION 区间 + docstring 20-30 + 模型 [15,35]；模型对标准卷过松（25-35 过门违反 :91 上限）对开卷过严（36-40 误拒，:193 允许 30-40）。权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | **反证尝试失败**：md 部分匹配 → 仅命中 section、matched=True、无 WARN（fields.py:55-64 实测）；AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 部分缺失时两处均不满足。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（复现）；executor.py:74-75/:107-108 ContractError 静默降级；`_truth_file_set`/`_decisions_file_set` 缓存固化空集复证。 |
| F220 | P2 | **成立（计数修正维持：6 条）** | ownership.py:73-94 实测 6 条目（含 foundation-review read 条目）；read_keys 死声明（review3 M）复证。**轻微严重度存疑**：矩阵规模小是 docstring 明示的过渡态设计（"支柱一续"），缺陷核心在 F233/F234 的接线盲区而非矩阵规模本身——但不构成整体误报。 |
| F221 | P2 | **成立** | 运行时复现：fixture 9 键含 tropeInventory，`GenreConfig.model_validate` PASS 且 tropeInventory 被静默丢弃（extra=ignore）；SKILL.md:286/:290 声称 8 键；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性）/g1（glob 未匹配）/dispatch_helper:382（设置者）三处语义分叉复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except。 |
| F227 | P1 | **成立，P1 维持** | **运行时复现**：fresh tmp 轮（真实 worldbuilding 契约）→ G1 FAIL（novel.json G1.1 not found）+ G2 FAIL（6 输出 G2.1 not-found 含 `truth/*.md` 字面）；round-exec.sh create-mode 只建脚手架（t1-reports/skill-output/progress.json/meta.json/summary.json，无 novel.json）→ 文档化 first-novel.md:38-41 流程第一步必失败。`git show 99f91f0` 原 shell 注释 "the skill execution happened earlier" 证实门序回归。反证不成立。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | derive_output_files 返回 `truth/*.md` 字面（audit/_shared.py:38-56 不展开 glob）；gate_G2 无 glob 展开（g2.py:44-49）→ 重试轮仍 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现；`derive_file_type("shenbi-worldbuilding") == "chapter"`；g2.py:76-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。**范围增补**：同一类别还波及 chapter-planning/volume-outlining/pacing-design 等 plan/outline 写者（"chapter" 型 → G2.6 字数下限/G2.8/G2.9 误拒），F229 的 (a) 是宽类别。 |
| F230 | P2 | **成立** | 运行时复现：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,187 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者。 |
| F232 | P2 | **成立** | 运行时复现：`Selection(severity="high", basis="manual_override", rationale="")` → ACCEPTED；`Adjustment(rationale="")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线。本轮补证：fixture pending-hooks-example.md 记录键集恰好 16 键 = _HOOK_KEYS_NEW_RECORD（键值对得上），但**接线缺失使该声明零效力**。 |
| F234 | P2 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三分支均不看 status；owned 文件删除零违规复读通过。 |
| F235 | P1 | **成立，P1 维持** | **运行时端到端复现**：PROJECT_DIR 保持真实仓库根（不 monkeypatch），fake dispatch rc=0 → `write_audit_gate_fail violations=['未声明写入: truth/bridge_tracker.md', '未声明写入: truth/character_matrix.md']`、rc=2、ledger blocked=True；`git ls-files truth/` 确认两文件 tracked；test_executor_audit.py:18,40 把 `ex.PROJECT_DIR = tmp_path`（==round_dir）编码了正确语义。反证（审计面意图为仓库根）不成立。P1 维持。 |
| F236 | P2 | **成立** | 运行时复现：`_matches_declared("truth/bridge_tracker.md", ["truth/*.md"], parametric_globs())` → False（直接 fnmatch → True）；`"truth/*.md" in parametric_globs()` → False。 |
| F237 | P2 | **成立** | 运行时复现：`compute_file_change("genre-config.json", None, '{"hacked": true}')` → `status='added', changed_top_keys=()` → `check_write_ownership` → `[]`（对照 modified + title 键 → 违规）。 |
| F238 | P2 | **成立** | 运行时复现：malformed `## hooks` YAML → `audit_writes` 裸 ParserError 崩溃（executor.py finally 无捕获，ledger 无记录）。 |
| F239 | P2 | **成立** | 运行时复现：`derive_file_type` worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归）；state-settling/plant → "truth"。 |
| F240 | P2 | **成立** | 运行时复现：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`。 |
| F241 | P2 | **成立** | 运行时复现：①`approval:{}` → PASS；②禁用非 list → 计数规则跳过；③替换建议非 dict → 替换规则跳过（`if decision and ...`/isinstance 守卫）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`；plant/track SKILL.md:14-15 自述 DEPRECATED。 |
| F243 | P2 | **成立** | `git show 99f91f0:tests/dispatch-subagent.sh` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（原第 4 参为独立评分 prompt）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |

**误报统计**：整体误报 **0 条**（F201-F244 共 44 条全部实质成立）；部分误报 1 条（F220 计数 5→6，实质成立）；表述修正 1 条（F211，实质成立）；F212 附注 1 条（修复方向需按"门 CLI 崩溃"修正表述，本体成立）。

---

## 二、新发现（F245-F246）

### F245 | contracts/paths.py N 占位符语义碰撞：arc-N/volume-N/stratum-N/escalation-N/AC-NNN 一律按章节号解析或丢弃；resolve_volume_path 除 closure 外零消费 → pipeline 卷/弧技能 dispatch 崩 UnresolvedPathError | 漏报（正常路径功能错误，跨区） | P2
- 证据: `src/shenbi/contracts/paths.py:28-34`（`resolve_chapter_path` 对所有 bounded `[-/]N[-/.]` 一律按章节替换）、`:50-55`（`resolve_or_skip` → 仅 resolve_chapter_path）；`resolve_volume_path`（:37-42）全仓唯一消费者 `pipeline/closure.py:158`（仅 G4 路径解析用）；executor.py:102（derive_input_files）、audit/_shared.py:52（derive_output_files）、dispatch_helper.py:581,656-658（pipeline 读/输出解析）全部走 resolve_chapter_path/resolve_or_skip；真实契约含非章节点位符：`truth/arcs/arc-N.md`（shenbi-memory-distill 写、shenbi-context-composing 读）、`audits/volume-N-payoff.md`（review-arc-payoff 写）、`audits/volume-N-score.md`（score-volume 写）、`audits/arc-N-score.md`（score-arc 写）、`audits/stratum-N-score.md`（score-stratum 写）、`audits/escalation-N-report.md`（escalation-review 写）、`benchmarks/anchors/AC-NNN.md`（anchor-curate 写）；truth-files.yaml:62-67,94,116 注册为 volume/arc/stratum/escalation/anchor parametric。
- 根因: 占位符解析只认识"chapter"语义——`resolve_chapter_path`/`resolve_or_skip` 无 volume/arc/stratum/escalation 上下文参数；`resolve_volume_path` 存在但从未接入契约解析链（仅 closure 的 G4 路径），`extract_chapter` 只提取章节号。结果：(a) chapter 非 None 时 `arc-N`/`volume-N` 等被填**章节号**（`volume-N-payoff.md` + chapter 7 → `volume-7-payoff.md`，语义错）；(b) chapter=None（卷/弧技能 prompt 无 "chapter N"）时整条丢弃（读/写路径从 G1/G2/审计消失）；(c) **pipeline API 路径**：closure step 的 prompt（"Execute shenbi-score-volume for book closure (step 5)"）无章节 → `_build_skill_prompt` 输出收集 `resolve_chapter_path("audits/volume-N-score.md", None)` 抛 UnresolvedPathError → `_dispatch_via_api` 捕获为 "Prompt build failed" → 4 个卷/弧技能（score-volume/score-stratum/memory-distill/review-arc-payoff）在 closure **确定性失败**（重试×3 后 escalation），是继 F371 之后第二个独立 closure 阻塞根因。
- 验证命令: `PYTHONPATH=src .venv/bin/python` →
  - `resolve_chapter_path("audits/volume-N-payoff.md", 7)` → `audits/volume-7-payoff.md`（章节号填入卷占位符）
  - `resolve_chapter_path("truth/arcs/arc-N.md", 7)` → `truth/arcs/arc-7.md`；`AC-NNN` + 7 → `AC-007.md`
  - `resolve_or_skip("audits/volume-N-payoff.md", None)` → None（整条丢弃）
  - `derive_output_files("shenbi-review-arc-payoff", 7)` → `['audits/volume-7-payoff.md', 'truth/audit_drift.md', 'truth/arc_payoff_trend.md']`
  - `_build_skill_prompt("shenbi-score-volume", tmp_pd, "Execute ... (step 5)", None)` → `RAISED UnresolvedPathError: audits/volume-N-score.md`（score-volume/review-arc-payoff/memory-distill/score-stratum 四者均崩）
  - `grep -rn "resolve_volume_path" src/` → 仅 closure.py:20,158
- 建议方向: 占位符解析引入类型化上下文（chapter/volume/arc/stratum/escalation/anchor 分离，如 `resolve(path, kind, value)`），derive_input_files/derive_output_files 按 registry parametric 语义选择解析器（复用 `resolves()` 的 registry 判定）；executor/audit/dispatch_helper 三处收敛为同一 helper；卷/弧技能 prompt 需携带卷号或 CLI 加 `--volume/--arc` 参数；补 runtime probe 测试（现 test_paths.py 只有 chapter-NNN 用例）
- 影响注记: standalone 路径被 F227/F235 掩盖（fresh 轮先死于门序/审计），pipeline 路径被 F371 等 closure 阻塞掩盖——建议在 F227/F235 修复后重评（届时本缺陷独立阻塞卷/弧技能正常路径，可升 P1）

### F246 | dispatch_skill legacy 回退默认路由到 shenbi-dispatch → pipeline 默认路径继承 standalone 全部缺陷（F227 门序 + F235 审计 rc=2）→ pipeline genesis 在无 API key/无 codex 环境无法完成 | 漏报（接线缺陷，跨区放大） | P2
- 证据: `src/shenbi/pipeline/dispatch_helper.py:1826-1880`（`dispatch_skill`：`if os.environ.get(_ENV_LLM_API_KEY)` → API；`if _find_ide_cli()` → IDE；**else legacy：`["uv","run","shenbi-dispatch", skill, test_type, rd, prompt]`**）；`executor.py:228-268`（shenbi-dispatch = dispatch_with_write_audit，即 F235 审计链 + F227 门序的全部实现）；pipeline-init 创建 novel.json（first-novel.md:227）→ pipeline legacy 路径 G1 能过，但 F235 审计（PROJECT_DIR=repo root watch truth/*.md → bridge_tracker/character_matrix 未声明写入 → rc=2）使 worldbuilding 派发恒 FAIL；`DispatchResult(r.returncode == 0, ...)` → genesis `handle_dispatch_failure` 重试×3 → escalation。
- 根因: pipeline 的默认（无 API key、无 IDE CLI 的 CI/开发环境）回退路径直接复用 standalone CLI，而 standalone CLI 的审计面（F235）、门序（F227）缺陷因此**继承为 pipeline 缺陷**；前五轮把 F235/F227 当作 standalone 路径问题处置，未指出 legacy 回退把 pipeline 主路径也接上同一故障实现。
- 验证命令: read dispatch_helper.py:1826-1865（回退分支）；F235 探针（本报告）：真实 PROJECT_DIR + fake dispatch rc=0 → rc=2 + ledger blocked；组合推理：pipeline legacy 路径 → shenbi-dispatch → F235 rc=2 → `DispatchResult(False, 2, ...)` → genesis retry/escalate
- 建议方向: legacy 回退路径显式标记并修复（随 F235/F227 修复联动）；或让 legacy 回退跳过审计/门序（与 API/IDE 路径语义对齐——二者不跑 write-audit，见 F512）；在 dispatch_skill 文档中明示"默认路径 = standalone dispatcher，继承其门序/审计语义"
- 影响注记: 属 F227/F235 的接线放大（根因在 Z2），不新增独立根因

### M 级观察（随文记录）
- `contracts/schemas/registry.py:55` `RegistryConcept.glob` 字段全仓零消费者（truth-files.yaml concepts 不用该字段，graph.py 只读 registry.globs/patterns 的 glob）——死字段（M）
- `dispatch_helper.py:1864` legacy 回退 `["uv","run","shenbi-dispatch",...]` 的 `rd` = round_dir or project_dir：pipeline 模式项目目录含 pipeline-state.json → executor `is_pipeline=True` → G2 跳过、但 **G1 仍在 is_pipeline 检查之前运行**（executor.py:190-197）——pipeline-mode CLI 派发仍执行 G1（对项目目录 contract reads），语义未文档化（M，F246 关联）
- `round-exec.sh` create-mode 初始 progress.json（t1-reports/…/progress.json）只含 completed_skill_names/skills/tier/expected_chapters，**无 current_scorer_agent/scoring_history**——若该轮次被 G3 评估（非独立路径）G3.4 必 FAIL（F214 语境补充，M）
- `executor.py:197` `is_pipeline = (round_dir / "pipeline-state.json").exists()`：陈旧 pipeline-state.json 使 standalone 轮次静默跳过 G2（G1 仍运行），无日志区分（M）

---

## 三、严重度异议（不修改前五轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1**（评分独立性证据被生产方伪造）：`run_gate_g3`（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id"，G3.5 恒 PASS）→ G3.4 fail-closed 以生产方自写证据放行。反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持 F218 P2 → P1**（部分匹配静默丢字段）：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 部分缺失时两处均不满足（fields.py:55-64 实测）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）但对开卷过严（36-40 误拒，SKILL.md:193 允许 30-40）——权威区间未定（SKILL.md 内部 5 处自相矛盾），维持低置信供协调者裁决。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮（输出已存在）G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖故维持低置信。
5. **F212 修复方向附注（非异议）**：gates/cli.py:148-150 对已知 gate 恒返回 0 → "rc!=0 即门失败"的修复方向在 gate FAIL 时永不触发（status 在 stdout）；建议方向应为"检查 rc!=0（CLI 崩溃）或 stdout 空 → 记 gate/rc/stderr 后返回失败"，findings 本体成立。
6. **F220 轻微严重度存疑（非异议）**：矩阵规模小系 docstring 明示的过渡态（支柱一续），核心缺陷在 F233/F234 接线盲区；若协调者认为"文档明示的过渡态"不构成缺陷，可考虑 P2→M——但 file-level 兜底对 glob 写者失效（F236）使过渡态非零风险，维持 P2 亦可。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**；运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim（5 维度 1 禁用）→ PASS（bug 复现）
  - F218 md 部分匹配 → 仅命中 section、matched=True、无 WARN
  - F230 json 键规范化缺失 → matched=False + 全文 + WARN（U+3000 声明键）
  - F232 Selection/Adjustment 空串 rationale → ACCEPTED；routine+low 空串被拒（不对称）
  - F215 嵌套 JSON → 取内层 `{"num":1,"score":88}`
  - F236 `_matches_declared` glob False / 直接 fnmatch True / parametric_globs 无 glob 键
  - F237 added JSON → FileChange(status='added', changed_top_keys=()) → check_write_ownership []
  - F238 audit_writes malformed `## hooks` → ParserError 崩溃、ledger 无记录
  - F239 derive_file_type 4 个 glob 写者全归 "chapter"（+ state-settling/plant → "truth"、chapter-drafting → "decisions"）
  - F240 extract_chapter 中文 → None / 英文 → 7
  - F241 genre_config ①空 approval PASS ②禁用非 list PASS
  - F227 fresh tmp 轮端到端：G1 FAIL（novel.json G1.1）+ G2 FAIL（6 输出 G2.1，含 `truth/*.md` 字面）
  - F235 审计面端到端：真实仓库根 + fake dispatch rc=0 → violations（bridge_tracker/character_matrix）→ rc=2、ledger blocked=True
  - F245 占位符碰撞：resolve_chapter_path(volume-N/arc-N/stratum-N/AC-NNN, 7) 全按章节填；resolve_or_skip(volume-N, None) → None；derive_output_files(review-arc-payoff, 7) → volume-7-payoff.md；`_build_skill_prompt` 对 score-volume/review-arc-payoff/memory-distill/score-stratum 全抛 UnresolvedPathError
  - F246 dispatch_skill legacy 回退分支 read 确认（dispatch_helper.py:1826-1865）+ F235 探针组合
  - F233 补证：fixture pending-hooks-example.md 记录键集 == _HOOK_KEYS_NEW_RECORD（16 键一致）
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243 的 cwd/角色差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c。

---

## 五、统计

- 新 finding：**2 条**（F245-F246：P2×2）；M 级观察 4 条随文
- 误报：**0 条整体误报**（F201-F244 共 44 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持）
- 严重度异议：**4 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1、F217→P1（低置信）、F228→P1（低置信）；另 F212 修复方向附注 1 条、F220 轻微存疑 1 条（均非异议）
- 前五轮 findings 判定：F201-F244 共 44 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否**（F245-F246 共 2 条新发现；F245 为独立根因的 N 占位符语义碰撞，含 pipeline closure 确定性失败路径，F227/F235 修复后需重评严重度）
