# Z11 分区初审报告 — 磁盘执行产物（novel-output/ + truth/ + .hypothesis/ + .superpowers/ + 日志）

- 日期：2026-08-14
- 审查层：D4（产物与 pipeline 不变量一致性）
- 清单：`zones/Z11.files`（1281 文件，全部存在，缺失 0）
- 只读约束：除本报告外未写仓库；所有验证命令实际运行（grep/read/python3 只读）
- 交叉引用：Z11-01（findings-ledger:15，decisions.json 83/145 无效，根因悬置）→ 本报告给出根因答案；T1-01/T1-02/T1-03/T1-05（thread-reports/T1.md）本报告独立复验；F324/F353/F397/F640（簇 1/2 数据与完成性）→ 本报告给出磁盘产物侧实证

---

## 0. 覆盖统计

| # | 类别（清单内路径模式） | audited | 主要不变量 |
|---|----------------------|---------|-----------|
| 1 | `novel-output/xinghuo-ranqiong/chapters/*.md`（56） | ✅ | chapter-file-format 契约（META 块/章节头/正文完整性） |
| 2 | `chapters/*decisions.json`（89 含 revision）+ staging decisions（56） | ✅ | shenbi-decisions-v1 schema + P2.5（含 Z11-01 根因） |
| 3 | `truth/*.md`（13）+ `staging/truth/*`（8）+ 根 `truth/*`（2） | ✅ | truth-files.yaml 声明 kind/路径一致 |
| 4 | `pipeline-state.json` / `progress.json` / `gate-markers/*`（22） | ✅ | round 状态标记一致性 |
| 5 | `cost/token-ledger.jsonl`（若存在） | ✅（不存在） | F302 少计对照 |
| 6 | `audits/*.md`（722） | ✅（抽样+模式） | 报告重复/冗余（F329/F387） |
| 7 | `config-change-log.jsonl` / `write-audit.jsonl` / `trace.jsonl` | ✅ | jsonl ERROR/WARN/异常 |
| 8 | `.superpowers/sdd/*`（15）+ `sdd-archive-issue24/*`（5） | ✅ | spec-deviations 声明修复核实 |
| 9 | `.hypothesis/examples/*`（43）+ patches（13） | ✅ | 17 失败样本修复状态 |
| 10 | `novel-output/test-validation/*`（27）+ `validation-results/*`（4） | ✅ | 产物真实性 |
| 11 | `snapshots/*`（52）`plans/*` `staging/plans/*` `context/*` `world/*` `outline/*` `genesis-context/*` `characters/*` `style/` `foundation/` `novel.json` `genre-config.json` `DEBUG_USE_MANUAL_CREATE.md` | ✅ | 与 DEBUG 文档/状态机一致性 |
| 12 | `./.codegraph/daemon.log` `./.playwright-mcp/console-*.log` | ✅ | 开发机日志（非 pipeline 产物） |

**audited 类别数：12/12；清单类别数：12；未覆盖：无。低置信度项见 §3。**

---

## 1. 类别条目

### novel-output/xinghuo-ranqiong/chapters/*.md（56 个正文章节）
- 处置: audited
- 声称检查的不变量: META-BEGIN/META-END 块存在且成对；`# Chapter N: [Title]` 章节头；正文（prose）完整存在
- findings: F1300, F1301, F1302, F1303
- 验证命令: `python3` 全量扫描 56 文件（META 块计数/头部正则/退化检测）；`head` 抽样 ch1/ch2/ch9/ch12/ch40/ch44/ch55；`grep -rl` 正文残留搜索
- 置信度: high（全量扫描 + 逐文件抽样）

### chapters/*decisions.json + staging decisions（145 个）
- 处置: audited
- 声称检查的不变量: JSON 可解析；`$schema: shenbi-decisions-v1`；必填键（skill/chapter/produced_at/selections）；selections/adjustments/budget 结构；P2.5 rationale 规则
- findings: F1304（Z11-01 根因更新）, F1305, F1306
- 验证命令: `python3` 全库扫描 145 文件分类失败模式（extra_data 67 / other 14 / control_char 2 / valid 62）；DecisionsDoc 必需键检查（57 schema-ok）；`json.loads` 逐文件
- 置信度: high（全量）

### truth/*（13 + 8 + 2）
- 处置: audited
- 声称检查的不变量: truth-files.yaml 声明的 kind/路径；staging 与正式 truth 一致性；根目录 truth/ 与项目 truth/ 的关系
- findings: F1307, F1308
- 验证命令: `diff` staging vs 正式（pending_hooks 9866 vs 4171 字节不一致）；`grep` bridge_tracker 在 yaml/index/skill 三方；`head` 根 truth 文件
- 置信度: high

### pipeline-state.json / progress.json / gate-markers/
- 处置: audited
- 声称检查的不变量: phase/closure 状态；chapter_states 完整；progress.json 内容与 DEBUG 文档声明一致；gate-marker 数量/内容与 skill 列表一致
- findings: F1309, F1310, F1311, F1312
- 验证命令: `python3` 读 pipeline-state 结构（closure=pending、closure_step=0、total_chapters ABSENT）；`cat` progress.json（仅 scorer 字段）；22 个 marker 全量时间戳对比；`diff` 双 resonance marker
- 置信度: high

### cost/token-ledger.jsonl
- 处置: audited
- 声称检查的不变量: F302 预测"ledger 恒空/dead-wire"；运行期应有 token 记录
- findings: F1313
- 验证命令: `find` 全库无 token-ledger.jsonl、无 cost/ 目录；`grep` dispatch_helper TokenLedger 接线（:1333 record 已接，2026-08-02 修复）
- 置信度: high（产物缺席本身即证据）

### audits/（722）
- 处置: audited
- 声称检查的不变量: 13 维 × 章 覆盖；无重复内容；F329/F387 双发模式（sensitivity 等）
- findings: F1314, F1315
- 验证命令: `python3` 维度计数（anti-ai 56 / dialogue 55 / sensitivity 55 等，ch56 缺 6 维）；md5 重复检测（0 组）；`python3` 计算 get_active_genre_audits（含 texture）vs 磁盘 texture=0
- 置信度: high

### jsonl 日志（config-change-log / write-audit / trace）
- 处置: audited
- 声称检查的不变量: 无 ERROR/WARN/traceback/retry 异常；配置变更日志与 genre-config 实际状态一致
- findings: F1316, F1317
- 验证命令: `cat` config-change-log.jsonl（1 行，old=true/new=true 无操作条目，时间戳晚于运行结束）；`python3` 读 write-audit/trace 4 条对齐（GATE_FAIL×3 worldbuilding + AUDIT_PASS）
- 置信度: high

### .superpowers/sdd/* + sdd-archive-issue24/*
- 处置: audited
- 声称检查的不变量: spec-deviations.md 声明的修复在当前代码中真实存在
- findings: 无（全部核实通过）
- 验证命令: `grep finish_reason` dispatch_helper（:1398-1438/1564-1565 存在）；`cat` executor_config.toml（drafting max_tokens=32768）；`grep` pricing.py（:52-53 raise ValueError）；ledger.py:23 `_safe_estimate_cost`；parallel_dispatch.py:36 RETRY_JITTER=2.0；`ls` pipeline/_shared.py + audit/_shared.py 存在；`grep deepseek-v4-pro` src/skills 仅归档+测试夹具
- 置信度: high

### .hypothesis/examples + patches
- 处置: audited
- 声称检查的不变量: Z7-d 确认的 17 个失败样本已修复；.gitignore 声称 "Examples ARE committed"
- findings: F1318
- 验证命令: `git ls-files .hypothesis`（仅 2 文件：.gitignore + .gitkeep）；`git check-ignore -v`（根 .gitignore:77 `.hypothesis/` 压制；43 个 example 全未跟踪）；patches 13 个共 17 个 `via('discovered failure')`；测试代码 blacklist_characters/blacklist_categories 存在（test_capability_fs_properties.py:16）
- 置信度: high（F861 复验 + 计数漂移实证）

### novel-output/test-validation/ + validation-results/
- 处置: audited
- 声称检查的不变量: 27+4 文件真实性；validation-report 声明与磁盘一致
- findings: F1319
- 验证命令: `find` test-validation（27 文件）；`cat` pipeline-state（genesis in-progress current_step=2，worldbuilding+genre-config done，LLM backend 缺失——与 report 一致）；truth 模板文件 49-105 字节
- 置信度: high

### 其余（snapshots/plans/context/world/outline/genesis-context/characters/style/foundation/novel.json/genre-config.json/DEBUG_USE_MANUAL_CREATE.md）
- 处置: audited
- 声称检查的不变量: DEBUG 文档计数 vs 磁盘；plan-decisions 提交状态；volume_map 卷边界；genre-config auditDimensions 与产物一致
- findings: F1320, F1321
- 验证命令: `python3` 逐目录计数（gate-markers 22 vs 文档 21；snapshots 51 vs 52；staging 121 vs 119；总 1229 vs 1226）；`ls` plans/*.json=0 vs staging 55；`grep volume_map` 中文卷边界（F324 生产实证）
- 置信度: high

---

## 2. Findings（编号 F1300-F1399）

### F1300 | 5 章正文被 revision 摘要整体覆写（ch2/9/12/44/55）——交付物级数据损坏 | 数据损坏（D4 产物侧） | **P0**
- 证据: `novel-output/xinghuo-ranqiong/chapters/chapter-2.md:1-2`（"All three files have been output. Here's a summary of the revision:"）；chapter-9.md（368 字节"未变更…详见上方 Input Files 章节"）；chapter-12.md（"The revision is complete. Here's a summary"）；chapter-44.md（"No-op (identical to Ch43 pattern)"）；chapter-55.md（104 字节"Chapter content unchanged — no revision needed. Use the existing chapters/chapter-55.md from input."）。全量扫描：5/56 章节命中 revision-summary 特征正则
- 根因: revision step 的 output_path 与 draft 同路径（`chapters/chapter-N.md`，T1-01 实证 files_checked 仅 .md），revision 摘要文本被当作正文整体写入；当前代码的防御（`_CONTENT_SIZE_MIN_RATIO=0.20` size guard + `_create_pre_revision_backup`，dispatch_helper.py:929/1844-1865）为 **2026-08-02 后加入**（git log d4b4e83），运行期（2026-07-15~17）无此保护
- 验证命令+输出: `python3` 56 文件扫描 → 5 个 degenerate（ch2 1035B / ch9 642B / ch12 1506B / ch44 1229B / ch55 104B）；`grep -rl "山风裹着铁锈味"` → 仅 audits/chapter-2-continuity.md（正文残留只在审计引用中）；`git show dd1fc62:.../chapter-2.md` = 1035B 摘要版（git 历史无正文版）
- 建议方向: 正文已不可恢复（ch2 无 snapshot，snapshots 从 ch5 起）——从 audits 引用只能碎片复原；修复已入代码但产物永久损坏，spec 需记录该运行数据不可逆损失；后续运行依赖 pre-rev 备份 + size guard 验证

### F1301 | 章节头契约零符合：56/56 无 `# Chapter N:` 头 | 显式契约违反 | **P1**
- 证据: chapter-file-format.md:30（"# Chapter N: [Title]"）；`python3` 正则 `^# Chapter \d+:` → 0/56 命中；实际头为标题本身（`# 废料场` ch1、`# 冷` ch31、`# Monday` ch37、ch40 `# 第40章`）
- 根因: G4/G2 均不校验章节头（G4 marker checks 无 header 项），契约文档与校验器脱钩；生成侧按标题而非契约头输出
- 验证命令+输出: `python3` `re.search(r'^# Chapter \d+:', raw, re.M)` → 0/56；抽样 head 显示实际头
- 建议方向: 契约头是纯文档声明还是应校验？若为契约则 G4 补查；否则订正 chapter-file-format 文档为"标题头即可"

### F1302 | 6 章无 META 块 + ch40 用 `## META` YAML 替代 `<!--META-BEGIN-->` | 显式契约违反 | **P1**
- 证据: chapter-file-format.md:19-45（META-BEGIN/END HTML 注释块）；ch2/9/12/40/44/55 零 `<!--META-BEGIN-->`；ch40.md:3 用 `## META` + `chapter: 40` YAML 键值（完全不同的格式）；全量 98 BEGIN/98 END 分布在 50 文件
- 根因: revision 覆写（F1300 同因）使 5 章失去 META；ch40 是"CONSTELLATION 实验风格"章节（正文为参数叙事体），用了自创 META 格式，绕过契约
- 验证命令+输出: `python3` 计数 → meta_begin=98/meta_end=98，6 文件 0 块；`head -40 ch40.md` 显示 `## META` 块
- 建议方向: ch40 格式应视为风格例外并记录（或归一化）；其余 5 章随 F1300 处理

### F1303 | DEBUG_USE_MANUAL_CREATE.md 暴露手动创建路径：decisions.json 的"Extra data"来源 | 写路径绕过（Z11-01 根因之一） | **P1**
- 证据: `DEBUG_USE_MANUAL_CREATE.md` 文件名本身（"DEBUG_USE_MANUAL_CREATE"）；chapters/chapter-14-decisions.json 尾部"实际写入文件需将以上内容保存至 `chapters/chapter-14.md` 和 `chapters/chapter-14-decisions.json`。当前会话因只读沙箱无法执行写操作，你可手动复制…"（LLM 输出提示手动落盘）
- 根因: 运行使用 IDE/手动直写路径（`modes/codex.py:61-66` workspace-write 直写；T1-03），绕过 `_validate_json_output`（其 raw_decode 恢复会截断 Extra data——未被截断证明非该函数落盘）
- 验证命令+输出: `python3` 读 chapter-14-decisions.json 尾部 → 手动复制提示原文；`read dispatch_helper.py:785-860`（_validate_json_output 恢复策略）
- 建议方向: 与 F1304 合并定级；spec 记录该运行使用 manual-create 模式的事实

### F1304 | Z11-01 根因确认：83/145 decisions.json 无效，成因=写路径绕过 + G4 从未收到 .json | 校验洞（根因级） | **P1（维持，根因已定位）**
- 证据: `gate-markers/G4-shenbi-chapter-drafting-generative.json`（`{"id":"G4.dec","s":"SKIP","r":"no files"}`，files_checked 仅 `chapters/chapter-56.md`）；`G4-shenbi-chapter-revision-generative.json`（gate=G4-decisions，checks=[], files_checked=`chapters/chapter-55.md`）；`_resolve_g4_files`（chapter_loop.py:565-584）只返回 step.output_path 单一 .md
- 根因: 双因素——(a) 门侧：G4 文件列表不含 .json → composite json 分区空 → `g4_decisions([])` SKIP PASS（T1-01 结论，磁盘 marker 实证）；(b) 写侧：invalid 文件若经 `_validate_json_output` 必被截断/拒绝（dispatch_helper.py:818-848），磁盘保留原始拼接内容证明这些文件由 agent 直写/手动落盘（F1303）；分类：extra_data 67（JSON+markdown 尾缀）、other 14（Expecting ','/value，真语法破损）、control_char 2
- 验证命令+输出: `python3` 全库扫描 → `{'extra_data': 67, 'valid': 62, 'other': 14, 'control_char': 2}`（invalid 83）；`cat` 两个 G4 marker → `G4.dec SKIP no files`
- 建议方向: 维持 P1；修复 = `_resolve_g4_files` 纳入契约 writes 全部路径 + 写路径前置 DecisionsDoc 校验（两者任一即可闭环）；产物 83 文件需重生成或标注无效

### F1305 | 57/145 可解析但 schema 违反：producer 模板零 schema 编码 | schema 违反（producer 侧） | **P1**
- 证据: 62 个可解析文件中 5 个缺必需键（chapter-30 缺 $schema/skill/produced_at/selections；chapter-6-revision 缺 selections；staging 3 个缺键）；`selections[].basis` 非枚举值（revision_routing/author_intent/continuation/audit_drift/pipeline_assembly 等，T1-02 全量实证）；`selected` 为 str 101 处；`handling:"apply"` 56 处
- 根因: 5 个声明 decisions 的 SKILL.md 输出模板零 schema 字段；dispatch_helper.py:725-726 仅一行 "must conform to schema" 提示，LLM 无文档可读（文档不在 prompt 内）→ 凭记忆生成必然漂移
- 验证命令+输出: `python3` 必需键检查 → 5 文件 MISSING 清单；`json.loads` 全量 + DecisionsDoc 字段核对
- 建议方向: 与 T1-02 合并；producer prompt 内联 schema 摘要（必填字段+枚举+P2.5 表）

### F1306 | revision-decisions 触发 34/56 但 `_ensure_revision_decisions_exists` 兜底写入的"minimal"文件仍违反 schema | 边界/死代码 | **P2**
- 证据: chapter_loop.py:1968-1984 `min_decisions` 含 `$schema/skill/chapter/selections/adjustments/produced_at` 但 **缺 `budget` 且 selections 用 `arc_relevance`+空 selected**——DecisionsDoc 若 required 含 budget 则失败；磁盘 34 个 revision-decisions 中 7 个无效（12/15/2/21/44/40/18-revision 等）
- 根因: 兜底写入本身未过 DecisionsDoc 校验（写路径直接 safe_write）；与 F1304 同链条
- 验证命令+输出: `python3` revision-decisions 单独扫描 → 34 中 7 invalid；`read chapter_loop.py:1968-1984`
- 建议方向: 兜底模板对齐 schema（含 budget），并纳入 F1304 修复后的统一校验

### F1307 | 根 `truth/`（bridge_tracker.md + character_matrix.md）与 truth-files.yaml 三源分裂 | 契约/信源断裂 | **P1**
- 证据: `truth/bridge_tracker.md`（898B 模板，全部 PENDING 空行）**不在 truth-files.yaml**（grep exit 1）但存在于 truth-files.index.json:631 与 `skills/shenbi-foreshadowing-track/SKILL.md:32`（writes）；`truth/character_matrix.md`（862B Genesis 模板）与项目 `novel-output/xinghuo-ranqiong/truth/character_matrix.md`（3711B 实际版）内容不同（md5 不同）
- 根因: 测试运行（test-validation worldbuilding 直写 root truth/，write-audit GATE_FAIL 记录"未声明写入: truth/bridge_tracker.md"）把文件落到仓库根 truth/；yaml 概念表未收录 bridge_tracker（skill 契约与 index 却声明）→ 三源不一致（F0-02 家族）
- 验证命令+输出: `grep bridge_tracker docs/framework/truth-files.yaml` → exit 1；`python3` index.json → 有 entry；`md5` 对比两个 character_matrix → False；`cat` write-audit.jsonl → GATE_FAIL 原文
- 建议方向: 根 truth/ 两文件为残留，应清理或正式登记；bridge_tracker 在 yaml 补概念或从契约删除

### F1308 | staging truth 与正式 truth 内容不一致（pending_hooks 9886 vs 4171） | 数据漂移 | **P2**
- 证据: `python3` 对比 → chapter_summaries/current_state/emotional_arcs/particle_ledger/subplot_board/character_matrix 六文件字节数相等（staging==正式），但 `pending_hooks.md` xinghuo=9886 vs staging=4171（staging 缺后半 6 条 hook 状态）
- 根因: staging 是中间态快照（F364/atexit 清理链），pending_hooks 由 foreshadowing-track 直写正式路径，staging 版本陈旧——不变量"staging 为正式前身"对 pending_hooks 不成立
- 验证命令+输出: `python3` 循环对比 → 6 等 1 不等
- 建议方向: 记录 staging 语义为"部分陈旧快照"，不作为 truth 一致性判据

### F1309 | progress.json 内容空壳：仅 scorer 字段，无任何进度 | 显式契约违反（F640 生产实证） | **P1**
- 证据: `progress.json` 全文 = `{"current_scorer_agent": "pipeline-g3-scorer-66b27075583e", "scoring_history": [{"agent": "pipeline-skill-generator", "g2_passed": true}]}`；DEBUG 文档称 progress.json 由 foundation-review 创建于 04:53 且"进度追踪"；cluster 2 F640（materialize_progress 零生产者→调用即覆盖）生产实证
- 根因: F640——progress 无真实生产者，任何 materialize 调用覆盖为空壳
- 验证命令+输出: `cat progress.json` → 上述空壳；`python3` pipeline-state 无 token_usage 字段（state.py to_dict 无该键，F332）
- 建议方向: 与 F640/F332 合并；progress.json 或删除或补真实生产者

### F1310 | pipeline 永不完成状态实证：closure=pending、closure_step=0、total_chapters 缺位 | 显式契约违反（簇 1 实证） | **P1**
- 证据: pipeline-state.json：`phase=chapter-loop`、`closure="pending"`、`closure_step=0`、`closure_skills_done=[]`、chapter_loop 无 `total_chapters`/`volume_boundaries` 键；`truth/book_spine.md:3` `total_chapters: 0`；`truth/chapter_summaries.md` 仅 2/56 章条目（ch55/ch56）
- 根因: 簇 1（F324/F353/F371/F373/F379 五独立根因）+ 簇 2（F397 append_dedup no-op → chapter_summaries 只留最后两章）——磁盘产物与已 verified 根因簇完全吻合
- 验证命令+输出: `python3` 读 pipeline-state → 上述字段；`grep total_chapters book_spine.md` → 0；`grep -c "^- " chapter_summaries.md` → 8 行但仅 2 章 H2 条目
- 建议方向: 不新增 finding，作为簇 1/2 的产物侧交叉证据引用

### F1311 | audit_reports 状态记录与磁盘 117 个审计文件脱节（resonance+review-summary 全缺） | 状态/产物不一致 | **P2**
- 证据: `python3` 交叉核对 → 磁盘 722 audits 中 117 个（resonance 55 + review-summary 55 + ch56 部分 7）从未出现在任何 chapter audit_results.audit_reports；ch1 audit_reports 仅 11 项（缺 resonance/review-summary），steps_done 含 review-resonance
- 根因: parallel wave 只把 core_tasks+genre_tasks 的 output_path 写入 audit_reports（chapter_loop.py:2621），resonance/review-summary 由独立路径产生不入列表；状态审计链（F338/F339 家族）记录不完整
- 验证命令+输出: `python3` 集合差 → `unclaimed: 117`；ch1 audit_reports 打印 11 项
- 建议方向: audit_reports 语义补 resonance/review-summary 或明确定义"仅记录 wave 内审计"

### F1312 | 双 resonance gate-marker：`G4-review-resonance-generative.json` 为验证运行写入的污染 marker | 报告冗余/污染 | **P2**
- 证据: `gate-markers/G4-review-resonance-generative.json`（2026-07-19T15:01:49，gate=G4-generic-gen，files_checked=`audits/chapter-1-resonance.md`）vs `G4-shenbi-review-resonance-generative.json`（2026-07-17T12:58:51，gate=G4-review-resonance，files_checked=`audits/chapter-55-resonance.md`）——diff 显示 gate 名/时间/文件全不同；前者时间戳 = validation-report "executed 2026-07-19T15:01 UTC"（test-validation stage4 用 xinghuo 数据替代验证）→ 验证运行把 marker 写进了生产项目 gate-markers/
- 根因: validation 运行以 xinghuo-ranqiong 为 substitute project_dir 执行 G4，marker 落盘到生产项目目录（F513 根错位家族）
- 验证命令+输出: `diff` 两 marker → 6 处不同；`python3` 读验证报告时间戳对齐
- 建议方向: 清理污染 marker；验证运行应使用独立输出目录

### F1313 | token ledger 缺席：cost/token-ledger.jsonl 不存在，F302 死接线预测验证 | 数据缺失（F302 实证） | **P1**
- 证据: `find` 全库（排除 node_modules/.git）→ 无 token-ledger.jsonl、无 cost/ 目录；pipeline-state 无 token_usage；运行期（07-15~17）早于 2026-08-02 接线修复（dispatch_helper.py:1333 TokenLedger.record 已接、d4b4e83）
- 根因: F302——运行期 TokenLedger.record 从未被调用，40 小时运行零成本记录；修复后接线存在但本运行无数据
- 验证命令+输出: `find . -name "token-ledger.jsonl"` → 空；`read dispatch_helper.py:1300-1345` → record 已接
- 建议方向: 维持 F302；本运行成本数据不可追溯，后续运行需 ledger 落盘验证

### F1314 | audits 722 无内容重复，但 texture 维度配置=true 而磁盘 0 文件 + sensitivity 双发（F329 实证） | 边界/优化 | **P2**
- 证据: `python3` md5 → 0 组重复；维度计数（anti-ai 56/character 56/continuity 56/dialogue 55/foreshadowing 56/memo-compliance 56/motivation 55/pacing 56/pov 56/resonance 55/review-summary 55/sensitivity 55/world-rules 55，ch56 缺 6 维）；`genre-config.json auditDimensions.texture=true` + `get_active_genre_audits` 返回含 shenbi-review-texture → 磁盘 0 个 texture 文件；`config-change-log.jsonl` 记录 texture 重启用（07-19，晚于运行结束）
- 根因: (a) texture 审计在运行期未生效/被 cascade 跳过（CASCADABLE_AUDITS 含 texture，3 章连胜即 skip）→ 配置与产物脱节；(b) sensitivity 属 GENRE_ACTIVATION_MATRIX + core 波双源 → F329/F387 双发，磁盘每章仅 1 文件（覆盖写）
- 验证命令+输出: `PYTHONPATH=src python3` get_active_genre_audits → 5 项含 texture；`ls audits/*texture*` → 0；md5 扫描 → 0 dups
- 建议方向: texture 配置与产物对齐（要么运行要么从配置移除）；F329/F387 双发修复已 specced

### F1315 | ch56 审计不完整：6/13 维缺失（dialogue/motivation/resonance/review-summary/sensitivity/world-rules）+ ch56 无 audit_reports 记录 | 边界 | **P2**
- 证据: `ls audits/chapter-56-*.md` → 仅 7 文件（anti-ai/character/continuity/foreshadowing/memo-compliance/pacing/pov）；pipeline-state ch56 audit_results=None、steps_done 仅 9 步（停在 drafting+state-settling 后）
- 根因: 运行在 ch56 审计波前中断（簇 1 永不完成 + DEBUG 文档"生成中断"）
- 验证命令+输出: `ls | xargs basename` → 7 文件；`python3` ch56 steps_done → 9 项
- 建议方向: 记录为中断态，不新增修复项

### F1316 | config-change-log.jsonl 单条无操作条目（old=true/new=true）且时间戳晚于运行结束 | 日志异常 | **P2**
- 证据: `cat config-change-log.jsonl` → `{"timestamp": "2026-07-19T05:48:59+00:00", "key": "auditDimensions.texture", "old": true, "new": true, "rationale": "Re-enabled texture audit..."}`——old==new==true 为无操作变更；时间戳（07-19）晚于最后一个 G4 marker（07-17T13:32）与 DEBUG 文档运行区间（07-15~17）
- 根因: 疑似 post-hoc 补录（验证/人工阶段）或 config_coherence.py 记录逻辑未过滤等值变更；无操作变更仍写 audit-trail（F614 家族幻影条目）
- 验证命令+输出: `cat` → 上述单条；`read config_coherence.py:80-95`（append 逻辑无等值过滤）
- 建议方向: 等值变更跳过记录；补录条目标注来源

### F1317 | write-audit/trace.jsonl 记录与 GATE_FAIL 语义一致但 root truth 残留仍落盘 | 边界 | **P2**
- 证据: `write-audit.jsonl` 3×GATE_FAIL（worldbuilding 未声明写入 truth/bridge_tracker.md + character_matrix.md）+ 1×AUDIT_PASS（escalation-review）；trace.jsonl 4 条对齐（seq/action/GATE_FAIL/AUDIT_PASS）；但根 truth/ 两文件仍存在（GATE_FAIL 未删除已写文件）
- 根因: dispatch_with_write_audit（executor.py:228-290）返回 rc=2 阻止推进但**不回滚已落盘文件**（F507 deleted 零拦截家族）；且 worldbuilding 运行期把文件写到 root truth/（F513 根错位）
- 验证命令+输出: `python3` 读 write-audit/trace → 4/4 对齐；`ls truth/` → 2 文件存在
- 建议方向: 与 F1307/F1312 同源，统一清理 + 审计回滚策略

### F1318 | .hypothesis "Examples ARE committed" 声明失效：43 个 example 全未跟踪（F861 复验+计数漂移） | 契约违反（声明 vs 现实） | **M**
- 证据: `.hypothesis/.gitignore` 声明 "Examples ARE committed — they capture discovered counterexamples"；但 `git ls-files .hypothesis/` 仅 2 文件（.gitignore + .gitkeep）；`git check-ignore -v` → 根 `.gitignore:77: .hypothesis/` 压制（否定规则 `!examples/` 不生效，因父目录已被忽略）；磁盘 43 个非 gitkeep example 文件（Z7-d 记录 11 个 0 字节——现 0 个 0 字节，43 个 1-71 字节，计数漂移）
- 根因: 根 .gitignore 直接忽略整个 .hypothesis/，子 .gitignore 的 negation 无法复活；且 example 文件被后续运行改写（计数从 11→43）
- 验证命令+输出: `git ls-files .hypothesis` → 2；`git check-ignore -v <file>` → `.gitignore:77: .hypothesis/`；`find .hypothesis/examples -type f ! -name .gitkeep | wc -l` → 43
- 建议方向: 与 F861 合并维持 M；修正 .gitignore 结构（根 ignore 去掉 .hypothesis/ 或移 negation 到根）或如实声明"examples 不提交"

### F1319 | test-validation 产物真实性确认：27 文件与 BLOCKED 状态自洽 | 无（真实性核验通过） | —
- 证据: `pipeline-state.json`（genesis in-progress current_step=2，worldbuilding+genre-config done，retry_feedback 显示 G4 FAIL 详情）；truth 模板文件 49-105 字节（空模板）；validation-results stage2 "BLOCKED — no LLM backend available" 与 `SHENLL_LLM_API_KEY unset` 一致；stage4 用 xinghuo-ranqiong substitute（解释 F1312 污染 marker 来源）
- 验证命令+输出: `find` → 27 文件；`cat` pipeline-state/validation-report 对照
- 置信度: high

### F1320 | DEBUG_USE_MANUAL_CREATE.md 计数漂移（1226 vs 1229；marker 21 vs 22；snapshots 52 vs 51） | 文案 | **M**
- 证据: `python3` 全树计数 → 1229（文档称 1226）；gate-markers 22（文档称 21，差额=F1312 污染 marker）；snapshots/*.md 51（文档称 52，manifest 51 章 5-55）；staging 121（文档称 119，因含 2 个 lockfile/多余文件）
- 根因: 文档为人工/运行中手写快照，未随产物同步；本身是 DEBUG 用途非契约
- 验证命令+输出: `python3 os.walk` 计数 vs 文档表格
- 建议方向: 文档标注"仅供参考"或归档；不作为断言依据

### F1321 | plan-decisions 全部滞留 staging（55 个），plans/ 零 decisions；ch54 缺 plan-decisions | 契约不一致 + 产物滞留（T1-05 实证） | **P2**
- 证据: `ls plans/*.json` → 0；`ls staging/plans/*-decisions.json` → 55（ch54 缺失，56 章中 1 章无 plan-decisions）；T1-05 已 verified（commit_staging 只提交契约路径/仅 *.md，chapter_loop.py:1078-1094）
- 根因: staging commit 过滤契约外文件（decisions 未声明 producer 契约 → 永不提交）
- 验证命令+输出: `ls` 两目录计数；`python3` staging decisions 章号集合 → 缺 54
- 建议方向: 与 T1-05 合并；plan-decisions 若需保留则补契约 producer + commit 路径

---

## 3. 低置信度项

- **F1316（config-change-log 无操作条目）**：单条日志无法确证是"补录"还是"运行期等值写入"；置信度 medium（等值写入的可能性来自 config_coherence.py 无过滤，post-hoc 补录的可能性来自时间戳晚于运行结束 2 天——两者均无法排除）
- **F1314 texture 未产出原因**：配置=true 但磁盘 0 文件，根因可能是 cascade-skip（CASCADABLE_AUDITS 含 texture，3 章连胜后 skip）或运行期配置未生效；未追踪 dispatch 日志，无法区分——置信度 medium（产物面事实 high，成因归因 medium）

---

## 4. 严重度汇总

| 严重度 | 数量 | 编号 |
|--------|------|------|
| P0 | 1 | F1300（5 章正文不可逆覆写） |
| P1 | 9 | F1301, F1302, F1303, F1304, F1305, F1307, F1309, F1310, F1313 |
| P2 | 9 | F1306, F1308, F1311, F1312, F1314, F1315, F1316, F1317, F1321 |
| M | 2 | F1318, F1320 |
| 合计 | 21 | 含 Z11-01 更新（F1304）与交叉实证（F1310/F1313） |

> 注：F1309/F1310/F1313 为已 specced/verified findings（F640/F324/F353/F397/F302）的产物侧实证，不重复开新 spec，仅供合并引用。

## 5. 未覆盖项

无（12/12 类别全部 audited，1281 文件清单缺失 0）。

## 6. Z11-01 处置结论

Z11-01（83/145 decisions.json 无效）根因已定位为**双因素**：(a) **门侧**——pipeline 模式 G4 文件列表不含 .json（`_resolve_g4_files` 只返回 step.output_path），`G4.dec` 恒 `SKIP "no files"` → PASS（生产 marker 实证，T1-01 成立）；(b) **写侧**——磁盘保留的"Extra data"完整拼接内容证明这些文件**未经过** `_validate_json_output`（其 raw_decode 恢复会截断/拒绝），由 IDE/手动直写路径落盘（DEBUG_USE_MANUAL_CREATE + codex workspace-write）。两者任一修复即可闭环；P1 维持。
