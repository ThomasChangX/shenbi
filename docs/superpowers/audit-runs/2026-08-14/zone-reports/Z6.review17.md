# Z6 分区第十七轮独立复核报告（fresh-context 全量重读）

> 复核人：第十七轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11（F601-F611）+ 一轮 8（F612-F619）+ 二轮 10（F620-F629）+ 三轮 7（F630-F636）+ 四轮 3（F637-F639）+ 五轮 3（F640-F642）+ 六轮 1（F643）+ 七轮 3（F644-F646）+ 八轮 2（F647-F648）+ 九轮 5（F649-F653）+ 十轮 2（F654-F655）+ 十一轮 3（F656-F658）+ 十二轮 2（F659-F660）+ 十三轮 2（F661-F662）+ 十四轮 1（F663）+ 十五轮 2（F664-F665）+ 十六轮 1（F666）逐条独立验证；新发现编号 F667 起。
> 方式：49/49 文件全文 read（含全部 `__init__.py`/`__main__.py` 薄文件）+ `PYTHONPATH=src python3` 实际运行复现（F601/F641/F642/F648/F652/F655/F656/F663/F664/F666 及全部新发现本轮回放）+ 全仓 grep 调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对（pending_hooks.md 143 行结构、resonance_trend.md 单表单行、genre-config.json 10 维、chapters/chapter-1/45/46.md、56 章 compute_all_stats/句数统计、skills/shenbi-chapter-pattern/SKILL.md 词表契约）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未完全达成（新发现 2 条，均 P2、潜伏/低影响；误报 0；无 P0/P1 新 finding）**。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §4）
- 初审 11 findings：**11/11 成立**（F601 严重度软异议支持前轮）
- 一轮 8：**8/8 成立**；二轮 10：**10/10 成立**；三轮 7：**7/7 成立**；四轮 3：**3/3 成立**
- 五轮 3：**3/3 成立**（F642 证据裁决本轮回放一致：raw 0.6267 / 生产顺序剥离 0.6367 / 交换顺序 0.57 / autojunk=False 0.77）
- 六轮 1：**1/1 成立**；七轮 3：**3/3 成立**；八轮 2：**2/2 成立**；九轮 5：**5/5 成立**；十轮 2：**2/2 成立**
- 十一轮 3：**3/3 成立**；十二轮 2：**2/2 成立**；十三轮 2：**2/2 成立**；十四轮 1：**1/1 成立**；十五轮 2：**2/2 成立**；十六轮 1：**1/1 成立**
- 新发现：**2**（F667-F668，均 P2、潜伏/低影响）
- 整条误报：**0**；严重度异议：**0 条新增**（支持既有软异议 2 条：F601 P1↔P2 可辩、F646 impact P2↔P1 可辩，均不修改定级）
- 判定：**非 0 新 finding（2 条，均 P2）**——按 review10-16 提议的"无 P0/P1 新 finding + 无整条误报"判据本轮达成收敛；按字面"0 新 finding"判据未完全收敛（本轮 2 条均为前十六轮未触达的**词表外盲区**与**引号劈句口径**，其中 F667 因 skill 文档显式规定"未分类"回退标签而具生产相关性）

---

## 1. F601-F666 判定表（逐条独立复核）

> 判定依据：49/49 文件全文精读 + 直接运行复现 + 全仓 grep + 真实数据核对 + 消费链（chapter_loop / write_audit / executor / g0 / audit_layer）交叉验证。前 16 轮详细证据链全部复核认可；表中仅列本轮独立验证要点与结论。**无整条误报。**

### 1.1 六条 P1 的反证核查（本轮以运行 + 代码精读 + 全仓 grep 三重独立复核，**均无反证**）

| 编号 | 标题 | 定级 | 本轮独立验证 | 判定 |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | 代码精读 linguistic_drift.py:211-218：`dialogue_ratio < 0.2` → `max(…,5.0)=5.0` 不满足 `:218` 严格 `>5.0` → 纯对话塌陷 is_drift 恒 False；docstring :185-188 承诺 "<20% of baseline" 触发 → off-by-one 成立。**生产影响全潜伏**：`_check_linguistic_drift`（chapter_loop.py:2014-2019）因 F602 无 baseline 于 :2017-2019 提前 return（本轮代码精读确认 baseline 检查在 compute_linguistic_metrics 之前）。**支持 review6-16 软异议：P1（显式契约静默失效）与 P2（死路径）均可辩护**，不构成误报。 | **成立**（软异议） |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py:6,28` re-export，生产零调用；`find . -name linguistic_baseline.json` 0 命中（style/ 仅 style_profile.md、context/ 无）。计划 07-19-07 Task 5a "Wire this into …" 显式接线指令未履行。 | **成立**（P1 维持） |
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | 本轮代码精读 chapter_loop.py:2023-2043：`if result.is_drift:` 包裹 log + ESCALATE raise（:2031-2038）+ HARD/WARN 注入全部干预；opening-similarity 检查 :2047-2056 在门**外**（与 F642 证据一致）。运行：base stm=60/cur=110 → `is_drift: False, severity: ESCALATE`。计划 07-19-07:749 绝对阈值契约。 | **成立**（P1 维持） |
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | 本轮代码精读 chapter_loop.py:2719-2727：:2719 注释自称 "non-blocking"，`try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed")`——函数内 :2031-2038 raise DriftEscalationError（"Pipeline paused for human review"）被调用点吞掉，`except Exception` 连带吞掉其他真实错误。grep `_check_linguistic_drift` 唯一生产调用点即 :2722。 | **成立**（P1 维持） |
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转 | P1 | 本轮 `grep -n "^#"` 全清单核对：真实文件标题仅 `# 伏笔追踪`/`## 第56章伏笔呈现`/`## 第56章生命周期状态更新` 等章节附录，**无 `## hooks`/`## 活跃伏笔`**；ch-025 快照有 `## 活跃伏笔`(:12)/`## hooks`(:20) → 格式分叉成立。消费链 write_audit.py:49-56 → record.py:24 block-ship 已接线但 drift 恒空。P1（"测试失效掩盖真实缺陷"档）。 | **成立**（P1 维持） |
| F640 | materialize_progress 输入契约零生产者 → 无条件覆盖真实 progress.json | P1 | 本轮 grep：`"INIT"`/`"MARK_DONE"` src/ 仅 materialize.py:49,53（消费方）+ 测试命中；生产 TraceWriter.append 调用点 = audit/record.py:44 + safe_write.py:132（trace_action 生产唯一传参处即 materialize.py:98 自身）+ migrate.py:30（migrate_from_progress 自身零调用）。`_maybe_materialize_progress`（chapter_loop.py:684-699）：steps_done%5==0（含 0，:692）+ `except Exception: pass`（:698-699）；`_auto_rebuild_progress_if_stale` 检查 `trace/` **目录**（:713）而 TraceWriter 写 `trace.jsonl` **文件**（writer.py:42）→ 路径错位自愈永不触发。真实 progress.json 为 scoring schema → 触发即数据丢失（激活应升级 P0）。 | **成立**（P1 维持） |

### 1.2 F603-F611（初审其余）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F603 | records drift 只检 md→YAML 方向 | P2 | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无永不进循环。 | 成立 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复字符 + :226 按字符迭代；chapter-1 引号 count=72（36 个 `"`×2 吻合）。 | 成立 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | `"|".join([])==""` → `re.findall("", text)` 全位置匹配；`system_terms: null`/`drift_detection: null` 两变体同根（linguistic_drift.py:79-84 无类型守卫）；真实 config 无 drift_detection 键 → bootstrap 回退。review1 影响修正（被 is_drift 门控、无管道暂停）复核同意。 | 成立 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过。review13 补充（bool 是 int 子类、被拦）复核正确。 | 成立 |
| F607 | 双 baseline 路径 + 术语分裂 | P2 | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs `dialogue_density`；:296 硬编码术语 vs :107 config 驱动；`_load_baseline`/`check_linguistic_drift` 生产零调用（死路径）。 | 成立 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | cjk.py:54 桶 token 为相邻双字符 `""`/`''`/`「」`/`『』`，真实文本引号包围内容不相邻 → count 恒 0（该函数无生产调用方，见 F659，影响面潜伏）。 | 成立 |
| F609 | replay 静默截断无日志 | P2 | replay.py 全文无 logging/structlog import；:40-43 两处 break 无日志、:47-48 safe_write 改写文件。 | 成立 |
| F610 | recall 缺 id KeyError | P2 | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无。F622 为独立类型失败模式。 | 成立 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | config_coherence.py:109 `key.startswith("auditDimensions.")` 只拦带点形式；整键对象覆盖绕过。F631（falsy）/F638（snake_case）/F643（键缺失）/F666（整键标量 falsy）为独立向量。 | 成立 |

### 1.3 F612-F619（一轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | 见 §1.1 | 成立 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | compute_stats.py:27 重复字符 + :226 按字符迭代 → 一个 `——` 计 4；chapter-1 破折号 100 vs cjk 25（4× 吻合）。 | 成立 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | config_coherence.py:105-133 循环内逐 key `_set_nested`+`_append_audit_trail`，safe_write 在循环外 :142 → 中途 ConfigError 时 trail 已追加、config 未落盘。生产零调用（F635）→ 实际影响面为零，不改变本体。 | 成立 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | writer.py:57 `json.loads(lines[-1])` 无 try；replay.py:38-43 同文件自愈 → 不对称。三个生产调用点均有外部缓解（safe_write.py:126-140 / audit/record.py:42-46 / migrate 先 replay）。 | 成立 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤。本轮运行复现：含 2 条标量脏数据 → 仅 1 条 dict 返回。 | 成立 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | 本轮 grep：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。 | 成立 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | grep `RHETORICAL` → 仅 compute_stats.py:37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex。 | 成立 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。 | 成立 |

### 1.4 F620-F629（二轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError | P1 | 见 §1.1 | 成立 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目；:170 main 默认 "未分类"。本轮运行：10 个词表外 pattern → 熵 0.0 "严重单调" vs 10 个已知 pattern → 3.3219 "优秀"。**同族补充（非新 finding）**：词表外 pattern 在连续运行/转移矩阵中同样被静默丢弃（见新 F667）。 | 成立 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | recall.py:41-46 `.get()` 只判 None 不判类型 → `int - str`/`int > str` TypeError。 | 成立 |
| F623 | AI_MARKERS "不是…而是" 单省略号恒不命中 | P2 | compute_stats.py:52 单 U+2026 vs 双 `……`（与 :23 PUNCT_MAP 一致）；56 章全量 37 次命中均位于 META/指令文本（review15 nuance 复核）。 | 成立 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无校验 | P2 | 本轮规范化重算：master 59、skills/ 目录 74、缺失恰好 15 个（名单与前九轮完全一致）、反向 0。generate.py:24-34/47-53 无 skills 完整性校验。 | 成立 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、无生产调用方）。 | 成立 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | 本轮 grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export，src 内无生产调用；review12 补充（`return is_drift and severity in ("HARD","ESCALATE")` 继承 F612 门控病）复核正确。 | 成立 |
| F627 | parse_trend 无表结束条件 | P2 | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件；对比 records/drift.py:34-39。真实 resonance_trend.md 单表 → 潜伏。 | 成立 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | compute_stats.py:16 SENT_ENDS 含 `；`，:63 只按 `。！？\n` 切句，:87 用 SENT_ENDS。**同族补充（非新 finding）**：引号内句末标点劈句见新 F668。 | 成立 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | 本轮 grep：`verify_preservation` 仅 preserve_check.py:27 定义，src/ 零调用。 | 成立 |

### 1.5 F630-F636（三轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移 | P2 | revision_router.py:62-64（注释明示 "config.resonance_global_floor" 后跟 `= 50`）vs thresholds.py:30 `=65`；chapter_loop.py:1897 显式传值覆盖默认 → 潜伏。 | 成立 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值绕过且审计实际禁用 | P2 | config_coherence.py:109、g0_config_coherence.py:92 均 `is False`；audit_layer.py:113 `audit_dims.get(dim_key, False)` 任何 falsy 即不激活。review12 补充（`[]` 亦 falsy）复核正确。 | 成立 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反单源声明 | P2 | linguistic_drift.py:222-226 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50；thresholds.py:5-10 docstring 自称单源。 | 成立 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | compute_stats.py:79 docstring vs :80 `re.split(r"\n\s*\n")`；`\n` ∈ SENT_ENDS :16。 | 成立 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | 真实 chapter-1/45/46.md 均以 `<!--META-BEGIN-->` 开头（chapter-1 有 2 块共 858/4177 字符）；两处统计入口（linguistic_drift.py:94-131、compute_stats.py:311-362）无剥离。review12 数字勘误（剥离方向随方法而变）复核正确，不影响本体。 | 成立 |
| F635 | update_genre_config 治理层零生产调用 | P2 | 本轮 grep：`update_genre_config` 仅 config_coherence.py 自身（+测试+计划文档）；真实 config-change-log.jsonl 仅 1 条 2026-07-19 记录；skills/shenbi-genre-config/SKILL.md 直写文件。 | 成立 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | linguistic_drift.py:202 `(6.0 if curr_val > 0 else 1.0)`；本轮运行：base 全 0 + cur em_dash=0.33 → `is_drift: True, severity: WARN`。 | 成立 |

### 1.6 F637-F639（四轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转 | P1 | 见 §1.1 | 成立 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过） | P2 | audit_layer.py:105-107 snake_case 回退 vs config_coherence.py:109/g0_config_coherence.py:88 只认 camelCase。真实配置 camelCase → 潜伏。 | 成立 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 半边不运行 + audit 追加无协调 | P2 | 真实 truth/ 仅 resonance_trend.md（单表单行 ch55，含 human_overridden 列），无 arc_payoff_trend.md/volume_score_trend.md；audit_drift.md 为 `## Ch55` 章节式（review-resonance 写），CLI `_append_audit` 追加 `## drift findings` 段与之无协调。 | 成立 |

### 1.7 F640-F642（五轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F640 | materialize_progress 输入契约零生产者 | P1 | 见 §1.1 | 成立 |
| F641 | records `_values_equal` 布尔比较孔 | P2 | 本轮运行：`_values_equal(True, 'true')` → False；`detect_cross_section_drift` 布尔字段 → 假 drift 1 条。**同根补充（非新 finding）**：`_values_equal(None, 'None')` → True（缺省键 vs 字面 "None" 单元格静默视为一致）——与 F655 的空串假 drift 恰成反向不对称（字面 "None" 掩盖缺省），修复应一并规范"无值"表示。 | 成立 |
| F642 | check_opening_similarity 比较 META 块非正文开头 | P2 | 本轮独立重放（同法剥离 `re.sub(r'<!--META-BEGIN-->.*?<!--META-END-->', '', t, flags=re.S)`）：raw [:300] (46,45)=**0.6267**；剥离后生产顺序 (46,45)=**0.6367** > 0.6；交换顺序 (45,46)=**0.57**；autojunk=False=**0.77**——0.57 与 0.637 **均可复现**（SequenceMatcher 默认 autojunk 参数顺序不对称，F649）。**review9-16 终裁复核成立、review8 裁定不成立**：按生产调用顺序（chapter_loop.py:2049 current 在前）剥离后仍 0.6367 > 0.6 → 剥离 META 不足以消除假阳性。 | 成立 |

### 1.8 F643（六轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F643 | 治理链"键缺失"默认语义矛盾 | P2 | g0_config_coherence.py:92 `audit_dims.get(dim, True)`（缺失=启用）vs audit_layer.py:113 `get(dim_key, False)`（缺失=禁用）vs config_coherence.py:109 只认显式 False。整键标量 falsy 的 G0 崩溃为独立向量（F666）。 | 成立 |

### 1.9 F644-F646（七轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F644 | compute_ngrams 未剔除标点 → 指纹被标点主导 | P2 | compute_stats.py:209 只滤空白；56 章全量 bigrams top1 = `('——', 26371)`、chapter-1 top = `('。他', 27)`。 | 成立 |
| F645 | compute_ttr 排除串缺 `“”`/`"`/`‘’` 引号 | P2 | compute_stats.py:171 排除串只含两个 ASCII 直单引号 `''`；真实 56 章 ttr total_chars=424014（含引号字符）。 | 成立 |
| F646 | AUDIT_SAFETY_MATRIX 6 维 vs 真实 config 10 维漂移 | P2 | 本轮实测：真实 genre-config.json auditDimensions 10 维全 True，`real - matrix = ['foreshadowing','motivation','sensitivity','worldRules']`、`matrix - real = []`。sensitivity 激活链核实为活（audit_layer GENRE_ACTIVATION_MATRIX → get_active_genre_audits → sensitivity_blocking → escalation）。**支持 review11-16 的 impact 软异议方向，维持 P2**（真实配置全 True 潜伏 + "absent → non-critical" 是 thresholds.py:48-49 显式声明设计；若协调者认定"BLOCKING 级安全网可无治理禁用"违反显式契约可辩 P1，不修改定级）。 | 成立（软异议） |

### 1.10 F647-F648（八轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F647 | records by_id 重复/缺失 id 静默塌缩 | P2 | drift.py:80 `{str(r.get("id")): r for r in yaml_records}` 后者覆盖前者；缺失 id → `str(None)="None"` 塌缩。本轮运行复现漏报/误报两方向。 | 成立 |
| F648 | compute_percentiles n=2 百分位倒挂 | P2 | 本轮运行：`compute_percentiles([1,2])` → `{'P25': 1, 'P50': 2, 'P75': 1, 'P95': 1}`（P75=P95 < P50 数学倒挂）。compute_stats.py:99-104。 | 成立 |

### 1.11 F649-F653（九轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F649 | SequenceMatcher 参数顺序不对称（autojunk） | P2 | 本轮独立重放：剥离后 (45,46) 生产顺序 0.6367 vs 交换顺序 0.57、autojunk=False 0.77；56 章相邻对 34/55 \|diff\|>0.01。linguistic_drift.py:249-253/256-268。 | 成立 |
| F650 | TraceWriter 信任尾部签名不校验 | P2 | writer.py:51-57 `_last_sig_existing` 只 `json.loads(lines[-1]).get("signature", GENESIS_PREV)` 不校验链一致；append 以坏签名为 prev；replay.py:42-43 从坏行截断 → 新合法事件被连带删除。与 F615 独立失败模式。 | 成立 |
| F651 | parser yaml.safe_load 重复键静默 last-wins | P2 | parser.py:37-45；PyYAML SafeLoader 默认映射重复键 last-wins 无异常（本轮运行复现）。 | 成立 |
| F652 | detect_rhetoric「反复」按子短语分解计数爆炸 | P2 | 本轮运行：`detect_rhetoric('冷在墙体第五层。'*3)` → `{'反复': 12}`（7 字短语×3 → 5×3-gram+4×4-gram+3×5-gram）；compute_stats.py:266-280。 | 成立 |
| F653 | parse_markdown_table 空行不终止表 | P2 | drift.py:34-39 `if not s: continue` 空行不 break；本轮运行复现第二张 `\|` 表并入。 | 成立 |

### 1.12 F654-F655（十轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F654 | `_try_float` 非有限值透传 | P2 | compute_drift.py:217-222 docstring 承诺"非有限值返回 None"但实现仅捕获 TypeError/ValueError；`float("nan")/("inf")/("1e999")` 透传（本轮运行复现 `nan inf inf 3.14 None`）→ 趋势序列静默污染。 | 成立 |
| F655 | records drift 缺省值比较孔（None vs '' 恒不等） | P2 | 本轮运行：`detect_cross_section_drift([{'id':'H01','type':'主线'}], {'H01':{...'subtlety':''}})` → 假 drift 1 条。drift.py:90-91 + :57-69。 | 成立 |

### 1.13 F656-F658（十一轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F656 | detect_rhetoric「排比」`[:20]` 截断恒判平行 | P2 | 本轮运行：chapter-1 排比=53（其中 40 为截断伪影）。compute_stats.py:256。 | 成立 |
| F657 | compute_drift 卷级检测丢弃 human_overridden 排除 | P2 | compute_drift.py:268-270 章节级排除；:275-278 卷级 `[score for score, _ in overall_series]` 丢弃标志。本轮运行复现被覆盖的 79 仍触发 VOLUME_DECLINE。 | 成立 |
| F658 | parse_markdown_table 重复 id 行 last-wins | P2 | drift.py:51-53 `if rid: out[rid] = row` 后者覆盖前者无日志（本轮运行复现首行消失）。 | 成立 |

### 1.14 F659-F660（十二轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F659 | compute_stats/cjk 双标点统计实现口径分叉 | P2 | compute_stats.py:17-28 PUNCT_MAP 与 cjk.py:44-55 PUNCTUATION_TOKENS 桶成员定义不同（ASCII 变体、`──`/`。。。`、引号 per-char vs adjacent-pair）；真实 chapter-1 同文本计数系统性不同（感叹号 0 vs 4、破折号 100 vs 25、引号 72 vs 3）。 | 成立 |
| F660 | parse_markdown_table 无表头表静默整表丢行 | P2 | drift.py:41-43 首 `\|` 行无条件当 header、:44-45 分隔行检查在其后 → 无表头表数据行无 id 键全部静默丢弃（本轮运行复现 `{}`）。 | 成立 |

### 1.15 F661-F662（十三轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F661 | `_short_chain_chars` 正则无左锚 | P2 | linguistic_drift.py:88-91 正则 `(?:[^。！？\n]{1,15}[。！？\n]){3,}` 无左锚 → 长句尾部入链/短句链打断丢失。本轮运行复现：长句+短句链文本 → 28（应 ~13）。 | 成立 |
| F662 | materialize `_as_float` 对 str 分数静默置 0.0 | P2 | materialize.py:18-23 `_as_int` 接受 str、`_as_float` 仅 int/float；本轮运行复现 `_as_int('88',0)=88`、`_as_float('88',0.0)=0.0`。F640 零生产者 → 潜伏。 | 成立 |

### 1.16 F663（十四轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F663 | compute_ttr 空输入早退分支缺 content_ttr/total_chars 键 | P2 | 本轮运行：`compute_ttr('')` → 3 键（缺 content_ttr/total_chars）；正常路径 5 键。compute_stats.py:172-173,196-202。 | 成立 |

### 1.17 F664-F665（十五轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F664 | 审计链对 yaml.YAMLError 无防护 | P2 | 本轮运行：`parse_records('## hooks\n- id: H01\n  state: "unclosed\n')` → ScannerError，且 `isinstance(e, ValueError)` = **False**（record.py:42-46 的 catch 模式兜不住）；write_audit.py:49,54 + snapshot.py:110 + executor.py:263-266 finally 块均无防护。 | 成立 |
| F665 | drift-guidance §11.8 卷级目标触发器在 compute_drift 未实现 | P2 | 本轮核实：SKILL.md:137-139 承诺"读取 truth/volume_score_trend.md，drift_detection 增加'卷级目标未达成'触发器"；`grep objective_achieved src/` → 0 命中；DriftKind 仅三值；真实项目无 volume_score_trend.md。 | 成立 |

### 1.18 F666（十六轮）判定

| 编号 | 标题 | 定级 | 本轮要点 | 判定 |
|---|---|---|---|---|
| F666 | 整键 auditDimensions 标量 falsy → Rule 1 绕过 + G0 AttributeError 崩溃 + 全 genre 审计停用 | P2 | 本轮代码精读：config_coherence.py:109 只拦带点键；g0_config_coherence.py:90-92 `audit_dims = config.get("auditDimensions", {})` 后直接 `.get(dim, True)`（标量 False/0/None → AttributeError）；**g0.py:658-668 except 子句仅 `(FileNotFoundError, json.JSONDecodeError, OSError)` 不兜 AttributeError**（本轮核实）；audit_layer.py:109-110 `if not isinstance(audit_dims, dict): return []` 全部 genre 审计停用。 | 成立 |

**判定汇总：F601-F666 共 66 条全部成立，无整条误报。** 六条 P1（F601/F602/F612/F620/F637/F640）本轮均以直接运行/代码精读/全仓 grep/真实数据/计划原文多重复核，**无反证**（F601 支持 review6-16 软异议 P1↔P2 可辩，不修改）。F642 证据争议按 review9-16 终裁复核成立（review8 裁定不成立）。P2 均无高判或低判。

---

## 2. 新发现（F667-F668）

### F667 | compute_pattern 词表外 pattern（含 skill 文档化回退标签"未分类"）在连续运行检测、最大连续、转移矩阵中被静默丢弃 → 单调性报警对词表外主模式完全失明（F621 熵失真的同族、独立向量） | 统计口径 | P2（潜伏）

- 证据：
  - `src/shenbi/skill_utils/chapter_pattern/compute_pattern.py:57-76`（`compute_consecutive`：内部 `runs` 含全部 pattern（含词表外），但 `result` 只输出 `PATTERNS` 键——词表外 run 被静默丢弃）；`:129-142`（`check_consecutive_warnings` 只遍历 `result` 的 PATTERNS 键 → 词表外连续 run 永不产生 warning）；`:145-158`（`compute_transition_matrix` 只建 PATTERNS 行/列 → 词表外转移全零）；`:170`（`main` 对缺 pattern 键默认 `"未分类"`——**该默认值本身不在 PATTERNS 列表**）；
  - skill 契约：`skills/shenbi-chapter-pattern/SKILL.md:54`（"任何章节必须能归入至少 1 种模式；**不可识别的标注'未分类'**"——词表外标签是文档化的合法生产值）；`:55`（"**单调性阈值硬性** — 连续 N 章同模式 ≥ genre-config 中定义必须报警"——工具核心承诺）；`:43`（compute_pattern.py 是 skill 的确定性计算环节）；
  - 对照已覆盖：F621 只覆盖**熵**的分母失真与 distribution ratio 之和不等于 1；**连续运行 warning 盲区与转移矩阵丢弃从未被任何轮次覆盖**（grep 前十六轮报告：连续/最大连续/转移矩阵相关 0 命中）。
- 根因：词表外 pattern（LLM 输出的非 13 模式标签，或 `main()` 自己的 "未分类" 回退）在 `compute_consecutive` 内部被统计（`runs`）却在输出层被过滤——报警链路的**输入端**有数据、**输出端**无通道；`check_consecutive_warnings`/`max_consecutive`/`compute_transition_matrix` 三个消费者全部只见 PATTERNS 子集。结果：连续 10 章同为词表外模式（如 "未分类"）→ 所有 pattern 的 max_run=0、warnings 空、转移矩阵全零——单调性报警盲区，且无任何告警。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.skill_utils.chapter_pattern.compute_pattern import compute_consecutive, check_consecutive_warnings, compute_transition_matrix
  print(compute_consecutive(['冒险']*10))
  -> {'引入': 0, '升级': 0, ..., '总结': 0}            # 10 连词表外 → 全 0
  print(check_consecutive_warnings(compute_consecutive(['冒险']*10)))
  -> []                                                # 无任何 warning
  print(check_consecutive_warnings(compute_consecutive(['日常']*3)))
  -> [{'pattern': '日常', 'max_run': 3, 'threshold': 3, 'level': 'med'}]  # 词表内 3 连即 med
  print([sum(v for v in r['to'].values()) for r in compute_transition_matrix(['冒险']*5)])
  -> [0, 0, ..., 0]                                    # 13 行全零，词表外转移丢失
  "
  ```
- 影响：shenbi-chapter-pattern 是活动 skill（SKILL.md:43 接线 compute_pattern.py）；当 LLM 输出 "未分类"（文档化回退）或任何词表外标签，且该类章节连续出现时——正是工具承诺要报警的"模式集中/单调"场景——确定性单调性检测**完全静默**（max_consecutive 全 0、无 warning、转移矩阵空）；与 F621（熵被低估）叠加使词表外 dominant pattern 在全部 4 个分析面（熵/分布/连续/转移）中 2 个失明。当前真实项目无 chapter_patterns.md 数据（novel-output 无该文件）→ 潜伏；skill 激活即达。
- 建议方向：与 F621 统一修复——`compute_consecutive` 结果按"实际出现的全部 pattern"输出（或增加 "其他/未分类" 桶并入 warnings 检查，`MAX_CONSECUTIVE` 对词表外 pattern 用 `DEFAULT_MAX_CONSECUTIVE`）；`compute_transition_matrix` 增加词表外行/列或至少把词表外转移计入"其他"桶；`main()` 的 pattern_distribution 增加 "未分类" 计数；补单测（10 连 "未分类" → 必须有 warning；词表外转移矩阵非全零）。

### F668 | segment_sentences 引号内句末标点劈句：闭合引号成为 1 字符伪句、引号句被拆成两段 → 句数/句长统计失真（56 章语料 172 个伪句，占 1.4%） | 统计口径 | P2（潜伏、低影响）

- 证据：`src/shenbi/skill_utils/style_learning/compute_stats.py:57-75`（`segment_sentences` 逐字符在 `"。！？\n"` 切句，**无引号上下文意识**——引号内的 `。`/`！`/`？` 同样触发切分，闭合引号字符随后成为独立"句子"）；消费方 `compute_all_stats` :334（`sentences = segment_sentences(combined)`）→ `compute_sentence_stats` :342 句长统计 + style-learning 报告（skills/shenbi-style-learning/SKILL.md:51 强制接线）；真实数据 56 章语料。
- 根因：对话文本 `他说："你好。"然后走了。` 中引号内的 `。` 触发切分 → 产出 `('他说："你好。', 7)` 与 `('"然后走了。', 6)` 两个"句子"（实际是一个引号句 + 后续叙述）；若引号句是句子末段，闭合引号单独成为 1 字符伪句。句数被虚高、引号句长度被截断、1-10 字符直方图桶被伪句填充。与 F628（`；` 口径）、F633（`\n` 计句末）同属"切句口径"族，但**引号劈句从未被任何轮次覆盖**（前十六轮 grep：引号/quote 与切句关联 0 命中）。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.skill_utils.style_learning.compute_stats import segment_sentences
  print(segment_sentences('他说：\"你好。\"然后走了。'))
  -> [('他说："你好。', 7), ('"然后走了。', 6)]       # 一个引号句被劈成两段
  # 56 章全量：1 字符伪句统计
  -> total sentences: 12218 | 1-char pseudo sentences: 172 (1.4%)
  chapter-1: 183 句中 9 个 1-char 伪句（均为闭合引号 '"'）
  "
  ```
- 影响：潜伏、低影响——真实 56 章语料句数虚高 1.4%（172 个伪句），句长直方图 1-10 桶被闭合引号填充、mean/median 轻微下偏；对话密集章节（引号多）失真更明显。style-learning 报告的句长统计（LLM 据此写风格描述）有系统性的小偏差；无 P0/P1 影响。测试（test_compute_stats.py）未覆盖引号句 → 未锁定也未发现。
- 建议方向：`segment_sentences` 增加引号状态跟踪（遇 `"`/`“`/「 进入引号态，仅在引号态外切句；或切句后剥离孤立的引号残句），与 compute_punctuation 的引号字符语义对齐；补单测（含引号句的文本：引号句不劈、无 1 字符伪句；真实 chapter-1 句数与人工计数一致）。

---

## 3. 严重度异议

| finding | 现定级 | 异议 | 理由 |
|---|---|---|---|
| F601 | P1 | **软异议（支持 review6-16）：可辩 P2** | 缺陷本体成立（对话塌陷 off-by-one），但生产影响全潜伏：detect_drift 的唯一生产入口 `_check_linguistic_drift` 在无 baseline（F602）时于 chapter_loop.py:2017-2019 提前 return（本轮代码精读确认 baseline 检查在 compute_linguistic_metrics 之前），detect_drift 从未被生产调用；即便接线，F612（is_drift 门控）与 F620（调用点吞异常）使干预仍不可达。按决策表"死代码/未接线"（P2）与"违反显式契约"（P1）均可辩护。不构成整条误报，不修改定级（无权限），供协调者裁决。 |
| F646 | P2 | **软异议（支持 review11-16）：impact 可辩 P1，维持 P2** | sensitivity 为 BLOCKING 级安全网且激活链是活的（audit_layer GENRE_ACTIVATION_MATRIX → get_active_genre_audits → sensitivity_blocking → escalation），禁用即静默移除能产生 BLOCKING 升级信号的审计，且写侧 rationale 与读侧 G0 均无信号；但真实配置当前 10 维全 True（潜伏）+ "absent → non-critical" 是 thresholds.py:48-49 显式声明设计 → 维持 P2 亦可辩护。不修改定级（无权限），供协调者裁决。 |

（其余 64 条无严重度异议。六条 P1 中 F602/F612/F620/F637/F640 本轮均经运行/代码/全仓 grep/真实数据多重复核，无反证；F642 证据裁决支持 review9-16 终裁，本轮回放一致，无新争议。）

---

## 4. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2056 漂移检查全链、:2014-2019 baseline 读、:2023-2043 is_drift 门控、:2047-2056 opening-similarity 门外交互、:2719-2727 ESCALATE 调用点吞异常、:684-743 `_maybe_materialize_progress`/`_auto_rebuild_progress_if_stale` trace 路径错位、:997-1004 check_escalation 接线）、gates/g0.py（:658-668 except 子句）、g0_config_coherence.py（:88-97 缺失语义 + `is False`）、audit_layer.py（:105-113 激活语义 + GENRE_ACTIVATION_MATRIX）、audit/write_audit.py（:49-56 records 消费、无 YAMLError 防护）、audit/snapshot.py（:110 parse_records 消费）、dispatcher/executor.py（:263-266 finally 块）、revision_router.py（:62-64 DEFAULT_RESONANCE_FLOOR=50）、plugins/master.json（59 skills 规范化对比）、skills/ 目录（74，缺 15 名单与前九轮一致）、skills/shenbi-chapter-pattern/SKILL.md（:43 compute_pattern 接线、:54 "未分类" 回退契约、:55 单调性报警承诺、:78 主/副模式）、skills/shenbi-style-learning/SKILL.md:51（compute_stats CLI 强制接线）、skills/shenbi-drift-guidance/SKILL.md（:137-139 §11.8 承诺）、novel-output/xinghuo-ranqiong/（pending_hooks.md 143 行章节附录结构、resonance_trend.md 单表单行 ch55 含 human_overridden 列、genre-config.json 10 维 camelCase dict 无 drift_detection 键、chapters/chapter-1/45/46.md META 结构与引号统计、56 章 compute_all_stats/句数全量运行、无 trace.jsonl、无 linguistic_baseline.json、无 volume_score_trend.md、无 chapter_patterns.md）、tests/fixtures/truth-pending_hooks.md + tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照）、tests/fixtures/genre-config-example.json（tropeInventory 2 项、无空 signature）、docs/superpowers/plans/archive/2026-07-19-07-*.md（F602 接线指令原文）。
- 维持观察（不重复立项，供协调者参考，均本轮复核确认仍成立）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）与同名文件跨目录覆盖、compute_pattern main 非 dict 章节 `.get` 崩溃、compute_pattern.classify_entropy 恰好等于阈值（h==2.5，measure-zero）、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize round 名解析脆弱性与 `init_tier = str(payload.get("tier", tier))` 遇 None → "None"、compute_drift `_append_audit` 重复追加无幂等 + `--write-audit-drift` CWD 相对路径、compute_connectives "时间"/"顺序" 类别重叠与 "但是"↔"但" 子串双计、cjk.count_words 非法 mode 静默走 mixed、`find_terms` 裸字符串 terms 逐字符迭代、plugins/generate.py `fields["marketplace"]` 缺 fields KeyError、`detect_chapter_drift` 平滑先于排除、check_window_redundancy docstring "sliding window" vs 实现仅取最后 4 章、frequency_divergence_alarms docstring ">3σ" vs 实现 4× 速率启发式、verify_chain 不校验链首、detect_drift 对话密度 0→正值不记录 deviation（健康信号）、`compute_linguistic_metrics` dialogue_density 不计「」/『』角引号、escalation 输入含 nan 时 slope 静默不触发（F654 值级同族）、G0.cc.threshold_mismatch 对合法提高 floor 同样报 mismatch（E11 单源设计使然）、AI_MARKERS 子串在成语中的假阳性（实证无影响）、`_load_config` 的 `assert isinstance(result, dict)` 在 `python -O` 下被剥离、parse_markdown_table 全 "-"/":" 单元格数据行被当分隔行丢弃、`_values_equal(None, 'None')` 字面 "None" 掩盖缺省键（F641/F655 修复时一并规范"无值"表示，本轮并入 F641 判定说明）。
- 关于"0 新 finding"收敛目标：F601-F666 共 66 条全部成立（含六条 P1），**无整条误报**；本轮新发现 2 条（F667/F668）均为前十六轮未触达的**词表外盲区**（连续运行/转移矩阵）与**引号劈句口径**，均 P2、潜伏/低影响、无 P0/P1。收敛曲线 11→8→10→7→3→3→1→3→2→5→2→3→2→2→1→2→1→2，自 F648 起连续 10 轮新发现全部为 P2 潜伏级。**按 review10-16 提议的"无 P0/P1 新 finding + 无整条误报"判据本轮达成收敛**；按字面"0 新 finding"判据未完全收敛（P2 潜伏级小口径缺陷理论可无限续轮）。建议协调者落定收敛判据（"无 P0/P1 新 finding + 无整条误报"）并终止轮次，优先排期修复 P1 接线族（F602/F612/F620/F640 绑定修复 + F637 格式决策 + F666 三处守卫）。

---

## 5. 返回摘要

**复核统计：新 finding 2（F667-F668，均 P2、潜伏/低影响）/ 误报 0 / 严重度异议 0 条新增（支持既有软异议 2 条：F601、F646）。**

**判定：非 0 新 finding（2 条，均 P2 潜伏级、无 P0/P1）——按"无 P0/P1 新 finding + 无整条误报"判据达成收敛；按字面"0 新 finding"判据未完全收敛。**

- F601-F666 共 66 条全部成立，**无整条误报**。六条 P1（F601/F602/F612/F620/F637/F640）本轮以直接运行 + 代码精读 + 全仓 grep + 真实数据 + 计划原文多重复核，**无反证**：
  - F601：baseline dialogue=10→current=1 → is_drift False、severity NONE（off-by-one 成立）；生产影响全潜伏（F602 无 baseline → detect_drift 从未被生产调用）→ 支持软异议 P1↔P2 可辩；
  - F602：establish_baseline 全仓零调用、无 linguistic_baseline.json（style/ 仅 style_profile.md）；
  - F612：base stm=60/cur=110 → is_drift False、severity ESCALATE（实测），chapter_loop.py:2023 门控吞掉绝对阈值干预；
  - F620：chapter_loop.py:2719-2727 "non-blocking" 注释 + `except Exception` 吞 DriftEscalationError（本轮代码精读确认）；
  - F637：真实 pending_hooks.md 无 `## hooks`/`## 活跃伏笔`（`grep -n "^#"` 全清单核对），ch-025 快照有权威结构 → 格式分叉，write_audit.py:49-56 生产接线确认；
  - F640：`"INIT"`/`"MARK_DONE"` 全仓仅消费方 materialize.py 与测试命中；`_maybe_materialize_progress` steps_done%5==0（含 0）+ `except Exception: pass`；`_auto_rebuild_progress_if_stale` 的 `trace/` 目录 vs `trace.jsonl` 文件路径错位；真实 progress.json 为 scoring schema（触发即数据丢失，激活应升级 P0）。
- **F642 证据裁决复核**：raw 0.6267 / 剥离后生产顺序 0.6367 / 交换顺序 0.57 / autojunk=False 0.77（本轮回放一致）——0.57 与 0.637 均可复现（SequenceMatcher 参数顺序不对称，F649），review8 裁定不成立，review9-16 终裁成立。
- 本轮 2 条新 finding 均为前十六轮未触达方向：
  1. **F667（P2）**：compute_pattern 词表外 pattern（含 skill 文档化回退标签 "未分类"）在连续运行检测/最大连续/转移矩阵中被静默丢弃——10 连词表外 → max_run 全 0、warnings 空、转移矩阵全零，单调性报警盲区且无告警（运行复现；skill 契约 SKILL.md:54-55 使词表外标签具生产相关性；F621 只覆盖熵分母，连续/转移盲区为独立向量）。
  2. **F668（P2）**：segment_sentences 引号内句末标点劈句——闭合引号成为 1 字符伪句、引号句被拆两段；56 章语料 172/12218（1.4%）伪句，句长直方图/句数系统性小偏差（运行复现；与 F628/F633 同族但引号向量从未覆盖）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F667 | compute_pattern 词表外 pattern（含"未分类"回退）连续运行/转移矩阵静默丢弃 → 单调性报警盲区 | P2 | compute_pattern.py:57-76,129-142,145-158,170; SKILL.md:54-55 |
| F668 | segment_sentences 引号内句末标点劈句 → 闭合引号 1 字符伪句、句数/句长失真（56 章 172 伪句） | P2 | compute_stats.py:57-75,334,342; 56 章语料 |
| F601（异议） | P1↔P2 可辩（生产影响全潜伏），支持 review6-16 软异议 | 严重度软异议 | linguistic_drift.py:211-218; chapter_loop.py:2017-2019 |
| F646（异议） | sensitivity（BLOCKING 级安全网）impact 可辩 P1，维持 P2 | 严重度软异议 | thresholds.py:50-84; audit_layer.py:44-45; chapter_loop.py:989-1003 |
| F642（裁决） | 0.57/0.637 均可复现（参数顺序不对称）；生产顺序剥离后 0.6367 仍 > 0.6；review8 裁定不成立 | 证据裁决 | linguistic_drift.py:249-253; chapter-45/46.md |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review17.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
