# Z6 分区第七轮独立复核报告（fresh-context 全量重读）

> 复核人：第七轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11（F601-F611）+ 一轮 8（F612-F619）+ 二轮 10（F620-F629）+ 三轮 7（F630-F636）+ 四轮 3（F637-F639）+ 五轮 3（F640-F642）+ 六轮 1（F643）逐条独立验证；新发现编号 F644 起。
> 方式：49/49 文件全文 read + `PYTHONPATH=src python3` 实际运行复现（F601/F605/F610/F612/F614/F615/F616/F622/F631/F636/F641/F643 及全部新发现本轮回放）+ 全仓 grep 调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对（genre-config.json 10 维 auditDimensions、progress.json scoring schema、chapters/chapter-1/45/46.md META 与引号、truth/pending_hooks.md、truth/resonance_trend.md、truth/audit_drift.md、master.json 59 skills）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未完全达成**（新发现 3 条，均 P2；另对 F642 证据勘误提出反勘误、支持 F601 严重度软异议）。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §3）
- 初审 11 findings：**11/11 成立**（F601 严重度软异议支持，见 §2）
- 一轮 8：**8/8 成立**
- 二轮 10：**10/10 成立**
- 三轮 7：**7/7 成立**
- 四轮 3：**3/3 成立**
- 五轮 3：**3/3 成立**（F642 证据勘误被本轮回放反勘误，见 §2.2）
- 六轮 1：**1/1 成立**
- 新发现：**3**（F644-F646，均 P2）
- 整条误报：**0**；严重度异议：**0 条新增**（支持 review6 对 F601 的软异议 1 条）；证据勘误反勘误：**1**（F642）
- 判定：**非 0 新 finding（3 条）**——收敛曲线 11→8→10→7→3→3→1→3，接近收敛但未达 0

---

## 1. F601-F643 判定表（逐条独立复核）

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（**支持 review6 软异议：可辩 P2**） | 本轮直接运行：baseline dialogue=10 → current=1 → `is_drift: False, severity: NONE`。linguistic_drift.py:215 `max(…,5.0)` 与 :218 严格 `>5.0`。docstring :185-188 承诺 "<20% of baseline" 触发 → off-by-one 成立。**生产影响全潜伏**（F602 无 baseline 时 `_check_linguistic_drift` 提前 return，detect_drift 从未被生产调用）——P1/P2 均可辩（见 §2.1）。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持） | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py:6,28` re-export，生产零调用；`find . -name linguistic_baseline.json` 0 命中。计划 07-19-07 Task 5a 显式接线指令未履行。P1 成立。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链 write_audit → record.py:24 block ship。test_drift.py 无 YAML-ghost 反向用例。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'`，:226 按字符迭代 → 一对 ASCII 引号 count=4。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持；三变体均复核） | 本轮运行：空 terms → `system_term_density: 1028.5714`；`system_terms: null` → `TypeError: 'NoneType' object is not iterable`（本轮实测复现）；`drift_detection: null` → `None.get` AttributeError（review6 变体，同根）。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过（前轮运行复现）。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs `dialogue_density`；:296 硬编码术语 vs :107 config 驱动。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | cjk.py:54 桶 token 为相邻双字符，正常文本引号包围内容不相邻 → `text.count` 恒 0。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py 全文无 logging 导入；:40-43 两处 break 无日志、:47-48 改写文件。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | 本轮运行：`[{'last_reinforced':1,'max_distance':2}]`（无 id、silence=9>2 逾期）→ `KeyError: 'id'`。recall.py:47 无防御。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:109 `key.startswith("auditDimensions.")` 只拦带点形式；整键对象覆盖绕过（前轮运行复现）。F631/F638/F643 为独立向量。P2 成立。 |

### 1.2 一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持） | 本轮直接运行：baseline stm=60 → current stm=110 → `is_drift: False, severity: ESCALATE`。chapter_loop.py:2023 `if result.is_drift:` 包裹全部干预（:2024-2043 代码精读确认）。计划 07-19-07:749 绝对阈值契约。P1 成立。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"破折号": "——"`/`"省略号": "……"` 重复字符 + :226 按字符迭代。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | 本轮运行：`{'some.new.key': 1, 'resonance_global_floor': 58}` + 60 字 rationale → ConfigError；config-change-log.jsonl 已有 1 条幻影条目（key=some.new.key）、genre-config.json 无该键。config_coherence.py:105-133 循环内逐 key 追加，safe_write 在循环外 :142。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | 本轮运行：合法行+撕裂行 → `TraceWriter(d)` 抛 JSONDecodeError；同文件 replay 自愈（不对称确认）。writer.py:57 无 try。P2 成立。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | 本轮运行：`parse_records('## hooks\n- id: H01\n  type: 主线\n- just a string\n- 42\n')` → 仅 1 条 dict，2 条脏数据静默消失。parser.py:45。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | 本轮 grep：两函数仅 linguistic_drift.py 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` → 仅 compute_stats.py:37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。P2 成立。 |

### 1.3 二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持） | 本轮代码精读：chapter_loop.py:2722-2728 `try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed")`；函数内 :2035-2039 raise DriftEscalationError；:2719 注释自称 "non-blocking"——调用点与函数内契约自相矛盾。grep 唯一生产调用点即 :2722。P1 成立。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目；:170 main 默认 "未分类" 且词表外字符串原样保留。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | 本轮运行：`last_reinforced='3'` → `TypeError: unsupported operand type(s) for -: 'int' and 'str'`。recall.py:41-46。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持） | compute_stats.py:52 单 U+2026 vs 真实文本双 `……`（与 :23 PUNCT_MAP 一致）。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无 skills 校验 | P2 | **成立**（P2 维持） | 本轮规范化重算：master 59、skills/ 目录 74、缺失恰好 15 个（名单与 review2-6 完全一致：anchor-curate、book-spine-init、escalation-review、foreshadowing-lifecycle/recall、memory-distill、review-arc-payoff、review-group-*×4、review-resonance、score-arc/stratum/volume）、反向 0。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、无生产调用方）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | 本轮 grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export，src 内无生产调用。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件；对比 records/drift.py:38-39 非 `|` 行即 break。真实 resonance_trend.md 单表 → 潜伏。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | compute_stats.py:16 SENT_ENDS 含 `；`，:63 segment_sentences 只按 `。！？\n` 切句，:87 segment_paragraphs 用 SENT_ENDS。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | 本轮 grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用。P2 成立。 |

### 1.4 三轮 F630-F636

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | **成立**（P2 维持） | 代码确认 revision_router.py:63-64（注释明示 "config.resonance_global_floor" 后跟 `= 50`）vs thresholds.py:30 `=65`；chapter_loop.py:1897 显式传值覆盖默认 → 潜伏。P2 成立。 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | **成立**（P2 维持） | 本轮运行：`{'auditDimensions.texture': 0}` + 短 rationale → ACCEPTED（Rule 1 不拦）；G0 `is False` 不报；`get_active_genre_audits({'auditDimensions': {'texture': 0}})` → texture 审计不激活。config_coherence.py:109、g0_config_coherence.py:92。P2 成立。 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | **成立**（P2 维持） | linguistic_drift.py:222-226 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50；thresholds.py:8-10 docstring 自称单源。P2 成立。 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | **成立**（P2 维持） | compute_stats.py:79 docstring vs :80 `re.split(r"\n\s*\n")`；`\n` ∈ SENT_ENDS :16。P2 成立。 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | **成立**（P2 维持） | 真实章节 chapter-1.md 含 `<!--META-BEGIN-->`~`<!--META-END-->`（约 515/4177 字符）；chapter-45/46 META 占比 12-25%；两处统计入口无剥离。P2 成立。 |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | **成立**（P2 维持） | 本轮 grep：`update_genre_config` 仅 config_coherence.py 自身 + 测试 + 计划文档；真实 config-change-log.jsonl 仅 1 条 2026-07-19 记录。P2 成立。 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | **成立**（P2 维持） | 本轮直接运行：base 全 0 + current em_dash=0.33 → `is_drift: True, severity: WARN`。linguistic_drift.py:202。P2 成立。 |

### 1.5 四轮 F637-F639

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转（格式分叉 + 测试掩盖） | P1 | **成立**（P1 维持） | 本轮全链复核：真实 pending_hooks.md（143 行，ch56）标题仅 `# 伏笔追踪`/`## 第56章伏笔呈现`/`## 第56章生命周期状态更新` 等章节附录，**无 `## hooks`/`## 活跃伏笔`**（`grep -c "## hooks"`=0）；ch-025 快照有 `## 活跃伏笔`(:12)/`## hooks`(:20)。消费链 write_audit.py:54-56 → record.py:24 block-ship 已接线但 drift 恒空。P1（"测试失效掩盖真实缺陷"档）成立。 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过 Rule 1 与 G0） | P2 | **成立**（P2 维持） | audit_layer.py:105-107 snake_case 回退 vs config_coherence.py:109/g0_config_coherence.py:79 只认 camelCase。真实配置 camelCase → 潜伏。P2 成立。 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 真实项目 arc/volume 半边不运行 + audit 追加无协调 | P2 | **成立**（P2 维持） | 本轮核对真实数据：truth/ 仅 resonance_trend.md（1 行 ch55），无 arc_payoff_trend.md/volume_score_trend.md；audit_drift.md 为 `## Ch55 —` 章节式（review-resonance 写），CLI `_append_audit` 追加 `## drift findings` 段与之无协调。P2 成立。 |

### 1.6 五轮 F640-F642

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F640 | materialize_progress 输入契约零生产者 → 无条件覆盖真实 progress.json（数据丢失+错误结果） | P1 | **成立**（P1 维持） | 本轮全链复核：`grep '"INIT"\|"MARK_DONE"'` src/ → 仅 materialize.py:49,53（消费方）+ 测试；生产 TraceWriter.append 调用点 = audit/record.py:44 + safe_write.py:132（trace_action 生产唯一传参处即 materialize.py:98 自身）。**本轮实际运行**：构造真实 schema 的 progress.json（`current_scorer_agent`/`scoring_history`）后调用 `materialize_progress` → 两键全部丢失，替换为 `completed_skill_names: []` 全 pending 视图。调用点 chapter_loop.py:2691/:3028（steps_done%5==0 含 0 即触发）+ cli `_auto_rebuild_progress_if_stale`（检查 `trace/` 目录 vs TraceWriter 写 `trace.jsonl` 文件——路径错位，自愈永不触发）。真实项目无 trace.jsonl、progress.json 未被破坏 → 潜伏。P1 成立（触发即数据丢失，激活应升级 P0）。 |
| F641 | records `_values_equal` 布尔比较孔 → YAML true vs md "true" 假 drift | P2 | **成立**（P2 维持） | 本轮运行：`_values_equal(True, 'true')` → False（假 drift）；`detect_cross_section_drift([{'id':'h','state': True}], {'h':{'id':'h','state':'true'}})` → 假 drift 1 条。drift.py:57-69 float 兜底对 bool 抛 ValueError 落入 return False。P2 成立。 |
| F642 | check_opening_similarity 比较 META 块非正文开头 | P2 | **成立**（本体成立；**review6 证据勘误被反勘误**，见 §2.2） | 代码层面成立：linguistic_drift.py:249-253 取 `[:300]` 首 300 字符，真实章节以 `<!--META-BEGIN-->…<!--META-END-->` 开头（chapter-45/46 均确认）→ 比较对象确为 META 块。**但 review6 的"剥离后 0.637 仍 > 0.6"本轮回放无法复现：同法剥离后 (45,46) = 0.570 < 0.6（见 §2.2），与 review5 原始数字一致**。P2 维持。 |

### 1.7 六轮 F643

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F643 | 治理链"键缺失"默认语义矛盾：G0 视缺失=启用、audit_layer 视缺失=禁用 → 移除/置空 auditDimensions 静默停用审计 | P2 | **成立**（P2 维持） | 本轮运行：`update_genre_config(d, {'auditDimensions': {'antiAi': True, 'continuity': True}}, rationale='x')` → ACCEPTED 且 texture 从落盘 config 移除；`check_config_coherence` → []（G0 不报）；`get_active_genre_audits` → []（审计停用）。g0_config_coherence.py:92 `get(dim, True)` vs audit_layer.py:113 `get(dim_key, False)` vs config_coherence.py:109 只认显式 False。P2 成立。 |

---

## 2. 严重度异议与证据勘误

### 2.1 F601 严重度软异议（支持 review6）

review6 提出 F601（P1）可辩 P2：缺陷本体成立（对话塌陷 off-by-one），但生产影响全潜伏——detect_drift 的唯一生产入口 `_check_linguistic_drift` 在无 baseline（F602）时提前 return，detect_drift 从未被生产调用；即便接线，F612/F620 使干预仍不可达。**本轮复核同意该异议**：按决策表"死代码/未接线"（P2 档）与"违反显式契约"（P1 档）均可辩护。不构成整条误报，不修改定级（无权限），供协调者裁决。

### 2.2 F642 证据勘误反勘误（review6 的修正不可复现）

- review5 声称：56 章全对扫描，META 剥离后 (45,46) max = 0.57 < 0.6，META 是越过阈值的主因。
- review6 声称：同法剥离后 (45,46) = **0.637** 仍 > 0.6，review5 的 0.57 有误，剥离 META 不足以修复。
- **本轮独立重放**（同一 `check_opening_similarity`、同一剥离方法 `re.sub(r'<!--META-BEGIN-->.*?<!--META-END-->', '', t, flags=re.S)`，ch45/46 含两个 META 块，两法均剥全部/首块）：
  ```
  raw [:300]           = 0.6267   # 越过 0.6 阈值
  strip-all then [:300] = 0.5700   # < 0.6 —— 与 review5 一致，review6 的 0.637 无法复现
  strip-first then [:300] = 0.5700
  stripped [:200]      = 0.865    # 去 META 后前 200 字符为近模板正文（# 第五周周X + 冷在…模板句）
  stripped [:500]      = 0.384
  full stripped text   = 0.6175   # 全文相似度 > 0.6（正文本身模板化）
  ```
- 结论：**review6 的实证修正（0.637）不可复现**；review5 的 0.57 被独立复现。F642 本体（比较对象=META 块）代码层面成立、影响（接线后真实数据假阳性）成立；**因果归因（META 是越过 [:300] 阈值的主因）按 review5 成立**。但需补充 nuance：剥离后 [:200] = 0.865 说明真实正文开头本身高度模板化（"冷在冬季周X。冷在墙体第五层。冷知道深度在第五层——"逐字模板），0.6 阈值对真实正文依然脆弱——修复建议 = 剥离 META + 阈值重新标定（两者都需要，review6 的"仅标定即可"与 review5 的"仅剥离即可"都不完整）。

---

## 3. 新发现（F644-F646）

### F644 | compute_ngrams 未剔除标点 → n-gram 风格指纹被标点对/标点串主导（真实 chapter-1 top bigram 为 `。他` 与 `——`） | 统计口径 | P2

- 证据：`src/shenbi/skill_utils/style_learning/compute_stats.py:209`（`cleaned = [c for c in text if c.strip()]` —— 只滤空白，标点全保留）；同文件 `compute_ttr` :171 却显式排除全部标点 → 同一模块两套口径；真实数据 `novel-output/xinghuo-ranqiong/chapters/chapter-1.md`。
- 根因：n-gram 提取把 `。` `，` `——` 等标点与正文字符混入滑动窗口。真实章节高频标点（句号/逗号/破折号）产生大量标点相邻对与纯标点串：实测 chapter-1 top bigram = `('。他', 27)`（句号+代词，句边界伪特征）与 `('——', 25)`（破折号本身作为"双字符组合"上榜）——纯标点串 `——` 作为"bigram"对风格指纹毫无信息量，却占据最高频位置，稀释内容型 n-gram 的风格信号。style-learning 报告据此做风格匹配/差异对比时指纹失真。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.skill_utils.style_learning.compute_stats import compute_ngrams
  import pathlib
  t = pathlib.Path('novel-output/xinghuo-ranqiong/chapters/chapter-1.md').read_text(encoding='utf-8')
  print(compute_ngrams(t, n=2, min_count=10, max_results=6))
  -> [('。他', 27), ('——', 25), ('灵能', 20), ('林烽', 18), ('矿工', 17), ('没有', 13)]
  "
  ```
- 建议方向：`compute_ngrams` 复用 compute_ttr 的标点排除集合（或显式过滤 `，。！？；：""''「」『』（）——……、`）后再滑窗；补单测（标点串不得进入 top n-gram，且与 compute_ttr 口径一致）。

### F645 | compute_ttr 排除串缺 `“”`/`"`/`‘’` 引号（只排 ASCII 直单引号 `''`）→ 对话文本引号字符计入 TTR token | 统计口径 | P2

- 证据：`src/shenbi/skill_utils/style_learning/compute_stats.py:171`（`chars = [c for c in text if c.strip() and c not in "。，！？；：''「」『』（）——……、\n"]` —— 排除串含两个 ASCII 直单引号 `''`，**缺 U+201C/U+201D 弯双引号 `“”`、ASCII 双引号 `"`、U+2018/U+2019 弯单引号 `‘’`、单 U+2026 `…`**）；真实数据 54/56 章含引号共 3422 个引号字符。
- 根因：TTR 的目的是正文词汇多样性（字符级 type-token），但引号字符（对话标记）未被排除 → 计入 total 与 unique。对话越多的章节 total 膨胀越明显（每对 `“”` 增加 2 个 token），TTR 被系统性压低；且与同模块 :226 `compute_punctuation` 把引号计入"引号"标点桶的语义相矛盾（同一字符既算标点又算 TTR token）。真实章节 54/56 含 `“”`/`"` → 影响普遍存在（每章约 61 个引号字符，约 1.7% token 占比）。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.skill_utils.style_learning.compute_stats import compute_ttr
  print(compute_ttr('他说：“你好。”然后离开了。')['total_chars'])  # 11：2 个弯引号被计入
  print(compute_ttr('他说你好然后离开了。')['total_chars'])        # 9
  "
  -> 11 / 9
  ```
- 建议方向：排除串补齐 `“”` `"` `‘’` `…`（与 `''` 并排），或统一为显式标点集合常量供 TTR/ngrams/句切分共用；补单测（含弯引号文本的 TTR 与无引号文本口径一致）。

### F646 | AUDIT_SAFETY_MATRIX 维度集与真实 genre-config auditDimensions 漂移（6 vs 10 维）：motivation/foreshadowing/sensitivity/worldRules 可无 rationale 禁用且 G0 无信号 | 跨文件状态一致性 | P2

- 证据：`src/shenbi/config/thresholds.py:50-84`（AUDIT_SAFETY_MATRIX 仅 6 维：texture/antiAi/continuity critical + dialogue/character/pacing 非 critical）；真实 `novel-output/xinghuo-ranqiong/genre-config.json` auditDimensions 10 维（antiAi, character, motivation, pacing, continuity, foreshadowing, sensitivity, worldRules, dialogue, texture）；`src/shenbi/config/config_coherence.py:109-118`（Rule 1 仅对矩阵内维强制 rationale）；`src/shenbi/gates/g0_config_coherence.py:37`（`_CRITICAL_DIMENSIONS` 只含矩阵 critical 维 → 对缺失维无 G0.cc.critical_audit_disabled 信号）。
- 根因：矩阵的"absent → non-critical"回退（thresholds.py:48-49 注释）使**真实配置里存在但矩阵未枚举的 4 个维度**（motivation/foreshadowing/sensitivity/worldRules）落入无治理区间：`update_genre_config(d, {'auditDimensions.motivation': False}, rationale='x')` → ACCEPTED（Rule 1 不触发）；G0 不报；`get_active_genre_audits` → motivation 审计不派发（audit_layer GENRE_ACTIVATION_MATRIX 含 motivation）。其中 sensitivity 是 BLOCKING 级安全网（chapter_loop.py:989-1003 `sensitivity_blocking` 喂 escalation/check.py:80-86 的 `sensitivity_blocking` 触发，且 audit_layer 将其映射到 shenbi-review-sensitivity）——禁用它即静默移除一个能产生 BLOCKING 升级信号的审计。矩阵 vs 真实配置的维度集分叉与 F624（master.json vs skills/）、F630/F632（阈值双源）同族：单源声明（thresholds.py:5-10 "every consumer reads the same value"）未覆盖"维度集合"本身。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.config.thresholds import AUDIT_SAFETY_MATRIX
  import json
  cfg = json.load(open('novel-output/xinghuo-ranqiong/genre-config.json', encoding='utf-8'))
  real = set(cfg['auditDimensions'].keys()); matrix = set(AUDIT_SAFETY_MATRIX)
  print(sorted(real - matrix))  # 真实配置有而矩阵无的维
  -> ['foreshadowing', 'motivation', 'sensitivity', 'worldRules']
  # 写侧：禁用矩阵外维无需 rationale
  update_genre_config(d, {'auditDimensions.sensitivity': False}, rationale='x') -> ACCEPTED
  # 读侧：G0 无信号
  check_config_coherence(d, resonance_global_floor=65) -> []
  "
  ```
- 建议方向：二选一——(a) 将真实配置全部维度补入 AUDIT_SAFETY_MATRIX 并逐个判定 critical 档（sensitivity 因 BLOCKING 能力建议 critical）；或 (b) 若设计即"仅 texture/antiAi/continuity 为 critical"，则在阈值模块/计划文档显式声明矩阵为刻意子集并补一条"矩阵维度集 vs 真实 config 维度集"的一致性测试（新维加入 config 时矩阵必须同步更新或显式豁免），防止未来 config 增长再次静默扩大无治理区间。补单测（矩阵外维禁用必须有治理信号或显式豁免声明）。

---

## 4. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2056 漂移检查全链、:2023-2044 is_drift 门控、:2047-2056 opening-similarity 门外、:2716-2728 ESCALATE 调用点吞异常、:684-741 materialize 调用、:2691/:3028、:989-1003 sensitivity_blocking、:2574-2575 get_active_genre_audits 消费）、revision_router.py（:63-64 DEFAULT_RESONANCE_FLOOR）、audit_layer.py（:105-113 激活语义 + GENRE_ACTIVATION_MATRIX 9 维）、g0_config_coherence.py（:37-42 critical 矩阵、:92 缺失语义）、audit/write_audit.py（:54-56 records 消费）、audit/record.py（:24,42-46 block + trace seam）、safe_write.py（:127-140 trace_action 唯一生产调用点）、plugins/master.json（59 skills）、skills/ 目录（74）、novel-output/xinghuo-ranqiong/（genre-config.json 10 维 camelCase 无 drift_detection 键、progress.json scoring schema、chapters/chapter-1/45/46.md META 结构与引号统计、truth/pending_hooks.md 143 行结构、truth/resonance_trend.md 单表单行、truth/audit_drift.md `## Ch55` 格式、无 trace.jsonl）、tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照）、docs/superpowers/plans/archive/2026-07-19-04-*.md（critical 维定义与测试锁定）。
- 维持观察（不重复立项，供协调者参考）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）、compute_pattern main 非 dict 章节 `.get` 崩溃、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize round 名解析脆弱性、compute_drift `_append_audit` 重复追加无幂等、compute_connectives "时间"/"顺序" 类别重叠、"----" 分隔行误判为数据行被跳过（parse_trend 对全 `-` 数据行）、**跨区观察**：shenbi-review-sensitivity 同时出现在 chapter_loop 固定步骤（CHAPTER_STEPS step 14）与 genre-circle GENRE_ACTIVATION_MATRIX → 配置启用时疑似双轮重复派发（Z3 区建议核查，非 Z6 finding）。
- 关于"0 新 finding"收敛目标：本轮独立重放确认 F601-F643 全部成立（含六条 P1），无整条误报；但发现 3 条前七轮均未覆盖的统计口径/治理覆盖新缺陷（F644-F646），均 P2、无 P0/P1。收敛曲线 11→8→10→7→3→3→1→3 表明 Z6 已进入"新发现基本为 P2 级小口径缺陷"区间。

---

## 5. 返回摘要

**复核统计：新 finding 3（F644-F646，均 P2）/ 整条误报 0 / 严重度异议 0 条新增（支持 review6 的 F601 软异议 1 条）/ 证据勘误反勘误 1（F642）。**

**判定：非 0 新 finding（3 条），未完全收敛。**

- F601-F643 共 43 条全部成立，无整条误报。六条 P1（F601/F602/F612/F620/F637/F640）本轮全部以实际运行/代码精读/全仓 grep/真实数据多重复核，无反证；其中 F601 支持 review6 的软异议（生产影响全潜伏，P1↔P2 均可辩，不修改）；F640 本轮实测复现"无条件覆盖真实 progress.json 的 scoring schema 为全 pending 视图"，P1 成立且触发即数据丢失（激活应升级 P0）。
- **F642 证据勘误反勘误（重要）**：review6 声称"剥离 META 后 (45,46) = 0.637 仍 > 0.6，review5 的 0.57 有误"——本轮回放**无法复现 0.637**，同法剥离得 0.570，与 review5 原始数字一致。F642 本体（检查比较对象=META 块）与影响（真实数据假阳性）成立；补充 nuance：剥离后前 200 字符相似度 0.865 表明真实正文开头本身高度模板化，0.6 阈值脆弱——修复需剥离 META + 阈值重新标定两者并施。
- 本轮 3 条新 finding 均为前七轮未触达的**统计口径 / 治理覆盖**方向：
  1. **F644（P2）**：compute_ngrams 未剔除标点 → 真实 chapter-1 top bigram 是 `。他`(27) 与 `——`(25)，内容 n-gram 指纹被标点污染（与同模块 compute_ttr 显式排除标点的口径矛盾）。
  2. **F645（P2）**：compute_ttr 排除串缺 `“”`/`"`/`‘’`（只排 `''`）→ 真实 54/56 章共 3422 个引号字符计入 TTR token，对话文本 TTR 系统性偏低。
  3. **F646（P2）**：AUDIT_SAFETY_MATRIX（6 维）与真实 genre-config auditDimensions（10 维）漂移——motivation/foreshadowing/sensitivity/worldRules 禁用无 rationale 强制且 G0 无信号，其中 sensitivity 为 BLOCKING 级安全网（与 F624/F630/F632 同族"单源未覆盖维度集合"）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F644 | compute_ngrams 未剔除标点 → n-gram 指纹被标点对/标点串主导 | P2 | compute_stats.py:209; chapter-1.md top bigrams |
| F645 | compute_ttr 排除串缺 `“”`/`"`/`‘’` 引号 → 引号字符计入 TTR token | P2 | compute_stats.py:171; 真实 3422 引号字符 |
| F646 | AUDIT_SAFETY_MATRIX 6 维 vs 真实 config 10 维漂移：4 维无 rationale/G0 保护（sensitivity 为 BLOCKING 级） | P2 | thresholds.py:50-84; genre-config.json; config_coherence.py:109; g0_config_coherence.py:37 |
| F642（反勘误） | review6 的剥离后 0.637 不可复现；同法剥离得 0.570 与 review5 一致 | 证据勘误 | chapter-45/46.md; linguistic_drift.py:249-253 |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review7.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
