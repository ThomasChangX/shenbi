# Z2 分区第四轮独立复核报告（Z2.review4）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）
- 模式: fresh-context 全量重读 38 文件；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖），不依赖前四轮转述；另读跨区消费面（g1/g2/g3/g3_independence/g4 三私有实现/gates/cli/gates/shared/audit/_shared/write_audit/snapshot/record/records/parser/drift/pipeline/dispatch_helper/scoring/status/first-novel.md/truth-files.yaml/相关 SKILL.md/git show 99f91f0:tests/dispatch-subagent.sh）。
2. **独立验证方式**：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前四轮一致）；15 个 `python -c`/heredoc 运行时探针（F216/F218/F230/F232/F215/F235/F236/F237/F238/F239/F240/F241/F242/F243/F244、F227 门序推理、F228 重试轮）。
3. **前四轮判定**：F201-F237 共 37 条**全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；已有 4 条严重度异议（F214/F218→P1、F217/F228→P1 低置信）经独立反证尝试后**全部支持**，本轮**不新增异议**。
4. **本轮新发现 7 条（F238-F244）**，集中于四轮都未触及的四类盲区：
   - **审计链解析崩溃**（F238）——malformed `## hooks` YAML 使 `audit_writes` 裸抛 ScannerError/ValueError，`dispatch_with_write_audit` finally 崩溃，"崩溃仍审计"保证失效、write-audit.jsonl 无 GATE_FAIL；
   - **derive_file_type glob 盲区**（F239）——truth/decisions 分类用精确字符串交集，`truth/*.md`/`snapshots/chapter-NNN/*` 写者（worldbuilding/sequel-writing/truth-sync/snapshot-manage）被误归 "chapter"，与函数 docstring 直接矛盾；
   - **中文 prompt 章节解析盲区**（F240）——`extract_chapter` 仅匹配英文 "chapter N"，中文 prompt → chapter=None → N/NNN 输出全部被丢弃 → standalone G2 整体跳过；
   - **OWNERSHIP 过期**（F242）——矩阵 2/6 条目指向 DEPRECATED 技能（plant/track，"Do not dispatch"），生产写者 foreshadowing-lifecycle 无条目 → pending_hooks.md 记录级保护对生产路径空转；
   - **PR-20 评分者角色回归**（F243）——原 shell 是"独立评分 subagent 派发（G3.4 wrapper）"，Python 翻译把 codex exec 改为生成器，分数从生成器 final message 自评解析 → standalone T1 结构上无独立评分者；
   - **G1.4/.bak 与 G2.11 范围错配**（F244）——.bak 由 G1 对 INPUTS 创建、G2.11 对 truth OUTPUTS diff，输出非输入的 truth-updater（state-settling 等）truth 防删保护静默失效；
   - **genre_config 规则 1 空 decision 绕过**（F241）——`if decision and ...` 守卫使缺失 decision 静默 PASS（F216 同类），且 禁用 非 list / 替换建议 非 dict 时计数/替换规则静默跳过。

---

## 一、复核结论表（F201-F237 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 重读三模型 + g4 私有实现（g4/chapter_planning.py:97-104 hook_ops 存在性 / context_composing.py:74-117 P1-P7/route 标题 / volume_outlining.py:59-72 KR/张力曲线）；grep 无生产消费者（仅 test_skill_integration.py:188 类名文本）；cov 34/38/40% 与报告吻合。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export，g4/genre_config.py:13 直接 import）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads（跨区，read 确认）。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g6.py:51 raw dict 读 target_word_count 属实——死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；g4/ 仅 genre_config+pacing_design import 契约模型（ls g4/ + grep 实测）。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797、:409-438 复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有，read 确认）。 |
| F210 | P2 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 40/79-80/94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 零测试（cov missing）；g1.py:174-190 与 dispatch_helper 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立** | run_g1/run_g2（executor.py:111-139）无 returncode 检查；rc!=0 且 stdout 空 → 裸 JSONDecodeError。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | P2 | **成立；支持异议→建议 P1** | codex.py:20-48 不写 current_scorer_agent；dispatch_helper.py:1934-1945 run_gate_g3 在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（read 复证）；G3.4（g3_independence.py:20-22 fail-closed）以生产方自写证据放行。反证尝试（独立路径不跑 G3）不成立——pipeline 主路径真实触发。 |
| F215 | P2 | **成立** | 运行时复现：`\{[^{}]*\}` 对嵌套 JSON 取内层（`{"final_score":90,"dimensions":[{"num":1,"score":88}]}` → 匹配 `{"num":1,"score":88}`）。 |
| F216 | P1 | **成立，P1 维持** | 运行时复现：空 customRules + disabled dim → `GenreConfig.model_validate` PASS（genre_config.py:94 `if disabled and self.custom_rules:`）。反证尝试（空规则无真实触发面）→ 新轮次 stub customRules 可空，路径真实可达。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | 实测 SKILL.md 内 5 处 CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :187 15-30 / :192-194 15-25·30-40 开卷·15-25）+ docstring 20-30 + 模型 15-35；模型对开卷 36-40% 反而**误拒**（SKILL.md:193 开卷允许 30-40）——模型既过松又过严，权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | 运行时复现：部分匹配 → 仅命中 section、matched=True、无 WARN（`_filter_md:55-64`）；AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入真空 PASS 复现（g1.py:281-282 G1.0 SKIP + 整体 PASS）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` 缓存固化空集复证。 |
| F220 | P2 | **成立（计数修正维持：6 条）** | ownership.py:73-94 实测 6 条目（含 foundation-review read 条目）；read_keys 死声明（review3 M）复证（grep `.read_keys` 仅定义处）。 |
| F221 | P2 | **成立** | 运行时复现：fixture 9 键含 tropeInventory，`GenreConfig.model_validate` PASS 且 tropeInventory 被静默丢弃（extra=ignore）；SKILL.md:286/:290 声称 8 键；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段 + docstring "8 camelCase keys"。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性）/g1（glob 未匹配）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except。 |
| F227 | P1 | **成立，P1 维持** | fresh round 门序复现（read + probe 推理）：G1 校验尚不存在的 novel.json、G2 校验尚不存在的输出；`git show 99f91f0:tests/dispatch-subagent.sh` 注释 "the skill execution happened earlier" 证实原语义为校验**已产出**输出；first-novel.md:38-41 round-exec.sh 只建脚手架。反证尝试（缺 novel.json 是预置问题）→ 文档化流程第一步即失败。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | 重试轮复现：底层 .md 已存在（模拟创建），`gate_G2(derive_output_files(...), "chapter", rd)` 仍 FAIL——`truth/*.md` 字面路径 not-found + JSON/散文误套 chapter 规则叠加；`derive_output_files` 不展开 glob（audit/_shared.py:38-56）。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现（因 writes 含 chapter-N-decisions.json）；`gate_G2` decisions 分支（g2.py:83-84）对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | P2 | **成立** | 运行时复现：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者。 |
| F232 | P2 | **成立** | 运行时复现：`Selection(severity="high", basis="manual_override", rationale="")` → ACCEPTED；`Adjustment(rationale="   ")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线。 |
| F234 | P2 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三分支均不看 status；owned 文件删除零违规复读通过。 |
| F235 | P1 | **成立，P1 维持** | 运行时复现：`PROJECT_DIR = REPO_ROOT`（executor.py:26），watch 含 `truth/*.md` → snapshot 命中仓库根 tracked 文件 `truth/bridge_tracker.md`/`truth/character_matrix.md` → `未声明写入` violations（F236 匹配失败）→ 任何 worldbuilding 派发 rc=2；round_dir 真实写入不被审计。反证尝试（审计面意图为仓库根）→ codex.py:62 cwd 已改 round_dir + test_executor_audit 编码 PROJECT_DIR==round_dir 正确语义，反证不成立。 |
| F236 | P2 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs，registry globs（`truth/*.md`）恒判未声明；复读 + F235 probe 内联复现。 |
| F237 | P2 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |

**误报统计**：整体误报 **0 条**（F201-F237 共 37 条全部实质成立）；部分误报 1 条（F220 计数 5→6，实质成立）；表述修正 1 条（F211，实质成立）。

---

## 二、新发现（F238-F244）

### F238 | audit_writes 对 malformed `## hooks` YAML 裸抛 ScannerError/ValueError → dispatcher 审计链崩溃，"崩溃仍审计"保证失效 | 漏报（错误处理缺陷） | P2
- 证据: `src/shenbi/audit/write_audit.py:53-56`（对每个 post .md 无 try 地调 `parse_records`/`parse_markdown_table`）；`src/shenbi/records/parser.py:40-44`（`yaml.safe_load` 可抛 ScannerError；非 list 抛 ValueError）；`src/shenbi/dispatcher/executor.py:263-268`（`audit_writes` 在 finally 中调用，异常直接传播、无捕获）；executor.py:245-246 自述 "if dispatch() crashes mid-write, still run the post-snapshot + audit so write overreach is caught even on failure paths"
- 根因: 审计链把 .md 内容解析当纯函数，未考虑 skill（LLM 驱动）产出 malformed YAML block 的现实；解析异常绕过整个审计记录（`record_audit_outcome` 永不执行 → write-audit.jsonl 无 GATE_FAIL）
- 验证命令: `PYTHONPATH=src .venv/bin/python` heredoc → `audit_writes("shenbi-foreshadowing-lifecycle", {…: None}, {…: "## hooks\n| id | state |…"})` → `CRASH: ScannerError while scanning a block scalar`；`## hooks\n{not: a list}` → `CRASH: ValueError ## hooks block 必须解析为列表`；两例均无 ledger 写入
- 建议方向: `audit_writes` 对 parse 异常 try/except → 记为 violations（"pending_hooks.md 解析失败"）而非崩溃；parser 内部对 ScannerError 降级为空记录 + WARN

### F239 | derive_file_type truth/decisions 分类 glob 盲区：精确字符串交集，`truth/*.md`/`snapshots/chapter-NNN/*` 写者被误归 "chapter" | 漏报（校验错位+文档自相矛盾） | P2
- 证据: `src/shenbi/dispatcher/executor.py:81-85`（`outputs & _truth_file_set()`/`& _decisions_file_set()` 为**精确字符串**交集）；`executor.py:64-70` docstring 自称 "ARTIFACT -> truth **iff** the skill writes/updates a file that truth-files.yaml lists as kind=truth"；`registry.py:37`（bootstrap_registry 仅 concepts 名）；`docs/framework/truth-files.yaml:120`（`truth/*.md` 是 glob 非 concept）；`graph.py:20-39`（`normalize_to_glob` 已存在但此处未复用）
- 根因: 分类逻辑未做 glob 展开/匹配；glob 写路径（`truth/*.md`）与 concept 精确名永不相等 → 9 技能中 4 个（worldbuilding/sequel-writing/truth-sync 的 `truth/*.md`、snapshot-manage 的 `snapshots/chapter-NNN/*`）被归 "chapter" → G2.11 truth .bak diff 永不运行 + chapter 规则（G2.6/2.8/2.9）误套 truth 文件
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `derive_file_type("shenbi-worldbuilding") == "chapter"`、`"shenbi-sequel-writing"→"chapter"`、`"shenbi-truth-sync"→"chapter"`、`"shenbi-snapshot-manage"→"chapter"`（对照 state-settling/plant→"truth"）；重试轮 probe：`gate_G2(derive_output_files("shenbi-worldbuilding",5,rd),"chapter",rd)` → `truth/*.md` 字面路径被套 G2.5/2.6/2.8/2.9
- 建议方向: 分类时对 outputs 逐项 `normalize_to_glob`（或 fnmatch registry globs）后再与 concept 集比对；与 F228（G2 侧 glob 不展开）联动修复

### F240 | extract_chapter 仅识别英文 "chapter N"：中文 prompt（框架主语言）→ chapter=None → N/NNN 输出全部丢弃 → standalone G2 整体静默跳过 | 漏报（校验空洞） | P2
- 证据: `src/shenbi/contracts/paths.py:45-47`（`re.search(r"\bchapter\s+(\d+)\b", text, re.IGNORECASE)`）；`executor.py:161-162`（`chapter = extract_chapter(prompt)`）；`audit/_shared.py:50-53`（chapter=None → resolve_or_skip 丢弃 N/NNN 路径）
- 根因: 章节解析只覆盖英文格式；中文 prompt（AGENTS.md/技能/decisions 均中文为主）下 chapter=None → `chapters/chapter-N.md`、`chapter-N-decisions.json` 等从 G1 输入/G2 输出集中消失 → `if output_files:` 为空 → **G2 完全不执行**（校验空洞，非误拒）
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `extract_chapter("写第7章，主角觉醒") == None` 且 `derive_output_files("shenbi-chapter-drafting", None, rd) == []`（G2 skipped）；对照 `extract_chapter("Draft chapter 7") == 7` 且 outputs 含 chapter-7.md/decisions.json
- 建议方向: 支持中文章节表达（`第\s*(\d+)\s*[章卷]` 等）；或 CLI 增加显式 `--chapter` 参数；至少对无法解析的含占位符输出 WARN

### F241 | genre_config 规则 1 approval.decision 缺失/空值时静默 PASS；禁用 非 list / 替换建议 非 dict 时对应规则静默跳过 | 漏报（校验洞，F216 同类） | P2
- 证据: `src/shenbi/contracts/skills/genre_config.py:41-42`（`if decision and decision not in ("approved","rejected")` —— decision 缺失/空串时整段跳过）；`:50-52`（`isinstance(banned, list)` 守卫，非 list 跳过 >50 计数）；`:58-60`/`:69-71`（`isinstance(replacements, dict)` 守卫，非 dict 跳过替换规则）；SKILL.md 规则 1 "approval.decision must be 'approved' or 'rejected'"、规则 3/4 "Every 禁用/慎用 word must have >= 1 replacement"
- 根因: 三个校验器都在"输入缺失或类型不符"时静默跳过（与 F216 的 `if disabled and self.custom_rules:` 同类缺陷）——worldbuilding 创建的 genre-config stub（无 approval.decision 或精简字段）经 g4 `GenreConfig.model_validate` 放行，规则表声明形同虚设
- 验证命令: `PYTHONPATH=src .venv/bin/python` heredoc → ①`approval:{}` → PASS；②`"禁用": "notalist"*100` → PASS（计数跳过）；③`"替换建议": ["not-a-dict"]` → PASS（替换规则跳过）
- 建议方向: `if decision and ...` 改为决策存在性校验（缺失即 FAIL 或显式 WARN）；类型守卫改为结构校验（非 list/dict 即 FAIL），与 F216 修复合并补测试

### F242 | OWNERSHIP 矩阵 2/6 条目指向 DEPRECATED 技能（plant/track），生产写者 foreshadowing-lifecycle 无条目 → pending_hooks.md 记录级写保护对生产路径空转 | 漏报（死线/未接线） | P2
- 证据: `src/shenbi/contracts/ownership.py:82-93`（plant record_create + track/resolve/state-settling record_field 条目）；`skills/shenbi-foreshadowing-plant/SKILL.md:2-3`、`shenbi-foreshadowing-track/SKILL.md:2-3`（"DEPRECATED: Superseded by shenbi-foreshadowing-lifecycle (2026-07-19)... Do not dispatch"）；`skills/shenbi-foreshadowing-lifecycle/SKILL.md` contract（`updates: truth/pending_hooks.md, mode: append_dedup`）且 lifecycle 自述 "new hooks by this skill (Phase 3)" + "sole driver of hook lifecycle state"
- 根因: 矩阵条目在 plant/track 弃用前编写，弃用后未迁移；生产写者 lifecycle 无 OWNERSHIP 条目 → `check_write_ownership` 返回 []（ownership.py:111-112）→ 走 file-level 声明检查（write_audit.py:62-64），记录级写越权保护（F233/F234 同链）在生产路径整体空转；F233-F237 的探针对象恰是已弃用技能
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `sorted(OWNERSHIP.keys())` 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`；`get_ownership("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md") == None`；对照 plant 条目存在
- 建议方向: 为 lifecycle 增加 record 级条目（record_create+record_field 混合语义需扩展 level 或拆分条目）；删除/标注弃用技能条目；在矩阵迁移（支柱一续）完成前由 write_audit 对无条目 truth 写者补记录键集检查

### F243 | 独立 dispatcher 无独立评分者：PR-20 把原 shell 的"独立评分 subagent 派发（G3.4 wrapper）"改为"生成器自评" | 漏报（G3.4 结构性违反，F214 互补） | P2
- 证据: `git show 99f91f0:tests/dispatch-subagent.sh:1-3`（"Wraps subagent dispatch for scoring independence (G3.4)"——原 codex exec 接收第 4 参 **subagent_prompt**，是独立评分 session）；`src/shenbi/dispatcher/modes/codex.py:61-87`（codex exec 现接收 CLI prompt——first-novel.md:41 传生成 prompt "Build the world"——即**生成器**，分数从生成器 final message 的 `\{[^{}]*\}` JSON 解析）；`scoring.py:441`（`--subagent` 仅写 `"scored_by": "subagent"` provenance 字符串，无独立 agent）；`tests/unit/dispatcher/test_codex_mark_done.py`（fixture 编码"raw 文件即分数 dict"的自评语义）；AGENTS.md "Scoring MUST use an independent subagent (G3.4); dispatcher-scored results are invalid"
- 根因: PR-20 翻译把原脚本"生成器(他处) + 独立评分 subagent(codex)"两角色合并为单一 codex exec：既是生成器又产出分数 dict → standalone T1 结构上 scorer==generator，G3.4 无法满足；且生成 prompt 不含 rubric 维度 → 解析出的任意 JSON 大概率被 shenbi-score REJECT（scoring.py:425-432 exit 2）。与 F214 互补：F214 是"缺 current_scorer_agent 证据 + pipeline 伪造"，本条是"即使有 agent id，评分者就是生成者"
- 验证命令: `git show 99f91f0:tests/dispatch-subagent.sh | head -5`（脚本头自述 G3.4 wrapper）；read codex.py:61-116 + scoring.py:441；`grep -rn "shenbi-score\|subagent" src/shenbi/dispatcher/` → 仅 codex.py:99-116 一处（评分=机械加权，维度分来自生成器输出）
- 建议方向: standalone T1 恢复独立评分 subagent（生成后以 rubric 维度 prompt 派发第二个 codex session），或文档明示 T1 自评设计并让 G3.4 对 standalone 显式豁免；与 F214 修复联动（修复后 current_scorer_agent 应记录真实评分 agent id）

### F244 | G1.4 的 .bak 由 INPUTS 创建、G2.11 对 truth OUTPUTS diff → 输出非输入的 truth-updater G2.11 永不触发 | 漏报（保护静默失效，跨区） | P2
- 证据: `src/shenbi/gates/g1.py:97-99`+`:164`（`compute_backup_targets(skill_name, fps, ...)` 对 **fps=输入文件** 全量建 .bak）；`src/shenbi/gates/g2.py:290-313`（G2.11 对 **truth 输出** `bak_path(fp)` 找 .bak，不存在则静默无检查）；`skills/shenbi-state-settling/SKILL.md` contract（reads 仅 `chapters/chapter-N.md`，updates 6 个 truth 文件——输出∉输入）；executor.py:190-204（G1→G2 编排使该链成为 standalone 流程）
- 根因: 备份面（输入）与 diff 面（输出）不一致：只有"read 与 update 同一 truth 文件"的技能（如已弃用的 plant）G2.11 才生效；state-settling/memory-distill 等生产 truth-updater 的 truth 输出 .bak 从未创建 → 防删除保护（只增不删铁律）静默失效；pipeline 侧完全不跑 G2（grep pipeline/ 无 gate_G2）→ .bak 机制实际仅 standalone 生效且被 F227/F228 掩盖
- 验证命令: read g1.py:97-99,164,224-235 + g2.py:290-313；`grep -rn "gate_G2" src/shenbi/pipeline/` → 空；state-settling contract reads/updates 对照（输出集∉输入集）
- 建议方向: G1.4 备份面改为"contract updates∩truth 输出"（或 G2.11 对无 .bak 的 truth 输出显式 WARN 而非静默跳过）；与 F239（glob truth 写者分类错误）联动

### M 级观察（随文记录）
- `fields.py:1-2` docstring 声称 "Replaces 3 divergent matching semantics: ... check_fields_exist (exact)"，但 `gates/g1.py:102-144` 的 `check_fields_exist` 仍以 exact 匹配存在且**全仓零消费者**（grep 仅定义处 + 两处注释）——"替换"未完成，声明与代码漂移（M）
- `contracts/__init__.py:28-44` `__all__` 缺 legacy re-export 名（ContractError/Contract/OutputKind/load_contract/load_registry/requires_independent_agent）——`from shenbi.contracts import *` 与模块属性面不一致（M）
- `audit/snapshot.py:103-104` pre==post 时返回 `status="modified"`（未变更文件报 modified，语义误导；因无键/记录差异无功能影响）（M）
- `audit/snapshot.py:57-59` 对非 UTF-8 watched 文件 `read_text` 裸 UnicodeDecodeError，且 `snapshot_tree` 在 `dispatch_with_write_audit` 的 try 块外（executor.py:244）→ 无日志直接 traceback（M，F238 关联）
- `dispatcher/modes/codex.py` 不删除 `raw_out`（原 shell `rm -f "${RAW_OUT}"`，git show 确认）→ 每次派发遗留 raw 文件在 t1-reports/（M）
- `gates/g2.py:40` docstring 声明 `file_type: chapter|report|truth`，但全函数无 "report" 分支——`legacy.py:41` OutputKind.REPORT 声称 "G2 report-type" 无对应校验实现，report 输出仅得存在性/非空/UTF-8/占位符/末行检查（M，跨区）
- `audit/write_audit.py:52-56` 对每个 post .md（含未变更）都跑 parse_records/parse_markdown_table/drift 检测（perf，M）
- `executor.py:167-188` 与 g1.py:174-190 对同一 `SHENBI_G1_SKIP_READS` env 双重过滤（executor 先按 basename+存在性，g1 再按 glob 未匹配），且两者均只 fnmatch basename——含 `/` 的 skip pattern 两侧都不生效（F222 增补，M）
- `executor.py:196-204` G2.7 `_is_important_chapter(fp, str(PROJECT_DIR))` 用仓库根做重要章检测 → standalone 重要章上限（CHAPTER_WORD_CEILING）判定失效（被 F227 掩盖，M，F239 关联）

---

## 三、严重度异议（不修改前四轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1**（评分独立性证据被生产方伪造）：`run_gate_g3`（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history` → G3.4 fail-closed 与 G3.5 以生产方自写证据放行（本轮 read 复证：伪造 `scoring_history` 条目键为 `"agent"` 而非 G3.5 读取的 `"agent_id"`，prior_agents 恒空 → G3.5 PASS）。反证尝试（独立路径不跑 G3 故 P2）不成立。
2. **支持 F218 P2 → P1**（部分匹配静默丢字段）：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 实测）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）但对开卷过严（36-40 误拒，SKILL.md:193 允许 30-40）——权威区间未定（SKILL.md 内部 5 处自相矛盾），维持低置信供协调者裁决。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮（输出已存在）G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖故维持低置信。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**；运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → PASS（bug 复现）
  - F218 md 部分匹配 → 仅命中 section、matched=True、无 WARN
  - F230 json 键规范化缺失 → matched=False + 全文 + WARN（U+3000 声明键）
  - F232 Selection/Adjustment 空串 rationale → ACCEPTED；routine+low 空串被拒（不对称）
  - F215 嵌套 JSON → 取内层 `{"num":1,"score":88}`
  - F235 审计面：仓库根 watch 命中 tracked `truth/bridge_tracker.md`/`truth/character_matrix.md` → violations → rc=2 语义复现
  - F228 重试轮：底层 .md 存在仍 FAIL（glob 字面 not-found + 误套规则）
  - F238 audit_writes malformed ## hooks → ScannerError/ValueError 崩溃、ledger 无记录
  - F239 derive_file_type 4 个 glob 写者全归 "chapter"
  - F240 中文 prompt → chapter=None → chapter-drafting outputs=[] → G2 跳过
  - F241 genre_config ①空 approval PASS ②禁用非 list PASS ③替换建议非 dict PASS
  - F242 lifecycle 无 OWNERSHIP 条目、plant/track 条目存在但已弃用
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243 的 cwd/角色差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c。

---

## 五、统计

- 新 finding：**7 条**（F238-F244：P2×7）；M 级观察 9 条随文
- 误报：**0 条整体误报**（F201-F237 共 37 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持）
- 严重度异议：**4 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1、F217→P1（低置信）、F228→P1（低置信）
- 前四轮 findings 判定：F201-F237 共 37 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否**（F238-F244 共 7 条新发现；其中 F238 审计链崩溃、F239 分类错位、F240 中文校验空洞、F242 保护空转、F243 G3.4 结构性违反均独立于前四轮已列缺陷）
