# Z1 分区第十轮独立复核报告（agent k）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`（F114–F122）+ `Z1.review2.md`（F123–F127）+ `Z1.review3.md`（F128–F133）+ `Z1.review4.md`（F134–F139）+ `Z1.review5.md`（F140–F143）+ `Z1.review6.md`（F144）+ `Z1.review7.md`（F145）+ `Z1.review8.md`（F146–F147）+ `Z1.review9.md`（F148–F153），共 **53 条**（F101–F153，其中前九轮新发现 40 条）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `gates/cli.py`、`gates/g5.py`、`gates/g2.py`、`gates/g4/generic.py`、`gates/shared.py`、`audit/_shared.py`、`dispatcher/executor.py`、`dispatcher/modes/codex.py`、`orchestration/scoring_bridge.py`、`tests/tiers/deps.json`、`command-to-give.md`、pyproject.toml、worldbuilding/_template/chapter-drafting rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`** 与 `PYTHONPATH=src` 纯内存解析器/函数复跑）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`，临时文件仅写 /tmp）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F154–F199（实际产出 F154–F155，共 2 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：2**（F154–F155，均 M；**无新 P0/P1**）
- **误报数：0**（F101–F153 全部 53 条成立；无一条被证伪。F115/F140 两个 P1 经本复核**独立实跑/复跑确认无反证**）
- **严重度异议数：新增 0**（维持既有 1 条：F109 的 M→P2 共识；F115/F140 的 P1 无反证维持；F134 维持 P2 的理由复核后认可）
- **0 新 finding 判定：否**（2 条 M 级漏报：F154 为 safe_write 双锁原语互斥洞、F155 为 scoring 双评员/塌缩函数数值边界缺陷——前九轮对两处均只做了"签名匹配/原语内竞态"层面检查，未覆盖）

---

## 1. 复核结论表（F101–F153 逐条判定）

| 编号 | 标题 | 前九轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 实证：`git ls-files -s docs/framework/dependency-dag.json tests/tiers/deps.json` → 100644 vs `ls -la` → `-rw-------`；根因确认 mkstemp 固定 0600 + os.replace 继承（safe_write.py:105,111）；git 不追踪 rw 位 → CI 幂等检查不可见 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | 重读 error_guidance.py:24-50：5 个顶层 docs/*.md 全 MISSING（docs/framework/ 下 gates.md/dispatcher.md 无对应锚点）；`ls tests/build_registry.py` → No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 仅 doc_url 破） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立 AST 扫描：exceptions.py 层级内直接 RAISED 恰为 DispatchWriteFailureError/DispatcherError/RetryExhaustedError/SubAgentProtocolError/SubAgentTimeoutError（5/22）；error_guidance/recovery 引用的 6 类全无站点；`grep get_guidance/ERROR_GUIDANCE/RECOVERY_STRATEGIES` src/ 除定义文件外 0 消费者 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | 走查 :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117 `str(None)`；独立走查 g5.py:115 `if project_dir:` 对真值字符串 "None" 成立 → `Path("None")` → G5.3 三项扫描全空 → **PASS（g5.py:202-203，非 R1 所称 SKIP）**；对无 expected_outputs 的 phase，G5.4 循环空、G5.5 SKIP → G5 整体可假 PASS。R3 对 R1 的事实修正独立确认 |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一无 exc_info 的 `except Exception`；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | grep phase_runner 全文 0 引用；仅 gates/cli.py:131-136 手动入口 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——抛 OSError 时 finally 不执行 → flock fd 泄漏；:89-91 unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M（R1 起异议 M→P2） | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert；docstring :110 "Raises on mismatch" 与 `-O` 下守卫消失冲突；工具仍会静默写 deps.json（:201）。**同意 R1–R9 共识：M→P2**（运行时正确性守卫而非文案；R4 注记"同源派生 → 近乎恒真"成立，失效风险在重构场景） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo("` src/ 0 命中；同族 `backup()`（paths.py:33-35）亦 0 调用方 |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `issubclass(subprocess.TimeoutExpired, OSError)==False`（mro 实测）；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有 |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **独立复跑真实解析器**：`load_applicability(worldbuilding)` → `{}`（表头 `\| # \| Dimension \| Bug-hunt Standard \| Clean Standard \|` 非解析器认的 `\| Dimension scope \|`，worldbuilding rubric:36-43 实证）；`filter_dimensions_by_test_type(dims, worldbuilding, 'bug-hunt')` → 返回全部 9 维（**含 dim 4 Prose quality，10% 权重，rubric:43 明示 "N/A — exempted … scoring.py renormalizes weights for remaining applicable dimensions"**）；对照组 chapter-drafting（"Dimension scope" 头）过滤确实生效（bug-hunt → [1,2,3,4,5,8,10]，权重和 75）。command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径。**反证尝试全部失败**（协议路径/测试目录/rubric 正文明示均核实）。按决策表"正常路径可复现功能错误"维持 P1 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径 G2 FAIL → return 1；command-to-give.md:48 "只在 G2 和 G4 都通过后…评分" 协议与相位机路径不符 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-121 G4 bug-hunt/clean 分支不写标记（仅 :121 generative 分支写，且 shared.py:195 status≠PASS 不写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 `-{test_type}` 拼接 |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关 → G2/G4 校验覆盖非确定 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `steps.append`；其余 5 命令均记录 |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS/capability_fs src/ tools/` → 仅自身 + 测试；0 生产消费者 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；缺参 → IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"`（排除 .git）→ 0 命中；command-to-give.md:48 原文引用 `bash tests/dispatch-subagent.sh`；ADR-0009 已迁移 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | **实跑实证**（见 F140 验证输出）：不带 --subagent 的文档化评分命令输出 `"scored_by": "interactive"` |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均合法 JSON → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫；对照 :300 `gate_type` 有 `idx+1 < len` 守卫 |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 status |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立** | :145-159 只查空 scores 不查空 dims；实测 `validate_scores({1:100,2:95}, [])` → `(True, [WARNING…])` + `compute_score([],…)` → 0 FAIL。R8 补充的 command-to-give.md:55 "dimensions 为空 → exit 2" 契约证据成立——但触发需 rubric 不可解析（82 份真实 rubric 均正常解析），按 R8/R9 维持 P2（本复核独立确认无真实 rubric 触发，不议升 P1） |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 NaN 字面量 → NaN 通过校验并产出 NaN final |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src/ → 仅 status.py:78 定义，0 消费 |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志（重读确认）；g4/generic.py:46-47 空文件 → SKIP 后 `passed(...)`（vacuous PASS，且写 PASS marker → pre-score 假通过） |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | `grep -c "log\."` 两文件均 0（排除 import 行） |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块（ls 实证） |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **独立实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**：完整 10 维 scores + `--test-type generative` → 计算完全成功但 **EXIT CODE: 1**，stdout 为合法 JSON，stderr 1189 字节为完整 result dict repr（`sys.exit(main())` → `SystemExit(dict)` → exit 1）；command-to-give.md:57-61 显式契约 "0 = 评分计算成功，结果写入 stdout。Dispatcher 将 stdout 保存到 t1-reports/..." 被违反（成功与错误同码 1 不可区分）；codex.py:105-108 `if result.returncode != 0: return result.returncode` → `_record_completion` 永不可达；`scoring.main` 无库调用方（排除"返回 dict 是 API 设计"）。pyproject.toml:60 `shenbi-score = "shenbi.scoring:main"` 实证。**反证尝试全部失败** |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | `isinstance(True, (int, float))` → True；`True < 0 or True > 100` 恒 False → 校验通过 → 按 1 计分 |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | filter docstring :107-109 宣称 "renormalize weights" 但 :135-136 仅过滤；compute_score :171-172 以 total_weight≠100 发 warning；实测 chapter-drafting bug-hunt 过滤后权重和 = 75 → 每条文档化路径恒误报 |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过；json.loads 仅对 .json 路径执行）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/缺 state 键/非法 JSON → 裸 TypeError/KeyError/JSONDecodeError traceback |
| F145 | phase_runner `load_deps()` 无任何错误处理 | M | **成立** | :32-33 `json.loads(...)` 无 try；:245/:318 调用；对照 g5.py:36-39 try/except → 结构化 FAIL、scoring.py:206 exists() 守卫——phase_runner 是 deps.json 的唯一无守卫消费者。定 M 恰当（fail-loud、无部分写入） |
| F146 | scoring.check_gate_markers t2 分支对 deps.json 仅 exists() 守卫：存在但损坏 → 裸 JSONDecodeError | M | **成立** | :205-207 `if deps_path.exists(): deps = json.loads(...)`——exists() 只防缺失不防损坏；同族三消费者中 scoring 站点漏报 |
| F147 | sync_contracts.main() deps.json 读取（:195）与 load_registry（:165）均无守卫 | M | **成立** | :195 `json.loads(DEPS_PATH.read_text(...))` 无 try；:165 `load_registry()` ContractError 裸上抛；对比同文件 :167-172 的 bail 有 log.error + return 1 |
| F148 | phase_runner post-skill 契约声明但磁盘缺失/为空的输出静默丢弃 | P2 | **成立** | :196-200 `exists()+st_size>0` 静默过滤缺失项无日志；:211-216 空 output_files → G2 SKIP + G4 空文件 vacuous PASS + PASS marker → 相位 OK 推进 |
| F149 | scoring filter 对无维度数字的排除 scope 静默 no-op（模板自身触发） | P2 | **成立** | 独立复跑 `_template/rubric.md`：表头 `\| Dimension scope \|` 正确匹配（属 44 个已解析之一），"Prose/narrative quality" 行标 No 但 scope 无维度数字 → excluded_nums 恒空 → no-op（`== True` 实证）；R2 备查 #5 "44 个未触发" 与事实不符（模板属 44 个之一且确实触发） |
| F150 | phase_runner run_gate("G2") 缺第 4 参 project_dir → G2.7 重要章节 ceiling 恒失效 | M | **成立** | :213 3 参；gates/cli.py:60-63 G2 分支支持 `pd = arg(3, None)`；g2.py:237 `_is_important_chapter(fp, project_dir or "")` + :382-384 `if not project_dir: return False`——相位机路径与 executor 传参（REPO_ROOT）均非真实项目目录 → 恒 False |
| F151 | scoring.py:419 scores.json 非数字键静默丢弃且无 WARNING | M | **成立** | :417-419 `{int(k): v for k, v in raw.items() if k.lstrip("-").isdigit()}`——非数字键在 validate_scores 之前被静默过滤；实测 `{"1": 90, "dimensin1": 100, "notes": "x"}` → `{1: 90}` |
| F152 | load_rubric kill-switch 解析漏 "→ detection dimension = 0" 类条目 | M | **成立** | :43-47 只匹配 "total score = 0"/"phase = 0"/"pipeline = 0"；实测 worldbuilding 捕获集缺失 `- Evidence without file+line citation → detection dimension = 0`（12+ 份 rubric 同） |
| F153 | sync_contracts.render_body_view 的 `skill` 参数从未使用（死参数） | M | **成立** | :124 定义 + :149 传参外，函数体内 `skill` 0 命中 |

**前九轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 **确认 R3 修正正确**；F101 波及文件数递增记录属实；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致。**无新的前轮事实错误需要修正。**

---

## 2. 新发现（F154–F155）

F154 | safe_write flock 主路径与 O_EXCL 回退路径互不感知对方锁原语：flock 持有者与 lockfile 持有者可并发写同一目标（第四处并发缺陷站点，前九轮只覆盖原语内竞态） | error | **M**（触发面窄；合并层若认为 EINTR 路径现实可达可议升 P2）
- 证据: src/shenbi/safe_write.py:54-62（flock 分支：`os.open(path.parent)` + `fcntl.flock(fd, LOCK_EX)` → 返回 `(fd, None)`，**从不检查 lockfile 存在性**）；:63-91（O_EXCL 回退：`os.open(lockfile, O_CREAT|O_EXCL|O_WRONLY)` → 返回 `(fd, lockfile)`，**从不检查目录 flock 状态**）；:61（`except (ImportError, OSError): pass` 是 flock→O_EXCL 的唯一切换点）；:103-105（两分支汇合后 mkstemp + os.replace 同一写入链）
- 根因: 两个锁原语不共享互斥语义——flock 锁在**父目录 fd** 上（per-directory），O_EXCL 锁在 **per-target 的 `.lock` 文件**上（existence-based），且互不探测。同一平台上通常行为一致（POSIX 全 flock / win32 全 O_EXCL）→ 混合场景仅当 flock 对单个写者**间歇失败**时出现（CPython 的 flock 不自动重试 EINTR，信号到达时抛 OSError → 落入回退），此时另一写者仍持目录 flock → 两写者对同一目标并发 `os.replace` → 后写者赢、先写者更新静默丢失。F108（mkstemp 锁泄漏）/F119（takeover 破坏活跃 O_EXCL 锁）/F133（释放路径删新持有者锁）均为**原语内**竞态，本条是**跨原语**互斥失效——前九轮并发分析未覆盖该站点
- 验证命令+输出: 代码走查 :54-91；`grep -n "lockfile\|flock" src/shenbi/safe_write.py` → flock 分支（:59）与 lockfile 分支（:64-91）互不引用；逻辑模拟（无文件写入）：`flock 持有者 A 写 target；B flock 抛 OSError → 走 :68 O_EXCL 建 lockfile → A/B 同时 os.replace(target)` → 无任何互斥机制阻止该交错
- 建议方向: 统一锁原语——O_EXCL 回退前先探测目录 flock（不可行则接受弱化并在 docstring 声明）；或 flock 失败时也创建 lockfile 并**先检查** lockfile（双原语全检查）；或 flock 分支对 EINTR 显式重试（`while True: try: flock ... except InterruptedError: continue`）；补"flock 失败→O_EXCL 与持锁者并发"测试

F155 | scoring.check_scorer_agreement 对 NaN 分数误报 agreed=True；flag_score_collapse 对单维度误报 all_identical（两函数数值边界缺陷；函数当前 dead（Z5 F500），接线前必须先修） | error | **M**（dead 代码内缺陷；若按 F500 接线进生产则升 P2）
- 证据: src/shenbi/scoring.py:240-246（`diff = abs(a - b)`；`max_diff = max(max_diff, diff)`；`if diff > threshold: disputed.append(dim)`——NaN 与任何数比较恒 False → NaN 维度**永不进入 disputed**，`max(nan, x)` 按迭代顺序返回 x 或 nan，`agreed` 由 disputed 空决定）；:262-267（`if len(set(values)) == 1: signals.append("all_identical")`——无 `len(values) >= 2` 守卫）
- 根因: ① `check_scorer_agreement` 用 `>` 比较而非 `math.isfinite` 前置校验——F135 的 NaN 若流入（json.load 接受 NaN 字面量 → 双评员分数可含 NaN），agreement 静默判定"一致"，仲裁信号消失（`{"1": NaN}` vs `{"1": 95}` → `agreed=True, max_diff=0.0`，实测）；② `flag_score_collapse` 的 `len(set(values)) == 1` 对单元素 scores 恒真——单维度 rubric（或稀疏 scores）恒报 collapse。前九轮对两函数仅验证"签名与 scoring_bridge 匹配"（Z1.a.md:186 声称"由 orchestration/scoring_bridge.py 消费（签名匹配）✓"），从未检查函数体数值边界；且 scoring_bridge 本身无生产消费者（Z5 F500 已覆盖接线空洞，本条为其**接线前置缺陷**）
- 验证命令+输出: `PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=/tmp/zb_r10/pycache PYTHONPATH=src python3 -c "from shenbi.scoring import flag_score_collapse, check_scorer_agreement; print(flag_score_collapse({1: 95})); print(check_scorer_agreement({1: float('nan'), 2: 90}, {1: 95, 2: 90}))"` → `{'collapse_suspected': True, 'signals': ['all_identical']}` / `{'agreed': True, 'max_diff': 0.0, 'disputed_dimensions': []}`（95 分差距被 NaN 掩盖为"一致"）
- 建议方向: ① agreement 入口加 `if not all(math.isfinite(v) for v in [*scores_a.values(), *scores_b.values()]): return {"agreed": False, "error": "non-finite score"}`（或视为 disputed）；② collapse 的 all_identical 加 `len(values) >= 2` 守卫；补 NaN/单维度单测；修复顺序应在 F500 接线之前

---

## 3. 严重度异议汇总

| 编号 | 前九轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有异议）** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移（CI 幂等检查为唯一间接兜底）。按决策表 P2"防御深度"而非 M"文案" |
| F115 | P1 | **维持 P1（无反证）** | 独立复跑真实解析器：worldbuilding applicability `{}`、filter 返回全 9 维（含 N/A 豁免的 dim 4，10% 权重）、rubric:43 明示 renormalize 承诺、4 技能均有 bug-hunt/clean 测试目录、command-to-give.md:64,71 文档化路径。**反证尝试全部失败**。按决策表"正常路径可复现功能错误"维持 |
| F140 | P1 | **维持 P1（无反证）** | 独立实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr 1189 字节 result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达；`scoring.main` 无库调用方。**反证尝试全部失败** |
| F134 | P2 | **维持 P2（不议升）** | command-to-give.md:55 确文档化 "dimensions 为空 → exit 2"，实现静默 0 分属契约背离；但触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析，R9 备查 #5 独立确认）——缺"正常路径可复现"，按 R8/R9 维持 P2 |

其余 49 条严重度无异议。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 全部独立通读；另核实 13 个跨区依赖文件/数据与 3 份真实 rubric）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型六方法逐一核实无洞；F123 覆盖无接线）、`cli_utils.py`（stdout 数据通道/stderr 日志分离自洽）、`logging.py`（双渲染器/stderr/`cache_logger_on_first_use=False` 自洽；SHENBI_LOG_FORMAT 非 "json" 静默降级为前九轮已记录低价值项，同意不立 finding）、`paths.py`（read/write 语义正确；F112 覆盖 repo()/backup() 死代码）、`error_guidance.py`/`recovery.py`（F103 覆盖 dead；F138 未用 log；import 顺序 E402 在 pyproject.toml per-file-ignores 列表（:156 起多处含 E402）→ lint 豁免，非缺陷）、`exceptions.py`（层级结构正确；F103 覆盖 dead）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（除 F127/F136 外词汇表自洽）、`sync_contracts.py`（F109/F117/F130/F147/F153 覆盖；`--project-dir` 约定 = `<round-dir>/project-output`（command-to-give.md:94,99,108），与 pre_score 硬编码 :262 一致，非漂移）、`phase_runner.py`（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150 覆盖）、`scoring.py`（F104/F106/F115/F118/F120/F126/F129/F131/F134/F135/F140/F141/F142/F146/F149/F151/F152/F155 覆盖）
- 新发现分布：`safe_write.py`（F154——双锁原语互斥失效）、`scoring.py`（F155——双评员/塌缩函数数值边界）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达（本复核独立确认 `grep render_body_into tests/` 0 命中）——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。
2. **filter 全排除回退**（scoring.py:136 `return filtered if filtered else dimensions`）：excluded_nums 非空但覆盖全部维度时，filtered=[] → 返回**未过滤原集**——"全部排除"≡"全部保留"，语义反转。当前 82 份 rubric 均含 Yes 行，无触发文件；与 F149（无数字 no-op）、R9 备查 #2（type_key 不匹配 no-op）同属 filter 家族潜在格式风险，未单独立 finding。
3. **scoring.py 数字键前导零重复**（:419 `{"1": 90, "01": 95}` → dict 推导后键 1 后者覆盖前者，静默丢 90）：F151 同族（键过滤期静默丢弃），极边缘，未单独立 finding。
4. **phase_runner 未知命令/usage 路径不 emit stdout JSON**（:339-343/:383-384 仅 stderr log + exit 1，对比 require_state F132 的"有 JSON 但缺 status"）：错误信封统一性家族的又一形态；shell 协议按 exit code 解析不受影响，未单独立 finding。
5. **`check_scorer_agreement`/`flag_score_collapse` 无生产接线**：Z5 区 F500 已覆盖（`validate_dual_scorer`/`check_single_scorer_collapse` 生产调用方=0、pipeline 层无双评员数据源），本复核确认不重复立 finding；F155 为其接线前置缺陷。
6. **AGENTS.md "67 functional + 2 meta = 69" 与现状 74 个 skills 目录不符**（R7 备查 #7 已记录）：非 Z1 区，供合并层转交。
7. **`--gate-only` G3 参数形状错配 / 无 --files vacuous G2 PASS / UNIMPLEMENTED 门 exit 0 / `--chapter abc` ValueError / `--interactive` 必须 argv[2] / find_flag 值不校验 / scoring 缺失 rubric/scores 裸 traceback / `--test-type` 无枚举校验**——R2/R3/R4/R5/R6/R7 备查已记录，同意不重复。

---

## 5. 其他说明

- 前九轮"P1×2（F115/F140）"结论：本复核独立实跑/复跑后**无反证，维持**；误报数 0（53/53 成立）。
- 新发现 F154–F155 均为 M 级，不改变"无 P0"结论，也不新增 P1；两者分别是 safe_write 并发家族（F108/F119/F133）之外的第 4 个独立缺陷站点与 scoring 双评员/塌缩函数（Z5 F500 dead 链）的接线前置数值缺陷。
- 跨区一致性核查：与 Z5 F500（scoring_bridge dead 链）无重复立 finding；与 Z3 F326/F382（safe_write 在 chapter_loop/pipeline 的消费方缺陷）无重叠；deps.json prerequisites 用全名（`shenbi-worldbuilding` 等）与 gates/cli SHORT_MAP 全名映射及 phase_runner/scoring 标记检查一致；command-to-give.md `--project-dir` 约定与 pre_score 硬编码一致。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140/F126 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本，`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`）**实证；F115/F134/F135/F141/F142/F149/F151/F152/F155 经 `PYTHONPATH=src` 纯内存导入复跑；F154 经纯逻辑模拟；临时文件仅写 /tmp，未触碰仓库文件。

---

## 6. 返回摘要

- **复核统计：新 finding 2（F154–F155，均 M）/ 误报数 0 / 新增严重度异议 0（维持 F109 M→P2 共识；F115/F140 的 P1 无反证维持；F134 维持 P2）**
- **0 新 finding 判定：否**（2 条 M 级新漏报：F154 safe_write flock↔O_EXCL 双锁原语互斥失效、F155 scoring 双评员/塌缩函数 NaN/单维度数值边界缺陷——均为前九轮未覆盖的独立缺陷站点；无 P0/P1 级新发现）
