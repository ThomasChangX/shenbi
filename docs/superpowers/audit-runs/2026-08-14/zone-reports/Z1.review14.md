# Z1 分区第十四轮独立复核报告（agent o）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`~`Z1.review13.md`（F114–F162），共 **62 条**
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `contracts/legacy.py`、`gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g4/*.py`（26 个 checker 逐一分类）、`gates/g6.py`、`audit/_shared.py`、`dispatcher/executor.py`、`dispatcher/modes/codex.py`、`tools/lint_status_strings.py`、`tests/tiers/deps.json`、`command-to-give.md`、82 份 rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`、端到端 phase_runner 穿越实证、G4 真实 gate 调用模拟（仅写 /tmp）、真实模块导入**）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑、真实控制台脚本实跑与 /tmp 内模拟（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F163–F199（实际产出 F163–F164，共 2 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：2**（F163 P2、F164 M；**无新 P0/P1**）
- **误报数：0**（F101–F162 全部 62 条成立；无一条被证伪。F115/F140/F158 三个 P1 经本复核**独立实跑/复跑/端到端实证确认无反证**）
- **严重度异议数：0 新增**（同意既有全部异议：F109 M→P2 共识、F150 R13 的 M→P2 升级——本复核独立验证 executor 阻断路径后认可、F115/F140/F158 的 P1 无反证维持、F134 维持 P2）
- **0 新 finding 判定：否**（2 条新漏报：F163 为 T2 流程 G4 目录错位——前十三轮对 G2 只覆盖了第 4 参（F150）、对 file_type 只覆盖了 decisions 分支（F156），从未检查 G4 的 round_dir-vs-project-output 目录语义，是本轮最重要发现；F164 为 emit 信封中唯一裸 GateStatus 字面量，初审"status 字符串 emit 用枚举成员"的不变量声明本身被此字面量证伪）

---

## 1. 复核结论表（F101–F162 逐条判定）

| 编号 | 标题 | 前十三轮严重度 | 复核判定 | 理由（本复核独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 实证：`git ls-files -s`（100644）vs `ls -la`（`-rw-------@`）；根因确认 mkstemp 0600（safe_write.py:105）+ os.replace 继承（:111）；git 不追踪 rw 位 → CI `git diff --exit-code` 不可见 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | 重读 error_guidance.py:24-50：5 个顶层 docs/*.md 全 MISSING；`ls tests/build_registry.py` No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 仅 doc_url 破） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立核实 error_guidance/recovery 引用的 6 类全无 raise 站点；两目录消费者仅测试；`get_guidance`/`RECOVERY_STRATEGIES` src/ 除定义文件外 0 消费者 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117/:307 `str(None)`；独立走查 g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **PASS（g5.py:202-203，非 R1 所称 SKIP）** |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一无 exc_info 的 `except Exception`（grep 实证）；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | phase_runner 全文 0 引用；仅 gates/cli.py:131-136 手动入口 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——抛 OSError 时 finally 不执行 → flock fd/.lock 泄漏；:89-91 unlink 后 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M（R1 起异议 M→P2） | **成立；同意 M→P2 共识** | sync_contracts.py:121 assert；docstring :110 "Raises on mismatch" 与 `-O` 下守卫消失冲突；工具仍会静默写 deps.json（:201） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 无单复数区分 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo(\|\.backup("` src/ 0 命中；read() 被 11 个 G4 checker 消费（21 处） |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | :153-154 无日志降级为空契约；对比 :74-75 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | `issubclass(subprocess.TimeoutExpired, OSError)==False`（mro 实测）；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两 subprocess 无 timeout |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **独立实跑真实解析器**：`load_applicability('tests/tiers/t1-skill/shenbi-worldbuilding/rubric.md')` → `{}`；`filter_dimensions_by_test_type(dims, p, 'bug-hunt')` → 全 9 维（**含 N/A 豁免 dim 4**）；worldbuilding rubric:43 明示 "scoring.py renormalizes weights…" 与实现不符；对照组 chapter-drafting（`\| Dimension scope \| Bug-hunt \| Clean \| Generative \|` 头，本复核全量扫描 44 个 Dimension-scope rubric 确认**全部**用该三列格式 → `capitalize()` 匹配生效，对照组过滤确实工作）；command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径（本复核确认原文存在）。**反证尝试全部失败** |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径阻断 → 双入口语义不一致 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | `grep _phase` 仅 :321,:331 两处赋值；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-110 G4 bug-hunt/clean 分支不写标记（仅 :121 generative 写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 `-{test_type}` 拼接 |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 `[...][:20]` 实证；rglob 顺序 OS 相关 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `steps.append`；其余 5 命令均记录（重读逐行确认） |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS src/ tools/` → 仅自身 + 测试 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；缺参 → IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"` 0 命中；ADR-0009 已迁移 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | **实跑实证**：文档化评分命令（不带 --subagent）输出 `"scored_by": "interactive"`；codex.py:99-106 恒传 --subagent（grep 实证）故生产路径正确 |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均通过 → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫；对照 :300 `gate_type` 有 `idx+1 < len` 守卫 |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立（维持 P2）** | 实测 `validate_scores({1:100,2:95}, [])` → `(True, [WARNING…])` + `compute_score([],…)` → 0 FAIL；触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析），缺"正常路径可复现" |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 NaN 字面量 |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src/ → 仅 status.py:78 定义，0 消费 |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志；g4/generic.py 空文件 → SKIP 后 vacuous PASS（本复核重读 generic.py:46-47 确认 `if not fps:` 仅 append SKIP check 且 mf 空 → passed） |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | 重读两文件：除 import 行外无任何 `log.` 调用 |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块 |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **独立实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**：完整 10 维 scores + `--test-type generative` → `final_score: 88.3 PASS (acceptable)` 完全成功但 **EXIT CODE: 1**，stderr 为完整 result dict repr（`sys.exit(main())` → `SystemExit(dict)`）；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 依赖退出码 → `_record_completion` 永不可达；`scoring.main` 无库调用方（grep 全 src 实证） |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | `isinstance(True, (int, float))` → True；`True < 0 or True > 100` 恒 False → 按 1 计分 |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | filter docstring :107-109 宣称 "renormalize weights" 但 :135-136 仅过滤；compute_score :171-172 以 total_weight≠100 发 warning；实测 chapter-drafting bug-hunt 过滤后权重和 = 75 |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过，无 json.loads、无 crash）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/缺 state 键/非法 JSON → 裸 traceback |
| F145 | phase_runner `load_deps()` 无任何错误处理 | M | **成立** | :32-33 `json.loads(...)` 无 try；:245/:318 调用；对照 g5.py:36-39 try/except → 结构化 FAIL |
| F146 | scoring.check_gate_markers t2 分支对 deps.json 仅 exists() 守卫 | M | **成立** | :205-207 `if deps_path.exists(): deps = json.loads(...)`——exists() 只防缺失不防损坏 |
| F147 | sync_contracts.main() deps.json 读取（:195）与 load_registry（:165）均无守卫 | M | **成立** | :195 `json.loads(DEPS_PATH.read_text(...))` 无 try；:165 ContractError 裸上抛；对比 :167-172 bail 有 log.error + return 1 |
| F148 | phase_runner post-skill 契约声明但磁盘缺失/为空的输出静默丢弃 | P2 | **成立** | :196-200 `exists()+st_size>0` 静默过滤缺失项无日志；空 output_files → G2 SKIP + G4 vacuous PASS |
| F149 | scoring filter 对无维度数字的排除 scope 静默 no-op（模板自身触发） | P2 | **成立** | 独立复跑 `_template/rubric.md`："Prose/narrative quality" 行标 No 但 scope 无维度数字 → excluded_nums 恒空 → no-op |
| F150 | phase_runner run_gate("G2") 缺第 4 参 project_dir → G2.7 重要章节 ceiling 恒失效 | M→P2（R13 升级） | **成立；同意 R13 的 M→P2 升级** | 独立走查：phase_runner.py:213 3 参；executor.py:25-26 `PROJECT_DIR = REPO_ROOT`、:122-134 传 4 参；g2.py:237,382-384 `if not project_dir: return False`；`ls outline/volume_map.md` → No such file（volume_map 实际在 novel-output/<project>/outline/）→ `_is_important_chapter` 两调用路径均查不到 → 恒 False → 重要章节 ceiling 恒 4500；executor 路径（非 pipeline 模式）G2 FAIL **当下 return 1 阻断**（executor.py:200-204）——R13 的 P2 升级依据独立确认，无异议 |
| F151 | scoring.py:419 scores.json 非数字键静默丢弃且无 WARNING | M | **成立** | :417-419 `{int(k): v for k, v in raw.items() if k.lstrip("-").isdigit()}`——非数字键在 validate_scores 之前被静默过滤 |
| F152 | load_rubric kill-switch 解析漏 "→ detection dimension = 0" 类条目 | M | **成立** | :43-47 只匹配 "total score = 0"/"phase = 0"/"pipeline = 0"；worldbuilding 等 12+ 份 rubric 的 `- Evidence without file+line citation → detection dimension = 0` 不被捕获 |
| F153 | sync_contracts.render_body_view 的 `skill` 参数从未使用（死参数） | M | **成立** | :124 定义 + :149 传参外，函数体内 `skill` 0 命中 |
| F154 | safe_write flock 主路径与 O_EXCL 回退路径互不感知对方锁原语 | M | **成立**（触发面窄，维持 M） | :54-62 flock 分支从不检查 lockfile 存在性；:63-91 O_EXCL 分支从不检查目录 flock 状态 |
| F155 | scoring.check_scorer_agreement NaN 误报 agreed；flag_score_collapse 单维度误报 all_identical | M | **成立** | `diff = abs(a - b)` 对 NaN 恒 False；`len(set(values)) == 1` 无 `len(values) >= 2` 守卫。函数当前 dead（Z5 F500） |
| F156 | phase_runner/executor 双路径对混合输出施加单一 derive_file_type → G2 decisions 分支整体跳过 .md | P2 | **成立** | **独立实证**：`derive_file_type('shenbi-chapter-drafting')` → `decisions`（本复核复跑确认）；g2.py:84 decisions 分支 `if not fp.endswith(".json"): continue` → .md 仅过 G2.1-2.3 |
| F157 | scoring 交互模式 `input()` 提示词写入 stdout 数据通道 | M | **成立** | :388/:397 `input()` prompt 经 sys.stdout 输出；:462 emit_json 追加同一 stdout → 混合体不可 JSON 解析 |
| F158 | phase_runner phase 参数路径穿越 | **P1** | **成立（P1 维持，无反证）** | **端到端实跑（本复核独立复现）**：`start ../../../../tmp/zb14_escaped --round-dir /tmp/zb14_r` → G5 FAIL（exit 1）但 **`/tmp/zb14_escaped.json` 被创建**（289 字节 state JSON，`"phase": "../../../../tmp/zb14_escaped"`，`find /tmp/zb14_r -type f` 0 个——写入完全逃逸 round_dir）。路径拼接 :37/:46-47 + 六命令直接透传 `args[0]` 无白名单 |
| F159 | sync_contracts 从不调用 configure_logging()：日志走 stdout + SHENBI_LOG_FORMAT 失效 | M | **成立** | **独立实证**：真实导入 `sc.log.info('sync_complete', skills=74)` → 捕获 stdout 有日志、stderr 空；`grep configure_logging src/shenbi/*.py` → sync_contracts 缺失（对照 phase_runner:337/scoring:283） |
| F160 | cli_utils.emit_json 不处理 BrokenPipeError | M | **成立** | :20-22 `sys.stdout.write` + `flush()` 无任何 except；emit_json 是全部 CLI stdout 数据通道唯一出口 |
| F161 | RoundPaths.write() 无任何生产调用方（死代码） | P2 | **成立** | `grep rp.write src/shenbi/gates/g4/*.py` → 0 命中；read() 21 处；write() 仅被同族死方法 backup() 与单测触达 |
| F162 | phase_runner cmd_post_skill 冗余调用 configure_logging() | M | **成立** | :185 + :337 两处；grep 全仓仅 phase_runner 有函数级重复；`cache_logger_on_first_use=False` 使重复配置幂等无行为差异 |

**前十三轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 **确认 R3 修正正确**；F101 波及文件数递增记录属实；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致（本复核独立重数）。**无新的前轮事实错误需要修正。**

---

## 2. 新发现（F163–F164）

F163 | phase_runner cmd_post_skill 的 G4 调用把 `round_dir` 传为 gate 第 3 参，而 T2 输出位于 `<round-dir>/project-output/`：rp.read 相对名查找的特定 checker（worldbuilding 等 9 个 genesis 技能）全部查不到输出 → G4 恒 FAIL → post-skill BLOCKED → 文档化 T2 流程（command-to-give.md 第六步）在首个 genesis 技能即永久阻塞 | error | **P2**（若 T2 已实际执行，按决策表"正常路径可复现功能错误"可议升 P1）
- 证据: src/shenbi/phase_runner.py:216（`run_gate("G4", [skill, ",".join(output_files) if output_files else "", str(round_dir)])`——第 3 参为 `round_dir`，而同函数 :189 已有 `proj = Path(project_dir)`（= `<round-dir>/project-output`，command-to-give.md:94,99,108 约定）却未传给 G4）；src/shenbi/gates/cli.py:112-121（G4 分支 `rd = arg(2)`、`project_dir=rd`——round_dir 被同时当作 project_dir）；src/shenbi/gates/g4/worldbuilding.py:20（`fps` 参数**仅声明、函数体 0 引用**）+ :30-110（全部经 `rp.read("novel.json")`/`rp.read("world/rules.md")` 等**相对名**在 round_dir 根下查找）；gates/g4/__init__.py:272（gate_G4 按 skill 名路由到 g4_worldbuilding）；command-to-give.md:120（对照：G6 协议 `shenbi-validate G6 <pipeline> <round-dir> <round-dir>/project-output`——**把 project-output 传为第 3 参**，而 phase_runner 的 G4 不这么做）
- 根因: phase_runner 是 T2 流程 G4 的**唯一调用站点**，传的是 round_dir 而非 project_dir；特定 checker 的 rp.read 相对名查找以 round_dir 为根，而 T2 协议把输出写在 `<round-dir>/project-output/`（pre_score :262 的 expected_outputs 检查也用 `round_dir/project-output`——框架自身两处对输出位置的认知一致，唯独 G4 调用点认知错位）。前十三轮对 G2 只覆盖了第 4 参缺失（F150）、对 file_type 只覆盖了 decisions 分支（F156）、对产出缺失只覆盖了 exists 过滤（F148），**从未检查 G4 第 3 参的目录语义**；且所有 phase_runner 测试用 fake_run_gate mock 掩盖了真实 gate 调用
- 验证命令+输出: ① 构造 `/tmp/zb14_g4r/project-output/{novel.json,genre-config.json,world/story_bible.md,world/rules.md}`（输出齐全）后按 phase_runner 原样调用 `shenbi-validate G4 shenbi-worldbuilding <files> /tmp/zb14_g4r`（即 `gate_G4(full_name, "generative", file_list, rd, project_dir=rd, ...)`）→ **`{"gate": "G4-worldbuilding", "status": "FAIL", "must_fix": ["G4.novel.not_found", "G4.genre_config.not_found", "G4.sb.not_found", "G4.rules.not_found", "G4.locations.not_found", "G4.truth.dir_not_found"]}`**（文件全在却全部 not_found）；② 反证对照组：第 3 参改传 `/tmp/zb14_g4r/project-output` → 核心文件检查全部 PASS（novel/genre/sb/rules）——证明机制为目录错位而非 checker 缺陷；③ `grep -n "fps" src/shenbi/gates/g4/worldbuilding.py` → 仅 :20 参数声明（函数体 0 引用）；④ 全量分类 26 个 checker：`rp.read` 查找的 checker = character_design(3)/faction_builder(1)/foreshadowing_track(1)/location_builder(1)/pacing_design(1)/plot_thread_weaver(1)/power_system(1)/relationship_map(2)/story_architecture(3)/volume_outlining(1)/worldbuilding(6)——其中 genesis 相位 11 个 prerequisite 中 9 个（worldbuilding/power-system/faction-builder/location-builder/character-design/relationship-map/story-architecture/volume-outlining/pacing-design）命中，drafting 相位含 foreshadowing-track 命中；⑤ deps.json 实证 genesis 相位 prerequisite 列表（python3 读取，见上）
- 建议方向: phase_runner.py:216 第 3 参改传 `str(proj)`（project_dir，与 G6 协议一致）；或 gates/cli.py:118 的 `project_dir=rd` 改为真实 project_dir（需 phase_runner 增传第 4 参）；同步修正 gates/cli G4 分支对 `project_dir` 的传递；补一个"T2 布局（输出在 project-output/）下 G4 特定 checker 通过"的端到端测试（不再 mock run_gate）。（严重度说明：按 F120/F156"文档化但未实际执行的路径 → P2"的先例定 P2；触发为完整执行 T2 协议（需全部 T1≥94 前置），且失败为 loud（BLOCKED）非静默数据损坏；若合并层确认 T2 已在运行，可依"正常路径可复现功能错误"议升 P1）

F164 | phase_runner.py:124 emit 信封 `"g5": "PASS"` 为裸 GateStatus 字面量：唯一裸状态字面量 emit 站点，逃逸 lint_status_strings 的键范围（仅查 status/state/classification），并证伪初审"status 字符串 emit 用枚举成员"的不变量声明 | error | **M**
- 证据: src/shenbi/phase_runner.py:124（`emit_json({"status": CommandStatus.OK, "phase": phase, "state": PhaseState.STARTED, "g5": "PASS"})`——同信封内 `status`/`state` 均用枚举成员，唯独 `"g5": "PASS"` 用裸字面量）；src/shenbi/status.py:3-5（"Emit sites use enum members through typed result structures"）；tools/lint_status_strings.py:25（`STATUS_KEYS = frozenset({"status", "state", "classification"})`——"g5" 键不在扫描范围，`"PASS"` 值永不触发）；Z1.a.md:133（初审不变量声明 "status 字符串 emit 用枚举成员（CommandStatus/PhaseState）"——被此站点证伪）
- 根因: "g5" 键携带 GateStatus 词汇表值但键名非 status/state/classification → lint 按设计豁免；实现沿用早期硬编码。若 gate 结果协议变化（如 PASS→WARN），此处硬编码值不会同步漂移
- 验证命令+输出: `grep -n '"g5"' src/shenbi/phase_runner.py` → `:124 "g5": "PASS"`（唯一）；`PYTHONPATH=src python3 tools/lint_status_strings.py` → 无输出（exit 0，确认 lint 不捕获该站点）；同文件其余 emit（:124/:129/:139-146/:166-175/:229-239/:254-275/:282-301/:316-333）均用枚举成员或 gate 返回 dict，仅 :124 为裸字面量
- 建议方向: 改 `"g5": GateStatus.PASS.value`（或 `"g5": g5.get("status")` 与 step 记录一致）；如需保留硬编码，至少与 step 的 `"g5_status": g5.get("status")` 统一来源（建议方向：直接用 g5.get("status")，消除两处状态来源不一致）

---

## 3. 严重度异议汇总

| 编号 | 前十三轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有共识）** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移 |
| F115 | P1 | **维持 P1（无反证）** | 独立实跑真实解析器：worldbuilding applicability `{}`、filter 返回全 9 维（含 N/A 豁免 dim 4，10% 权重）、rubric:43 明示 renormalize 承诺、44 个 Dimension-scope rubric 表头全量确认（`Bug-hunt \| Clean \| Generative`）、command-to-give.md:64,71 文档化路径实证存在。**反证尝试失败** |
| F140 | P1 | **维持 P1（无反证）** | 独立实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达；`scoring.main` 无库调用方。**反证尝试失败** |
| F158 | P1 | **维持 P1（无反证）** | 端到端实跑复现穿越写出（`/tmp/zb14_escaped.json` 被创建、round_dir 内 0 文件）。按 R12/R13 的决策表张力说明维持 P1（触发需操作者主动传路径形态 phase，合并层可议 P0，本复核不新增异议） |
| F150 | M→P2（R13 升级） | **同意 P2（无异议）** | 独立验证 executor 路径（非 pipeline 模式）G2 FAIL **当下 return 1**（executor.py:200-204），且 `volume_map.md` 不在 REPO_ROOT/outline/（实测 `ls outline/volume_map.md` No such file；实际位于 novel-output/<project>/outline/）→ `_is_important_chapter` 恒 False → 重要章节（4500-10000 字）经 dispatcher 路径当下误 FAIL。R13 升级依据成立 |
| F134 | P2 | **维持 P2（不议升）** | 触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析），缺"正常路径可复现" |

**新增严重度异议：0。** 其余 56 条严重度无异议（含 F163 的 P2 定级与 F164 的 M 定级；本复核不新增任何异议）。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py` 全部独立通读；另核实跨区依赖：contracts/legacy.py、gates/cli.py、gates/shared.py（resolve_input_path/write_gate_marker）、gates/g2.py、gates/g4/__init__.py、gates/g4/generic.py、全部 26 个 g4/*.py checker、gates/g6.py、audit/_shared.py、dispatcher/executor.py、dispatcher/modes/codex.py、tools/lint_status_strings.py、tests/tiers/deps.json、command-to-give.md、82 份 rubric）
- 判定无新缺陷的文件：`__init__.py`（F111/F139 覆盖）、`capability_fs.py`（权限模型核实无洞；F123 覆盖无接线）、`cli_utils.py`（F160 覆盖唯一剩余分支；allow_nan 为 R11 备查已记录）、`error_guidance.py`/`recovery.py`（F102/F103/F138 覆盖）、`exceptions.py`（F103/F110 覆盖；AST 扫描独立复核）、`logging.py`（双渲染器/stderr 自洽；SHENBI_LOG_FORMAT 校验为前轮已记录低价值项）、`paths.py`（F112/F161 覆盖）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（F127/F136 覆盖）、`safe_write.py`（F101/F108/F119/F133/F154 覆盖；剩余分支为防御/平台边界）、`sync_contracts.py`（F109/F117/F130/F147/F153/F159 覆盖）
- 新发现分布：`phase_runner.py`+`gates/cli.py`+`gates/g4/*.py` 跨文件（F163——G4 第 3 参目录错位，T2 特定 checker 输出查找全 miss）、`phase_runner.py`（F164——唯一裸状态字面量 emit）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。**本复核新增第 2 个 must-test 空洞**：G4 特定 checker × T2 project-output 布局的端到端路径无任何测试（F163 根因即此）。
2. **`--gate-only` 对 G4 的参数形状同样错配**（scoring.py:304-305 恒传 `[gate_type, files, ftype]`，gates/cli.py G4 分支期望 `[skill, files, rd]`——`--gate-only G4` 时 files 被当 skill 名、ftype 被当文件串）：R7 备查 #4 只点名了 G3，G4 同族错配未点名；属同一根因（gate-only 调试模式的参数形状假设），并入该备查家族，不单独立 finding。
3. **executor.run_g1 的 subprocess 无 timeout**（executor.py:114-118）：F114 只覆盖了 phase_runner/scoring 的 subprocess；executor 为 dispatcher 区（非 Z1），且同样无 timeout——供合并层转交 dispatcher 区。
4. **特定 checker 的 `fps` 死参数族**：worldbuilding.py:20 的 `fps` 参数 0 引用（F163 证据之一），faction_builder/plot_thread_weaver 等 rp.read 类 checker 同样几乎不用 fps——若 F163 修复为传 project_dir，这些 checker 的 fps 参数仍形同虚设，属 F163 修复范围内的连带清理项，不单独立 finding。
5. **`--test-type` 大小写敏感与 marker 名交互**：`--test-type Generative` → filter 经 capitalize() 生效但 marker 检查拼 `G4-{skill}-Generative.json`（写侧恒小写）→ MARKER_MISSING；属 F120 家族（标记名命名一致性），用户传大写值为误用，不单独立 finding。
6. **AGENTS.md "67 functional + 2 meta = 69" 与现状 74 个 skills 目录不符**（R7 备查 #7）：非 Z1 区，供合并层转交。

---

## 5. 其他说明

- 前十三轮"P1×3（F115/F140/F158）"结论：本复核独立实跑/复跑/端到端实证后**无反证，维持**；误报数 0（62/62 成立）。
- 新发现 F163–F164：F163 为 P2 级 T2 流程功能阻塞（G4 目录错位），是本轮最重要发现——前十三轮对 G2 只查了第 4 参（F150）、对 G4 只查了标记名（F120）与空文件 vacuous PASS（F137），**G4 第 3 参的目录语义（round_dir vs project-output）从未被检查**，而它与 G6 协议（command-to-give.md:120 显式传 project-output）直接矛盾；F164 为 M 级裸状态字面量（同时证伪初审 :133 的不变量声明）。
- 跨区一致性核查：F163 与 G6 协议（传 project-output）、pre_score :262（`round_dir/project-output` 检查 expected_outputs）对照，确认框架对"输出位置"有两处正确认知、仅 G4 调用点一处错位；与 Z5 F500（scoring_bridge dead 链）、Z3 区 pipeline 消费方无重叠（pipeline/cli.py 不调用 phase_runner post-skill，grep 实证）。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140/F126 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**实证；F158 经**端到端实跑 phase_runner CLI（写目标仅限 /tmp）**实证穿越写出；F163 经**真实 gate_G4/g4_worldbuilding 调用模拟（/tmp 内构造输出目录）**实证 FAIL 与对照组 PASS；F115/F134/F135/F141/F142/F149/F151/F152/F155/F156 经 `PYTHONPATH=src` 纯内存导入复跑；F159 经真实模块导入捕获 stdout/stderr 实证；临时文件仅写 /tmp，未触碰仓库文件。

---

## 6. 返回摘要

- **复核统计：新 finding 2（F163 P2 / F164 M）/ 误报数 0 / 新增严重度异议 0（维持 F109 M→P2 共识、F150 的 R13 M→P2 升级、F115/F140/F158 的 P1；F134 维持 P2）**
- **0 新 finding 判定：否**（2 条新漏报：F163 phase_runner G4 调用传 round_dir 而非 project-dir → T2 流程中 9 个 genesis 技能 + drafting 的 foreshadowing-track 的特定 G4 checker 相对名查找全部 miss → G4 恒 FAIL → post-skill BLOCKED（端到端实证 FAIL + 对照组 PASS 证明为目录错位）；F164 phase_runner.py:124 `"g5": "PASS"` 唯一裸 GateStatus 字面量（逃逸 lint 键范围，证伪初审不变量声明）。均无新 P0/P1）
