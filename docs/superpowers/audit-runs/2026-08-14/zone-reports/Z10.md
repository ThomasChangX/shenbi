# Z10 分区初审报告（Z10 段）

> 审查人：Z10 初审 agent（只读）
> 范围：`.github/` + `pyproject.toml` + `justfile` + `uv.lock` + `tools/` + `scripts/` + `plugins/` + `benchmarks/` + `executor_config.toml` + `run_pipeline.sh` + `mkdocs.yml` + `cliff.toml`（清单 Z10.files 共 52 文件）
> 方式：全部 deep-read；配合 `grep`/`read`/`.venv/bin/python`（只读分析）与既有 lint 工具实际运行验证；未运行任何写入仓库的命令（仅读日志、读文件、运行只打印的 lint）。
> 发现编号段：F1200–F1299。
> 只读声明：本报告写入前/后均未创建/修改/删除任何仓库文件（除本段文件 Z10.md）；未 git add/commit。

---

## 0. 总览

- deep-read 文件数：**52 / 52**（清单全部覆盖）
- 未覆盖文件：**0**（清单每个文件都有本条报告）
- findings 数：**20**（P1 × 2，P2 × 14，M × 4）
- 低置信度文件：无（每文件均全文精读或逐行分析；关键断言均有命令输出佐证）
- 已知 findings 的 Z10 侧确认（均已在本报告 findings 段重新取证，编号见下）：F0-07（→F1215）、T9-01（→F1214）、T201（→F1201）、F0-02（→F1212）、T12-03（→F1204）、T13 系列 T1301–T1306（→F1211）、F624（→F1213）、T1107（→F1216）、D1-02（→F1218）。
- 覆盖缺口处置（d1-06-coverage-gaps.txt）：Z10 文件以配置/脚本为主，`tools/` 各 lint 均有对应单测（`tests/unit/tools/test_lint_contract_graph.py`、`test_audit_skill_descriptions.py`、`tests/unit/test_lint_{status_strings,repo_consistency,contracts,no_fs_mutation,no_forbid_with_computed_field}.py`、`test_migrate_contract.py`、`test_generate_autocheck_docs.py`）；**无测试**：`scripts/lint_contract_fields.py`（T201 佐证）、`tools/check_fixture_mirror.py`、`tools/compare_mutation_score.py`、`tools/pre-push-check.sh`。

---

## 1. findings（F1200–F1299）

### F1200 | lint_contract_graph.py 自称 "marquee CI mechanism / block PR"，实际未接入 ci.yml、pre-commit、pre-push-check.sh（T201 同类第二实例） | error | P2
- 证据：`tools/lint_contract_graph.py:7-11`（docstring "Failure semantics (the marquee CI mechanism): ORPHAN_READ → exit 1 (FAIL, block PR)"）、`:98-108`（main 注释 "ORPHAN_READ → stderr (FAIL, blocking)"）；`ci.yml:53-56`（仅 `lint_contracts.py` + `lint_repo_consistency.py`，无 graph）；`.pre-commit-config.yaml:63-67`（lint-contracts hook 仅 `tools/lint_contracts.py`）；`tools/pre-push-check.sh:27-34`（无 graph）；`tests/unit/tools/test_lint_contract_graph.py:4`（测试 docstring 同样声称 "ORPHAN_READ → CI FAIL (exit 1, block PR)"）
- 根因：graph 闭包检查只挂在 `justfile:53` 的 `lint-contracts` 配方（本地 `just check` 经 `just lint-contracts` 执行），CI/预提交/预推送三处门禁均未接线；docstring 与测试注释的 "block PR" 在 CI 面不成立。
- 验证命令+输出：`grep -n "lint_contract_graph" .github/workflows/ci.yml .pre-commit-config.yaml tools/pre-push-check.sh` → 0 命中（仅 justfile）；`grep -rn "lint_contract_graph" justfile` → justfile:53。
- 影响：契约图 ORPHAN_READ（契约断裂、下游不可靠读）可在无 CI 兜底的情况下合入；契约单信源层的唯一阻断式闭包检查形同虚设。
- 建议方向：将 `tools/lint_contract_graph.py` 并入 ci.yml 的 "Contract + repo-consistency lints" 步骤与 pre-commit `lint-contracts` hook；或修正 docstring/测试注释的 "block PR" 声明。

### F1201 | lint_contract_fields.py（B.5 字段漂移 lint）未接入 CI/pre-commit/pre-push/测试（= 已知 T201，Z10 侧重新取证） | error | P2
- 证据：`scripts/lint_contract_fields.py:20-21`（docstring "A declared field that no actual heading/key matches is a contract drift -> exit 1 (FAIL, blocks PR)"）、`:8`（"Hooked into ``just check`` via ``just lint-contracts``"）；`justfile:52-55`（唯一接线点）；`ci.yml:53-56`、`.pre-commit-config.yaml:63-67`、`tools/pre-push-check.sh:27-34` 均无；`ls tests/unit | grep contract_fields` → 0 命中
- 根因：B.5 lint 仅本地 `just check` 执行，无 CI 兜底；docstring 声称 "blocks PR" 在 CI 面不成立。
- 验证命令+输出：`.venv/bin/python scripts/lint_contract_fields.py` → "All contract field declarations match truth files."（当前干净，但无 CI 强制执行）；grep 三处门禁 0 命中。
- 影响：契约 reads 字段漂移可静默合入（契约单信源层唯一未被 CI 覆盖的 lint，T201 原话成立）。
- 建议方向：CI 增加步骤或补 pytest 兜底；同步修正 docstring。

### F1202 | ci.yml codegen-idempotency 对 `.codex-plugin/` 的 `git diff --exit-code` 检查空转（目录被 gitignore 且未入库，diff 恒空） | error | P2
- 证据：`ci.yml:88-89`（`git diff --exit-code -- tests/tiers/deps.json docs/framework/ skills/ \` + `.codex-plugin/`）；`.gitignore:20`（`.codex-plugin/` 忽略）；`git ls-files .codex-plugin` → 0 行；`ls .codex-plugin` → 目录不存在
- 根因：`git diff` 只比较已跟踪内容；`.codex-plugin/` 被 ignore 且从未入库 → 该路径永远无 diff，插件清单（plugin.json）生成结果无论怎样漂移都过 CI。
- 验证命令+输出：`git ls-files .codex-plugin | wc -l` → 0；`ls -la .codex-plugin` → No such file。
- 影响：`shenbi-generate-plugins` 产物新鲜度检查静默失效（与 F624/F1213 叠加：master.json 未列出的 skill 不会发布到插件清单，且无任何 CI 信号）。
- 建议方向：将 `.codex-plugin/` 从 .gitignore 移除并入库（或把 plugin.json 生成结果 diff 改为对临时目录/内存内容比较）；Z6.a 曾记 "未入库、生成无 CI 接线属运维面非缺陷"，本报告判定为 CI 检查空转缺陷。

### F1203 | ci.yml quality job 对 macOS 全 job `continue-on-error: true`，macOS 矩阵失败不阻断 CI | error | M
- 证据：`ci.yml:29-30`（`continue-on-error: >- ${{ matrix.python-version == '3.13' || contains(matrix.os, 'macos') }}`）；`:23`（matrix os: [ubuntu-latest, macos-latest]）
- 根因：注释（:28）称 macOS 容错针对 "uv sync timeouts"；但 continue-on-error 作用于整个 job —— macOS 上 lint/mypy/pytest 的任何真实失败都只留 annotation 不红 workflow。ubuntu 仍硬阻断，3.13 在 PR 矩阵不含（仅 main push 才有 3.13 行）。
- 验证命令+输出：读 ci.yml:28-30 确认 continue-on-error 表达式；matrix 表达式 `github.event_name == 'pull_request' && ["3.11","3.12"] || ["3.11","3.12","3.13"]`（:25-27）。
- 影响：macOS 作为 1/2 矩阵位，其 lint/测试失败可静默放行（与 "just check 是权威、workflow 不得漂移" 的本地一致性要求相悖——本地 macOS 上 `just check` 失败会阻止提交，CI 不阻止）。属文档化权衡但影响 CI 门禁一致性。
- 建议方向：macOS 容错收窄到 `uv sync` 单步，或接受并显式记录该权衡。

### F1204 | run_pipeline.sh 将 `$PROJECT_DIR` 插值进 `python3 -c "…"` 双引号串 → `'`/`"`/`$()`/反引号命令注入（= 已知 T12-03，Z10 侧确认 + 精确定位） | security | P2
- 证据：`run_pipeline.sh:31-38`（`CURRENT_STEP=$(python3 -c "\nimport json\ntry:\n d=json.load(open('$PROJECT_DIR/pipeline-state.json'))…"`）、`:70-79`（同样模式写 `pipeline-state.json`）
- 根因：PROJECT_DIR 源自 `$1`（:6），直接嵌入双引号 Python 源码串；`$PROJECT_DIR` 内的 `'`、`"`、`$(…)`、反引号可逃逸出字符串/命令边界执行任意命令。
- 验证命令+输出：代码精读 :31-38/:70-79；`tests/round-exec.sh:19,29,92,108` 存在同类 `python3 -c "${ROUND_DIR}"` 插值（T12 线程第二实例）。
- 影响：恶意/意外目录名可注入任意 shell 命令（本地运维脚本面，非 CI/服务端）；与 T12 线程结论一致（P2）。
- 建议方向：改用 `python3 -c` + 环境变量传参（`PROJECT_DIR="$PROJECT_DIR" python3 -c '…os.environ…'`）或 `json.dumps` 转义；round-exec.sh 同修。

### F1205 | run_pipeline.sh 错误自动放行启发式 `grep -q "escalation\|gate\|dispatch"` 过宽 → 真实门禁失败消息含 "gate" 即被 auto-approve | error | P2
- 证据：`run_pipeline.sh:88-91`（`error|failed)` 分支：`if echo "$OUTPUT" | grep -q "escalation\|gate\|dispatch"; then log "Auto-approving error..."; uv run pipeline review "$PROJECT_DIR" approve …`）
- 根因：三个关键词均为 pipeline 输出中的高频词（如 gate 失败信息 "G4 gate failed"、dispatch 错误、escalation 字眼），任何含其一的消息都触发自动 approve，绕过 checkpoint 人工/门禁决策。
- 验证命令+输出：代码精读 :88-91；构造示例：error 输出含 "gate dispatch failure" 即命中分支。
- 影响：自动化运行中质量门禁失败可被静默放行（脚本设计为 auto-approve，但关键词面显著过宽）。
- 建议方向：仅对明确的 checkpoint 阻断消息 approve，或对 error 态一律退出并人工处理。

### F1206 | run_pipeline.sh 绕过 `pipeline review` CLI 直接改写 pipeline-state.json（step_index+1、retry_counts 清零）→ 状态机不变量由外部脚本篡改 | error | P2
- 证据：`run_pipeline.sh:70-79`（`python3 -c "…cl['step_index'] = cl.get('step_index',0) + 1; cl['retry_counts'] = {}; json.dump(d, open('$PROJECT_DIR/pipeline-state.json','w'), indent=2)"`）
- 根因：stuck 3 次后脚本直接改状态文件前进，绕过 phase_runner/CLI 的 step 校验；retry_counts 整体清零可重置重试预算，step_index 越界不校验。
- 验证命令+输出：代码精读 :66-79；:79 错误输出被 `2>/dev/null` 吞掉（写失败静默）。
- 影响：pipeline 状态机不变量（step 顺序、重试计数、checkpoint 链）可被外部直接破坏；真实产物与状态不一致时排障困难。
- 建议方向：stuck 前进改由 CLI 支持（如 `pipeline review` 的跳过语义）或至少校验 step_index 范围与文件原子写。

### F1207 | codeql.yml 无 pull_request 触发 → 根 SECURITY.md:21 "CodeQL static analysis runs on every PR and weekly" 的 "every PR" 声明不成立（F0-07 家族第二半） | error | P2
- 证据：`codeql.yml:12-16`（`on: push:` + `schedule: cron '0 4 * * 1'` + `workflow_dispatch:`，无 `pull_request`）；`:9-11` 注释自述 "Triggers on PUSH (full-ref scan), not pull_request"；根 `SECURITY.md:21`（"CodeQL static analysis runs on every PR and weekly"）
- 根因：PR 检查列表无 CodeQL 结果（push 扫描覆盖 PR 分支提交，但 PR 界面/required checks 不含 CodeQL）；文档声明与触发面语义不符。
- 验证命令+输出：`grep -n "pull_request" .github/workflows/codeql.yml` → 0 命中；`:15` schedule 存在（weekly ✓）。
- 影响：供应链/静态分析声明失真（与 F0-07 的 pip-audit "weekly" 缺口同族：SECURITY.md 两项声明各有一半不成立）。
- 建议方向：改文档为 "on every push and weekly"，或在 PR 上补 pull_request 触发（注意注释中的 300-file 限制权衡）。

### F1208 | changelog 双机制漂移：release.yml 用 `git log` 平铺生成 release notes，cliff.toml + `just changelog`（git-cliff）未接入发布流程 | error | P2
- 证据：`release.yml:23-38`（`git log --format='- %s (%h)'` 生成 notes）；`justfile:98-99`（`changelog:` → `uv run git-cliff --unreleased -p CHANGELOG.md`）；`cliff.toml:1-36`（完整 git-cliff 配置，commit_parsers 分组 Added/Fixed/Documentation/…）；`CHANGELOG.md:1-8`（header 为 git-cliff 风格但多了 "and this project adheres to Semantic Versioning" 一句，与 cliff.toml header 不完全一致）
- 根因：发布流程未调用 git-cliff（dev group 含 `git-cliff>=2.4.0`，pyproject.toml:45），release notes 无 Conventional-Commits 分组；cliff.toml 成为未接线配置。
- 验证命令+输出：`grep -n "git-cliff\|git log" .github/workflows/release.yml justfile` → release.yml 仅 git log 三处；justfile:99 git-cliff；`sed -n '4,9p' cliff.toml` vs `head -5 CHANGELOG.md` 确认 header 差异。
- 影响：发布 notes 与 CHANGELOG.md 风格割裂；cliff.toml/CHANGELOG 头漂移会在下次 `just changelog` 时被改写。
- 建议方向：release.yml 改用 `uv run git-cliff -t <tag>` 生成 notes（与 just changelog 同源）；统一 CHANGELOG 头。

### F1209 | dependabot.yml:5 与 embeddings-smoke.yml:7 引用已移入 archive 的 spec 路径（`docs/superpowers/specs/…` 应为 `…/specs/archive/…`） | error | M
- 证据：`dependabot.yml:5`（"详见 docs/superpowers/specs/2026-08-02-pr20-followup-dependabot-and-embeddings-ci-design.md"）；`embeddings-smoke.yml:7`（"spec: 2026-08-02-pr20-followup-dependabot-and-embeddings-ci-design.md"）；`ls docs/superpowers/specs/2026-08-02-pr20-*` → 不存在；`ls docs/superpowers/specs/archive/ | grep pr20` → `2026-08-02-pr20-followup-dependabot-and-embeddings-ci-design.md` 存在
- 根因：spec 归档移动后配置文件内引用未更新路径。
- 验证命令+输出：`ls docs/superpowers/specs/2026-08-02-pr20-followup-dependabot-and-embeddings-ci-design.md` → No such file；archive 目录下存在同名。
- 影响：读者按注释路径找不到设计文档（轻微文档漂移）。
- 建议方向：两处引用补 `archive/` 前缀。

### F1210 | lint_contract_graph 的 dag_key 无法连接目录读（`benchmarks/anchors/`）与文件写（`benchmarks/anchors/AC-*.md`）→ 真实消费关系被误报 DANGLING_WRITE；registry 对同一资产的 producer 分类矛盾 | error | P2
- 证据：`tools/lint_contract_graph.py:88-93`（dangling 判定 `if key in … all_read_keys: continue`）；`src/shenbi/contracts/graph.py:42-59`（dag_key 归一化：registry glob 匹配不到则 `normalize_to_glob`）；实测 `dag_key('benchmarks/anchors/')` → `'benchmarks/anchors/'`、`dag_key('benchmarks/anchors/AC-*.md')` → `'benchmarks/anchors/AC-*.md'`（不相等）；registry：`benchmarks/anchors/` producer=`external`、`benchmarks/anchors/AC-NNN.md` producer=`skill`；`skills/shenbi-review-resonance/SKILL.md:19`、`skills/shenbi-score-{arc,stratum,volume}/SKILL.md` 均声明 `reads: benchmarks/anchors/`
- 根因：目录级读与文件级写在 dag_key 下分裂为两个键，dangling 检查判"无消费者"（假阳性 WARN）；同时 registry 把目录标 external（ORPHAN_READ 跳过）而文件标 skill 写——同一资产两种 producer 分类。
- 验证命令+输出：`.venv/bin/python tools/lint_contract_graph.py 2>&1 | grep anchor` → `DANGLING_WRITE (warn): skill=shenbi-anchor-curate writes=benchmarks/anchors/AC-*.md no consumer`（共 11 条 DANGLING_WRITE）；dag_key 实测如上。
- 影响：契约图把实际被 4 个评分/评审 skill 消费的锚点库判为悬挂写（噪音）；若未来有人据此"清理"锚点会误删活资产。
- 建议方向：dag_key 增加目录→`dir/**` 归一化，或 dangling 检查对 registry 声明 reads 的目录路径放行；统一 `benchmarks/anchors/` 的 producer 分类。

### F1211 | 依赖锁定卫生：dev group 冗余/休眠依赖 + 版本漂移（= 已知 T1301–T1306，全部确认） | deps | P2
- 证据：`pyproject.toml:30`（pytest-asyncio>=0.23.0 + `:440` asyncio_mode="auto"）、`:33`（pytest-ordering>=0.6）、`:14`（numpy>=1.26.0 核心依赖）、`:48`（setuptools>=81.0.0 dev）、`:3`（version 0.1.0 vs `plugins/master.json:3` version 0.2.0）、`:11`（pydantic 注释 "P-1 不用；为 P0 schemas 准备"）；`uv.lock`（pytest-asyncio 1.4.0 / pytest-ordering 0.6 / setuptools 83.0.0 / numpy 2.4.6）
- 根因：见各子项：
  - pytest-asyncio：全仓零 `async def test`/`@pytest.mark.asyncio`（仅 `tests/unit/test_test_density.py:31` 注释提及计数），asyncio_mode=auto 无任何生效面（T1301）；
  - pytest-ordering：零 `@pytest.mark.order`/import（T1302，0.6 为 2019 版无维护）；
  - numpy：唯一引用 `src/shenbi/pipeline/truth_embed.py:185`（函数内 import，Route B 可选路径）（T1303）；
  - setuptools：src/tools/tests 零引用（T1304，torch 传递依赖已带入）；
  - master.json 0.2.0 vs pyproject 0.1.0 无同步机制（T1305）；
  - pydantic 注释过期——实际 `src/shenbi/gates/g4/*.py`、`g2.py`、`trace/event.py`、`contracts/legacy.py`、`pipeline/dispatch_helper.py` 已大量使用 pydantic（T1306）。
- 验证命令+输出：`grep -rn "pytest.mark.asyncio\|async def test" tests/ --include="*.py"` → 0 命中；`grep -rn "pytest.mark.order" tests/ src/` → 0 命中；`grep -rn "import numpy" src/ tools/` → 仅 truth_embed.py:185；`grep -rn setuptools src/ tools/ tests/ --include="*.py"` → 0 命中；`grep -rln pydantic src/shenbi/` → 9+ 模块；`python3 -c` 解析 master.json version=0.2.0。
- 影响：dev 安装体积/解析时间虚增；asyncio/ordering 声明误导（"插件列表"含无用插件）；版本双源漂移无检测。
- 建议方向：删除 pytest-asyncio/pytest-ordering/setuptools 直接声明；numpy 移入 embeddings optional 或注释更新；master.json version 与 pyproject 统一（或由 sync 生成）；更新 pydantic 注释。

### F1212 | deps.json 契约缺 5 skill 登记（foreshadowing-lifecycle + review-group-{character,craft,factual,plan}），executor_config 引用之；lint_repo_consistency 无 skill↔deps.json 完整性检查（= 已知 F0-02，Z10 侧确认） | error | P1
- 证据：`executor_config.toml:26-27`（`[overrides."shenbi-foreshadowing-lifecycle"]`）；`tests/tiers/deps.json`（t2-phases/t3-pipelines/_out_of_pipeline 均无此 5 名）；`tools/lint_repo_consistency.py:41-46`（`_skill_files()` 仅收集 skills/*/SKILL.md，`main` 无 deps.json 交叉检查）
- 根因：契约三源（deps.json / skills 目录 / executor_config）不同步；lint 无 skill↔deps.json 完整性检查（body-ban/terminology/section-header/loader-uniqueness/dead-decisions 均不覆盖该不变量）。
- 验证命令+输出：`python3` 统计 → deps.json 引用 71 名，`_out_of_pipeline` 含 market-radar/sequel-writing/anchor-curate/escalation-review/foreshadowing-resolve/2 meta；skills/ 74 − 已登记 − meta − out_of_pipeline = **5 未登记**：`shenbi-foreshadowing-lifecycle`、`shenbi-review-group-character/craft/factual/plan`（与 F0-02 清单完全一致）；`.venv/bin/python tools/lint_repo_consistency.py` → rc=0（不报）。
- 影响：契约单信源被破坏且无检测；pipeline 可调度未登记 skill（executor_config 引用）而 T2/T3 契约无定义。
- 建议方向：lint 增加 skill 目录↔deps.json 闭包检查；登记 5 skill 或明确 out-of-pipeline。

### F1213 | plugins/master.json skills 清单 59 vs skills/ 74（15 缺），generate.py 无任何 skills/ 交叉校验 → 新 skill 静默不发布（= 已知 F624，Z10 侧确认） | error | P2
- 证据：`plugins/master.json:8-68`（59 条 skills）；skills/ 目录 74 项；`src/shenbi/plugins/generate.py:21-33`（仅 REQUIRED_FIELDS 与字段缺失检查）、`:52`（`out["skills"] = master["skills"]` 原样拷贝，无存在性/完整性校验）
- 根因：master.json 手工维护；generate.py 不校验 skills 条目对应文件存在、也不校验 skills/ 目录全覆盖 → 缺 15 skill（anchor-curate/book-spine-init/escalation-review/foreshadowing-lifecycle/foreshadowing-recall/memory-distill/review-arc-payoff/review-group-{character,craft,factual,plan}/review-resonance/score-{arc,stratum,volume}）无任何报错；叠加 F1202（CI 的 .codex-plugin diff 空转）→ 静默不发布。
- 验证命令+输出：`python3` diff → missing 15（如上清单），extra 0；读 generate.py 确认无 skills 校验分支。
- 影响：codex 插件清单缺 15 个已存在 skill；发布内容与仓库状态漂移且无检测（F0-02 同根因家族）。
- 建议方向：generate.py 增加 skills 条目存在性 + 目录闭包校验（缺一即 exit 1）；把 master.json 纳入 sync_contracts 生成或 CI 校验。

### F1214 | lint_status_strings 对词表外发明值全盲：status/state/classification 键上 8 文件 10 站点发射 STATUS_STRING_LITERALS 之外的字面量，lint rc=0（= 已知 T9-01，Z10 侧重新取证） | error | P1
- 证据：`tools/lint_status_strings.py:25`（STATUS_KEYS={"status","state","classification"}）、`:29-30`（`_is_status_value` 仅在 STATUS_STRING_LITERALS 内判定）；`src/shenbi/status.py:91`（19 个白名单值）；实测 10 站点：`src/shenbi/gates/g4/chapter_revision.py:97`（`"status": GateStatus.PASS if not issues else "HARD_FAIL"`）、`pipeline/truth_embed.py:247`（`"degraded"`）、`pipeline/cli.py:836`（`"not_implemented"`）、`pipeline/hook_planting.py:188`（`"state": "PLANTED"`）、`pipeline/truth_index.py:199`（`"state"`、`""`）、`trace/materialize.py:28`（`"pending"`）、`:59`（`"status"`、`"done"`）、`dispatcher/modes/codex.py:44`（`"done"`）
- 根因：lint 只查"值 ∈ 白名单"，词表外发明值（HARD_FAIL/degraded/not_implemented/PLANTED/pending/done/…）全部放行；status.py docstring（:1-6）声称 "THE single definition of every status string" 被 8 处违反而无检测。
- 验证命令+输出：`.venv/bin/python tools/lint_status_strings.py` → rc=0（0 输出）；自定义 AST 扫描（同 STATUS_KEYS 定义）→ `non-whitelist literal emits: 10`（逐站点如上）。
- 影响：状态词表契约被静默破坏；G2/G7/审计对状态字符串的依赖（精确匹配/分类）与真实发射值不一致，属"契约被静默违反"（与 T9-01 线程结论一致）。
- 建议方向：lint 增加"非白名单字面量"检查（任何 status/state/classification 键上的 str 字面量必须 ∈ STATUS_STRING_LITERALS 或经 enum 成员）；8 站点改用 enum 或扩白名单。

### F1215 | security.yml 无 schedule → 根 SECURITY.md:20 "pip-audit runs on every PR and weekly" 的 weekly 半句不成立（= 已知 F0-07，Z10 侧确认） | error | P2
- 证据：`.github/workflows/security.yml:1-5`（`on: push: branches [main] + pull_request:`，无 schedule）；根 `SECURITY.md:20`；`nightly.yml:19-21`（schedule 注释掉，仅 workflow_dispatch，且无 pip-audit job）
- 根因：供应链审计只在 push/PR 触发，无任何定期执行；nightly 全部 job disabled（windows-smoke/python-313-migration/doc-links，无安全审计）。
- 验证命令+输出：`grep -c "schedule" .github/workflows/security.yml` → 0；读 nightly.yml:18-21 确认 schedule 被注释。
- 影响：供应链漏洞审计频率声明失真（每 PR 有，weekly 无）。
- 建议方向：security.yml 加 `schedule: cron`（或并入 nightly），或改文档声明。

### F1216 | tests/benchmark/ 空洞（仅 __init__.py）+ pyproject norecursedirs "tests/benchmarks"（复数）指向不存在目录 + 唯一 benchmark 测试为 1+1 冒烟（= 已知 T1107，Z10 侧确认） | error | P2
- 证据：`pyproject.toml:414`（norecursedirs `"tests/benchmarks"`）；`tests/benchmark/__init__.py:1`（仅 docstring）；`tests/unit/test_pytest_framework.py:42-45`（`@pytest.mark.benchmark def test_benchmark_marker_works(benchmark): benchmark(lambda: 1 + 1)`）；`ls tests/benchmarks` → No such file
- 根因：benchmark 基础设施（pytest-benchmark 4.0.0 + addopts `--benchmark-min-rounds=5` 等）已装，但测试面为空；norecursedirs 复数目录名与实际单数目录不符（`tests/benchmark` 未列入 norecursedirs，因其本就只含 __init__.py）。
- 验证命令+输出：`ls tests/benchmark/ tests/benchmarks/` → 前者仅 __init__.py、后者 No such file；`find tests -iname "*benchmark*"` → 仅 tests/benchmark。
- 影响：性能回归无任何真实基准；norecursedirs 配置指向死路径（若未来有人建 tests/benchmarks/ 会被静默排除而不自知）。
- 建议方向：补真实 benchmark 测试或删除该空包与配置；norecursedirs 改为 `tests/benchmark`（单数）。

### F1217 | justfile `check` 与 ci.yml 双向漂移：just check 缺 N7/purity lint（CI 有），CI 缺 graph/fields lint（just 有） | error | P2
- 证据：`justfile:14-24`（check 配方：lint_status_strings → lint_repo_consistency → `just lint-contracts`（graph+fields+contracts）→ ruff/mypy/basedpyright → sync idempotency → pytest 两步）；`ci.yml:51-60`（CI 跑 lint_status_strings + lint_contracts + lint_repo_consistency + lint_no_forbid_with_computed_field + lint_no_fs_mutation；**无** graph/fields）；`ci.yml:57-60`（N7/purity 在 CI，不在 justfile check）
- 根因：两套清单各自演化；justfile:13 声称 "Run all checks (contract lints + ruff + mypy + basedpyright + sync idempotency + tests)"，CI 与本地面不一致。
- 验证命令+输出：`grep -n "lint_no_forbid\|lint_no_fs" justfile` → 0 命中（仅 pre-commit/CI 有）；`grep -n "lint_contract_graph\|lint_contract_fields" .github/workflows/ci.yml` → 0 命中。
- 影响：本地 `just check` 通过 ≠ CI 通过（本地缺 N7/purity，CI 缺 graph/fields）；"justfile 是权威，workflow 不得漂移" 的双向漂移。
- 建议方向：统一为单一清单（just check 增补 N7/purity；ci.yml 增补 graph/fields），或显式区分 "just check = 子集" 并文档化。

### F1218 | pytest addopts 全局 `--cov` 使 `--collect-only` 等非测试调用产生 16.08% 假 FAIL 并覆写 tests/coverage/；ci.yml "Run coverage threshold test" step 名与 `--no-cov` 行为错位（= 已知 D1-02，Z10 侧确认） | error | P2
- 证据：`pyproject.toml:420-431`（addopts 无条件 `--cov=shenbi`、`--cov-report=html:...`、`--cov-report=xml:.../coverage.xml`、`--cov-branch`）；`ci.yml:65-66`（step 名 "Run coverage threshold test (serial, last only)" 实跑 `pytest -p no:xdist -m "last" --no-cov`）；`d1-11-collect-only-full.log`（collect-only 输出 "FAIL Required test coverage of 85.0% not reached. Total coverage: 16.08%"）
- 根因：addopts 无条件附加 cov 插件，collect-only/单文件调用也执行覆盖并触发 `[tool.coverage.report] fail_under=85`（pyproject:452）假失败，且反复覆写 tests/coverage/coverage.xml（G3 证据链污染，D1-02 同根因）；ci.yml 中阈值实际由第一步 pytest（addopts 生效）强制，"coverage threshold" step 只跑 `last` 标记测试读 coverage.xml（--no-cov 正确但命名误导）。
- 验证命令+输出：`grep -n "fail_under" pyproject.toml` → :452 `fail_under = 85`；d1-11-collect-only-full.log 尾部 FAIL 输出（16.08%）；`sed -n '65,66p' .github/workflows/ci.yml`。
- 影响：collect-only 产生误导性失败输出；coverage.xml 被污染破坏审计证据链（协调者已重生成真实 85.16% 版本，F508 merged-into-D1-02）。
- 建议方向：addopts 条件化 `--cov`（如 `--cov` 仅在有 `--cov` 显式传入时附加，或 collect-only 传 `--no-cov`）；修正 ci.yml step 命名。

### F1219 | executor_config.toml `shenbi-chapter-drafting` override 的 PRE-DEPLOYMENT 探测注释悬置（max_tokens=32768 是否被模型接受未确认/未清理） | error | M
- 证据：`executor_config.toml:7-11`（"PRE-DEPLOYMENT: probe `deepseek-v4-flash` with max_tokens=32768 to confirm model accepts it. If model hard-limits below 32768, root cause shifts…"）
- 根因：配置携带"待验证"状态注释，无后续探测记录/移除标记；若模型实际硬限低于 32768，配置持续以未验证值运行。
- 验证命令+输出：读 executor_config.toml:5-12；grep "PRE-DEPLOYMENT" 仅此处。
- 影响：运维配置携带悬置的部署前待办（轻微文档/状态漂移）。
- 建议方向：完成探测后更新注释或记录验证结果（spec §2.2/§2.9 引用）。

---

## 2. per-file 报告（52/52）

### .github/CODEOWNERS
- 处置: deep-read
- 声称检查的不变量: 默认 owner + src/tests/docs/skills 四段 ownership；无重复/冲突 pattern
- findings: 无
- 验证命令: read（12 行全文）；与 git config 无关（仓库单 owner ThomasChangX，无团队/org 引用）
- 置信度: high

### .github/ISSUE_TEMPLATE/bug.md
- 处置: deep-read
- 声称检查的不变量: bug 模板字段（描述/复现/环境/日志）；`uv run python -c "import shenbi; print(shenbi.__version__)"` 命令可用（entry point 已装）
- findings: 无
- 验证命令: read（30 行）；`grep -n "__version__" src/shenbi/__init__.py` → 存在
- 置信度: high

### .github/ISSUE_TEMPLATE/config.yml
- 处置: deep-read
- 声称检查的不变量: blank_issues_enabled=false + 单一 contact link（GitHub Discussions 路径合法）
- findings: 无
- 验证命令: read（5 行）
- 置信度: high

### .github/ISSUE_TEMPLATE/feature.md
- 处置: deep-read
- 声称检查的不变量: feature 模板三段（Problem/Proposed solution/Alternatives）
- findings: 无
- 验证命令: read（17 行）
- 置信度: high

### .github/ISSUE_TEMPLATE/skill-proposal.md
- 处置: deep-read
- 声称检查的不变量: skill 提案字段（kebab-case 命名、purpose、reads/writes、pipeline phase 枚举 genesis/architecture/planning/drafting/audit/foundation/management）
- findings: 无（观察：phase 枚举 "management" 与 deps.json 实际 phase 名 genesis/architecture/planning/drafting/audit/foundation 相比多了 "management"——模板宽松，非漂移）
- 验证命令: read（29 行）；`python3 -c` 解析 deps.json t2-phases 键 = [genesis, architecture, planning, drafting, audit]
- 置信度: high

### .github/PULL_REQUEST_TEMPLATE.md
- 处置: deep-read
- 声称检查的不变量: PR checklist 与 AGENTS.md 约定一致（no print、no ignore_errors/exclude、CHANGELOG、ADR）
- findings: 无
- 验证命令: read（31 行）；对照 AGENTS.md PR 协议（just check 前置、回复并 resolve 评论）
- 置信度: high

### .github/SECURITY.md
- 处置: deep-read
- 声称检查的不变量: 3 行 stub 仅指向根 SECURITY.md（redirect 语义）
- findings: 无（相关声明在根 SECURITY.md:20-21，见 F1207/F1215）
- 验证命令: read（3 行）；`ls SECURITY.md` 存在
- 置信度: high

### .github/dependabot.yml
- 处置: deep-read
- 声称检查的不变量: uv + github-actions 两个 ecosystem；weekly；allow 仅 direct（过滤 torch 传递依赖）；open-pull-requests-limit=5；labels/commit-message
- findings: [F1209]
- 验证命令: read（37 行）；`ls docs/superpowers/specs/2026-08-02-pr20-followup-dependabot-and-embeddings-ci-design.md` → 不存在（archive 下有）
- 置信度: high

### .github/workflows/ci.yml
- 处置: deep-read
- 声称检查的不变量: 与 justfile check 一致性；matrix（ubuntu+macos；PR 3.11/3.12、main push 加 3.13）；uv lock --check + frozen sync；ruff/mypy/basedpyright；5 个自定义 lint；两步 pytest；codegen-idempotency（sync-contracts + autocheck-docs + plugins + git diff）；dependency-review；action-validation
- findings: [F1200, F1201, F1202, F1203, F1217, F1218]
- 验证命令: read 全文（108 行）；grep 交叉核对各 lint 在 justfile/pre-commit 的接线（见 F1200/F1201/F1217）；`git ls-files .codex-plugin` → 0；`sed -n '65,66p'` 确认 coverage step
- 置信度: high

### .github/workflows/codeql.yml
- 处置: deep-read
- 声称检查的不变量: security-and-quality suite；push 全 ref + weekly schedule + dispatch；无 pull_request（注释解释 300-file 限制）；permissions 最小化（security-events: write）
- findings: [F1207]
- 验证命令: read（36 行）；`grep -n "pull_request" codeql.yml` → 0
- 置信度: high

### .github/workflows/docs.yml
- 处置: deep-read
- 声称检查的不变量: `--group docs` 与 pyproject docs group 一致；libcairo2-dev 安装（mkdocs-material[imaging] 需要）；mkdocs build --strict；gh-deploy 仅 main push
- findings: 无
- 验证命令: read（32 行）；`grep -n "docs" pyproject.toml` → :50-55 docs group 存在；mkdocs.yml nav 13 个文件全部存在（见 mkdocs.yml 条目）
- 置信度: high

### .github/workflows/embeddings-smoke.yml
- 处置: deep-read
- 声称检查的不变量: daily schedule + dispatch；dev group 含 sentence-transformers+torch（与 pyproject dev 组一致）；HF 模型缓存；真实 encode 冒烟（shape (2,384)）
- findings: [F1209]
- 验证命令: read（47 行）；pyproject.toml:47 dev 组确含 sentence-transformers（与 D1-01 的 :17 注释矛盾同源）
- 置信度: high

### .github/workflows/nightly.yml
- 处置: deep-read
- 声称检查的不变量: 3 job（windows-smoke/python-313-migration/doc-links）全部 disabled（无 schedule，仅 workflow_dispatch）；顶部注释说明禁用理由与启用步骤
- findings: 无（disabled 为文件内文档化的故意状态；与 F0-07/F1215 交叉引用——nightly 无 pip-audit job，weekly 供应链审计无兑现面）
- 验证命令: read（76 行）；`:19-21` schedule 被注释确认；`grep -n "pip-audit" nightly.yml` → 0
- 置信度: high

### .github/workflows/pre-commit-autoupdate.yml
- 处置: deep-read
- 声称检查的不变量: weekly + dispatch；pre-commit autoupdate；验证 hook 工作（rc>1 才阻断 PR）；create-pull-request 分支/labels
- findings: 无
- 验证命令: read（41 行）
- 置信度: high

### .github/workflows/release.yml
- 处置: deep-read
- 声称检查的不变量: tag v* 触发；SBOM 生成（cyclonedx-py environment）；uv build（sdist+wheel）；changelog 生成；gh-release 附带 dist + sbom
- findings: [F1208]
- 验证命令: read（46 行）；`grep -n "git-cliff" release.yml` → 0（changelog 用 git log，:26/:30/:33）
- 置信度: high

### .github/workflows/security.yml
- 处置: deep-read
- 声称检查的不变量: push(main)+PR 触发 pip-audit + SBOM 上传
- findings: [F1215]
- 验证命令: read（19 行）；`grep -c "schedule" security.yml` → 0
- 置信度: high

### .gitignore
- 处置: deep-read
- 声称检查的不变量: 标准忽略面 + `.codex-plugin/` + round 产物 pattern + novel-output 例外（!novel-output/**）+ staging/novel-output 等
- findings: [F1202]（.codex-plugin/ 被忽略是 CI 空转检查的根因）
- 验证命令: read（94 行）；`git ls-files .codex-plugin` → 0；`git check-ignore .codex-plugin/plugin.json` → 命中
- 置信度: high

### .pre-commit-config.yaml
- 处置: deep-read
- 声称检查的不变量: ruff/mypy/pre-commit-hooks/yamllint 四 remote + 7 个 local hook（basedpyright/lint-status/lint-contracts/lint-repo-consistency/contract-sync/fixture-mirror/autocheck-docs/N7/purity）+ pre-push-check（stages: pre-push）
- findings: [F1200, F1201]（lint-contracts hook 仅 tools/lint_contracts.py，graph/fields 未入 pre-commit）
- 验证命令: read（108 行）；`grep -n "lint_contract_graph\|lint_contract_fields" .pre-commit-config.yaml` → 0
- 置信度: high

### .yamllint
- 处置: deep-read
- 声称检查的不变量: extends default；document-start 禁用；truthy check-keys 关；line-length 100 warning；indent 2 spaces
- findings: 无
- 验证命令: read（13 行）；`.venv/bin/yamllint --strict .github/workflows/` → rc=0（ci.yml action-validation 同命令可过）
- 置信度: high

### benchmarks/anchors/AC-001.md … AC-011.md（11 文件）
- 处置: deep-read（全部 11 个）
- 声称检查的不变量: YAML frontmatter（id 唯一、category、source_work、source_ref、calibrates）；工艺分析 + 评分校准基准（88-97/75-87/<75 三档）；被评分/评审 skill 以 `reads: benchmarks/anchors/` 消费（review-resonance、score-arc/stratum/volume）
- findings: [F1210]（契约图 DANGLING_WRITE 假阳性 + registry producer 分类矛盾；锚点库本身非空洞——11 个文件全部有效，20 个 calibrates 维度覆盖 章级/弧段/卷级/大弧级）
- 验证命令: 全部 read；`python3 -c` 解析 11 个 frontmatter → 11 个唯一 id、20 个 calibrate 维度；`.venv/bin/python tools/lint_contract_graph.py 2>&1 | grep anchor` → DANGLING_WRITE 1 条（AC-*.md）
- 置信度: high
- 备注: AC-010/AC-011（政治表达/金手指限制）超出 wave3 计划声明的 9 类（AC-001~009），无类别索引文档；锚点 dims 与评分 rubric schema 无机械一致性校验（M 级观察，并入 F1210 建议）。

### cliff.toml
- 处置: deep-read
- 声称检查的不变量: git-cliff 配置（Keep a Changelog header、conventional_commits、commit_parsers 6 组、filter_unconventional=false）
- findings: [F1208]（配置存在但 release.yml 未使用；header 与 CHANGELOG.md 有差异）
- 验证命令: read（36 行）；对照 CHANGELOG.md:1-8 与 justfile:99
- 置信度: high

### executor_config.toml
- 处置: deep-read
- 声称检查的不变量: default temperature/max_tokens + 18 个 skill override；override 键全部存在于 skills/；foreshadowing-lifecycle override 与 deps.json 登记一致
- findings: [F1212, F1219]
- 验证命令: read（68 行）；`python3 -c` tomllib 解析 → 18 个 override 全部在 skills/ 存在；`grep -n "foreshadowing-lifecycle" tests/tiers/deps.json` → 0（F1212）
- 置信度: high

### justfile
- 处置: deep-read
- 声称检查的不变量: check 配方 = 全部 CI 检查（权威面）；install/test/test-all/test-file/fix/lint-*/generate/gate/dispatch/docs/build/clean/precommit/mutate/mutate-check/changelog/pipeline-* 配方正确性
- findings: [F1200, F1201, F1208, F1217]
- 验证命令: read（115 行）；grep 各 lint 接线；`just --list` 未运行（避免任何副作用——配方均为只读/生成，未执行）
- 置信度: high

### mkdocs.yml
- 处置: deep-read
- 声称检查的不变量: nav 13 个条目对应 docs/ 文件全部存在；mkdocstrings python handler 指向 API 页；social 插件依赖 imaging extra
- findings: 无
- 验证命令: read（76 行）；`for f in $(nav 13 路径); do test -f docs/$f` → 13/13 OK；pyproject:52 docs group 含 mkdocs-material[imaging]
- 置信度: high

### plugins/master.json
- 处置: deep-read
- 声称检查的不变量: 结构字段（name/version/description/author/homepage/license/skills/platforms）；skills 59 条路径全部存在；platforms.codex 配置与 GENERATORS 键匹配；version 与 pyproject 一致
- findings: [F1211, F1213]
- 验证命令: read（79 行）；`python3 -c` → 59 条 listed（全部存在）、74 dirs、missing 15（F1213）；version 0.2.0 vs pyproject 0.1.0（F1211/T1305）；platforms.codex.format="codex-cli" 与 GENERATORS 键匹配
- 置信度: high

### pyproject.toml
- 处置: deep-read
- 声称检查的不变量: PEP 621 元数据；dev/docs 组与 uv.lock 一致；scripts 入口与 CI 调用一致；mypy/basedpyright/ruff/pytest/coverage/mutmut 配置自洽
- findings: [F1211, F1216, F1217, F1218]（另确认 F0-06：:8 requires-python>=3.11 vs :359 mypy 3.12 vs :378 basedpyright 3.11；F0-08：:447-451 注释称 90/89 vs :452 fail_under=85；D1-01：:17 注释与 :47 dev 组 sentence-transformers 矛盾——均归入相应已知线程）
- 验证命令: read 全文（459 行）；grep 逐项核对；`.venv/bin/python` 运行各 lint rc=0
- 置信度: high

### run_pipeline.sh
- 处置: deep-read
- 声称检查的不变量: 循环 resume→解析 status/checkpoint/phase→stuck 检测→auto-approve/前进→完成退出；shell 正确性（set -euo pipefail、tee 日志、多用户隔离）
- findings: [F1204, F1205, F1206]
- 验证命令: read（107 行）；逐行追踪 `$PROJECT_DIR` 插值路径 :31-38/:70-79；grep 分支 :42-103
- 置信度: high

### scripts/lint_contract_fields.py
- 处置: deep-read
- 声称检查的不变量: B.5 字段漂移（声明 fields vs 实际 heading/key，match_field 统一匹配）；fixture 样本解析；无样本路径跳过（WARN-only）；exit 1 阻断
- findings: [F1201]
- 验证命令: read（244 行）；`.venv/bin/python scripts/lint_contract_fields.py` → rc=0 "All contract field declarations match truth files."；grep CI/pre-commit/pre-push 接线 0
- 置信度: high

### tools/__init__.py
- 处置: deep-read
- 声称检查的不变量: 空包标记（0 行，使 tools 可被 unit tests import）
- findings: 无
- 验证命令: read（0 行）
- 置信度: high

### tools/audit-skill-descriptions.py
- 处置: deep-read
- 声称检查的不变量: description ≤500 chars + 非行为化描述（复用 G0.skill_contract 规则）；--skills-dir 参数；exit 0/1/2
- findings: 无（观察：未接 CI/pre-commit/justfile——手动审计工具，含单测 tests/unit/tools/test_audit_skill_descriptions.py）
- 验证命令: read（77 行）；`.venv/bin/python tools/audit-skill-descriptions.py` → rc=0 "OK: all descriptions compliant"
- 置信度: high

### tools/check_fixture_mirror.py
- 处置: deep-read
- 声称检查的不变量: MIRROR_MAP 单一来源（复用 g0）；sha256 镜像一致性；source 缺失跳过
- findings: 无（观察：仅 pre-commit 接线，CI/justfile 未跑；无单测）
- 验证命令: read（43 行）；`.venv/bin/python tools/check_fixture_mirror.py` → rc=0；grep 接线 → 仅 .pre-commit-config.yaml:79-83
- 置信度: high

### tools/compare_mutation_score.py
- 处置: deep-read
- 声称检查的不变量: 解析 mutmut results 与 baseline；drop>threshold 报 REGRESSION；参数固定列表 subprocess（无注入）；docstring "Used by CI in PR-35"
- findings: 无（观察：docstring :5 声称 CI 使用，实际 ci.yml 无 mutmut 步骤——仅 justfile mutate-check 手动配方；历史声明漂移 M 级，未单列）
- 验证命令: read（72 行）；`grep -n "mutmut\|compare_mutation" .github/workflows/ci.yml` → 0
- 置信度: high

### tools/generate_autocheck_docs.py
- 处置: deep-read
- 声称检查的不变量: P6 auto-check 块（AUTO-CHECK-START/END sentinel）幂等注入；CI+pre-commit 均接 git diff
- findings: 无
- 验证命令: read（156 行）；ci.yml:83 与 .pre-commit-config.yaml:84-89 接线确认；tests/unit/test_generate_autocheck_docs.py 存在
- 置信度: high

### tools/lint_contract_graph.py
- 处置: deep-read
- 声称检查的不变量: 闭包检查（ORPHAN_READ 阻断 / DANGLING_WRITE WARN）；dag_key glob 感知 + glob-superset；external/pipeline producer 跳过
- findings: [F1200, F1210]
- 验证命令: read（112 行）；`.venv/bin/python tools/lint_contract_graph.py` → rc=0、11 条 DANGLING_WRITE（含 anchor-curate AC-*.md，F1210）；dag_key 实测
- 置信度: high

### tools/lint_contracts.py
- 处置: deep-read
- 声称检查的不变量: load-all（每个 in-pipeline skill 契约可加载）+ completeness（REPORT 被下游消费须有持久写，glob 感知）
- findings: 无
- 验证命令: read（88 行）；`.venv/bin/python tools/lint_contracts.py` → rc=0；tests/unit/test_lint_contracts.py 存在
- 置信度: high

### tools/lint_no_forbid_with_computed_field.py
- 处置: deep-read
- 声称检查的不变量: N7（computed_field + extra='forbid' 同 class body 检测）；exit 0/1/2；CI+pre-commit 接线
- findings: 无（观察：`"forbid" in ast.unparse(...)` 子串启发式，宽匹配可能误报；文档化 limitation 不含继承）
- 验证命令: read（80 行）；`.venv/bin/python tools/lint_no_forbid_with_computed_field.py src/shenbi/contracts` → rc=0；ci.yml:58 + pre-commit:90-95 接线
- 置信度: high

### tools/lint_no_fs_mutation.py
- 处置: deep-read
- 声称检查的不变量: P4 purity（open 写模式/os/shutil/Path 写方法）；PERMANENT+TRANSITIONAL allowlist 语义（transitional 须收缩）；已知局限文档化
- findings: 无（观察：`from os import replace` 别名调用、动态 mode、os.open 不检测——均文档化局限；allowlist 收缩义务无机械强制）
- 验证命令: read（183 行）；`.venv/bin/python tools/lint_no_fs_mutation.py src/shenbi` → rc=0；tests/unit/test_lint_no_fs_mutation.py 存在
- 置信度: high

### tools/lint_repo_consistency.py
- 处置: deep-read
- 声称检查的不变量: body-ban / loader-uniqueness / terminology / section-header / dead-decisions-sidecar（5 类）
- findings: [F1212]（无 skill↔deps.json 完整性检查——F0-02 根因在 Z10 侧确认）
- 验证命令: read（247 行）；`.venv/bin/python tools/lint_repo_consistency.py` → rc=0；tests/unit/test_lint_repo_consistency.py 存在
- 置信度: high

### tools/lint_status_strings.py
- 处置: deep-read
- 声称检查的不变量: D3 无裸状态字面量（status/state/classification 键）；递归扫描 ternary/bool-op；status.py 豁免
- findings: [F1214]
- 验证命令: read（107 行）；`.venv/bin/python tools/lint_status_strings.py` → rc=0（0 输出）；自定义 AST 扫描 → 10 站点非白名单字面量；tests/unit/test_lint_status_strings.py 存在
- 置信度: high

### tools/migrate_contract_to_frontmatter.py
- 处置: deep-read
- 声称检查的不变量: 一次性迁移（CLASSIFICATION 表 60 skill）；幂等（已迁移 no-op）；frontmatter 重建 + body 数据契约剥离；assert 防御
- findings: 无（观察：硬编码 CLASSIFICATION 表与当前 skill 契约的漂移风险——迁移工具已一次性完成，未来契约变更不回灌此表；tests/unit/test_migrate_contract.py 存在）
- 验证命令: read（595 行）；抽样 CLASSIFICATION 条目与对应 SKILL.md frontmatter 一致（shenbi-chapter-drafting 等）
- 置信度: high

### tools/pre-push-check.sh
- 处置: deep-read
- 声称检查的不变量: CI 模拟（lock/ruff/mypy/basedpyright/5 lint/pip-audit/mkdocs 条件/pytest 两步/死代码 WARN/contract-sync/autocheck-docs）；--no-cov 防 coverage.xml 覆写（:82-85 注释）
- findings: [F1200, F1201]（无 graph/fields lint）+ 确认 F1217 面
- 验证命令: read（97 行）；grep 各步骤与 ci.yml 对照；:44 `git diff --name-only main...HEAD`（推送范围 idiom 正确）
- 置信度: high

### uv.lock
- 处置: deep-read（结构分析 + 关键包核对）
- 声称检查的不变量: lock 与 pyproject 同步（uv lock --check rc=0）；version=1 revision=3；requires-python>=3.11；resolution markers 5 档；关键包版本（numpy 2.4.6/2.5.0、pytest-asyncio 1.4.0、pytest-ordering 0.6、setuptools 83.0.0、sentence-transformers、torch、git-cliff、mutmut、pip-audit、cyclonedx-bom）
- findings: [F1211]（冗余/休眠依赖在 lock 中的体现：dev group 段 3352-3414 含 pytest-asyncio/pytest-ordering/setuptools/sentence-transformers）
- 验证命令: `uv lock --check` → "Resolved 180 packages in 17ms" rc=0；`python3` 正则提取各包版本；d1-09-uv-audit.log（无已知漏洞）
- 置信度: high

---

## 3. 通用维度小结

1. **CI 与 just check 一致性**：双向漂移（F1217）——CI 独有 N7/purity lint（ci.yml:57-60），just check 独有 graph/fields lint（justfile:52-55）；且 graph（F1200）与 fields（F1201）两 lint 的 "block PR" docstring 在 CI 面均不成立。pre-push-check.sh 与 ci.yml 面基本一致，但同样缺 graph/fields；fixture-mirror 仅 pre-commit。
2. **脚本正确性**：tools/ 各 lint 均有单测且当前全部 rc=0；T9-01 词表外发明值（F1214）与 F0-02 完整性缺口（F1212）是工具正确性两个真洞；run_pipeline.sh 有注入（F1204）、过宽 auto-approve（F1205）、直接改状态（F1206）三问题。
3. **subprocess/shell 调用面**：run_pipeline.sh 两处 `python3 -c` 双引号插值（T12-03）；compare_mutation_score.py 用固定参数列表 subprocess（安全）；pre-push-check.sh 的 `git diff main...HEAD` idiom 正确。
4. **配置漂移**：executor_config.toml ↔ deps.json（F1212）；mkdocs.yml nav 与 docs 结构一致（13/13 存在，无漂移）；cliff.toml ↔ release.yml changelog 双机制（F1208）；master.json version ↔ pyproject（F1211/T1305）；dependabot/embeddings-smoke 的 spec 路径引用过期（F1209）。
5. **依赖锁定卫生**：uv.lock 与 pyproject 同步（uv lock --check rc=0、d1-12 一致）；冗余声明 pytest-asyncio/pytest-ordering/setuptools（零消费）+ numpy 仅 Route B（F1211/T1301-1304）；pydantic 注释过期（T1306）。
6. **安全初查**：无凭证/敏感数据；无路径穿越（写路径均 repo 固定或配置控制）；唯一注入面为 run_pipeline.sh/round-exec.sh 的 python3 -c 插值（T12-03，P2，运维脚本面）；workflow 无 secret 滥用（permissions 最小化良好）。
7. **文档引用**：config 内 spec 引用两处过期（F1209）；workflow 顶部注释与实现基本一致（nightly disabled 状态在文件内文档化）；SECURITY.md 声明两处半真（F1207/F1215）。
8. **覆盖缺口**：Z10 工具面测试齐全（除 lint_contract_fields/check_fixture_mirror/compare_mutation_score/pre-push-check 无单测）；benchmark 面空洞（F1216）。
9. **benchmarks/ 澄清**：根 `benchmarks/anchors/` 并非空洞——11 个 AC 锚点有效且被 4 个评分/评审 skill 运行时消费；空洞在 `tests/benchmark/`（T1107）；契约图对目录读/文件写无法连接产生 DANGLING_WRITE 假阳性（F1210）。

---

## 4. 返回摘要

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F1200 | lint_contract_graph 未接 CI（docstring 声称 block PR） | P2 | lint_contract_graph.py:7-11; ci.yml:53-56 |
| F1201 | lint_contract_fields 未接 CI/pre-commit/pre-push/测试（T201） | P2 | lint_contract_fields.py:20-21; justfile:52-55 |
| F1202 | codegen-idempotency `.codex-plugin/` diff 空转（gitignore 未入库） | P2 | ci.yml:88-89; .gitignore:20 |
| F1203 | ci.yml macOS continue-on-error 全 job 容错 | M | ci.yml:29-30 |
| F1204 | run_pipeline.sh python3 -c 插值注入（T12-03） | P2 | run_pipeline.sh:31-38,70-79 |
| F1205 | run_pipeline.sh auto-approve 关键词过宽（escalation\|gate\|dispatch） | P2 | run_pipeline.sh:89 |
| F1206 | run_pipeline.sh 直接改写 pipeline-state.json 绕过状态机 | P2 | run_pipeline.sh:70-79 |
| F1207 | codeql.yml 无 PR 触发 vs SECURITY.md "every PR" | P2 | codeql.yml:12-16; SECURITY.md:21 |
| F1208 | 双 changelog 机制（release.yml git log vs git-cliff/cliff.toml） | P2 | release.yml:23-38; justfile:99; cliff.toml:4-9 |
| F1209 | dependabot/embeddings-smoke 引用 archive spec 路径 | M | dependabot.yml:5; embeddings-smoke.yml:7 |
| F1210 | 契约图 dir-read vs file-write 不连接（anchors DANGLING_WRITE 假阳性） | P2 | lint_contract_graph.py:88-93; graph.py:42-59 |
| F1211 | T13 依赖冗余/休眠 + 版本漂移（asyncio/ordering/numpy/setuptools/0.2.0/pydantic 注释） | P2 | pyproject.toml:30,33,14,48,3,11 |
| F1212 | deps.json 缺 5 skill 登记 + lint 无完整性检查（F0-02） | P1 | executor_config.toml:26-27; lint_repo_consistency.py:41-46 |
| F1213 | master.json 59 vs 74 + generate.py 无 skills 校验（F624） | P2 | master.json:8-68; generate.py:21-33,52 |
| F1214 | lint_status_strings 词表外发明值 8 文件 10 站点全盲（T9-01） | P1 | lint_status_strings.py:25,29-30; 10 站点 |
| F1215 | security.yml 无 schedule vs SECURITY.md weekly（F0-07） | P2 | security.yml:1-5; SECURITY.md:20 |
| F1216 | tests/benchmark 空洞 + norecursedirs 复数错位 + 1+1 冒烟（T1107） | P2 | pyproject.toml:414; test_pytest_framework.py:42-45 |
| F1217 | justfile check ↔ ci.yml 双向漂移（N7/purity 仅 CI；graph/fields 仅 just） | P2 | justfile:14-24; ci.yml:51-60 |
| F1218 | addopts 全局 --cov 假 FAIL + 覆写 coverage.xml（D1-02）；step 名与 --no-cov 错位 | P2 | pyproject.toml:420-431; ci.yml:65-66 |
| F1219 | executor_config PRE-DEPLOYMENT 探测注释悬置 | M | executor_config.toml:7-11 |

**覆盖统计**：deep-read 52/52；未覆盖文件 0。
**低置信度文件**：无。
**未覆盖文件**：无（清单全部有报告）。

**与已知 findings 的对应**：F0-07→F1215、T9-01→F1214、T201→F1201、F0-02→F1212、T12-03→F1204、T1301-1306→F1211、F624→F1213、T1107→F1216、D1-02→F1218；F0-06/F0-08/D1-01 在 pyproject 条目内确认（属其他区线程）。
