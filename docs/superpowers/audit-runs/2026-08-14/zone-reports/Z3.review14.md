# Z3 分区第十四轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第十四轮独立复核（本文件 `Z3.review14.md`，与初审 `Z3.a.md`、一轮至十三轮 `Z3.review.md`~`Z3.review13.md` 全部隔离）
> 复核范围：34/34 文件全量逐行重读（13,641 行；`__init__.py` 1 行 → `chapter_loop.py` 3053 行 → `dispatch_helper.py` 1986 行 → `cli.py` 939 行 → `triggers.py` 656 行 → `state.py` 536 行等全部读完），逐条独立验证 F300-F399 + F3A0-F3A1（100 条编号）；以真实生产项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式、audits 722 文件全旧式名、truth/pending_hooks.md body 表格格式、truth/chapter_summaries.md 仅 2 章条目、novel.json 无 total_chapters、pipeline-state.json phase=chapter-loop ch=56 closure_step=0、style/style_profile.md 含 confidence: low）为格式/行为对照基准
> 本轮发现编号：**F3A2-F3A4（3 条，全部 M 级；无新增 P0/P1/P2）**——编号沿用 F3A0 段（F400-F4ZZ 已由 Z4 占用，全仓检索 F3A2-F3ZZ 无占用）
> 新增误报 0 条（F325 维持二~十三轮撤销，本轮独立复证）；新增严重度异议 0 条（支持既有 F302/F364/F397 的 P1→P0 依据，详见异议节）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证全部在 /tmp 临时目录与只读 python 中完成）
> 方法：34 文件逐行重读 + grep/uv run python/契约 load_contract 实证 + 真实产物对照 + **synthetic volume_map 沙箱实验（本轮唯一新验证手段，见 F3A2）+ 损坏 state 反序列化实证（F3A3）**

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮独立实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`、`_count_total_chapters → 0`、`is_volume_boundary(15) → False`（见验证节）。生产 volume_map 全中文（`:3` `## 第一卷：觉醒之火（第1-15章）`、`:7` `**章节范围**: 第1章 - 第15章（共15章）`），`_END_RE`/`_RANGE_RE` 零命中；且当前版本两个权威 skill 模板（`skills/shenbi-volume-outlining/SKILL.md:135-139`、`shenbi-story-architecture/SKILL.md:108`）同样为中文格式——排除"生产文件为旧版遗留"的版本偏移反证。novel.json keys 无 total_chapters（本轮实测）。
2. **F325（误报）维持撤销，本轮独立复证**：4 个 group skill 契约 `writes:` 即旧式路径（本轮 load_contract 实证 `group-factual → ['audits/chapter-N-continuity.md','audits/chapter-N-world-rules.md','audits/chapter-N-pacing.md']`）；生产 audits/ 722 文件全为旧式名；`_any_audit_has_findings`（chapter_loop.py:1779-1793）扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING 被 route 正则与文件扫描双重命中 → revision 执行。F325 原判"并行波写 group-*.md、旧式名永不产生"与磁盘/契约事实相反。
3. **F364（P1 贴 P0）维持成立**：本轮代码级复核 `crash_recovery.py:66 atexit.register(_emergency_cleanup)` + `:144-148` 正常退出无条件 `clear_staging` + `:113-127` 无条件把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交）成立。
4. **F397（P1 贴 P0）维持成立，本轮契约+生产三重独立复证**：(a) 契约 `write_semantics` 6 个累积文件全 `append_dedup`（本轮 load_contract 实证：current_state/particle_ledger/emotional_arcs/subplot_board/chapter_summaries → key=chapter、pending_hooks → key=hook_id），`reads` 仅 `['chapters/chapter-N.md']`；(b) `_write_parsed_outputs`（dispatch_helper.py:1044-1194）对全部 writes/updates 整文件覆写（`safe_write(full_path, content)` :1127），docstring :1061-1067 明示 append_dedup NOT branched here；(c) 生产 56 章书 `truth/chapter_summaries.md` 仅 2 章条目（本轮实测 `grep -c "^## 第"` → 2）。
5. **F379/F371/F373/F353/F340/F341/F345/F354 全部维持成立**（逐条独立验证见结论表；无任何反证）。
6. **本轮新发现 3 条，全部 M 级**：(F3A2) `plan_skeleton.generate_plan_skeleton` 的 `next_chapter = min(chapter + 1, ch_end)`（plan_skeleton.py:86）在卷末章自指——synthetic volume_map 沙箱实测（卷末章 ch15 的 section 6 渲染 "Next chapter (Ch15) role: closing"，即同一章）；(F3A3) `pipeline-state.json` 损坏/版本不匹配时 `machine.load_state → from_json/from_dict` 抛 JSONDecodeError/ValueError，cli.py 六个命令仅捕 FileNotFoundError → 裸 traceback（损坏 state 反序列化实证）；(F3A4) `_append_integrity_findings`（dispatch_helper.py:1026-1041）对 `.integrity-findings-{N}.jsonl` 的读-改-写无锁，并行审计波两线程并发追加时 lost-update（诊断文件丢记录）。
7. **版本偏移注记（沿用三~十三轮）**：生产 pipeline-state 为 Plan 18 重构前旧版管线产物（steps_done 含旧 skill 名），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据（本轮新增对照：当前 `shenbi-volume-outlining`/`shenbi-story-architecture` SKILL.md 模板仍为中文格式，与生产 volume_map 一致，佐证 F324 非版本偏移）；`which codex → /opt/homebrew/bin/codex`（本环境 IDE 路径激活）。

---

## 一、复核结论总表（F300-F399 + F3A0-F3A1 逐条判定）

> 判定基于本轮独立 read/grep/uv run python/契约 load_contract 实证与真实产物对照。全部 34 文件逐行重读。**误报 1 条（F325，二轮撤销、三~十三轮维持，本轮独立复证维持）**；**F348 为"成立但机制描述部分不准"（非误报）**；**F3A2-F3A4 为本轮新发现（见二节）**。

### 初审（F300-F319/F322-F323）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | 本轮 `python3 -c 'print("\u003c"=="<")'` → True；dispatch_helper.py:734 `content.replace("<","\u003c")` 源码字面量求值即 `replace("<","<")` 恒 no-op |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 chapter_loop.py:2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1`(14) → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析/趋势落盘）不可达（本轮逐行确认无其他调用点）；cs.resonance_score 并行路径恒 None；resonance_trend.md 由 shenbi-review-resonance 契约 updates 自身写入（本轮 load_contract 实证 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`） |
| F302 | **成立** | P1 维持（支持 P1→P0 依据，裁定留 stage 4） | `dispatch_skill(state=)` 仅 chapter_loop.py:2799（串行可及）/ :2970（串行 BLOCKING 环，不可达）/ :1250（foreshadowing-track 死分支）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751 均未传（本轮 grep 实证）；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0（PipelineState 无 chapter 属性，本轮读 state.py:161-183 确认） |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（`_snapshot_chapter_files` 死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转（仅 linguistic-drift-check 有实现体）；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError（cli.py:613/785）；machine.clear_checkpoint 的 ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93） |
| F305 | **成立但框定需修正（同意二~十三轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 记账漂移 |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方；:97 `Chapter {chapter}` 子串缺陷；volume_context 无消费方（_INJECT_FROM_CACHE 只注入 4 字段，:601-621） |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用；`_parse_hook_entries`（hook_planting.py:144）的 `^(MH|H)-` 对生产 plan section 7 的 MH-001/H-N01 行会接受但 raw_op=="advance" 非 plant/open 被跳过（:151）；检索侧断裂见 F346 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state（state.py:383-428）无生产调用 |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:428-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方（本轮 grep：唯一生产 upsert 调用 chapter_loop.py:3014 为 upsert_markdown_row） |
| F313 | **成立** | P1 维持，无异议（注意 F379 交互） | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换（`resolve_volume_path`，contracts/paths.py `_BOUND_N` 实证）；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（closure.py:91-95 + shenbi-review-long-span 契约 writes 实证）→ G4 校验错位；API/IDE 路径下步骤 6 在派发阶段即失败（F379），本 finding 的 G4 失效面在 legacy 路径与 F379 修复后激活 |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（dispatch_helper.py:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551，core_skills 实为 6 个）；F318.6 scr_extractor.py:313 三次相同引号判断（`'"' in p or '"' in p or '"' in p`）；F318.7 route_chapter_revision 的 blocking 形参未用（revision_router.py:86，函数体 0 次引用）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback（:552）无章号绑定、注入下一次串行 dispatch（:2775-2780）；同源缺口见 F365 |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用（grep 实证；generate_review_checklist 本体已在 dispatch_helper.py:767 接线）；`P\d+-[a-z]+` 不匹配生产数字 id；`_extract_hooks_from_plan` 还用 `chapter-{chapter:03d}-plan.md` 补零文件名（生产不补零，F335 同族） |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符（双重死） |

### 一轮（F324-F334）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`、`_count_total_chapters → 0`、`is_volume_boundary(15) → False`；`_END_RE`/`_RANGE_RE` 对生产 `**章节范围**: 第1章 - 第15章（共15章）` 零命中；novel.json keys 无 total_chapters；pipeline-state phase=chapter-loop ch=56 closure_step=0。**版本偏移排除**：当前 `shenbi-volume-outlining/SKILL.md:135-139`（`## 第N卷：{卷名}`/`**章节范围**: 第N章 - 第M章（共 K 章）`）与 `shenbi-story-architecture/SKILL.md:108` 同为中文格式。影响链（卷边界触发全家 / `cli.py:219 if total > 0:` 跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 章节节点提取裸数字 `\|\s*1\s*\|` 误中跨卷钩子表 `| 1 |` 行）全部成立 |
| **F325** | **不成立（误报，维持二~十三轮撤销）** | **P1 裁定撤回维持** | 本轮 load_contract 实证 group skill 契约 writes 即旧式路径；生产 audits/ 722 文件全旧式名、group-* 数=0；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物；真实 BLOCKING（`严重度: BLOCKING`）被 collect_audit_issues 正则命中 → route≠NO_REVISION → 文件扫描同文件必 True → revision 执行。残余真值（P2）：清单缺 resonance/review-summary、含生产不产生的 texture/reader-pull |
| F326 | **成立** | P1 维持，无异议 | 本轮 load_contract 实证：lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件 → 并行 post-draft（chapter_loop.py:2405-2419）两线程均以 uses_staging 写同一 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用（本轮 grep 实证）；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）；四轮补充成立：`_parse_resonance_score`（:1285-1321）三模式均不匹配生产 `**结果**: 通过 (77/100)` 格式 |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代（grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议；注记：完整重复面见 F387 | 本轮实测 production genre-config auditDimensions.sensitivity=true → `get_active_genre_audits` 含 shenbi-review-sensitivity；core 波（:2551，含 step 14 shenbi-review-sensitivity）与 genre 波双发同一 skill 写同一文件 |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接（本轮重读确认）；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进（本轮逐行确认） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless 落入成功路径 :341-355）；triggers._update_total_chapters（求和 :376）vs cli._update_total_chapters（max :159）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |

### 二轮（F335-F344）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 补零数=0；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在） |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-531 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死（本轮重读确认短路语义） |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 全路径无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用（本轮 grep 实证） |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:23-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |

### 三轮（F345-F352）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；本轮实测 6 个 audit skill 全部 requires_independent=True；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 MH-001/H-N01 零匹配（本轮实测 `MH-001 → []`、`H-N01 → []`）→ master hooks 从 Route A 检索静默缺失；`MH02 → ['H02']` 误截成立 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys=`[title,project,version,last_updated,type,category,status,filled_by,last_chapter,track_chapter]`（无 `hooks` 键，本轮实测），hook 以 body 表格呈现（`| P0-4 |` 行）；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)"；`_read_spine_master_hooks` 的 `\| MH` 行兜底对生产 book_spine.md 零命中 |
| F348 | **成立但机制描述部分不准（维持四~十三轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入（本轮 load_contract 实证 group-character reads 含 truth/character_matrix.md）；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md 生产均 MISSING）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层（:675-696） |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout（:1527/:1731/:1855） |

### 四轮（F353-F363）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F353 | **成立** | P1 维持，无异议（贴 P0 边界） | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 writes 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成；生产 novel.json keys 无 total_chapters（本轮实测） |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进至 `_LAST_AUDIT_IDX+1`；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；本轮 grep skills/ 契约 0 个引用该文件（生产 context/ 下无 curated 文件，本轮实测）；drafting 契约 reads `context/chapter-N-context.md` → 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断，audit_context_cache.py:65）每章每审计恒注入 |
| F358 | **成立但机制前置需修正（维持九~十三轮修正）** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError（:2034 raise）；drift-correction/warning/opening-variation 指令文件无任何消费者。机制链在更早处已断（F389：基线缺失 → `_check_linguistic_drift` 恒返回 None） |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra（hook content/state、rule content）被丢弃；drafting 契约 reads 不含 characters/*.md、world/rules.md → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存，无写盘副作用）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358（CLI main） |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |

### 五轮（F364-F370）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F364 | **成立** | **P1 维持；支持 P1→P0（决策表"数据损坏/丢失 + 契约被静默违反"字面命中；裁定留 stage 4）** | `atexit.register(_emergency_cleanup)`（crash_recovery.py:66）在 chapter_loop.py:2508-2510 的 `step_idx == 0` 分支注册且无去重；`_emergency_cleanup` 正常退出时无条件 `clear_staging`（:144-148）+ save_state + 写紧急快照 + 把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`（:113-127）；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交 → chapter-planning 无限循环）本轮代码级全链复核成立 |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交 + 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`——step 2 时当章 plan 尚不存在（由本步 dispatch 创建于 staging，approve 后才提交）→ FileNotFoundError → 捕获 → `_write_minimal_context_fallback`；且 chapter-planning 契约 reads 不含 context/chapter-N-context.md → 组装成功也不被消费 |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格（本轮实测）→ 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |

### 六轮（F371-F372）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F371 | **成立** | P1 维持，无异议（注意 F379 交互） | `_resolve_closure_g4_path`（closure.py:148-159）对无 N 路径原样返回目录 `final-snapshot/`（closure.py:113-116 step 10）→ `run_gate_g4(step.skill, ["final-snapshot/"], ...)`（:260-261）generic checker 对目录返回 `G4.gen.not_found`/`G4.gen.read_error`（gates/shared.py `resolve_input_path` 仅 join rd+fp，无目录语义；前几轮 G4 CLI 实测两态恒 FAIL）→ 重试×3 后 ESCALATION → pipeline 永不 COMPLETED（第三条"永不完成"独立根因）。API/IDE 路径下 step 10 在派发阶段即失败（F379），本 finding 的 G4 失效面在 legacy 路径与 F379 修复后激活 |
| F372 | **成立** | P2 维持（latent），无异议 | `_style_profile_is_stale`（triggers.py:103-124）`chapters_dir.glob("chapter-*.md")` 计数把 `chapter-N-pre-rev.md` 计入（F350 备份恰落入通配；对照 `_load_previous_titles` :2251 用 `chapter-(\d+)\.md` 正则排除）→ style-learning 自愈触发提前/误触发 |

### 七轮（F373-F378）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F373 | **成立** | P1 维持（潜伏），无异议 | `run_triggered_skills`（triggers.py:584）把字面 N 路径直接传 G4（gates/shared.py `resolve_input_path` 无 N 占位替换，本轮读码确认），dispatch 侧经 `resolve_chapter_path` 解析为章号（contracts/paths.py `_BOUND_N` 实证）→ 5 个 N 型触发步骤（memory-distill L2 / score-arc / score-stratum / score-volume / review-arc-payoff）G4 恒 not_found（前几轮 G4 CLI 实测 arc-N-score.md FAIL / arc-5-score.md PASS） |
| F374 | **成立** | P2 维持，无异议 | `classify_skill_write_safety('shenbi-review-resonance').value → read_only_audit`（write_safety.py:45-46 前缀分类），而契约 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`（本轮 load_contract 实证）→ 并行审计波在无串行保护下并发写 truth 文件 |
| F375 | **成立** | P2 维持，无异议 | dispatch_helper.py:1667-1671 `missing` 非空仅 log.error 后 `return DispatchResult(True, ...)`（IDE 路径 :1770-1772 同款）；`_write_parsed_outputs`（:1153-1168）对 parsed 缺失路径走空内容 skip → state-settling 6+1 文件漏写其一仍 success，G4 只校验现存文件 |
| F376 | **成立** | M 维持，无异议 | truth_embed.py:243-251 `update` 无 `--text` 时落 `emit_json(OK)` 不做任何嵌入 |
| F377 | **成立** | M 维持，无异议 | llm_output_integrity.py:51 `VERDICT_MARKERS` 含通用词 "通过"，:149 `has_verdict` 恒 True → `G4.ac.no_verdict`（:150-153）几乎不触发 |
| F378 | **成立（影响面修正维持十二轮）** | P2 维持，无异议 | `context/volume-N-complete.json` 仅 chapter_loop.py:955-964 读取、全仓 0 写入方；`last_trigger_failure` 仅 triggers.py:575/593 写入、无消费者。十二轮修正成立：F324 现状下 `_find_current_volume(15,set()) → None` → `_check_volume_completion(None) → True` → volume_objective_missed 信号当前不触发（本轮重读 :955-964 确认 `if current_volume is None: return True`），F324 修复后激活 |

### 八轮（F379-F383）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F379 | **成立** | P1 维持，无异议 | closure prompt（closure.py:245-247）无 "chapter N" → `extract_chapter` 返回 None（contracts/paths.py:45-47 实证）→ `resolve_chapter_path(path, None)` 对含 N/NNN 路径抛 UnresolvedPathError（contracts/paths.py:28-34 实证）→ `_dispatch_via_api`/`_dispatch_via_ide`（:1515-1516/:1718-1719）捕获后返回 DispatchResult(False,"Prompt build failed") → `_handle_closure_failure` 重试×3 → ESCALATION——10 步中 5 步（2/4/5/6/10，契约 writes 含 N/NNN）永不派发（前几轮全步骤 prompt-build 实证） |
| F380 | **成立** | P2 维持，无异议 | genesis step 16（shenbi-anchor-curate，optional=True）契约 writes 含 `benchmarks/anchors/AC-NNN.md`（本轮 load_contract 实证）→ chapter=None 时抛 UnresolvedPathError → `_handle_failure`（genesis.py:222-225）对 optional 步骤直接跳过 → anchors 永不产出且无错误信号 |
| F381 | **成立** | P2 维持，无异议 | `_ensure_revision_decisions_exists`（chapter_loop.py:2755-2760 对 NO_REVISION 章节也写回退文件）+ `_is_revision_routed`（:1928-1933 对 'no-revision' 返回 True）→ `_heal_revision_counts`（state_heal.py:45-73）把全部未修订章节的 revision_count 抬到 ≥1（前几轮复现：`chapter-3-revision-decisions.json` 存在 + revision_count=0 → heal 输出 0->1） |
| F382 | **成立** | P2 维持，无异议 | 本轮 grep：`safe_write(` 于 src/shenbi/pipeline/*.py 中 `round_dir` 命中数=0；`MARK_DONE`/`TraceWriter` 于 pipeline 无命中（safe_write.py:125-137 仅两者都提供才写 trace 事件）→ trace 目录只有 materialize 自造事件 → progress.json 物化为"全 pending"失真视图 |
| F383 | **成立** | P2 维持（latent/条件触发），无异议 | dispatch_helper.py:903-922 `_parse_file_outputs` 无 `### FILE:` 匹配时返回 `{"__stdout__": response}`；:1159 `content = parsed.get(rel_path, parsed.get("__stdout__", ""))` 把整段 stdout 写入每个字面输出路径；`_dispatch_via_ide`（:1753-1759）以 parsed=None 调用（本环境 `which codex → /opt/homebrew/bin/codex` → IDE 路径激活） |

### 九轮（F384-F390）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F384 | **成立** | P2 维持，无异议 | truth_embed.py:120-121 `embed_and_store` 函数体内每次 `st.SentenceTransformer("bge-large-zh")` 全新加载；genesis `_update_route_b`（:162-175）逐 hook/rule 调 embed_and_store + context_assemble.py:156 `_route_b` 同款每次加载（每章 context assembly 2 次） |
| F385 | **成立** | P2 维持，无异议 | dispatch_helper.py:1832-1841 无 API key 且无 IDE CLI → legacy 子进程 `uv run shenbi-dispatch`（:1864）；本轮读码实证 `dispatcher/executor.py:142-152` detect_mode 无 codex 时返回 "internal"、`dispatcher/modes/internal.py:22` internal 模式硬拒 → 必失败死路由；uses_staging 被忽略（:1844-1849） |
| F386 | **成立** | P2 维持，无异议 | `_check_soft_fail_escalation`（:967-1035）派发 escalation-review 但无任何 set_checkpoint；调用方随后把步骤正常推进（:2865-2905 落至 advance）→ 升级不暂停、报告成孤儿 |
| F387 | **成立** | P2 维持（贴 P1 边界），无异议 | 本轮实测 production genre-config auditDimensions 的 worldRules/motivation/dialogue/texture 全 true；group 波契约 writes（group-character→dialogue/motivation、group-craft→texture，本轮 load_contract 实证）与 genre 波单维 skill 契约 writes 同名 → 每章重复审计并覆盖 group 波产物（F329 的完整版） |
| F388 | **成立** | P2 维持，无异议 | `_audit_context_coverage`（chapter_loop.py:2104-2125）docstring 声称"pipeline resume 初始化时调用"，cli 从未接线（本轮 grep 0 命中） |
| F389 | **成立** | P2 维持，无异议 | `style/linguistic_baseline.json` 唯一写入方 `establish_baseline`（skill_utils/drift_detection/baseline.py:78）全仓 0 调用方（grep 实证）；`_check_linguistic_drift`（chapter_loop.py:2013-2019）缺基线 → `return None` → spec §3.4 三级干预生产不可达；生产 style/ 仅 style_profile.md（本轮实测） |
| F390 | **成立** | M 维持，无异议 | cli.py:533-535 `Path(args.feedback).read_text(...)` 在 try 块内；:569-571 `except FileNotFoundError: "project not found"` → feedback 文件缺失误报项目不存在 |

### 十轮（F391-F395）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F391 | **成立** | P2 维持，无异议 | dispatch_helper.py:3-6 模块 docstring 声称 "The dispatcher runs G1 and G2 internally"；:1827-1841 API/IDE 两条路径均无 G1/G2 调用（本轮 grep `run_gate_g1\|run_gate_g2` 于 src/shenbi/pipeline/ 0 命中）；`_resolve_read_path`（:390-407）对缺失文件返回 [] → 缺失 reads 静默排除 |
| F392 | **成立** | P2 维持，无异议 | `_write_one`（:1113-1119）`_validate_json_output` 的 ValueError 被 raise 重抛；`_dispatch_via_api`（:1657-1663）仅捕 DispatchWriteFailureError → ValueError 冒泡至 CLI 顶层（cli.py 仅捕 FileNotFoundError） |
| F393 | **成立** | P2 维持，无异议 | `_extract_hook_deliverables`（review_checklist.py:315-375）只解析 frontmatter `hooks` 列表（无 body 兜底）；生产 pending_hooks.md frontmatter 无 `hooks` 键（本轮实测，body 表格含 P0-* 命中）→ 注入每个审计 prompt 的 hook_deliverables 恒空 |
| F394 | **成立** | M 维持，无异议 | crash_recovery.py:39-42 注释声称 "remove any atexit hooks registered by earlier tests" 但从未调用 atexit.unregister（本轮 grep src/ tests/ 0 命中） |
| F395 | **成立** | M 维持，无异议 | dispatch_helper.py:1548-1551 `except Exception` 对任何 API 异常调用 `_handle_timeout_gracefully`（:116-126 仅 log，无保存动作）→ 非超时错误也打 "dispatch_timeout ... saving_partial_output" 误导日志 |

### 十一轮（F396）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F396 | **成立** | P2 维持，无异议 | `_WARNING_RE`（triggers.py:326-329 `(?:warning|drift|fatigue)\s*[:\uff1a]\s*(.+)`）对 production `truth/audit_drift.md`（本轮实测 `## Ch55 — 推进/转折` + `- [场景临场感] ...` 子弹格式）与 drift-guidance 规范 YAML（`severity: warning` 在值位）均 0 命中 → spec §6.6 genre-config 更新触发恒 False，`config.genre_config_update_on_drift`（默认 True，triggers.py:438 唯一消费点）全路径无功能效果 |

### 十二轮（F397-F399）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F397 | **成立** | **P1 维持；支持 P1→P0（决策表"数据损坏/丢失"字面命中；裁定留 stage 4）** | 本轮三重实证：(a) 契约 `write_semantics` 6 个累积文件全 `append_dedup`（本轮 load_contract 输出 `{current_state/particle_ledger/emotional_arcs/subplot_board/chapter_summaries: {mode: append_dedup, key: chapter}, pending_hooks: {mode: append_dedup, key: hook_id}}`）、`reads` 仅 `['chapters/chapter-N.md']`；(b) `_write_parsed_outputs` 整文件覆写（:1127），docstring :1061-1067 明示 append_dedup NOT branched here 并错误假设 "state-settling skill calls write_truth_file"（LLM skill 无法执行 Python）；(c) `write_truth_file` 唯一生产调用 chapter_loop.py:3014 在串行块（F301 下不可达，本轮 grep 实证）；(d) 生产 56 章书 `truth/chapter_summaries.md` 仅 2 章条目（本轮实测 `grep -c "^## 第"` → 2）、`emotional_arcs.md` 仅 1 条 |
| F398 | **成立** | M 维持，无异议 | cli.py:661-669：PER_CHAPTER approve 后 resume 时 `current_chapter=N+1`，而 `plans/chapter-(N+1)-plan.md` 由 step 2 才创建（守卫仅豁免 ch=1，`if ch > 1` 反而每章边界触发）→ 每章一次假阳性告警（本轮重读确认） |
| F399 | **成立** | P2 维持，无异议 | cmd_next（:588-618）无任何 checkpoint_history 检查、不调用转换；GENESIS_COMPLETE approve 后 next → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint 无转换、恒报 OK；VOLUME_BOUNDARY approve 后 next → step_index=1（C1，cli.py:243-245/263-265）跳过触发块 → 延迟快照与 `_update_total_chapters`（仅 cmd_resume:748-755）永不执行 |

### 十三轮（F3A0-F3A1）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F3A0 | **成立** | P2 维持（latent），无异议 | `_build_skill_prompt` "Files to create" 清单跳过全部通配路径（dispatch_helper.py:718-721 `if "*" not in p`）；shenbi-character-design 契约 writes 含 `characters/major/*.md`、`characters/minor/*.md`（本轮 load_contract 实证）而 `gates/g4/character_design.py:118-155` **无条件**要求 major ≥3 / minor ≥2 文件（本轮读码确认）→ genesis step 3 与卷边界 expand 触发步 G4 缺口；生产 characters/ 仅 protagonist.md + relationships.md（本轮实测） |
| F3A1 | **成立** | M 维持，无异议 | dispatch_skill 的 test_type/round_dir/skip_reads 形参（:1796-1798）在 API/IDE 路由（:1827-1841）被静默丢弃，仅 legacy 子进程路径使用（:1857/:1858/:1850-1862）；docstring :1814-1816 声称三参数语义；生产调用方无 round_dir=（grep 实证） |

**误报统计**：F300-F399 + F3A0-F3A1 中仅 **F325** 为误报（二轮撤销、三~十三轮维持，本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名 + `_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：0 条新增。立场声明（均无新增异议）：
1. **F397（P1→P0）**：决策表 P0"数据损坏/丢失"字面命中——state-settling 累积 truth 文件每章被整文件覆写为仅当章内容、前章行确定性丢失且无任何错误信号（本轮契约+生产三重复证）；但正文文件不丢、LLM 行为有微小自愈可能，P1 亦可辩护——**裁定留 stage 4**。
2. **F364（P1→P0）**：决策表"数据损坏/丢失 + 契约被静默违反"字面命中——正常退出确定性清空待审 staging + 持久化 current_step 被改写为 EMERGENCY_SHUTDOWN；但 staging 门控由人工 review 兜底、正文文件不丢，P1 亦可辩护——**裁定留 stage 4**。
3. **F302（P1→P0）**：决策表 P0 例字面列明 "TokenLedger 少计"——账本系统性少计 + chapter 恒 0；但账本为成本侧车不影响正文执行，P1 亦可辩护——**裁定留 stage 4**。
另注记（非正式异议）：F353/F371/F373/F379 贴 P0 边界（"pipeline 永不完成"根因族，F324 修复后激活）。

**事实修正（不改严重度）**：0 条新增（F358 机制前置修正与 F324 修复完整性注记已在九轮完成；F378 影响面修正已在十二轮完成；本轮复证无新修正；F324 新增"当前 skill 模板仍为中文格式"的版本偏移排除证据）。

---

## 二、新发现（F3A2-F3A4）

> 编号说明：任务书指定 F400-F4ZZ，但该段已由 Z4 分区占用（Z4.review*.md 已用 F400-F490）；按任务书"若与 Z4 冲突用 F3A0-F3ZZ 替代"沿用 F3A0 段（全仓检索 F3A2-F3ZZ 无占用，无冲突）。

### F3A2 | plan_skeleton 卷末章 `next_chapter = min(chapter+1, ch_end)` 自指：卷末章的 section 6 "Next chapter" 渲染为同一章（Ch{N} role 重复当前章节点） | error | M（latent——F324 修复后激活）

- 证据：`plan_skeleton.py:86`（`next_chapter = min(chapter + 1, ch_end)`——当 `chapter == ch_end`（卷末章）时 `min(ch_end+1, ch_end) = ch_end = chapter`，next_chapter 与当前章相同）；`:88`（`next_node = _extract_chapter_node(volume_map_text, next_chapter)` 因此取回同一章节点）；`:137-144`（section 6 渲染 "Next chapter (Ch{next_chapter}) role: {next_node['role']}"）。
- 根因：卷末章的"下一章"语义应为下一卷首章（或"本卷结束"），`min` 截断使自指；`_resolve_volume_at_runtime` 只返回 (name, start, end) 无跨卷信息。
- 验证命令+输出（本轮 /tmp 沙箱实验，唯一新验证手段）：
  ```
  uv run python -c "
  from pathlib import Path; import tempfile, re
  d = tempfile.mkdtemp(); Path(d+'/outline').mkdir(parents=True)
  Path(d+'/outline/volume_map.md').write_text('## Volume 1: Arc One\nChapter End: 15\n\n## Volume 2: Arc Two\nChapter End: 30\n\n## Chapter Nodes\n| 15 | closing | final fight |\n| 1 | opening | start |\n')
  from shenbi.pipeline.plan_skeleton import generate_plan_skeleton
  s = generate_plan_skeleton(Path(d), 15)
  m = re.search(r'Next chapter \(Ch(\d+)\)', s); m2 = re.search(r'Next chapter \(Ch\d+\) role:\*\* (.+)', s)
  print('next_chapter:', m.group(1) if m else '?', '| next node role:', m2.group(2) if m2 else '?')"
  # → next_chapter: 15 | next node role: closing   （章 15 的 section 6 声称"下一章"是 Ch15 自己）
  ```
- 影响：F324 修复（volume_map 可解析）后，卷末章（如 15/30/45…）的 plan skeleton section 6 向 LLM 提供"下一章 = 本章"的自指信息（End-of-Chapter Change 的过渡指引错误）；LLM 以 skeleton 为可编辑上下文，通常能自我纠正，但属确定性错误内容（对照非卷末章 `min(ch+1, ch_end)=ch+1` 正确）。当前被 F324 屏蔽（boundaries 空 → resolved None → `next_chapter = chapter + 1` 分支）。
- 建议方向：卷末章时 `next_chapter` 取下一卷首章（用边界集推导）或置 "volume_end" 标记并在 section 6 渲染"下一章：新卷首章（待定）"；与 F324 的解析器修复一并处置。

### F3A3 | pipeline-state.json 损坏/版本不匹配时 machine.load_state 抛 JSONDecodeError/ValueError，cli 六个命令仅捕 FileNotFoundError → 裸 traceback（无 emit_json、无 checkpoint） | error | M

- 证据：`machine.py:29-37`（`load_state` → `PipelineState.from_json`，:35）；`state.py:366-375`（`from_json` 对损坏 JSON `log.error("state_json_decode_error")` 后 **re-raise** JSONDecodeError）；`state.py:324/344/346`（`from_dict` 对无效 phase/closure/checkpoint type 值 `PipelinePhase(data.get("phase", "genesis"))` 等抛 ValueError）；`cli.py` 六个读写命令（cmd_init :480、cmd_status :480、cmd_review :569、cmd_next :613、cmd_resume :785、cmd_chapters :800）**全部仅捕 FileNotFoundError**；`cli.py:931-935`（main() 无顶层异常处理）。
- 根因：状态文件的错误出口只覆盖"文件缺失"，未覆盖"文件损坏/非法枚举值"（与 F369 的 seed 缺失、F390 的 feedback 缺失同族：错误分类不全）；safe_write 原子写保证正常流程不会产生损坏 state，但人工编辑/版本回退/磁盘故障可触发。
- 验证命令+输出（本轮实际运行）：
  ```
  uv run python -c "
  from shenbi.pipeline.state import PipelineState
  try: PipelineState.from_dict({'phase': 'bogus-phase'})
  except Exception as e: print('from_dict invalid phase ->', type(e).__name__, str(e)[:60])
  try: PipelineState.from_json('{not valid json')
  except Exception as e: print('from_json corrupt ->', type(e).__name__, str(e)[:60])"
  # → from_dict invalid phase -> ValueError 'bogus-phase' is not a valid PipelinePhase
  #   from_json corrupt -> JSONDecodeError Expecting property name enclosed in double quotes: ...
  grep -n "except" src/shenbi/pipeline/cli.py   # → 仅 141/165(JSON 读) 351(FileNotFoundError) 385(TimeoutError) 393(Exception) 480/569/613/785/800(FileNotFoundError) 878(Exception)
  ```
- 影响：`pipeline next/resume/review` 遇到损坏或版本不匹配的 pipeline-state.json 时输出完整 traceback（破坏 cli_utils emit_json 的机器可读 stdout 约定），无 JSON 错误信封、无 checkpoint 保留说明；与 F304/F392 的"未捕获异常穿透 CLI"同族但异常源不同（状态文件反序列化 vs dispatch 写路径）。属边界/错误处理缺陷，M 级。
- 建议方向：`load_state`/`from_json` 的损坏路径改为返回结构化错误（或在 cmd_* 统一捕 `(JSONDecodeError, ValueError)` → `emit_json({"status": ERROR, "message": "pipeline-state.json corrupt or incompatible version"})` + return 1）；与 F369/F390 的 CLI 错误出口统一。

### F3A4 | `_append_integrity_findings` 对 `.integrity-findings-{N}.jsonl` 的无锁读-改-写：并行审计波两线程并发追加同一诊断文件时 lost-update（丢记录/交错覆盖） | error | M

- 证据：`dispatch_helper.py:1026-1041`（`existing = out.read_text(...) if out.exists() else ""` → 追加 → `safe_write(out, existing)`——无任何锁）；`:1150`（`_write_one` 内调用）；并行审计波（`parallel_dispatch.py:171` ThreadPoolExecutor(max_workers=len(tasks))）中每个审计 dispatch 在独立线程执行 `_write_parsed_outputs → _write_one → _append_integrity_findings`——同一章（同一 N）两个审计同时命中 `_append_integrity_findings` 时（no_verdict/line-ref skew/stale 等完整性 issue 常见）并发读-改-写同一 `.integrity-findings-{N}.jsonl`。
- 根因：该函数未使用 `truth_io._path_lock` 类 per-path 锁；safe_write 只保证单次写原子，不保证读-改-写事务串行（truth_io.py:10-14 文档自述的 §2.2 lost-update 竞态形态在此独立复现，但作用于诊断 jsonl 而非正文）。
- 验证命令+输出：
  ```
  sed -n '1026,1042p' src/shenbi/pipeline/dispatch_helper.py   # → 无 lock 引用（读码实证）
  ls novel-output/xinghuo-ranqiong/audits/.integrity-findings-* 2>/dev/null | head -1   # → 无输出（旧版管线未产生；当前并行波路径潜伏）
  ```
- 影响：并行审计波中同章两个审计同时写诊断 jsonl 时，后写者覆盖先写者的追加（先写者的 integrity findings 记录丢失）或交错内容；该文件仅供 G4/排查读取（best-effort 诊断，主信号已经 structlog 落日志），故不造成正文数据损坏——M 级。与 F326（并行 post-draft 写 staging 竞态）同族但影响面远小（诊断 artifact）。
- 建议方向：`_append_integrity_findings` 改用 `truth_io._path_lock(out)`（或统一走 `write_truth_file` 的 per-path 锁），或把该文件写入下沉到主线程（consolidate 后统一追加）。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件逐行重读；`__init__.py` 1 行 → `chapter_loop.py` 3053 行 → `dispatch_helper.py` 1986 行 → `cli.py` 939 行 → `triggers.py` 656 行 → `state.py` 536 行等全部读完；`contracts/paths.py`、`gates/shared.py`、`gates/g4/character_design.py`、`dispatcher/executor.py`、`dispatcher/modes/internal.py`、`skill_utils/escalation/check.py`、`safe_write.py` 作为证据文件交叉阅读）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries → set()`、`_resolve_volume_at_runtime → None`、`_count_total_chapters → 0`、`is_volume_boundary(15) → False`；novel.json keys 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0；truth/pending_hooks.md frontmatter 无 hooks 键、body 为 P0-* 表格；truth/audit_drift.md `- [维度]` 子弹格式对 `_WARNING_RE` 0 命中；truth/chapter_summaries.md 仅 2 章条目；style/style_profile.md 含 confidence: low；audits/ 722 文件全旧式名）；当前版本 skill 模板格式对照（`shenbi-volume-outlining/SKILL.md:135-139`、`shenbi-story-architecture/SKILL.md:108` 中文格式 → F324 版本偏移排除）；`uv run python` 实证：**synthetic volume_map 沙箱 `generate_plan_skeleton` 卷末章 next_chapter 自指**（F3A2）、**损坏 state 反序列化 ValueError/JSONDecodeError**（F3A3）、`_count_total_chapters`/`is_volume_boundary`（F324）、load_contract 契约 reads/writes/updates/write_semantics（state-settling/group-factual/character-design/volume-outlining/resonance）、`_append_integrity_findings` 无锁读-改-写读码实证（F3A4）。
- 前十三轮覆盖空洞（本轮已补）：(1) **plan_skeleton 卷末章 next_chapter 自指**——前几轮审过 F314（bridge 窗口）与 F324（节点提取），从未验证 `min(chapter+1, ch_end)` 在卷末章的行为 → F3A2；(2) **损坏 pipeline-state.json 的反序列化异常穿透**——F304/F392 只覆盖 RetryExhaustedError 与 decisions JSON ValueError，未覆盖 `from_dict` 枚举值 ValueError/`from_json` JSONDecodeError 的 CLI 传播 → F3A3；(3) **`.integrity-findings-*.jsonl` 无锁读-改-写竞态**——前几轮审过 F326（staging 竞态）与 truth_io 的 per-path 锁设计，从未对照 `_append_integrity_findings` 的裸读-改-写 → F3A4。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 的实际 codex 输出格式未做端到端执行验证（F383/F392 为静态推演 + 单元实证）；(c) 测试集未全量重跑；(d) skills/ 契约文件与 gates/ 检查器作为证据使用（不在 Z3 清单内），group skill 契约旧式 writes 是 F325 反证的关键、character_design G4 是 F3A0 证据的关键。

---

## 四、汇总

- **新 finding 数**：3（F3A2-F3A4，全部 M 级；**无新增 P0/P1/P2**——编号沿用 F3A0 段，因 F400-F4ZZ 已由 Z4 占用）
- **误报数**：0 新增（F300-F399 + F3A0-F3A1 中仅 F325 为误报，二~十三轮已撤销、本轮独立复证维持；F348 维持"成立但机制描述部分不准"；F378 十二轮影响面修正维持）
- **严重度异议数**：0 新增（立场声明：支持既有 F302/F364/F397 的 P1→P0 依据并裁定留 stage 4——F397 本轮契约+生产三重复证、F364 代码级全链复核、F302 账本少计为成本侧车 P1 亦可辩护；另注记 F353/F371/F373/F379 贴 P0 边界）
- **0 新 finding 判定**：**否（未收敛到 0）** —— 本轮仍发现 3 条 M 级漏报（F3A2 plan_skeleton 卷末章自指、F3A3 损坏 state 反序列化异常穿透、F3A4 诊断 jsonl 无锁竞态）。但连续六轮（九轮 7 条 → 十轮 5 条 → 十一轮 1 条 → 十二轮 3 条 → 十三轮 2 条 → 本轮 3 条）均无新增 P0/P1/P2，本轮 3 条全属"边界/错误处理/文案"类别：F3A2/F3A4 为 F324 修复后或并行波下的潜伏 M 级缺陷，F3A3 为低概率外部事件（state 损坏）的错误出口缺口；P0/P1 级结论（F324 成立、F325 误报、F340/F341/F345/F353/F354/F364/F371/F373/F379/F397 成立）经本轮独立复证全部成立，**无任何反证**。
- **校准升级提示**：F3A2 建议并入 F324 的"volume_map 解析修复"一并处置（解析器 + 卷末章 next 语义一起修）；F3A3 建议并入 F369/F390 的"CLI 错误出口统一"族（FileNotFoundError 之外补 JSONDecodeError/ValueError）；F3A4 建议并入 F326 的"并行写竞态"族（per-path 锁统一）；F324/F353/F371/F373/F379 的"pipeline 永不完成"根因族维持阶段 4 复核优先级首位。

---

## 五、独立复核声明

本报告为 fresh-context 第十四轮独立复核：34/34 文件逐行重读；对 F300-F399 + F3A0-F3A1 的判定全部基于本轮独立 read/grep/uv run python/契约 load_contract/沙箱实验与真实产物对照（未复用前十三轮结论）；新增 3 条发现（F3A2-F3A4）均为前十三轮报告未以编号形式出现的观察，其中 F3A2 的验证手段（synthetic volume_map 沙箱实测 `generate_plan_skeleton` 卷末章 section 6 自指）与 F3A3（损坏 state 反序列化 ValueError/JSONDecodeError 实证）为本轮新证据路径；F324/F325/F364/F379/F397/F353/F371/F373/F340/F341/F345/F354/F357/F374/F396 的关键实证为本轮独立复现；对 F324/F326/F340/F341/F345/F353/F354/F364/F371/F373/F379/F397 逐条寻找反证——均未发现（其中 F324 另以当前 skill 模板格式对照排除了"旧版产物"版本偏移反证）。
