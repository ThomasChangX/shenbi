# Z2 分区第七轮独立复核报告（Z2.review7）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）
- 模式: fresh-context 全量重读 38 文件；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖），不依赖前七轮转述；另读跨区消费面验证裁决（gates/cli.py、g1.py、g2.py、g3.py、g3_independence.py、gates/shared.py、audit/{_shared,snapshot,write_audit,record}.py、safe_write.py、pipeline/dispatch_helper.py 关键段、round-exec.sh、first-novel.md、tests/unit/dispatcher/*、git show 99f91f0:tests/dispatch-subagent.sh）。
2. **独立验证方式**：`COVERAGE_FILE=/tmp/z2r7-cov .venv/bin/python -m pytest -o addopts="" -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前七轮一致）；十余个 `python -c` 运行时探针（F216/F248 复现、F251 新证据、BACKUP_SKILLS glob 方向验证、from_markdown 跨捕获证伪、pipeline SKILL.md 注入对照）。
3. **前七轮判定**：F201-F249 共 **49 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；已有 5 条严重度异议（F214/F218→P1、F217/F228/F238→P1 低置信）经独立反证尝试后**全部支持**；本轮**新增 1 条低置信异议（F229 P2→P1）**并支持 F220 维持 P2。
4. **本轮新发现 1 条（F250）+ M 级观察 4 条**。本轮曾候选的 two 个新 finding 经实测后**主动撤回**：
   - `pacing_design.from_markdown` 的「beat 百分比跨捕获」候选——实测 `re.search` 的 `.*?` 默认不跨换行，逐行表格格式下散文提及行无数字 → 不触发（仅同行紧凑格式可触发，触发面过窄，降为 M 观察，不立 finding）；
   - `g1.derive_backup_skills` glob 更新不匹配候选——实测 `fnmatch.fnmatch(t, f)` 方向（t=concept 名作 string、f=`truth/*.md` 作 pattern）**能**命中，docstring 声称成立，候选撤回。
   - 唯一保留的新发现 F250：**独立 dispatcher codex 模式不注入技能定义**（无 SKILL.md/契约/rubric），codex agent 仅有裸 prompt——与 F225（字段过滤）、F243（评分角色）机械上独立，被 F227 掩盖。

---

## 一、复核结论表（F201-F249 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 三模型（chapter_planning/context_composing/volume_outlining）无生产消费者（grep 仅 test_skill_integration.py:188 类名文本）；g4 私有实现规则不同（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer 激活计划；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）；cov 34/38/40% 与报告吻合。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads（跨区 read 确认）。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g6.py:51 以 raw dict 读 target_word_count——死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证；g4/score_*.py 用 RouteC/RouteA 正则检查）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无生产加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点（audit_layer/revision_router/parallel_dispatch/route.py）+ chapter_loop.py:1797（"BLOCKING"/"FAIL" in text）、:409-438（G4Severity 自建词表）本轮 read 复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 GateStatus 实有，read 确认）；GateOutcome 无生产消费者（过渡类型）。 |
| F210 | P2 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing（74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知 gate 恒返回 0 → 修复方向应为「检查 rc!=0（CLI 崩溃）或 stdout 空 → 容错」，本体成立。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | P2 | **成立；支持异议→建议 P1** | **反证尝试**（独立路径不跑 G3 故 P2？）不成立——run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（read 复证；键 "agent" 非 "agent_id" → G3.5 prior_agents 恒空 → PASS）→ G3.4 fail-closed 以生产方自写证据放行。 |
| F215 | P2 | **成立** | `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（codex.py:75，read 复证 + 前轮复现）。 |
| F216 | P1 | **成立，P1 维持** | **本轮复现**：`GenreConfig.model_validate`（空 customRules + 1 disabled dim）→ PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。P1 维持。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | SKILL.md 内 6 处 CONSTELLATION 区间 + docstring 20-30 + 模型 [15,35]；模型对标准卷过松（25-35 过门违反 :91 上限）对开卷过严（36-40 误拒，:193 允许 30-40）。权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | fields.py:55-64 `_filter_md` 部分匹配 → 仅命中 section、matched=True、无 WARN（read + 前轮复现）；AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集复读通过。 |
| F220 | P2 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（含 foundation-review read 条目）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，建议维持 P2（不降 M）。 |
| F221 | P2 | **成立** | fixture 9 键含 tropeInventory，model_validate PASS 且 tropeInventory 被静默丢弃（extra=ignore）；SKILL.md:286/:292 声称 8 键；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段。四源漂移确认。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性）/g1（glob 未匹配）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except（read 复证）。 |
| F227 | P1 | **成立，P1 维持** | first-novel.md:38-41 文档化流程 = round-exec.sh 建脚手架（无 novel.json）后立即 `shenbi-dispatch shenbi-worldbuilding ...` → G1.1 novel.json not found → rc=1；`git show 99f91f0` 原 shell 注释 "the skill execution happened earlier" 证实门序回归（技能执行被并入 G2 之后的 dispatch_codex，codex.py:61-66）。反证不成立。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | derive_output_files 返回 `truth/*.md` 字面（audit/_shared.py:38-56 不展开 glob）；gate_G2 无 glob 展开 → 重试轮仍 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持；本轮新提低置信异议 P2→P1，见第三节）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现（因 writes 含 chapter-N-decisions.json）；g2.py:83-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失——主章节写作链路的散文校验洞。 |
| F230 | P2 | **成立** | `_filter_json:76` 裸 `k in fields`（无规范化），与 `_filter_md` canonical rule 不一致（read + 前轮复现）。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者。消费侧引用悬空确认。 |
| F232 | P2 | **成立** | decisions.py:33 `has = rationale is not None`——空串/纯空白满足 REQUIRED 且 ≤100 字符（read 复证 + 前轮复现）；routine+low 空串被拒，两侧语义不对称。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | P2 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | P1 | **成立，P1 维持** | `PROJECT_DIR = REPO_ROOT`（executor.py:26），watch 含 `truth/*.md` → snapshot 命中仓库根 tracked 文件 → `未声明写入` violations → rc=2；round_dir 真实写入不被审计（write overreach 检测空转）。反证尝试（审计面意图为仓库根）不成立——codex.py:62 cwd 已改 round_dir + test_executor_audit.py:18,40 编码 PROJECT_DIR==round_dir 正确语义。 |
| F236 | P2 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs，registry globs（`truth/*.md`）恒判未声明（read 复证 + 前轮复现）。 |
| F237 | P2 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | P2 | **成立；支持 review6 低置信异议（P2→P1）** | audit_writes（write_audit.py:53-56）对 malformed `## hooks` YAML 裸抛 ScannerError/ValueError；executor.py:263-268 finally 无捕获 → `record_audit_outcome` 永不执行（write-audit.jsonl 无 GATE_FAIL）——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | P2 | **成立** | derive_file_type worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归；`outputs & _truth_file_set()` 精确字符串交集，executor.py:81-85）；state-settling/plant → "truth"、chapter-drafting → "decisions"。 |
| F240 | P2 | **成立** | extract_chapter（paths.py:45-47）仅识别英文 "chapter N"；中文 prompt → chapter=None → N/NNN 输出全部丢弃 → G2 静默跳过（read + 前轮复现）。 |
| F241 | P2 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证 + 前轮复现）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`（get_ownership → None）；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"。生产写者 lifecycle 无条目 → 记录级写保护对生产路径空转。 |
| F243 | P2 | **成立** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（原第 4 参为独立评分 prompt）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak；g2.py:289-313 G2.11 对 **truth 输出**找 .bak；state-settling 输出集∉输入集 → G2.11 静默无检查。本轮补证：worldbuilding 的 `truth/*.md` 在 **writes**（非 updates）→ 不在 BACKUP_SKILLS → 重跑轮 truth 覆写亦无 .bak 保护（F244 同链扩展，实测 BACKUP_SKILLS 含 sequel-writing/truth-sync/state-settling、不含 worldbuilding）。 |
| F245 | P2 | **成立** | resolve_chapter_path 对所有 bounded N 一律按章节替换；resolve_volume_path 仅 closure.py:20,158 消费；`_build_skill_prompt`（dispatch_helper.py:656-658）对 volume-N/arc-N 等 chapter=None 时抛 UnresolvedPathError → 卷/弧技能 pipeline dispatch 崩（前轮复现 + read 复证）。 |
| F246 | P2 | **成立** | dispatch_skill（dispatch_helper.py:1864）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径）→ pipeline 继承 F227（fresh 轮 G1 失败）/F235（审计 rc=2）缺陷（read 复证）。 |
| F247 | P1 | **成立，P1 维持** | `_declared_patterns`（write_audit.py:19-26）以 chapter=None 派生声明集（N 路径全丢），watch 面（executor.py:220-225）以提取到的 chapter 解析 → 40 个 N 写技能成功派发恒判「未声明写入」rc=2（前轮端到端复现 + read 复证）。 |
| F248 | P2 | **成立** | **本轮复现**：`PacingDesign.from_markdown(md).chapter_sequence == []`（恒空）→ `_no_three_consecutive_same`（pacing_design.py:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）。 |
| F249 | P2 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500 → 4500-10000 字重要章被 G2.7 误拒（前轮复现 + read 复证）。 |

**误报统计**：整体误报 **0 条**（F201-F249 共 49 条全部实质成立）；部分误报 1 条（F220 计数 5→6，实质成立，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注维持（修复方向按"门 CLI 崩溃"修正，本体成立）。

---

## 二、新发现（F250-F251）

### F250 | 独立 dispatcher codex 模式不注入技能定义：codex agent 仅有裸 prompt（无 SKILL.md/契约/rubric），无法产出契约一致输出 | 漏报（正常路径功能缺陷，F227 掩盖） | P2
- 证据: `src/shenbi/dispatcher/modes/codex.py:61-62`（`["codex", "exec", "-C", str(round_dir), "-o", str(raw_out), prompt]`——唯一指令输入是 CLI 裸 prompt，无 system prompt、无 SKILL.md、无契约 reads/writes、无 rubric）；`grep -rn "SKILL.md\|read_text" src/shenbi/dispatcher/` → 仅 codex.py:29（progress.json）与 :74（raw_out），**无任何技能定义读取**；对照 `src/shenbi/pipeline/dispatch_helper.py:554-557`（`system_prompt = skill_file.read_text(...)`——pipeline 注入 SKILL.md 为 system prompt，独立路径无对称实现）；`docs/getting-started/first-novel.md:41`（`uv run shenbi-dispatch shenbi-worldbuilding generative <round_dir> "<prompt>"`，prompt 为短语如 "Build the world"）；`git show 99f91f0:tests/dispatch-subagent.sh`（原 shell 的 codex exec 第 4 参是**评分 prompt**，技能执行"happened earlier"由调用方完成；Python 翻译把**生成**并入该 exec 而未补齐技能上下文）。
- 根因: PR-20 把"技能执行（他处，调用方掌握完整技能上下文）"并入单一 codex exec 后，未随角色迁移注入技能定义——生成 agent 不知道契约文件路径（novel.json/genre-config.json/world/story_bible.md/truth/*.md）、输出 schema、SKILL.md 规则 → 产出落点/格式与契约 writes 无法对齐；G2（执行前）与审计（F235/F247 已坏）均无法事后兜底。
- 验证命令: `grep -rn "SKILL.md" src/shenbi/dispatcher/ --include=*.py` → 空；`grep -rn "read_text\|read_bytes" src/shenbi/dispatcher/ --include=*.py` → 仅 codex.py:29（progress.json 读取）、:74（raw_out 读取）；read codex.py:51-66 + dispatch_helper.py:554-557 对照；tests/unit/dispatcher/test_codex_mark_done.py 的 fake_run/raw fixture 证实 codex 路径只喂 prompt。
- 建议方向: standalone codex 路径组装完整技能 prompt（SKILL.md 作 system 上下文 + 契约 writes/reads 清单 + rubric 维度），或文档明示 standalone 为"薄壳、调用方须在 prompt 内自备技能定义"；与 F243（评分角色）、F225（字段过滤）修复联动，三者同属"standalone 路径信息缺失"家族但机制独立。
- 影响注记: fresh 轮被 F227（G1 先死）掩盖；F227/F235/F247 修复后，本缺陷独立阻塞 standalone 生成产出的契约一致性（重试轮 codex 跑通但写错路径/格式）——建议届时重评。

### M 级观察（随文记录）
- `pacing_design.from_markdown` beat/line-ratio 百分比**同行跨捕获**（F248 扩展）：`re.search(rf"{beat}.*?(\d+)\s*%")` 的 `.*?` 不跨换行——逐行表格格式下散文提及行无数字、不触发（本轮实测证伪"跨行捕获"）；仅**同行紧凑格式**（如 `爆发 是高潮。铺垫 | 25% 升级 | 30% 爆发 | 20% 余波 | 25%`）且 beat 名先于另一 beat 百分比出现时捕获错值（实测 → sum≠100 误拒）。触发面过窄，不立 finding，随 F248 修复一并改为表结构解析即可覆盖 (M)
- `codex.py:74` 若 codex exec rc=0 但未产出 raw_out（协议异常）→ `raw_out.read_text` 裸 FileNotFoundError（F223 协议边界防护扩展；loud 失败，触发面窄）(M)
- `executor.py:29,46` `_truth_files_cache`/`_decisions_files_cache` module 级永久缓存，与 `audit/snapshot.py:22-24` 显式去除 lru_cache 的注释（"truth-files.yaml can change across rounds in a long-running process, stale globs cause false negatives"）语义相反——理论 staleness（CLI 单进程无实害；若未来 executor 被长驻服务复用则 `derive_file_type` 分类固化）(M)
- `g3.py:160-174` G3.3 内部调 `gate_G2(output_files, ftype, str(rd))` **不传 project_dir** → `_is_important_chapter` 恒 False → 重要章上限 4500（与 F249 同根不同面；G3.3 因 F224 无 output_files 生产者而休眠，F224/F249 修复后需一并对齐）(M)

---

## 三、严重度异议（不修改前七轮，仅提理由）

1. **支持 F214 P2 → P1**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → G3.5 prior_agents 恒空 → PASS）→ G3.4 fail-closed 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持 F218 P2 → P1**：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 实测）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——权威区间未定（SKILL.md 内部 6 处自相矛盾），维持低置信。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：executor.py:245-246 文档化保证"崩溃仍审计"——`audit_writes` 对 malformed `## hooks` YAML 裸抛发生在 finally 内，使 `record_audit_outcome` 永不执行（审计记录丢失）；崩溃本身 loud，维持低置信。
6. **新提（低置信）F229 P2 → P1**：decisions 型技能（chapter-drafting/context-composing/chapter-revision——主章节写作链路）的 .md 散文在 G2 中被 `continue` 跳过（g2.py:83-84），G2.6 字数下限 / G2.8 PRE_WRITE_CHECK / G2.9 POST_WRITE_SELF_CHECK 对章节正文**整体静默缺失**——与 F228 同型（正常路径校验洞），受 F227/F247 掩盖故低置信，供协调者裁决（若 F227/F235/F247 修复后升 P1）。
7. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`COVERAGE_FILE=/tmp/z2r7-cov .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前七轮一致）。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F248 from_markdown chapter_sequence 恒空（3 连续同型规则死代码复现）
  - **F250（新）**：`grep SKILL.md src/shenbi/dispatcher/` → 空；pipeline dispatch_helper.py:554-557 注入 SKILL.md 对照；test_codex_mark_done.py 仅喂 prompt
  - **撤回候选 1**：from_markdown beat 跨捕获——逐行表格格式下 `re.search` 的 `.*?` 不跨换行 → 散文提及不触发（证伪"跨行捕获"）；同行紧凑格式可触发（sum≠100 误拒复现）
  - **撤回候选 2**：`fnmatch.fnmatch(t, f)` 方向验证——`truth/*.md` 更新命中 19 个 truth concept（BACKUP_SKILLS 含 sequel-writing/truth-sync/state-settling、不含 worldbuilding——后者 truth/*.md 在 writes 非 updates，F244 同链扩展）
  - F244 补证：worldbuilding 不在 BACKUP_SKILLS（truth 写为 writes 非 updates）
  - F243：`git show 99f91f0` 头注 + usage `[subagent_prompt]` 复证（原第 4 参为独立评分 prompt）
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c。

---

## 五、统计

- 新 finding：**1 条**（F250：P2×1）；M 级观察 4 条随文
- 误报：**0 条整体误报**（F201-F249 共 49 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注维持）
- 严重度异议：**6 条**（支持已有 5 条：F214→P1、F218→P1、F217→P1 低置信、F228→P1 低置信、F238→P1 低置信；**新提 1 条低置信：F229 P2→P1**）；另 F220 严重度存疑支持 1 条（建议维持 P2）
- 前七轮 findings 判定：F201-F249 共 49 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否**（F250 为新发现：独立 dispatcher codex 路径不注入技能定义——与 F225/F243 机械独立、被 F227 掩盖的独立根因；F227/F235/F247 修复后独立阻塞 standalone 生成的契约一致性）
