# Z6 分区第六轮独立复核报告（fresh-context 全量重读）

> 复核人：第六轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11（F601-F611）+ 一轮 8（F612-F619）+ 二轮 10（F620-F629）+ 三轮 7（F630-F636）+ 四轮 3（F637-F639）+ 五轮 3（F640-F642）逐条独立验证；新发现编号 F643 起。
> 方式：49/49 文件全文 read + `PYTHONPATH=src python3` 实际运行复现（F601/F605/F612/F614/F631/F636/F639/F641/F643/F642 均本轮回放）+ 全仓 grep 调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对（pending_hooks.md、resonance_trend.md、audit_drift.md、genre-config.json、progress.json、chapters/chapter-45/46.md、config-change-log.jsonl、56 章 opening 相似度全对扫描）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未完全达成**（新发现 1 条 P2；另有 F642 证据勘误 1 处、F601 严重度软异议 1 处）。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §3）
- 初审 11 findings：**11/11 成立**（F601 严重度软异议，见 §1.1 与 §4）
- 一轮 8：**8/8 成立**
- 二轮 10：**10/10 成立**
- 三轮 7：**7/7 成立**
- 四轮 3：**3/3 成立**
- 五轮 3：**3/3 成立**（F642 证据勘误——核心实证被本轮回放否定，见 §1.6 与 §4）
- 新发现：**1**（F643，P2）
- 整条误报：**0**；严重度异议：**1**（F601 软异议）；证据/影响勘误：**1**（F642）
- 判定：**非 0 新 finding（1 条）**——较前五轮（11/8/10/7/3/3）收敛至 1，接近收敛但未达 0

---

## 1. F601-F642 判定表（逐条独立复核）

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（**严重度软异议**：可辩 P2，见 §4） | 本轮直接运行：baseline dialogue=10 → current=1 → `max(…,5.0)=5.0` 不满足 `>5.0` → `is_drift: False, severity: NONE`。linguistic_drift.py:215/218 代码确认；docstring :185-188 承诺 "<20% of baseline" 触发 → off-by-one 成立。**反证核查**：无任何路径使纯对话塌陷置 is_drift=True。**但生产影响全潜伏**：`_check_linguistic_drift` 在无 baseline 时（F602）`return None`，detect_drift 从未被生产调用——影响链死。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持） | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py:6,28` re-export，**生产零调用**；`find . -name linguistic_baseline.json` 0 命中（含 novel-output style/ 仅 style_profile.md）；chapter_loop.py:2017-2019 无 baseline → `log.warning("no_linguistic_baseline")` + return None，:2722 每章走该分支。计划 07-19-07 Task 5a 显式接线指令未履行。P1 成立。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链：write_audit.py:54-56 → record.py:24 `blocked = bool(violations) or bool(drift)`。test_drift.py 无 YAML-ghost 反向用例。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'`，:226 `sum(text.count(c) for c in chars)` 按字符迭代 → 一对 ASCII 引号 count=4。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持；补充同根第三变体） | 本轮运行：空 terms → `compute_linguistic_metrics('参数系统阈值。'*5)` → `system_term_density: 1028.5714`；`system_terms: null` → `list(None)` TypeError；**`drift_detection: null`（整个 section 为 null）→ `None.get` AttributeError（本轮实测，:79-84 无类型守卫）**——三变体同根（配置驱动 terms 无类型校验），F605 建议的"非 list → 回退默认"一并覆盖。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs detect_drift 读 `dialogue_density`；:296/:335 硬编码术语 vs :107 config 驱动。真实项目 context/ 亦无 linguistic_baseline.json。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | cjk.py:54 桶 token 为相邻双字符 `""`/`''`/`「」`/`『』`，正常文本引号包围内容不相邻 → `text.count` 恒 0；:61-62 "whole token" 修复对引号桶反而永久为 0。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py 全文无 logging/structlog import；:40-43 两处 break 无日志、:47-48 safe_write 改写文件。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无。F622 为独立类型失败模式。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持；review1 证据细化正确） | config_coherence.py:109 `key.startswith("auditDimensions.")`；整键对象覆盖绕过。F631（falsy 值）/F638（snake_case 键）为独立向量；F643（本轮新，键缺失向量）为第四向量。P2 成立。 |

### 1.2 一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持，无反证） | 本轮直接运行：baseline stm=60 → current stm=110 → `is_drift: False, severity: ESCALATE`。chapter_loop.py:2022-2044 `if result.is_drift:` 包裹 log + ESCALATE raise + HARD/WARN 注入（:2033/:2037/:2039 代码精读确认）；opening-similarity 检查 :2047-2056 在门**外**（与 F642 证据一致）。计划 07-19-07:749 绝对阈值触发契约。P1 成立。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"破折号": "——"`/`"省略号": "……"` 重复字符 + :226 按字符迭代 → 一个 `——` 计 4。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | 本轮运行：`{'some.new.key': 1, 'resonance_global_floor': 58}` + 60 字 rationale → ConfigError；config-change-log.jsonl 已有 1 条幻影条目、genre-config.json 无 `some.new.key`。config_coherence.py:105-133 循环内逐 key `_append_audit_trail`，safe_write 在循环外 :142。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | writer.py:57 `json.loads(lines[-1])` 无 try；replay.py:38-43 同文件可自愈 → 不对称。三个生产调用点均有外部缓解（safe_write.py:127-135 / audit/record.py:42-46 / migrate 先 replay）。P2 成立。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | 本轮 grep：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` → 仅 compute_stats.py:37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。P2 成立。 |

### 1.3 二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持，无反证） | 本轮代码精读：chapter_loop.py:2722-2728 `try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed")`；函数内 :2034-2038 raise DriftEscalationError（"Pipeline paused for human review"）；:2719 注释自称 "non-blocking"——调用点与函数内契约自相矛盾，`except Exception` 连带吞掉其他真实错误。grep 唯一生产调用点即 :2722。P1 成立。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目；:170 main 用默认值 "未分类" 且词表外字符串原样保留。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | recall.py:41-46 `.get()` 只判 None 不判类型 → `int - str` / `int > str` TypeError。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持） | compute_stats.py:52 单 U+2026 vs 真实文本双 `……`（与 :23 PUNCT_MAP 一致）。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无 skills 校验 | P2 | **成立**（P2 维持） | 本轮规范化重算：master 59、shenbi-* 目录 73（+using-shenbi 共 74）、缺失恰好 15 个（名单与 review2/3/4/5 一致）。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、`TraceEvent.schema_version` Field(ge=1) 阻止 v0 构造、migrate_to_current 无生产调用方——g7 只用 assert_monotonic）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | 本轮 grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export，src 内无生产调用。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件；对比 records/drift.py:38-39 非 `|` 行即 break。真实 resonance_trend.md 单表 → 潜伏。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | compute_stats.py:16 SENT_ENDS 含 `；`，:63 segment_sentences 只按 `。！？\n` 切句，:87 segment_paragraphs 用 SENT_ENDS。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | 本轮 grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用。P2 成立。 |

### 1.4 三轮 F630-F636

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | **成立**（P2 维持） | 代码确认 revision_router.py:63-64（注释明示 "config.resonance_global_floor" 后跟 `= 50`）vs state.py:72 `resonance_global_floor: int = DEFAULT_THRESHOLDS.resonance_global_floor`（65）；chapter_loop.py:1897 显式传 `state.config.resonance_global_floor` 覆盖默认值 → 潜伏。P2 成立。 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | **成立**（P2 维持） | 本轮运行：`{'auditDimensions.texture': 0}` + 短 rationale → ACCEPTED；G0 → []；`get_active_genre_audits` → texture 审计不激活。config_coherence.py:109、g0_config_coherence.py:92 均 `is False`（本轮核对行号：g0 为 :92）。P2 成立。 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | **成立**（P2 维持） | linguistic_drift.py:222-229 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50；thresholds.py:8-10 docstring 自称单源。P2 成立。 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | **成立**（P2 维持） | compute_stats.py:79 docstring vs :80 `re.split(r"\n\s*\n")`；`\n` ∈ SENT_ENDS :16。P2 成立。 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | **成立**（P2 维持） | 本轮复核：chapter-45.md 全文 11702 字符 / META 块约 2877 字符（len(strip) 8825）；chapter-46.md 全文 13350 / META 1350（len(strip) 12000）——META 占比 12-25% 因章而异，两处统计入口（linguistic_drift.py:94-131、compute_stats.py:311-362 + establish_baseline 亦经 compute_linguistic_metrics）均无剥离。P2 成立。 |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | **成立**（P2 维持） | 本轮 grep：`update_genre_config` 仅 config_coherence.py 自身 + 测试 + 计划文档；真实 config-change-log.jsonl 仅 1 条 2026-07-19 E34 一次性修复产物。P2 成立。 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | **成立**（P2 维持） | 本轮直接运行：base 全 0 + current em_dash=0.33 → `is_drift: True, severity: WARN`。linguistic_drift.py:202。P2 成立。 |

### 1.5 四轮 F637-F639

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转（格式分叉 + 测试掩盖） | P1 | **成立**（P1 维持，无反证） | 本轮全链复核：真实 pending_hooks.md（56 章）标题仅 `# 伏笔追踪`/`## 第56章伏笔呈现`/`## 第56章生命周期状态更新（foreshadowing-track）` 等章节附录结构，**无 `## hooks`/`## 活跃伏笔`**（`grep -c "## hooks"`=0）；ch-025 快照有 `## 活跃伏笔`(:12)/`## hooks`(:20) → 格式分叉成立。运行：`parse_records(真实文件)` → []、`parse_markdown_table` → {}、`detect_cross_section_drift` → []。消费链：write_audit.py:54-56（对每个 post .md 跑三件套）→ record.py:24 `blocked = bool(violations) or bool(drift)`。P1（"测试失效掩盖真实缺陷"档）成立。 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过 Rule 1 与 G0） | P2 | **成立**（P2 维持） | audit_layer.py:105-107 snake_case 回退 vs config_coherence.py:109/g0_config_coherence.py:79 只认 camelCase。真实配置 camelCase → 潜伏。P2 成立。 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 真实项目 arc/volume 半边不运行 + audit 追加无协调 | P2 | **成立**（P2 维持） | 本轮运行：`--resonance /nonexistent/trend.md --arc-payoff /nonexistent/arc.md` → 无输出 exit 0；真实项目 truth/ 仅 resonance_trend.md（1 行 ch55），无 arc_payoff_trend.md/volume_score_trend.md（仅 emotional_arcs.md，非期望名）；audit_drift.md 现有格式为 `## Ch55 —` 章节式（review-resonance 写），CLI `_append_audit` 追加的 `## drift findings` 段与之无协调。P2 成立。 |

### 1.6 五轮 F640-F642

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F640 | materialize_progress 输入契约零生产者 → 无条件覆盖真实 progress.json（数据丢失+错误结果） | P1 | **成立**（P1 维持；补充同链第三死函数） | 本轮全链复核：`grep '"INIT"\|"MARK_DONE"'` src/ → 仅 materialize.py:49,53（消费方）；生产 TraceWriter.append 调用点全集 = audit/record.py:44（GATE_FAIL/AUDIT_PASS）+ safe_write.py:132（trace_action，生产唯一传参处即 materialize.py:98 自身）。调用点：chapter_loop.py:691-697 `steps_done % 5 == 0`（含 0）→ :2691/:3028；`_auto_rebuild_progress_if_stale`（:702-741）检查 `project_dir/"trace"` **目录**而 TraceWriter 写 `project_dir/"trace.jsonl"` **文件**（writer.py:42）→ 路径错位使自愈分支永不触发。真实 progress.json schema 为 `current_scorer_agent`/`scoring_history`（本轮核实），与 materialize 输出 schema 不同。**补充（同链第三死函数）**：`compact` 与 `migrate_from_progress` 同样全仓零生产调用（仅 trace/__init__ re-export + 测试；g7 只用 verify_chain）——F640 证据中"migrate.py:30（LEGACY_MIGRATION）"实为 migrate_from_progress 内部调用，而该函数本身无调用方 → 该 append 从不执行，**Tier A 事件溯源除 audit seam 外整体未接线**。P1 成立（触发即数据丢失，激活应升级 P0）。 |
| F641 | records `_values_equal` 布尔比较孔 → YAML true vs md "true" 假 drift | P2 | **成立**（P2 维持） | 本轮运行：`_values_equal(True, 'true')` → False；`detect_cross_section_drift([{'id':'h','state': True}], {'h':{'id':'h','state':'true'}})` → 假 drift 1 条。drift.py:57-69 float 兜底对 bool 抛 ValueError 落入 return False。P2 成立。 |
| F642 | check_opening_similarity 比较 META 块非正文开头，真实对 (45,46) 0.627>0.6 | P2 | **成立但证据勘误（重大）**：代码层面成立（比较对象=META 块）；**实证层面被否定**——见 §2.2 | 代码层面：linguistic_drift.py:249-253 取 `[:300]` 首 300 字符，真实章节以 `<!--META-BEGIN-->…<!--META-END-->` 开头（chapter-45/46 均确认）→ 比较对象确为 META 块，语义承诺（正文开头相似度）与测量对象不符，成立。**但 review5 的核心实证"去 META 后 0.57 < 0.6，证实 META 是越过阈值的主因"被本轮回放否定**（详见 §2.2）：META 剥离后 (45,46) 相似度 **0.637，仍 > 0.6**，假阳性在纯正文上依然触发 → 因果归因错误、建议方向（剥离 META）不足以修复。 |

**严重度异议：1 条（F601 软异议，见 §4）。** 其余五条 P1（F602/F612/F620/F637/F640）本轮全部以直接运行/代码精读/全仓 grep 复核，无反证，定级符合决策表 P1 档（显式契约静默失效 / 测试失效掩盖真实缺陷 / 数据丢失）；P2 均属边界/统计口径/死代码/潜伏类，无高判或低判。

---

## 2. 新发现与勘误

### F643 | 治理链对"键缺失"的默认语义自相矛盾：G0 视缺失为启用（`get(dim, True)`）、audit_layer 视缺失为禁用（`get(dim_key, False)`）→ 从 auditDimensions 移除关键维度（或置空对象）静默停用审计且两道治理同时失明 | 治理绕过（F611/F631/F638 同族第四向量） | P2

- 证据：
  - `src/shenbi/gates/g0_config_coherence.py:92`（`if audit_dims.get(dim, True) is False:` —— 注释明示 "Default True if the key is absent (only flag explicit disabling)" → **缺失=启用**，G0 不报 G0.cc.critical_audit_disabled）；
  - `src/shenbi/pipeline/audit_layer.py:113`（`audit_dims.get(dim_key, False)` → **缺失=禁用**，审计 skill 不激活）；消费方 `src/shenbi/pipeline/chapter_loop.py:2574-2575`（`genre_skills = get_active_genre_audits(genre_gc)` 决定哪些 genre 审计被派发）；
  - 写侧 `src/shenbi/config/config_coherence.py:109`（Rule 1 只拦 `key.startswith("auditDimensions.") and new_value is False` —— 整键替换时无任何 `new_value is False` 可触发）。
- 根因：三层治理对同一配置键的"键缺失"语义不一致——G0 缺省视为启用、audit_layer 缺省视为禁用、Rule 1 只认显式 False。于是**通过整键替换"移除"关键维度**（如 `changes={"auditDimensions": {"antiAi": True, "continuity": True}}` 把 texture 从对象中删除）或 `auditDimensions: {}` 置空：写侧 Rule 1 无 False 值可拦（不要求 rationale）、读侧 G0 因缺失→True 不报、audit_layer 因缺失→False 不激活 texture 审计——与 F611（整键带显式 False 值）、F631（falsy 标量值 0/null/""）、F638（snake_case 键形）并列的**第四绕过向量：键缺失/移除**，且是唯一一个"G0 读侧也失明"的向量（F631 的 G0 失明是值类型孔，本向量是缺省语义矛盾）。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3（/tmp 临时目录）
  # 写侧：整键替换移除 texture
  update_genre_config(d, {'auditDimensions': {'antiAi': True, 'continuity': True}}, rationale='x')
  -> ACCEPTED（无 ConfigError，rationale 仅 1 字符）
  -> 落盘 config: {'auditDimensions': {'antiAi': True, 'continuity': True}}  # texture 已移除
  # 读侧：G0 与 audit_layer 对同一配置的矛盾判定
  check_config_coherence(d, resonance_global_floor=65) -> []            # G0 不报（缺失=启用）
  get_active_genre_audits({'auditDimensions': {}}) -> []                # 全部审计不激活（缺失=禁用）
  get_active_genre_audits({'auditDimensions': {'antiAi': True, 'continuity': True}}) -> []
  ```
- 影响：genre-config skill（或人工）通过整键覆盖移除/省略关键审计维度（texture/antiAi/continuity）时，无任何治理信号——audit 层静默停用该维度审计、G0 审计检查仍通过、write-side 无 rationale 强制。当前真实配置 10 维全 True（本轮核实）→ 潜伏；与 F611/F631/F638 修复应统一处理（全链统一"缺失即显式声明"语义）。
- 建议方向：三层统一缺省语义——G0 改为与 audit_layer 一致（缺失=禁用并报 issue），或 audit_layer 改为缺失=启用（`get(dim_key, True)`）并让 Rule 1 覆盖"auditDimensions 整键替换导致关键维缺失"（比较新对象与旧对象的关键维集合）；补单测（auditDimensions 置空/移除 texture：G0 必须报 issue 或 Rule 1 必须拒绝）。

### F642 证据勘误（非整条误报，findings 本体成立但核心实证被否定）

- review5 声称："56 章全对扫描……META-stripped max: 0.57 (45, 46) # 剥离后全部 < 0.6"、"证实 META 是越过阈值的主因"。
- 本轮独立全量 56 章相邻对重扫（同一 `check_opening_similarity`、同一剥离方法 `re.sub(r'<!--META-BEGIN-->.*?<!--META-END-->', '', t, flags=re.S)`）：
  ```
  full（含 META）max:     0.627 (45, 46)   # 与本轮一致
  META 剥离后 max:         0.637 (45, 46)   # 剥离后更高，且仍 > 0.6
  META 剥离后 >0.6 的对:   仅 [(45, 46, 0.637)]
  ```
- 结论：**剥离 META 不能消除该假阳性**——(45,46) 的正文本身（去 META 后前 300 字符）相似度 0.637 > 0.6，触发与 META 无关。review5 的 "0.57 < 0.6" 与本轮实测 0.637 矛盾（差异原因无法追溯——review5 未公布剥离方法细节；本轮回放使用与其描述一致的剥离方法）。F642 本体（检查比较的是 META 块而非正文开头，测量对象与语义承诺不符）代码层面成立、影响（接线后真实章节假阳性触发 opening-variation 指令）依然成立，但**因果归因（META 是越过阈值的主因）错误、建议方向（剥离 META）不足以修复**——修复需同时下调阈值或改正文提取 + 阈值标定。

---

## 3. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2056 漂移检查全链、:2017-2019 baseline 读、:2022-2044 干预门控、:2047-2056 opening-similarity 门外交互、:2716-2728 ESCALATE 调用点吞异常、:684-741 materialize 调用、:1897 check_resonance 显式 floor、:2574-2575 get_active_genre_audits 消费、:3028 调用点）、revision_router.py（:63-64 DEFAULT_RESONANCE_FLOOR）、state.py（:72 floor 默认 65）、audit_layer.py（:100-113 激活语义）、g0_config_coherence.py（:35-42 关键维矩阵、:79-92 floor/audit 检查）、audit/write_audit.py（:54-56 records 消费）、audit/record.py（:24 block 语义、:42-46 trace seam）、dispatcher/executor.py（:240,266 audit_writes 接线）、gates/g7_trace.py（:50 assert_monotonic 消费——versioning 检查已接线）、safe_write.py（:126-140 trace_action 唯一生产调用点）、plugins/master.json（59 skills）、skills/ 目录（73 shenbi-* + using-shenbi）、novel-output/xinghuo-ranqiong/（pending_hooks.md 结构、resonance_trend.md 单表单行、audit_drift.md `## Ch55` 章节格式、genre-config.json 10 维 camelCase 无 drift_detection 键、progress.json scoring schema、chapters/chapter-45/46.md META 结构与 56 章 opening 全对扫描、config-change-log.jsonl 单条、style/ 与 context/ 均无 linguistic_baseline.json、无 trace.jsonl）、tests/fixtures/truth-pending_hooks.md + tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照）。
- 维持观察（不重复立项，供协调者参考）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）、compute_pattern main 非 dict 章节 `.get` 崩溃、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize round 名解析脆弱性、compute_drift `_append_audit` 重复追加无幂等、compute_connectives "时间"/"顺序" 类别重叠（"接着"/"然后" 同词双计入类）、`drift_detection: null` → AttributeError（F605 同根第三变体，已并入 F605 判定）、`compact`/`migrate_from_progress` 零生产调用（已并入 F640 判定）。

---

## 4. 严重度异议

| finding | 现定级 | 异议 | 理由 |
|---|---|---|---|
| F601 | P1 | **软异议：可辩 P2** | 缺陷本体成立（对话塌陷 off-by-one），但**生产影响全潜伏**：detect_drift 的唯一生产入口 `_check_linguistic_drift` 在无 baseline（F602）时于 chapter_loop.py:2017-2019 `return None`，detect_drift 从未被生产调用；即便接线，F612/F620 使干预仍不可达。按决策表，"死代码/未接线"（P2 档）与"违反显式契约"（P1 档）均可辩护——若协调者认定 docstring + 计划 07-19-07:566-567 的 "<20% 触发" 为显式契约，维持 P1 合理；若按"当前仓库内无任何执行路径"从严，P2 更贴切。**不构成整条误报，仅定级讨论。** |

（其余 41 条无严重度异议：五条 P1 中 F602/F612/F620/F637/F640 均经运行/代码/grep 复核无反证；P2 均无高判或低判。）

---

## 5. 返回摘要

**复核统计：新 finding 1（F643，P2）/ 整条误报 0 / 严重度异议 1（F601 软异议）/ 证据勘误 1（F642）。**

**判定：非 0 新 finding（1 条），未完全收敛——但收敛曲线显著（11→8→10→7→3→3→1）。**

- F601-F642 共 42 条全部成立（无整条误报）；其中 **F642 需证据勘误**（review5 的 "去 META 后 0.57 < 0.6" 被本轮回放否定：剥离后 (45,46) = 0.637 仍 > 0.6，假阳性与 META 无关，修复方向需重新标定），**F601 有严重度软异议**（可辩 P2），F640 补充同链第三死函数（compact/migrate_from_progress 零调用，Tier A 除 audit seam 外整体未接线）。
- 本轮 1 条新 finding（F643）落在前六轮未触达的**治理层"键缺失"默认语义矛盾**方向：G0 `get(dim, True)` 视缺失为启用 vs audit_layer `get(dim_key, False)` 视缺失为禁用 vs Rule 1 只认显式 False——整键替换移除关键维度（或置空 auditDimensions）静默停用审计且写侧 rationale、读侧 G0 同时失明（运行复现，与 F611/F631/F638 并列第四绕过向量）。建议与 F611/F631/F638 统一修复（全链统一缺失语义 + 补置空/移除回归测试）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F643 | 治理链"键缺失"默认语义矛盾：G0 视缺失=启用、audit_layer 视缺失=禁用 → 移除/置空 auditDimensions 静默停用审计 | P2 | g0_config_coherence.py:92; audit_layer.py:113; config_coherence.py:109; chapter_loop.py:2574-2575 |
| F642（勘误） | 实证错误：剥离 META 后 (45,46) 相似度 0.637 仍 > 0.6（review5 声称 0.57） | 证据勘误 | novel-output/xinghuo-ranqiong/chapters/chapter-45/46.md; linguistic_drift.py:249-253 |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review6.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
