# Z4.review3 复核报告 — src/shenbi/gates/（含 g4 checkers）第三轮独立复核

- 复核 agent：第三轮独立复核（fresh-context，全量重读，不依赖前三轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`（F419-F430）+ `Z4.review2.md`（F431-F439）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 24 个 checker 逐个通读，含 character_design.py 全文 256 行）
- 实测基线：`.venv/bin/python -m pytest tests/unit/gates/test_g3.py test_g1.py test_g2.py` → **90 passed**（子集实测；全量 785 passed 沿用前三轮基线）；本轮新增验证命令 20+ 组（`.venv/bin/python -c` 只读 + /tmp 临时目录构造；git status 复查无跟踪文件污染，tests/coverage 为 gitignored 既有产物）
- 复核结论：**误报 0**；**新增严重度异议 0**（确认前两轮 F404→P1、F408→P1 异议成立，本轮独立反证无推翻理由）；**新发现 6**（F440-F445，1×P1 + 4×P2 + 1×M，全部实测或代码路径验证）
- 结论：**0 新 finding 判定 = 否**（发现 6 个前三轮漏报，未收敛到 0）

---

## 一、复核结论表（F400-F439 逐条判定）

| 编号 | 初审严重度 | 前两轮判定 | 本轮判定 | 理由（本轮独立证据） |
|---|---|---|---|---|
| F400 | P2 | 成立 | **成立** | 实测 `gate_G2([manifest.json],'report')` → `G2.12 WARN 'may be truncated'`（本次复现）；g2.py:315-322 无 file_type 守卫，G2.10（273-287）同缺。 |
| F401 | P1 | 成立（masking 附条件） | **成立** | 反证核查：GR.2 解析（g_reconcile.py:52-62）`stem.rsplit("-",2)` → candidate_tt=`generative-scores`，progress 键为 `generative`（materialize.py:66-67 / codex.py:46 均为 `skills[skill][test_type]` 形状）→ `td={}` → `status != "DONE"` → 恒 FAIL。且 progress 无 skills 键时（run_gate_g3 伪造形状，见 F408）GR.2 对盘上任何报告也 FAIL。无推翻理由。 |
| F402 | P1 | 成立 | **成立** | length_normalizing.py:29 `pf = resolve_input_path(fp, rd)` vs :35 `word_count_md(fp)` 用原始 fp；test 全部绝对路径+rd=None。 |
| F403 | P2 | 成立 | **成立** | cli.py:113-121 无 try/except；11 个 RoundPaths checker 双 None 时 raise ValueError（本轮逐一读到 character_design.py:28-29 等）。 |
| F404 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 反证核查（直接读 contracts/schemas/decisions.py:30-45）：`has = rationale is not None`；`Selection(basis='manual_override', severity='low', rationale='')` → PASS。AGENTS.md:70 契约在正常路径被空串绕过。P1 无推翻理由。 |
| F405 | P2 | 成立 | **成立** | 实测 G4_CHECKER_SKILLS=22（shared.py:247-270）vs generic.py checkers 分发表 31 项；11 个缺注册技能目录全部 EXISTS（本轮复验）。 |
| F406 | P2 | 成立（影响面限定） | **成立**（影响面限定） | `ls -d skill-output` 不存在、`novel-output` 存在；g0.py:195/323、g7.py:66 用 skill-output；round-exec 测试 round 布局有 skill-output 的限定维持。 |
| F407 | P2 | 成立 | **成立** | g0.py:177-179 无 seed → `return passed("G0", [G0.1 SKIP])`，G0.2-G0.16/G0.cc 全跳过（读码确认）。 |
| F408 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 反证核查（直接读 dispatch_helper.py:1921-1945）：run_gate_g3 在 progress.json 缺失时 safe_write 写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history → G3.4 fail-closed 被击穿并制造虚假审计证据。P1 无推翻理由。 |
| F409 | P2 | 成立 | **成立** | g1.py:281-282 空输入仅记 G1.0 SKIP → PASS（读码确认）。 |
| F410 | M | 成立 | **成立** | 实测 11 个技能目录全部存在（本轮复验）；SHORT_MAP 仅 20 项。 |
| F411 | M | 成立 | **成立** | g0.py:220 `missing_dirs` 永不 append、:227 恒假；:532 注释 "20 skills" vs 22。 |
| F412 | M | 成立 | **成立** | g3_independence.py:3 引 g3.py:144-162，实际 G3.4 在 193-206（读码确认）。 |
| F413 | M | 成立 | **成立** | decisions_validator.py:175 引 shared.py:113；fail() 在 125-136。 |
| F414 | M | 成立 | **成立** | chapter_revision.py:97 `"HARD_FAIL"` 不在 GateStatus 词表（读码确认）。 |
| F415 | P2 | 成立 | **成立** | SKILL.md:125=`<!--META-END-->`，规则在 :140（`grep -n "不要包含章节号"` → 140）。 |
| F416 | M | 成立 | **成立** | decisions-schema.md:66-68 仅 low/high；decisions.py:12 含 medium。 |
| F417 | — | 成立 | **成立** | `ls tests/unit/gates/g4/` 复验：无 test_memory_distill.py；tests/unit/gates 无 test_gate_manifest.py。 |
| F418 | P2 | 成立 | **成立** | g0.py:665 `check_config_coherence(project_dir)` 未传 floor；g0_config_coherence.py:65 仅 floor 非 None 执行两类检测；docstring "state-config" 半场（函数体只读 genre-config.json）的延伸注记亦属实。 |
| F419 | P2 | 成立 | **成立** | g1.py:210-213 / g2.py:169-172 except 元组缺 ValueError（读码确认；`gate_G2` 对 `[1,2,3]` 输出实测 CRASH ValueError）。 |
| F420 | P2 | 成立 | **成立** | g6.py:49-59 novel.json/genre-config.json jload 无保护（读码确认）。 |
| F421 | P2 | 成立 | **成立** | g_reconcile.py:33 无 try；g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError。 |
| F422 | P2 | 成立 | **成立** | cli.py:83-87 JSON 解析失败 → `input_files=[]`（读码确认），叠加 F409 后 G1 CLI 面零保护。 |
| F423 | P2 | 成立 | **成立** | `ls tests/scoring.py` 不存在，实现在 src/shenbi/scoring.py（初审已实测，读码确认 g0.py:358）。 |
| F424 | P2 | 成立（影响面限定） | **成立**（影响面限定） | chapter_drafting.py:219-222 skill-output 爬升；真实布局下 `read_genre_config(project_root)` 读不到 → FATIGUE_BASE 兜底。pipeline 布局限定维持。 |
| F425 | P2 | 成立（影响面限定） | **成立**（影响面限定） | g7.py:66+86-91 G7.6 嵌在 `if no_dir.exists()` 之下；pipeline 布局恒 SKIP。 |
| F426 | P2 | 成立 | **成立** | genre_config.py:28 `str(Path(rd) / fps[0]) if rd and fps else fps[0]` 未走 resolve_input_path（读码确认）。 |
| F427 | P2 | 成立 | **成立** | chapter_revision.py:45 `Path(fp)` 原始路径（读码确认）。 |
| F428 | P2 | 成立 | **成立** | g3.py:103 直读路径用 `acceptance.t1`=94 vs :113/:125 回退硬编码 90（读码确认分叉）。 |
| F429 | P2 | 成立 | **成立** | g1.py:228-232 gate 内 safe_write .bak；AGENTS.md 纯验证契约偏离，FAIL 判定前已落盘（读码确认）。 |
| F430 | P2 | 成立 | **成立** | foreshadowing_track.py:41-46 裸词 `操作` 命中即 PASS（读码确认；review2 已实测）。 |
| F431 | P2 | — | **成立**（范围不全，见 F440/F441 扩展） | 枚举的 8 处调用点均读码确认（g1:260-262 / g3:148-149,195-196,210-211 / g5:55-57 / g7:34-35,56-57,239-246 / shared:229-235 / gate_manifest:30-35）；但**漏掉 g0.py 的 2 处与 G0.14 的 json.loads 直读**——见新 F440/F441。 |
| F432 | P1 | — | **成立** | 反证核查：ALL_SKILLS=74（实测）vs tests/tiers/t1-skill 69 项，缺 5 个无脚手架技能（实测名单一致）；G7.1b（g7.py:59）以 74 为全集 → 完整 round 恒 FAIL。无推翻理由。 |
| F433 | P2 | — | **成立** | chapter_drafting.py:331-334 条件判 project_dir、取值用 project_root（读码确认；同 F424 爬升缺陷第 5 消费方）。 |
| F434 | P2 | — | **成立** | state_settling.py:14/46-53 单字符子串匹配（读码确认）。 |
| F435 | P2 | — | **成立** | g0.py:521-529 读 exempt_skills 后零消费（grep 仅 522/527 两处），:530-533 注释声称的校验未实现。 |
| F436 | M | — | **成立** | g6.py:125/142 planted_chapters 零消费（读码确认）。 |
| F437 | P2 | — | **成立** | g5.py:274-296 except Exception → WARN（读码确认）；对照 G6.3/G7.13 fail-closed。 |
| F438 | M | — | **成立** | chapter_drafting.py:202-206 `max(5, wc//1000)` vs SKILL.md:85/124/134 `≤1/3000`（本轮 grep 复验三处原文）。 |
| F439 | P2 | — | **成立** | style_polishing.py:42-43 读 `.bak`；全仓 .bak 生产者仅 g1.py:229，BACKUP_SKILLS 不含 style-polishing（读码+grep 确认）。 |

**误报判定：0 条。** F400-F439 全部成立。不准确处仅限证据范围：F431 漏列 g0.py 调用点（F440/F441 补充）；F406/F424/F425 的"恒 SKIP/永不命中"仅对 pipeline 布局成立（round-exec 测试布局有 skill-output，维持 review2 限定）。

**严重度异议：0 新增。** 对任务点名的 4 个 P1（F401/F404/F408/F432）逐一做了反证核查，全部确认成立、无推翻证据；确认前两轮 2 条异议（F404 P2→P1、F408 P2→P1）。

---

## 二、新发现（F440-F445）

### F440 | g0.py G0.3/G0.12 的 jload ValueError 未捕获 → gate_G0 崩溃（F431 家族在 g0.py 的 2 处漏网） | error | P2
- 证据: g0.py:202-207（G0.3 `except (json.JSONDecodeError, OSError)`，jload 在 :203）、g0.py:524-528（G0.12 读 g4-exemptions.json 同型 except）。shared.jload（shared.py:52-55）对合法非 dict JSON 抛 `ValueError`，两处 except 元组均不含 ValueError。
- 根因: F431 枚举了 8 处 jload 调用点但漏掉 g0.py 这两处；genre-config.json / g4-exemptions.json 为 JSON 数组/字符串时 G0.3 / G0.12 直接崩溃，`shenbi-validate G0` 无 JSON 输出。
- 验证命令+输出:
  - `.venv/bin/python -c`（patch PROJECT + `skill-output/demo/genre-config.json='[1,2,3]'` + seed 含"目标字数：6000"）→ `CRASH: ValueError ... expected JSON object, got list`（G0.3）
  - 同法 `g4-exemptions.json='["x"]'` → `CRASH: ValueError ... expected JSON object, got list`（G0.12）
- 建议方向: 两处 except 统一加 `ValueError`（与 g5.py:38/g6.py:37 写法对齐）；G0.12 豁免文件解析失败应降级空集而非崩溃（:528-529 的 pass 语义本就意图容忍）。

### F441 | G0.14 deps.json 为合法非 dict JSON → AttributeError 崩溃（json.loads 直读后未校验即 .get） | error | P2
- 证据: g0.py:98-99（`deps = json.loads(deps_path.read_text(...))`，except 仅 JSONDecodeError/OSError）、:107（`deps.get("_calibration_hashes", {})` 无 isinstance 守卫）。
- 根因: 与 F440 同源但机制不同——deps.json 用 json.loads 直读而非 jload，非 dict JSON（数组/字符串）解析成功后在 `.get` 处抛 AttributeError；check_calibration_integrity 的 try 块未覆盖该调用。
- 验证命令+输出: `.venv/bin/python -c`（patch PROJECT，`tests/tiers/deps.json='[1,2,3]'`，exemptions 为合法 dict）→ `CRASH: AttributeError: 'list' object has no attribute 'get'`。
- 建议方向: `deps` 加载后加 `isinstance(deps, dict)` 守卫，非 dict 走 `"_calibration_hashes.combined missing"` 的 FAIL 分支（复用现有语义）；或改用 jload 并捕获 ValueError。

### F442 | g4_length_normalizing 未实现 SKILL.md 压缩双底线（≥25% 原始长度）→ 过度压缩静默通过 | error | P2
- 证据: length_normalizing.py:21-24 docstring 自认 ">10000 compression: chapter body ≥ 3000 AND ≥ 25% original"；:61-64 实际只查 `wc < 3000` / `wc > 10000`；skills/shenbi-length-normalizing/SKILL.md:38（"压缩后不得 < 3000 字且不得 < 原始长度的 25%"）、:51-53（flow "Compressed ≥ 3000 AND ≥ 25% original?"）、:62（"压缩双底线"）。
- 根因: 双底线规则只存在于 docstring 与 SKILL.md，未落到 gate 校验；40000 字章节压到 5000 字（12.5% 原始，< 25% 底线）时 `wc ≥ 3000 且 ≤ 10000` → PASS。SKILL.md:93-97 的归一化报告模板已含"原始字数/归一化后字数"字段，可解析但未消费。
- 验证命令+输出: `grep -n "25%\|双底线" skills/shenbi-length-normalizing/SKILL.md` → :38/:51-53/:62（原文如上）；read length_normalizing.py 全文确认 :61-64 无 25% 逻辑。
- 建议方向: 从归一化报告解析"原始字数"（N）并检查 `M ≥ max(3000, 0.25×N)`（压缩路径）；补"过度压缩"负例（40000→5000 应 FAIL）。

### F443 | g1.check_fields_exist 死代码：仅测试引用，gate_G1 与生产均未接线（B.4 字段软检查未生效） | dead-wire | P2
- 证据: g1.py:102-144 定义；全仓 grep `check_fields_exist` → 仅 g1.py:102 定义、dispatch_helper.py:1201 注释、contracts/fields.py:2 docstring 提及；gate_G1（g1.py:147-291 全文）不调用；tests/unit/gates/test_g1_fields.py 直接 import 函数测其本身。
- 根因: 字段级 reads 的 WARN 软检查（B.4）在契约/字段过滤重构（contracts/fields.py 新语义）后未接入 gate 主流程，测试钉住函数而非接线，生产路径上声明字段漂移无任何 WARN。
- 验证命令+输出: `grep -rn "check_fields_exist" src/ --include='*.py'` → 仅 102 行定义 + 1201 行注释；`grep -n "check_fields_exist" src/shenbi/gates/g1.py` → 仅 102 行。
- 建议方向: 在 gate_G1 内对 reads 声明字段调用并输出 WARN，或在 dispatch_helper 接线；否则删除函数+测试并归档 B.4。

### F444 | G3.3 output_files 读取层级与 progress.json 实际结构不匹配 → G3.3 恒 SKIP，G2 复查死路（测试用非生产形状掩盖） | error | P1
- 证据: g3.py:151-153（`skills.get(skill_name, {}).get("output_files", [])`）vs 全部生产 progress 写入方：codex.py:40-46（`skill_entry[test_type] = {"score","status"}`）、trace/materialize.py:66-67/85-87（`skills[skill][test_type] = {status,score,...}`）、run_gate_g3 伪造 progress（dispatch_helper.py:1933-1945，无 skills 键）；g_reconcile.py:36-40 亦按 `[skill][test_type]` 解析同一结构。tests/unit/gates/test_g3.py:118/220 构造 `{"skills": {"shenbi-x": {"output_files": [...]}}}`（skill 层）钉死 G3.3 代码路径——**非生产形状**。
- 根因: G3.3 在 skill 层取 output_files，而生产写入在 test_type 层；"G3.3 Output files passed G2" 复查在全部已知生产 progress 形状下永不执行（恒 SKIP），且测试用合成形状掩盖该死路（决策表"测试失效掩盖真实缺陷"→ P1）。
- 验证命令+输出: 构造 codex 形状 progress（output_files 嵌在 `skills[shenbi-x][generative]` 内）→ `gate_G3('shenbi-x','generative',rd)` → `G3.3 check: [{'id': 'G3.3', 's': 'SKIP', 'r': 'no output_files'}]`（output_files 在错层不可见）。
- 附带: 若未来修复为正确层级，gate_G2 对非 dict JSON 输出抛的 ValueError（F419 家族）会穿透 G3.3 外层 except（g3.py:188 仅 JSONDecodeError/OSError）→ gate_G3 崩溃；修复时应一并处理。
- 建议方向: 改为 `skills.get(skill_name, {}).get(test_type or "generative", {}).get("output_files", [])`；测试改用生产形状（[skill][test_type] 层）或注明形状来源；except 加 ValueError。

### F445 | shared.unimplemented() 无调用方（死代码） | dead-wire | M
- 证据: shared.py:210-222 定义 `unimplemented(gate_name, note)`；全仓 grep 无任何 import/调用（g_dispatch.py:68-69、g_transition.py:67/88/91 均内联 dict 而非调用本函数）；`tests/unit/gates` 亦无引用。
- 根因: 早期 stub gate 工具函数，被内联 UNIMPLEMENTED check dict 取代后遗留。
- 验证命令+输出: `grep -rn "unimplemented(" src/shenbi --include='*.py'` → 仅定义行 210；`grep -rn "from shenbi.gates.shared import" src/shenbi | grep -o "unimplemented"` → 无结果。
- 建议方向: 删除该函数（或让 g_dispatch/g_transition 的 UNIMPLEMENTED check 统一走它，恢复单源）。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部实际读全文；g4 24 个 checker 逐个通读，含此前轮次仅引用未深读的 character_design.py 全文）
- 测试基线：`pytest tests/unit/gates/test_g3.py test_g1.py test_g2.py` → **90 passed**（子集实测；全量 785 passed 沿用前三轮基线）；覆盖率数据未重采（信任前三轮 /tmp 采集结论）
- 未重跑项：全量 coverage 采集；dispatch_helper / codex.py / materialize.py / phase_runner / round-exec.sh 属他区，仅做交叉证据核实（F444 的 progress 形状、F408 行号、F406/F425 的 round 布局限定）
- 跨区交叉确认建议：F444 的 progress.json 实际形状建议与 Z5/Z6（pipeline/dispatcher 区）最终确认；F408 的 run_gate_g3 调用语义 ↔ Z5/Z6

## 四、最终消息（复核统计）

- **新 finding：6**（F440-F445：1×P1、4×P2、1×M，全部实测或代码路径验证；重点核查了任务点名的 G0 各子检查、g1/g3 剩余路径、length-normalizing 业务规则、progress 形状契约、dead-wire 存量）
- **误报：0**（F400-F439 全部成立；对任务点名的 F401/F404/F408/F432 四个 P1 逐一反证核查，均无推翻证据；仅 F431 范围不全——漏 g0.py 调用点，由 F440/F441 补充）
- **严重度异议：0 新增**（确认前两轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度按决策表核对无异议）
- **0 新 finding 判定：否**（本轮发现 6 个前三轮漏报，未收敛到 0）
