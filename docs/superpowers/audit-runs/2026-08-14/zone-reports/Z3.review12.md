# Z3 分区第十二轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第十二轮独立复核（本文件 `Z3.review12.md`，与初审 `Z3.a.md`、一轮至十一轮 `Z3.review.md`~`Z3.review11.md` 全部隔离）
> 复核范围：34/34 文件全量逐行重读（`__init__.py` 1 行 → `chapter_loop.py` 3053 行 → `dispatch_helper.py` 1986 行 → `cli.py` 939 行 → `triggers.py` 656 行全部读完），逐条独立验证 F300-F396；以真实生产项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式、audits 722 文件全旧式名、truth/chapter_summaries.md 仅 2 章条目、novel.json 无 total_chapters、pipeline-state.json phase=chapter-loop ch=56 closure_step=0、style/ 仅 style_profile.md）为格式/行为对照基准
> 本轮发现编号：**F397-F399（3 条，其中 1 条 P1（贴 P0）、1 条 P2、1 条 M）**；新增误报 0 条（F325 维持二~十一轮撤销，本轮独立复证）；新增严重度异议 0 条（支持既有 F302/F364 的 P1→P0 依据，裁定留 stage 4）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证仅在 /tmp 临时目录与只读 python/G4-CLI 中完成）

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`、`is_volume_boundary(15) → False`、`_count_total_chapters → 0`。生产 volume_map 全中文（`**章节范围**: 第1章 - 第15章（共15章）`、`## 第一卷：觉醒之火（第1-15章）`），`_END_RE`/`_RANGE_RE` 均 0 命中（本轮实测）。novel.json keys 无 total_chapters（56 章完成仍在 chapter-loop、closure_step=0）。影响链全部成立。
2. **F325（误报）维持撤销，本轮独立复证**：本轮 `load_contract` 实证 4 个 group skill 契约 `writes:` 即旧式路径（group-factual → continuity/world-rules/pacing 等）；生产 audits/ 722 文件全旧式名、group-*.md 数=0；`_any_audit_has_findings`（chapter_loop.py:1779-1793）扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING（`严重度: BLOCKING`）被 collect_audit_issues 正则命中 → route≠NO_REVISION → 文件扫描同文件必 True → revision 执行。F325 原判"并行波写 group-*.md、旧式名永不产生"与磁盘/契约事实相反。
3. **F364（P1 贴 P0）维持成立，本轮 /tmp 沙箱独立复现**：`register_emergency_handlers` → `atexit.register(_emergency_cleanup)`（crash_recovery.py:66），正常进程退出即无条件 `clear_staging`（:144-148）+ 把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`（:113-127）。本轮实测：`staging before exit: True` → 退出后 `staging` 目录整体消失、`pipeline-state.json` 的 current_step=`EMERGENCY_SHUTDOWN_AT_unknown`（无信号、无异常）。消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交）成立。
4. **本轮最重要新发现（F397，P1 贴 P0）**：**state-settling 的 append_dedup 写语义在派发写路径是 no-op——`_write_parsed_outputs` 对全部契约 writes/updates 做整文件覆写，而契约与 SKILL.md 声明的是按 key（chapter/hook_id）合并追加；且 state-settling 的契约 reads 只有 `chapters/chapter-N.md`（LLM 拿不到任何既有 truth 内容）→ 每章把 `chapter_summaries.md`/`emotional_arcs.md`/`particle_ledger.md`/`subplot_board.md` 等累积 truth 文件覆写为仅当章内容，前章数据确定性丢失**。生产佐证：56 章书的 `truth/chapter_summaries.md` 仅存 2 章条目。这是前十一轮全部未覆盖的独立漏报（详见 F397）。
5. **F379/F371/F373/F353 维持成立（本轮全部实测复现）**：closure 10 步中 step 2/4/5/6/10 抛 `UnresolvedPathError`（5/10 步永不派发，本轮 prompt-build 实证）；`final-snapshot/` 目录 → G4 `read_error` 恒 FAIL（本轮 G4 CLI 实证）；`run_triggered_skills` 把字面 N 路径直接传 G4（arc-N-score.md → FAIL / arc-5-score.md → PASS，本轮 G4 CLI 实证）；novel.json.total_chapters 写点（cli.py:748 / triggers.py:623）均前置依赖 `cli.py:219 if total > 0:` 自锁守卫。
6. **版本偏移注记（沿用三~十一轮）**：生产 pipeline-state 的 steps_done 为 Plan 18 重构前的旧 skill 名（本轮实测 step_index=9 为旧版中间态），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据（`truth/chapter_summaries.md` 仅 2 章条目作为 F397 的格式佐证，不依赖旧版行为细节）。

---

## 一、复核结论总表（F300-F396 逐条判定）

> 判定基于本轮独立 read/grep/uv run python/G4-CLI/沙箱实验/契约 load_contract/prompt-build 实证与真实产物对照。全部 34 文件逐行重读。**误报 1 条（F325，二轮撤销、三~十一轮维持，本轮独立复证维持）**；**F348 为"成立但机制描述部分不准"（非误报）**；**事实修正 1 条（F378 影响面被 F324 掩盖）**。

### 初审（F300-F319/F322-F323）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | 本轮 `python3 -c` 实证 `"\u003c"=="<"` True → dispatch_helper.py:734 `content.replace("<","\u003c")` 恒 no-op |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 :2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1` → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析/趋势落盘）不可达（本轮逐行确认无其他调用点）；cs.resonance_score 并行路径恒 None；resonance_trend.md 由 shenbi-review-resonance 契约 updates 自身写入（本轮 load_contract 实证） |
| F302 | **成立** | P1 维持（**支持 P1→P0 依据，裁定留 stage 4**） | `dispatch_skill(state=)` 仅 chapter_loop.py:2799（串行可及）/ :2970（串行 BLOCKING 环，不可达）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751 均未传（本轮 grep 实证）；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0（PipelineState 无 chapter 属性） |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（`_snapshot_chapter_files` 死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError；machine.clear_checkpoint 的 ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93） |
| F305 | **成立但框定需修正（同意二~十一轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 记账漂移 |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方（本轮实测生产该路径 MISSING）；:97 `Chapter {chapter}` 子串匹配缺陷；volume_context 无消费方 |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用；`_parse_hook_entries`（hook_planting.py:144）对生产 plan section 7 的 MH-001/H-N01 行会接受但 raw_op=="advance" 非 plant/open 被跳过（:151） |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state（state.py:383-428）无生产调用 |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:428-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方（本轮 grep：唯一生产 upsert 调用 chapter_loop.py:3014 为 upsert_markdown_row） |
| F313 | **成立** | P1 维持，无异议（注意 F379 交互） | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（契约 writes 实证）→ G4 校验错位；API/IDE 路径下步骤 6 在派发阶段即失败（F379） |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback（:552）无章号绑定、注入下一次串行 dispatch（:2775-2780）；同源缺口见 F365 |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用（grep 实证；generate_review_checklist 本体已在 dispatch_helper.py:767 接线）；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符（双重死） |

### 一轮（F324-F334）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`、`is_volume_boundary(15) → False`、`_count_total_chapters → 0`；`_END_RE`/`_RANGE_RE` 对生产 `**章节范围**: 第1章 - 第15章（共15章）` 零命中；novel.json keys 无 total_chapters |
| **F325** | **不成立（误报，维持二~十一轮撤销）** | **P1 裁定撤回维持** | 本轮 load_contract 实证 group skill 契约 writes 即旧式路径（group-factual → continuity/world-rules/pacing 等）；生产 audits/ 722 文件全旧式名、group-* 数=0；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物；真实 BLOCKING（`严重度: BLOCKING`）被 collect_audit_issues 正则命中 → route≠NO_REVISION → 文件扫描同文件必 True → revision 执行。残余真值（P2）：清单缺 resonance/review-summary、含生产不产生的 texture/reader-pull |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证：state-settling updates 6 个文件含 pending_hooks.md）；并行 post-draft（:2405-2419）两线程均以 uses_staging 写 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用（本轮 grep 实证）；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过） |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代（grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议；注记：完整重复面见 F387 | 本轮实测 production genre-config auditDimensions.sensitivity=true → `get_active_genre_audits` 含 shenbi-review-sensitivity；core 波（:2551）与 genre 波双发同一 skill 写同一文件 |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接（本轮 grep 实证）；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进（本轮逐行确认） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless 落入成功路径）；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失，本轮 grep 确认） |

### 二轮（F335-F344）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 补零数=0；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在） |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死 |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 全路径无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用 |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:23-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |

### 三轮（F345-F352）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；本轮实测 6 个 audit skill 全部 requires_independent=True；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 MH-001/H-N01 零匹配（本轮实测 `MH-001 → []`、`H-N01 → []`）→ master hooks 从 Route A 检索静默缺失 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys 无 `hooks` 键（本轮实测，hook 数据在 body 表格 `| P0-4 |`）；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)" |
| F348 | **成立但机制描述部分不准（维持四~十一轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md 生产均 MISSING）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout |

### 四轮（F353-F363）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F353 | **成立** | P1 维持，无异议（贴 P0 边界） | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 writes 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；本轮 grep skills/ 契约 0 个引用该文件；drafting 契约 reads `context/chapter-N-context.md`（本轮 load_contract 实证）→ 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断）每章每审计恒注入（幽灵输入 + token 浪费） |
| F358 | **成立但机制前置需修正（维持九/十/十一轮修正）** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError；机制链在更早处已断（F389：基线缺失 → `_check_linguistic_drift` 恒返回 None，本轮实测生产 style/ 无 linguistic_baseline.json） |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra 被丢弃 → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358 |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |

### 五轮（F364-F370）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F364 | **成立** | **P1 维持；支持 P1→P0（依据决策表"数据损坏/丢失 + 契约被静默违反"字面命中；裁定留 stage 4）** | 本轮 /tmp 沙箱独立复现：`register_emergency_handlers` 后正常退出 → staging 被 atexit 清空（`staging before exit: True` → 退出后 staging 目录整体消失）+ pipeline-state.json 的 current_step 被改写为 `EMERGENCY_SHUTDOWN_AT_unknown`（无信号、无异常）；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交 → chapter-planning 无限循环）成立 |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交 + 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`——step 2 时当章 plan 尚不存在 → FileNotFoundError → 捕获 → `_write_minimal_context_fallback` |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格 → 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |

### 六轮（F371-F372）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F371 | **成立** | P1 维持，无异议（注意 F379 交互） | 本轮 G4 CLI 实测：`shenbi-snapshot-manage "final-snapshot/"`（目录已存在且含 snapshot.md）→ `G4.gen.read_error:final-snapshot/` FAIL（不存在则 not_found）——两态恒 FAIL；`_resolve_closure_g4_path`（closure.py:148-159）对无 N 路径原样返回目录 → closure step 10 重试×3 后 ESCALATION |
| F372 | **成立** | P2 维持（latent），无异议 | `_style_profile_is_stale`（triggers.py:103-124）`chapters_dir.glob("chapter-*.md")` 计数把 `chapter-N-pre-rev.md` 计入（F350 备份恰落入通配；对照 `_load_previous_titles` :2251 用 `chapter-(\d+)\.md` 正则排除）→ style-learning 自愈触发提前/误触发 |

### 七轮（F373-F378）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F373 | **成立** | P1 维持（潜伏），无异议 | 本轮 G4 CLI 实测：`shenbi-score-arc "audits/arc-N-score.md"` → `G4.not_found:audits/arc-N-score.md` FAIL；同内容 `audits/arc-5-score.md` → PASS。`run_triggered_skills`（triggers.py:584）把字面 N 路径直接传 G4，dispatch 侧经 `resolve_chapter_path` 解析为章号 → 5 个 N 型触发步骤 G4 恒 not_found |
| F374 | **成立** | P2 维持，无异议 | 本轮实测 `classify_skill_write_safety('shenbi-review-resonance').value → read_only_audit`（write_safety.py:45-46 前缀分类），而契约 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`（本轮 load_contract 实证）→ 并行审计波在无串行保护下并发写 truth 文件 |
| F375 | **成立** | P2 维持，无异议 | dispatch_helper.py:1667-1671 `missing` 非空仅 log.error 后 `return DispatchResult(True, ...)`；`_write_parsed_outputs`（:1153-1168）对 parsed 缺失路径走空内容 skip → state-settling 6+1 文件漏写其一仍 success |
| F376 | **成立** | M 维持，无异议 | truth_embed.py:243-251 `update` 无 `--text` 时落 `emit_json(OK)` 不做任何嵌入 |
| F377 | **成立** | M 维持，无异议 | llm_output_integrity.py:51 `VERDICT_MARKERS` 含通用词 "通过"，:149 `has_verdict` 恒 True → `G4.ac.no_verdict`（:150-153）几乎不触发 |
| F378 | **成立（影响面修正，见下方事实修正 1）** | P2 维持，无异议 | `context/volume-N-complete.json` 仅 chapter_loop.py:955-964 读取、全仓 0 写入方（本轮 grep 实证）；`last_trigger_failure` 仅 triggers.py:575/593 写入、无消费者。**修正**：其"恒发 volume_objective_missed 信号"的影响面当前被 F324 掩盖（见事实修正 1），F324 修复后才激活 |

### 八轮（F379-F383）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F379 | **成立** | P1 维持，无异议 | 本轮全步骤 prompt-build 执行实证：closure step 2/4/5/6/10 抛 `UnresolvedPathError`（`truth/arcs/arc-N.md`、`audits/volume-N-score.md`、`audits/volume-N-payoff.md`、`audits/chapter-N-long-span.md`、`snapshots/chapter-NNN/*`）；`extract_chapter('Execute ... for book closure (step 2)...') → None`；`_dispatch_via_api`/`_dispatch_via_ide`（:1515-1516/:1718-1719）捕获后返回 DispatchResult(False,"Prompt build failed") → `_handle_closure_failure` 重试×3 → ESCALATION——10 步中 5 步永不派发 |
| F380 | **成立** | P2 维持，无异议 | 本轮全步骤 prompt-build 实证：genesis step 16（shenbi-anchor-curate，optional=True）契约 writes 含 `benchmarks/anchors/AC-NNN.md`（本轮 load_contract 实证）→ chapter=None 时抛 UnresolvedPathError → `_handle_failure`（genesis.py:222-225）对 optional 步骤直接跳过 → anchors 永不产出且无错误信号 |
| F381 | **成立** | P2 维持，无异议 | 本轮复现：`chapter-3-revision-decisions.json` 存在 + `revision_count=0` → `heal_state_counters` 返回 `['revision_count_healed:ch3:0->1']`；`_ensure_revision_decisions_exists`（:2759 对 NO_REVISION 章节也写回退文件）+ `_is_revision_routed`（:1928-1933 对 'no-revision' 返回 True）→ 全部未修订章节的 revision_count 被 heal 抬到 ≥1 |
| F382 | **成立** | P2 维持，无异议 | 本轮 grep：`safe_write(` 于 src/shenbi/pipeline/*.py 中 `round_dir` 命中数=0；`MARK_DONE`/`TraceWriter` 于 pipeline 无命中 → trace 目录只有 materialize 自造事件 → progress.json 物化为"全 pending"失真视图 |
| F383 | **成立** | P2 维持（latent/条件触发），无异议 | dispatch_helper.py:903-922 `_parse_file_outputs` 无 `### FILE:` 匹配时返回 `{"__stdout__": response}`；:1159 `content = parsed.get(rel_path, parsed.get("__stdout__", ""))` 把整段 stdout 写入每个字面输出路径；`_dispatch_via_ide`（:1753-1759）以 parsed=None 调用 |

### 九轮（F384-F390）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F384 | **成立** | P2 维持，无异议 | truth_embed.py:120-121 `embed_and_store` 函数体内每次 `st.SentenceTransformer("bge-large-zh")` 全新加载；genesis `_update_route_b` 逐 hook/rule 调 embed_and_store + context_assemble.py:156 `_route_b` 同款每次加载（每章 context assembly 2 次） |
| F385 | **成立** | P2 维持，无异议 | dispatch_helper.py:1832-1841 无 API key 且无 IDE CLI → legacy 子进程；`dispatcher/modes/internal.py:22` internal 模式硬拒 → 必失败死路由；uses_staging 被忽略（:1844-1849） |
| F386 | **成立** | P2 维持，无异议 | `_check_soft_fail_escalation`（:967-1035）派发 escalation-review 但无任何 set_checkpoint（本轮 sed 逐行确认）；调用方随后把步骤正常推进 → 升级不暂停、报告成孤儿 |
| F387 | **成立** | P2 维持（贴 P1 边界），无异议 | 本轮实测 production genre-config auditDimensions 的 worldRules/motivation/dialogue/texture 全 true；group 波契约 writes（group-character→dialogue/motivation、group-craft→texture）与 genre 波单维 skill 契约 writes 同名 → 每章重复审计并覆盖 group 波产物（F329 的完整版） |
| F388 | **成立** | P2 维持，无异议 | `_audit_context_coverage`（chapter_loop.py:2104-2125）docstring 声称"pipeline resume 初始化时调用"，cli 从未接线（本轮 grep 0 命中） |
| F389 | **成立** | P2 维持，无异议 | `style/linguistic_baseline.json` 唯一写入方 `establish_baseline`（skill_utils/drift_detection/baseline.py）全仓 0 调用方（本轮 grep 实证）；`_check_linguistic_drift`（chapter_loop.py:2013-2019）缺基线 → `return None` → spec §3.4 三级干预生产不可达 |
| F390 | **成立** | M 维持，无异议 | cli.py:533-535 `Path(args.feedback).read_text(...)` 在 try 块内；:569-571 `except FileNotFoundError: "project not found"` → feedback 文件缺失误报项目不存在 |

### 十轮（F391-F395）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F391 | **成立** | P2 维持，无异议 | dispatch_helper.py:3-6 模块 docstring 声称 "The dispatcher runs G1 and G2 internally"；:1827-1841 API/IDE 两条路径均无 G1/G2 调用（本轮 grep `run_gate_g1\|run_gate_g2` 于 src/shenbi/pipeline/ 0 命中）；`_resolve_read_path`（:390-407）对缺失文件返回 [] → 缺失 reads 静默排除 |
| F392 | **成立** | P2 维持，无异议 | `_write_one`（:1113-1119）`_validate_json_output` 的 ValueError 被 raise 重抛；`_dispatch_via_api`（:1657-1663）仅捕 DispatchWriteFailureError → ValueError 冒泡至 CLI 顶层 |
| F393 | **成立** | P2 维持，无异议 | `_extract_hook_deliverables`（review_checklist.py:315-375）只解析 frontmatter `hooks` 列表（无 body 兜底）；生产 pending_hooks.md frontmatter 无 `hooks` 键（本轮实测生产 body 含 79 个 P0-* 命中）→ 注入每个审计 prompt 的 hook_deliverables 恒空 |
| F394 | **成立** | M 维持，无异议 | crash_recovery.py:39-42 注释声称 "remove any atexit hooks registered by earlier tests" 但从未调用 atexit.unregister（本轮 grep src/ tests/ 0 命中） |
| F395 | **成立** | M 维持，无异议 | dispatch_helper.py:1548-1551 `except Exception` 对任何 API 异常调用 `_handle_timeout_gracefully`（:116-126 仅 log，无保存动作）→ 非超时错误也打 "dispatch_timeout ... saving_partial_output" 误导日志 |

### 十一轮（F396）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F396 | **成立** | P2 维持，无异议 | 本轮实测 production `truth/audit_drift.md` 对 `_WARNING_RE`（triggers.py:326-329 `(?:warning|drift|fatigue)\s*[:\uff1a]\s*(.+)`）**0 命中**（文件首行为 `# Audit Drift` + `- [维度] ...` 子弹格式）；drift-guidance 规范 YAML（`severity: warning` 在值位）与 resonance 子弹格式同样 0 命中 → spec §6.6 genre-config 更新触发恒 False，`config.genre_config_update_on_drift`（默认 True）全路径无功能效果 |

**误报统计**：F300-F396 中仅 F325 为误报（二轮撤销、三~十一轮维持，本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名 + `_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：0 条新增。立场声明（均无新增异议）：F302（P1 维持，P0 依据存在——决策表 P0 例字面列明 "TokenLedger 少计"，裁定留 stage 4）、F364（P1 维持，本轮沙箱独立复现"正常退出确定性清空 staging + 持久化状态被改写为 EMERGENCY_SHUTDOWN"，决策表 P0"数据损坏/丢失 + 契约被静默违反"字面命中，但 staging 门控由人工 review 兜底、正文文件不丢，P1 亦可辩护——裁定留 stage 4）、F305（同意 P2）、F301（同意贴 P0 校准）、F324（P0）、F340/F341/F345/F353/F354/F371/F373/F379（P1）。

**事实修正（不改严重度）**：1 条——**F378 的影响面修正**：F378 原判"`_check_volume_completion` 恒 False → 恒发 volume_objective_missed 信号"。本轮实测：在 F324 现状下（volume_map 解析为空集），`_find_current_volume(15, set())` → None → `_check_volume_completion(pd, None, 15)` → **True**（`if current_volume is None: return True` 兜底分支）→ `check_escalation` 的 volume_objective_missed 信号**当前不触发**。即 F378 的"恒发信号"影响面被 F324 掩盖，F324 修复（边界可解析）后才激活。F378 的核心缺口（`volume-N-complete.json` 全仓无写入方、`last_trigger_failure` 无消费者）不变，成立性不受影响。

---

## 二、新发现（F397-F399）

### F397 | state-settling 的 append_dedup/upsert 写语义在派发写路径是 no-op：累积 truth 文件（chapter_summaries/emotional_arcs/particle_ledger/subplot_board）每章被整文件覆写为仅当章内容，前章数据确定性丢失 | error | **P1**（贴 P0 边界——正常路径确定性数据丢失，无任何错误信号）

- 证据：
  - **契约侧**：`skills/shenbi-state-settling/SKILL.md` frontmatter `updates:` 对 `truth/current_state.md`、`truth/particle_ledger.md`、`truth/emotional_arcs.md`、`truth/subplot_board.md`、`truth/chapter_summaries.md` 全部声明 `mode: append_dedup`（key: chapter），`truth/pending_hooks.md` 声明 `append_dedup`（key: hook_id）（本轮 load_contract 实证）；SKILL.md 正文明确指示累积文件"output ONLY the new chapter's row, NOT the entire file…The pipeline's `write_truth_file()` will dedup by natural key… and merge"（`skills/shenbi-state-settling/SKILL.md:57-66`）。
  - **实现侧**：`_write_parsed_outputs`（dispatch_helper.py:1044-1194）对全部契约 writes/updates 做**整文件覆写**（`safe_write(full_path, content)`，:1127），`append_dedup` 只在 :1129 的日志行里出现（`mode = semantics.get(rel_path, {}).get("mode")` 仅用于 log），docstring 明确"append_dedup is intentionally NOT branched here"（:1165-1167，声称"the state-settling skill calls write_truth_file with a real key"——但 skill 是 LLM 提示词，无法调用 Python 函数）。
  - **数据源侧**：state-settling 契约 `reads: ['chapters/chapter-N.md']`（本轮 load_contract 实证）；本轮 prompt-build 实测 state-settling 的 user prompt 中**唯一**输入文件是 `<document name="chapters/chapter-5.md">`（整章正文），**没有任何既有 truth 文件内容**——LLM 无从复现前章行。
  - **合并实现不存在**：`write_truth_file` 在 src/ 的唯一生产调用点是 chapter_loop.py:3014（resonance_trend.md，串行块内、F301 下不可达）；state-settling 产物路径上无任何 read-merge-write（`_commit_staging_for_checkpoint` 只做 staging → truth 的整文件拷贝，cli.py:336-353）。
  - **生产佐证**：56 章书的 `truth/chapter_summaries.md` 仅存 2 章条目（`## 第55章`、`## 第56章`，本轮实测）；`truth/emotional_arcs.md` 仅 1 条。
- 根因：契约声明的"按 key 合并追加"语义在派发写路径从未实现（dispatch 只写整文件），而"由调用方（skill）调用 write_truth_file"的委托假设不成立（LLM skill 不能执行 Python）；SKILL.md 指示 LLM 对累积文件只输出新行（期待不存在的合并），dispatch prompt 又要求"complete file content"（LLM 拿不到既有内容也无法完整复现）——两条指令无论 LLM 遵循哪条，累积文件最终都只剩当章内容。
- 验证命令+输出：
  ```
  uv run python -c "from shenbi.contracts.legacy import load_contract; c=load_contract('shenbi-state-settling'); print(c.get('reads')); print(c.get('write_semantics'))"
  # → ['chapters/chapter-N.md'] / {current_state.md: {mode: append_dedup, key: chapter}, ... 6 个文件全部 append_dedup}
  uv run python -c "<state-settling prompt-build 实证>"   # → 唯一输入 <document name="chapters/chapter-5.md">；output_paths 含 staging/truth/current_state.md 等 7 个
  grep -n "write_truth_file" src/shenbi/ --include="*.py" | grep -v "def write_truth_file\|truth_io.py"
  # → 仅 chapter_loop.py:3011/3014（串行 resonance 路径，F301 下不可达）
  grep -c "^## 第" novel-output/xinghuo-ranqiong/truth/chapter_summaries.md   # → 2（56 章书仅 2 章条目）
  ```
- 影响：每章 state-settling 把 `chapter_summaries.md`（全书章节摘要累积）、`emotional_arcs.md`（情感弧累积）、`particle_ledger.md`、`subplot_board.md` 覆写为仅当章数据——**前章累积行确定性丢失**（非概率性，不依赖 LLM 行为）；下游 chapter-planning/审计读到的 truth 历史被截断为"只有上一章"。属正常路径确定性数据丢失 + 契约（append_dedup）被静默违反，与 F326（竞态丢数据）同族但更根本（F326 是并发竞态，本 finding 是语义未实现）。与 F312（upsert_yaml 丢 body）/F368（hook_planting frontmatter-only）同属"声明了合并语义、实现只写整文件"族，但本 finding 是唯一在正常路径每章触发、且数据源（reads）不含既有内容使 LLM 无法自愈的实例。
- 建议方向：为 state-settling 的 updates 引入真实合并层——在 `_write_parsed_outputs` 对 `write_semantics[mode=append_dedup]` 的路径调用 `write_truth_file(mode="upsert_markdown_row"/"upsert_yaml", key_field=...)`（读回 staging/truth 既有文件 + 新行按 key 去重合并）；或把 state-settling 的累积文件 reads 加回（使 LLM 至少能复现既有内容）；同时修正 dispatch_helper.py:1064/1167 的"skill 调用 write_truth_file"错误注释；补一条"两章连续 state-settling 后 chapter_summaries 仍含两行"的集成测试。

### F398 | `_verify_truth_integrity` 在每章边界误报：`plans/chapter-{current_chapter}-plan.md` 在章首本来就不存在（step 2 才创建），却被当作缺失关键文件告警——"fail fast"检查每章产生一次假阳性 | error | M

- 证据：`cli.py:661-669`（`if state.phase == PipelinePhase.CHAPTER_LOOP: ch = state.chapter_loop.current_chapter; chapter_plan = project_dir / "plans" / f"chapter-{ch}-plan.md"; if not chapter_plan.exists(): if ch > 1: missing.append(...)`）；`_complete_chapter`（chapter_loop.py:922-924）在章完成时把 `current_chapter` 置为 N+1 并设置 PER_CHAPTER 检查点 → PER_CHAPTER approve 后 `cmd_resume` 调用 `_verify_truth_integrity` 时 `current_chapter = N+1`，而 `plans/chapter-(N+1)-plan.md` 由章 N+1 的 step 2（chapter-planning，:150-151）才创建。
- 根因：守卫只豁免了"刚从 genesis 转换的 chapter 1"（:665-669 注释），未覆盖"每章开始的常态"——plan 在 step 2 之前不存在是正常状态而非缺失。
- 验证命令+输出：
  ```
  uv run python -c "<构造 phase=chapter-loop、current_chapter=6、step_index=0、genesis 产物齐全的临时状态，调 _verify_truth_integrity>"
  # → 2026-08-14 [warning] truth_integrity_check_failed missing=['plans/chapter-6-plan.md']（第 5 章刚完成、第 6 章尚未规划——plan 缺失是正常状态）
  ```
- 影响：每章 PER_CHAPTER 边界 resume 产生一条 `truth_integrity_check_failed` 假阳性告警（第 2 章起每章一次），稀释该检查对真实缺失（truth 文件被误删）的告警价值；无行为破坏（返回值未接线，F330）。
- 建议方向：把该检查改为"仅在 `step_index >= 2`（step 2 已执行且未跳过）时校验当章 plan"（或校验 `plans/` 目录存在而非当章 plan），或删除该子检查并在注释中说明 plan 缺失为常态。

### F399 | `next` 命令在审批过渡型检查点后静默跳过相位转换/延迟动作：GENESIS_COMPLETE approve 后 `next` 恒报 OK 无进展；VOLUME_BOUNDARY approve 后 `next` 不派发延迟快照、不更新 total_chapters | error | P2

- 证据：`cli.py:730-772`（相位转换 + VOLUME_BOUNDARY 延迟快照/`_update_total_chapters` 只存在于 `cmd_resume` 的 `last.get("decision") == "approve"` 分支）；`cmd_next`（cli.py:588-618）无任何历史检查、不调用转换——`_orchestrate_to_checkpoint` 直接按 `state.phase` 继续。
  - GENESIS 卡死路径：`review approve`（GENESIS_COMPLETE）→ clear_checkpoint（pending=NONE）→ 用户执行 `next` → `_orchestrate_to_checkpoint` → `run_genesis_step`（genesis.py:280-281 `step_idx(17) >= len(GENESIS_STEPS)` 恒 True，无 checkpoint）→ 循环退出 → `_emit_orchestration_result` 报 OK（phase 仍 genesis）→ **每次 next 都是静默 no-op，永不进入 chapter-loop**（只有 resume 触发 `transition_genesis_to_chapter_loop`）。
  - VOLUME_BOUNDARY 卡点：`run_triggered_skills` 设 checkpoint 时 `cli.py:243-245/263-265` 已把 `cl.step_index = 1`（C1 防重发）→ approve 后 `next` → step_index=1 跳过触发块 → 直接跑 step 2；`cmd_resume:740-765` 才有的延迟 `dispatch_skill("shenbi-snapshot-manage", ...)` 与 `_update_total_chapters(project_dir)` 永不执行。
- 根因：`cmd_next` 与 `cmd_resume` 在"审批后如何继续"上职责不对称——`next` 未对历史最后决策做转换处理，且 CLI 不阻止审批后用 `next` 继续（`next` 只在"有 pending checkpoint"时报 blocked）。
- 验证命令+输出：
  ```
  uv run python -c "from shenbi.pipeline.state import PipelineState, GenesisState; from shenbi.pipeline.genesis import run_genesis_step; st=PipelineState.default('/tmp/x'); st.genesis.current_step=17; st.genesis.state=GenesisState.CHECKPOINT_PENDING; print(run_genesis_step(st,'/tmp/x'), st.pending_checkpoint.type.value, st.phase.value)"
  # → True none genesis（approve 后 next：无 checkpoint、无转换、恒报 OK）
  ```
- 影响：审批后误用 `next`（CLI 未阻止、help 文本也未警告）时——genesis 相位永不转换（静默停滞，需 resume 自救）；VOLUME_BOUNDARY 的延迟快照与 total_chapters 更新被静默跳过（快照欠账，`volume_snapshot_pending` 死函数 F342 亦无消费者）。属正常可复现的"命令语义不对称"功能错误（P2；M 亦可辩护——用 `resume` 可恢复）。
- 建议方向：`cmd_next` 开头复用 `cmd_resume` 的"最后决策 → 转换/延迟动作"块（抽公共函数），或审批后清空 `checkpoint_history` 末条前的转换状态并在 `next` 检测到"最近有未消费的 approve 转换"时 emit_json 提示改用 resume；补一条"GENESIS_COMPLETE approve 后 next 应触发转换"集成测试。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件逐行重读；`__init__.py` 1 行至 `chapter_loop.py` 3053 行、`dispatch_helper.py` 1986 行、`cli.py` 939 行、`triggers.py` 656 行、`state.py` 536 行等全部读完）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries → set()`、`_resolve_volume_at_runtime → None`、`is_volume_boundary(15) → False`、`_count_total_chapters → 0`；novel.json keys 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0 step_index=9（旧版中间态）；truth/chapter_summaries.md 仅 2 章条目、emotional_arcs.md 仅 1 条、pending_hooks.md frontmatter 无 hooks 键（body 含 79 个 P0-* 命中）、audit_drift.md 对 `_WARNING_RE` 0 命中；style/ 仅 style_profile.md 无 linguistic_baseline.json；audits/ 722 文件全旧式名）；`uv run python` 实证：**closure 10 步全量 prompt-build 执行**（F379 复现）、**state-settling prompt-build 输入/输出清单**（F397 关键证据：唯一输入 chapter-N.md、7 个 staging/truth 输出）、**`_check_volume_completion(None)` 返回 True**（F378 影响面修正）、**`_verify_truth_integrity` 章边界误报复现**（F398）、**genesis-complete approve 后 run_genesis_step no-op 复现**（F399）、`requires_independent` 6 审计 skill 全 True（F345）、`classify_skill_write_safety('shenbi-review-resonance')`（F374）、`_heal_revision_counts` 抬升（F381 复现）、`_style_profile_is_stale` 计数（F372 复现）；**G4 CLI 直接执行**（F371 复现：final-snapshot/ 两态恒 FAIL；F373 复现：arc-N-score.md FAIL / arc-5-score.md PASS）；**/tmp 沙箱 atexit 退出实验**（F364 独立复现：staging GONE + current_step=EMERGENCY_SHUTDOWN_AT_unknown）；load_contract 契约 reads/writes/updates/write_semantics（group-*/resonance/lifecycle/state-settling/drafting/volume-outlining/anchor-curate/snapshot-manage）。
- 前十一轮覆盖空洞（本轮已补）：(1) **state-settling 累积 truth 文件的 append_dedup 语义 no-op**——前十一轮审过 F326（两写者竞态）、F375（部分输出缺失）、F312（upsert_yaml 丢 body），但从未对照"契约 write_semantics append_dedup × 6 文件 + 契约 reads 仅 chapter-N.md + `_write_parsed_outputs` 整文件覆写 + `write_truth_file` 唯一调用点不可达"四者 → 漏掉确定性数据丢失（本轮最重要的新发现，验证手段为 state-settling prompt-build 输入清单 + 生产 chapter_summaries 仅 2 章条目佐证）；(2) **`_verify_truth_integrity` 章边界假阳性**（守卫只豁免 chapter 1）；(3) **`next` 与 `resume` 的审批后语义不对称**（GENESIS_COMPLETE/VOLUME_BOUNDARY 转换只挂在 resume）。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 的实际 codex 输出格式未做端到端执行验证（F383/F392 为静态推演 + 单元实证）；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内），group skill 契约旧式 writes 是 F325 反证的关键；(e) F397 的"LLM 实际输出形态"未做真实 codex/API 端到端验证（契约/实现/数据源三侧证据已充分，但实际丢失行为依赖 LLM 遵循任一指令，两者均指向丢失）。

---

## 四、汇总

- **新 finding 数**：3（F397 = P1（贴 P0）、F398 = M、F399 = P2；**无新增 P0**）
- **误报数**：0 新增（F300-F396 中仅 F325 为误报，二~十一轮已撤销、本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名 + `_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物；F348 维持"成立但机制描述部分不准"）
- **严重度异议数**：0 新增（立场声明：F302 与 F364 的 P1→P0 依据均支持并裁定留 stage 4——F364 本轮沙箱独立复现"正常退出确定性清空 staging + 持久化状态被改写为 EMERGENCY_SHUTDOWN"，决策表 P0"数据损坏/丢失 + 契约被静默违反"字面命中，但 staging 门控由人工 review 兜底、正文文件不丢，P1 亦可辩护；F302 账本少计为成本侧车，P1 亦可辩护。另注记 F353/F371/F373/F379 贴 P0 边界）
- **事实修正（不改严重度）**：1（F378 影响面修正——`_check_volume_completion` 在 F324 现状下因 `current_volume=None` 走 `return True` 兜底，volume_objective_missed 信号当前不触发，F324 修复后才激活；核心缺口"marker 无写入方"不变）
- **0 新 finding 判定**：**否** —— 本轮仍发现 1 条 P1 漏报（F397 state-settling append_dedup no-op → 累积 truth 确定性丢失）与 1 条 P2（F399 next 审批后语义不对称）及 1 条 M（F398 章边界假阳性）；F324（P0）维持成立、F325（误报）维持撤销、F340/F341/F345/F353/F354/F364/F371/F373/F379（P1）全部维持成立。前十一轮共 73 条新 finding（F324-F396，其中 F325 误报撤销），本轮新 3 条（F397-F399）。收敛轨迹：九轮 7 条 → 十轮 5 条 → 十一轮 1 条 → 本轮 3 条（反弹），且本轮出现 1 条 P1 级漏报——收敛目标（0 新 finding）**未达成**。
- **校准升级提示**：F397 建议与 F326/F312/F368 合并为"truth 写语义族"处置（append_dedup/upsert 在派发路径的真实合并实现 + SKILL.md/契约/注释三方对齐 + 累积文件 reads 补全），修复后必测"两章连续 state-settling 后累积文件含两行"；F399 建议并入 F340（REJECT/命令语义族——`next`/`resume` 审批后职责统一）；F324/F353/F371/F373/F379 的"pipeline 永不完成"根因族维持阶段 4 复核优先级首位。

---

## 五、独立复核声明

本报告为 fresh-context 第十二轮独立复核：34/34 文件逐行重读；对 F300-F396 的判定全部基于本轮独立 read/grep/uv run python/G4-CLI/沙箱实验/契约 load_contract/prompt-build 实证与真实产物对照（未复用前十一轮结论）；新增 3 条发现（F397-F399）均为前十一轮报告未以编号形式出现的观察，其中 F397 的验证手段（state-settling prompt-build 输入清单 + 契约 write_semantics 与 `_write_parsed_outputs` 覆写路径 + `write_truth_file` 调用点对照 + 生产 chapter_summaries 仅 2 章条目）为前几轮未覆盖的新证据路径；F364 的 atexit 沙箱实验、F371/F373 的 G4 CLI 实验、F379 的 closure prompt-build 实验为本轮独立复现。
