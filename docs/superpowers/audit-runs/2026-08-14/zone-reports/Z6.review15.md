# Z6 分区第十五轮独立复核报告（fresh-context 全量重读）

> 复核人：第十五轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11（F601-F611）+ 一轮 8（F612-F619）+ 二轮 10（F620-F629）+ 三轮 7（F630-F636）+ 四轮 3（F637-F639）+ 五轮 3（F640-F642）+ 六轮 1（F643）+ 七轮 3（F644-F646）+ 八轮 2（F647-F648）+ 九轮 5（F649-F653）+ 十轮 2（F654-F655）+ 十一轮 3（F656-F658）+ 十二轮 2（F659-F660）+ 十三轮 2（F661-F662）+ 十四轮 1（F663）逐条独立验证；新发现编号 F664 起。
> 方式：49/49 文件全文 read（含全部 `__init__.py`/`__main__.py` 薄文件）+ `PYTHONPATH=src python3` 实际运行复现（F601/F612/F642/F648/F649/F652/F656/F661/F663/F664 及全部新发现本轮回放）+ 全仓 grep 调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对（56 章 compute_all_stats 全量运行、chapter-1/15/19/24/25/45/46.md、truth/pending_hooks.md、truth/resonance_trend.md、truth/audit_drift.md、genre-config.json、progress.json、config-change-log.jsonl、plugins/master.json 59 vs skills/ 74）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未达成**（新发现 2 条，均 P2、均潜伏；误报 0）。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §4）
- 初审 11 findings：**11/11 成立**（支持既有软异议 2 条：F601、F646）
- 一轮 8：**8/8 成立**；二轮 10：**10/10 成立**；三轮 7：**7/7 成立**；四轮 3：**3/3 成立**
- 五轮 3：**3/3 成立**（F642 证据裁决支持 review9-14 终裁，本轮独立重放一致，见 §1.6）
- 六轮 1：**1/1 成立**；七轮 3：**3/3 成立**；八轮 2：**2/2 成立**；九轮 5：**5/5 成立**；十轮 2：**2/2 成立**
- 十一轮 3：**3/3 成立**；十二轮 2：**2/2 成立**；十三轮 2：**2/2 成立**；十四轮 1：**1/1 成立**
- 新发现：**2**（F664-F665，均 P2、均潜伏）
- 整条误报：**0**；严重度异议：**0 条新增**（支持既有软异议 2 条，不修改定级）；证据争议：无新争议（F642 终裁复核一致）
- 判定：**非 0 新 finding（2 条，均 P2 潜伏级、无 P0/P1）**——连续 8 轮新发现均为 P2 潜伏级，且本轮两条均落在"消费链/契约侧"（前 14 轮以模块内部为主），建议协调者按 review10-14 提议落定收敛判据（"无 P0/P1 新 finding + 无整条误报"）后终止轮次。

---

## 1. F601-F663 判定表（逐条独立复核）

> 判定依据：本轮 49/49 文件全文精读 + 直接运行复现 + 全仓 grep + 真实数据核对。表中仅列本轮独立验证要点与前轮证据链的复核结论。

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（**支持 review6-14 软异议：可辩 P2**） | 代码精读 linguistic_drift.py:211-218：`dialogue_ratio < 0.2` 时 `max_deviation_ratio = max(…,5.0)` 恰为 5.0，`is_drift = 5.0 > 5.0` = False；docstring :185-188 承诺 "<20% of baseline" 触发 → off-by-one 成立。**生产影响全潜伏**：唯一生产入口 `_check_linguistic_drift` 因 F602 无 baseline 于 chapter_loop.py:2017-2019 提前 return（本轮代码精读确认 baseline 检查在 compute_linguistic_metrics 之前），detect_drift 从未被生产调用；即便接线，F612 门控 + F620 吞异常使干预仍不可达。P1（显式契约静默失效）与 P2（死路径）均可辩护，不构成整条误报。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持） | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py:6,28` re-export，生产零调用；`find . -name linguistic_baseline.json` 0 命中（style/ 仅 style_profile.md、context/ 无）；chapter_loop.py:2014-2019 读 `style/linguistic_baseline.json` 不存在 → `log.warning("no_linguistic_baseline")` + return None，:2722 每章走该分支。计划 07-19-07 Task 5a "Wire this into _run_context_assembly or chapter_loop" 显式接线指令未履行。P1 成立。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链 write_audit.py:49-56（compute_file_change + 三件套）→ record.py:24 block ship。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'`，:226 `sum(text.count(c) for c in chars)` 按字符迭代。本轮 56 章全量：compute_stats 引号=6902（含 2× 膨胀）。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持；三变体均复核） | linguistic_drift.py:107 `"|".join([])==""` → `re.findall("", text)` 全位置匹配；`system_terms: null` → `list(None)` TypeError、`drift_detection: null` → `None.get` AttributeError（:79-84 无类型守卫）同根。真实 genre-config.json 无 drift_detection 键（本轮核实）→ 走 bootstrap 回退。review1 影响修正（被 is_drift 门控、无管道暂停）复核同意。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过。review13 补充（bool 是 int 子类、被拦，非绕过）复核正确。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs detect_drift 读 `dialogue_density`；:296/:335 硬编码术语 vs :107 config 驱动。`_load_baseline`/`check_linguistic_drift` 生产零调用 → context/ 平行实现是死路径。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | cjk.py:54 桶 token 为相邻双字符 `""`/`''`/`「」`/`『』`，正常文本引号包围内容不相邻 → `text.count` 恒 0；:61-62 "whole token" 修复对引号桶反而永久为 0。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py 全文无 logging/structlog import（本轮全文精读确认）；:40-43 两处 break 无日志、:47-48 safe_write 改写文件。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无 → 单条脏数据整批崩溃。F622 为独立类型失败模式。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:109 `key.startswith("auditDimensions.")` 只拦带点形式；整键对象覆盖绕过。F631（falsy）/F638（snake_case）/F643（键缺失）为独立向量。P2 成立。 |

### 1.2 一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持，无反证） | 本轮代码精读 chapter_loop.py:2023-2043：`if result.is_drift:` 包裹 log + ESCALATE raise（:2034-2038）+ HARD/WARN 注入全部干预；opening-similarity 检查 :2047-2056 在门**外**。运行：baseline stm=60 → current stm=110 → `is_drift: False, severity: ESCALATE`。计划 07-19-07:749 绝对阈值触发契约。P1 成立。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 重复字符 + :226 按字符迭代。本轮 56 章全量：破折号 count=105850（cjk 侧 ~26462 → 4× 膨胀吻合）、省略号 212（cjk 53 → 4×）。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | config_coherence.py:105-133 循环内逐 key `_set_nested`+`_append_audit_trail`，safe_write 在循环外 :142 → 中途 ConfigError 时 trail 已追加、config 未落盘。生产零调用（F635）→ 实际影响面为零，不改变本体。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | writer.py:57 `json.loads(lines[-1])` 无 try；replay.py:38-43 同文件可自愈 → 不对称。三个生产调用点均有外部缓解（safe_write.py:126-140 / audit/record.py:42-46 / migrate 先 replay，本轮全部核实）。P2 成立。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | 本轮 grep：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` → 仅 compute_stats.py:37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex（`.*` vs `[^？]{0,15}` 语义不同）。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。P2 成立。 |

### 1.3 二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持，无反证） | 本轮代码精读 chapter_loop.py:2719-2727：注释自称 "non-blocking"（:2719），`try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed", exc_info=True)`——函数内 :2034-2038 raise DriftEscalationError（"Pipeline paused for human review"）被调用点吞掉。grep `_check_linguistic_drift` 唯一生产调用点即 :2722。P1 成立。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目；:170 main 默认 "未分类" 且词表外字符串原样保留。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | recall.py:41-46 `.get()` 只判 None 不判类型 → `int - str` / `int > str` TypeError。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持；**影响描述补 nuance**） | compute_stats.py:52 单 U+2026 vs 标准中文双 `……`（与 :23 PUNCT_MAP 一致）——双省略号真实文本恒不命中（前轮运行复现）。**nuance（非误报）**：本轮 56 章全量 `count_ai_markers` 命中 322 次，其中 "不是…而是" 37 次——核查这些命中全部位于 META/审计指令文本（如 `"不是…而是…"句式MINOR` 的**自我指涉**描述，ch19/ch24/ch25 抽查确认均在 META 块内或指令性文本）而非正文创作；即该标记对真实正文的双省略号写法恒不命中（本体成立），37 次命中是 META 文本被 count_ai_markers 计入（F634 同根第四消费方，非新 finding）。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无 skills 校验 | P2 | **成立**（P2 维持） | 本轮规范化重算：master 59 名、skills/ 目录 74、缺失恰好 15 个（名单与前九轮完全一致）、反向 0。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、Field(ge=1) 阻止 v0 构造、无生产调用方）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export，src 内无生产调用。review12 补充（`return is_drift and severity in ("HARD","ESCALATE")` 继承 F612 门控病）复核正确。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件（非 `|` 行 `continue` 而非 break）。真实 resonance_trend.md 单表单行 → 潜伏。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | compute_stats.py:16 SENT_ENDS 含 `；`，:63 只按 `。！？\n` 切句，:87 用 SENT_ENDS。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | 本轮 grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用。P2 成立。 |

### 1.4 三轮 F630-F636

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | **成立**（P2 维持） | revision_router.py:62-64（注释明示 "config.resonance_global_floor" 后跟 `= 50`）vs thresholds.py:30 `=65`；chapter_loop.py:1897 显式传值覆盖默认 → 潜伏。P2 成立。 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | **成立**（P2 维持） | config_coherence.py:109、g0_config_coherence.py:92 均 `is False`（本轮代码精读确认）；audit_layer.py:113 `audit_dims.get(dim_key, False)` 任何 falsy 即不激活。P2 成立。 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | **成立**（P2 维持） | linguistic_drift.py:222-226 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50；thresholds.py:5-10 docstring 自称单源。P2 成立。 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | **成立**（P2 维持） | compute_stats.py:79 docstring vs :80 `re.split(r"\n\s*\n")`；`\n` ∈ SENT_ENDS :16。P2 成立。 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | **成立**（P2 维持；review12 数字勘误复核；**本轮补充第四消费方**） | 真实 chapter-1/45/46.md 均以 `<!--META-BEGIN-->` 开头（chapter-1 有 2 块共 858/4177 字符，本轮核实）；两处统计入口（linguistic_drift.py:94-131、compute_stats.py:311-362）无剥离。剥离方向随方法而变（review12 已勘误）不影响本体。**补充（非新 finding）**：本轮 56 章全量 compute_all_stats 的 trigrams top3 含 `('qui', 2786)`/`('uie', 2781)`——来源为 chapter-15 META 指令块中的英文模板文本（"where clause boundaries require…"，抽查确认）→ count_ai_markers 的 37 次 "不是…而是" 命中与 ngrams 的英文碎片同为 META 文本计入统计（F634 同根第四/第五消费方）。P2 成立。 |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | **成立**（P2 维持） | 本轮 grep：`update_genre_config` 仅 config_coherence.py 自身（+测试+计划文档）；真实 config-change-log.jsonl 仅 1 条 2026-07-19 记录（本轮核实：auditDimensions.texture old=true/new=true 无变化条目）。P2 成立。 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | **成立**（P2 维持） | 本轮直接运行：base 全 0 + current em_dash=0.33 → `is_drift: True, severity: WARN`。linguistic_drift.py:202。P2 成立。 |

### 1.5 四轮 F637-F639

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转（格式分叉 + 测试掩盖） | P1 | **成立**（P1 维持，无反证） | 本轮全链复核：真实 pending_hooks.md（143 行，ch56）标题仅 `# 伏笔追踪`/`## 第56章伏笔呈现`/`## 第56章生命周期状态更新（foreshadowing-track）` 等章节附录（`grep -n "^#"` 全清单核对），**无 `## hooks`/`## 活跃伏笔`**；ch-025 快照与 fixture 均有 `## 活跃伏笔`(:12)/`## hooks`(:20) → 格式分叉成立。消费链 write_audit.py:49-56（compute_file_change + 三件套，本轮代码精读确认）→ record.py:24 block-ship 已接线但 drift 恒空。P1（"测试失效掩盖真实缺陷"档）成立。 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过 Rule 1 与 G0） | P2 | **成立**（P2 维持） | audit_layer.py:105-107 snake_case 回退 vs config_coherence.py:109/g0_config_coherence.py:79 只认 camelCase。真实配置 camelCase（本轮核实 `has audit_dimensions: False`）→ 潜伏。P2 成立。 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 真实项目 arc/volume 半边不运行 + audit 追加无协调 | P2 | **成立**（P2 维持） | 真实 truth/ 仅 resonance_trend.md（单表单行 ch55，含 human_overridden 列，本轮核实），无 arc_payoff_trend.md/volume_score_trend.md（find 全仓 0 命中）；audit_drift.md 为 `## Ch55 —` 章节式（review-resonance 写，本轮核实），CLI `_append_audit`（compute_drift.py:225-233）追加 `## drift findings` 段与之无协调。P2 成立。 |

### 1.6 五轮 F640-F642

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F640 | materialize_progress 输入契约零生产者 → 无条件覆盖真实 progress.json（数据丢失+错误结果） | P1 | **成立**（P1 维持；激活应升级 P0） | 本轮全链复核 + 代码精读：`grep '"INIT"\|"MARK_DONE"'` src/ → 仅 materialize.py:49,53（消费方）+ 测试；生产 TraceWriter.append 调用点 = audit/record.py:44 + safe_write.py:132（trace_action 生产唯一传参处即 materialize.py:98 自身）+ migrate.py:30（migrate_from_progress 自身零调用）。`_maybe_materialize_progress`（chapter_loop.py:684-699）：`if steps_done % 5 == 0`（steps_done=0 即满足，:692）+ `except Exception: pass`（:698-699）；调用点 :2691/:3028 在活动章节循环内（本轮代码精读确认）。`_auto_rebuild_progress_if_stale`（:702-743）检查 `project_dir/"trace"` **目录**（:713）而 TraceWriter 写 `trace.jsonl` **文件**（writer.py:42）→ 路径错位使自愈分支永不触发（cli.py:728 接线确认）。真实 progress.json 为 `current_scorer_agent`/`scoring_history` schema（本轮核实），与 materialize 输出不同 → 触发即数据丢失。真实项目无 trace.jsonl、progress.json 未被破坏 → 潜伏。P1 成立。 |
| F641 | records `_values_equal` 布尔比较孔 → YAML true vs md "true" 假 drift | P2 | **成立**（P2 维持） | drift.py:57-69 `str(True)="True"` != `"true"`，`float("true")` ValueError 落入 return False → 假 drift。P2 成立。 |
| F642 | check_opening_similarity 比较 META 块非正文开头 | P2 | **成立**（本体成立；**证据裁决支持 review9-14 终裁，否定 review8**） | 本轮独立重放（同法剥离 `re.sub(r'<!--META-BEGIN-->.*?<!--META-END-->', '', t, flags=re.S)`，ch45 两个 META 块 / ch46 一个）：raw [:300] ratio(46,45)=**0.6267**；剥离后生产顺序 (46,45)=**0.6367** > 0.6；交换顺序 (45,46)=**0.57**；autojunk=False=**0.77**；56 章相邻对 34/55 \|diff\|>0.01、生产顺序仅 (45,46)=0.627 > 0.6——0.57 与 0.637 均可复现（SequenceMatcher 默认 autojunk 参数顺序不对称，F649）；按生产调用顺序（chapter_loop.py:2049 current 在前）剥离后仍 0.6367 > 0.6 → 剥离 META 不足以消除假阳性（review6/9/10/11/12/13/14 结论成立，review8 的"0.637 不可复现"不成立）。本体（比较对象=META 块而非正文开头）与 P2 定级维持。 |

### 1.7 六轮 F643

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F643 | 治理链"键缺失"默认语义矛盾：G0 视缺失=启用、audit_layer 视缺失=禁用 → 移除/置空 auditDimensions 静默停用审计 | P2 | **成立**（P2 维持） | 本轮代码精读：g0_config_coherence.py:92 `audit_dims.get(dim, True)`（注释明示缺失=启用）vs audit_layer.py:113 `audit_dims.get(dim_key, False)`（缺失=禁用）vs config_coherence.py:109 只认显式 False——三层缺省语义矛盾。P2 成立。 |

### 1.8 七轮 F644-F646

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F644 | compute_ngrams 未剔除标点 → n-gram 指纹被标点对/标点串主导 | P2 | **成立**（P2 维持） | 本轮 56 章全量：bigrams top1 = `('——', 26371)`（纯标点串占据最高频）；chapter-1 top bigram = `('。他', 27)`。compute_stats.py:209 只滤空白。P2 成立。 |
| F645 | compute_ttr 排除串缺 `“”`/`"`/`‘’` 引号 → 引号字符计入 TTR token | P2 | **成立**（P2 维持） | compute_stats.py:171 排除串只含两个 ASCII 直单引号 `''`；真实 56 章语料 ttr total_chars=424014（含引号字符）。P2 成立。 |
| F646 | AUDIT_SAFETY_MATRIX 6 维 vs 真实 config 10 维漂移：4 维无 rationale/G0 保护（sensitivity 为 BLOCKING 级） | P2 | **成立**（P2 维持；**支持 review11-14 的 impact 软异议方向，维持 P2**） | 本轮实测：真实 genre-config.json auditDimensions 10 维全 True，`real - matrix = ['foreshadowing','motivation','sensitivity','worldRules']`、`matrix - real = []`（输出确认）。sensitivity 激活链核实为活（audit_layer → get_active_genre_audits → sensitivity_blocking → escalation）。但真实配置全 True（潜伏）+ "absent → non-critical" 是 thresholds.py:48-49 显式声明设计 → P2 恰当；若协调者认定"BLOCKING 级安全网可无治理禁用"违反显式契约可辩 P1。不修改定级。 |

### 1.9 八轮 F647-F648

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F647 | records by_id 重复/缺失 id 静默塌缩 → drift 漏报或误报且无告警 | P2 | **成立**（P2 维持） | drift.py:80 `{str(r.get("id")): r for r in yaml_records}` dict 推导后者覆盖前者；缺失 id → `str(None)="None"` 塌缩。P2 成立。 |
| F648 | compute_percentiles n=2 百分位倒挂（P75/P95 < P50） | P2 | **成立**（P2 维持） | 本轮运行：`compute_percentiles([1,2])` → `{'P25': 1, 'P50': 2, 'P75': 1, 'P95': 1}`（输出确认）。compute_stats.py:99-104 `int(n*0.75)-1`/`int(n*0.95)-1` 截断 + `values[n//2]` 组合在 n=2 时 P75=P95=values[0]=1 < P50=values[1]=2。P2 成立。 |

### 1.10 九轮 F649-F653

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F649 | SequenceMatcher 参数顺序不对称（autojunk）→ 相似度非良定义、近阈值翻转；F642 之争根源 | P2 | **成立**（P2 维持） | 本轮独立重放：剥离 META 后 (45,46) 生产顺序 0.6367 vs 交换顺序 0.57、autojunk=False 0.77；56 章相邻对 34/55 \|diff\|>0.01（输出确认）。linguistic_drift.py:249-253/256-268。P2 成立。 |
| F650 | TraceWriter 信任尾部签名不校验 → 签名无效尾部链上新事件，replay 静默删除新合法事件 | P2 | **成立**（P2 维持） | writer.py:51-57 `_last_sig_existing` 只 `json.loads(lines[-1]).get("signature", GENESIS_PREV)`，不校验签名与链一致；append 以坏签名为 prev 链新事件；replay.py:42-43 从坏行截断 → 新合法事件被连带删除。与 F615（JSON 不可解析尾部）为独立失败模式。P2 成立。 |
| F651 | parser yaml.safe_load 重复键静默 last-wins → 权威记录源静默损坏 | P2 | **成立**（P2 维持） | parser.py:37-45；PyYAML SafeLoader 默认映射重复键 last-wins 无异常。P2 成立。 |
| F652 | detect_rhetoric「反复」按子短语分解计数爆炸（7 字短语×3 → 12） | P2 | **成立**（P2 维持） | compute_stats.py:266-280 枚举全部 3/4/5-gram 各计 1。本轮 56 章全量：ch46 反复=1864、全语料反复=23054（量级爆炸随文本长度放大）。P2 成立。 |
| F653 | parse_markdown_table 空行不终止表 → 第二张表并入派生视图 | P2 | **成立**（P2 维持） | drift.py:34-39 `if not s: continue` 空行不 break。P2 成立。 |

### 1.11 十轮 F654-F655

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F654 | `_try_float` 非有限值透传（nan/inf/1e999）→ 趋势序列被静默污染、σ 触发失效 | P2 | **成立**（P2 维持） | compute_drift.py:217-222 docstring 承诺"非有限值返回 None"但实现仅捕获 TypeError/ValueError；`float("nan")`/`float("inf")`/`float("1e999")` 透传（前轮运行复现）。P2 成立。 |
| F655 | records drift 缺省值比较孔：`None` vs `''` 恒不等 → 空单元格/多余列假 drift | P2 | **成立**（P2 维持） | drift.py:90-91 `rec.get(key)`→None；:57-69 `_values_equal(None, '')` → `str(None)="None"` != `""`、`float(None)` TypeError → False → 假 drift。P2 成立。 |

### 1.12 十一轮 F656-F658

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F656 | detect_rhetoric「排比」`[:20]` 截断 → 连续长句恒判平行（真实 chapter-1 排比 53 中 40 为伪影） | P2 | **成立**（P2 维持） | compute_stats.py:256 `sent_texts[i][:20]` → ≥20 字长句截断后长度全 20 → `abs(20-20)/20=0 < 0.3` 恒判平行。本轮运行：ch1 排比=53、56 章全语料排比=3816（全语料量级虚高）。P2 成立。 |
| F657 | compute_drift 卷级检测丢弃 human_overridden 排除（章节级排除、卷级不排除） | P2 | **成立**（P2 维持） | 本轮代码精读：compute_drift.py:268-270 章节级 `excl = {i for i, (_, e) in enumerate(series) if e}`；:275-278 卷级 `volume_scores = [score for score, _ in overall_series]` 排除标志被丢弃。真实 resonance_trend.md 含 human_overridden 列 → 该列语义在卷级路径被静默忽略。P2 成立。 |
| F658 | parse_markdown_table 重复 id 行 last-wins 静默塌缩（F647 md 侧镜像） | P2 | **成立**（P2 维持） | drift.py:51-53 `if rid: out[rid] = row` dict 赋值后者覆盖前者，无日志。P2 成立。 |

### 1.13 十二轮 F659-F660

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F659 | compute_stats/cjk 双标点统计实现口径分叉（同文本计数系统性不同） | P2 | **成立**（P2 维持） | compute_stats.py:17-28 PUNCT_MAP 与 cjk.py:44-55 PUNCTUATION_TOKENS 桶成员定义不同（ASCII 变体、`──`/`。。。`、引号 per-char vs adjacent-pair）。本轮 56 章全量：compute_stats 感叹号=4（cjk 计 "!"/"！" 两者）等分叉一致。P2 成立。 |
| F660 | parse_markdown_table 无表头表静默整表丢行（分隔行被当表头） | P2 | **成立**（P2 维持） | drift.py:41-43 首 `|` 行无条件当 header、:44-45 分隔行检查在其后。P2 成立。 |

### 1.14 十三轮 F661-F662

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F661 | `_short_chain_chars` 正则无左锚 → 长句尾部入链/短句链被打断丢失，真实章节密度偏差 | P2 | **成立**（P2 维持） | 本轮运行复现：`'这是一个超过十五个字符的很长很长的句子因为要超过十五字。短句一。短句二。短句三。'` → 28（长句尾部 15 字入链）vs 纯短句 12；ch46 正则 0（F661 语义计数 57）。linguistic_drift.py:88-91 正则 `(?:[^。！？\n]{1,15}[。！？\n]){3,}` 无左锚。P2 成立。 |
| F662 | materialize `_as_float` 对 str 分数静默置 0.0（`_as_int` 接受 str 的不对称） | P2 | **成立**（P2 维持） | materialize.py:18-23 `_as_int` 接受 str（int(value)）、`_as_float` 仅接受 int/float 否则返回 default。本轮运行复现：`_as_int('88', 0)=88`、`_as_float('88', 0.0)=0.0`。P2 成立。 |

### 1.15 十四轮 F663

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F663 | compute_ttr 空输入早退分支缺 content_ttr/total_chars 键（schema 不一致） | P2 | **成立**（P2 维持） | 本轮运行：`compute_ttr('')` → `{'global_ttr': 0, 'sliding_ttr_mean': 0, 'sliding_ttr_std': 0}`（缺 content_ttr/total_chars）；正常路径 5 键。compute_stats.py:172-173,196-202。P2 成立。 |

**判定汇总：F601-F663 共 63 条全部成立，无整条误报。** 六条 P1（F601/F602/F612/F620/F637/F640）本轮均以直接运行/代码精读/全仓 grep/真实数据/计划原文多重复核，无反证；其中 F601 支持 review6-14 软异议（生产影响全潜伏，P1↔P2 可辩，不修改）；F640 确认 `_maybe_materialize_progress` 触发链 + `_auto_rebuild_progress_if_stale` 的 `trace/` 目录 vs `trace.jsonl` 文件路径错位（cli.py:728 接线确认），P1 成立且激活应升级 P0。

---

## 2. 新发现（F664-F665）

### F664 | 审计链对 records.parser 的 yaml.YAMLError 无防护：坏 YAML 的 post .md 使 `dispatch_with_write_audit` 的 finally 块整次审计崩溃（写越权/drift 门禁被旁路、成功 dispatch 的 rc 被异常取代、无 write-audit.jsonl 记录） | 边界/错误处理（F616 静默丢弃 / F651 静默 last-wins 的"崩溃"极） | P2（潜伏）

- 证据：`src/shenbi/records/parser.py:40`（`data = yaml.safe_load(body)`——无 try）；消费链 `src/shenbi/audit/write_audit.py:49`（`change = compute_file_change(...)`，内部 snapshot.py:110 也调 `parse_records`）、`:54`（`recs = parse_records(post_content)`——两处均无 try/except）；生产唯一调用点 `src/shenbi/dispatcher/executor.py:263-266`（`finally: post = snapshot_tree(...); result = audit_writes(skill, pre, post)`——finally 块内无防护）。
- 根因：判据 12 三件套的写审计消费方没有任何异常防护。`yaml.safe_load` 对语法损坏的 YAML 抛 `yaml.YAMLError`（ScannerError/ParserError 子类）——该异常**既不是 ValueError 也不是 TypeError**，因此即使按 `record_audit_outcome` 的 catch `(OSError, ValueError, TypeError)` 模式（record.py:42-46）也兜不住。任一 post .md 含 `## hooks` 段且 YAML 语法损坏（LLM/手工写入的未闭合引号、坏缩进——fixture 权威格式本身由 LLM 维护，格式一旦恢复即进入活动路径，见 F637 修复方向 (a)）时：`audit_writes` 在 finally 块抛异常 → (a) 成功的 dispatch（rc=0）被异常取代、调用方收到崩溃；(b) `record_audit_outcome` 不执行——无 write-audit.jsonl 记录、无 GATE_FAIL；(c) 写越权/drift block-ship 门禁整体旁路（崩溃是"响"的，但审计结果被丢失且其余文件的检查全部中止）。与 F651（重复键静默 last-wins）恰成两极：同一解析步骤，脏数据的"静默损坏"已被 F651 覆盖，"响亮崩溃"未被任何轮次覆盖（前 14 轮 grep YAMLError/ScannerError/ParserError 0 命中）。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.records.parser import parse_records
  parse_records('## hooks\n- id: H01\n  state: \"unclosed\n')
  -> RAISED: yaml.scanner.ScannerError while scanning a quoted scalar（行 2, 列 10）
  from shenbi.audit.write_audit import audit_writes
  audit_writes('shenbi-foreshadowing-track', {}, {'truth/pending_hooks.md': 坏文件})
  -> RAISED: yaml.scanner.ScannerError
  yaml.YAMLError 是 ValueError 子类？ -> False（ValueError 兜不住）
  "
  grep -rn "YAMLError\|ScannerError\|ParserError" docs/superpowers/audit-runs/2026-08-14/zone-reports/Z6*.md -> 0 命中
  ```
- 影响：潜伏（真实项目 post .md 无 `## hooks` 段，F637）——但该缺陷位于**每次 dispatch 都执行的 finally 审计路径**，且与 F637 修复方向 (a)（恢复权威格式）绑定即激活；激活后单文件坏 YAML 使整批审计失效并旁路写越权门禁。同族对照：record_audit_outcome 对 trace seam 异常有 catch（F615 缓解面），audit_writes 自身对 records 解析异常却零防护——审计管线内部的不对称。
- 建议方向：二选一并补测试——(a) `parse_records` 捕获 `yaml.YAMLError` → log WARNING（含段位置/错误类别）+ 返回 []（与 F616 非 dict 丢弃同语义，但需显式告警避免静默空转）；或 (b) `audit_writes` 对 `parse_records`/`compute_file_change` 包 try/except yaml.YAMLError → 记 ledger（含 parse_error 字段）+ 按 fail-closed 处理（该文件视为 unverifiable → block ship，而非静默跳过或崩溃）。补单测（坏 YAML 的 post .md：audit 不崩、ledger 有记录、其余文件检查继续；并断言"unverifiable"语义不静默放行）。

### F665 | drift-guidance skill §11.8 "卷级目标未达成"触发器在 compute_drift 未实现：SKILL.md 指示 LLM 读取 volume_score_trend.md 并从 drift_detection 获得卷级目标漂移信号，但 CLI 无该输入参数、无该 DriftKind、无 objective_achieved 解析 | 跨文件契约漂移（F617/F626/F629 同族：计划承诺的确定性防线未实现/未接线） | P2（潜伏）

- 证据：`skills/shenbi-drift-guidance/SKILL.md:137-139`（"## 卷级目标达成漂移（spec §11.8） 读取 truth/volume_score_trend.md，drift_detection 增加'卷级目标未达成'触发器。当 volume_score_trend 最近一行 objective_achieved=false 时，生成卷级漂移指导。"）；`src/shenbi/skill_utils/drift_detection/compute_drift.py:41-44`（DriftKind 仅 MONOTONIC_DECLINE/BELOW_MEAN_2SIGMA/VOLUME_DECLINE 三值）、`:236-286`（main() 仅 `--resonance`/`--arc-payoff` 两输入）；对照 `src/shenbi/skill_utils/escalation/check.py:88-91`（`volume_objective_missed` 触发器存在但属 escalation 模块，经 chapter_loop.py:997-1004 接线）。
- 根因：spec §11.8 承诺的卷级目标漂移触发器在 drift_detection（drift-guidance skill 实际调用的 CLI）中**从未实现**：main() 不接受 volume_score_trend.md 输入、全文件无 `objective_achieved` 解析、DriftKind 无对应值。skill 指示 LLM"读取 volume_score_trend.md 并生成卷级漂移指导"，但确定性 CLI 永远无法产生该信号 → LLM 要么静默遗漏（卷级目标漂移无传导）、要么绕过确定性助手自行读文件判断（违背模块"deterministic helper"定位）。`escalation.check_escalation` 的 volume_objective_missed 是另一条路径（升级触发），不能覆盖 drift-guidance 的漂移指导职责。全仓 `find . -name volume_score_trend.md` 0 命中（文件也不存在）——输入面、实现面、数据面三缺。前 14 轮仅以"缺失输入文件"角度覆盖（F639），未识别"该触发器在代码中不存在"这一实现缺口（与 F617/F626/F629 同族但为"承诺功能未实现"而非"已实现未接线"）。
- 验证命令+输出：
  ```
  grep -rn "objective_achieved" src/ skills/ -> 仅 SKILL.md:139（无任何代码解析）
  grep -n "VOLUME\|DriftKind" src/shenbi/skill_utils/drift_detection/compute_drift.py -> DriftKind 三值：MONOTONIC_DECLINE / BELOW_MEAN_2SIGMA / VOLUME_DECLINE（无卷级目标类）
  find . -name "volume_score_trend.md" -> （空）
  python3 -m shenbi.skill_utils.drift_detection --help -> 仅 --resonance / --arc-payoff / --write-audit-drift
  ```
- 影响：潜伏（真实项目无 volume_score_trend.md 且卷级检测本就未运行，F639）——但 skill 契约（SKILL.md 是 LLM 的执行指令）承诺的确定性信号缺失，一旦卷级趋势文件被补齐（F639 建议方向之一），LLM 会在无确定性信号的情况下自行判断或遗漏，与"漂移检测应确定性兜底"的设计意图相悖。
- 建议方向：二选一并补测试——(a) 在 compute_drift 实现 §11.8 触发器（新增 DriftKind（如 `VOLUME_OBJECTIVE_MISSED`）+ `--volume-objective <path>` 参数解析 volume_score_trend.md 的 objective_achieved 列，末行 false 时产出 finding），并与 SKILL.md:139 的指示对齐；(b) 若判定卷级目标漂移职责归 escalation/check_escalation（volume_objective_missed），则从 drift-guidance SKILL.md 移除该 §11.8 段落或改写为"由 escalation 路径承担"，避免对 LLM 承诺不存在的信号。补单测（构造末行 objective_achieved=false 的 volume_score_trend.md → CLI 产出卷级目标 finding）。

---

## 3. 严重度异议

| finding | 现定级 | 异议 | 理由 |
|---|---|---|---|
| F601 | P1 | **软异议（支持 review6-14）：可辩 P2** | 缺陷本体成立（对话塌陷 off-by-one），但生产影响全潜伏：detect_drift 的唯一生产入口 `_check_linguistic_drift` 在无 baseline（F602）时于 chapter_loop.py:2017-2019 提前 return（本轮代码精读确认 baseline 检查在 compute_linguistic_metrics 之前），detect_drift 从未被生产调用；即便接线，F612（is_drift 门控）与 F620（调用点吞异常）使干预仍不可达。按决策表"死代码/未接线"（P2）与"违反显式契约"（P1）均可辩护。不构成整条误报，不修改定级（无权限），供协调者裁决。 |
| F646 | P2 | **软异议（支持 review11-14）：impact 可辩 P1，维持 P2** | sensitivity 为 BLOCKING 级安全网且激活链是活的（audit_layer → get_active_genre_audits → sensitivity_blocking → escalation），禁用即静默移除能产生 BLOCKING 升级信号的审计，且写侧 rationale 与读侧 G0 均无信号；但真实配置当前 10 维全 True（潜伏）+ "absent → non-critical" 是 thresholds.py:48-49 显式声明设计 → 维持 P2 亦可辩护。不修改定级（无权限），供协调者裁决。 |

（其余 61 条无严重度异议。六条 P1 中 F602/F612/F620/F637/F640 本轮均经运行/代码/全仓 grep/真实数据多重复核，无反证；F640 触发即数据丢失、激活应升级 P0 的既有评估本轮复核支持。F642 证据裁决支持 review9-14 终裁，本轮重放一致，无新争议。）

---

## 4. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2056 漂移检查全链、:2014-2019 baseline 读、:2023-2043 is_drift 门控、:2047-2056 opening-similarity 门外交互、:2719-2727 ESCALATE 调用点吞异常、:684-743 `_maybe_materialize_progress`/`_auto_rebuild_progress_if_stale` trace 路径错位、:997-1004 check_escalation 接线、:2691/:3028 materialize 调用点）、revision_router.py（:62-64 DEFAULT_RESONANCE_FLOOR=50）、audit_layer.py（:100-113 激活语义 + snake_case 回退）、g0_config_coherence.py（:88-97 缺失语义 + `is False`）、audit/write_audit.py（:49-56 records 消费、无 YAMLError 防护）、audit/snapshot.py（:110 parse_records 消费）、audit/record.py（:24,42-46 block + trace seam catch (OSError, ValueError, TypeError)）、dispatcher/executor.py（:263-266 finally 块 audit_writes 无防护）、safe_write.py（:126-140 trace_action 唯一生产调用点）、dispatcher/cli.py（dispatch_with_write_audit 别名）、pipeline/cli.py:726-728（_auto_rebuild_progress_if_stale 接线）、plugins/master.json（59 skills 规范化对比）、skills/ 目录（74）、skills/shenbi-drift-guidance/SKILL.md（:51-58 CLI 调用、:137-139 §11.8 卷级目标触发器承诺）、skills/shenbi-style-learning/SKILL.md:51（compute_stats CLI 强制接线）、novel-output/xinghuo-ranqiong/（56 章 compute_all_stats 全量运行、chapters/chapter-1/15/19/24/25/45/46.md META 结构、truth/pending_hooks.md 143 行章节附录结构、truth/resonance_trend.md 单表单行 ch55 含 human_overridden 列、truth/audit_drift.md `## Ch55` 格式、genre-config.json 10 维 camelCase 无 drift_detection 键、progress.json scoring schema、config-change-log.jsonl 单条、style/ 与 context/ 均无 linguistic_baseline.json、无 trace.jsonl、无 volume_score_trend.md）、tests/fixtures/truth-pending_hooks.md + tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照，含 core_hook: true 布尔字段但不在 md 表列中 → F641 潜伏）、docs/superpowers/specs/archive/2026-07-19-18-pipeline-architecture-optimization-design.md（§11.8 卷级目标出处）、docs/superpowers/plans/archive/2026-07-19-07-*.md（F602 接线指令原文）。
- 维持观察（不重复立项，供协调者参考，均本轮复核确认仍成立）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）与同名文件跨目录覆盖、compute_pattern main 非 dict 章节 `.get` 崩溃、compute_pattern.classify_entropy 恰好等于阈值（h==2.5 落下一档，measure-zero）、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize round 名解析脆弱性（`name.split("-")[1]`）与 `_as_int` str 数值 ValueError（F662 镜像）与 `init_tier = str(payload.get("tier", tier))` 遇 None → "None"、compute_drift `_append_audit` 重复追加无幂等 + `--write-audit-drift` 用 CWD 相对路径 `Path("truth/audit_drift.md")`、compute_connectives "时间"/"顺序" 类别重叠与 "但是"↔"但" 同类别子串双计、cjk.count_words 非法 mode 静默走 mixed、`find_terms` 裸字符串 terms 逐字符迭代、plugins/generate.py `fields["marketplace"]` 缺 fields KeyError、`detect_chapter_drift` 平滑先于排除、check_window_redundancy docstring "sliding window" vs 实现仅取最后 4 章、frequency_divergence_alarms docstring ">3σ" vs 实现 4× 速率启发式、verify_chain 不校验链首且 compact 后至多一条 COMPACTION 使链校验空转（设计属性）、detect_drift 对话密度 0→正值不记录 deviation（健康信号，疑有意为之）、`compute_linguistic_metrics` dialogue_density 不计「」/『』角引号（真实数据用 ASCII `"`）、escalation 输入含 nan 时 slope 静默不触发（F654 值级同族）、`_load_config` 的 `assert isinstance(result, dict)` 在 `python -O` 下被剥离（genre-config.json 为 repo 控制文件）、skills/shenbi-escalation-review/SKILL.md:34 `escalation_check` 概念名 vs 实际 `check_escalation`（review12 已述）。
- 关于"0 新 finding"收敛目标：F601-F663 共 63 条全部成立（含六条 P1），**无整条误报**；本轮新发现 2 条均为前十四轮未触达的**消费链/契约侧**方向（审计链 YAMLError 崩溃、drift-guidance §11.8 触发器未实现），均 P2、潜伏、无 P0/P1。收敛曲线 11→8→10→7→3→3→1→3→2→5→2→3→2→2→1→2，自 F648 起连续 8 轮新发现全部为 P2 潜伏级（本轮 2 条，数量回到个位低位）。按 review10-14 提议的"无 P0/P1 新 finding + 无整条误报"判据本轮达成收敛；按字面"0 新 finding"判据未完全收敛。建议协调者落定收敛判据后终止轮次，并优先排期修复 P1 接线族（F602/F612/F620/F640 绑定修复 + F637 格式决策，其中 F664 应随 F637 修复方向 (a) 一并处理——恢复权威格式即激活 F664 崩溃路径）。

---

## 5. 返回摘要

**复核统计：新 finding 2（F664-F665，均 P2、均潜伏）/ 误报 0 / 严重度异议 0 条新增（支持既有软异议 2 条：F601、F646）。**

**判定：非 0 新 finding（2 条，均 P2 潜伏级、无 P0/P1）——按"无 P0/P1 新 finding + 无整条误报"判据达成收敛；按字面"0 新 finding"判据未完全收敛。**

- F601-F663 共 63 条全部成立，**无整条误报**。六条 P1（F601/F602/F612/F620/F637/F640）本轮以直接运行 + 代码精读 + 全仓 grep + 真实数据 + 计划原文多重复核，无反证：
  - F601：baseline dialogue=10→current=1 → is_drift False、severity NONE（off-by-one 成立）；生产影响全潜伏（F602 无 baseline → detect_drift 从未被生产调用）→ 支持软异议 P1↔P2 可辩；
  - F602：establish_baseline 全仓零调用、无 linguistic_baseline.json（style/ 仅 style_profile.md）；计划 Task 5a 接线指令原文核实；
  - F612：base stm=60/cur=110 → is_drift False、severity ESCALATE（实测），chapter_loop.py:2023 门控吞掉绝对阈值干预；
  - F620：chapter_loop.py:2719-2727 "non-blocking" 注释 + `except Exception` 吞 DriftEscalationError；
  - F637：真实 pending_hooks.md 无 `## hooks`/`## 活跃伏笔`（`grep -n "^#"` 全清单核对），三件套在真实文件上全空转，write_audit.py:49-56 生产接线确认；
  - F640：`"INIT"`/`"MARK_DONE"` 全仓仅消费方 materialize.py 与测试命中；`_maybe_materialize_progress` steps_done%5==0（含 0）+ `except Exception: pass`；`_auto_rebuild_progress_if_stale` 检查 `trace/` 目录 vs TraceWriter 写 `trace.jsonl` 文件（路径错位，cli.py:728 接线确认）；真实 progress.json 为 scoring schema（触发即数据丢失，激活应升级 P0）。
- **F642 证据裁决复核**：raw 0.6267 / 剥离后生产顺序 0.6367 / 交换顺序 0.57 / autojunk=False 0.77；56 章相邻对 34/55 \|diff\|>0.01（本轮重放一致）——0.57 与 0.637 均可复现（SequenceMatcher 参数顺序不对称，F649），review8 裁定不成立，review9-14 终裁成立。
- 本轮 2 条新 finding 均为前十四轮未触达的**消费链/契约侧**方向，均 P2、潜伏：
  1. **F664（P2）**：records.parser 的 `yaml.YAMLError` 在写审计消费链（write_audit.py:49,54 / snapshot.py:110 / executor.py:263-266 finally 块）无任何防护——坏 YAML 的 post .md 使整次审计崩溃、成功 dispatch 的 rc 被异常取代、write-audit.jsonl 无记录、写越权/drift 门禁被旁路（运行复现 ScannerError；yaml.YAMLError 非 ValueError 子类，record.py 的 catch 模式兜不住；与 F651 静默 last-wins 成"崩溃"极；F637 修复方向 (a) 恢复权威格式即激活）。
  2. **F665（P2）**：drift-guidance SKILL.md:137-139 承诺的 §11.8 "卷级目标未达成"触发器在 compute_drift 未实现——CLI 无 volume_score_trend.md 输入参数、DriftKind 仅三值、无 objective_achieved 解析（grep 证实）；escalation 模块的 volume_objective_missed 属另一路径；LLM 从确定性 CLI 永远拿不到该信号（与 F617/F626/F629 同族但为"承诺功能未实现"而非"已实现未接线"；F639 仅覆盖文件缺失角度）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F664 | 审计链对 yaml.YAMLError 无防护：坏 YAML 的 post .md 使 finally 块整次审计崩溃、写越权/drift 门禁旁路 | P2 | parser.py:40; write_audit.py:49,54; snapshot.py:110; executor.py:263-266 |
| F665 | drift-guidance §11.8 卷级目标未达成触发器在 compute_drift 未实现（无输入参数/DriftKind/objective 解析） | P2 | SKILL.md:137-139; compute_drift.py:41-44,236-286; escalation/check.py:88-91 |
| F601（异议） | P1↔P2 可辩（生产影响全潜伏），支持 review6-14 软异议 | 严重度软异议 | linguistic_drift.py:215/218; chapter_loop.py:2017-2019 |
| F646（异议） | sensitivity（BLOCKING 级安全网）impact 可辩 P1，维持 P2 | 严重度软异议 | thresholds.py:50-84; chapter_loop.py:989-1003 |
| F642（裁决） | 0.57/0.637 均可复现（参数顺序不对称）；生产顺序剥离后 0.6367 仍 > 0.6；review8 裁定不成立 | 证据裁决 | linguistic_drift.py:249-253; chapter-45/46.md |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review15.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
