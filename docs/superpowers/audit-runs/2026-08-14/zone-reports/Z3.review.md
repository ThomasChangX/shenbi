# Z3 分区独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 独立复核（本文件 `Z3.review.md`，与初审 `Z3.a.md` 隔离）
> 复核范围：34/34 文件全量重读（13,641 行），独立验证每条初审 finding，另做跨文件接线/格式实证
> 复核发现编号：F324-F334（11 条新发现，其中 1 条 P0 级）
> 方法：初审结论逐条重验（grep + python3/uv run 实证 + 真实生产文件 `novel-output/xinghuo-ranqiong/` 对照）；对初审严重度仅提异议不改动

---

## 一、复核结论总表（初审 22 条编号 finding，F300-F319 + F322-F323）

> 说明：初审声称 "23 findings F300-F323"，实际编号仅 22 条（**F320/F321 缺失**，见 F334）。逐条判定如下。

| 编号 | 判定 | 严重度裁定 | 理由 |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | `python3 -c 's="\u003c"; print(s=="<")'` → True；`"a<b</document>c".replace("<","\u003c")==原串` → True。no-op 实证确凿 |
| F301 | **成立** | P1 维持（贴 P0，同意初审校准） | run_audit_layer 唯一调用点 chapter_loop.py:2932（串行块内）；resonance_score 唯一赋值点 :2997（串行块内）；并行波无条件触发（:2515 无环境开关）→ 串行审计/重审环/resonance 落盘/boundary circle 全部不可达。**叠加 F325 后实际影响比初审描述更重**（BLOCKING 永不修复且无升级路径），P0 边界成立 |
| F302 | **成立** | **严重度异议：P1 → P0（决策表依据）** | dispatch_skill(state=) 仅 chapter_loop.py:2794（串行可及路径，即 chapter-planning/chapter-drafting 两步）；genesis:302、closure:253、triggers:561、parallel_dispatch:86、cli.py:123/751 均无 state。`getattr(state,"chapter",0)`（:1335）——PipelineState 无 chapter 属性 → 恒 0。决策表 P0 例子列明 "TokenLedger 少计"；账本行静默丢失 = 数据丢失。**但**账本为成本侧车（不影响正文执行），P1 亦可辩护——初审可保留 P1，复核认为按表应升 P0 |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（死函数内）；restore_from_snapshot 无 src 调用方；_prune_old_snapshots 仅 :1711/:1737（死函数内）；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转（仅 linguistic-drift-check 有实现体）；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError raise 于 chapter_loop.py:626/:2882，src/ 内无任何 except；cmd_next/cmd_resume 仅捕 FileNotFoundError。crash-resume + 持久化 budget 路径下必然裸崩（无 checkpoint、无 JSON） |
| F305 | **成立但框定不准确** | **严重度异议：P2 → P1** | 初审只描述"裸子串导致假阳性"。实证显示**生产影响是系统性假阴性**：`_any_audit_has_findings`（:1797）扫描 `chapter-N-{continuity,character,world-rules,...}` 等 13 个旧式文件名，而并行波实际产物是 `chapter-N-group-{factual,character,craft,plan}.md` / `chapter-N-resonance.md`（audit_suffix 实证），两集合交集仅 `sensitivity` → 该检查在生产**几乎恒 False**，直接门控 step 16 revision（见 F325）。假阳性只是次生问题 |
| F306 | **成立** | P2 维持，无异议；补一条实证 | truth/volume_map.md 全仓 0 写入方（grep 实证）；`Chapter {chapter}` 子串匹配缺陷成立。**补充**：volume_context 即使修好路径也从不被注入——dispatch_helper._INJECT_FROM_CACHE（:601-621）只注入 world_rules/character_list/style_profile/pending_hooks 四字段，SharedAuditContext.volume_context 无消费方 → 双重失效 |
| F307 | **成立** | P2 维持，无异议 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 已合并为 lifecycle）；_parse_hook_entries 只认 `^(MH|H)-` 或含 "hook"，与生产 `P0-4` 不兼容 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state 无生产调用 |
| F309 | **成立** | P2 维持，无异议 | _validate_state_consistency/_heal_current_step 注释声称 cli.py 调用，grep cli.py 无引用；测试 test_state_machine_heal 直调函数本体（掩蔽接线缺失） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（:428-433）缓存存在即返回，无 mtime/内容校验；revision（step 16）改写正文后不再重提 |
| F312 | **成立** | P2 维持（latent），无异议 | upsert_yaml 无生产调用方；_serialize_yaml_records 只写 frontmatter；测试断言未检查 body 存留 |
| F313 | **成立** | P1 维持，无异议 | resolve_volume_path 的 _BOUND_N 把 `audits/chapter-N-long-span.md` 的 N（章号语义）替换为卷数；closure step 6 G4 校验 `chapter-{卷数}-long-span.md` 恒错位 → G4 FAIL → 重试 3 次后 ESCALATION，closure 正常路径可复现阻塞 |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | _merge_step_result 检查 `getattr(result,"result",None)`——DispatchResult 无该属性 → 恒 no-op；chapter_loop.py:2425-2426 调用无效果 |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重；consolidate_review_results 只提行不去重。注：F325 使 revision 在产线暂不可达，此缺口暂为潜伏 |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES 无使用点；handle_dispatch_failure 用 max_revision_retries 作 dispatch 限额 |
| F318 | **成立** | M 维持 | 逐条验证：dispatch_helper.py:93 "base = 300s" vs :100 `base=900`；chapter_loop.py:4 "20 steps" vs 16；:11 "Step 17" vs step 13；:19-21 过期货注；:2550 "7 skills" vs 实际 6；scr_extractor.py:313 三次相同引号判断；revision_router.py:86 blocking 形参未用（body 实证）。**F318.8 异议**：:2607-2608 的 `current_step=""` 与 _advance 之间无任何 I/O/await，同步函数内不存在实际 crash 窗口，风险被高估（维持 M 即可） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback 无章号绑定，注入下一章首次 dispatch（chapter_loop.py:2775-2780） |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached 无调用方；且路径 `config/genre-config.json`（:198）与生产 `genre-config.json` 也不一致（双重死） |

**初审漏报统计**：F300-F319、F322、F323 全部成立（0 条误报）；2 条严重度异议（F302、F305）；F301/F326 贴 P0 边界；另有 11 条新发现（F324-F334），其中 **F324 为 P0 级漏报**（初审 _shared.py 判 "无（区间构造正确）" 且 F306 假定 outline/volume_map.md 可正常解析——均未对真实 volume_map 做过格式验证）。

---

## 二、新发现（F324-F334）

### F324 | 生产 volume_map 中文格式与全部解析器不匹配：卷边界系统、closure 转换、卷上下文、计划骨架全线静默失效 | error | **P0**

- 证据：
  - 解析器 `_shared.py:35-42`：`_END_RE` 仅匹配英文 "Chapter End:/End:/Chapter End:"，`_RANGE_RE` 仅匹配 "Chapters N-M"/"ch N-M"（`(?:chapters?|ch)\s*(\d+)\s*[-...]\s*(\d+)`）。
  - 生产格式（两个权威 skill 模板 + 真实产物一致）：`skills/shenbi-story-architecture/SKILL.md:104-124`（`## 第N卷：{卷名}`、`**章节范围**: 第N章 - 第M章（共 K 章）`）、`skills/shenbi-volume-outlining/SKILL.md:123-165,250`（同款中文 + `**章节数**: X`）；真实产物 `novel-output/xinghuo-ranqiong/outline/volume_map.md`（473 行，全中文，无任何 "Chapter End"/"Chapters N-M"）。
  - `triggers.py:371` `_count_total_chapters` 正则 `(?:章节数|Chapters?)\s*:\s*(\d+)` 对 `**章节数**: X` 的加粗格式同样不匹配。
  - 章节节点提取 `plan_skeleton.py:199` / `context_assemble.py:244` / `chapter_loop.py:2184` 用 `\|\s*{chapter}\s*\|`（裸数字），生产表为 `| 第1章 | 开篇 | ... |` → 不匹配（第 1 章还会误中跨卷桥接表的 `| 1 | ... |` 行）；桥接提取 `\|\s*(V\d+-B\d+)\s*\|` 对生产 `| # | 钩子内容 | ... |` 表 0 命中。
- 根因：解析器按英文格式实现并以英文格式自测（`tests/unit/pipeline/test_triggers.py:58-70` 构造 "## Volume N / Chapter Start/End"），而两个生产 skill 与真实输出均为中文格式——三方格式契约断裂，无任何转换层。
- 验证命令+输出：
  ```
  uv run python -c "from pathlib import Path; from shenbi.pipeline._shared import read_volume_boundaries,_resolve_volume_at_runtime; from shenbi.pipeline.triggers import _count_total_chapters,is_volume_boundary; pd=Path('novel-output/xinghuo-ranqiong'); print(read_volume_boundaries(pd)); print(_resolve_volume_at_runtime(pd,20)); print(_count_total_chapters(pd)); print(is_volume_boundary(15,pd))"
  # → set() / None / 0 / False   （ch15 是第 1 卷末章，应为 True）
  python3 -c "import re; t='**章节数**: 15'; print(re.findall(r'(?:章节数|Chapters?)\s*:\s*(\d+)', t))"
  # → []   （加粗格式不匹配）
  grep -rn "Chapter Start\|Chapter End\|Chapters [0-9]" skills/ tests/fixtures/   # → 0 命中（生产侧无英文格式）
  python3 -c "import json; print(json.load(open('novel-output/xinghuo-ranqiong/novel.json')).get('total_chapters'))"
  # → None（真实项目 total_chapters 从未被 pipeline 写入）
  ```
- 影响链（全部静默）：
  1. `is_volume_boundary` 恒 False → §6.4/6.5 卷边界触发全家（foreshadowing-resolve、volume-consolidation L3、review-arc-payoff、score-volume、memory-distill L5、style-learning 卷级、drift-guidance、四路 expansion、VOLUME_BOUNDARY checkpoint、卷级快照）永不执行；
  2. `cli._update_total_chapters`（max(空集)=0）与 `triggers._update_total_chapters`（0）都无法写 novel.json.total_chapters → `_read_total_chapters`=0 → `_orchestrate_to_checkpoint` 的 `if total > 0:` 守卫（cli.py:219）跳过全部章间触发 → **book_closure 永不触发 → pipeline 永不进入 CLOSURE 阶段，整本书无法通过 pipeline 完成**（真实项目 novel.json total_chapters=None 佐证）；
  3. `_resolve_volume_at_runtime` → None → plan_skeleton 卷名 "Unknown"、context_assemble 卷上下文空、audit_context_cache volume_context 空（与 F306 叠加）；
  4. 计划骨架章节节点提取失效（第 1 章误读桥接行内容为"节点角色"）。
- 建议方向：在 `_shared.read_volume_boundaries` 增加中文格式解析（`第N章 - 第M章`、`（共K章）`、`章节数` 及 `| 第N章 |` 表格行），章节节点/桥接正则同步支持 `第N章` 单元格；补一条以真实中文 volume_map 为 fixture 的解析测试；确认 novel.json.total_chapters 写入路径（genesis step 6 完成后由代码固化而非依赖 LLM）。

### F325 | step 16 revision 双重门控矛盾：`_any_audit_has_findings` 扫描旧式审计文件名，BLOCKING 路由后 revision 仍被跳过（审计 BLOCKING 永不修复） | error | P1

- 证据：`chapter_loop.py:1770-1799`（_any_audit_has_findings 遍历 13 个旧式维度名 continuity/character/world-rules/pacing/dialogue/motivation/pov/memo-compliance/foreshadowing/anti-ai/texture/reader-pull/sensitivity）；并行波实际写入 `chapter-{N}-{audit_suffix(skill)}.md`（chapter_loop.py:2557，audit_suffix("shenbi-review-group-factual")="group-factual"）；revision 门控顺序 `_is_revision_skipped`（:2755，route 权威）→ `_should_run_step`→`_any_audit_has_findings`（:2763，旧文件名扫描，第二个检查否决第一个）。
- 根因：重构合并 7 个串行审计为 4 个 group 波 + resonance + sensitivity 后，revision 的条件门控未同步更新文件名清单；两个门控信号（route vs 文件扫描）互为矛盾实现。
- 验证命令+输出：
  ```
  uv run python -c "from shenbi.pipeline.audit_layer import audit_suffix; prod=['chapter-N-'+audit_suffix(s)+'.md' for s in ['shenbi-review-group-factual','shenbi-review-group-character','shenbi-review-group-craft','shenbi-review-group-plan','shenbi-review-resonance','shenbi-review-sensitivity']]; legacy=['chapter-N-continuity.md','chapter-N-character.md','chapter-N-world-rules.md','chapter-N-pacing.md','chapter-N-dialogue.md','chapter-N-motivation.md','chapter-N-pov.md','chapter-N-memo-compliance.md','chapter-N-foreshadowing.md','chapter-N-anti-ai.md','chapter-N-texture.md','chapter-N-reader-pull.md','chapter-N-sensitivity.md']; print(set(legacy)&set(prod))"
  # → {'sensitivity'}   （仅 sensitivity 重叠；其余 12 个文件名生产永不产生）
  ```
- 影响：并行路径下 BLOCKING 审计 → collect_audit_issues 检出 blocking → route=REVISION（存 audit_results）→ step 16 先过 `_is_revision_skipped`（非 NO_REVISION，不跳过）→ 再过 `_should_run_step`→`_any_audit_has_findings`（旧文件名几乎全不存在）→ **revision 被跳过** → 章节以 BLOCKING 未修复状态"完成"；配合 F301，无重审、无 escalation、revision_count 虚增。F305 的"假阳性"只是此问题的次生面（sensitivity 文件含裸 "BLOCKING" 时误触发）。
- 建议方向：`_should_run_step` 的 revision 门控直接采用 `cs.audit_results[_REVISION_ROUTE_KEY] != NO_REVISION`（删除 _any_audit_has_findings 或将其文件名清单更新为 group-* 并改用 revision_router 正则）。

### F326 | 并行 post-draft 违反 write_safety WRITE_SHARED 串行不变量：lifecycle 与 state-settling 并发写 truth/pending_hooks.md（lost-update 竞态） | error | P1

- 证据：`chapter_loop.py:2405-2419`（run_parallel_post_draft_steps 用 ThreadPoolExecutor(2) 并发 dispatch lifecycle + state-settling）；`write_safety.py:22-35,45-50`（两 skill 均在 _WRITE_SHARED_SKILLS，明确"WRITE_SHARED 必须串行"、assert_parallelizable 只保护审计波路径）；两 skill contract 均声明更新 pending_hooks.md：
  ```
  uv run python -c "from shenbi.contracts.legacy import load_contract; print(load_contract('shenbi-foreshadowing-lifecycle').get('updates')); print(load_contract('shenbi-state-settling').get('updates'))"
  # → ['truth/pending_hooks.md'] / ['truth/current_state.md','truth/particle_ledger.md','truth/emotional_arcs.md','truth/subplot_board.md','truth/pending_hooks.md','truth/chapter_summaries.md']
  ```
- 根因：该并行路径未调用 assert_parallelizable 也未走 truth_io 的 per-path 锁（dispatch 写路径是 safe_write 整文件替换，读-改-写无锁）；两线程各自把 LLM 输出的 pending_hooks.md 整文件覆盖 → 后写者赢，先写者的 hook 变更静默丢失（spec §2.2 lost-update race，truth_io.py:10-14 文档自述的竞态正是此形态）。
- 影响：foreshadowing-lifecycle 与 state-settling 对 pending_hooks.md 的并发写竞态——任一方的 hook 种植/状态更新可能整份丢失；且与 write_safety.py 自身声明的 spec §3.4 不变量直接冲突（该模块文档宣称"any skill not classified READ_ONLY_AUDIT must run serially"）。测试 test_parallel_steps.py 全部 mock dispatch_skill，未覆盖真实写路径竞态。
- 建议方向：两 skill 的 pending_hooks.md 写路径收敛到 `write_truth_file(mode="upsert_yaml")`（或串行化该文件写入，如合并后主线程统一落盘）；或按 write_safety 不变量把 lifecycle 移出并行对。

### F327 | §6.3 决策树未接线：decide_revision 无生产调用；route_chapter_revision 忽略 blocking；resonance floor 仅 log 不决策 | error | P2

- 证据：`revision_router.py:119-140`（decide_revision 实现完整 §6.3 树）；grep src/ → 无调用（仅 tests/unit/pipeline/test_revision_router.py）；`revision_router.py:86-105` route_chapter_revision 的 blocking 形参在函数体内 0 次使用；`chapter_loop.py:1894-1904` resonance below-floor 只 log.warning，不产生 REVISION 决策。
- 根因：生产路由路径只调用 route 子层（issues→mode），BLOCKING→REVISION 与 floor→REVISION 两个顶层分支从未接线；config.resonance_global_floor 为装饰性旋钮（且 F301 下 resonance_score 恒 None → check_resonance(None)=True，floor 恒过）。
- 验证命令+输出：`grep -rn "decide_revision" src/` → 仅 revision_router.py:119 定义。
- 影响：§6.3 决策契约（无 BLOCKING + resonance<floor → revision）静默不执行；floor 配置误导用户。
- 建议方向：生产路径调用 decide_revision（把 cs.resonance_score 传入）；删除 blocking 形参或真正参与决策。

### F328 | CONDITIONAL_STEPS 从未被迭代（intent-management/drift-guidance/snapshot-manage 每章门控死代码）+ 自适应触发死簇 | optimization | P2

- 证据：`chapter_loop.py:266-290` 定义 CONDITIONAL_STEPS；grep src/ → 仅定义处与注释，无任何迭代/引用；`_should_run_recall`（:1391）/`_should_run_drift`（:1469）/`_check_conditional_resolve`（:1230，仅被 `"foreshadowing-track" in step.skill` 死分支调用）/`_update_last_recall_manifest`（:3048 对不存在的 step.skill）/`_should_run_step` 的 intent-management/drift-guidance 分支（:1814-1818，对应 skill 不在 CHAPTER_STEPS）全部无生产触发路径；tests/pipeline/test_chapter_steps_restructured.py:40-95 直接调用 _should_run_step 验证这些死分支（测试掩蔽接线缺失）。
- 根因：Plan 18 重构把 3 个条件步骤移出 CHAPTER_STEPS 后未接回任何执行点（intent-management 仅 genesis step 14 一次、drift-guidance 仅卷边界触发器、snapshot-manage 仅 closure step 10/resume）。
- 验证命令+输出：`grep -rn "CONDITIONAL_STEPS" src/` → 仅 chapter_loop.py:131(注释)/266(定义)。
- 影响：每章 intent-management / drift-guidance 门控（spec §6.1 条件步骤）缺失；自适应 recall/drift 触发逻辑全部空转（manifest last_recall/last_drift 永不被写）。
- 建议方向：明确 CONDITIONAL_STEPS 去留——接回 run_chapter_step 的索引分支或删除并迁移测试。

### F329 | shenbi-review-sensitivity 每章双发（core 波 + genre 波同一 skill，写同一文件） | optimization | P2

- 证据：`chapter_loop.py:2551` core_skills 含 `shenbi-review-sensitivity`（CHAPTER_STEPS step 14，is_audit）；`audit_layer.py:44-54` GENRE_ACTIVATION_MATRIX["sensitivity"]="shenbi-review-sensitivity" 且 `_CORE_CIRCLE_KEYS`（:57-67）不含 "sensitivity"；fixture `tests/fixtures/genre-config-example.json` auditDimensions.sensitivity=true → 两波均含该 skill，`output_path` 均为 `audits/chapter-N-sensitivity.md`（后写覆盖先写）。
- 根因：sensitivity 同时是 core-circle 步骤与 genre 维度，去重逻辑（_CORE_CIRCLE_KEYS）漏掉它。
- 验证命令+输出：`uv run python -c "..."`（见上 audit_suffix 列表：sensitivity 同时在两集合）。
- 影响：每章 1 次多余 LLM 调用（约 1 次全章审计成本），双倍 token 浪费；CASCADABLE 过滤对两波同判（同跳或同跑）。
- 建议方向：把 "sensitivity" 加入 _CORE_CIRCLE_KEYS（genre 波跳过 core 已有的维度），或两波合并前去重。

### F330 | cmd_resume 忽略 `_verify_truth_integrity` 返回值——"fail fast"文档声称未兑现 | error | P2

- 证据：`cli.py:723` `_verify_truth_integrity(state, project_dir)` 返回值未接（docstring :626-627 声称 "the pipeline can fail fast here"）；函数返回 missing 列表仅 log.warning（:671-672）。
- 根因：校验实现为纯日志，未接到实际阻断/检查点逻辑。
- 影响：resume 时关键 truth 文件缺失（如 chapters/、truth/pending_hooks.md 被误删）仍继续执行，直到首个 G1/G4 才报错；文档-行为漂移。
- 建议方向：missing 非空时设置 ESCALATION checkpoint（或 emit_json ERROR）后返回，并把返回值接入 cmd_resume。

### F331 | 并行 post-draft 的 G4 失败只 log 不重试/不升级（与串行语义不一致），lifecycle G4 失败输出被静默接受 | error | P2

- 证据：`chapter_loop.py:2664-2675`（G4 失败仅 log.warning，然后 add_step_done + 推进）；对照串行路径 `_handle_failure`（:587-671）会对 G4 失败重试/升级。lifecycle（step 7）无 checkpoint，其 G4 失败输出直接进 pending_hooks.md。
- 根因：并行重构未迁移 G4 重试/软失败跟踪语义到并行块。
- 影响：foreshadowing-lifecycle 结构不合格的输出静默入库（下游 truth_index/context 读到残缺 hook 数据）；state-settling 的 G4 失败虽有人工 checkpoint 兜底，但不重试即进入 review。
- 建议方向：并行块对 G4 失败复用 _handle_failure（含 SoftFailTracker 与预算递增），或在提交 staging 前重跑 G4。

### F332 | state.token_usage 不进 to_dict/from_dict——token 汇总跨进程/跨 save 丢失 | error | P2

- 证据：`dispatch_helper.py:1314-1329`（_record_token_usage 动态挂 `state.token_usage`）；`state.py:228-294` to_dict 无 token_usage 键、:300-363 from_dict 不读该键。
- 根因：Task 7 的账本接线只覆盖内存侧，序列化侧遗漏。
- 影响：print_token_summary（chapter_loop.py:920，章末触发）只反映本进程内存累计；每次 save/load 后归零；与 F302（账本落盘缺失）同根不同面。
- 建议方向：to_dict/from_dict 补 token_usage（或明确其为进程内瞬态并在文档标注）。

### F333 | genesis G4 auto 模式开关语义错位（per_chapter_review_enabled 兼任 genesis 严格度开关）+ 章数口径不一致 | error | P2

- 证据：`genesis.py:328` `if state.config.per_chapter_review_enabled: return _handle_failure(...)`（per_chapter_review_enabled=False 即 --auto 时，G4 失败 1 次重试后"continue regardless"，静默接受 G4 不合格输出）；`triggers.py:371` `_count_total_chapters` 按"章节数 字段求和" vs `cli.py:159` `_update_total_chapters` 按 max(boundaries)——两口径对生产格式均返回 0（见 F324），即便修复格式两者仍可能不一致（求和 vs 最大值）。
- 根因：复用章节审查开关承担 genesis 语义；两条 total_chapters 更新路径口径漂移。
- 影响：--auto 模式下 genesis 步骤 G4 失败产物被静默放行（无 CORRECTIVE 循环）；total_chapters 更新逻辑双实现。
- 建议方向：genesis 引入独立 `genesis_g4_auto_continue` 配置；统一 total_chapters 计算（以 _shared.read_volume_boundaries 为唯一来源）。

### F334 | 初审报告编号空洞：声称 23 findings，实际编号仅 22 条（F320/F321 缺失） | error | M

- 证据：`docs/superpowers/audit-runs/2026-08-14/zone-reports/Z3.a.md` 头部 "23 findings F300-F323" 与任务书一致，但正文与汇总仅出现 F300-F319 + F322 + F323（22 条编号）；`grep "F320\|F321"` → 0 命中。
- 影响：跨分区编号连续性破坏，后续引用 F320/F321 会产生歧义。
- 建议方向：在初审报告中补注 F320/F321 为保留编号或重编号对齐。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件全量 deep-read，共 13,641 行）。
- 初审覆盖缺口表的处置（dead-code/must-test/acceptable 分类）与复核观察一致；复核补充确认：**初审的 must-test 清单遗漏了卷边界解析（F324）与 revision 门控（F325）两个生产主路径缺口**——初审对 _shared.py 的"无 finding"判定与 F306 对 outline/volume_map.md 的"canonical"假定均未做真实文件格式验证，是本轮最大覆盖空洞。
- 复核新增验证资产：`novel-output/xinghuo-ranqiong/outline/volume_map.md`（真实生产产物）作为格式对照基准；未创建/修改任何仓库文件（除本报告）。

---

## 四、汇总

- **新 finding 数**：11（F324-F334，其中 F324=P0、F325/F326=P1、F327-F333=P2、F334=M）
- **误报数**：0（初审 22 条编号 finding 全部成立）
- **严重度异议数**：2（F302 P1→P0、F305 P2→P1；另 F301 同意"贴 P0"校准、F318.8 风险高估但维持 M）
- **0 新 finding 判定**：**否**——存在 1 条 P0 级漏报（F324 volume_map 格式断裂）与 2 条 P1 级漏报（F325 revision 门控失效、F326 并行写竞态），初审结论需修正。
- **校准升级提示**：F324 应进入阶段 4 复核优先级首位（阻断整本书完成 + 卷级特性全家静默失效，实证于真实产物）；F325/F326 为生产主路径可复现缺陷。
