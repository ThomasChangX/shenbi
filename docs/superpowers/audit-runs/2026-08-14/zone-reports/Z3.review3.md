# Z3 分区第三轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第三轮独立复核（本文件 `Z3.review3.md`，与初审 `Z3.a.md`、第一轮 `Z3.review.md`、第二轮 `Z3.review2.md` 隔离）
> 复核范围：34/34 文件全量重读（13,641 行），逐条独立验证 F300-F344；以真实生产项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行、audits 722 文件、chapters 145 文件、plans、truth 文件、pipeline-state.json、novel.json）为格式/行为对照基准
> 本轮发现编号：F345-F352（8 条新发现，其中 1 条 P1）
> 方法：对前两轮每条 finding 独立重验（read 全部 34 文件 + grep + uv run/python 实证 + 真实生产文件对照）；对前几轮严重度仅提异议不改动
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：真实 volume_map.md 全中文格式（`## 第一卷：觉醒之火（第1-15章）`、`**章节范围**: 第1章 - 第15章（共15章）`、`| 第1章 | 开篇 | …`）与 `_shared.read_volume_boundaries`（仅英文 `Chapter End:`/`Chapters N-M`）零交集 → 边界集空 → `is_volume_boundary` 恒 False、`total_chapters` 永不写入（生产 novel.json total_chapters=None）、`cli.py:219` 的 `if total > 0:` 守卫跳过全部章间触发 → **pipeline 永不进入 CLOSURE**（生产 pipeline-state.json phase=chapter-loop、current_chapter=56、closure_step=0 佐证）。**注意版本偏移**：生产 pipeline-state 的 steps_done 含旧版 skill 名（shenbi-intent-management / foreshadowing-plant / pipeline-context-assemble / context-composing / foreshadowing-track / foreshadowing-recall），说明该项目由 Plan 18 重构**之前**的管线版本运行；state 级证据（step_index 等）不代表当前代码行为，**文件格式类证据仍然有效**。
2. **F325（误报）维持撤销，本轮新增两重反证**：(a) 当前 4 个 group skill 的 contract `writes:` 声明的就是旧式路径（`shenbi-review-group-factual → ['audits/chapter-N-continuity.md','audits/chapter-N-world-rules.md','audits/chapter-N-pacing.md']` 等，uv run 实证）——`_any_audit_has_findings` 扫描的 13 个旧式文件名正是磁盘上真实产物（生产 audits/ 722 文件全为旧式名，group-*.md 数量为 0）；(b) 生产审计文件中的 "BLOCKING" 全部是**否定式散文**（"未发现 BLOCKING/CRITICAL pacing 缺陷"、"不构成 BLOCKING"、"无 BLOCKING 或 CRITICAL 级别缺陷"），`_SEVERITY_BLOCKING_RE` 正确不命中（route 判定正确），而真实 BLOCKING（`严重度: BLOCKING`）会被文件扫描+route 双重命中 → revision 会执行。F305 的 P1 升级依据（"系统性假阴性"）随之崩塌（维持 P2）。
3. **F326（P1）维持成立**，机制细化：两线程均以 `uses_staging=True` 写 `staging/truth/pending_hooks.md`（lifecycle `updates` + state-settling `updates` 均含该文件，实证）；`safe_write` 对父目录有阻塞式 flock（`safe_write.py:_acquire_lock` 锁 `path.parent`），写盘被串行化——但两者都是基于**同一输入快照**的整文件替换（无读-改-写合并），串行化不改变"后写者赢、先写者 hook 变更整体丢失"的语义，且该并行路径未调用 `assert_parallelizable`（write_safety 声明的 WRITE_SHARED 串行不变量被违反）。
4. **F340（P1）/F341（P1）维持成立**：cmd_review REJECT 对全部 checkpoint 类型仅 clear_staging+clear_checkpoint（无回滚），genesis-complete reject 后 `run_genesis_step(17>=17)` 恒 True → 无 checkpoint、无进展、`cmd_next`/`cmd_resume` 恒报 OK——永久卡死（复现路径已推演）；并行 post-draft 块（chapter_loop.py:2702-2708）无条件 set_checkpoint(STATE_SETTLE)，`state_settle_review_required` 唯一消费点（:1087）在串行 `_advance`，并行块不经过 → `--auto` 每章必停（grep 实证）。
5. **本轮新漏报 1 条 P1**：并行审计波**完全绕过 G3**——6 个 audit skill（group-factual/group-character/group-craft/group-plan/review-resonance/review-sensitivity）`requires_independent()` 全部为 True，串行路径本应逐 step 跑 G3（chapter_loop.py:2907-2917），并行波（parallel_dispatch）无任何 G3 调用 → 审计评分独立性校验在正常路径静默缺失（违反 AGENTS.md "no gate can be skipped" 与 G3.4）。

---

## 一、复核结论总表（F300-F344 逐条判定）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | `"a<b".replace("<","\u003c")==原串`（Python 源码字面量求值）；dispatch_helper.py:731-735 注释声称的转义未生效 |
| F301 | **成立** | P1 维持，无异议；**影响(c)需修正** | 并行波 `chapter_loop.py:2515` 无条件触发（无开关）→ `:2607` 跳到 `_LAST_AUDIT_IDX+1` → 串行块 `:2924`（run_audit_layer）/`:2993`（resonance 解析）不可达。**修正**：影响(c) "resonance_trend.md 由 pipeline 永不写入 → escalation_bridge 数据源为空" 过强——`shenbi-review-resonance` 契约 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`（uv run 实证），共振审计 skill 自身写盘；生产 `truth/resonance_trend.md` 存在且含行（`| 55 | 推进/转折 | 18 | 12 | 23 | 17 | 70 | mid |  |`，9 列格式，与 `_build_resonance_trend_row` 的 7 列 `Ch{N}` 格式不同）。真正失效的是：cs.resonance_score 并行路径恒 None（floor 判定恒过）、boundary circle 不可达、BLOCKING 重审/escalation 环不可达、审计产物无 G4 |
| F302 | **成立** | **严重度异议：支持 review1 的 P1→P0 依据（决策表 P0 例字面列明 "TokenLedger 少计"）** | dispatch_skill(state=) 仅 chapter_loop.py:2799（串行可及路径）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli.py:123/751、audit_layer.py:170 均未传；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0。账本系统性少计 + 章节维度恒 0。成本侧车不影响正文执行，P1 亦可辩护——裁定留 stage 4 |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转（仅 linguistic-drift-check 有实现体）；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（grep 仅二进制 pyc 命中）；cmd_next/cmd_resume 仅捕 FileNotFoundError；ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93）→ crash-resume 与 escalation-resume 两个等价触发路径 |
| F305 | **成立但框定需修正（同意 review2 降 P2）** | P2 维持，无异议 | 生产实证：审计文件中 "BLOCKING" 全部为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中（collect_audit_issues 返回 blocking=False、issues=[]，实测 ch1/ch10）；route==NO_REVISION 时 `_is_revision_skipped`（:2755）在 `_should_run_step` 前短路。残余真缺口 = CRITICAL-only 路由（route=spot-fix）且文件不含字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings` 为 False → revision 被门控跳过（窄缺口，P2） |
| F306 | **成立** | P2 维持，无异议 | truth/volume_map.md 全仓 0 写入方；`Chapter {chapter}` 子串缺陷；补充实证：`_INJECT_FROM_CACHE`（dispatch_helper.py:601-621）不注入 volume_context/chapter_text → 双重失效 |
| F307 | **成立（核心）** | P2 维持，无异议；**id 子句方向需注记** | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用。**注记**：id 子句"只认 MH-/H-，与生产 P0-4 不兼容"方向半反——生产 plan section 7 行恰为 `MH-001`/`H-N01`（hook_planting 会接受），P0-* 出现在 pending_hooks body（不在 plan section 7）；真正不兼容的是 F346（truth_index 对 MH-*/H-N* 零匹配）。不影响本 finding 成立性 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state 无生产调用 |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用 |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop:2133 重复实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（:428-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后缓存不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | upsert_yaml 无生产调用；_serialize_yaml_records 只写 frontmatter 丢 body |
| F313 | **成立** | P1 维持，无异议 | `resolve_volume_path` 的 `_BOUND_N`（contracts/paths.py，`(?<=[-/])N(?=[-./]|$)`）把 `audits/chapter-N-long-span.md` 的 N（章号语义）替换为卷数 → closure step 6 G4 校验 `chapter-{卷数}-long-span.md` 恒错位；叠加 F324 下 `_current_volume` 恒 1（boundaries 空）→ 校验 `chapter-1-long-span.md` |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:333-339）无 `.result` 属性 → `_merge_step_result` 恒 no-op |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口为活跃状态） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES 无使用点；dispatch/gate 重试绑定 max_revision_retries |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（dispatch_helper.py:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551）；F318.6 三次相同引号判断（scr_extractor.py:313，生产章用 ASCII 引号故功能上恰好可用，但三次判断相同是事实）；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前两轮"窗口内无 I/O/无 save_state，风险被高估（M）" |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback 无章号绑定、注入下一章首次串行 dispatch（chapter_loop.py:2775-2780；并行块不消费） |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用；`P\d+-[a-z]+` 不匹配数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached 无调用方；路径 config/genre-config.json 与生产不符（双重死） |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 见下方专节 |
| **F325** | **不成立（误报，维持 review2 撤销）** | **P1 裁定撤回维持** | 见下方专节 |
| F326 | **成立** | P1 维持，无异议（机制细化见〇.3） | 两 skill contract updates 均含 truth/pending_hooks.md（uv run 实证）；并行 post-draft（chapter_loop.py:2405-2419）未走 assert_parallelizable/per-path read-merge 锁；safe_write 父目录 flock 串行化写盘但两者为整文件替换 → 先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision 无生产调用；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → floor 恒过） |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS 无迭代（grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest（:3048-3051 对不存在的 skill）死簇 |
| F329 | **成立** | P2 维持，无异议 | 生产 genre-config.json auditDimensions.sensitivity=true（实证）+ CHAPTER_STEPS step 14 含 shenbi-review-sensitivity → core 波与 genre 波双发同一 skill 写同一文件（两波顺序执行，1 次多余 LLM 调用/章） |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 approve 提交路径静默入库 |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict 无 token_usage 键（state.py:228-363）；token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 145 文件补零数=0（实测）；其余读章路径全不补零；且 chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict（:240-263）/from_dict（:332-343）无该键 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖（实测 `check_triggers(s,1,0).book_closure` → True） |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict（grep 无写入方）→ _get_audit_history 恒 [] → 级联跳过恒不触发 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True,0,output_text,"")（:1671，stdout=LLM JSON 文本）；consolidate 扫 stdout 行首 "BLOCKING"/"CRITICAL" 恒 0 命中（实证：真实 BLOCKING 内容在 JSON 内也不被行首扫描捕获）；生产 audit_results blocking_found=False 与文件含 "BLOCKING"（否定式）并存佐证信号失真；audit_reports 记录 group-*.md 追踪标签（磁盘不存在） |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → run_genesis_step(17>=17) 恒 True → 无 checkpoint 无进展，next/resume 恒报 OK——永久卡死 |
| F341 | **成立** | P1 维持，无异议 | chapter_loop.py:2696-2709 并行块无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；state_settle_review_required 唯一消费点 :1087（串行 _advance 的 auto-commit），并行块不经过（grep 实证） |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用 |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:24-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |

---

## 二、F324 复核专证（P0 维持）

- 解析器 `_shared.py:35-42`：`_END_RE` 仅匹配英文 "Chapter End:/End:"，`_RANGE_RE` 仅匹配 "Chapters N-M"/"ch N-M"。
- 生产格式（真实产物 + 权威 skill 模板三方一致）：`novel-output/xinghuo-ranqiong/outline/volume_map.md`（473 行，全中文）：`## 第一卷：觉醒之火（第1-15章）`、`**章节范围**: 第1章 - 第15章（共15章）`、章节节点表 `| 第1章 | 开篇 | 穿越即负债… |`。
- 验证（本轮实际运行）：
  ```
  uv run python -c "from pathlib import Path; from shenbi.pipeline._shared import read_volume_boundaries,_resolve_volume_at_runtime; pd=Path('novel-output/xinghuo-ranqiong'); print(read_volume_boundaries(pd)); print(_resolve_volume_at_runtime(pd,15))"
  # → set() / None
  uv run python -c "import re; from pathlib import Path; t=Path('novel-output/xinghuo-ranqiong/outline/volume_map.md').read_text(); print(re.findall(r'(?:章节数|Chapters?)\s*:\s*(\d+)', t)); m=re.search(r'\|\s*1\s*\|([^|]+)\|([^|]+)\|', t); print(repr(m.group(1).strip()[:20]),'|',m.group(2).strip()); print('has Cross-Volume Bridges:', '## Cross-Volume Bridges' in t)"
  # → [] / '梵天铭文金属片——未被完全解读的梵天知识' | '物品' / False
  #    （ch1 节点提取误中跨卷钩子表 | 1 | 行；生产无桥接节标题 → 桥接提取恒空）
  python3 -c "import json; d=json.load(open('novel-output/xinghuo-ranqiong/pipeline-state.json')); print(d['phase'], d['chapter_loop']['current_chapter'], d.get('closure_step'), json.load(open('novel-output/xinghuo-ranqiong/novel.json')).get('total_chapters'))"
  # → chapter-loop 56 0 None
  ```
- 影响链（全部成立）：(1) `is_volume_boundary` 恒 False → 卷边界触发全家永不执行；(2) total_chapters=None → `cli.py:219 if total > 0:` 跳过全部章间触发（含 ch%12/ch%36）→ 无 book_closure → pipeline 永不进入 CLOSURE（56 章实证）；(3) `_resolve_volume_at_runtime` → None → 卷名 "Unknown"/卷上下文空/volume_context 空（叠加 F306）；(4) 章节节点提取 `\|\s*{chapter}\s*\|` 对生产 `| 第N章 |` 不匹配，且第 1 章误中 `| 1 | 梵天铭文… | 物品 |` 行（数据污染实证）。
- 版本偏移注记：生产 pipeline-state 的 steps_done 为旧版 skill 名（Plan 18 重构前），故 state 级细节不可作当前代码行为证据；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据。

---

## 三、F325 反证复核（误报维持）

- F325 原判："并行波实际写入 `chapter-N-group-*.md` → `_any_audit_has_findings` 扫描的旧式文件名生产永不产生 → 恒 False → revision 被跳过"。
- **反证（本轮全部实测）**：
  1. 当前 4 个 group skill 契约 writes 声明旧式路径（uv run load_contract 实证）：group-factual → `audits/chapter-N-{continuity,world-rules,pacing}.md`；group-character → `{character,dialogue,motivation,pov}`；group-craft → `{texture,reader-pull,anti-ai}`；group-plan → `{memo-compliance,foreshadowing}`；resonance → `audits/chapter-N-resonance.md`；sensitivity → `audits/chapter-N-sensitivity.md`。`_build_skill_prompt` 的 "Files to create" 清单与 `_write_parsed_outputs` 落盘均按契约路径 → **13 个旧式文件名恰是真实落盘名**（生产 audits/ 722 文件全为旧式名，group-*.md 数=0）。
  2. `ReviewTask.output_path`（group-*.md）只是追踪标签（chapter_loop.py:2557），从不传进 dispatch_skill。
  3. 真实 BLOCKING 判定链：`collect_audit_issues`（revision_router.py:199-206）`_SEVERITY_BLOCKING_RE` 命中真实 `严重度: BLOCKING` → route≠NO_REVISION → `_is_revision_skipped` False → `_should_run_step`→`_any_audit_has_findings` 对同一文件必 True → **revision 执行**。
- **残余真值（P2 级，非 F325 原判）**：(a) `_any_audit_has_findings` 清单缺 `chapter-N-resonance.md`——仅 resonance 文件含 BLOCKING 时会被门控跳过；(b) CRITICAL-only 路由（route=spot-fix）且文件无字面 "BLOCKING"/"FAIL" → 被门控跳过（F305 残余，本轮实测生产否定式 "BLOCKING" 使 _any_audit_has_findings 反而常为 True，反向错误影响小）；(c) 双重门控互为冗余实现。
- 连带：F305 的 P1 升级依据不成立（维持 P2）；F316 从"潜伏"恢复为"活跃"。

---

## 四、新发现（F345-F352）

### F345 | 并行审计波完全绕过 G3（scoring independence）——6 个 audit skill 均 requires_independent=True | 契约违反 | P1
- 证据：`chapter_loop.py:2515-2630`（并行波：build context → dispatch_reviews_parallel → consolidate → 路由，无 G3）；`parallel_dispatch.py:60-128`（_dispatch_with_retry 仅 dispatch_skill，无 run_gate_g3）；对照串行路径 `chapter_loop.py:2907-2917`（`if requires_independent(step.skill): run_gate_g3(...)`，review-resonance 曾按 docstring 显式 G3）；`dispatch_helper.py:342-350`（requires_independent）；AGENTS.md G3.4（"Scoring MUST use an independent subagent"）。
- 根因：并行重构（Plan 18 Task 7）把 6 个 audit step 移入线程波时未迁移串行路径的 G3 校验；`requires_independent_agent` 对这些 skill 均返回 True 使串行 G3 分支形同虚设。
- 验证：
  ```
  uv run python -c "from shenbi.pipeline.dispatch_helper import requires_independent; [print(s, requires_independent(s)) for s in ['shenbi-review-group-factual','shenbi-review-group-character','shenbi-review-group-craft','shenbi-review-group-plan','shenbi-review-resonance','shenbi-review-sensitivity']]"
  # → 全部 True
  grep -n "run_gate_g3\|G3" src/shenbi/pipeline/parallel_dispatch.py   # → 无命中
  ```
- 影响：审计/评分独立性从未在正常路径校验（gate 被静默跳过，违反 "no gate can be skipped"）；与 F301 的 "审计产物无 G4" 同族，但 G3 缺失是并行波自身的结构缺陷（F301 修复后仍存在）。
- 建议方向：并行波在 consolidate 后对每个 requires_independent 的 audit skill 调用 `run_gate_g3`（或按 G3 语义仅对 review-resonance/score 类执行）；补并行路径 G3 集成测试。

### F346 | truth_index._HOOK_ID_RE 不匹配生产带连字符 hook id（MH-001/H-N01）→ master hooks 从 Route A 检索静默缺失 | error | P2
- 证据：`truth_index.py:32`（`_HOOK_ID_RE = re.compile(r"(?:[HM]\d+|P\d*-\d+)")`）；生产 `plans/chapter-5-plan.md:67-71`（`| MH-001 | advance | … | 0 |`、`| H-N01 | advance | … |`）；`extract_entities_from_plan`（truth_index.py:315-332）以该正则从 plan 提取 hook。
- 根因：正则仅支持无连字符旧式（H01/MH02）与 P 连字符式（P0-4），遗漏生产 plan 实际使用的 MH-N/H-N 连字符格式；且 `[HM]\d+` 对 "MH02" 会误截为 "H02"（部分 id 损坏）。
- 验证：
  ```
  python3 -c "import re; r=re.compile(r'(?:[HM]\d+|P\d*-\d+)'); [print(h, r.findall(h)) for h in ['MH-001','H-N01','P0-4','H01','MH02']]"
  # → MH-001 [] / H-N01 [] / P0-4 ['P0-4'] / H01 ['H01'] / MH02 ['H02']
  ```
- 影响：计划中引用的 MH-* master hooks 不会被索引/检索 → Route A（P1 direct involvement，权重 1.0）的 hook 命中恒缺 master 系列；与 F307 的 hook_planting 侧（接受 MH-/H-）方向相反，形成"写入侧认、检索侧不认"的断裂。
- 建议方向：`_HOOK_ID_RE` 增加 `(?:MH|H)-?\d+`（与 `P\d*-\d+` 并列），并补含生产 MH-001/H-N01 的测试。

### F347 | 生产 hook 数据格式与上下文"伏笔债务简报"解析全错位：简报恒"(无)" | error | P2
- 证据：`context_curation.py:361-384`（`_read_pending_hooks` 只读 YAML frontmatter `hooks:` 列表，body 表格格式零解析）+ `:387-435`（`_read_spine_master_hooks` 需 `hook_master_list`/`master_hooks` 键或 `| MH` 行）；生产 `truth/pending_hooks.md` frontmatter keys=`[title, project, version, last_updated, type, category, status, filled_by, last_chapter, track_chapter]`（无 `hooks` 键，hook 以 body markdown 表格 `| P0-4 (quiet阈结构) | RELEVANT→TRIGGERED … |` 呈现）；生产 `truth/book_spine.md` 无 `hook_master_list`/`master_hooks` 键、body 无 `| MH` 行。
- 根因：生产/生命周期 skill 约定（body 表格）与解析器假设（frontmatter YAML 列表）断裂；`_build_hook_debt_briefing` 的 MH*/H* 两 tier 全部取空。
- 验证：
  ```
  python3 -c "import yaml; t=open('novel-output/xinghuo-ranqiong/truth/pending_hooks.md').read(); p=t.split('---',2); fm=yaml.safe_load(p[1]); print(list(fm.keys()), 'has hooks:', 'hooks' in fm)"
  # → ['title','project','version','last_updated','type','category','status','filled_by','last_chapter','track_chapter'] has hooks: False
  grep -c "^| MH" novel-output/xinghuo-ranqiong/truth/book_spine.md   # → 0
  ```
- 影响：curate_context 的 9 节文档第 9 节（Hook 债务简报）在生产恒输出 "(无)"，即使存在活跃伏笔（P0-* 全在 body）；计划阶段 LLM 看不到伏笔债务信息（context_curation 由 chapter_loop.py:2712-2715 在 context-prepare 接线）。
- 建议方向：`_read_pending_hooks` 增加 body 表格兜底（复用 truth_index 的 body 扫描）；`_read_spine_master_hooks` 增加 `MH-\d+` 行扫描；补以生产 pending_hooks.md 为 fixture 的测试。

### F348 | SharedAuditContext 缓存注入与审计契约 reads 错位——缓存对预期目标近乎死线 | optimization | P2
- 证据：`dispatch_helper.py:601-621`（注入仅在 `if cached and fname not in raw_inputs` 时生效）；审计契约 reads（uv run 实证）：group-character 读 `truth/character_matrix.md`、group-craft/plan 读 `truth/pending_hooks.md`、review-resonance 读 `style/style_profile.md` → 这些文件在磁盘读路径已入 raw_inputs → 缓存注入被跳过；group-factual 读 `world/rules.md`（非 `truth/world_rules.md`）→ 缓存注入的 `truth/world_rules.md`（生产不存在，恒空）与 3000 字截断 character_matrix 摘要成为 group-factual 未声明的额外上下文。
- 根因："仅补缺"注入策略与契约 reads 集合重叠，预期收益（6 个审计器共享同一批 I/O）从未实现；`build_shared_audit_context` 的 chapter_text（F335 下恒空）与 volume_context（F306 路径错误）不在注入表。
- 验证：`uv run python -c "…load_contract('shenbi-review-group-factual').get('reads')"` → `['chapters/chapter-N.md','truth/current_state.md','truth/chapter_summaries.md','world/rules.md','world/power_system.md','world/locations.md','world/story_bible.md','genre-config.json']`（不含 truth/world_rules.md）。
- 影响：每章仍由各审计器全量读盘（缓存是纯开销）；唯一副作用是给 group-factual 注入未声明、截断的 character_matrix 摘要（上下文污染风险）。
- 建议方向：缓存注入改为"命中即替换磁盘读"（统一 key 后以缓存值为准）或删除 shared_context 机制并让各审计器按契约读盘；修复 chapter_text 补零（F335）后再启用。

### F349 | MODIFY 派生的 truth re-dispatch 无重试预算/无升级：持续失败时每次 step 迭代重复重发 | error | P2
- 证据：`cli.py:90-131`（`_execute_pending_re_dispatches`：失败时 `remaining.append(entry)` 保留，无 attempt 计数、无上限、无 escalation）+ `cli.py:200`（`_orchestrate_to_checkpoint` while 循环顶部每次迭代执行）。
- 根因：re-dispatch 语义只实现"失败保留重试"，未接 retry 预算/升级路径；`_orchestrate_to_checkpoint` 每推进一个 step 都重跑一次。
- 验证：`grep -n "pending_re_dispatches" src/shenbi/pipeline/cli.py` → 78/80/90/95/101/130/200（:200 为循环顶部调用点）。
- 影响：chapter-memo/state-settle MODIFY 后若派生 skill（pacing-design / relationship-map / foreshadowing-resolve）持续失败，每次 cmd_next/resume 的每个 step 迭代都重复 1 次 LLM dispatch（token 放大、日志噪音），失败永不升级为 ESCALATION checkpoint；与 F317（重试绑定）不同源（此处无任何重试绑定）。
- 建议方向：为 re-dispatch 引入 attempt 计数（复用 max_revision_retries 或独立上限），耗尽后转 ESCALATION checkpoint 并保留 entry 供人工处理。

### F350 | 每章无条件创建 chapter-N-pre-rev.md 备份（即使 route=no-revision 无 revision 发生） | optimization | M
- 证据：`chapter_loop.py:1875`（`_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用，早于路由判定）；`:2621`（并行波总是调用 `_route_revision_after_resonance`）。
- 根因：备份时机放在"路由前"而非"确认要 revision 后"。
- 影响：每章多一份整章副本（100 章多 100 个 `chapter-N-pre-rev.md` 文件）；对 no-revision 章节是纯冗余（唯一回滚安全网已被 F303 覆盖，此处是次级浪费）。
- 建议方向：`if route != RevisionRoute.NO_REVISION: _create_pre_revision_backup(...)`（路由后备份）。

### F351 | API JSON 模式未传 response_format，依赖提示词合规 + 回退解析（非合规输出先失败一次再重试） | error | M
- 证据：`dispatch_helper.py:675-696`（json_mode 提示词要求"输出 ONLY JSON"）；`:1536-1547`（`_call_llm_streaming_with_retry(client, model, messages, temperature=…, max_tokens=…, timeout=…)`——无 `response_format={"type":"json_object"}`）；`_parse_structured_output`（:877-886）失败后回退 `_parse_file_markers`（:889-900）对无 `### FILE:` 的 JSON 返回空 files → `_write_parsed_outputs` written=[] → "No output files written" → DispatchResult(False) → 上层重试。
- 根因：json_mode 仅存在于提示词层，API 层未强制结构化输出。
- 影响：模型输出 markdown 包裹 JSON 时浪费 1 次调用+1 次重试（功能靠重试兜底，无数据损坏）；流式路径 `response_format` 亦未传。
- 建议方向：`_call_llm_streaming_with_retry` 在 json_mode 时传 `response_format={"type": "json_object"}`（非流式）或流式下先剥离 ```json 围栏再解析。

### F352 | dispatch_skill 的 timeout 形参死代码（三条路径均用 _compute_dispatch_timeout） | optimization | M
- 证据：`dispatch_helper.py:1797`（`timeout: int = 900` 形参）；函数体内无 `timeout` 引用（三条路径分别调 `_compute_dispatch_timeout`：:1527/:1731/:1855）。
- 根因：Task 14 动态超时落地后未删除旧形参。
- 影响：API 表面误导（调用方以为可调超时，实际不可调）；docstring 亦未说明。
- 建议方向：删除形参或改为 `timeout: int | None = None` 透传给 `_compute_dispatch_timeout`。

---

## 五、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件全量 deep-read，共 13,641 行）。
- 本轮验证资产：真实生产项目 `novel-output/xinghuo-ranqiong/`（outline/volume_map.md 473 行中文格式；audits/ 722 文件全旧式名、group-*.md 数=0；chapters/ 145 文件不补零；plans/chapter-5-plan.md section 7 为 `MH-001`/`H-N01` 格式；truth/pending_hooks.md frontmatter 无 hooks 键（body 表格）；truth/book_spine.md 无 hook_master_list；truth/resonance_trend.md 9 列格式含 ch55 行；pipeline-state.json 与 novel.json 佐证 F324）。
- 前几轮最大覆盖空洞（本轮已补）：(1) **G3 在并行波的缺失**（前两轮均未验证 requires_independent × 并行 dispatch 的交集）；(2) **hook id 双格式断裂**（truth_index 检索侧 MH-*/H-N* 零匹配，与 F307 的写入侧方向相反）；(3) **hook 债务简报数据源格式错位**（生产 pending_hooks/spine 格式 vs frontmatter 解析）；(4) **SharedAuditContext 注入"仅补缺"策略**（契约 reads 已覆盖缓存键 → 缓存失效）；(5) **MODIFY re-dispatch 无重试预算**。
- 剩余覆盖洞：(a) 生产 pipeline-state.json 为旧版管线产物（steps_done 为旧 skill 名），state 级行为证据不可直接外推当前代码（文件格式证据不受影响）；(b) IDE CLI 路径（codex/zcode）本环境不可执行，仅静态分析；(c) 测试集未全量重跑（前两轮 collect 994）；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内），其中 group skill 契约的旧式 writes 是 F325 反证的关键。

---

## 六、汇总

- **新 finding 数**：8（F345-F352，其中 F345=P1，F346-F349=P2，F350-F352=M）
- **误报数**：0（F300-F344 全部成立/维持；F325 维持 review2 的误报撤销，本轮未发现新的误报）
- **严重度异议数**：1（F302：支持 review1 的 P1→P0 异议，依据决策表 P0 例字面列明 "TokenLedger 少计"；裁定留 stage 4）
- **事实修正（不改严重度）**：2（F301 影响(c) "resonance_trend.md 由 pipeline 永不写入" 过强——resonance skill 契约自身更新该文件且生产存在 9 列格式产物；F307 id 子句方向半反——生产 plan section 7 恰为 MH-*/H-N*，真正检索侧断裂见 F346）
- **0 新 finding 判定**：**否** —— 本轮发现 1 条 P1 漏报（F345 并行审计波 G3 缺失）与 4 条 P2 漏报（F346 检索侧 hook id 断裂、F347 债务简报恒空、F348 审计上下文缓存失效、F349 re-dispatch 无预算）及 3 条 M；F324（P0）维持成立、F325（误报）维持撤销、F340/F341（P1）维持成立。
- **校准升级提示**：F345（G3 静默跳过，正常路径 gate 契约违反）建议进入阶段 4 复核优先级；F324 维持首位；F346/F347 建议与 F307/F322 的 hook 生态修复合并处理。
