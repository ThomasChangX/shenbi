# Z3 分区第五轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第五轮独立复核（本文件 `Z3.review5.md`，与初审 `Z3.a.md`、一轮 `Z3.review.md`、二轮 `Z3.review2.md`、三轮 `Z3.review3.md`、四轮 `Z3.review4.md` 隔离）
> 复核范围：34/34 文件全量重读（13,641 行），逐条独立验证 F300-F363；以真实生产项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行、audits 722 文件、chapters 145 文件、plans、truth、pipeline-state.json、novel.json、staging 119 文件）为格式/行为对照基准
> 本轮发现编号：F364-F370（7 条新发现，其中 **1 条 P1（贴 P0）**、2 条 P2、4 条 M）；**新增误报 0 条**；严重度异议 0 条（立场声明见结论表）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证仅在 /tmp 下做临时目录实验）
> 方法：34 文件逐行重读 + grep/uv run python 实证 + 真实产物对照 + **/tmp 沙箱进程退出行为实验**（本轮唯一新验证手段，见 F364）

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`（ch15 是第 1 卷末章）。生产 volume_map 全中文（`**章节范围**: 第1章 - 第15章（共15章）`、`| 第1章 | 开篇 | …`），`_END_RE`/`_RANGE_RE`/`章节数` 三个正则对其均 0 命中（本轮实测）；`_extract_chapter_node` 裸数字 `\|\s*1\s*\|` 误中跨卷钩子表 `| 1 | 梵天铭文金属片… | 物品 |`（本轮实测）。novel.json 无 total_chapters（56 章仍在 chapter-loop、closure_step=0）。
2. **F325（误报）维持撤销**：本轮实测 production audits/ 后缀分布——13 类全为旧式名（continuity/character/world-rules/pacing/dialogue/motivation/pov/memo-compliance/foreshadowing/anti-ai/sensitivity/resonance/review-summary），`group-*` 数量 = 0；4 个 group skill 契约 `writes:` 即旧式路径（load_contract 实证）；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING（`严重度: BLOCKING`）必被 route 与文件扫描双重命中 → revision 执行。
3. **本轮最重要新发现（F364，P1 贴 P0）**：`crash_recovery._emergency_cleanup` 被 `atexit.register` 无条件挂接（crash_recovery.py:66），**在每次正常进程退出时无差别执行 `clear_staging`**（crash_recovery.py:144-148）——即 `pipeline next`/`resume` 到达 CHAPTER_MEMO/STATE_SETTLE 检查点并正常退出后，**staging/ 中的待审产物被整体删除**；随后 `review approve` 的 `_commit_staging_for_checkpoint` 因 staging 文件缺失而静默 no-op（cli.py:348-353 捕 FileNotFoundError 后 pass），**人审通过的 plan/truth 永不被提交**，resume 后重新 dispatch 同一步骤——交互模式下 pipeline 在 chapter-planning 处无限循环（每轮 approve 都重新生成一份从未被提交的新 plan），auto 模式下 state-settle 的 truth 更新永不落盘。已用 /tmp 沙箱实测（见 F364 验证节）。
4. **F341（P1）维持成立，且本轮补强**：并行块无条件 set_checkpoint(STATE_SETTLE)（:2702-2708）；进一步实证——**CHAPTER_STEPS 中没有任何 step 声明 `checkpoint=CheckpointType.STATE_SETTLE`**（唯一 checkpoint 声明是 chapter-planning 的 CHAPTER_MEMO，:148），因此 `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）本身即死代码，`state_settle_review_required` 配置**在任何路径上都没有功能效果**（不只是"并行块不经过它"）。
5. **F353（P1）/F354（P1）/F345（P1）/F340（P1）/F326（P1）全部维持成立**（逐条独立实证见结论表）。
6. **版本偏移注记（沿用三/四轮）**：生产 pipeline-state 的 steps_done 为 Plan 18 重构前的旧 skill 名（shenbi-context-composing / foreshadowing-plant 等），state 级行为证据不可外推当前代码；**staging/ 目录 119 个文件全部来自旧版管线遗留**（旧版无退出清理，恰好反衬当前代码 atexit 清理行为的破坏性——见 F364）；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据。

---

## 一、复核结论总表（F300-F363 逐条判定）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")`——源码字面量求值后为 `replace("<","<")` no-op；注释宣称的标签注入缓解未生效 |
| F301 | **成立** | P1 维持（贴 P0 校准维持） | 并行波 :2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1` → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析）不可达（本轮逐行确认无其他调用点）。三轮修正成立：resonance_trend.md 由 shenbi-review-resonance 契约 updates 自身写入（load_contract 实证）且生产存在 9 列格式（cells[6]=overall，与 escalation_bridge 读取一致）；真正失效的是 cs.resonance_score 并行路径恒 None、boundary circle 不可达、BLOCKING 重审环不可达、审计产物无 G4/G3 |
| F302 | **成立** | P1 维持（P0 依据存在，裁定留 stage 4，无异议） | dispatch_skill(state=) 仅 chapter_loop.py:2799（串行可及）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751、audit_layer:170、revision_router:160 均未传；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0（PipelineState 无 chapter 属性）。账本系统性少计 + 章节维度恒 0 |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（`_snapshot_chapter_files` 死函数内）；restore_from_snapshot 无 src 调用方（本轮 grep 仅 snapshot_diff.py + test）；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转（仅 linguistic-drift-check 有实现体）；cmd_rollback not_implemented（cli.py:823-843）；`_get_snapshot_retention` 硬编码 50（:1371-1376） |
| F304 | **成立** | P1 维持，无异议；补第三条触发路径 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882），**src/ 内 0 个 except**（本轮 grep 实证）；cmd_next/cmd_resume 仅捕 FileNotFoundError。触发路径：(a) crash-resume 后 retry_budget_consumed 持久化而 retry_counts 清空；(b) ESCALATION 解决只清 retry_counts 不清 budget（machine.py:85-93）；(c) **本轮新增**：G4 hard-fail 路径（:2879-2885）在 `consumed > max_audit_retries` 时直接 raise，而该路径在 --auto 模式下 count<=1 重试、二次失败后"continue regardless"（:2892-2904），budget 仍持续累计——多次 auto 放行后同一 chapter 的后续 hard-fail 也可能触发裸崩 |
| F305 | **成立但框定需修正（同意二/三/四轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；route==NO_REVISION 时 `_is_revision_skipped`（:2755）先于 `_should_run_step` 短路。残余真缺口 = CRITICAL-only 路由（route≠NO_REVISION）且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过；四轮补充的 revision_count 记账漂移（:1886 先 +1 而 revision 未执行）成立 |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方（grep 实证）；:97 `f"第{chapter}章" in line / f"Chapter {chapter}" in line` 子串缺陷；volume_context 无消费方（dispatch_helper._INJECT_FROM_CACHE 只注入 4 字段，:601-621） |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用。**注记（三轮）**：`_parse_hook_entries`（hook_planting.py:144）的 `^(MH|H)-` 对生产 plan section 7 的 `MH-001`/`H-N01` 行**会接受**（本轮实测 `| MH-001 | advance | … | 0 |` 首格匹配），但操作 `advance` 非 plant/open 仍被跳过（:151）；真正的检索侧断裂见 F346 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state 无生产调用（grep 实证） |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（grep 实证，注释"called from cli.py on resume"过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 重复实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:431-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方 |
| F313 | **成立** | P1 维持，无异议 | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（shenbi-review-long-span 契约 writes 实证 `audits/chapter-N-long-span.md`）；F324 下 `_current_volume` 恒 1 → G4 校验 `chapter-1-long-span.md` 恒错位 |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:333-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op；chapter_loop.py:2425-2426 调用无效果 |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重；consolidate 只提行不去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（dispatch_helper.py:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551，core_skills 实为 6 个）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback 无章号绑定、注入下一次串行 dispatch（:2775-2780）。**同源缺口见 F365（STATE_SETTLE MODIFY 不重设 checkpoint）** |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用（grep 实证）；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached 无调用方；路径 config/genre-config.json 与生产不符（双重死） |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`；`_END_RE`/`_RANGE_RE`/`章节数` 全 0 命中；`| 1 |` 裸数字节点提取误中跨卷钩子表。影响链（卷边界触发全家 / total_chapters=None → cli.py:219 `if total > 0:` 跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 桥接提取恒空）全部成立 |
| **F325** | **不成立（误报，维持二/三/四轮撤销）** | **P1 裁定撤回维持** | 本轮实测 audits/ 13 类后缀全旧式名、group-* 数=0；group skill 契约 writes 即旧式路径；真实 BLOCKING 双重命中 → revision 执行。残余真值（P2）：`_any_audit_has_findings` 清单缺 resonance/review-summary（清单 13 名中有 texture/reader-pull 生产不产生），仅 resonance 文件含 BLOCKING 时门控可跳过 |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证）；并行 post-draft（:2405-2419）两线程均以 uses_staging 写 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）。四轮补充成立：`_parse_resonance_score`（:1285-1321）三模式均不匹配生产 `**结果**: 通过 (77/100)` 格式 |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议 | core_skills（:2551，含 review-resonance + review-sensitivity）与 genre 波（genre-config auditDimensions.sensitivity=true 实证 → get_active_genre_audits 含 shenbi-review-sensitivity）双发同一 skill 写同一文件 `audits/chapter-N-sensitivity.md` |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库 |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless）；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 145 文件补零数=0（本轮实测）；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS（本轮实测）→ 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；review-summary.md 恒报 0 问题 |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死 |
| F341 | **成立（本轮补强）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）。**补强事实**：CHAPTER_STEPS 中唯一声明的 checkpoint 是 chapter-planning 的 CHAPTER_MEMO（:148）——**没有任何 step 声明 STATE_SETTLE checkpoint**，故 `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 配置在全部路径上均无功能效果（不止"并行块不经过它"） |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用（grep 实证） |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:24-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；本轮实测 6 个 audit skill 全部 requires_independent=True；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 `MH-001`/`H-N01` 零匹配（本轮实测 `MH-001 → []`、`H-N01 → []`、`MH02 → ['H02']` 误截）→ master hooks 从 Route A 检索静默缺失 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys=`[title,project,version,last_updated,type,category,status,filled_by,last_chapter,track_chapter]`（无 `hooks` 键，本轮实测），hook 以 body 表格呈现；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)" |
| F348 | **成立但机制描述部分不准（维持四轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入、磁盘读仍全量发生（成立）；**style_profile/world_rules 注入键为幽灵路径**（truth/style_profile.md、truth/world_rules.md——生产均不存在（本轮实测两者均 MISSING）且无契约读取）→ 这两项注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层；非合规输出先失败一次再重试 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout |
| F353 | **成立** | P1 维持，无异议 | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 updates 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进至 `_LAST_AUDIT_IDX+1`；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；consolidate 对失败结果 continue（:210-221）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；grep skills/ 契约 reads 0 个引用该文件；drafting 契约 reads `context/chapter-N-context.md`（本轮 load_contract 实证）→ 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘" 与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（生产不存在，本轮实测 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断）每章每审计恒注入（幽灵输入 + token 浪费） |
| F358 | **成立** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError（:2034 raise，docstring "Pipeline paused for human review"）；drift-correction/warning/opening-variation 指令文件无任何消费者（grep 实证） |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra（hook content/state、rule content）被丢弃；drafting 契约 reads 不含 characters/*.md、world/rules.md → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存，无写盘副作用）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358（CLI main）→ 每 15 章"重建"只产日志 |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |

**误报统计**：F300-F363 中仅 F325 为误报（二轮撤销、三/四轮维持、本轮独立复证维持）。F348 为"成立但机制描述部分不准"（非误报）。

**严重度异议**：0 条新增。立场声明：F302（P1 维持，P0 依据存在，留 stage 4）、F305（同意 P2）、F301（同意贴 P0 校准）、F324（P0）、F340/F341/F345/F353/F354（P1）均无新异议；**F364（本轮新发现）按决策表可论 P0（数据丢失 + 契约被静默违反），裁定 P1 留 stage 4**。

---

## 二、新发现（F364-F370）

### F364 | atexit 紧急清理在每次正常进程退出时无条件清空 staging/——人审通过的 plan/truth 永不被提交，pipeline 卡死于 chapter-planning 循环 | error | **P1**（按决策表可论 P0，留 stage 4）

- 证据：
  - `crash_recovery.py:66`（`atexit.register(_emergency_cleanup)`，注册时机为 chapter_loop.py:2510 `if step_idx == 0: register_emergency_handlers(...)`——每章第一步即注册）；
  - `crash_recovery.py:92-151`（`_emergency_cleanup` 无"是否紧急"判断：步骤 4 无条件 `clear_staging(project_dir)`，:144-148）；
  - 全仓无 `atexit.unregister`（grep 实证）；
  - 消费链：`pipeline next`（cli.py:588-618）→ `_orchestrate_to_checkpoint` → 到达 CHAPTER_MEMO/STATE_SETTLE 检查点（staging/ 含待审产物）→ save_state → **进程正常退出 → atexit → staging/ 被 rmtree**；
  - `review approve`（cli.py:526-527 + checkpoint.py:44-46）：commit_staging 因 staging 文件缺失 raise FileNotFoundError → cli.py:348-353 捕获后 pass（注释 "Expected: file may have been cleared since the checkpoint was raised"）→ **静默 no-op**；
  - `resume`（cli.py:783）→ step_index 仍为检查点前 → 重新 dispatch 同一步骤（chapter-planning）→ 新 checkpoint → 退出 → 再清空 → 无限循环。
- 根因：注册将紧急清理与正常退出混为一谈——`_emergency_cleanup` 本应只在 `_check_emergency_flag`（信号触发）后执行，但 atexit 注册使其在**每次正常退出**（CLI 一次性进程模型，cli.py docstring 明示 next/review/resume 为独立命令）都跑；且清理内部无 `_emergency_flag` 判断。
- 验证（/tmp 沙箱实验，实际运行）：
  ```
  cd /tmp && mkdir -p dsh-test-staging/staging/plans && echo "test plan" > dsh-test-staging/staging/plans/chapter-1-plan.md
  cd <repo> && uv run python -c "
  from pathlib import Path
  from shenbi.pipeline.crash_recovery import register_emergency_handlers
  from shenbi.pipeline.state import PipelineState
  pd = Path('/tmp/dsh-test-staging'); st = PipelineState.default(str(pd))
  st.chapter_loop.current_chapter = 1
  register_emergency_handlers(pd, st)
  print('staging before exit:', (pd/'staging/plans/chapter-1-plan.md').exists())"
  # → staging before exit: True
  # → 2026-08-14 ... emergency_cleanup_started / pipeline_state_saved /
  #   emergency_snapshot_created / staging_cleared staging_dir=/tmp/dsh-test-staging/staging
  # → staging after exit: staging dir GONE（无任何信号、无异常，纯正常退出即被清空）
  ```
  生产对照：`novel-output/xinghuo-ranqiong/staging/` 现存 119 个旧版遗留文件（chapter-6~56 的 plan/decisions），证明旧版管线无此清理、staging 内容在检查点周期内本应存活。
- 影响：交互模式下**每轮 approve→resume 都重新生成一份从未被提交的新 plan**（人审内容静默作废，token 反复浪费，pipeline 无法越过 chapter-planning）；auto 模式下（F341 的 STATE_SETTLE 检查点）state-settle 的 truth 更新永不落盘（下游审计读陈旧 truth）；每次运行还附带多余 save_state 与 `snapshots/chapter-N-emergency.md` 快照噪音。属正常路径可复现 + 数据丢失 + 契约静默违反。
- 建议方向：`_emergency_cleanup` 增加门控（仅 `_emergency_flag` 置位或显式 shutdown 请求时清理 staging；正常退出只保留 save_state 与 snapshot），或在 `register_emergency_handlers` 中区分注册时机；补一条"正常退出后 staging 存活"的集成测试（当前 test_crash_recovery 只测信号路径）。

### F365 | STATE_SETTLE MODIFY 重跑不重设 checkpoint：重跑结果悬空不提交、重审门被静默跳过（`_advance` 的 STATE_SETTLE 分支本身即死代码） | error | P2

- 证据：
  - cli.py:545-546（MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`）；CHAPTER_STEPS[7] = shenbi-state-settling（chapter_loop.py:193-200，**checkpoint=None**——STATE_SETTLE 检查点只在并行块 :2702-2708 手动设置）；
  - resume 后 step_idx==7 走**串行** dispatch 路径（:2794-2800，不经过并行块）→ 写 staging/truth/*.md → G4 → `_advance(7)`（:3053）→ step.checkpoint 为 None → 不设任何 checkpoint → step_index=8 → 直接进入审计波；
  - 由此：重跑产出的 staging 文件**无人提交**（`_commit_staging_for_checkpoint` 只由 review 触发，而此刻无 checkpoint），truth 保持 pre-MODIFY 版本；且 MODIFY 用户本应复审的"反馈落地结果"门被静默跳过（对比 CHAPTER_MEMO MODIFY：step 1 的 checkpoint=CHAPTER_MEMO，`_advance` :1070-1119 会重新拉起检查点，语义自洽）。
  - 关联事实：CHAPTER_STEPS 中唯一 checkpoint 声明是 :148 的 CHAPTER_MEMO（grep `checkpoint=CheckpointType` 实证）→ `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码 → `state_settle_review_required` 全路径失效（补强 F341）。
- 根因：STATE_SETTLE 检查点由并行块"内联"而非走 step.checkpoint 机制，MODIFY 回滚语义未同步迁移。
- 验证：`grep -n "checkpoint=CheckpointType" src/shenbi/pipeline/chapter_loop.py` → 仅 :148（CHAPTER_MEMO）；read cli.py:541-554 与 chapter_loop.py:2696-2709、3053。
- 影响：STATE_SETTLE 被 MODIFY 后，反馈被注入重跑 prompt（:2775-2780 消费 modify_feedback），但重跑结果不提交、不复审；与 F364 叠加时该结果随后被 atexit 清空。
- 建议方向：MODIFY 回滚 STATE_SETTLE 后，重跑完成需按并行块同款逻辑重新 set_checkpoint(STATE_SETTLE)（或把 state-settling 的 checkpoint 声明移入 ChapterStep 统一由 `_advance` 管理，让 :1086-1105 分支复活并消费 state_settle_review_required）。

### F366 | 5 个 PipelineConfig 死旋钮零消费者：genesis_review_required / volume_boundary_review_required / style_learning_interval / context_budget_override / snapshot_retention_chapters | optimization | P2

- 证据：`state.py:59-73`（PipelineConfig 定义）；grep 全 src/：
  - `genesis_review_required`、`volume_boundary_review_required`、`style_learning_interval`、`context_budget_override` → **0 个消费点**；
  - `snapshot_retention_chapters` → 仅 chapter_loop.py:1374 docstring（`_get_snapshot_retention` 硬编码 50，F318.5）。
- 根因：配置面先行落地、消费侧未接线（与 F317 死常量、F308 死代码同族）。
- 验证：`grep -rn "\.genesis_review_required\|\.volume_boundary_review_required\|\.style_learning_interval\|\.context_budget_override" src/ --include="*.py"` → 无命中（state.py 定义与序列化除外）。
- 影响：用户设置这些旋钮**静默无效**（触发周期恒 12/36（triggers.py:67-70 硬编码 STYLE_INTERVAL/STRATUM_INTERVAL）、VOLUME_BOUNDARY 检查点恒设（triggers.py:630-642）、GENESIS_COMPLETE 检查点恒设（genesis.py:190-195）、token 预算恒取 BUDGET_BY_ROLE（context_assemble.py:40-44））；配置 API 表面误导。
- 建议方向：接线或删除并同步 to_dict/from_dict；至少将 STYLE_INTERVAL 改读 `config.style_learning_interval`。

### F367 | step 2 chapter-planning 的 context assembly 恒读不存在的当章 plan → 每章恒失败走 minimal fallback + 一次无效 curation | error | M

- 证据：`chapter_loop.py:2712-2715`（`if step.calls_context_assembly:` → `_run_context_assembly` + `_run_context_curation`）；CHAPTER_STEPS[1]（chapter-planning）calls_context_assembly=True（:143-152）；`_run_context_assembly`（:1126-1159）→ `assemble_context(project_dir, "plans/chapter-N-plan.md")`（context_assemble.py:302 直接 read_text）——**step 2 时当章 plan 尚不存在**（由本步 dispatch 创建于 staging，plan 终稿在 CHAPTER_MEMO approve 后才落盘）→ FileNotFoundError → 被 :1153-1154 捕获 → `_write_minimal_context_fallback`（:1162-1197）。
- 根因：step 2 的 calls_context_assembly 语义错位（组装需要 plan，而 plan 是 step 2 的输出）；仅 MODIFY 后（plan 已提交）该步才真正组装成功。
- 验证：read context_assemble.py:294-305（assemble_context 对缺失 plan 抛 FileNotFoundError）与 chapter_loop.py:1126-1159；chapter-planning 契约 reads（load_contract 实证）为 7 个 truth/outline 文件、**不含 context/chapter-N-context.md** → 即使组装成功 plan 也不消费它，step 3（context-prepare）的组装才是 drafting 真正消费的产物（step 3 会覆写同一 context 文件）。
- 影响：每章产生 1 条 `context_assembly_failed` warning 噪音 + 1 次对 fallback 内容的无效 curation（P1-P7 除 plan 外全 "(未产出)"，随后被 step 3 覆写）；行为无功能破坏。
- 建议方向：step 2 移除 calls_context_assembly（或改为在 dispatch 后、approve 前组装），消除无效调用与误导日志。

### F368 | hook_planting._append_to_pending_hooks 以 frontmatter-only 覆写 pending_hooks.md——接线即破坏生产 body 表格格式 | error | M（latent）

- 证据：`hook_planting.py:260-269`（读 frontmatter `hooks` 列表 → 只回写 `---\n{hooks: all_hooks}\n---`，**丢弃全部 body**）；生产 pending_hooks.md 的 hook 数据在 body 表格（frontmatter 无 `hooks` 键，本轮实测）→ 若接线（F307 建议"接进 lifecycle 前修正 id 匹配"被采纳），首次种植即把整个 body 表格覆写为 `hooks: []`。
- 根因：与 truth_io.upsert_yaml（F312）同类的 frontmatter-only 序列化缺陷，独立实现、独立死线。
- 验证：read hook_planting.py:222-269；python3 读生产 pending_hooks.md frontmatter keys（无 `hooks` 键）。
- 影响：当前死代码（F307）无运行时影响；是"接线前必须处理"的潜在地雷。
- 建议方向：接线前改为保留 body（读回 parts[2] 拼接）或改 body 表格追加；与 F312/F346/F347 合并处置 hook 生态。

### F369 | cmd_init 对缺失 seed 文件未捕获 FileNotFoundError → 裸 traceback 而非 emit_json ERROR | error | M

- 证据：`cli.py:421`（`seed_data = parse_seed(args.seed_file)`）；`seed_parser.py:102-103`（parse_seed 对缺失路径 raise FileNotFoundError）；cmd_init 无 try/except（对比 cmd_next/cmd_status/cmd_review/cmd_resume 均捕 FileNotFoundError 输出 JSON 错误）；main()（:884-935）无顶层异常处理。
- 根因：init 命令漏接统一错误出口。
- 验证：read cli.py:364-470 与 seed_parser.py:98-103。
- 影响：`pipeline init nonexistent-seed.md` 输出 traceback（破坏机器可读 stdout 约定 cli_utils emit_json）。
- 建议方向：cmd_init 捕 FileNotFoundError → emit_json ERROR + return 1。

### F370 | API 路径解析出的 SkillOutput.decisions 字段从不落盘（JSON 模式 decisions 侧车静默丢弃） | error | M

- 证据：`dispatch_helper.py:1646-1656`（`output = _parse_structured_output(output_text)` → `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions` 从未被读取/写入）；`SkillOutput.decisions`（:329）；json_mode 提示词明确要求模型输出 `"decisions": ...` 键（:680-694）。
- 根因：decisions 侧车只进了 schema 与提示词，写路径（`_write_parsed_outputs`）只认 files。
- 验证：grep `\.decisions` src/shenbi/pipeline/dispatch_helper.py → 仅 :329 定义、:680-694 提示词字符串；read :1646-1656。
- 影响：模型若按 schema 把 decisions 放顶层键而非文件条目，decisions.json 永不被写（G2 的 decisions 校验静默不触发，Layer A 侧车缺失）；合规模型按 "Files to create" 写文件条目则无碍——属契约脆弱点。
- 建议方向：`_write_parsed_outputs` 增加 `decisions` 分支（按契约 `*-decisions.json` 路径落盘）或在 json_mode 提示词中删除该顶层键以避免歧义。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件全量 deep-read，共 13,641 行）。
- 前几轮最大覆盖空洞（本轮已补）：(1) **进程退出生命周期**——前五轮均只审 crash_recovery 的信号路径（初审明确判"无 findings"），未验证 atexit 清理在**正常退出**下的行为 → 漏掉 F364（本轮唯一新增验证手段：/tmp 沙箱进程退出实验）；(2) **MODIFY STATE_SETTLE 回滚后的检查点语义**（F319 只覆盖 PER_CHAPTER 无回滚，未覆盖 STATE_SETTLE 回滚后 checkpoint 不重设）；(3) **`_advance` 的 STATE_SETTLE 分支死代码**（前几轮以"并行块不经过 :1087"框定 F341，未发现 CHAPTER_STEPS 根本无 STATE_SETTLE checkpoint 声明）；(4) **配置旋钮消费面**（genesis_review_required 等 5 项零消费者，前几轮只查了 retry 常量 F317 与 snapshot_retention F318.5）。
- 本轮验证资产：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → 解析 0 命中；audits 13 类后缀全旧式名、group-* 数=0；chapters 145 文件补零数=0；plans/chapter-5-plan.md section 7 为 MH-001/H-N01；truth/pending_hooks.md frontmatter 无 hooks 键；style/ 有 style_profile.md 而 truth/style_profile.md、truth/world_rules.md 均缺失；novel.json 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0；staging/ 119 个旧版遗留文件）；`uv run python` 实证 `read_volume_boundaries/requires_independent/_audit_short_name`、load_contract 契约 reads/writes/updates（lifecycle/state-settling/drafting/long-span/volume-outlining/resonance/sensitivity/group-*）；`/tmp` 沙箱进程退出实验（F364）。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 路径（codex/zcode）本环境不可执行，仅静态分析；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内），group skill 契约旧式 writes 是 F325 反证的关键。

---

## 四、汇总

- **新 finding 数**：7（F364-F370，其中 F364=P1（贴 P0）、F365/F366=P2、F367-F370=M）
- **误报数**：0 新增（F300-F363 全部成立/维持；F325 维持二/三/四轮误报撤销，本轮独立复证；F348 维持"成立但机制描述部分不准"）
- **严重度异议数**：0（立场声明：F364 按决策表可论 P0 但裁定 P1 留 stage 4；F302 的 P1→P0 依据维持留 stage 4；F305 同意 P2；F341 维持 P1 且本轮补强事实）
- **事实修正（不改严重度）**：1（F341 补强——`state_settle_review_required` 因 CHAPTER_STEPS 无 STATE_SETTLE checkpoint 声明而全路径失效，`_advance` :1086-1105 为死代码；另 F304 补充第三条触发路径）
- **0 新 finding 判定**：**否** —— 本轮发现 1 条 P1 漏报（F364 atexit 正常退出清空 staging：人审产物不提交、pipeline 卡死于 chapter-planning 循环，实测验证）与 1 条 P2（F365 STATE_SETTLE MODIFY 重跑悬空）及 1 条 P2（F366 死配置旋钮）与 4 条 M；F324（P0）、F340/F341/F345/F353/F354（P1）维持成立、F325（误报）维持撤销。
- **校准升级提示**：F364 建议进入阶段 4 复核优先级前列（正常路径确定性复现、数据丢失 + 契约静默违反，且与 F341/F365 叠加使 staging 门控机制整体失效）；F365/F366 建议与 F341、F317 合并处置（checkpoint 机制统一 + 配置消费面清理）。
