# Z1 分区第二轮独立复核报告（agent c）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`zone-reports/Z1.a.md`（13 findings F101–F113）+ `zone-reports/Z1.review.md`（9 findings F114–F122）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（无一处依赖前两轮转述；`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件通读）
- 只读约束：仅 grep / read / python3 只读 AST 与代码路径模拟；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F123–F199（实际产出 F123–F127，共 5 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：5**（F123–F127；P2×3、M×2；**无新 P0/P1**）
- **误报数：0**（前两轮 22 条全部成立；无一条被证伪）
- **严重度异议：1**（F109：同意 R1 的 M→P2 异议，复核维持；F115 的 P1 **无反证，维持**，但附 2 处事实性修正）
- **0 新 finding 判定：否**（5 条新漏报，均低/中危；其中 F123 为 capability_fs 权限模型的"覆盖空洞"——前两轮只验证了模型本身，未发现模块无生产接线）

---

## 1. 复核结论表（F101–F122 逐条判定）

| 编号 | 标题 | 前两轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 复现：`git ls-files -s` 三文件均 100644 vs `ls -la` 均 `-rw-------`；全仓扫描 **89 个** 100644 索引文件工作树 mode≠644（R1 报 85，差值为复核时点工作树增量，结论不变）；git 不追踪 rw 位 → CI `git diff --exit-code` 看不见 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `find docs -name registry.md -o -name schemas.md -o -name integrity.md` → 0 命中；`ls tests/build_registry.py` → No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 的 action 不破，仅 doc_url 破） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立 AST 扫描 src/ 全量 raise：RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentTimeoutError/SubAgentProtocolError/RetryExhaustedError（5/22）；NEVER RAISED 17 类；`grep get_guidance/ERROR_GUIDANCE/RECOVERY_STRATEGIES` 在 src/ 除本文件外 **0 消费者**（error_guidance/recovery 目录整体 dead） |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 validate_scores 空 → `REJECT: scores is empty` → sys.exit(2)；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立** | :360 `required=False` + :117 `str(None)`；`test_routes_start_command`（test_phase_runner.py:668）用 `fake_g5_pass` mock 掩盖——测试恒 PASS 但真实 G5 收到 "None" → `Path("None")` 相对 CWD；:188 assert 在 `-O` 下移除后 `Path(None)` → TypeError |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :356-362 唯一 `except Exception`（grep 证实）；G3 subprocess 无 timeout（:343-355），超时/崩溃 → 静默跳过 |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | phase_runner 全文 0 引用；仅 gates/cli.py:126-134 手动入口；三 gate 单测存在 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :103-105 mkstemp 在 try 外（若抛 OSError，:117-125 finally 不执行 → flock fd/.lock 泄漏）；:89-91 unlink 后立即 O_EXCL 重建的 FileExistsError 无 try 捕获 |
| F109 | verify_bijection 用 assert | M | **成立；严重度异议维持** | sync_contracts.py:121 assert 实证；`-O` 下整守卫消失与 docstring :110 "Raises on mismatch" 冲突。**同意 R1 异议：M → P2**——assert 是运行时正确性守卫而非文案；守卫失效时该工具仍会静默写入 deps.json（:201），属"防御深度/优化机会"而非"错别字/格式" |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证；test_exceptions.py:64 同款文案 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9 实证；gates/cli.py、dispatcher/cli.py、dispatcher/executor.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo(" src/shenbi` 0 命中；**同族 `backup()`（paths.py:33-35）亦 0 调用方**（复核确认）；RoundPaths 本身被 11 个 gates/g4 checkers 消费（read() 有调用方） |
| F113 | phase_runner pre-skill ContractError 静默吞错 | P2 | **成立** | :153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `subprocess.TimeoutExpired.__mro__` = (…SubprocessError, Exception…) 且 `issubclass(TimeoutExpired, OSError) == False`；:94 `except (json.JSONDecodeError, ValueError, OSError)` 漏列 → 60s 超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有（复核确认） |
| F115 | scoring 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持；附 2 处事实性修正）** | 独立模拟 `load_applicability`：worldbuilding/faction-builder/location-builder/story-architecture → `{}`；chapter-drafting → `{Bug-hunt,Clean,Generative}` 且 Bug-hunt/Clean 正确排除 dims 6,7,9（机制在格式匹配时**确实工作**）。82 个 rubric：44 含 "Dimension scope"，38 不含（其中 20 个完全无 "## Dimension Applicability" 节、18 个有节但表头非 "Dimension scope" → 解析器返回 {}）；4 个 N/A 豁免技能（worldbuilding dim4 / faction-builder dim8 / location-builder dim5 / story-architecture dim6，均 10% 权重）的豁免被静默忽略，rubric 文本明示 "scoring.py renormalizes weights for remaining applicable dimensions" 不成立；command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 评分路径上可复现 → 文档化路径静默错误评分，**P1 维持**（影响面若仅 4 技能×1 维度可议降 P2，合并层裁量，但按决策表"正常路径可复现功能错误"维持 P1）。**事实性修正 1**：组成数应为 **9 t2-phase + 3 t3-pipeline + 26 t1-skill = 38**（原写 10+3+25，总数对、分项错——t2-phase 实为 9 个目录）；**修正 2**：38 个 no-op 中 34 个为良性（未声明任何豁免），真正受害者为 4 个 N/A 技能 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | executor.py:200-204 同路径 G2 FAIL → `return 1`；phase_runner :211-239 仅 G4 FAIL 阻断（:228-230）；grep `"g2"` 在 phase_runner 外 0 消费 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值；:334-362 tier 块仅 `tier == "T1" and test_type` 有 G3 逻辑；usage :289 宣称 `--tier T1\|T2\|T3 --phase <name>` |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/心跳/锁龄证据）；:54-62 `os.open` 成功后 `flock` 抛 OSError → fd 未 close 即落 O_EXCL 分支 → fd 泄漏 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:101-121 G4 bug-hunt/clean 分支**不写任何标记**（仅 :121 generative 分支写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 test_type 拼接。**补充证据**：goal-prompt.md:86 明示 "G4 gate markers 覆盖 generative、bug-hunt、clean 三种测试类型"——文档契约与实现不符；但 command-to-give.md T1 协议评分步骤（:49,64,71）均**不带 --round-dir** → 标记检查被跳过，触发面为"带 --round-dir 的非 generative 评分"（潜在/未来路径），**P2 恰当** |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关；未排除 round-dir/非输出 .md |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `state["steps"].append(...)`；对照其余 5 命令均记录；grep `"action": "pre-score"` 0 命中 |

**前两轮陈述事实修正 2 处（非 finding）**：
1. F115 组成数：10 t2-phase → **9**（t2-phase 实有 9 目录：genesis/architecture/planning/drafting/audit/management/import/foundation/short-story），25 t1-skill → **26**；总数 38 不变。
2. F101 波及文件数：R1 报 85，复核时点实测 89（差异为两次复核间工作树增量写入，如 `tests/coverage/` 产物被其他 agent 并发改写——与 Z1.a.md 关于 coverage.xml 被并发覆写的说明一致）；结论不受影响。

**严重度异议汇总**：F109（M→P2）同意 R1；F115（P1）无反驳证据、维持；其余 20 条严重度无异议。

---

## 2. 新发现（F123–F127）

F123 | capability_fs.CapabilityFS 无任何生产接线：支柱五"读 provenance 运行时兜底"仅测试消费，模块为生产死代码 | dead-wire/未接线 | P2
- 证据: src/shenbi/capability_fs.py:3-7（docstring "把可写 Path 换成只读句柄注入门/测试…是「门纯度」的运行时兜底"）、:15-54（class CapabilityFS 全部方法）；消费点 grep 全仓：仅 tests/unit/test_capability_fs.py + tests/property/gates/test_capability_fs_properties.py；设计文档 docs/superpowers/specs/archive/2026-06-29-contract-single-source-design.md:76,93（"读 provenance: 仅 internal mode CapabilityFS"）
- 根因: 前两轮复核聚焦权限模型本身（resolve+relative_to 拒 symlink 逃逸、写侧 4 方法全拒——模型验证正确），但**未检查模块是否有生产调用方**；实际 src/ + tools/ 无一处 import（AST 全量 grep 0 命中），设计承诺的"注入门"从未落地，门函数不接受 fs 句柄；唯一的运行时"门纯度"强制是 AST lint（tools/lint_no_fs_mutation.py 禁写原语），读侧 provenance 无任何运行时执行
- 验证命令+输出: `grep -rn "CapabilityFS\|capability_fs" src/ tools/ --include="*.py"` → 0 命中（仅 capability_fs.py 自身与测试文件）；`grep -rln "CapabilityFS" docs/` → 仅归档设计/计划文档
- 建议方向: 与 F107/F112 同族处置——要么接线（gate 函数签名注入 fs 句柄，把读侧 read_text/exists 收敛到 CapabilityFS），要么在 docstring 明示"当前仅测试工具，读 provenance 未落地"并降级为测试设施；避免保留"运行时兜底"的虚假承诺

F124 | phase_runner CLI 无位置参数校验：缺参 → IndexError traceback；flag 被解析为位置参数 | error | P2
- 证据: src/shenbi/phase_runner.py:364-381（各命令分支 `args[0]`/`args[1]` 无长度检查）、:348-362（find_flag 只在 args 中查找，位置参数与 flag 混排时 `phase = args[0]` 直接取首元素）
- 根因: main() 仅校验 `len(sys.argv) < 2`（:338），未按命令校验位置参数个数；flags 必须位于位置参数之后，前置 flag 会被当 phase/skill 消费
- 验证命令+输出: python3 模拟——`phase-runner pre-score`（无位置参数）→ `args[0]` IndexError；`phase-runner pre-skill genesis`（缺 skill）→ `args[1]` IndexError；`phase-runner start --round-dir /tmp/r` → phase 被解析为 `'--round-dir'` → 下游 G5 收到 phase_name="--round-dir" 误导性 FAIL（fail-loud 但不优雅，且与 F105 的 "None" 家族同根：CLI 层未按 usage 校验）。对比 scoring.py:284 有 usage 长度守卫，phase_runner 无
- 建议方向: 每条命令按 usage 校验位置参数个数（不足 → emit_json ERROR + exit 1）；或改用 argparse 统一解析（位置参数 + 可选 flag 混排）

F125 | command-to-give.md:48 引用不存在的 tests/dispatch-subagent.sh：PR-20 迁移后执行协议断链 | doc↔code drift | P2
- 证据: command-to-give.md:48（"新开独立 subagent 评分（使用 `bash tests/dispatch-subagent.sh <skill> generative <round_dir> "<prompt>"`）"）；`ls tests/dispatch-subagent.sh` → No such file（全仓 find 0 命中）；docs/adr/0009-dispatcher-python-rewrite.md（"Python rewrite of dispatch-subagent.sh"，PR-20 已把 247 行 shell 迁移为 src/shenbi/dispatcher/）；pyproject.toml:59 替代入口 `shenbi-dispatch = "shenbi.dispatcher.cli:main"` 存在
- 根因: PR-20 迁移删除了 shell 脚本但未同步 command-to-give.md（AGENTS.md 标注的 "Execution protocol"）；照文档执行的 agent 在该步骤必然 `No such file or directory`（与 F102 的断链同族，但 F102 是错误引导目录，本条是**执行协议本身断链**）
- 验证命令+输出: `find . -name "*dispatch-subagent*" | grep -v .git` → 0 命中；`grep -rn "dispatch-subagent" --include="*.md" .` → 仅 ADR 与归档计划提及迁移历史
- 建议方向: 改为 `uv run shenbi-dispatch <skill> <test_type> <round_dir> "<prompt>"`（与 AGENTS.md/ADR-0009 一致）；同步核对 command-to-give.md 其余 `bash tests/*.sh` 引用是否仍存在

F126 | scoring.py 批次评分 scored_by 误标 "interactive"：非交互文件评分且未传 --subagent 时审计元数据失真 | M
- 证据: src/shenbi/scoring.py:441（`"scored_by": "subagent" if "--subagent" in sys.argv else "interactive"`）；command-to-give.md:49 文档化评分命令 `uv run shenbi-score <rubric> <scores.json> --test-type generative` **不带 --subagent**；goal-prompt.md:88（"_provenance 字段记录评分者身份（scored_by）"——评分独立性审计）
- 根因: scored_by 只有 "subagent"/"interactive" 两态，且默认态取 "interactive"；文件评分（batch）路径与交互路径共用同一表达式，未传 --subagent 的 batch 评分被标为 interactive；生产 runner（src/shenbi/dispatcher/modes/codex.py:99,106）恒传 --subagent 故生产路径正确，但手工/文档路径失真
- 验证命令+输出: 代码走查 :438-446；`grep -rn "shenbi-score" src/shenbi/dispatcher/modes/codex.py` → :99 带 `--subagent`(:106)
- 建议方向: 三态化——`"batch"`（文件评分无 --subagent）/`"subagent"`/`"interactive"`；或文档显式要求评分调用方必须传 --subagent

F127 | status.py ScoringStatus.OK / ScoringStatus.UNIMPLEMENTED 死成员：单一定义词汇表中 2/5 成员无任何 emit/read 站点 | M
- 证据: src/shenbi/status.py:53（`OK = "ok"`）、:55（`UNIMPLEMENTED = "UNIMPLEMENTED"`）；grep `ScoringStatus.OK\|ScoringStatus.UNIMPLEMENTED` src/shenbi → 0 命中（REJECT :158、MARKER_MISSING :371 在用）；GateStatus 五成员（含 UNIMPLEMENTED :27，gates/shared.py:215 用）与 PhaseState/CommandStatus/ScoreClassification 均有消费
- 根因: status.py 是"状态字符串单一定义"词汇表，ScoringStatus 设计含 ok/REJECT/MARKER_MISSING/UNIMPLEMENTED 四态，但实际 emit 仅 REJECT/MARKER_MISSING；OK 与 UNIMPLEMENTED 为预留成员未接线（scoring 的 gate-only/UNIMPLEMENTED 路径从未落地）
- 验证命令+输出: `grep -rn "ScoringStatus" src/shenbi --include="*.py" | grep -v status.py` → 仅 :158（REJECT）、:371（MARKER_MISSING）
- 建议方向: 删除两成员（保持词汇表与实际 emit 一致）或接线（如 gate-only 模式的 ok 态改用 ScoringStatus.OK）；M 级，随 status.py 词汇表维护

---

## 3. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读，无一处依赖前两轮转述）
- 与 R1 一致判定无生产缺陷的文件：`cli_utils.py`（stdout 数据通道/stderr 日志分离，emit_json 无问题）、`logging.py`（双渲染器/stderr/`cache_logger_on_first_use=False` 测试可重绑——设计自洽；见下"备查"）、`status.py`（wire 值/单一定义核实，仅 F127 死成员）、`py.typed`（0 字节 marker + hatchling 默认全量打包，配置层一致）、`recovery.py`（F103 已覆盖 dead）、`exceptions.py`（F103 已覆盖层级设计；22 类层级本身结构正确，无新缺陷维度）
- 新发现分布：`capability_fs.py`（F123 覆盖空洞——权限模型无洞但模块无接线）、`phase_runner.py`（F124）、`command-to-give.md`（F125，协议断链）、`scoring.py`（F126）、`status.py`（F127）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **logging.py SHENBI_LOG_FORMAT 无校验**：非 "json" 精确值（如 "JSON"/"Json"）静默降级 console，无告警。R1 已记录为 M 级低价值并明确"可并入后续"，本复核**同意其判定**（日志仍工作，仅 CI 日志解析形态漂移，无正确性影响），不重复立 finding。
2. **scoring.py --gate-only 模式**（:296-313）：json.loads(proc_result.stdout) 无 try 捕获（gate 子进程崩溃 → ValueError traceback，fail-loud 但形态与 :356-362 的吞错相反，处理不一致）；subprocess.run 无 timeout（属 F114 建议范围）；且 --gate-only 全仓无生产调用方（仅 usage 文档 + 测试）。三要素各自低危，未单独立 finding。
3. **scoring.py --interactive 必须是 argv[2]**：`scoring.py <rubric> --test-type X --interactive` 会走文件分支 → FileNotFoundError traceback（flag 顺序脆弱，与 F124 同族；usage :291 展示的 `scoring.py <rubric.md> --interactive` 路径可用）。
4. **exceptions.py `to_dict` 无生产调用方**（并入 F103 注记）；**paths.py `backup()` 0 调用方**（并入 F112 注记）——前两轮已注明，不重复。
5. **F115 的 filter 排除解析脆弱性**：`re.findall(r"#?(\d+)", scope)` 会把 "Phase-3 output" 这类 scope 中的 "3" 当维度号；且 "No" 但无可解析数字的 scope 静默 no-op（:133-134 `if not excluded_nums: return dimensions`）。当前 44 个 Dimension-scope rubric 未触发错误分支，属潜在格式风险（并入 F115 根因族）。

---

## 4. 其他说明

- 前两轮"无 P0/P1"结论在 R1 已因 F115 修正；本复核确认 F115 无反驳证据（独立模拟 4 个 N/A 技能 applicability 全部返回 `{}`，chapter-drafting 对照组格式正确时过滤**确实生效**），P1 维持。
- F120 补充证据（goal-prompt.md:86 明示三类型 marker 覆盖）与约束证据（T1 协议评分不带 --round-dir → 触发面为潜在路径）均已独立核实，P2 恰当。
- 环境备注：本复核全部验证经 python3 只读模拟 + grep/read 完成，未运行 pytest（避免写 __pycache__/缓存，遵守只读禁令）；未使用 `uv run`（无网络/安装需求）。
