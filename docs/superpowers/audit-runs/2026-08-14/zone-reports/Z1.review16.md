# Z1 分区第十六轮独立复核报告（agent q）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`~`Z1.review15.md`（F114–F166），共 **66 条**
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读，不依赖任何前轮转述；对跨区依赖 `contracts/paths.py`、`gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g3.py`、`gates/g4/__init__.py`、`gates/g4/generic.py`、`gates/g4/worldbuilding.py`、`gates/g5.py`、`audit/_shared.py`、`dispatcher/executor.py`、`tools/lint_status_strings.py`、`tests/tiers/deps.json`、`command-to-give.md`、worldbuilding/chapter-drafting/_template 三份 rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`** 与 `PYTHONPATH=src` 纯内存解析器/函数复跑）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp/zb16_pycache`，临时文件仅写 /tmp）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F167–F199（**实际产出 0 条新 finding**）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：0**（F167–F199 无产出——前十五轮 66 条 finding 覆盖已收敛，本复核 14/14 文件全量重读 + 跨区依赖核实 + 关键 P1 实证后未发现任何未覆盖的独立缺陷站点）
- **误报数：0**（F101–F166 全部 66 条成立；无一条被证伪。F115/F140/F158/F163 四个 P1（F163 为合并层按 T2 已运行假设归入 P1）经本复核**独立实跑/代码走查/端到端逻辑推演确认无反证**）
- **严重度异议数：0 新增**（同意既有全部：F109 M→P2 共识、F150 的 R13 M→P2 升级、F115/F140/F158 的 P1、F163 的 P1 定位（条件性，见 §3）、F134 维持 P2）
- **0 新 finding 判定：是**（本轮零新发现；Z1 区十六轮复核累计 66 条 finding（F101–F166），全部成立，无 P0，P1×3 稳 + F163 条件性 P1）

---

## 1. 复核结论表（F101–F166 逐条判定）

本复核对 66 条 finding 全部以 fresh-context 重读源码 + 独立实验验证；**结论：66/66 成立，误报数 0**。P1 四条（F115/F140/F158/F163）的独立验证见 §2，其余按文件族分组简述（每条均已与当前源码行号逐一核对，无行号漂移）。

### 1.1 P1 判定（F115/F140/F158/F163）——无反证，全部维持

| 编号 | 标题 | 定级 | 本复核独立验证 |
|---|---|---|---|
| F115 | 维度过滤对 38/82 rubric 静默 no-op | P1 | **实跑复现**：`load_applicability('tests/tiers/t1-skill/shenbi-worldbuilding/rubric.md')` → `{}`（表头 `\| # \| Dimension \| Bug-hunt Standard \| Clean Standard \|`（rubric.md:38），scoring.py:88 只认 `\| Dimension scope \|` → header_dims 恒空）；`filter_dimensions_by_test_type(dims, p, 'bug-hunt')` → 9 维原样返回（含 N/A 豁免的 dim 4 Prose quality，10% 权重，rubric.md:43 明示 "scoring.py renormalizes weights for remaining applicable dimensions"）；对照组 chapter-drafting（`\| Dimension scope \|` 头）过滤确实生效。**反证尝试失败**：4 技能（worldbuilding/faction-builder/location-builder/story-architecture）bug-hunt/clean 测试目录均存在（ls 实证）；command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径原文存在。按决策表"正常路径可复现功能错误"维持 P1 |
| F140 | `shenbi-score` 成功路径恒 exit 1 | P1 | **实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**：10 维 scores + `--test-type generative` → 评分完全成功（stdout 合法 JSON、`final_score` 正常）但 **EXIT: 1**，stderr 为完整 result dict repr（`sys.exit(main())` 包装实证——cat 脚本内容 `sys.exit(main())`）；`main()` 成功路径 `return result`（scoring.py:463）→ `SystemExit(dict)` → exit 1；违反 command-to-give.md:57-61 "0=成功" 契约。反证（"返回 dict 是库 API 设计"）被 `scoring.main` 无库调用方 + 对照 shenbi-phase（返回 None→exit 0）/shenbi-sync-contracts（返回 int）证伪 |
| F158 | phase_runner phase 参数路径穿越（写侧） | P1 | 代码走查 :37（`state_file = Path(round_dir) / "phase-state" / f"{phase}.json"`）、:46-47（`state_file = state_dir / f"{state['phase']}.json"`）、:126-129（G5-FAIL 分支无条件 save_state）、六命令直接透传 `args[0]` 无白名单——`../` 形态 phase 使写操作送出 round_dir；前轮端到端实跑（`/tmp/zb1x_escaped.json` 被创建、round_dir 内 0 文件）为本复核代码走查所支持。维持 P1（触发需操作者主动传路径形态 phase，合并层可议 P0，本复核不新增异议） |
| F163 | phase_runner G4 第 3 参传 round_dir 而非 project-dir → T2 特定 checker 输出查找全 miss | P1（合并层按 T2 已运行归入；R14/R15 原定 P2） | **代码走查确认机制**：phase_runner.py:216 `run_gate("G4", [skill, files, str(round_dir)])` → gates/cli.py:118 `project_dir=rd`（=round_dir）→ g4_worldbuilding.py:30-34 `RoundPaths(round_dir=rd, project_dir=rd)` → `rp.read("novel.json")` 等在 round_dir 根下查找，而 T2 输出位于 `<round-dir>/project-output/`（pre_score :262 `Path(round_dir)/"project-output"` 与 command-to-give.md 协议佐证）→ 恒 not_found → G4 FAIL → post-skill BLOCKED；11 个 rp.read 类 checker 中 genesis 相位 9 个 prerequisite 命中。**R14 对照组（3rd 参改传 project-output → PASS）**证实为目录错位而非 checker 缺陷。**P1 定位的条件性说明见 §3** |

### 1.2 其余 62 条判定（成立；独立验证要点）

- **safe_write 家族（F101/F108/F119/F133/F154）**：F101 实证 `git ls-files -s`（100644）vs 工作树 0600（mkstemp 固定 0600 + os.replace 继承，safe_write.py:105,111）；F108 mkstemp（:105）在 try（:106）外、flock fd 泄漏路径成立；F119 :83-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）+ :54-62 flock 抛 OSError 时 fd 未 close；F133 :117-125 先 close(fd) 再按路径 unlink（删新持有者锁）；F154 flock 分支（:54-62）从不检查 lockfile、O_EXCL 分支（:63-91）从不检查目录 flock——两原语互斥语义不共享，:61 `except (ImportError, OSError): pass` 为唯一切换点。
- **scoring 校验链（F104/F106/F118/F126/F129/F131/F134/F135/F141/F142/F146/F149/F151/F152/F155/F157）**：F134 实测 `validate_scores({1:100,2:95}, [])` → `(True, ['WARNING: unexpected dimension keys…'])` + `compute_score([],…)` → 0 FAIL（只查空 scores 不查空 dims，:145-159/:169-170）；F135 NaN 通过 `score < 0 or score > 100`（对 NaN 恒 False）+ `json.load` 接受 NaN 字面量；F141 `isinstance(True, (int,float))` → True 按 1 计；F142 实测 chapter-drafting bug-hunt 过滤后权重和 75 → `weight_mismatch` 恒误报（:171-172 warning vs :107-109 docstring 宣称的 filter 内重归一）；F149 `_template/rubric.md` "Prose/narrative quality \| No" 行 scope 无维度数字 → excluded_nums 恒空 → no-op（表头匹配但机制静默失效）；F151 `{"1":90,"dimensin1":100,"notes":"x"}` → `{1:90}`（:417-419 非数字键静默过滤）；F152 worldbuilding kill-switch 节 5 行 `= 0` 中 "detection dimension = 0" 不被捕获（:43-47 只认 total score/phase/pipeline = 0）；F155 `check_scorer_agreement({1:nan,2:90},…)` → agreed=True/max_diff=0.0、`flag_score_collapse({1:95})` → all_identical（:240-246/:266 无 isfinite/len≥2 守卫）；F157 交互 `input()` prompt 经 sys.stdout（:388/:397）→ stdout 混合体不可 json.loads；F126 实跑输出 `"scored_by": "interactive"`（不带 --subagent 的文档化命令）；F118 `_phase` 仅 :321/:331 两处赋值；F129 `--type` 尾参无长度守卫（:303 对比 :300）；F131 `gate_markers_verified: bool(round_dir and test_type)`（:443）；F146 t2 分支 `if deps_path.exists(): json.loads(...)`（:205-207）只防缺失不防损坏；F106 :361-362 唯一无 exc_info 的 `except Exception` + G3 subprocess 无 timeout（grep 实证）。
- **phase_runner 状态机/CLI（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150/F156/F162/F164/F165/F166）**：F105 走查 g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **PASS（g5.py:202-203，非 R1 所称 SKIP）**——R3 修正正确；F114 `issubclass(subprocess.TimeoutExpired, OSError)==False`（mro 实证）+ :94 except 元组漏列 + scoring.py:304,343 两 subprocess 无 timeout（grep 实证）；F116 :228-230 仅 `g4_status=="FAIL"` 阻断，G2 FAIL 只进 step/manifest（executor.py:200-204 同路径 return 1 → 双入口语义不一致）；F121 :208-210 `[...][:20]` + rglob 顺序 OS 相关；F122 cmd_pre_score（:242-275）无 `state["steps"].append`（其余 5 命令均记录）；F124 :364-381 `args[0]`/`args[1]` 无长度检查；F128 :292 仅 `json.loads` 语法校验（`[]`/`"hello"` 均通过）；F132 :105-111 emit `{"error","phase"}` 无 status 键（对照 :139-146/:281-289）；F137 audit/_shared.py:57-58 + executor.py:74-75 均 `except ContractError: return 默认值` 无日志 + g4/generic.py:57-58 空文件 → SKIP 后 `passed(...)` vacuous PASS；F143 走查 g2.py:76-84 decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过，无 json.loads、无 crash）——注释 crash 论断不成立；F144 load_state（:36-40）`json.loads` 无 try + cast 无运行时断言；F145 load_deps（:32-33）无 try（对照 g5.py:36-39 try/except → 结构化 FAIL）；F148 :196-200 `exists()+st_size>0` 静默过滤缺失项无日志 → 空 output_files → G2 SKIP + G4 vacuous PASS + PASS marker；F150 phase_runner.py:213 3 参 vs g2.py:237,382-384 `if not project_dir: return False`（重要章节 ceiling 恒 4500；executor.py:25-26 PROJECT_DIR=REPO_ROOT 亦非真实项目目录，R13 的 P2 升级独立确认）；F156 `derive_file_type('shenbi-chapter-drafting')`→'decisions' + `derive_output_files` → `['chapters/chapter-5.md','chapters/chapter-5-decisions.json']` 混合输出 + g2.py:84 decisions 分支跳过 .md（G2.4-2.12 静默不执行，受害技能 3 个）；F162 :185 与 :337 双 configure_logging（六命令中唯一函数级重复，幂等无行为差异）；F164 :124 `"g5": "PASS"` 裸字面量 + tools/lint_status_strings.py:25 STATUS_KEYS 仅 {status,state,classification}（"g5" 键逃逸，实跑 lint exit 0）；F165 phase_runner.py:2 + __init__.py:7 "T2/T3" 承诺 vs g5.py:40 只认 t2-phases（`shenbi-validate G5 long-form` → FAIL "unknown phase"）+ command-to-give.md:126 "T3 不使用 phase-runner.py"；F166 :137 `PROJECT / "skills" / skill / "SKILL.md"` 拼接（skill 直接来自 args[0]）可穿越 skills/ 目录（读侧存在性探测 + 外部 SKILL.md 冒充契约）。
- **sync_contracts（F109/F117/F130/F147/F153/F159）**：F109 :121 assert 实证（docstring :110 "Raises on mismatch" 与 -O 下守卫消失冲突，工具仍静默写 deps.json :201）；F117 :40-41 `except ContractError: continue` 无日志 + :86-92 被跳技能产出空 + :196-200 verify_bijection 同源盲区；F130 :146-147 `if not m: return` 无日志 + :152 `pattern.sub(block, body, count=1)` 只替换首个；F147 :165 `load_registry()` ContractError 裸上抛 + :195 `json.loads(DEPS_PATH.read_text(...))` 无 try（对照 :167-172 bail 有 log.error + return 1）；F153 :124 定义 + :149 传参外 `skill` 参数体内 0 命中；F159 全文无 configure_logging（对照 phase_runner:337/scoring:283/gates/cli:57/dispatcher/cli:19）→ structlog 默认 PrintLoggerFactory → 日志走 stdout + SHENBI_LOG_FORMAT 静默失效。
- **死代码/词汇表（F103/F110/F112/F123/F127/F136/F138/F139/F161）**：F103 独立 AST 扫描 5/22 RAISED（DispatchWriteFailureError/DispatcherError/RetryExhaustedError/SubAgentProtocolError/SubAgentTimeoutError），error_guidance/recovery 引用的 6 类全无站点；F127 grep `ScoringStatus.OK\|ScoringStatus.UNIMPLEMENTED` → 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用；GateStatus.UNIMPLEMENTED/WARN 由 gates 区消费非死成员）；F136 `CommandResult` 仅 status.py:78 定义 0 消费；F161 `grep rp.write` 全仓 0 命中（read 21 处，gates/g4 实证）；F138 error_guidance/recovery 两文件 `log.` 0 命中（除 import 行）；F112 `\.repo(` 0 命中 + backup() 同族；F123 `grep CapabilityFS src/ tools/` 0 生产消费者；F139 __init__.py:3-10 列 6 项 vs 实际 12 模块；F110 exceptions.py:57 无单复数区分。
- **文档/断链（F102/F111/F125）**：F102 5 个顶层 docs/*.md 全 MISSING + `ls tests/build_registry.py` No such file（ToolTamper 的 action tests/lock-tool-hashes.sh 存在，仅 doc_url 破）；F125 `find . -name "*dispatch-subagent*"` 0 命中（ADR-0009 已迁移 shenbi-dispatch）；F111 __init__.py:8-9 "forwarder until PR-19/20" 过期。
- **加载边界家族（F144/F145/F146/F147）**：逐行核对均无 try/形状守卫（对照 g5.py:36-39 结构化 FAIL 与 sync_contracts :171 bail），全部成立。
- **G2 混合输出/回退（F121/F143/F148/F150/F156）**：见 phase_runner 族，全部成立。
- **cli_utils（F160）**：:20-22 `sys.stdout.write` + `flush()` 无任何 except，BrokenPipeError 未处理（emit_json 是全部 CLI stdout 数据通道唯一出口）。

**前十五轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 **确认 R3 修正正确**；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致（deps.json 9 个 t2 键 + 3 个 t3 键实测）；F101 波及文件数递增记录属实（本复核时点 dependency-dag.json/deps.json 均 0600 实证）。**无新的前轮事实错误需要修正。**

---

## 2. P1 反证尝试汇总（全部失败）

| 编号 | 反证假设 | 证伪结果 |
|---|---|---|
| F115 | "bug-hunt 协议不实际使用 worldbuilding 等 4 技能 rubric" | 4 技能均有 bug-hunt/clean 测试目录（ls 实证）；command-to-give.md:64,71 文档化路径原文存在；实跑 `load_applicability` 返回 `{}`、filter 返回全 9 维 |
| F140 | "`scoring.main` 返回 dict 是库 API 设计，无 CLI 消费方" | `.venv/bin/shenbi-score` 包装 `sys.exit(main())`（cat 脚本实证）；实跑成功评分 **EXIT CODE: 1**；`scoring.main` 无库调用方（grep 全 src）；对照 shenbi-phase/shenbi-sync-contracts 退出码正常 |
| F158 | "phase 参数仅用于状态文件名，`..` 会被 Path 拒绝" | 前轮端到端实跑（`/tmp/zb1x_escaped.json` 被创建、round_dir 内 0 文件）+ 本复核代码走查 :37/:46-47/:126-129 支持——`..` 在文件名内由 OS 归一化，写侧逃逸成立 |
| F163 | "G4 失败是 checker 缺陷或输出格式问题，非目录错位" | R14 对照组：同一输出集，3rd 参改传 project-output → PASS 全 check 通过；本复核走查 g4_worldbuilding.py:30-34 RoundPaths(rd, rd) + rp.read 相对名 → 目录错位铁证 |

---

## 3. 严重度异议汇总

| 编号 | 前十五轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有共识）** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移（CI 幂等检查为唯一间接兜底）。按决策表 P2"防御深度"而非 M"文案" |
| F150 | M→P2（R13 升级） | **同意 P2（无异议）** | 独立走查 executor.py:25-26 PROJECT_DIR=REPO_ROOT + :200-204 G2 FAIL 当下 return 1 阻断 + g2.py:237/382-384 `if not project_dir: return False`——重要章节（4500-10000 字）经 dispatcher 路径当下误 FAIL（真实功能阻断，非元数据失真） |
| F115 | P1 | **维持 P1（无反证）** | 实跑真实解析器：worldbuilding applicability `{}`、filter 返回全 9 维（含 N/A 豁免 dim 4，10% 权重）、rubric:43 明示 renormalize 承诺、4 技能 bug-hunt/clean 测试目录存在、command-to-give.md:64,71 文档化路径。**反证尝试失败** |
| F140 | P1 | **维持 P1（无反证）** | 实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达；`scoring.main` 无库调用方。**反证尝试失败** |
| F158 | P1 | **维持 P1（无反证）** | 代码走查 + 前轮端到端实证支持路径穿越写侧；触发需操作者主动传路径形态 phase（正常管线恒用 deps.json 固定 9 相位名），按 R12/R13 的决策表张力说明维持 P1（合并层可议 P0，本复核不新增异议） |
| F163 | P1（合并层归入）/ P2（R14/R15 原定） | **机制成立；P1/P2 定级为条件性——不构成新异议** | 机制本身（G4 第 3 参 round_dir vs project-output 目录错位 → T2 genesis 9 技能 + drafting 的 foreshadowing-track 的 rp.read 类 checker 恒 FAIL → post-skill BLOCKED）经本复核代码走查确认真实存在，非误报。P1 vs P2 的分歧点仅在"T2 是否已实际运行/为文档化活跃路径"：若 T2 已运行，按决策表"正常路径可复现功能错误"P1 恰当；若 T2 从未执行（失败为 loud BLOCKED、无静默数据危害），P2 恰当。本复核不掌握 T2 运行证据，维持"条件性 P1"的合并层裁量，不新增异议 |
| F134 | P2 | **维持 P2（不议升）** | command-to-give.md:55 确文档化 "dimensions 为空 → exit 2"，实现静默 0 分属契约背离；但触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析——本复核复跑确认仅 `_template` 占位符 weight≠100），缺"正常路径可复现"，维持 P2 |

**新增严重度异议：0。** 其余 59 条严重度无异议。

---

## 4. 新发现（F167–F199）

**无。** 本复核 14/14 文件全量重读 + 跨区依赖核实后未发现任何前十五轮未覆盖的独立缺陷站点。

为证明收敛而非遗漏，本复核对"最后可能残留的候选站点"逐一实测排查，全部落空：

1. **候选：scores.json 非字符串键 → `k.lstrip` AttributeError**——**证伪**：`json.loads('{1: 100}')` 被 Python json 解码器拒绝（`Expecting property name enclosed in double quotes`），非字符串键根本不可能到达 :419 的 `k.lstrip`；列表型 scores（`raw.items()` AttributeError）已由 R4 备查 #3 覆盖。
2. **候选：G5.4 在 `project_dir="None"` 下对 glob expected_outputs 的行为**——属 F105 家族内部细节：有 expected_outputs 的 phase → G5.4 no_matches → FAIL（loud）；无 expected_outputs 的 phase → G5.3 空证据 PASS + G5.4 空循环 + G5.5 SKIP → 假 PASS（R3 分析精确）。无独立新站点。
3. **候选：`--gate-only` G3/G4 参数形状错配、无 --files vacuous G2 PASS、UNIMPLEMENTED 门 exit 0、`--chapter abc` ValueError、`--interactive` 必须 argv[2]、find_flag 值不校验、缺失 rubric/scores 裸 traceback、`--test-type` 无枚举校验、filter 全排除反转、emit_json allow_nan、capability_fs symlink 环 RuntimeError、gate_manifest pipeline-manifest 无生产读取方、G2.10 注释与实现不符、AGENTS.md 67+2=69 与 74 skills 不符、WARNING 类 validation_errors 以 log.error 输出**——均为 R2–R15 备查已记录项，本复核独立确认后同意不立 finding（其中多项为 gates/pipeline/skills 非 Z1 区或纯潜在格式风险，当前 82 份 rubric 无触发）。
4. **候选：`render_body_into` 对 SKILL.md 缺失的 FileNotFoundError**——load_contract 成功即证明 SKILL.md 存在（契约源自身），文件缺失仅存于"加载后删除"竞态，不可达，不立 finding。
5. **候选：交互模式 kill-switch `input()` 的 EOFError 处理**——`printf '' | ... --interactive` → kill-switch EOF pass + 维度全 0 → final 0 FAIL，行为可预期无危害。

---

## 5. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py` 全部独立通读；另核实跨区依赖：contracts/paths.py、gates/cli.py、gates/shared.py、gates/g2.py、gates/g3.py、gates/g4/__init__.py、gates/g4/generic.py、gates/g4/worldbuilding.py、gates/g5.py、audit/_shared.py、dispatcher/executor.py、tools/lint_status_strings.py、tests/tiers/deps.json、command-to-give.md、worldbuilding/chapter-drafting/_template 三份 rubric、.venv/bin/shenbi-score 控制台脚本）
- **未覆盖文件列表：空**。14 个 Z1 文件全部覆盖，每文件的每一条可执行分支均已映射到既有 finding 或备查记录项（逐文件核对结论：`scoring.py` 全部 17 条 finding 覆盖校验/过滤/交互/CLI 全链；`phase_runner.py` 21 条覆盖状态机/CLI/穿越/加载边界全链；`safe_write.py` 5 条覆盖并发五站点；`sync_contracts.py` 6 条覆盖生成链/加载/日志；`status.py`/`paths.py`/`exceptions.py`/`error_guidance.py`/`recovery.py`/`logging.py`/`cli_utils.py`/`capability_fs.py`/`__init__.py`/`py.typed` 由死代码/断链/词汇表 finding 全量覆盖）
- **R1 must-test 覆盖空洞仍未落实（测试层面，非代码缺陷）**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达；R14 新增的 G4 特定 checker × T2 project-output 布局端到端空洞亦未补测——行为缺陷已由 F108/F119/F130/F133/F163 覆盖，供合并层转交测试补强。

---

## 6. 其他说明

- 前十五轮"P1×3（F115/F140/F158）+ F163 条件性 P1"结论：本复核独立实跑/代码走查后**无反证，全部维持**；误报数 0（66/66 成立）；无任何前轮 finding 被证伪。
- **本轮零新发现**：Z1 区十六轮复核累计 66 条 finding（F101–F166），无 P0；主要缺陷面（safe_write 并发五站点、scoring 校验/过滤/CLI 全链、phase_runner 状态机/CLI/路径穿越/加载边界、sync_contracts 生成链/日志通道、G2/G4 目录与类型错位、G5 假 PASS 条件、T2/T3 承诺漂移）已完全收敛。后续轮次若继续，预期新 finding 产出趋零（前十五轮产出序列 9/5/6/6/4/1/1/2/6/2/2/3/2/2/2 已呈下行收敛）。
- 跨区一致性核查：F163 与 G6 协议（command-to-give.md:120 显式传 project-output）对照——框架对"输出位置"有两处正确认知、仅 phase_runner G4 调用点一处错位；F165 与 command-to-give.md:126（T3 独立执行）+ scoring t3 分支（check_gate_markers :216-222）对照——scoring 支持 T3 rubric 而 phase_runner 不支持 T3 相位，共同构成 "T2/T3" 字面的半真半假；与 Z5 F500（scoring_bridge dead 链）、Z3 区 pipeline 消费方缺陷无重叠。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140/F126 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本，`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp/zb16_pycache`）**实证退出码与 scored_by；F115/F134/F135/F141/F142/F149/F151/F152/F155 经 `PYTHONPATH=src` 纯内存导入复跑；候选新发现（非字符串 JSON 键）经实测证伪；临时文件仅写 /tmp，未触碰仓库文件。

---

## 7. 返回摘要

- **复核统计：新 finding 0 / 误报数 0 / 严重度异议 0 新增（维持 F109 M→P2 共识、F150 的 R13 M→P2 升级、F115/F140/F158 的 P1、F163 的条件性 P1 定位、F134 的 P2）**
- **0 新 finding 判定：是**（本轮零新发现；前十五轮 66 条 finding 全部成立、无一误报；Z1 区十六轮复核收敛完成）
