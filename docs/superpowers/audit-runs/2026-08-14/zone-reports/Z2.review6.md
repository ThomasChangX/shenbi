# Z2 分区第六轮独立复核报告（Z2.review6）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）
- 模式: fresh-context 全量重读 38 文件；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖），不依赖前六轮转述；另读跨区消费面（gates/cli.py、g1.py、g2.py、g3.py、g3_independence.py、shared.py、g4/{genre_config,pacing_design,score_*,decisions_validator,worldbuilding,book_spine_init}.py、audit/{_shared,snapshot,write_audit,record}.py、records/{parser,drift}.py、pipeline/dispatch_helper.py、scoring.py、safe_write.py、cli_utils.py、round-exec.sh、first-novel.md、truth-files.yaml、相关 SKILL.md、git show 99f91f0:tests/dispatch-subagent.sh）。
2. **独立验证方式**：`COVERAGE_FILE=/tmp/z2r6-cov .venv/bin/python -m pytest -o addopts="" --cov=shenbi.dispatcher --cov=shenbi.contracts ...` → **203 passed**，逐文件覆盖率与五轮报告完全吻合（chapter_planning 34% / context_composing 38% / volume_outlining 40% / executor 74% / codex 75% / genre_config 88% 缺 95-100 / registry 79% / decisions 94% 缺 35,58 / cli 0%）；20+ 个 `python -c`/heredoc 运行时探针（F216/F218/F221/F230/F232/F235/F236/F238/F240/F241/F245/F247/F248/F249 复现 + 反证尝试）。
3. **前六轮判定**：F201-F246 共 **46 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；已有 4 条严重度异议（F214/F218→P1、F217/F228→P1 低置信）经独立反证尝试后**全部支持**；本轮**新增 1 条低置信异议（F238 P2→P1）**并**支持 review5 对 F220 的 P2→M 存疑（建议维持 P2）**。
4. **本轮新发现 3 条（F247-F249）+ M 级观察 4 条**，集中于六轮都未触及的三类盲区：
   - **F247（P1）**：审计链 N 占位符解析不一致 —— `_declared_patterns`（write_audit.py:22-24）以 `chapter=None` 派生声明集（N 路径全丢），而 watch 面（executor.py:220-225 `_audit_watch_paths`）以提取到的 chapter 解析 → **40 个含 N/NNN 写入占位符的技能**（chapter-drafting/全部 review-*/score-*/memory-distill 等）在 prompt 含章节号时，成功派发也被判「未声明写入」→ **rc=2 GATE_FAIL**（端到端复现）；
   - **F248（P2）**：pacing `from_markdown` 两个解析失真 —— `chapter_sequence` 恒空使「不连续 3 章同类型」规则在 g4 门路径死代码；`scene_types` 固定词表子串扫描使散文提及虚增计数 / 词表外类型漏计 → g4 对正常 rhythm_principles.md 误拒（复现）；
   - **F249（P2）**：`executor.run_g2` 把 `str(PROJECT_DIR)`（shenbi 仓库根）当 G2 CLI 第 4 参 project_dir → `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限 10000 退化为 4500 → **4500-10000 字重要章被 G2.7 误拒**（6002 字重要章复现：仓库根接线 FAIL / round_dir 接线 PASS）；PR-20 把 codex cwd 改 round_dir（codex.py:62）后此参数与真实项目位置不一致（与 F235 同源不同面）。

---

## 一、复核结论表（F201-F246 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 三模型无生产消费者（grep 仅 test_skill_integration.py:188 类名文本）；g4 私有实现规则不同（chapter_planning g4 hook ops 存在性 vs 模型 typed change/defer；context_composing g4 P1-P7/route 标题 vs 模型 section_count==9；volume_outlining g4 KR/张力曲线 vs 模型 entity_hooks>=3 且 types>=2）；cov 34/38/40% 复测吻合。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export + g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）；grep 复证。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads（跨区 read 确认）。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g4/worldbuilding.py:31-32 与 g6.py:51 以 raw dict 读 target_words/target_word_count —— 死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证；g4/score_*.py 用 RouteC/RouteA 正则检查而非模型）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无生产加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797（"BLOCKING"/"FAIL"）、:409-438（G4Severity 自建词表）复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有，read 确认）。 |
| F210 | P2 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing（复测 74% 缺 169-188）；g1.py:245-258 与 dispatch_helper.py:172 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知 gate 恒返回 0 → 修复方向应为「检查 rc!=0（CLI 崩溃）或 stdout 空 → 容错」，本体成立。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | P2 | **成立；支持异议→建议 P1** | **反证尝试**（独立路径不跑 G3 故 P2？）不成立——run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → G3.5 prior_agents 恒空 → PASS）→ G3.4 fail-closed 以生产方自写证据放行。 |
| F215 | P2 | **成立** | 运行时复现：`\{[^{}]*\}` 对嵌套 JSON 取内层 `{"num":1,"score":88}`。 |
| F216 | P1 | **成立，P1 维持** | **反证尝试失败**：model_validate(空 customRules + disabled dim) → PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。P1 维持。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | 实测 SKILL.md 内 6 处 CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :187 15-30 / :192 15-25 / :193 30-40 开卷 / :194 15-25）+ docstring 20-30 + 模型 [15,35]；模型对标准卷过松（25-35 过门违反 :91 上限）对开卷过严（36-40 误拒，:193 允许 30-40）。权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | **反证尝试失败**：md 部分匹配 → 仅命中 section、matched=True、无 WARN（fields.py:55-64 实测）；AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 部分缺失时两处均不满足。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282）；executor.py:74-75/:107-108 ContractError 静默降级；`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集复读通过。 |
| F220 | P2 | **成立（计数修正维持：6 条；严重度存疑维持）** | ownership.py:73-94 实测 6 条目（含 foundation-review read 条目）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。**本轮补充**：支持 review5 的 P2→M 存疑——矩阵小系 docstring 明示的过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，建议维持 P2。 |
| F221 | P2 | **成立** | 运行时复现：fixture 9 键含 tropeInventory，model_validate PASS 且 tropeInventory 被静默丢弃（extra=ignore）；SKILL.md:286/:292 声称 8 键；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性）/g1（glob 未匹配）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except。 |
| F227 | P1 | **成立，P1 维持** | **反证尝试失败**：first-novel.md:38-41 文档化流程 = round-exec.sh 建脚手架（progress/meta/summary + 子目录，无 novel.json）后立即 `uv run shenbi-dispatch shenbi-worldbuilding ...` → worldbuilding reads novel.json → G1.1 not found → rc=1；`git show 99f91f0` 原 shell 注释 "the skill execution happened earlier" 证实门序回归（技能执行被并入 G2 之后的 dispatch_codex）。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | derive_output_files 返回 `truth/*.md` 字面（audit/_shared.py:38-56 不展开 glob）；gate_G2 无 glob 展开 → 重试轮仍 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现（全技能枚举另见 shenbi-chapter-revision/short-drafting/market-radar 同型）；g2.py:76-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | P2 | **成立** | 运行时复现：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者。 |
| F232 | P2 | **成立** | 运行时复现：`Selection(severity="high", basis="manual_override", rationale="")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | P2 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | P1 | **成立，P1 维持** | **运行时端到端复现**：PROJECT_DIR 保持真实仓库根（不 monkeypatch），fake dispatch rc=0 → `violations=['未声明写入: truth/bridge_tracker.md','未声明写入: truth/character_matrix.md']`、rc=2、ledger blocked=True；`git ls-files truth/` 确认两文件 tracked；test_executor_audit.py:18,40 把 `ex.PROJECT_DIR = tmp_path`（==round_dir）编码了正确语义。**反证尝试**（审计面意图为仓库根）不成立——codex.py:62 cwd 已改 round_dir，审计根未跟随。 |
| F236 | P2 | **成立** | 运行时复现：`_matches_declared("truth/bridge_tracker.md", ["truth/*.md"], parametric_globs())` → False（直接 fnmatch → True）；`"truth/*.md" in parametric_globs()` → False（truth/*.md 是 registry glob 非 parametric）。 |
| F237 | P2 | **成立** | 运行时复现：`compute_file_change("genre-config.json", None, '{"hacked": true}')` → `status='added', changed_top_keys=()` → check_write_ownership → `[]`。 |
| F238 | P2 | **成立；新提低置信异议（P2→P1）见第三节** | malformed `## hooks` YAML → audit_writes 裸 ParserError/ValueError 崩溃（executor.py:263-268 finally 无捕获，record_audit_outcome 永不执行 → write-audit.jsonl 无 GATE_FAIL）——executor.py:245-246 文档化保证"崩溃仍审计"被违反。 |
| F239 | P2 | **成立** | 运行时复现：derive_file_type worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归）；state-settling/plant → "truth"、chapter-drafting → "decisions"。 |
| F240 | P2 | **成立** | 运行时复现：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | P2 | **成立** | 运行时复现：①`approval:{}` → PASS；②禁用非 list → 计数规则跳过；③替换建议非 dict → 替换规则跳过（`if decision and ...`/isinstance 守卫）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`（get_ownership → None）；plant/track SKILL.md:29-30/:18-19 自述 DEPRECATED "Do not dispatch"。 |
| F243 | P2 | **成立** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（原第 4 参为独立评分 prompt）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator；scoring.py:441 `--subagent` 仅写 provenance 字符串；scoring.py:422-432 REJECT exit 2 复读确认。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:289-313 G2.11 对 **truth 输出**找 .bak；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | P2 | **成立** | **运行时复现**：resolve_chapter_path(volume-N/arc-N/stratum-N/AC-NNN, None) 全抛 UnresolvedPathError；`_build_skill_prompt("shenbi-score-volume", tmp_pd, "...", None)` → `UnresolvedPathError: audits/volume-N-score.md`（score-volume/review-arc-payoff/memory-distill/score-stratum 四者均崩）；resolve_volume_path 仅 closure.py:20,158 消费。 |
| F246 | P2 | **成立** | dispatch_helper.py:1864 legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key / 无 IDE CLI 时默认路径）→ pipeline 继承 F227（fresh 轮 G1 失败）/F235（审计 rc=2）缺陷；read 复证 + F235 探针组合。 |

**误报统计**：整体误报 **0 条**（F201-F246 共 46 条全部实质成立）；部分误报 1 条（F220 计数 5→6，实质成立）；表述修正 1 条（F211，实质成立）；F212 附注 1 条（修复方向，本体成立）。

---

## 二、新发现（F247-F249）

### F247 | 审计链 N 占位符解析不一致：watch 面解析 N 而 declared 面不解析 → 40 个 N 写技能成功派发恒被判「未声明写入」rc=2 GATE_FAIL | 漏报（正常路径功能错误/审计失效） | P1
- 证据: `src/shenbi/audit/write_audit.py:19-26`（`_declared_patterns(skill)` → `derive_output_files(skill)` **无 chapter 参数** → `resolve_or_skip` 丢弃全部 N/NNN 路径 → 声明集为空）；`src/shenbi/dispatcher/executor.py:220-225`（`_audit_watch_paths(skill, chapter)` **带 chapter** → 解析出 `chapters/chapter-7.md` 等具体路径）、`:242-243`（`chapter = extract_chapter(prompt)`）；`write_audit.py:48-64`（对 watch 面每一 rel（含 pre=post=None 的不存在路径）跑 `_matches_declared`，declared 为空 → 恒「未声明写入」）
- 根因: 审计链内部两处对契约写入面做**不同粒度的占位符解析**：watch 面（executor）用 prompt 提取的 chapter 解析 N，声明面（write_audit._declared_patterns）不解析 → 对含 N/NNN 写入占位符的技能（枚举 **40 个**：chapter-drafting/anti-detect/length-normalizing/style-polishing/sequel-writing/short-drafting/chapter-planning/chapter-revision/context-composing/全部 review-*/score-*/escalation-review/memory-distill/anchor-curate/snapshot-manage 等），只要 prompt 含可提取章节号，任何成功派发（含 dispatch rc=0、零写入）都产生「未声明写入: chapters/chapter-7.md」等违规 → rc=2 GATE_FAIL。与 F235（审计根=仓库根）正交：F235 修根后本缺陷仍在；与 F236（glob 不 fnmatch）也正交（N 路径整个不在声明集中，非匹配问题）。
- 验证命令: `PYTHONPATH=src .venv/bin/python`（真实 REPO_ROOT，fake dispatch rc=0，tmp round）→ `dispatch_with_write_audit("shenbi-chapter-drafting", "generative", tmp, "Draft chapter 7")` → `rc = 2`、`violations=['未声明写入: chapters/chapter-7-decisions.json（…）','未声明写入: chapters/chapter-7.md（…）']`；`shenbi-context-composing` 同 → rc=2（`context/chapter-7-context-decisions.json`）；`_declared_patterns("shenbi-chapter-drafting") == []` vs `_audit_watch_paths("shenbi-chapter-drafting", 7) == ['chapters/chapter-7.md', 'chapters/chapter-7-decisions.json']`
- 建议方向: `_declared_patterns` 增加 chapter 参数并在 audit 入口与 watch 共用同一 `derive_output_files(skill, chapter)`（或让 audit_writes 接收与 watch 相同的 resolved 声明集）；补一条 N 写技能端到端审计测试（现 test_executor_audit.py 全用 genre-config.json 非 N 路径，未暴露）
- 影响注记: fresh 轮被 F227（G1 先死）掩盖；重试轮（G1/G2 过）真实触发 rc=2。F227/F235 修复后本缺陷独立阻塞 40 技能的 standalone 重试路径——建议届时重评（若按 F235 同级可维持 P1）

### F248 | pacing_design.from_markdown 解析失真：chapter_sequence 恒空使「不连续 3 章同类型」规则在 g4 门路径死代码；scene_types 固定词表子串扫描致 g4 误拒/虚增 | 漏报（校验洞+误拒） | P2
- 证据: `src/shenbi/contracts/skills/pacing_design.py:77-130`（`from_markdown` 对 `chapter_sequence` 恒赋 `[]`，`:129`；`scene_types` 以硬编码 `known_types`（21 个英/中词）对全文子串扫描，`:96-123`）；`src/shenbi/gates/g4/pacing_design.py:47`（唯一生产消费 = `PacingDesign.from_markdown(content)`）；SKILL.md:69/110（"场景类型不重复超 3 章"铁律）、SKILL.md:97（"至少定义 6-8 种场景类型"）
- 根因: 模型把「连续 3 章同类型」规则编码进 `_no_three_consecutive_same`（pacing_design.py:66-74），但唯一解析入口 `from_markdown` 从不填充 `chapter_sequence` → 该规则在门路径**永久死代码**（可检查规则未执行 = 校验洞，F216 同类）；`scene_types` 依赖固定词表子串存在性——散文正文提及（"battle"/"探索"）虚增计数（false PASS），词表外类型（如「权谋」「复仇」）漏计 → `_eight_scene_types`（6-12）对正常文件误拒（false FAIL，复现：正文仅含 5 个词表类型 → ValidationError "expected 6-12 scene types, got 5"）
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `PacingDesign.from_markdown(md).chapter_sequence == []`；含 "连续 3 章战斗" 文本的 md → model_validate PASS（规则死）；仅 5 个词表类型的 md → `ValueError: expected 6-12 scene types, got 5`
- 建议方向: `from_markdown` 从 rhythm_principles.md 的章节序列段落/表格解析 `chapter_sequence`（或显式文档声明该规则仅手工构造时生效）；scene_types 解析改为表结构解析（`## 场景类型` 表行）而非全文子串；补 g4 pacing 误拒/死规则测试
- 影响注记: g4 pacing-design 是正常路径门（story-architecture 产出 rhythm_principles.md 骨架后由 pacing-design 细化）——误拒直接阻塞 T1 层进；死规则使「防单调」检查空转

### F249 | executor.run_g2 把 PROJECT_DIR（shenbi 仓库根）当 G2 project_dir 传入 → _is_important_chapter 恒 False → 重要章 4500-10000 字被 G2.7 误拒 | 漏报（正常路径误拒，跨区接线） | P2
- 证据: `src/shenbi/dispatcher/executor.py:122-139`（run_g2 CLI 第 4 参 `str(PROJECT_DIR)`）；`gates/cli.py:90-96`（`pd = arg(3, None)` → `gate_G2(files, ftype, rd, pd)`）；`src/shenbi/gates/g2.py:237-238,382-413`（`_is_important_chapter(fp, project_dir)` 查 `project_dir/outline/volume_map.md` + `project_dir/plans/chapter-N-plan.md`；`ceiling = CHAPTER_WORD_CEILING if is_important else int(FLOOR*1.5)` = 10000 vs 4500）；仓库根无 outline/、plans/（实测）；`git show 99f91f0:tests/dispatch-subagent.sh:85,133`（原 shell 亦传 PROJECT_DIR，但 codex 以 `-C "${PROJECT_DIR}"` 执行——项目即仓库根，语义自洽）；Python 翻译把 codex cwd 改为 round_dir（codex.py:62）而 G2 参数未跟随 → **项目位置与重要章检测位置分叉**
- 根因: PR-20 的 cwd 迁移（codex → round_dir）只改了一处；G2 的 project_dir 参数仍指向 shenbi 仓库根 → standalone 流程中 volume_map.md/plans 的实际位置（round_dir）与检测位置（仓库根）不一致 → 重要章永不识别 → 上限恒 4500
- 验证命令: `PYTHONPATH=src .venv/bin/python`（6002 字重要章，round_dir/outline/volume_map.md 标注"第7章 高潮"）→ `gate_G2([ch7.md], "chapter", rd, REPO_ROOT)` → `status=FAIL must_fix=['G2.7:...']`（ceiling 4500, is_important False）；`gate_G2([ch7.md], "chapter", rd, rd)` → `status=PASS`（ceiling 10000, is_important True）
- 建议方向: run_g2 第 4 参改传 `str(round_dir)`（与 codex cwd/G1 round_dir 一致）；或 gate_G2 对相对 project_dir 场景从 round_dir 派生；与 F235（审计根）一起在 PR-20 迁移清单中统一"项目根"语义
- 影响注记: fresh 轮被 F227 掩盖、重试轮被 F247 掩盖（rc=2 先于 G2 结果生效）；两者修复后本缺陷独立阻塞 >4500 字章节（含重要章）的 standalone 正常路径——建议届时重评

### M 级观察（随文记录）
- `codex.py:70-72,111-112` rc 直传语义污染：shenbi-score REJECT（scoring.py:432 exit 2）与 codex exec rc 原样返回，而 `dispatch_with_write_audit` docstring（executor.py:232）声明 "2 = GATE_FAIL (write overreach or drift)"——调用方无法区分「评分拒绝」与「写越权 GATE_FAIL」（M，F212 附注关联）
- `cli.py:25-28` 无短名归一化：gates/cli.py SHORT_MAP（:32-53）对 G4 生效，但 shenbi-dispatch 把 argv[1] 原样传 dispatch → 传短名（如 `worldbuilding`）时 load_contract 静默失败 → derive_file_type 误归 "chapter"、G1 无备份、rubric 路径缺失——与 G4 短名支持不一致（M）
- `docs/getting-started/first-novel.md:31-36` Step-1 worldbuilding 写入清单（9 文件含 world/power_system.md、world/factions.md、characters/*、outline/*）与 SKILL.md 契约（writes: novel.json, genre-config.json, world/{story_bible,rules,locations}.md, truth/*.md）内容漂移（M，文档↔契约）
- `executor.py:79-80` derive_file_type EPHEMERAL 分支为死代码（当前 69 技能无 kind=ephemeral，实测枚举为空）（M）

---

## 三、严重度异议（不修改前六轮，仅提理由）

1. **支持 F214 P2 → P1**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → G3.5 恒 PASS）→ G3.4 fail-closed 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。本轮反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持 F218 P2 → P1**：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 实测）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——权威区间未定（SKILL.md 内部 6 处自相矛盾），维持低置信。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **新提（低置信）F238 P2 → P1**：executor.py:245-246 文档化保证 "if dispatch() crashes mid-write, still run the post-snapshot + audit"——`audit_writes` 对 malformed `## hooks` YAML 裸抛（F238 复现）发生在 finally 内，使 `record_audit_outcome` 永不执行（write-audit.jsonl 无 GATE_FAIL 记录）——写越权保护在"仅需 LLM 产出 malformed YAML"的正常概率路径上静默消失（审计**记录**丢失；崩溃本身是 loud 的，故降为低置信，若协调者认为"审计保证失效"即 P1 则升）。
6. **F220 严重度存疑支持（非新异议）**：矩阵规模小系 docstring 明示过渡态；但 F236 使 glob 写者 file-level 兜底失效 → 过渡态非零风险，建议维持 P2（不降 M）。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`COVERAGE_FILE=/tmp/z2r6-cov .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" --cov=shenbi.dispatcher --cov=shenbi.contracts --cov-report=term-missing tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**；逐文件覆盖率与六轮报告一致：chapter_planning 34%、context_composing 38%、volume_outlining 40%、executor 74%（缺 169-188 即 F211/F222 区域、251-262 must-test）、codex 75%、genre_config 88%（缺 95-100 = F216 规则 7 路径零测试）、registry 79%（缺 73-75 known_skill_names must-test）、decisions 94%（缺 35/58 rationale>100 must-test）、cli 0%（F226 区域）。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 model_validate(空 customRules + disabled dim) → PASS（bug 复现）
  - F218 md 部分匹配 → 仅命中 section、matched=True、无 WARN
  - F221 fixture 9 键 model_validate PASS + tropeInventory 静默丢弃；SKILL.md:286/:292 "8 键" 复证
  - F230 json 键规范化缺失 → matched=False + 全文 + WARN（U+3000 声明键）
  - F232 Selection(high/manual_override, rationale="") → ACCEPTED
  - F235 真实仓库根 + fake dispatch rc=0 → rc=2、violations 为 bridge_tracker/character_matrix、ledger blocked=True
  - F236 `_matches_declared` glob False / 直接 fnmatch True / parametric_globs 无 glob 键
  - F238 audit_writes malformed `## hooks` → ParserError 崩溃、ledger 无记录
  - F240 extract_chapter 中文 → None / 英文 → 7
  - F241 genre_config ①空 approval PASS ②禁用非 list PASS
  - F245 `_build_skill_prompt(score-volume, ..., None)` → UnresolvedPathError: audits/volume-N-score.md；resolve_chapter_path 四类占位符全抛
  - **F247** dispatch_with_write_audit(chapter-drafting, "Draft chapter 7") → rc=2 + 未声明写入 violations（N 解析不一致端到端）
  - **F248** from_markdown chapter_sequence 恒空（3 连续同型 PASS）+ scene_types 子串启发（5 类型误拒）
  - **F249** 6002 字重要章：project_dir=REPO_ROOT → G2.7 FAIL / project_dir=round_dir → PASS
  - F242 lifecycle get_ownership → None、plant/track DEPRECATED 复证；F210 hooks 行号漂移复证；F217 SKILL.md 6 处区间复证
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243 的 cwd/角色差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c。

---

## 五、统计

- 新 finding：**3 条**（F247-F249：P1×1、P2×2）；M 级观察 4 条随文
- 误报：**0 条整体误报**（F201-F246 共 46 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注维持）
- 严重度异议：**5 条**（支持已有 4 条：F214→P1、F218→P1、F217→P1 低置信、F228→P1 低置信；**新提 1 条低置信：F238 P2→P1**）；另 F220 严重度存疑支持 1 条（建议维持 P2）
- 前六轮 findings 判定：F201-F246 共 46 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否**（F247-F249 共 3 条新发现；F247 为独立根因的审计链 N 占位符解析不一致，40 个 N 写技能成功派发恒 rc=2 GATE_FAIL，F227/F235 修复后需重评严重度）
