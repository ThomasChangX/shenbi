# Z3 分区第九轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第九轮独立复核（本文件 `Z3.review9.md`，与初审 `Z3.a.md`、一轮 `Z3.review.md` 至八轮 `Z3.review8.md` 全部隔离）
> 复核范围：34/34 文件全量逐行重读（`__init__.py` 1 行 → `chapter_loop.py` 3053 行 → `dispatch_helper.py` 1986 行 → `cli.py` 939 行 → `triggers.py` 656 行全部读完），逐条独立验证 F300-F383；以真实生产项目 `novel-output/xinghuo-ranqiong/` 为格式/行为对照基准
> 本轮发现编号：F384-F390（7 条新发现，其中 0 条 P0/P1、6 条 P2、1 条 M）；**新增误报 0 条**；严重度异议 2 条（F302/F364 的 P1→P0 依据，均为此前已提出、本轮独立复证支持）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证全部在 /tmp 临时目录与只读 python 中完成）
> 方法：34 文件逐行重读 + grep/uv run python/契约 load_contract/G4-CLI/沙箱进程退出实验 + 真实产物对照（详见各 finding 验证节）

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`、`_count_total_chapters → 0`、`is_volume_boundary(15) → False`。生产 volume_map 为全中文格式（`## 第一卷：觉醒之火（第1-15章）`、`**章节范围**: 第1章 - 第15章（共15章）`），`_END_RE`/`_RANGE_RE` 均 0 命中（本轮实测）。novel.json 无 total_chapters（56 章完成仍在 chapter-loop、closure_step=0）。
2. **F325（误报）维持撤销**：本轮实测 4 个 group skill 契约 `writes:` 即旧式路径（`group-factual → ['audits/chapter-N-continuity.md','audits/chapter-N-world-rules.md','audits/chapter-N-pacing.md']` 等，load_contract 实证）；生产 audits/ 722 文件全为旧式名、`group-*` 数=0；`_any_audit_has_findings`（chapter_loop.py:1779-1793）扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING 被 route 正则与文件扫描双重命中 → revision 执行。
3. **F364（P1 贴 P0）维持成立，本轮 /tmp 沙箱独立复现**：`register_emergency_handlers` → `atexit.register(_emergency_cleanup)`（crash_recovery.py:66），正常进程退出即无条件 `clear_staging`（:144-148）+ 把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`（:113-127）。本轮实测：`staging before exit: True` → 退出后 `staging` 目录整体消失、`pipeline-state.json` 的 current_step=`EMERGENCY_SHUTDOWN_AT_shenbi-chapter-planning`（无任何信号、无异常，纯正常退出）。
4. **F379/F380 本轮以全步骤 prompt-build 执行实证**：closure 10 步中 step 2/4/5/6/10 抛 `UnresolvedPathError`（5/10 步永不派发）；genesis step 16（anchor-curate，optional）同样抛错并被 optional-skip 静默吞掉。
5. **F371/F373 本轮 G4 CLI 实证**：`final-snapshot/` 目录 → `G4.gen.read_error`（目录存在）/`not_found`（不存在）恒 FAIL；`audits/arc-N-score.md` → `G4.not_found` FAIL / `audits/arc-5-score.md` → PASS。
6. **本轮最重要新发现（F389，P2）**：linguistic drift 的**数据源侧全链路 inert**——`_check_linguistic_drift` 读 `style/linguistic_baseline.json`（chapter_loop.py:2014），而该文件的唯一写入方 `establish_baseline`（baseline.py:24-78）**全仓 0 调用方**（grep 实证）→ 每章 drift 检查恒 `no_linguistic_baseline` → `return None` → spec §3.4 三级干预（WARN/HARD/ESCALATE）与 DriftEscalationError **从未可达**（F358 只覆盖了 ESCALATE 被吞与指令文件无消费者，但整个链条在基线缺失处更早断裂）。
7. **版本偏移注记（沿用三~八轮）**：生产 pipeline-state 为 Plan 18 重构前旧版管线产物（steps_done 含旧 skill 名），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据；staging/ 119 个文件为旧版遗留（旧版无 atexit 清理）。

---

## 一、复核结论总表（F300-F383 逐条判定）

> 判定基于本轮独立 read/grep/uv run python/G4-CLI/沙箱实验与真实产物对照。全部 34 文件逐行重读。**误报 1 条（F325，已撤销维持）**；**事实修正 2 条（F341/F358 的机制前置，见对应行）**。

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")` 源码字面量求值即 `<` 恒 no-op（本轮 `python3 -c` 实证 True） |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 chapter_loop.py:2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1`(14) → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析）不可达；cs.resonance_score 并行路径恒 None |
| F302 | **成立** | P1 维持（**严重度异议支持 P1→P0 依据，裁定留 stage 4**） | dispatch_skill(state=) 仅 chapter_loop.py:2799（串行可及）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751 均未传；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0（PipelineState 无 chapter 属性，本轮读 state.py 确认） |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（`_snapshot_chapter_files` 死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError；machine.clear_checkpoint 的 ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93） |
| F305 | **成立但框定需修正（同意二~八轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文（本轮实测 chapter-10-character.md:179 "不构成 BLOCKING"、:184 "未发现 OOC/降智/BLOCKING 问题"）→ `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 记账漂移 |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方；:97 `Chapter {chapter}` 子串缺陷；volume_context 无消费方（_INJECT_FROM_CACHE 只注入 4 字段，:601-621） |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用；`_parse_hook_entries`（hook_planting.py:144）的 `^(MH|H)-` 对生产 plan section 7 的 MH-001/H-N01 行会接受但 raw_op=="advance" 非 plant/open 被跳过（:151） |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state（state.py:383-428）无生产调用（grep 实证） |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:428-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方 |
| F313 | **成立** | P1 维持，无异议 | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（契约 writes 实证）→ G4 校验错位。注：API/IDE 路径下 step 6 在派发阶段即失败（F379），F313 的 G4 失效面在 legacy 路径与 F379 修复后激活 |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback 无章号绑定、注入下一次串行 dispatch（:2775-2780）；同源缺口见 F365 |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用（grep 实证；generate_review_checklist 本体已在 dispatch_helper.py:767 接线）；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符 |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`、`is_volume_boundary(15) → False`、`_count_total_chapters → 0`；`_END_RE`/`_RANGE_RE` 对生产 `**章节范围**: 第1章 - 第15章（共15章）` 零命中。影响链（卷边界触发全家 / total_chapters=None → cli.py:219 `if total > 0:` 跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 章节节点提取裸数字 `\|\s*1\s*\|` 误中跨卷钩子表行）全部成立。**修复完整性注记**：即使解析器修复，`_resolve_volume_at_runtime` 返回的英文卷名 "Volume {i}"（_shared.py:93）与生产中文标题 `## 第一卷：` 仍不匹配，context_assemble.py:226-228 的 `vol_heading_pattern` 与 audit_context_cache 的章节匹配仍会失效——F324 修复需同步调整消费者侧的格式假设 |
| **F325** | **不成立（误报，维持二~八轮撤销）** | **P1 裁定撤回维持** | 本轮实测 4 个 group skill 契约 writes 即旧式路径（load_contract 实证）；生产 audits/ 722 文件全旧式名、group-* 数=0；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物；真实 BLOCKING 被 collect_audit_issues 正则命中 → route≠NO_REVISION → `_any_audit_has_findings` 同文件必 True → revision 执行。残余真值（P2）：清单缺 resonance/review-summary、含生产不产生的 texture/reader-pull |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证）；并行 post-draft（chapter_loop.py:2405-2419）两线程均以 uses_staging 写 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用（本轮 grep 实证）；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）；四轮补充成立：`_parse_resonance_score` 三模式均不匹配生产 `**结果**: 通过 (77/100)` 格式（本轮实测 bold/plain 两模式对 production chapter-10-resonance.md 均返回 None） |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代（grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议；**注记：完整重复面见 F387** | 本轮实测 production genre-config auditDimensions.sensitivity=true → `get_active_genre_audits` 含 shenbi-review-sensitivity；core 波（:2551，含 step 14 shenbi-review-sensitivity）与 genre 波双发同一 skill 写同一文件 `audits/chapter-N-sensitivity.md` |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库（该路径被 F364 清空后实际"静默不入库"） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless）；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 补零数=0；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；**本轮生产实证**：`audits/chapter-1-review-summary.md` 内容为 "**BLOCKING Issues**: 0 / No BLOCKING or CRITICAL issues found"（与实际审计文件中的 "不构成 BLOCKING" 否定式并存） |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死 |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 配置在全部路径上均无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用（grep 实证） |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:24-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；6 个 audit skill 全部 requires_independent=True（实证）；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 `MH-001`/`H-N01` 零匹配（本轮实测）→ master hooks 从 Route A 检索静默缺失；`MH02 → ['H02']` 误截成立 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys 无 `hooks` 键（hook 以 body 表格呈现，本轮实测 79 个 P0-* 命中）；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)"；`_read_spine_master_hooks` 的 `\| MH` 行兜底对生产 book_spine.md 零命中 |
| F348 | **成立但机制描述部分不准（维持四~八轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入（load_contract 实证）；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md 生产均 MISSING）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层；非合规输出先失败一次再重试 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout（:1527/:1731/:1855） |
| F353 | **成立** | P1 维持，无异议（**注记：贴 P0 边界**——与 F324 同为"pipeline 静默永不完成"的独立根因，且为自锁：写点均前置依赖自身字段） | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 writes 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；grep skills/ 契约 reads 0 个引用该文件（本轮实测生产 context/ 下 curated 文件数=0）；drafting 契约 reads `context/chapter-N-context.md` → 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断）每章每审计恒注入（幽灵输入 + token 浪费） |
| F358 | **成立但机制前置需修正** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError（:2034 raise）；drift-correction/warning/opening-variation 指令文件无任何消费者。**本轮修正**：该机制链在更早处已断——`_check_linguistic_drift` 恒因 `style/linguistic_baseline.json` 缺失返回 None（见新发现 F389），ESCALATE raise 与指令文件写入在生产**当前不可达**；F358 的"吞异常"描述是 F389 修复后才会激活的次生面 |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra 被丢弃；drafting 契约 reads 不含 characters/*.md、world/rules.md → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存，无写盘副作用）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358（CLI main） |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |
| F364 | **成立** | **P1 维持；严重度异议支持 P1→P0（依据决策表"数据损坏/丢失 + 契约被静默违反"字面命中；裁定留 stage 4）** | 本轮 /tmp 沙箱独立复现：`register_emergency_handlers` 后正常退出 → staging 被 atexit 清空（`staging before exit: True` → 退出后 staging 目录整体消失）+ pipeline-state.json 的 current_step 被改写为 `EMERGENCY_SHUTDOWN_AT_shenbi-chapter-planning`（无信号、无异常）；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交）成立 |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交 + 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`——step 2 时当章 plan 尚不存在 → FileNotFoundError → 捕获 → `_write_minimal_context_fallback`；且 chapter-planning 契约 reads 不含 context/chapter-N-context.md → 组装成功也不被消费 |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格 → 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |
| F371 | **成立** | P1 维持，无异议（注意 F379 交互） | 本轮 G4 CLI 实测：`shenbi-snapshot-manage "final-snapshot/"`（目录已存在）→ `G4.gen.read_error:final-snapshot/` FAIL（不存在则 not_found）——两态恒 FAIL。`_resolve_closure_g4_path`（closure.py:148-159）对无 N 路径原样返回目录 → closure step 10 重试×3 后 ESCALATION。API/IDE 路径下 step 10 在派发阶段即失败（F379），本 finding 的 G4 失效面在 legacy 路径与 F379 修复后激活 |
| F372 | **成立** | P2 维持（latent），无异议 | 本轮 /tmp 实测：2 真实章 + 1 个 chapter-1-pre-rev.md → `_style_profile_is_stale()=True`（应为 False）；去掉 pre-rev → False。`_style_profile_is_stale`（triggers.py:103-124）`glob("chapter-*.md")` 计数把 pre-rev 备份计入 |
| F373 | **成立** | P1 维持（潜伏），无异议 | 本轮 G4 CLI 实测：`shenbi-score-arc "audits/arc-N-score.md"` → `G4.not_found:audits/arc-N-score.md` FAIL；同内容 `audits/arc-5-score.md` → PASS。`run_triggered_skills`（triggers.py:584）把字面 N 路径直接传 G4，dispatch 侧经 `resolve_chapter_path` 解析为章号 → 5 个 N 型触发步骤 G4 恒 not_found |
| F374 | **成立** | P2 维持，无异议 | 本轮实测 `classify_skill_write_safety('shenbi-review-resonance').value → read_only_audit`（write_safety.py:45-46 前缀分类），而契约 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`（load_contract 实证）→ 并行审计波在无串行保护下并发写 truth 文件 |
| F375 | **成立** | P2 维持，无异议 | dispatch_helper.py:1667-1671 `missing` 非空仅 log.error 后 `return DispatchResult(True, ...)`；`_write_parsed_outputs`（:1153-1168）对 parsed 缺失路径走空内容 skip → state-settling 6+1 文件漏写其一仍 success，G4 只校验现存文件 |
| F376 | **成立** | M 维持，无异议 | truth_embed.py:243-251 `update` 无 `--text` 时落 `emit_json(OK)` 不做任何嵌入 |
| F377 | **成立** | M 维持，无异议 | llm_output_integrity.py:51 `VERDICT_MARKERS` 含通用词 "通过"，:149 `has_verdict` 恒 True → `G4.ac.no_verdict`（:150-153）几乎不触发 |
| F378 | **成立** | P2 维持，无异议 | 本轮实测：`context/volume-N-complete.json` 仅 chapter_loop.py:959/963 读取、全仓 0 写入方 → `_check_volume_completion` 恒 False；`check_escalation`（skill_utils/escalation/check.py:88-93）对 `not volume_objective_met` 无条件发 volume_objective_missed 信号；`last_trigger_failure` 仅 triggers.py:575/593 写入、无消费者 |
| F379 | **成立** | P1 维持，无异议 | 本轮全步骤 prompt-build 执行实证：closure step 2/4/5/6/10 抛 `UnresolvedPathError`（truth/arcs/arc-N.md、audits/volume-N-score.md、audits/volume-N-payoff.md、audits/chapter-N-long-span.md、snapshots/chapter-NNN/*）；`extract_chapter('Execute ... for book closure (step 2)...') → None`（对照 trigger prompt → 5）；`_dispatch_via_api`/`_dispatch_via_ide` 在 :1515-1516/:1718-1719 捕获后返回 DispatchResult(False,"Prompt build failed") → `_handle_closure_failure` 重试×3 → ESCALATION——10 步中 5 步永不派发 |
| F380 | **成立** | P2 维持，无异议 | 本轮全步骤 prompt-build 实证：genesis step 16（shenbi-anchor-curate，optional=True）抛 `UnresolvedPathError: benchmarks/anchors/AC-NNN.md`；`_handle_failure`（genesis.py:222-225）对 optional 步骤直接跳过 → anchors 永不产出且无错误信号 |
| F381 | **成立** | P2 维持，无异议 | 本轮复现：`chapter-3-revision-decisions.json` 存在 + `revision_count=0` → `heal_state_counters` 返回 `['revision_count_healed:ch3:0->1']`。`_ensure_revision_decisions_exists`（chapter_loop.py:2755-2760 对 NO_REVISION 章节也写回退文件）与 `_is_revision_routed`（:1928-1933 对 'no-revision' 返回 True）使全部未修订章节的 revision_count 被 heal 抬到 ≥1 |
| F382 | **成立** | P2 维持，无异议 | 本轮 grep：`safe_write(` 于 src/shenbi/pipeline/*.py 中 `round_dir` 命中数=0；`MARK_DONE`/`TraceWriter` 于 pipeline 仅注释；trace 目录只有 materialize 自造 MATERIALIZE 事件 → progress.json 物化为"全 pending"失真视图 |
| F383 | **成立** | P2 维持（latent/条件触发），无异议 | dispatch_helper.py:903-922 `_parse_file_outputs` 无 `### FILE:` 匹配时返回 `{"__stdout__": response}`；:1159 `content = parsed.get(rel_path, parsed.get("__stdout__", ""))` 把整段 stdout 写入每个字面输出路径；`_dispatch_via_ide`（:1753-1759）以 parsed=None 调用（IDE 路径在本环境激活——本轮 `which codex → /opt/homebrew/bin/codex`） |

**误报统计**：F300-F383 中仅 F325 为误报（二轮撤销、三~八轮维持，本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：2 条（均为既有异议的独立复证支持，无新增）：
1. **F364（P1→P0）**：决策表 P0 触发条件"数据损坏/丢失 + 契约被静默违反"字面命中——人审通过的 plan/truth 在每次正常退出时被确定性清空、持久化状态被改写为 EMERGENCY_SHUTDOWN（本轮沙箱复现），属确定性数据丢失；但 staging 门控由人工 review 兜底、正文文件不丢，P1 亦可辩护——裁定留 stage 4。
2. **F302（P1→P0）**：决策表 P0 例字面列明 "TokenLedger 少计"——账本系统性少计 + chapter 恒 0；但账本为成本侧车不影响正文执行，P1 亦可辩护——裁定留 stage 4。
另注记（非正式异议）：F353 贴 P0 边界（与 F324 同为"pipeline 静默永不完成"独立根因，自锁性质）；F387（本轮新发现）贴 P1 边界（决策表"显著 token 浪费"，与 F329 同机制故裁定 P2 保持一致）。

---

## 二、新发现（F384-F390）

### F384 | bge-large-zh SentenceTransformer 模型在每次 embed/检索调用中重复加载：genesis 每步全量重嵌（每 hook/rule 一次全新模型加载）+ 每章 context assembly 两次加载 | optimization | P2

- 证据：
  - `truth_embed.py:120-121`（`embed_and_store` 函数体内 `model = st.SentenceTransformer("bge-large-zh")`——每次调用重新加载 ~1.3GB 模型）；
  - `genesis.py:162-175`（`_update_route_b` 对 `index.hooks` 全部条目与 `index.rules` 全部条目循环调 `embed_and_store`）→ `genesis.py:351-352`（每个 `_INDEX_UPDATE_SKILLS` 步骤成功后调 `_update_indexes` → `_update_route_b`，15 个步骤各一次）；
  - `context_assemble.py:155-157`（`_route_b` 同样每次加载模型，每章 context assembly 调用 2 次（step 2 与 step 3））。
- 根因：模型实例未做模块级缓存/单例；`embed_and_store` 把"加载模型"与"嵌入单条文本"耦合在同一个函数里。
- 验证命令+输出：
  ```
  uv run python -c "from pathlib import Path; from shenbi.pipeline.truth_index import build_index; idx = build_index(Path('novel-output/xinghuo-ranqiong')); print(len(idx.hooks), len(idx.rules))"
  # → 7 10  （单次 genesis 索引刷新即 17 次 embed_and_store 调用 = 17 次模型加载）
  # 15 个索引更新步骤 × 17 次 ≈ 255 次 bge-large-zh 完整加载/次 genesis（模型可用时）
  ```
- 影响：嵌入模型可用（`uv sync --extra embeddings`）时，genesis 全流程重复加载模型约 255 次（每次 10s-2min 级）、每章 context assembly 额外 2 次加载——纯 wall-clock 浪费，无数据错误；当前环境 sentence_transformers 未装（降级路径），属潜伏性能缺陷。
- 建议方向：模块级 `lru_cache` 单例化模型（或传 `model` 参数复用）；`embed_and_store` 拆分 `load_model()`/`embed(text, model)`。

### F385 | dispatch_skill 第三路由（legacy `shenbi-dispatch` 子进程）为必失败死端：internal 模式硬拒 + uses_staging 被忽略 | error | P2

- 证据：
  - `dispatch_helper.py:1832-1841`（路由：无 API key 且 `_find_ide_cli()` 为空 → 走 legacy 子进程）；
  - `dispatch_helper.py:1844-1880`（legacy 路径 `uses_staging` 仅 `log.warning("legacy_dispatch_ignores_staging")` 后忽略；spawn `uv run shenbi-dispatch skill generative rd prompt`）；
  - `dispatcher/executor.py:142-152`（`detect_mode()` 仅检查 `codex`，否则返回 `"internal"`）；`dispatcher/modes/internal.py:14-24`（`dispatch_internal` 直接 `raise DispatcherError("internal mode has no LLM backend...")`）；`dispatcher/executor.py:228-271`（`dispatch_with_write_audit` 捕获后 re-raise → 子进程非零退出 → `DispatchResult(False, rc, ...)`）。
- 根因：路由表把"legacy 子进程"列为可用 fallback，但该子进程在任何可达配置下都失败：(a) 有 codex 时 `dispatch_skill` 已优先走 IDE 路径，子进程不被调用；(b) 无 codex 时子进程 internal 模式硬拒。即使未来 internal 模式被修复，`uses_staging` 被忽略会使 staging 步骤的 G4 校验（`_resolve_g4_path` 返回 `staging/plans/chapter-N-plan.md`）恒 `not_found` → HARD fail → 重试 → ESCALATION。
- 验证命令+输出：
  ```
  grep -n "legacy_dispatch_ignores_staging" src/shenbi/pipeline/dispatch_helper.py  # → :1845
  grep -n "raise DispatcherError" src/shenbi/dispatcher/modes/internal.py           # → :22（internal 模式硬拒）
  grep -n "return \"internal\"" src/shenbi/dispatcher/executor.py                   # → :152（无 codex 时恒 internal）
  ```
- 影响：无 API key 且无 codex/zcode 的环境（CI、精简部署）下，**每个 LLM 步骤的 dispatch 都失败** → 串行路径 `_handle_failure` 重试×3 → ESCALATION，pipeline 第一步即停；与路由 docstring（dispatch_helper.py:8-11 声称三条可用路径）矛盾。属文档-行为漂移 + 死路由。
- 建议方向：删除 legacy 子进程路由分支（保留 `shenbi-dispatch` 供 T1 测试直调），或在 `dispatch_skill` 路由到 legacy 前先探测 `detect_mode()=="codex"` 且 codex 缺失时立即返回明确错误（而非寄望于子进程）；若保留 staging 语义，legacy 路径必须走 `_build_skill_prompt` 的 staging 前缀。

### F386 | 软失败升级路径派发 escalation-review 但不设 ESCALATION checkpoint——升级从不暂停、escalation 报告成为孤儿 | error | P2

- 证据：
  - `chapter_loop.py:2865-2874`（G4 soft-fail 达阈值时调 `_check_soft_fail_escalation`，之后 `if hard_fails:` 为空 → :2905 "No hard fails → fall through to advance" → :3024 `add_step_done` + `_advance`）；
  - `chapter_loop.py:967-1035`（`_check_soft_fail_escalation`：`any_escalated` → `check_escalation(...)` → 有信号则写 sidecar + `dispatch_escalation(...)`——**无任何 `set_checkpoint` 调用**）；
  - 对照 `_handle_failure`（:644-671）在升级路径设置 `CheckpointType.ESCALATION` checkpoint 暂停管线。
- 根因：软失败升级只"派发 escalation-review 技能"未"设置 escalation checkpoint"；调用方随后把步骤正常推进，escalation 语义半接线。
- 验证命令+输出：read chapter_loop.py:967-1035（无 set_checkpoint）+ read :2865-2905（软失败后落至 advance）。
- 影响：同一 G4 软失败（transition/fatigue）在 5 章窗口内第 3 次触发时，escalation-review 被 dispatch（LLM 成本 + `context/chapter-N-escalation-signals.json` sidecar 落盘），但管线**继续推进**、无 checkpoint、报告无人审阅；叠加 F378（volume_objective_met 恒 False）时每次软失败升级必带 volume_objective_missed 信号——"升级"成为纯日志/文件噪音，spec 22 E32 宣称的"pipeline paused for human review"语义未兑现。
- 建议方向：`_check_soft_fail_escalation` 在 `signals` 非空时 `set_checkpoint(state, CheckpointType.ESCALATION, chapter=chapter, ...)` 并返回 True 供调用方暂停（`_advance` 前的 `return True`），或与 F378 一并重做软失败升级链。

### F387 | genre 波与 group 波文件级重复不止 sensitivity：worldRules/motivation/dialogue/texture 四维每章重复审计并覆盖 group 波产物（F329 的完整版） | optimization | P2（贴 P1 边界——决策表"显著 token 浪费"）

- 证据：
  - `audit_layer.py:44-54`（`GENRE_ACTIVATION_MATRIX`：worldRules/motivation/dialogue/texture/sensitivity 均映射到独立 review skill）；`_CORE_CIRCLE_KEYS`（:57-67）仅含 antiAi/character/pacing/continuity/foreshadowing/memoCompliance/pov——**不含** worldRules/motivation/dialogue/texture/sensitivity；
  - 生产 `genre-config.json` auditDimensions（本轮实测）：`worldRules/motivation/dialogue/texture/sensitivity` 全为 true → `get_active_genre_audits` 返回 5 个 skill；
  - group 波契约（本轮 load_contract 实证）：`group-factual → audits/chapter-N-world-rules.md`、`group-character → audits/chapter-N-dialogue.md + chapter-N-motivation.md`、`group-craft → audits/chapter-N-texture.md`——**与 genre 波 4 个单维 skill 的输出文件同名**（`shenbi-review-world-rules → chapter-N-world-rules.md` 等）；
  - `chapter_loop.py:2551`（core_skills 含 review-sensitivity）与 :2575（genre_skills）两波顺序执行 → 后写者覆盖先写者。
- 根因：MERGE-2 把 7 个串行审计合并为 4 个 group 调用后，genre 圆的去重只排除 `_CORE_CIRCLE_KEYS`，未排除"group 波已覆盖的维度文件"；F329 只识别了 sensitivity（因为它在 CHAPTER_STEPS 中可见），未识别其余 4 个经 group 契约间接覆盖的维度。
- 验证命令+输出：
  ```
  uv run python -c "from shenbi.contracts.legacy import load_contract; [print(s, load_contract(s).get('writes')) for s in ['shenbi-review-world-rules','shenbi-review-motivation','shenbi-review-dialogue','shenbi-review-texture']]"
  # → 均 ['audits/chapter-N-<dim>.md']（与 group 波契约 writes 同名）
  uv run python -c "import json; print(json.load(open('novel-output/xinghuo-ranqiong/genre-config.json')).get('auditDimensions'))"
  # → 5 维全 true
  ```
- 影响：每章在 4 个 group 审计调用之外，额外 dispatch 5 个单维审计调用（sensitivity + worldRules + motivation + dialogue + texture），每个都是全章规模的 LLM 审计（输入 ~30KB+），且其中 4 个的输出**覆盖 group 波已写好的同名文件**——重复劳动 + 结果被后写覆盖；100 章规模约 500 次冗余审计调用。F329 只覆盖了 sensitivity 一维。
- 建议方向：genre 波去重改为"按输出文件集合"去重（构建 group 波产物文件名集合，跳过同名 genre skill），或在 `GENRE_ACTIVATION_MATRIX` 增加 group-wave-covered 过滤；与 F329 合并修复。

### F388 | `_audit_context_coverage` 死函数：docstring 声称"pipeline resume 初始化时调用"（spec §3.1 的 77% 上下文覆盖缺口检测），cli 从未接线 | error | P2

- 证据：`chapter_loop.py:2104-2125`（`_audit_context_coverage`，`# pyright: ignore[reportUnusedFunction]`；docstring "Called at pipeline resume initialization to surface the 77% coverage gap (spec §3.1)"）；`grep _audit_context_coverage src/shenbi/pipeline/cli.py` → 0 命中（本轮实测仅 chapter_loop 定义 + tests/unit/pipeline/test_context_audit.py 直调）。
- 根因：spec §3.1 覆盖缺口检测（与 F330/F309 同族的"注释声称已接线、实际未接线"）。
- 验证命令+输出：`grep -rn "_audit_context_coverage" src/ --include="*.py"` → 仅定义处；`grep -rn "_audit_context_coverage" src/shenbi/pipeline/cli.py` → 无。
- 影响：resume 时上下文覆盖缺口（context/chapter-N-context.md 缺失清单）从不检测/从不报告，注释误导维护者以为已生效；属半接线死信号（与 F360/F378/F382 同族）。
- 建议方向：在 `cmd_resume`（cli.py:723 附近）调用并 log 结果，或删除函数与过期注释并迁移测试。

### F389 | linguistic drift 数据源侧全链路 inert：`style/linguistic_baseline.json` 唯一写入方 `establish_baseline` 全仓 0 调用方 → 每章 drift 检查恒 "no baseline" 返回 None → spec §3.4 三级干预（WARN/HARD/ESCALATE）与 DriftEscalationError 生产不可达 | error | P2

- 证据：
  - `chapter_loop.py:2013-2019`（`_check_linguistic_drift`：`baseline_file = project_dir / "style" / "linguistic_baseline.json"`，不存在 → `log.warning("no_linguistic_baseline")` → `return None`——漂移评估根本不执行）；
  - `skill_utils/drift_detection/baseline.py:24-78`（`establish_baseline` 写入 `style/linguistic_baseline.json`，:78）——**全仓 0 个生产/测试调用方**（grep 实证，仅 `__init__.py:6` 的 re-export）；
  - 路径双轨：`linguistic_drift.py:278` 的 `_load_baseline` 读的是 `context/linguistic_baseline.json`（另一路径），与 chapter_loop 读的 `style/` 不一致；
  - `chapter_loop.py:2720-2728`（step 5 pipeline-linguistic-drift-check 调用，每章一次）。
- 根因：基线建立（establish_baseline）从未接线到任何执行点（genesis/closure/第 3 章后自建均无）；drift 检测读的路径与唯一写入方脱节。
- 验证命令+输出：
  ```
  grep -rn "establish_baseline" src/ tests/ --include="*.py" | grep -v "__init__\|def establish"
  # → 0 命中（无任何调用方）
  ls novel-output/xinghuo-ranqiong/style/   # → style_profile.md（无 linguistic_baseline.json）
  ```
- 影响：当前代码唯一有实现体的 pipeline-* 步骤（linguistic-drift-check）在生产**恒 no-op**（每章一条 `no_linguistic_baseline` warning）；F358 描述的"ESCALATE 被 except 吞 + 指令文件无消费者"实际不可达（漂移评估在基线缺失处提前返回）；spec §3.4 三级语言漂移干预（WARN/HARD/ESCALATE + drift-correction/opening-variation 指令）整体失效。
- 建议方向：在 genesis step 15（style-learning）后或第 3 章完成后调用 `establish_baseline`（或让 `_check_linguistic_drift` 在缺基线时按 `_load_baseline` 的"早期章节自建"语义自动建基）；统一 `style/` vs `context/` 路径；补一条"基线建立 → drift 检测 → HARD 指令落盘"的集成测试。

### F390 | cmd_review `--feedback` 文件缺失时误报 "project not found" | error | M

- 证据：`cli.py:533-535`（`if args.feedback: feedback = Path(args.feedback).read_text(...)`）位于 `with WriteLock` 内的大 `try:` 中；`cli.py:569-571`（`except FileNotFoundError: emit_json({"status": ERROR, "message": "project not found"})`）——feedback 文件缺失的 FileNotFoundError 被当作"项目不存在"处理。
- 根因：cmd_review 只捕获 FileNotFoundError 且 message 写死为项目路径；未区分 feedback 文件与 pipeline-state.json 两种缺失来源。
- 验证命令+输出：read cli.py:511-571（feedback 读取在 try 块内、except 分支 message 固定）。
- 影响：`pipeline review approve --feedback 不存在的文件` 输出误导性错误信息（"project not found"），机器可读 stdout 语义错误。
- 建议方向：feedback 读取移出 try 或单独捕获并输出 `{"status": ERROR, "message": "feedback file not found: <path>"}`。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件逐行重读；`__init__.py` 1 行至 `chapter_loop.py` 3053 行、`dispatch_helper.py` 1986 行、`cli.py` 939 行、`triggers.py` 656 行等全部读完）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries → set()`；audits/ 722 文件全旧式名、group-* 数=0、chapter-1-review-summary.md 恒报 0 问题；chapters/ 145 文件补零数=0、pre-rev 数=0；novel.json 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0；style/ 仅 style_profile.md、无 linguistic_baseline.json；snapshots/ 52 个旧版遗留快照；truth/pending_hooks.md body 含 79 个 P0-* 命中）；`uv run python` 实证：**closure 10 步与 genesis 17 步全量 prompt-build 执行**（F379/F380 复证）、`extract_chapter` 对 closure/trigger prompt 的取值、`_style_profile_is_stale` 计数实验（F372 复证）、`heal_state_counters` revision_count 抬升实验（F381 复证）、`build_index` 生产实体计数（F384 依据）、`classify_skill_write_safety('shenbi-review-resonance')`（F374）；**G4 CLI 直接执行**（F371/F373 复证）；**/tmp 沙箱 atexit 退出实验**（F364 独立复现）；load_contract 契约 writes/updates（group-*/resonance/lifecycle/state-settling/drafting/volume-outlining/anchor-curate/snapshot-manage）。
- 前几轮覆盖空洞（本轮已补）：(1) **linguistic drift 基线无写入方**——前八轮只审了 ESCALATE 吞异常（F358）与指令文件无消费者，从未验证 `style/linguistic_baseline.json` 的生产来源 → 漏掉 F389（漂移检测整体 inert，比 F358 更前置）；(2) **SentenceTransformer 重复加载**（F384）；(3) **legacy 子进程路由死端**（F385）；(4) **软失败升级不设 checkpoint**（F386）；(5) **genre 波 4 维文件级重复**（F387，F329 只覆盖 sensitivity）；(6) **`_audit_context_coverage` 死函数**（F388）；(7) **cmd_review --feedback 误报**（F390）。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 的实际 codex 输出格式未做端到端执行验证（F383 为静态推演，未真实调用 codex exec）；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内）；(e) legacy 子进程路径（F385）与 internal 模式的交互未做子进程级执行验证（静态分析）。

---

## 四、汇总

- **新 finding 数**：7（F384-F390，其中 F384-F389 = P2、F390 = M；**无新增 P0/P1**）
- **误报数**：0 新增（F300-F383 中仅 F325 为误报，二~八轮已撤销、本轮独立复证维持；F348 维持"成立但机制描述部分不准"）
- **严重度异议数**：2（F302 P1→P0 依据支持、F364 P1→P0 依据支持——均为此前已提出、本轮独立复证，裁定留 stage 4；另注记 F353 贴 P0 边界、F387 贴 P1 边界）
- **事实修正（不改严重度）**：2（F358 机制前置修正——ESCALATE 吞异常实际不可达，漂移检测在基线缺失处更早断裂，见 F389；F324 修复完整性注记——`_resolve_volume_at_runtime` 英文卷名与生产中文标题不匹配，修复需同步调整消费者格式假设）
- **0 新 finding 判定**：**否** —— 本轮仍发现 6 条 P2 漏报（F384-F389）与 1 条 M（F390）；F324（P0）维持成立、F325（误报）维持撤销、F340/F341/F345/F353/F354/F364/F371/F373/F379（P1）维持成立。前九轮共 67 条编号 finding（F300-F390，其中 F325 误报撤销）。收敛目标（0 新 finding）尚未达成，但本轮 7 条均为 P2/M 级（无新增 P0/P1），且多属"半接线死信号/性能/边界"类别，说明前八轮对主路径正确性（P0/P1 级）的覆盖已基本收敛。
- **校准升级提示**：F389 建议与 F358 合并处置（drift 三级干预整体重做：基线建立 + 路径统一 + ESCALATE 接 checkpoint + 指令文件注入）；F387 建议与 F329 合并修复（genre 波按输出文件去重）；F386 建议与 F378 合并处置（软失败升级链：信号 → checkpoint 暂停）；F384 建议在启用 embeddings 前修复（否则 genesis 时间成本爆炸）。

---

## 五、独立复核声明

本报告为 fresh-context 第九轮独立复核：34/34 文件逐行重读；对 F300-F383 的判定全部基于本轮独立 read/grep/uv run python/G4-CLI/沙箱实验与真实产物对照（未复用前九轮结论）；新增 7 条发现（F384-F390）均为前十轮报告未以编号形式出现的观察，其中 F389 的验证手段（drift 基线写入方全仓 grep + 生产 style/ 目录对照）、F384（生产实体计数 × 模型加载耦合）、F385（legacy 路由与 internal 模式硬拒的联动）、F386（软失败升级无 checkpoint）、F387（genre/group 波文件级同名覆盖）为前几轮未覆盖的新证据路径；F364 的 atexit 沙箱实验与 F371/F373 的 G4 CLI 实验为本轮独立复现。
