# Z6 分区第九轮独立复核报告（fresh-context 全量重读）

> 复核人：第九轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11（F601-F611）+ 一轮 8（F612-F619）+ 二轮 10（F620-F629）+ 三轮 7（F630-F636）+ 四轮 3（F637-F639）+ 五轮 3（F640-F642）+ 六轮 1（F643）+ 七轮 3（F644-F646）+ 八轮 2（F647-F648）逐条独立验证；新发现编号 F649 起。
> 方式：49/49 文件全文 read + `PYTHONPATH=src python3` 实际运行复现（F601/F612/F636/F641/F643/F644/F645/F646/F647/F648 及全部新发现本轮回放）+ 全仓 grep 调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对（chapters/chapter-1/45/46.md、truth/pending_hooks.md、truth/resonance_trend.md、truth/audit_drift.md、genre-config.json、progress.json、无 trace.jsonl）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未达成**（新发现 5 条，均 P2；含 1 条对 F642 争议的裁决级新证据）。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §3）
- 初审 11 findings：**11/11 成立**（F601 严重度软异议支持，见 §4）
- 一轮 8：**8/8 成立**
- 二轮 10：**10/10 成立**
- 三轮 7：**7/7 成立**
- 四轮 3：**3/3 成立**
- 五轮 3：**3/3 成立**
- 六轮 1：**1/1 成立**
- 七轮 3：**3/3 成立**
- 八轮 2：**2/2 成立**
- 新发现：**5**（F649-F653，均 P2）
- 整条误报：**0**；严重度异议：**1 条（F601 软异议，支持 review6/7/8）**；F642 证据争议裁决：**本轮推翻 review8 的裁定**（见 §2.2）
- 判定：**非 0 新 finding（5 条）**——收敛曲线 11→8→10→7→3→3→1→3→2→5；本轮 5 条均为前八轮未触达的方向，其中 **F649 同时终结了 F642 跨三轮的 0.57/0.637 之争**（双方数字均可复现，根源是 SequenceMatcher 参数顺序不对称——双方都未识别）

---

## 1. F601-F648 判定表（逐条独立复核）

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（**支持 review6/7/8 严重度软异议：可辩 P2**） | 本轮直接运行：baseline dialogue=10 → current=1 → `is_drift: False, severity: NONE`。linguistic_drift.py:215 `max(…,5.0)` 与 :218 严格 `>5.0` off-by-one 成立；docstring :185-188 承诺 "<20% of baseline" 触发。**生产影响全潜伏**：唯一生产入口 `_check_linguistic_drift` 因 F602 无 baseline 提前 return（chapter_loop.py:2017-2019），detect_drift 从未被生产调用；即便接线，F612/F620 使干预仍不可达。P1（显式契约静默失效）与 P2（死路径）均可辩护，不构成整条误报，供协调者裁决。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持） | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py` re-export，生产零调用；`find . -name linguistic_baseline.json` 0 命中。计划 07-19-07 Task 5a 显式接线指令未履行。P1 成立。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链 write_audit → record.py:24 block ship。test_drift.py 无 YAML-ghost 反向用例。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'`，:226 按字符迭代 → 一对 ASCII 引号 count=4。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持） | 本轮运行：空 terms → `system_term_density: 1028.5714`；`system_terms: null` → `list(None)` TypeError、`drift_detection: null` → `None.get` AttributeError 均同根（config 驱动 terms 无类型校验）。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过（前轮运行复现）。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs detect_drift 读 `dialogue_density`；:296 硬编码术语 vs :107 config 驱动。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | cjk.py:54 桶 token 为相邻双字符 `""`/`''`/`「」`/`『』`，正常文本引号包围内容不相邻 → `text.count` 恒 0。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py 全文无 logging 导入；:40-43 两处 break 无日志、:47-48 safe_write 改写文件。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无。F622 为独立类型失败模式。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持） | 本轮运行复现：`{'auditDimensions': {'texture': False}}` + 短 rationale → ACCEPTED。config_coherence.py:109 `key.startswith("auditDimensions.")` 只拦带点形式。F631（falsy）/F638（snake_case）/F643（键缺失）为独立向量。P2 成立。 |

### 1.2 一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持） | 本轮直接运行：baseline stm=60 → current stm=110 → `is_drift: False, severity: ESCALATE`（输出 `False ESCALATE`，且 message="No linguistic drift detected."——severity 与 message 自相矛盾，佐证门控缺陷）。chapter_loop.py:2023 `if result.is_drift:` 包裹 ESCALATE raise + HARD/WARN 注入（:2024-2043 代码精读确认）。计划 07-19-07:749 绝对阈值触发契约。P1 成立。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"破折号": "——"`/`"省略号": "……"` 重复字符 + :226 按字符迭代 → 一个 `——` 计 4。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | config_coherence.py:105-133 循环内逐 key `_append_audit_trail`，safe_write 在循环外 :142 → 中途 ConfigError 时 trail 已追加、config 未落盘。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | writer.py:57 `json.loads(lines[-1])` 无 try；replay.py:38-43 同文件可自愈 → 不对称。三个生产调用点均有外部缓解（safe_write.py:127-135 / audit/record.py:42-46 / migrate 先 replay）。P2 成立。**注意**：F615 只覆盖"不可解析"尾部；"可解析但签名无效"的尾部是独立失败模式（见新 F650）。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | 本轮 grep：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` → 仅 compute_stats.py:37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。P2 成立。 |

### 1.3 二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持） | 本轮代码精读：chapter_loop.py:2722-2728 `try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed")`；函数内 :2034-2038 raise DriftEscalationError；:2719 注释自称 "non-blocking"——调用点与函数内契约自相矛盾。grep 唯一生产调用点即 :2722。P1 成立。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | recall.py:41-46 `.get()` 只判 None 不判类型 → `int - str` / `int > str` TypeError。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持） | compute_stats.py:52 单 U+2026 vs 真实文本双 `……`（与 :23 PUNCT_MAP 一致）。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无 skills 校验 | P2 | **成立**（P2 维持） | 本轮规范化重算：master 59 名、skills/ 目录 74、缺失恰好 15 个（名单与 review2-8 完全一致）、反向 0。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、无生产调用方）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export，src 内无生产调用。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件。真实 resonance_trend.md 单表 → 潜伏。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | compute_stats.py:16 SENT_ENDS 含 `；`，:63 只按 `。！？\n` 切句，:87 用 SENT_ENDS。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用。P2 成立。 |

### 1.4 三轮 F630-F636

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | **成立**（P2 维持） | revision_router.py:63-64（注释明示 "config.resonance_global_floor" 后跟 `= 50`）vs thresholds.py:30 `=65`；chapter_loop.py:1897 显式传值覆盖默认 → 潜伏。P2 成立。 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | **成立**（P2 维持） | config_coherence.py:109、g0_config_coherence.py:92 均 `is False`；audit_layer.py:113 `audit_dims.get(dim_key, False)` 任何 falsy 即不激活。P2 成立。 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | **成立**（P2 维持） | linguistic_drift.py:222-226 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50。P2 成立。 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | **成立**（P2 维持） | compute_stats.py:79 docstring vs :80 `re.split(r"\n\s*\n")`；`\n` ∈ SENT_ENDS :16。P2 成立。 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | **成立**（P2 维持） | 真实 chapter-1/45/46.md 均以 `<!--META-BEGIN-->` 开头（45 有 2 块、46 有 1 块）；两处统计入口无剥离。P2 成立。 |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | **成立**（P2 维持） | grep：`update_genre_config` 仅 config_coherence.py 自身 + 测试 + 计划文档；真实 config-change-log.jsonl 仅 1 条 2026-07-19 记录。P2 成立。 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | **成立**（P2 维持） | 本轮直接运行：base 全 0 + current em_dash=0.33 → `is_drift: True, severity: WARN`（输出 `True WARN`）。linguistic_drift.py:202。P2 成立。 |

### 1.5 四轮 F637-F639

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转（格式分叉 + 测试掩盖） | P1 | **成立**（P1 维持） | 本轮全链复核：真实 pending_hooks.md（143 行）标题仅 `# 伏笔追踪`/`## 第56章伏笔呈现`/`## 第56章生命周期状态更新` 等章节附录，**无 `## hooks`/`## 活跃伏笔`**（grep 确认）；ch-025 快照有 `## 活跃伏笔`(:12)/`## hooks`(:20) → 格式分叉成立。消费链 write_audit.py:54-56 → record.py:24 block-ship 已接线但 drift 恒空。P1（"测试失效掩盖真实缺陷"档）成立。 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过 Rule 1 与 G0） | P2 | **成立**（P2 维持） | audit_layer.py:105-107 snake_case 回退 vs config_coherence.py:109/g0_config_coherence.py:79 只认 camelCase。真实配置 camelCase → 潜伏。P2 成立。 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 真实项目 arc/volume 半边不运行 + audit 追加无协调 | P2 | **成立**（P2 维持） | 真实 truth/ 仅 resonance_trend.md（1 行 ch55 数据），无 arc_payoff_trend.md/volume_score_trend.md；audit_drift.md 为 `## Ch55 —` 章节式（review-resonance 写）。P2 成立。 |

### 1.6 五轮 F640-F642

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F640 | materialize_progress 输入契约零生产者 → 无条件覆盖真实 progress.json（数据丢失+错误结果） | P1 | **成立**（P1 维持） | 本轮全链复核：`grep '"INIT"\|"MARK_DONE"'` src/ → 仅 materialize.py:49,53（消费方）+ 测试；生产 TraceWriter.append 调用点 = audit/record.py:44 + safe_write.py:132（trace_action 生产唯一传参处即 materialize.py:98 自身）。`_maybe_materialize_progress`（chapter_loop.py:684-697）：`if steps_done % 5 == 0`（steps_done=0 即满足，:691）+ `except Exception: pass`（:695——覆盖失败也静默）。调用点 :2691/:3028 在活动章节循环内。`_auto_rebuild_progress_if_stale`（:702-741）检查 `project_dir/"trace"` **目录**而 TraceWriter 写 `trace.jsonl` **文件**（writer.py:42）→ 路径错位使自愈分支永不触发。真实项目无 trace.jsonl、progress.json 未被破坏 → 潜伏。P1 成立（触发即数据丢失，激活应升级 P0）。 |
| F641 | records `_values_equal` 布尔比较孔 → YAML true vs md "true" 假 drift | P2 | **成立**（P2 维持） | 本轮运行：`_values_equal(True, 'true')` → False（假 drift）；`_values_equal(True, 'True')` → True；`detect_cross_section_drift([{'id':'h','state':True}], {'h':{'id':'h','state':'true'}})` → 假 drift 1 条。drift.py:57-69 float 兜底对 bool 抛 ValueError 落入 return False。P2 成立。 |
| F642 | check_opening_similarity 比较 META 块非正文开头 | P2 | **本体成立**（**证据争议本轮裁决见 §2.2：review8 的裁定被推翻**） | 本体：linguistic_drift.py:249-253 取 `[:300]` 首 300 字符，真实章节以 META 指令块开头 → 比较对象确为 META 块，成立。**但** review5/7/8 的 "剥离后 0.57 < 0.6，META 是越过阈值的主因" 与 review6 的 "剥离后 0.637 仍 > 0.6" **两个数字在本轮均被独立复现**（取决于 SequenceMatcher 参数顺序与剥离方法，见 F649）——跨三轮之争的根源是**参数顺序不对称**，双方都未识别；按生产调用顺序（current 在前）剥离后 (45,46) = **0.6367 > 0.6**，与 review6 一致 → **剥离 META 不足以消除假阳性**（review6 结论成立，review8 裁定不成立）。P2 维持（修复需剥离 META + 对称化 + 阈值重标定三者并施）。 |

### 1.7 六轮 F643

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F643 | 治理链"键缺失"默认语义矛盾：G0 视缺失=启用、audit_layer 视缺失=禁用 → 移除/置空 auditDimensions 静默停用审计 | P2 | **成立**（P2 维持） | 本轮运行：`update_genre_config(d, {'auditDimensions': {'antiAi': True, 'continuity': True}}, rationale='x')` → ACCEPTED 且 texture 从落盘 config 移除；g0_config_coherence.py:92 `get(dim, True)` vs audit_layer.py:113 `get(dim_key, False)` vs config_coherence.py:109 只认显式 False。P2 成立。 |

### 1.8 七轮 F644-F646

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F644 | compute_ngrams 未剔除标点 → n-gram 指纹被标点对/标点串主导 | P2 | **成立**（P2 维持） | 本轮实测：真实 chapter-1 top bigram = `('。他', 27)`/`('——', 25)`；compute_stats.py:209 只滤空白。P2 成立。 |
| F645 | compute_ttr 排除串缺 `“”`/`"`/`‘’` 引号 → 引号字符计入 TTR token | P2 | **成立**（P2 维持） | 本轮实测：`compute_ttr('他说：“你好。”然后离开了。')['total_chars']` = 11 vs 无引号 9。compute_stats.py:171 排除串只含两个 ASCII 直单引号 `''`。P2 成立。 |
| F646 | AUDIT_SAFETY_MATRIX 6 维 vs 真实 config 10 维漂移：4 维无 rationale/G0 保护（sensitivity 为 BLOCKING 级） | P2 | **成立**（P2 维持） | 真实 genre-config.json auditDimensions 10 维，`real - matrix = ['foreshadowing','motivation','sensitivity','worldRules']`、`matrix - real = []`。P2 成立。 |

### 1.9 八轮 F647-F648

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F647 | records by_id 重复/缺失 id 静默塌缩 → drift 漏报或误报且无告警 | P2 | **成立**（P2 维持） | 本轮运行：权威 YAML 两条同 id 记录（type 主线/支线）+ md 对齐后者 → drift `[]`（漏报）；对齐前者 → 假 drift 1 条。drift.py:80 dict 推导后者覆盖前者。P2 成立。 |
| F648 | compute_percentiles n=2 百分位倒挂（P75/P95 < P50） | P2 | **成立**（P2 维持） | 本轮运行：`compute_percentiles([1,2])` → `{'P25': 1, 'P50': 2, 'P75': 1, 'P95': 1}`（P75=P95 < P50）。compute_stats.py:99-104 `int(n*0.75)-1` 截断 + `values[n//2]` 组合在 n=2 时倒挂。P2 成立。 |

**严重度异议：1 条（F601 软异议，支持 review6/7/8，见 §4）。** 六条 P1（F601/F602/F612/F620/F637/F640）本轮全部以直接运行/代码精读/全仓 grep/真实数据多重复核，无反证；P2 均无高判或低判。

---

## 2. F642 证据争议裁决（推翻 review8 裁定）

### 2.1 争议回放

- review5 声称：META 剥离后 (45,46) = **0.57** < 0.6，META 是越过阈值的主因。
- review6 声称：同法剥离后 = **0.637** 仍 > 0.6，review5 的 0.57 有误。
- review7/review8 声称：无法复现 0.637，同法剥离得 **0.570**（review8 据此"裁定"支持 review5/7/8、否定 review6）。

### 2.2 本轮独立重放（决定性证据）

同一 `check_opening_similarity`、同一剥离正则 `re.sub(r'<!--META-BEGIN-->.*?<!--META-END-->', '', t, flags=re.S)`，ch45 两个 META 块 / ch46 一个：

```
SequenceMatcher(None, s46[:300], s45[:300]).ratio() = 0.6367   # 生产调用顺序 (current=46, prev=45)
SequenceMatcher(None, s45[:300], s46[:300]).ratio() = 0.5700   # 交换顺序 (45, 46)
SequenceMatcher(..., autojunk=False).ratio()           = 0.77   # 关闭 autojunk 后两个方向 ~0.77/0.78
```

**关键机制**：`difflib.SequenceMatcher.ratio()` 默认 `autojunk=True`（长度 ≥200 时启用，把第二参数中出现 ≥1% 的字符当 junk 排除）→ **比值不对称**。同一对文本 (45,46) 的剥离后相似度，按生产参数顺序算 = **0.6367**（> 0.6），交换顺序 = **0.57**（< 0.6）。另外，剥离方法变体（仅删标签保留 META 内容）也给出 0.5733。

- **review6 的 0.637 是生产调用顺序下的真实值**（chapter_loop.py:2049 `check_opening_similarity(chapter_text, prev_text)`，current 在前）；
- **review5/7/8 的 0.57 是交换顺序（或仅删标签）下的值**，与生产顺序不符；
- **review8 的"裁定"（0.637 不可复现、支持 0.57）本身就是参数顺序伪影**——0.57 可复现只说明他们用了交换顺序，不说明生产顺序下假阳性不触发。

### 2.3 结论

1. F642 本体（检查对象 = META 块而非正文开头）**成立**；
2. F642 影响（接线后真实章节假阳性触发 opening-variation 指令）**成立**——且**按生产顺序剥离 META 后 (45,46) 仍为 0.6367 > 0.6**，假阳性与 META 无关（正文开头本身模板化，autojunk=False 时高达 0.77）——**review6 的"剥离不足以修复"结论成立，review5/7/8 与 review8 裁定不成立**；
3. 该争议的根源是 **SequenceMatcher 参数顺序不对称**（新 F649），跨三轮无人识别。

---

## 3. 新发现（F649-F653）

### F649 | check_opening_similarity/check_window_redundancy 用 SequenceMatcher.ratio() 默认 autojunk → 相似度随参数顺序不对称（0.57 vs 0.637），近阈值分类不稳定；F642 跨三轮之争的根源 | 统计口径/边界 | P2

- 证据：`src/shenbi/skill_utils/drift_detection/linguistic_drift.py:249-253`（`check_opening_similarity`：`SequenceMatcher(None, opening1, opening2).ratio()`，opening 为 `[:300]`）、`:256-268`（`check_window_redundancy`：`SequenceMatcher(None, window[i][:500], window[j][:500]).ratio()`）；消费方 `chapter_loop.py:2049`（`check_opening_similarity(chapter_text, prev_text)`）+ `:2050`（`if opening_sim > 0.6`）。
- 根因：`difflib.SequenceMatcher` 默认 `autojunk=True`（序列长度 ≥200 时启用，把第二序列中出现频率 ≥1% 的字符排除在匹配外）。`ratio()` 因而**依赖参数顺序**——同一对文本按 (A,B) 与按 (B,A) 计算可得到显著不同的相似度。真实 56 章全对扫描（raw 文本、生产顺序 vs 交换顺序）：**55 对中 34 对 |diff| > 0.01**，极端例 (7,6) 0.28 vs 0.21、(14,13) 0.29 vs 0.307；剥离 META 后 (45,46) 0.6367 vs 0.57。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  s46,s45 = 剥离 META 后的 chapter-46/45
  SequenceMatcher(None, s46[:300], s45[:300]).ratio() -> 0.6367   # 生产顺序 (current, prev)
  SequenceMatcher(None, s45[:300], s46[:300]).ratio() -> 0.5700   # 交换顺序
  SequenceMatcher(None, s46[:300], s45[:300], autojunk=False).ratio() -> 0.77
  56 章相邻对 |diff|>0.01 的对数: 34/55
  "
  ```
- 影响：相似度不是良定义的对称度量；近阈值（0.6）分类随参数顺序翻转（同一对章节，一个顺序触发 opening-variation 指令、另一个顺序不触发）；`check_window_redundancy` 同样受影响（内容循环检测阈值 0.35）；**直接终结 F642 争议**——review5/7/8 的 0.57 与 review6 的 0.637 均可复现，前者是交换顺序伪影、后者是生产顺序真值，review8 的裁定本身不成立；且 autojunk=False 时真实正文相似度高达 0.77，0.6 阈值对真实正文系统性脆弱（与 F642 的阈值标定建议合并评估）。
- 建议方向：`SequenceMatcher(..., autojunk=False)`（消除不对称）+ 显式对称化（比较 min(ratio(a,b), ratio(b,a)) 或统一归一化）；阈值需按真实正文分布重新标定；补单测（同一对章节交换参数顺序结果一致；真实 (45,46) 剥离后无论顺序均触发或不触发有确定预期）。

### F650 | TraceWriter 信任尾部签名不做链校验：可解析但签名无效的尾部（篡改/坏写）静默链上新事件，下次 replay 把新合法事件与坏尾部一起截断删除——静默数据丢失（F615 的另一失败模式） | 边界/错误处理 | P2

- 证据：`src/shenbi/trace/writer.py:51-57`（`_last_sig_existing`：`json.loads(lines[-1]).get("signature", GENESIS_PREV)`——只取签名，不校验它是合法 TraceEvent、不校验签名与链一致）；`:65-99`（`append` 以该签名为 prev 链新事件）；对比 `replay.py:38-48`（首坏行截断自愈）。
- 根因：F615 覆盖"尾部 JSON 不可解析 → writer 崩溃（可见、被调用方缓解）"；本缺陷是**独立失败模式**：尾部是可解析 JSON 但签名错误/缺失（篡改、坏写、手工编辑）时，writer 不崩溃、不告警，直接以坏签名为 prev 写入新事件；下次 `replay` 从坏行截断 → **新写入的合法事件与坏尾部一起被静默删除**，无任何日志（replay 无 logging，F609）。当前生产写者仅 audit/record.py:44 的 GATE_FAIL/AUDIT_PASS seam（safe_write 的 trace_action 无生产传参），触发需 trace 先被篡改/损坏 → 潜伏。
- 验证命令+输出：
  ```
  构造 trace.jsonl = 1 条合法事件（签名被篡改为 '0'*64，JSON 仍有效）
  TraceWriter(d) -> 不崩溃，prev = 篡改签名
  w.append(...)  -> 合法新事件 seq=2 写入成功
  replay(d)      -> 事件数 0 | 新事件丢失: True | 文件被截断为空
  ```
- 影响：G7 篡改模型的排障路径上，篡改后的任何追加（审计记录）都会在下次 replay 时被静默销毁——审计账本静默缺事件，且无任何告警提示根因（与 F609 同属"截断不可追踪"族，但本缺陷的独特破坏性是**合法新数据被连带销毁且写入侧无异常**）。
- 建议方向：`_last_sig_existing` 改为验证末行签名与链一致（自末向前逐行 `_verify`，取最后一个合法事件的签名；坏尾部视为撕裂 → 与 replay 语义对齐：告警 + 截断或拒绝 append）；补单测（签名无效尾部：writer 构造不静默链上坏签名，append 后 replay 保留新事件）。

### F651 | records parser yaml.safe_load 对重复键静默 last-wins（`state: A` + `state: B` → B，无错误无告警）→ 权威记录源静默损坏，drift 与错误值比较 | 边界/数据视图损失 | P2

- 证据：`src/shenbi/records/parser.py:37-45`（`_parse_body`：`data = yaml.safe_load(body)` 直接解析，无重复键检测）；docstring :4-6（"YAML block 为权威记录源"）；消费链 `write_audit.py:54-56` → `record.py:24` block ship。
- 根因：PyYAML `SafeLoader` 默认对映射重复键**静默保留最后一个值**（实测 `- id: H01\n  state: A\n  state: B\n` → `{'id': 'H01', 'state': 'B'}`，无异常）。权威 YAML 由 LLM/手工写入，出现重复键时（如更新记录时保留旧行又追加新行）记录被静默取末值：派生 md 表与末值一致时**漏报**（首值差异不可见）、与首值一致时**误报**。与 F616（非 dict 条目）、F647（记录级重复 id）不同：本缺陷是**记录内部映射级重复键**，属第三层静默视图损失。当前真实数据无 `## hooks`（F637）→ 潜伏；权威格式恢复后进入活动路径。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  import yaml
  yaml.safe_load('- id: H01\n  state: A\n  state: B\n') -> [{'id': 'H01', 'state': 'B'}]   # 无异常，last-wins
  parse_records('## hooks\n- id: H01\n  state: A\n  state: B\n') -> [{'id': 'H01', 'state': 'B'}]
  "
  ```
- 建议方向：解析后检测重复键（自定义 SafeLoader 的 construct_mapping 记录重复键 → log WARNING / 抛 ValueError 与 :43-44 顶层非 list 一致），或解析前用 strict YAML 校验；补单测（含重复键的 hooks 块必须有信号）。

### F652 | detect_rhetoric「反复」指标按子短语分解计数：一个 7 字短语重复 3 次 → 反复=12（5+4+3 个子 3/4/5-gram 各计 1）——量级随短语长度膨胀且非重复次数本身 | 统计口径 | P2

- 证据：`src/shenbi/skill_utils/style_learning/compute_stats.py:266-280`（`for phrase_len in [3, 4, 5]` 枚举全部 3/4/5-gram，`len(positions) >= 3` 且相邻间隔 <100 时 `repetition += 1` 并 break）；`test_compute_stats.py:428-434`（只断言 `result["反复"] >= 1`，未锁定量级）。
- 根因：重复短语 L 的每个 3/4/5 字符子短语都独立触发计数——一个 7 字短语 "冷在墙体第五层" 重复 3 次 → 5 个 3-gram + 4 个 4-gram + 3 个 5-gram 各计 1 = **12**；而 4 字短语重复 3 次 → 2+1 = **3**。指标是短语长度的函数而非实际重复次数，报告间不可比。
- 验证命令+输出：
  ```
  detect_rhetoric('冷在墙体第五层。冷在墙体第五层。冷在墙体第五层。') -> {'排比': 1, '设问': 0, '反问': 0, '反复': 12}
  detect_rhetoric('主角笑了主角笑了主角笑了。')                       -> {'反复': 3}
  # 同一个"1 个短语重复 3 次"，7 字计 12、4 字计 3 —— 量级随长度膨胀
  ```
- 影响：style-learning 报告的修辞统计中「反复」值不可解释（与重复次数无稳定比例），风格对比/阈值判断失真；测试仅断言 ≥1 未发现。
- 建议方向：改为按**去重后的重复短语**计数（每短语至多计 1）或将计数限定为"重复短语数"而非 gram 数；与 F618（死 RHETORICAL 字典）合并重设计 detect_rhetoric；补单测（同一短语重复 3 次 → 反复=1，不同长度短语结果可比）。

### F653 | records parse_markdown_table 空行不终止表（仅非空非 `|` 行 break）→ 空行后的第二张 `|` 表被并入派生视图（H02 行混入 H01 表） | 边界 | P2

- 证据：`src/shenbi/records/drift.py:34-39`（`if not s: continue` 跳过空行；`if not s.startswith("|"): break` 只在非空非 | 行终止）——**空行不终止**，与 markdown 语义（空行结束表格）不符；对比 `compute_drift.py:189-209`（F627：无终止条件）——records 侧被前几轮视为"正确终止"的模板，实际终止不完整。
- 根因：`## 活跃伏笔` 表后若为空行 + 另一张 `|` 表（无中间标题），第二张表的行被当作首表行合并进派生视图 → 假 id 行进入 `{id: row}`，与 YAML 权威源比较时产生假 drift 或掩盖真实 drift。真实 fixture 各表间有 `## ` 标题（标题行 break）→ 潜伏；但该终止逻辑被 review2/3/4 作为 F627 的"正确对照"引用，其空行孔未被识别。
- 验证命令+输出：
  ```
  parse_markdown_table('## 活跃伏笔\n\n| Hook ID | 类型 | 状态 |\n| --- | --- | --- |\n| H01 | 主线 | PLANTED |\n\n| H02 | 支线 | RELEVANT |\n')
  -> {'H01': {...}, 'H02': {...}}   # 空行后的 H02 被并入首表
  ```
- 建议方向：与 markdown 语义对齐——空行也 break（`if not s or not s.startswith("|"): break`）；补单测（表后空行 + 第二表 → 只取第一表）。

---

## 4. 严重度异议

| finding | 现定级 | 异议 | 理由 |
|---|---|---|---|
| F601 | P1 | **软异议（支持 review6/7/8）：可辩 P2** | 缺陷本体成立（对话塌陷 off-by-one），但生产影响全潜伏：detect_drift 的唯一生产入口 `_check_linguistic_drift` 在无 baseline（F602）时于 chapter_loop.py:2017-2019 提前 return，detect_drift 从未被生产调用；即便接线，F612/F620 使干预仍不可达。按决策表"死代码/未接线"（P2）与"违反显式契约"（P1）均可辩护。不构成整条误报，不修改定级（无权限），供协调者裁决。 |

（其余 47 条无严重度异议：六条 P1 中 F602/F612/F620/F637/F640 均经运行/代码/grep 复核无反证；P2 均无高判或低判。）

## 5. F642 裁决声明（对 review8 的反对意见，随 F649 立项）

review8 的裁定（"review6 的 0.637 不可复现、连续三轮重放均为 0.57、F642 按 review5/review7 证据成立"）**不成立**：本轮独立重放证明 0.57 与 0.637 均可复现，差异源于 `SequenceMatcher.ratio()` 参数顺序不对称（F649）。按生产调用顺序（chapter_loop.py:2049 current 在前），剥离 META 后 (45,46) = **0.6367 > 0.6**——与 review6 一致，假阳性在剥离后依然触发。**F642 的影响与修复方向应以 review6 为准**（剥离不足以修复，需阈值重标定 + 对称化），F642 本体（比较对象=META 块）与 P2 定级维持不变。

---

## 6. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2060 漂移检查全链、:2017-2019 baseline 读、:2023-2044 is_drift 门控、:2047-2056 opening-similarity 门外交互、:2716-2728 ESCALATE 调用点吞异常、:684-697 `_maybe_materialize_progress` steps_done%5 + except:pass、:702-741 `_auto_rebuild_progress_if_stale` trace 路径错位、:2691/:3028 调用点）、audit_layer.py（:105-113 激活语义）、g0_config_coherence.py（:89-97 缺失语义 + `is False`）、audit/write_audit.py（:54-56 records 消费）、audit/record.py（:24,42-46 block + trace seam）、safe_write.py（:127-140 trace_action 唯一生产调用点）、plugins/master.json（59 skills）、skills/ 目录（74）、novel-output/xinghuo-ranqiong/（chapters/chapter-1/45/46.md META 结构与 56 章相邻对相似度全扫、truth/pending_hooks.md 143 行章节附录结构、truth/resonance_trend.md 单表单行 ch55、truth/audit_drift.md `## Ch55` 格式、genre-config.json 10 维 camelCase、progress.json scoring schema、config-change-log.jsonl 单条、无 trace.jsonl、无 linguistic_baseline.json）、tests/fixtures/truth-pending_hooks.md + tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照）、tests/unit/skill_utils/test_compute_stats.py（detect_rhetoric 测试存在性核对）、tests/unit/trace/*（writer/replay 测试存在性核对）。
- 维持观察（不重复立项，供协调者参考）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）、compute_pattern main 非 dict 章节 `.get` 崩溃、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize round 名解析脆弱性（`name.split("-")[1]`）、compute_drift `_append_audit` 重复追加无幂等、compute_connectives "时间"/"顺序" 类别重叠、cjk.count_words 非法 mode 静默走 mixed、`_set_nested` 深层嵌套 bool 目标 TypeError（F611 证据细化已述）、`compute_linguistic_metrics` 术语匹配顺序敏感（alternation 顺序影响计数，确定性但顺序相关）、replay 截断后 seq 空洞（G7 只查 schema_version 单调，seq 缺口不报）。
- 关于"0 新 finding"收敛目标：F601-F648 共 48 条全部成立（含六条 P1），无整条误报；但发现 5 条前八轮均未覆盖的方向（统计口径 2、trace 边界 1、records 数据视图 2），均 P2、潜伏、无 P0/P1。其中 **F649 同时裁决了 F642 跨三轮的证据之争**（双方数字均成立、根源为 SequenceMatcher 不对称，review8 的裁定不成立）——这是本轮最重要的产出。收敛曲线 11→8→10→7→3→3→1→3→2→5 波动，建议协调者评估：新 finding 已全部为 P2 潜伏级，且本轮发现证明"多轮同法复现同一数字"也可能只是伪影（F642 教训），收敛判定建议改为"无 P0/P1 新 finding + 无整条误报"或引入跨轮交叉方法审查。

---

## 7. 返回摘要

**复核统计：新 finding 5（F649-F653，均 P2）/ 整条误报 0 / 严重度异议 1（F601 软异议，支持 review6/7/8）/ F642 证据争议裁决 1（推翻 review8 裁定）。**

**判定：非 0 新 finding（5 条），未完全收敛。**

- F601-F648 共 48 条全部成立，无整条误报。六条 P1（F601/F602/F612/F620/F637/F640）本轮以直接运行 + 代码精读 + 全仓 grep + 真实数据多重复核，无反证；F601 支持 review6/7/8 的软异议（生产影响全潜伏，P1↔P2 均可辩）；F640 确认 `_maybe_materialize_progress` 的 `steps_done % 5 == 0`（含 0）触发 + `except Exception: pass` 静默，P1 成立且激活应升级 P0。
- **F642 证据争议裁决（本轮最重要产出之一）**：独立重放证明 **0.57 与 0.637 均可复现**——根源是 `SequenceMatcher.ratio()` 默认 autojunk 导致**参数顺序不对称**（生产顺序 current 在前 = 0.6367，交换顺序 = 0.57；autojunk=False = 0.77）。review6 的 0.637 是生产顺序真值，review5/7/8 的 0.57 是交换顺序伪影，**review8 的裁定不成立**；剥离 META 后生产顺序仍 0.6367 > 0.6 → 剥离不足以修复（review6 结论成立）。
- 本轮 5 条新 finding 均为前八轮未触达方向：
  1. **F649（P2）**：check_opening_similarity/check_window_redundancy 的 SequenceMatcher 参数顺序不对称（34/55 真实相邻对 |diff|>0.01），近阈值分类不稳定，且是 F642 跨三轮之争的根源（见 §2）。
  2. **F650（P2）**：TraceWriter 信任尾部签名不校验——可解析但签名无效的尾部静默链上新事件，下次 replay 将新合法事件与坏尾部一起静默删除（F615 之外的独立失败模式，运行复现）。
  3. **F651（P2）**：records parser yaml.safe_load 对重复键静默 last-wins——权威记录源静默损坏（F616/F647 之外第三层视图损失，运行复现）。
  4. **F652（P2）**：detect_rhetoric「反复」按子短语分解计数爆炸——一个 7 字短语重复 3 次计 12、4 字短语计 3，量级随长度膨胀（运行复现，测试仅断言 ≥1）。
  5. **F653（P2）**：parse_markdown_table 空行不终止表——空行后的第二张 `|` 表被并入派生视图（运行复现；该逻辑曾被作为 F627 的"正确对照"引用，空行孔未被识别）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F649 | SequenceMatcher 参数顺序不对称（autojunk）→ 相似度非良定义、近阈值翻转；F642 之争根源（生产顺序 0.6367） | P2 | linguistic_drift.py:249-253,256-268; chapter_loop.py:2049-2050 |
| F650 | TraceWriter 信任尾部签名不校验 → 签名无效尾部链上新事件，replay 静默删除新合法事件 | P2 | writer.py:51-57,65-99; replay.py:38-48 |
| F651 | parser yaml.safe_load 重复键静默 last-wins → 权威记录源静默损坏 | P2 | parser.py:37-45 |
| F652 | detect_rhetoric「反复」按子短语分解计数爆炸（7 字短语×3 → 12） | P2 | compute_stats.py:266-280; test_compute_stats.py:428-434 |
| F653 | parse_markdown_table 空行不终止表 → 第二张表并入派生视图 | P2 | drift.py:34-39 |
| F642（裁决） | review8 裁定不成立：0.57 为交换顺序伪影，生产顺序剥离后 0.6367 仍 > 0.6（支持 review6） | 证据裁决 | linguistic_drift.py:249-253; chapter-45/46.md |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review9.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
