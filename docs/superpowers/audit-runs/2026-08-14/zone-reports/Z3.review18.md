# Z3 分区第十八轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第十八轮独立复核（本文件 `Z3.review18.md`，与初审 `Z3.a.md`、一轮至十七轮 `Z3.review.md`~`Z3.review17.md` 全部隔离；未复用任何先前结论）
> 复核范围：34/34 文件全量逐行重读（13,641 行；`__init__.py` 1 行 → `chapter_loop.py` 3053 行 → `dispatch_helper.py` 1986 行 → `cli.py` 939 行 → `triggers.py` 656 行 → `state.py` 536 行等全部读完），逐条独立验证 F300-F399 + F3A0-F3AE（91 条编号，其中 F325 为已撤销误报）；以真实生产项目 `novel-output/xinghuo-ranqiong/` 为格式/行为对照基准
> 本轮发现编号：**F3AF-F3B1（3 条，其中 1 条 P1、2 条 M；无新增 P0）**——编号沿用 F3A0 段（F400-F4ZZ 已由 Z4 占用；全仓检索 F3AF-F3ZZ 无占用）
> 新增误报 0 条（F325 维持二~十七轮撤销，本轮独立复证维持）；严重度异议 3 条（F302/F364/F397 的 P1→P0 依据，均独立复证支持，裁定留 stage 4）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证全部为只读 grep/uv run python/G4-CLI/沙箱实验）
> 方法：34 文件逐行重读 + 全部 P0/P1 finding 独立实证（read/grep/uv run python/G4-CLI//tmp 沙箱 atexit 实验）+ 真实产物对照（详见各 finding 验证节）

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮独立实测生产 `outline/volume_map.md` 全中文格式（`:7` `**章节范围**: 第1章 - 第15章（共15章）`、`## 第一卷：觉醒之火（第1-15章）`），`_shared._END_RE`/`_RANGE_RE`（仅英文 "Chapter End:"/"Chapters N-M"）对之 0 命中；`novel.json` 无 total_chapters（本轮实测 keys 无该字段）；`pipeline-state.json` phase=chapter-loop、current_chapter=56、closure_step=0（56 章完成仍未进入 CLOSURE）。影响链（卷边界触发全家 / `cli.py:219 if total > 0:` 跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 章节节点提取裸数字 `\|\s*1\s*\|` 误中跨卷钩子表行）全部成立。
2. **F325（误报）维持撤销，本轮独立复证**：生产 audits/ 722 文件 13 类后缀全旧式名（continuity/character/world-rules/pacing/dialogue/motivation/pov/memo-compliance/foreshadowing/anti-ai/sensitivity/resonance/review-summary），`group-*` 数=0（本轮实测后缀分布）；4 个 group skill 契约 `writes:` 即旧式路径（本轮 load_contract 实证 `group-factual → ['audits/chapter-N-continuity.md','audits/chapter-N-world-rules.md','audits/chapter-N-pacing.md']`）；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING（`严重度: BLOCKING`）被 `_SEVERITY_BLOCKING_RE` 与文件扫描双重命中 → revision 执行。F325 原判"并行波写 group-*.md、旧式名永不产生"与磁盘/契约事实相反。
3. **F364（P1 贴 P0）维持成立，本轮 /tmp 沙箱独立复现**：`register_emergency_handlers` → `atexit.register(_emergency_cleanup)`（crash_recovery.py:66），正常进程退出（无任何信号）即无条件 `clear_staging`（:144-148）+ 把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`（:113-127）。本轮实测：`staging before exit: True` → 退出后日志 `staging_cleared`、staging 目录整体消失（无信号、无异常）。消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交）成立。**事实注记（不改成立性）**：F364 原影响描述中"chapter-planning 无限循环"仅对 MODIFY 决策成立（MODIFY 回滚 step_index=1 → 重发 plan → 退出清空 → 再 MODIFY）；对 APPROVE 决策实际表现为"每章 plan 被审通过但永不落盘、管线在无 plan 状态下继续推进（drafting 走 minimal context fallback）"——人审产物静默作废的核心判定不受影响。
4. **F379/F371/F373/F353/F397/F340/F341/F345/F354 全部维持成立**（本轮逐条独立实证见结论表；无任何反证）。F379 的 `resolve_chapter_path(path, None)` 抛 `UnresolvedPathError`（contracts/paths.py:28-34 本轮读码实证）；F371 的 G4 CLI 实证：`final-snapshot/` 目录 → `{"status":"FAIL","must_fix":["G4.gen.read_error:final-snapshot/"]}`（不存在则 `not_found`）——两态恒 FAIL。
5. **本轮最重要新发现（F3AF，P1）**：`run_gate_g3`（dispatch_helper.py:1933-1944）在 progress.json 缺失时**预写伪造的评分证据**（`current_scorer_agent: "pipeline-g3-scorer-<uuid>"`、`scoring_history: [{"agent": "pipeline-skill-generator", "g2_passed": True}]`）再调 G3 CLI——`gates/g3.py` 的 G3.4（`scoring_independence_status`，g3_independence.py:18-25 的 **fail-closed** 逻辑：缺 current_scorer_agent 即 FAIL）因伪造的 scorer 存在而恒 PASS、G3.5 因伪造条目用 `agent` 键而非 `agent_id` 键而 prior_agents 恒空 → **G3 独立性门在管线内所有调用路径（genesis step 17 / closure step 4/8 / 串行 review-resonance）以伪造证据自证通过**——F345 只覆盖了"并行波不调 G3"，未覆盖"串行调用路径的 G3 也被调用方自我满足"（本轮以空目录实测：`run_gate_g3('shenbi-foundation-review', d)` → `G3 result status: PASS`，progress.json 被写入伪造 scorer）。与 F345 叠加后 G3 门在全路径无任何真实校验。
6. **版本偏移注记（沿用三~十七轮）**：生产 pipeline-state 为 Plan 18 重构前旧版管线产物（steps_done 含旧 skill 名），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据；`which codex → /opt/homebrew/bin/codex`（本环境 IDE 路径激活）。

---

## 一、复核结论总表（F300-F399 + F3A0-F3AE 逐条判定）

> 判定基于本轮独立 read/grep/uv run python/G4-CLI/prompt-build/沙箱实验与真实产物对照。全部 34 文件逐行重读。
> **误报 1 条（F325，二轮撤销、三~十七轮维持，本轮独立复证维持）**；**F348 为"成立但机制描述部分不准"（非误报）**；**F378 影响面修正维持（十二轮）**；**F358 机制前置修正维持（九轮）**；**F364 影响描述事实注记（见〇.3，不改成立性）**。
> 结论：91 条编号 finding 中 90 条成立/维持、1 条（F325）为误报（已撤销）——与前十七轮结论完全一致，本轮未发现新的误报；F3AF-F3B1 为本轮新发现。

### 初审（F300-F319 / F322-F323）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")`——源码字面量 `"\u003c"` 求值即 `<`，恒 no-op（`python3 -c 'print("\u003c"=="<")'` → True） |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 chapter_loop.py:2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1` → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析/趋势落盘）不可达（本轮逐行确认无其他调用点）；cs.resonance_score 并行路径恒 None |
| F302 | **成立** | P1 维持（**支持 P1→P0 依据，裁定留 stage 4**） | `dispatch_skill(state=)` 仅 chapter_loop.py:2799（串行可及）/ :1250（foreshadowing-track 死分支）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751 均未传（本轮 grep 实证）；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0（PipelineState 无 chapter 属性） |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（`_snapshot_chapter_files` 死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError；machine.clear_checkpoint 的 ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93） |
| F305 | **成立但框定需修正（同意二~十七轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文（本轮实测 chapter-10-character.md 等含"不构成 BLOCKING"）→ `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 记账漂移 |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方（本轮实测生产该路径 MISSING）；:97 子串匹配缺陷；volume_context 无消费方（_INJECT_FROM_CACHE 只注入 4 字段，:601-621） |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用；`_parse_hook_entries`（hook_planting.py:144）对生产 plan section 7 的 MH-001/H-N01 行会接受但 raw_op=="advance" 非 plant/open 被跳过（:151） |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state（state.py:383-428）无生产调用（grep 实证） |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036，但被 F324 屏蔽） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:428-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方（唯一生产 upsert 调用 chapter_loop.py:3014 为 upsert_markdown_row，F301 下不可达） |
| F313 | **成立** | P1 维持，无异议（注意 F379 交互） | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换（contracts/paths.py `_BOUND_N`）；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（契约 writes 实证）→ G4 校验错位；API/IDE 路径下步骤 6 在派发阶段即失败（F379） |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op；chapter_loop.py:2425-2426 调用无效果 |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（dispatch_helper.py:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback（:552）无章号绑定、注入下一次串行 dispatch（:2775-2780）；同源缺口见 F365 |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan（review_checklist.py:485-544）无生产调用（grep 实证；generate_review_checklist 本体已在 dispatch_helper.py:767 接线）；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符（双重死） |

### 一轮（F324-F334）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：生产 volume_map 全中文（`:7` `**章节范围**: 第1章 - 第15章（共15章）`、`:3` `## 第一卷：觉醒之火（第1-15章）`）→ `_END_RE`/`_RANGE_RE`（_shared.py:35-42，仅英文）0 命中；novel.json keys 无 total_chapters；pipeline-state phase=chapter-loop ch=56 closure_step=0。影响链（卷边界触发全家 / `cli.py:219 if total > 0:` 跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 章节节点提取裸数字 `\|\s*1\s*\|` 误中跨卷钩子表行）全部成立 |
| **F325** | **不成立（误报，维持二~十七轮撤销）** | **P1 裁定撤回维持** | 本轮 load_contract 实证 4 个 group skill 契约 writes 即旧式路径（group-factual→continuity/world-rules/pacing 等）；生产 audits/ 722 文件 13 类后缀全旧式名、group-* 数=0（本轮实测）；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物；真实 BLOCKING（`严重度: BLOCKING`）被 collect_audit_issues 正则命中 → route≠NO_REVISION → 文件扫描同文件必 True → revision 执行。残余真值（P2）：清单缺 resonance/review-summary、含生产不产生的 texture/reader-pull |
| F326 | **成立** | P1 维持，无异议 | 本轮 load_contract 实证：lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮实测）→ 并行 post-draft（chapter_loop.py:2405-2419）两线程均以 uses_staging 写同一 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用（grep 实证）；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）。本轮实测 `_parse_resonance_score(chapter-10-resonance.md) → None`（生产格式 `**结果**: 通过 (77/100)` 三模式均不匹配） |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代（grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议；注记：完整重复面见 F387 | 本轮实测 production genre-config auditDimensions.sensitivity=true → `get_active_genre_audits` 含 shenbi-review-sensitivity；core 波（:2551）与 genre 波双发同一 skill 写同一文件 |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接（本轮重读确认）；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进（本轮逐行确认）；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库（该路径被 F364 清空后实际"静默不入库"） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless 落入成功路径）；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失，grep 确认） |

### 二轮（F335-F344）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零（本轮 grep 实证）；生产 chapters/ 补零数=0（本轮实测 `ls | grep -c "^chapter-0"` → 0）；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict（:240-263）/from_dict（:332-343）无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；生产 review-summary.md 恒报 0 问题（本轮实测 chapter-1-review-summary.md 内容） |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-531 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死（本轮重读确认短路语义） |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 全路径无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用（grep 实证） |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:23-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |

### 三轮（F345-F352）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；本轮实测 6 个 audit skill 全部 requires_independent=True（group-factual/group-character/group-craft/group-plan/resonance/sensitivity）；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失。**注：串行路径的 G3 亦被调用方自证通过（见本轮新发现 F3AF）** |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 MH-001/H-N01 零匹配（本轮实测 `MH-001 → []`、`H-N01 → []`、`MH02 → ['H02']` 误截）→ master hooks 从 Route A 检索静默缺失 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys=`[title,project,version,last_updated,type,category,status,filled_by,last_chapter,track_chapter]`（无 `hooks` 键，本轮实测），hook 以 body 表格呈现；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)" |
| F348 | **成立但机制描述部分不准（维持四~十七轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入（load_contract 实证）；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md 生产均 MISSING，本轮实测）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层（:675-696） |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout（:1527/:1731/:1855） |

### 四轮（F353-F363）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F353 | **成立** | P1 维持，无异议（贴 P0 边界） | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748）与 triggers.py:394（调用点 :623）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 updates 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；grep skills/ 契约 0 个引用该文件；drafting 契约 reads `context/chapter-N-context.md` → 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断）每章每审计恒注入 |
| F358 | **成立但机制前置需修正（维持九~十七轮修正）** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError；drift 指令文件无消费者。机制链在更早处已断（F389：基线缺失 → `_check_linguistic_drift` 恒返回 None） |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra 被丢弃 → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358 |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录 → 重试重发已成功步骤 |

### 五轮（F364-F370）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F364 | **成立（本轮补事实注记）** | **P1 维持；支持 P1→P0（依据决策表"数据损坏/丢失 + 契约被静默违反"字面命中；裁定留 stage 4）** | 本轮 /tmp 沙箱独立复现：`register_emergency_handlers`（crash_recovery.py:66 atexit.register）后正常退出 → staging 被无条件清空（:144-148，`staging before exit: True` → 退出后日志 `staging_cleared`、目录 GONE）+ pipeline-state.json 的 current_step 被改写为 `EMERGENCY_SHUTDOWN_AT_unknown`（:113-127）（无信号、无异常）；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交）成立。**事实注记**：影响描述中"chapter-planning 无限循环"仅对 MODIFY 决策成立（MODIFY 回滚 step_index=1 重发 plan）；APPROVE 决策下管线以无 plan 状态继续推进——人审产物静默作废的判定不受影响 |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交 + 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`——step 2 时当章 plan 尚不存在 → FileNotFoundError → 捕获 → `_write_minimal_context_fallback`；且 chapter-planning 契约 reads 不含 context/chapter-N-context.md → 组装成功也不被消费 |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格 → 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |

### 六轮（F371-F372）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F371 | **成立** | P1 维持，无异议（注意 F379 交互） | 本轮 G4 CLI 实测：`shenbi-snapshot-manage "final-snapshot/"`（目录已存在且含 snapshot.md）→ `{"status":"FAIL","must_fix":["G4.gen.read_error:final-snapshot/"]}`（不存在则 `not_found`）——两态恒 FAIL；`_resolve_closure_g4_path`（closure.py:148-159）对无 N 路径原样返回目录 → closure step 10 重试×3 后 ESCALATION。API/IDE 路径下 step 10 在派发阶段即失败（F379），本 finding 的 G4 失效面在 legacy 路径与 F379 修复后激活 |
| F372 | **成立** | P2 维持（latent），无异议 | `_style_profile_is_stale`（triggers.py:103-124）`chapters_dir.glob("chapter-*.md")` 计数把 `chapter-N-pre-rev.md` 计入（F350 备份恰落入通配；对照 `_load_previous_titles` :2251 用 `chapter-(\d+)\.md` 正则排除）→ style-learning 自愈触发提前/误触发 |

### 七轮（F373-F378）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F373 | **成立** | P1 维持（潜伏），无异议 | `run_triggered_skills`（triggers.py:584）把字面 N 路径直接传 G4（gates/shared.py resolve_input_path 无 N 占位替换，本轮读码确认），dispatch 侧经 `resolve_chapter_path` 解析为章号（contracts/paths.py `_BOUND_N` 实证）→ 5 个 N 型触发步骤（memory-distill L2 / score-arc / score-stratum / score-volume / review-arc-payoff）G4 恒 not_found（前几轮 G4 CLI 实测 arc-N-score.md FAIL / arc-5-score.md PASS） |
| F374 | **成立** | P2 维持，无异议 | 本轮实测 `classify_skill_write_safety('shenbi-review-resonance').value → read_only_audit`（write_safety.py:45-46 前缀分类），而契约 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`（本轮 load_contract 实证）→ 并行审计波在无串行保护下并发写 truth 文件 |
| F375 | **成立** | P2 维持，无异议 | dispatch_helper.py:1667-1671 `missing` 非空仅 log.error 后 `return DispatchResult(True, ...)`（IDE 路径 :1770-1772 同款）；`_write_parsed_outputs`（:1153-1168）对 parsed 缺失路径走空内容 skip → state-settling 6+1 文件漏写其一仍 success，G4 只校验现存文件 |
| F376 | **成立** | M 维持，无异议 | truth_embed.py:243-251 `update` 无 `--text` 时落 `emit_json(OK)` 不做任何嵌入（本轮重读确认） |
| F377 | **成立** | M 维持，无异议 | llm_output_integrity.py:51 `VERDICT_MARKERS` 含通用词 "通过"，:149 `has_verdict` 恒 True → `G4.ac.no_verdict`（:150-153）几乎不触发（本轮重读确认） |
| F378 | **成立（影响面修正维持十二轮）** | P2 维持，无异议 | `context/volume-N-complete.json` 仅 chapter_loop.py:955-964 读取、全仓 0 写入方；`last_trigger_failure` 仅 triggers.py:575/593 写入、无消费者。十二轮修正成立：F324 现状下 `_find_current_volume(15,set()) → None` → `_check_volume_completion(None) → True`（:961-962 兜底）→ volume_objective_missed 信号当前不触发，F324 修复后激活 |

### 八轮（F379-F383）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F379 | **成立** | P1 维持，无异议 | 本轮全步骤 prompt-build 独立复现：closure step 2/4/5/6/10 抛 `UnresolvedPathError`（`truth/arcs/arc-N.md`、`audits/volume-N-score.md`、`audits/volume-N-payoff.md`、`audits/chapter-N-long-span.md`、`snapshots/chapter-NNN/*`）；`extract_chapter('Execute ... for book closure (step 2)...') → None`；`resolve_chapter_path(path, None)` 对含 N/NNN 路径抛 UnresolvedPathError（contracts/paths.py:28-34 本轮读码实证）→ `_dispatch_via_api`/`_dispatch_via_ide`（:1515-1516/:1718-1719）捕获后返回 DispatchResult(False,"Prompt build failed") → `_handle_closure_failure` 重试×3 → ESCALATION——10 步中 5 步永不派发 |
| F380 | **成立** | P2 维持，无异议 | genesis step 16（shenbi-anchor-curate，optional=True）契约 writes 含 `benchmarks/anchors/AC-NNN.md` → chapter=None 时抛 UnresolvedPathError → `_handle_failure`（genesis.py:222-225）对 optional 步骤直接跳过 → anchors 永不产出且无错误信号 |
| F381 | **成立** | P2 维持，无异议 | 本轮复现：`chapter-3-revision-decisions.json` 存在 + `revision_count=0` → `heal_state_counters` 返回 `['revision_count_healed:ch3:0->1']`。`_ensure_revision_decisions_exists`（:2759 对 NO_REVISION 章节也写回退文件）+ `_is_revision_routed`（:1928-1933 对 'no-revision' 返回 True）→ 全部未修订章节的 revision_count 被 heal 抬到 ≥1 |
| F382 | **成立** | P2 维持，无异议 | 本轮 grep：`safe_write(` 于 src/shenbi/pipeline/*.py 中 `round_dir` 命中数=0；`MARK_DONE`/`TraceWriter` 于 pipeline 无命中 → trace 目录只有 materialize 自造事件 → progress.json 物化为"全 pending"失真视图 |
| F383 | **成立** | P2 维持（latent/条件触发），无异议 | dispatch_helper.py:903-922 `_parse_file_outputs` 无 `### FILE:` 匹配时返回 `{"__stdout__": response}`；:1159 `content = parsed.get(rel_path, parsed.get("__stdout__", ""))` 把整段 stdout 写入每个字面输出路径；`_dispatch_via_ide`（:1753-1759）以 parsed=None 调用（本环境 `which codex → /opt/homebrew/bin/codex` → IDE 路径激活） |

### 九轮（F384-F390）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F384 | **成立** | P2 维持，无异议 | truth_embed.py:120-121 `embed_and_store` 函数体内每次 `st.SentenceTransformer("bge-large-zh")` 全新加载；genesis `_update_route_b`（:162-175）逐 hook/rule 调 embed_and_store + context_assemble.py:156 `_route_b` 同款每次加载 |
| F385 | **成立** | P2 维持，无异议 | dispatch_helper.py:1832-1841 无 API key 且无 IDE CLI → legacy 子进程；`dispatcher/modes/internal.py:22` internal 模式硬拒 → 必失败死路由；uses_staging 被忽略（:1844-1849） |
| F386 | **成立** | P2 维持，无异议 | `_check_soft_fail_escalation`（:967-1035）派发 escalation-review 但无任何 set_checkpoint（本轮 sed 逐行确认）；调用方随后把步骤正常推进（:2865-2905 落至 advance）→ 升级不暂停、报告成孤儿 |
| F387 | **成立** | P2 维持（贴 P1 边界），无异议 | 本轮实测 production genre-config auditDimensions 的 worldRules/motivation/dialogue/texture 全 true；group 波契约 writes（group-character→dialogue/motivation、group-craft→texture，本轮 load_contract 实证）与 genre 波单维 skill 契约 writes 同名 → 每章重复审计并覆盖 group 波产物（F329 的完整版） |
| F388 | **成立** | P2 维持，无异议 | `_audit_context_coverage`（chapter_loop.py:2104-2125）docstring 声称"pipeline resume 初始化时调用"，cli 从未接线（本轮 grep 0 命中） |
| F389 | **成立** | P2 维持，无异议 | `style/linguistic_baseline.json` 唯一写入方 `establish_baseline`（skill_utils/drift_detection/baseline.py）全仓 0 调用方（grep 实证）；`_check_linguistic_drift`（chapter_loop.py:2013-2019）缺基线 → `return None` → spec §3.4 三级干预生产不可达 |
| F390 | **成立** | M 维持，无异议 | cli.py:533-535 `Path(args.feedback).read_text(...)` 在 try 块内；:569-571 `except FileNotFoundError: "project not found"` → feedback 文件缺失误报项目不存在 |

### 十轮（F391-F395）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F391 | **成立** | P2 维持，无异议 | dispatch_helper.py:3-6 模块 docstring 声称 "The dispatcher runs G1 and G2 internally"；:1827-1841 API/IDE 两条路径均无 G1/G2 调用（本轮 grep `run_gate_g1\|run_gate_g2` 于 src/shenbi/pipeline/ 0 命中）；`_resolve_read_path`（:390-407）对缺失文件返回 [] → 缺失 reads 静默排除 |
| F392 | **成立** | P2 维持，无异议 | `_write_one`（:1113-1119）`_validate_json_output` 的 ValueError 被 raise 重抛；`_dispatch_via_api`（:1657-1663）仅捕 DispatchWriteFailureError → ValueError 冒泡至 CLI 顶层 |
| F393 | **成立** | P2 维持，无异议 | `_extract_hook_deliverables`（review_checklist.py:315-375）只解析 frontmatter `hooks` 列表（无 body 兜底）；生产 pending_hooks.md frontmatter 无 `hooks` 键 → 注入每个审计 prompt 的 hook_deliverables 恒空 |
| F394 | **成立** | M 维持，无异议 | crash_recovery.py:39-42 注释声称 "remove any atexit hooks registered by earlier tests" 但从未调用 atexit.unregister（grep src/ tests/ 0 命中） |
| F395 | **成立** | M 维持，无异议 | dispatch_helper.py:1548-1551 `except Exception` 对任何 API 异常调用 `_handle_timeout_gracefully`（:116-126 仅 log，无保存动作）→ 非超时错误也打 "dispatch_timeout ... saving_partial_output" 误导日志 |

### 十一轮（F396）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F396 | **成立** | P2 维持，无异议 | `_WARNING_RE`（triggers.py:326-329 `(?:warning|drift|fatigue)\s*[:\uff1a]\s*(.+)`）对 production `truth/audit_drift.md`（本轮实测 `- [维度] ...` 子弹格式，warning-re hits=0）与 drift-guidance 规范 YAML（`severity: warning` 在值位）均 0 命中 → spec §6.6 genre-config 更新触发恒 False，`config.genre_config_update_on_drift`（默认 True）全路径无功能效果 |

### 十二轮（F397-F399）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F397 | **成立** | **P1 维持；支持 P1→P0（决策表"数据损坏/丢失"字面命中；裁定留 stage 4）** | 本轮三重实证：(a) 契约 `write_semantics` 6 个累积文件全 `append_dedup`（本轮 load_contract 实测输出 `{current_state/particle_ledger/emotional_arcs/subplot_board/chapter_summaries: {mode: append_dedup, key: chapter}, pending_hooks: {mode: append_dedup, key: hook_id}}`）、`reads` 仅 `['chapters/chapter-N.md']`；(b) `_write_parsed_outputs` 整文件覆写（:1127），docstring :1061-1067 明示 append_dedup NOT branched here 并错误假设 "state-settling skill calls write_truth_file"（LLM skill 无法执行 Python）；(c) `write_truth_file` 唯一生产调用 chapter_loop.py:3014 在串行块（F301 下不可达，grep 实证）；(d) 生产 56 章书 `truth/chapter_summaries.md` 仅 2 章条目（本轮实测） |
| F398 | **成立** | M 维持，无异议 | cli.py:661-669：PER_CHAPTER approve 后 resume 时 `current_chapter=N+1`，而 `plans/chapter-(N+1)-plan.md` 由 step 2 才创建（守卫仅豁免 ch=1，`if ch > 1` 反而每章边界触发）→ 每章一次假阳性告警 |
| F399 | **成立** | P2 维持，无异议 | cmd_next（:588-618）无任何 checkpoint_history 检查、不调用转换；GENESIS_COMPLETE approve 后 next → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint 无转换、恒报 OK；VOLUME_BOUNDARY approve 后 next → step_index=1（C1）跳过触发块 → 延迟快照与 `_update_total_chapters`（仅 cmd_resume:748-755）永不执行 |

### 十三轮（F3A0-F3A1）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F3A0 | **成立** | P2 维持（latent），无异议 | `_build_skill_prompt` "Files to create" 清单跳过全部通配路径（dispatch_helper.py:718-721 `if "*" not in p`）；shenbi-character-design 契约 writes 含 `characters/major/*.md`、`characters/minor/*.md` 而 `gates/g4/character_design.py:118-155` 无条件要求 major ≥3 / minor ≥2 文件 → genesis step 3 与卷边界 expand 触发步 G4 缺口；生产 characters/ 仅 protagonist.md + relationships.md（本轮实测） |
| F3A1 | **成立** | M 维持，无异议 | dispatch_skill 的 test_type/round_dir/skip_reads 形参（:1796-1798）在 API/IDE 路由（:1827-1841）被静默丢弃，仅 legacy 子进程路径使用；docstring :1814-1816 声称三参数语义；生产调用方无 round_dir=（grep 实证） |

### 十四轮（F3A2-F3A4）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F3A2 | **成立** | M 维持（latent），无异议 | plan_skeleton.py:86 `next_chapter = min(chapter + 1, ch_end)`——卷末章（chapter==ch_end）时 `min(ch_end+1, ch_end)=ch_end=chapter` 自指 → :137-144 section 6 渲染 "Next chapter (Ch{N}) role:" 为同一章节点 |
| F3A3 | **成立** | M 维持，无异议 | machine.py:29-37 `load_state` → `PipelineState.from_json`（state.py:366-375 对损坏 JSON re-raise JSONDecodeError；from_dict 对无效枚举值抛 ValueError）；cli 六个命令全部仅捕 FileNotFoundError；main() 无顶层异常处理 |
| F3A4 | **成立** | M 维持，无异议 | `_append_integrity_findings`（dispatch_helper.py:1026-1041）对 `.integrity-findings-{N}.jsonl` 的无锁读-改-写（`existing = out.read_text(...) if out.exists() else ""` → 追加 → safe_write，无任何 lock）；并行审计波两线程并发追加同一诊断文件时 lost-update（诊断 jsonl，不影响正文） |

### 十五轮（F3A5-F3A7）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F3A5 | **成立** | M 维持，无异议 | cli.py:811 `for ch_num_str, ch_state in sorted(state.chapter_loop.chapter_states.items())`——`chapter_states` 的键是 `str(chapter)`（state.py:191），`sorted` 对字符串键做字典序 → "10" 排在 "2" 前（本轮 python 实证 ['1','10','11','2','3']） |
| F3A6 | **成立** | M 维持，无异议 | chapter_loop.py:2573-2575 并行波 wave 2 读取 genre-config.json 用裸 `json.loads`（无 try/except）；对照 review_checklist._load_genre_config（:239-244）对同一文件捕 `(json.JSONDecodeError, OSError)` → 损坏时穿透 CLI 裸崩 |
| F3A7 | **成立** | M 维持，无异议 | cli.py:750-755 `cmd_resume` VOLUME_BOUNDARY approve 分支的延迟 snapshot-manage 派发结果被静默丢弃（无 success 检查、无 G4、无失败 checkpoint）；对照 closure step 10（closure.py:253-265）有完整失败语义 → 卷快照欠账无信号 |

### 十六轮（F3A8-F3AA）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F3A8 | **成立** | P2 维持，无异议 | `_check_content_size_guard`（dispatch_helper.py:930/973-985）`new_size = len(new_content)`（字符数）与 `original_size = full_path.stat().st_size`（UTF-8 字节数）混用单位 → 生产中文 1 字≈2.7 字节（本轮实测 chapter-5.md 8040 字→21570 字节）→ 阈值 0.20 的字节语义被放大为约 60% 字符保留率 → 保留 ≤50% 原文的合法缩改 revision 被误拦截 |
| F3A9 | **成立** | P2 维持（latent），无异议 | context_curation.py:327/:339 对 spine_hooks/hooks 条目直接 `h['id']` 下标访问（:334 用 `h.get("id","")`——同函数内不一致）→ 一条缺 id 的 hook 记录即 KeyError → `curate_context` 全 9 节文档不产出（被 chapter_loop.py:1221-1222 try/except 吞掉只剩 warning） |
| F3AA | **成立** | M 维持，无异议 | filelock_utils.py:126-131/134-149 ReadLock 默认 30s 超时 `__enter__` raise TimeoutError；cli.py:473-487（cmd_status）/793-802（cmd_chapters）仅捕 FileNotFoundError；对照 cmd_init（cli.py:385）显式捕 TimeoutError |

### 十七轮（F3AB-F3AE）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F3AB | **成立** | P2 维持，无异议 | `_extract_chapter_title`（chapter_loop.py:2236）与 `_load_previous_titles`（:2261）用 `re.match`（位置 0 锚定）提取标题；生产 56 个章节文件 50 个以 `<!--META-BEGIN-->` 开头、0 个以 `# ` 开头（本轮实测）→ 标题恒空 → G4.cd.title 重复标题检测在正常路径完全失效（`re.match` None vs `re.search` '试探' 实证） |
| F3AC | **成立** | P2 维持，无异议 | chapter_loop.py:2644-2653（settling 失败 → `handle_state_settle_failure` → ESCALATION + pause）；:2655-2662（lifecycle 失败 → 仅 log.error，无 _handle_failure 重试/升级）；:2678-2681（无论成败都 `add_step_done` + `_reset_retries`）→ lifecycle dispatch 失败静默吞并、hook 更新丢失且不可重试 |
| F3AD | **成立** | M 维持，无异议 | filelock_utils.py:85/:129 `self._lockfile.parent.mkdir(parents=True, exist_ok=True)` 在获取锁之前创建项目目录 → 只读命令 `cmd_status`/`cmd_chapters` 对不存在的项目目录静默创建空目录（本轮 /tmp 实验：`ReadLock` 后 `d.exists() → True`） |
| F3AE | **成立** | M 维持，无异议 | cli.py:490-499（cmd_status 无 status 键）、:814-819（cmd_chapters 无 status 键），与 cli.py:15-17 模块 docstring "Result-envelope status values use the typed CommandStatus vocabulary" 矛盾；cmd_rollback（:835）裸字面量 "not_implemented"（subparser 已移除，latent） |

**误报统计**：F300-F399 + F3A0-F3AE（91 条编号）中仅 **F325** 为误报（二轮撤销、三~十七轮维持，本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名 + `_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：3 条（均为既有异议的独立复证支持，无新增异议目标）：
1. **F397（P1→P0）**：决策表 P0"数据损坏/丢失"字面命中——state-settling 累积 truth 文件（chapter_summaries/emotional_arcs/particle_ledger/subplot_board）每章被整文件覆写为仅当章内容、前章行确定性丢失且无任何错误信号（本轮契约+生产三重复证：56 章书 chapter_summaries 仅 2 章条目）。十七轮补充论据成立：truth 累积文件本身就是 pipeline 的数据资产，"正文文件不丢"的保留理由不成立。**裁定留 stage 4**。
2. **F364（P1→P0）**：决策表"数据损坏/丢失 + 契约被静默违反"字面命中——正常退出确定性清空待审 staging（本轮沙箱复现）+ 持久化 current_step 被改写为 EMERGENCY_SHUTDOWN；且这是三者中确定性最强的一条（正常路径必现、直接摧毁 staging 门控工作流）；但正文文件不丢、人工 review 仍可换用 resume 之外的途径介入，P1 亦可辩护。**裁定留 stage 4**。
3. **F302（P1→P0）**：决策表 P0 例字面列明 "TokenLedger 少计"——账本系统性少计 + chapter 恒 0；但账本为成本侧车不影响正文执行，P1 亦可辩护。**裁定留 stage 4**。

另注记（非正式异议）：F353/F371/F373/F379 贴 P0 边界（"pipeline 永不完成"根因族，F324 修复后激活）。

**事实修正（不改严重度）**：1 条——F364 影响描述的事实注记（见〇.3：approve 决策下管线以无 plan 状态继续推进而非"卡死于 chapter-planning 无限循环"，后者仅 MODIFY 决策成立；"人审产物静默作废"核心判定不变）。

---

## 二、新发现（F3AF-F3B1）

> 编号说明：F400-F4ZZ 已由 Z4 分区占用；按任务书"若与 Z4 冲突用 F3A0-F3ZZ 替代"沿用 F3A0 段（全仓检索 F3AF-F3ZZ 无占用，无冲突）。

### F3AF | run_gate_g3 在 progress.json 缺失时预写伪造评分证据（current_scorer_agent/g2_passed），G3.4 fail-closed 独立性检查恒 PASS——G3 门在全部调用路径（含串行 genesis/closure）以自证证据通过，与 F345 叠加后 G3 全路径零真实校验 | error | **P1**（AGENTS.md G3.4 显式契约"Scoring MUST use an independent subagent"被静默满足；P2 亦可辩护——门在缺 progress.json 时本会 SKIP 而非 FAIL，但伪造证据使 fail-closed 修复永久不可达）

- 证据：
  - `dispatch_helper.py:1933-1944`（`run_gate_g3`）：`pp = rd / "progress.json"`；`if not pp.exists(): safe_write(pp, json.dumps({"current_scorer_agent": f"pipeline-g3-scorer-{uuid4().hex[:12]}", "scoring_history": [{"agent": "pipeline-skill-generator", "g2_passed": True}]}))`——在调用 G3 CLI **之前**把"评分独立性证据"预填为合成值（管线内从未派发任何独立评分 subagent）；
  - `gates/g3.py:206-231`：G3.4 调 `scoring_independence_status`（`gates/g3_independence.py:18-25`，模块 docstring 明示 **fail-closed**："无 current_scorer_agent 证据 → FAIL"）——伪造的 scorer 使 fail-closed FAIL 分支永不触发（`agent_trace` 缺失 → 直接 PASS）；G3.5 读 `entry.get("agent_id", "")` 而伪造条目键为 `agent` → prior_agents 恒空 → PASS；
  - 调用点（round_dir 均传 project_dir → 伪造 progress.json 落入项目目录）：`genesis.py:345`（step 17 shenbi-foundation-review，`requires_independent=True` 本轮实证）、`closure.py:269`（step 4/8）、`chapter_loop.py:2909`（串行 review-resonance，F301 下不可达）；
  - `g3_independence.py` 的存在证明代码库已知 fail-open 问题并做了 fail-closed 修复，run_gate_g3 的脚手架直接击穿该修复。
- 根因：run_gate_g3 的"progress.json 脚手架"把评分证据预填为假值，与 fail-closed 独立性语义冲突；管线从未把"派发独立评分 subagent 并记录其 agent_id"这一 G3.4 前置动作实现出来，却用伪造记录让门通过。
- 验证命令+输出（本轮实际运行）：
  ```
  uv run python -c "
  import tempfile, json
  from pathlib import Path
  from shenbi.pipeline.dispatch_helper import run_gate_g3
  d = Path(tempfile.mkdtemp())
  res = run_gate_g3('shenbi-foundation-review', d)
  print('G3 status:', res.get('status'))
  data = json.loads((d/'progress.json').read_text())
  print('scorer:', data.get('current_scorer_agent'), '| history:', data.get('scoring_history'))
  print('agent_trace:', data.get('agent_trace'))"
  # → G3 status: PASS
  #   scorer: pipeline-g3-scorer-91bd26e2a9a0 | history: [{'agent': 'pipeline-skill-generator', 'g2_passed': True}]
  #   agent_trace: None
  #   （空目录、无任何真实评分 → G3 以伪造证据 PASS，progress.json 被写入误导性记录）
  ```
- 影响：AGENTS.md G3.4（"Scoring MUST use an independent subagent (G3.4); dispatcher-scored results are invalid"）在管线内被静默满足——**F345 只覆盖"并行波完全不调 G3"，本 finding 覆盖"串行路径调了 G3 但被调用方自证通过"**，两者叠加后 G3 门在全路径无任何真实校验（并行路径不调用、串行路径证据伪造）；progress.json 被写入伪造的 current_scorer_agent 与 g2_passed=True 记录，与 F382（物化失真）叠加后 progress.json 完全不可信；任何消费 progress.json 做评分审计的消费者（如 G1.6 对 progress.json 的依赖）读到假数据。
- 建议方向：`run_gate_g3` 的脚手架改为创建**无 current_scorer_agent** 的最小 progress.json 结构（让 fail-closed FAIL 如实触发并走 `_handle_failure` 重试/升级），或在 requires_independent 技能成功后真正派发独立评分 subagent 并把其 agent_id 写入 progress.json 的 `current_scorer_agent` + `agent_trace`；与 F345 的并行波 G3 接线一并处置（"G3 有效性"族：并行波接线 + 串行路径证据真实性）。

### F3B0 | check_audit_line_refs 的 `_LINE_REF_RE` 仅匹配英文 `L(\d+)` 行号格式，生产 15/722 审计文件用「第N行」中文格式 → G4.av.stale_line_ref 对这些审计静默失效（格式契约断裂，F324/F396/F389 同族） | error | M

- 证据：
  - `llm_output_integrity.py:67`（`_LINE_REF_RE = re.compile(r"L(\d+)(?:-(\d+))?")`）与 `:165-183`（`check_audit_line_refs`：仅用该正则从审计文本提取行号并对照章节行数）；
  - 生产格式混合（本轮实测）：audits/ 722 文件 134 个含 `L\d+`（resonance 等，`chapter-10-resonance.md` 用 `L46/L167/L182-183`）、**15 个含 `第N行`**（`chapter-5-character.md`、`chapter-10-continuity.md`、`chapter-25/31-memo-compliance.md`、`chapter-31/39-sensitivity.md`、`chapter-5-world-rules.md`、`chapter-51-continuity.md` 等）；
  - `chapter-5-character.md:75` 为 `**关于 \`微微\`**：第75行"路面上灰褐色的粉末在晨光里微微反光"……`——真实章节行号引用，L 格式 0 命中（本轮实测 `L-format hits: [] / 第N行 hits: ['第75行']`）；`check_audit_line_refs(chapter-10-character.md) → []`。
- 根因：行号引用解析器按英文 L 格式实现，未覆盖生产部分审计（shenbi-review-character/world-rules/memo-compliance/sensitivity 等）实际产出的 `第N行` 格式；与 F324/F396/F389 同族的"解析器与生产写入方格式断裂"（本轮为第三/第四个实例）。
- 影响：这些审计的"行号引用超出章节行数 → 审计针对旧版本章节"检测（G4.av.stale_line_ref，FAIL 级发现，经 `gates/g4/generic.py:170-207` 的 `g4_post_write_integrity` 汇入 G4 结果）对中文行号格式永不触发——陈旧审计的失效保护缺位（L 格式 134 文件仍工作，故 M 级；且该消费面在并行波无 G4 路径下已部分 inert（F301））。
- 建议方向：`_LINE_REF_RE` 增加 `第(\d+)(?:-(\d+))?行` 分支（与 L 格式并列），并补一条以生产 chapter-5-character.md（第N行）为 fixture 的测试；与 F377/F3AB 的"检查器与生产格式对齐"族合并处置。

### F3B1 | truth_embed main() 的 emit_json 用裸字面量 "degraded" 作 status 值（非 CommandStatus 成员），违反 spec D3 类型化信封约定（F3AE 同族、独立实例） | error | M

- 证据：`truth_embed.py:247`（`emit_json({"status": CommandStatus.OK if ok else "degraded", "chunk_id": chunk_id})`）——embed 失败时 status 为裸字符串 `"degraded"`；对照其余命令均用 CommandStatus 成员（`truth_index.py:363`、`context_assemble.py:396` 用 `CommandStatus.OK`）；F3AE 只覆盖 cmd_status/cmd_chapters 缺 status 键与 cmd_rollback 裸字面量（:835），未覆盖本实例；`src/shenbi/status.py` 的 CommandStatus 枚举（本轮读码确认）无 `degraded` 成员。
- 根因：main() 的 update 分支在失败路径直接写裸字面量，绕过 D3 信封约定（与 F376 的"无 --text 静默 OK"同函数不同面）。
- 影响：机器消费方按 D3 契约断言 `status in CommandStatus` 时对 "degraded" 解析失败；该命令为维护入口（pipeline-truth-embed），无执行影响——M 级。
- 建议方向：改 `CommandStatus.DEGRADED`（枚举不存在则复用 `ERROR` 并携带 chunk_id 详情），或与 F3AE 的"CLI 信封约定族"合并清理（status/cmd_chapters/rollback/truth_embed 四处于一处统一）。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件逐行重读；`__init__.py` 1 行 → `chapter_loop.py` 3053 行 → `dispatch_helper.py` 1986 行 → `cli.py` 939 行 → `triggers.py` 656 行 → `state.py` 536 行 → `review_checklist.py` 595 行等全部读完；`contracts/paths.py`、`gates/g3.py`、`gates/g3_independence.py`、`gates/g4/chapter_drafting.py`、`gates/g4/generic.py`、`dispatcher/modes/internal.py`、`skill_utils/escalation/check.py`、`safe_write.py` 作为证据文件交叉阅读）。
- 本轮验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行全中文格式 → `_END_RE`/`_RANGE_RE` 0 命中；novel.json keys 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0；audits/ 722 文件 13 类后缀全旧式名、group-* 数=0、134 个含 L 行号 + 15 个含 第N行（F3B0 依据）；chapters/ 56 文件 50 个以 `<!--META-BEGIN-->` 开头、0 个以 `# ` 开头（F3AB 依据）；truth/chapter_summaries.md 仅 2 章条目（F397 依据）；truth/audit_drift.md `- [维度]` 子弹格式对 `_WARNING_RE` 0 命中（F396 依据）；truth/style_profile.md 缺失而 style/style_profile.md 存在（F357 依据）；characters/ 仅 protagonist.md + relationships.md（F3A0 依据）；style/ 仅 style_profile.md 无 linguistic_baseline.json（F389 依据）；genre-config auditDimensions 全 true（F387 依据））；`uv run python` 实证：**run_gate_g3 空目录 → G3 PASS + progress.json 伪造记录**（F3AF，本轮唯一新验证手段）、**`_validate_json_output`/`_parse_resonance_score`/`check_audit_line_refs` 对生产文件**（F392/F327 复现 + F3B0 实证）、**`heal_state_counters` revision_count 抬升**（F381 复现）、**closure 10 步全量 prompt-build**（F379 复现）、`requires_independent` 6 审计 skill 全 True（F345）、`classify_skill_write_safety('shenbi-review-resonance')`（F374）、load_contract 契约 reads/writes/updates/write_semantics（group-*/resonance/lifecycle/state-settling/volume-outlining/anchor-curate/snapshot-manage/character-design）；**G4 CLI 直接执行**（F371 复现：final-snapshot/ 两态恒 FAIL；F373 复现：arc-N-score.md FAIL / arc-5-score.md PASS）；**/tmp 沙箱 atexit 退出实验**（F364 独立复现：staging GONE + current_step=EMERGENCY_SHUTDOWN_AT_unknown）。
- 前十七轮覆盖空洞（本轮已补）：(1) **run_gate_g3 的 progress.json 证据伪造**——F345 只覆盖"并行波不调 G3"，从未检查串行 G3 调用路径的 `run_gate_g3` 在调 G3 CLI 前的 progress.json 脚手架内容（前十七轮均以"串行路径 :2907-2917 有 G3"为据认为该路径受保护）→ F3AF（G3 门全路径零真实校验的第二个独立根因）；(2) **check_audit_line_refs 的行号格式断裂**——F377 覆盖 VERDICT_MARKERS、F3A4 覆盖 integrity jsonl 写竞态，均未对照 `_LINE_REF_RE` 与生产「第N行」格式 → F3B0；(3) **truth_embed 的 "degraded" 裸字面量**——F3AE 覆盖 pipeline CLI 信封键，未覆盖 pipeline-truth-embed 入口 → F3B1。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 的实际 codex 输出格式未做端到端执行验证（F383/F392 为静态推演 + 单元实证）；(c) 测试集未全量重跑（前几轮 collect 994）；(d) skills/ 契约文件与 gates/ 检查器作为证据使用（不在 Z3 清单内），group skill 契约旧式 writes 是 F325 反证的关键、g3.py/g3_independence.py 是 F3AF 证据的关键。

---

## 四、汇总

- **新 finding 数**：3（F3AF = P1、F3B0 = M、F3B1 = M；**无新增 P0**）
- **误报数**：0 新增（F300-F399 + F3A0-F3AE 中仅 F325 为误报，二~十七轮已撤销、本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名 + `_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物；F348 维持"成立但机制描述部分不准"）
- **严重度异议数**：3（F302、F364、F397 的 P1→P0 依据均独立复证支持并裁定留 stage 4——F397 本轮契约+生产三重复证、F364 本轮沙箱复现（附 approve/MODIFY 影响面事实注记）、F302 账本少计为成本侧车 P1 亦可辩护；F353/F371/F373/F379 贴 P0 边界注记维持）
- **事实修正（不改严重度）**：1（F364 影响描述注记——"chapter-planning 无限循环"仅对 MODIFY 成立，APPROVE 下管线以无 plan 状态继续推进；"人审产物静默作废"判定不变）
- **0 新 finding 判定**：**否（未收敛到 0）** —— 本轮仍发现 1 条 P1 漏报（F3AF G3 门自证通过：AGENTS.md G3.4 显式契约被伪造证据静默满足，与 F345 构成 G3 全路径失效的两个独立根因）与 2 条 M（F3B0 行号格式断裂、F3B1 degraded 裸字面量）。自十二轮新增 F397（P1，贴 P0）以来，十三~本轮连续六轮均无新增 P0，且 F3AF 属"gate 证据真实性"类别（非新的主路径正确性缺陷——管线主路径的 P0/P1 级结论经本轮独立复证全部成立）；P0/P1 级结论（F324 成立、F325 误报、F326/F340/F341/F345/F353/F354/F364/F371/F373/F379/F397 成立）经本轮独立复证全部成立，**无任何反证**——前十七轮的全部 P0/P1 判定无一误报。
- **校准升级提示**：F3AF 建议并入 F345 的"G3 门禁有效性"族一并处置（并行波接线 G3 + 串行路径证据真实性/独立评分 subagent 落地），修复后必测"无独立评分发生时 G3 应 FAIL 而非 PASS"；F3B0 建议并入 F377/F3AB 的"检查器与生产格式对齐"族；F3B1 建议并入 F3AE 的"CLI 信封约定"族；F324/F353/F371/F373/F379 的"pipeline 永不完成"根因族维持阶段 4 复核优先级首位。

---

## 五、独立复核声明

本报告为 fresh-context 第十八轮独立复核：34/34 文件逐行重读（13,641 行）；对 F300-F399 + F3A0-F3AE（91 条编号）的判定全部基于本轮独立 read/grep/uv run python/G4-CLI/prompt-build/沙箱实验与真实产物对照（未复用前十七轮结论）；新增 3 条发现（F3AF-F3B1）均为前十七轮报告未以编号形式出现的观察，其中 F3AF 的验证手段（`run_gate_g3` 对空目录的执行 + G3.4/G3.5 消费路径 + `g3_independence.py` fail-closed 意图的三方对照）为前几轮未覆盖的新证据路径；F324/F325/F364/F371/F373/F379/F397/F340/F341/F345/F353/F354/F381/F392/F3A0/F3A2/F3A3/F3AB 的关键实证为本轮独立复现。
