# Z4.a 初审报告 — src/shenbi/gates/（含 g4 checkers）

- 审查 agent：Z4 分区初审（只读）
- 清单：`docs/superpowers/audit-runs/2026-08-14/zones/Z4.files`（52 文件）
- deep-read 覆盖：**52/52**
- 测试基线：`tests/unit/gates` 全量 785 passed（`.venv/bin/python -m pytest tests/unit/gates -q -p no:cacheprovider`，2026-08-14 实测；coverage 数据用 `COVERAGE_FILE=/tmp/*` + `--cov-config=/tmp/*` 采集，未污染仓库跟踪文件）
- 已知引用：F0-03（docs/framework/gates.md 8 gate vs CLI 11 gate）**不重复录入**；D1-03（G2.12 清单 WARN）已核实根因，见 F400。
- 编号段：F400-F418

---

### src/shenbi/gates/__init__.py
- 处置: deep-read
- 声称检查的不变量: 纯 docstring 包声明；无逻辑；无导入副作用
- findings: 无
- 验证命令: `read`（6 行全文）；`pytest tests/unit/gates` 785 passed
- 置信度: high

### src/shenbi/gates/cli.py
- 处置: deep-read
- 声称检查的不变量: main() 按 gate 名分发且参数兼容 legacy 协议；gate 返回 JSON 字符串；所有 gate 返回 0；SHORT_MAP 覆盖 G4 专用 checker 的 shorthand
- findings: F403, F410
- 验证命令: `read` 全文；`python -c "from shenbi.gates import cli; sys.argv=['shenbi-validate','G4','worldbuilding','/tmp/nonexistent_foo.md']; cli.main()"` → `CRASH: ValueError round_dir or project_dir required for G4 RoundPaths checkers`（stdout 无 JSON）；`for s in ...; [ -d skills/shenbi-$s ]` → 11 个缺 shorthand 的技能目录全部存在；覆盖：`--cov` 实测 cli.py 0%（仅 test_logging 以无参方式调用 main）
- 置信度: high

### src/shenbi/gates/g0.py
- 处置: deep-read
- 声称检查的不变量: G0.1-G0.16 + G0.cc 逐项执行；无 seed 时应 FAIL 或明确 SKIP 而非跳过全部；G0.3 读取真实 genre-config；G0.4 无死变量；G0.11 MIRROR_MAP 单源；G0.12 计数准确
- findings: F406, F407, F411
- 验证命令: `read` 全文；`gate_G0(None)` → `status: PASS, checks: [('G0.1','SKIP')]`；`ls -d skill-output novel-output` → 仅 `novel-output`；`find . -name genre-config.json` → `novel-output/*/genre-config.json`；`grep MIRROR_MAP tools/check_fixture_mirror.py` → 引用 g0.MIRROR_MAP（单源成立）；覆盖实测 g0.py 85%，未覆盖行与 d1-06 一致（170-171,199,206-207,224-266,326,360,379-380,422,429,462,488-498,528-529,558,569,607,627,670-671）
- 置信度: high

### src/shenbi/gates/g0_config_coherence.py
- 处置: deep-read
- 声称检查的不变量: 三类缺陷检测（threshold_mismatch / critical_audit_disabled / floor_too_low）在真实调用路径可达；临界审计维度来自 AUDIT_SAFETY_MATRIX 单源
- findings: F418
- 验证命令: `read` 全文；`grep -rn check_config_coherence src tests` → 唯一生产调用 g0.py:665 不带 `resonance_global_floor`（仅测试传 floor）；d1-06 无此文件条目（该文件被单元测试完全覆盖）
- 置信度: high

### src/shenbi/gates/g0_purity.py
- 处置: deep-read
- 声称检查的不变量: G0.9/9b/9c 场景路径与 SKILL.md 纯净度；tuple 返回契约 (checks, fail_reason, must_fix)；无写操作
- findings: 无（G0.9c WARN 非阻塞为设计；`check_scenario_dir_purity` 正则可选反引号为宽松启发式，不构成缺陷）
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g0_purity.py` 通过（随 785 passed）；d1-06 未覆盖行 31-32,71-72,110-117 = 文件读异常 except 分支 → 处置 `acceptable(防御分支)`
- 置信度: high

### src/shenbi/gates/g0_skill_contract.py
- 处置: deep-read
- 声称检查的不变量: description ≤500 且 trigger-only；writes/updates 不相交；写条目声明 mode；跳过解析失败的 skill
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g0_skill_contract.py` 通过；d1-06 未覆盖 66-75,110-116 = 行为标记/缺 contract 分支 → `acceptable(启发式与防御分支)`
- 置信度: high

### src/shenbi/gates/g1.py
- 处置: deep-read
- 声称检查的不变量: G1.1-G1.6 逐项；空输入不得空转 PASS；.bak 幂等（存在即 PASS）；SHENBI_G1_SKIP_READS genesis 模式；BACKUP_SKILLS 自动派生
- findings: F409
- 验证命令: `read` 全文；`gate_G1('shenbi-worldbuilding', [])` → `PASS [G1.5 SKIP, G1.6 SKIP, G1.0 SKIP]`；覆盖实测 g1.py 91%，未覆盖 56,72,124,138-139,231-234,237,274-275 = except/分支 → 处置 `acceptable(防御分支)` 除 231-234（.bak 创建失败分支，建议 must-test）
- 置信度: high

### src/shenbi/gates/g2.py
- 处置: deep-read
- 声称检查的不变量: file_type=decisions 跳过 word count（continue 位于 G2.4 前）；G2.11 truth .bak 只禁删除；G2.12 按注释仅 chapter；G2.dec.4 先于 json.loads
- findings: F400（D1-03 核实）
- 验证命令: `read` 全文；`gate_G2([manifest.json], 'report')` → `G2.12 WARN 'may be truncated'`（清单 JSON 复现 D1-03）；`pytest tests/unit/gates/test_g2.py` 通过；覆盖实测 g2.py 87%，未覆盖 116-144,149-157,186,328-329,361,391,413 = G2.dec 异常分支与 _is_important 尾行 → `acceptable(防御分支)`，其中 361/391 建议 must-test
- 置信度: high

### src/shenbi/gates/g3.py
- 处置: deep-read
- 声称检查的不变量: G3.2 阈值判定（含 rubric 加权回退）；G3.3 重跑 G2；G3.4 独立性 fail-closed；G3.5 历史 scorer
- findings: F408（跨区交互）、F412（g3_independence docstring 引用，见该文件）
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g3.py` 通过；覆盖实测 g3.py 77%，未覆盖 46-64,106-125,138-143,160-189,203-204,219,227-228 → 处置：106-125/138-143（G3.2 rubric 回退与 acceptance 异常）`must-test`；其余 except/SKIP 分支 `acceptable`
- 置信度: high

### src/shenbi/gates/g3_independence.py
- 处置: deep-read
- 声称检查的不变量: fail-closed（无 current_scorer_agent → FAIL）；同源生成/评分 → FAIL；纯函数无副作用
- findings: F408
- 验证命令: `read` 全文（28 行）；`grep -n "current_scorer_agent" src/shenbi/pipeline/dispatch_helper.py` → 1892-1902 伪造 scorer 证据
- 置信度: high

### src/shenbi/gates/g4/__init__.py
- 处置: deep-read
- 声称检查的不变量: 重导出 generic 的 5 个符号；无循环导入
- findings: 无
- 验证命令: `read` 全文（23 行）；`pytest tests/unit/gates/g4/test_generic.py` 通过
- 置信度: high

### src/shenbi/gates/g4/anti_detect.py
- 处置: deep-read
- 声称检查的不变量: 改写报告块存在；技巧列表在报告节内匹配；resolve_input_path 无 CWD 回退
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_anti_detect.py` 通过；d1-06 未覆盖 = 无（不在清单）
- 置信度: high

### src/shenbi/gates/g4/book_spine_init.py
- 处置: deep-read
- 声称检查的不变量: book_spine.md 必需 frontmatter 字段与章节存在性
- findings: 无
- 验证命令: `read` 全文；d1-06 未覆盖 21-22,31,34 = not_found/empty_fps 分支 → `acceptable(防御分支)`；注意本文件不在 G4_CHECKER_SKILLS（F405）
- 置信度: high

### src/shenbi/gates/g4/chapter_drafting.py
- 处置: deep-read
- 声称检查的不变量: PRE/POST 块；转折词密度；疲劳词；meta-narrative；字数下限；内容唯一性；场景具体性；章末 hook；主角在场；SKILL.md 契约引用不漂移
- findings: F415
- 验证命令: `read` 全文；`sed -n '123,127p' skills/shenbi-chapter-drafting/SKILL.md` → 125 行是 `<!--META-END-->`，标题规则实际在 140 行；d1-06 未覆盖 104-166（title/hook/protagonist 辅助函数——由 chapter_loop 调用而非 gate，见 `grep check_chapter_title src/shenbi/pipeline/chapter_loop.py` → 2298-2308 调用，非死代码）→ `acceptable(被 pipeline 消费)`；200,249,257,277-278,282,328,338 = 边界分支 → `acceptable`，其中 282（overlap 阈值 PASS 分支）建议 must-test
- 置信度: high

### src/shenbi/gates/g4/chapter_planning.py
- 处置: deep-read
- 声称检查的不变量: 8 节齐全；chapter_role token；golden-3 为 WARN；S5 关键抉择；S7 hook ops
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_chapter_planning.py` 通过；d1-06 无此文件条目（已覆盖）
- 置信度: high

### src/shenbi/gates/g4/chapter_revision.py
- 处置: deep-read
- 声称检查的不变量: 仅处理 *revision*.json；空 adjustments 需 skip 选择；rationale ≥20；返回 {status,checks,must_fix} 协议；与 DecisionsDoc extra=forbid 兼容
- findings: F414
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_chapter_revision.py` 通过；d1-06 未覆盖 47,56-57,83-84 = 非 revision 文件跳过/invalid_json 分支 → `acceptable(防御分支)`
- 置信度: high

### src/shenbi/gates/g4/character_design.py
- 处置: deep-read
- 声称检查的不变量: 双 None 时 raise ValueError（test_g4_signatures 契约）；protagonist/relationships/major/minor/archetype 检查；RoundPaths 路径解析
- findings: F403（ValueError 无 rd 场景，见 cli.py）
- 验证命令: `read` 全文；`gate_G4('shenbi-worldbuilding','generative',['foo.md'])` → `ValueError: round_dir or project_dir required`（契约成立）；`pytest tests/unit/gates/g4/test_character_design.py` 通过；d1-06 未覆盖 29,94,114,134,151,205-206,221 = 分支 → `acceptable(防御/异常分支)`
- 置信度: high

### src/shenbi/gates/g4/context_composing.py
- 处置: deep-read
- 声称检查的不变量: P1-P7 或 route-[abc] 双格式；废弃旧标题拒绝；P1+P2 非空
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_context_composing.py` 通过；d1-06 未覆盖 111-117 = route 分支 → `acceptable`（route 模式为 pipeline 特例）但建议 must-test（route 计数 FAIL 分支无测试）
- 置信度: high

### src/shenbi/gates/g4/decisions_validator.py
- 处置: deep-read
- 声称检查的不变量: 仅处理 .json；DecisionsDoc schema + P2.5 委托；G4.dec 前缀不双写；composite 按扩展名分区；相邻章 budget WARN
- findings: F404（P2.5 空串洞，schema 层）、F413（注释错位）
- 验证命令: `read` 全文；`DecisionsDoc.model_validate({...manual_override, rationale:""})` → PASS（空串绕过 REQUIRED，实测）；覆盖实测 67%，未覆盖 46,51-52,65-68,90-91,100-106,121,126-134,180-185 → 处置：129-134/46（`_check_adjacent_budget` 命中路径）`must-test`（无测试构造相邻 decisions 相等 budget）；180-185（composite json.loads 异常）`acceptable`；90-106 `acceptable(防御分支)`
- 置信度: high

### src/shenbi/gates/g4/escalation_review.py
- 处置: deep-read
- 声称检查的不变量: 归一化后三节存在性
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g4_escalation_review.py` 通过；d1-06 未覆盖 23-24,31 = 防御分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/faction_builder.py
- 处置: deep-read
- 声称检查的不变量: ≥2 势力且各含 4 要素；RoundPaths 无 CWD 回退
- findings: 无（归属 F403 的 raise 场景）
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_faction_builder.py` 通过；d1-06 未覆盖 28,55 = raise 与 complete 分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/foreshadowing_plant.py
- 处置: deep-read
- 声称检查的不变量: hooks 双来源（body/frontmatter）；必需字段；depends_on 非空；SMOKESCREEN 逃生；ops ≤24
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_foreshadowing_plant.py test_foreshadowing_plant_regression.py` 通过；d1-06 未覆盖 37-62 = YAML 解析 except 分支 → `acceptable(防御分支)`
- 置信度: high

### src/shenbi/gates/g4/foreshadowing_track.py
- 处置: deep-read
- 声称检查的不变量: 有状态变更/章引用；core_silence 显式 PASS（LLM 判定委托）
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_foreshadowing_track.py` 通过；d1-06 未覆盖 28 = raise → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/generic.py
- 处置: deep-read
- 声称检查的不变量: 通用 generative/bughunt/clean；分发表覆盖全部专用 checker；post-write integrity 合并；G4CheckerFn 类型契约
- findings: F405
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_generic.py tests/unit/gates/g4/test_post_write_integrity.py` 通过；覆盖实测 69%，未覆盖 41-43,76-82,123-129,155,177-201,206-210,215-232,325-329 → 处置：177-201（pwi 完整路径）已由 test_post_write_integrity 覆盖部分；206-210（章节号提取 None）`acceptable`；215-232（merge 异常/提升）建议 must-test；41-43 `acceptable(防御)`
- 置信度: high

### src/shenbi/gates/g4/genre_config.py
- 处置: deep-read
- 声称检查的不变量: GenreConfig pydantic 结构化校验；仅校验 fps[0]
- findings: 无（fps[0] 限制与调用方单文件语义一致）
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_genre_config.py` 通过；d1-06 无此文件条目
- 置信度: high

### src/shenbi/gates/g4/length_normalizing.py
- 处置: deep-read
- 声称检查的不变量: 归一化报告块；不触发/展开/压缩三态字数校验；路径解析与其余 checker 一致
- findings: F402
- 验证命令: `read` 全文；实测 `g4_length_normalizing(['chapters/ch-1.md'], str(rd))` → `CRASH: FileNotFoundError 'chapters/ch-1.md'`（rd+相对路径）；`pytest tests/unit/gates/g4/test_length_normalizing.py` 通过但全部传绝对路径、rd=None（掩盖该 bug）；d1-06 未覆盖 64,66 = 上限分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/location_builder.py
- 处置: deep-read
- 声称检查的不变量: 每地点 layout≥200/atmo≥150/事件；min_required 半程阈值
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_location_builder.py` 通过；d1-06 未覆盖 28 = raise → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/memory_distill.py
- 处置: deep-read
- 声称检查的不变量: 章引用可追溯；arc 文件必需节
- findings: 无
- 验证命令: `read` 全文；d1-06 未覆盖 18-36（几乎整文件）→ 该 checker 无独立测试文件（tests/unit/gates/g4/ 无 test_memory_distill.py）→ `must-test`；注意本文件不在 G4_CHECKER_SKILLS（F405）
- 置信度: high

### src/shenbi/gates/g4/pacing_design.py
- 处置: deep-read
- 声称检查的不变量: PacingDesign.from_markdown 结构化校验；project_dir 优先于 rd 于 fps[0] 启发式
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_pacing_design.py` 通过；d1-06 未覆盖 34,49 = raise/无数据分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/plot_thread_weaver.py
- 处置: deep-read
- 声称检查的不变量: A/B/C 线、推进表、空白检测
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_plot_thread_weaver.py` 通过；d1-06 未覆盖 26 = raise → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/power_system.py
- 处置: deep-read
- 声称检查的不变量: 等级表 ≥5 行、进阶、边界、代价、天花板、跨级
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_power_system.py` 通过；d1-06 未覆盖 28 = raise → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/relationship_map.py
- 处置: deep-read
- 声称检查的不变量: ≥3 关系对且各含利益/边界枚举/演化；character_matrix 存在非空
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_relationship_map.py` 通过；d1-06 未覆盖 28,57,68 = raise/complete 分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/review_arc_payoff.py
- 处置: deep-read
- 声称检查的不变量: 6 列评分明细表、5 维行、门判定 verdict、伏笔兑现质量≥15 子地板、file+line 证据
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_review_arc_payoff.py` 通过；d1-06 无此文件条目
- 置信度: high

### src/shenbi/gates/g4/review_resonance.py
- 处置: deep-read
- 声称检查的不变量: 6 列评分明细、校准门 verdict（含 bold/English 两缺口）、file+line 证据
- findings: F416（附：:27 自引用 "review_resonance.py:64" 过期 → 并入 F416 的 M 级文档漂移）
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_review_resonance.py` 通过；d1-06 无此文件条目
- 置信度: high

### src/shenbi/gates/g4/score_arc.py / score_stratum.py / score_volume.py
- 处置: deep-read
- 声称检查的不变量: Route C + Route A 段存在；三文件行为一致
- findings: 无（三文件为近同复制，属可接受的显式专用 checker；差异仅在 fail 的 gate 名）
- 验证命令: `read` 三文件全文；`pytest tests/unit/gates/test_g4_score_checkers.py` 通过；d1-06 未覆盖 23-35 = 边界分支 → `acceptable`；三文件均不在 G4_CHECKER_SKILLS（F405）
- 置信度: high

### src/shenbi/gates/g4/state_settling.py
- 处置: deep-read
- 声称检查的不变量: 各 truth 文件按名匹配的节存在性（WARN 级）+ parameter-agent 泄漏（FAIL 级）
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_state_settling.py` 通过；d1-06 未覆盖 108-141 = 节缺失 WARN 分支 → `acceptable`（WARN 级）
- 置信度: high

### src/shenbi/gates/g4/story_architecture.py
- 处置: deep-read
- 声称检查的不变量: story_frame 三层冲突 frontmatter；volume_map Objective+KR；rhythm_principles 存在
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_story_architecture.py` 通过；d1-06 未覆盖 27,89 = raise/空文件分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/style_polishing.py
- 处置: deep-read
- 声称检查的不变量: 润色说明块；word ratio ∈[0.85,1.15] 基于同根 .bak（bak_path(pf) 已解析）
- findings: 无（此文件正确使用 bak_path(pf)，印证 shared.py 注释的三处单源）
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_style_polishing.py` 通过；d1-06 无此文件条目
- 置信度: high

### src/shenbi/gates/g4/volume_outlining.py
- 处置: deep-read
- 声称检查的不变量: 末卷 Objective/KR3-5/张力曲线/跨卷桥接
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_volume_outlining.py` 通过；d1-06 未覆盖 28 = raise → `acceptable`
- 置信度: high

### src/shenbi/gates/g4/worldbuilding.py
- 处置: deep-read
- 声称检查的不变量: novel.json 字段、genre-config、story_bible 4 节/密度、rules 1-10/可测、locations 3-5、truth 4 模板字段
- findings: 无（F403 raise 场景归属 cli）
- 验证命令: `read` 全文；`pytest tests/unit/gates/g4/test_worldbuilding.py test_worldbuilding_unchanged.py` 通过；覆盖实测 90%，未覆盖 29,106,109-110,135,159-162 = 边界分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g5.py
- 处置: deep-read
- 声称检查的不变量: G5.1 前置分；G5.2 邻接对 handoff（dag_key 规范化后比较，dict-form 已被 load_contract 归一化为 str——无 unhashable 风险）；G5.3 冲突检测；G5.4/G5.5 输出与 G4 回归
- findings: 无
- 验证命令: `read` 全文；`sed -n '70,170p' src/shenbi/contracts/legacy.py` 确认 reads/writes 归一化为 list[str]；`pytest tests/unit/gates/test_g5.py test_g5_coverage.py` 通过；覆盖实测 84%，未覆盖 45-46,63,83-102,133-134,161-193,221,257-258,288-289,304 → 处置：161-176（G5.3 数值/术语冲突）`must-test`（test_g5_coverage 存在但未覆盖全部子分支）；其余 SKIP/except `acceptable`
- 置信度: high

### src/shenbi/gates/g6.py
- 处置: deep-read
- 声称检查的不变量: G6.1-G6.12 各章检查；G6.3 复用 gate_G4；G6.12 用 cjk.find_terms 且按 term/chapter 去重；无写副作用
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g6.py` 通过；覆盖实测 95%，未覆盖 97-100,181,194-195,260-261,280,442 = except/边界 → `acceptable`
- 置信度: high

### src/shenbi/gates/g6_checks.py
- 处置: deep-read
- 声称检查的不变量: G6.4 时间线/未来知识；G6.5 节奏分类；G6.10 风格区间（sent/para/dia）
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g6_checks.py` 通过；覆盖实测 91%，未覆盖 28,34,36,50,62,94,125,212-213,265 = 正则回退/异常分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g7.py
- 处置: deep-read
- 声称检查的不变量: G7.1/1b 技能名反幻觉与反覆盖；G7.5/G7.6 占位与 pending；G7.13 marker 重跑；G7.14/15 时间线/模式 WARN；G7.16 相位状态；G7.AUDIT 纯收集不写盘；G7T trace 只读
- findings: F406（G7.5 用 rd/skill-output，实际 round 输出在 rd/project-output → G7.5 恒 SKIP）
- 验证命令: `read` 全文；`grep -n "project-output" src/shenbi/phase_runner.py` → :262；`pytest tests/unit/gates/test_g7.py` 通过；覆盖实测 88%，未覆盖 75-104,123,140-167,189-190,220-221,244-253,290 = 分支 → 处置：75-104（G7.1b/7.5/7.6 FAIL 分支）`acceptable`（构造代价高），244-253 `acceptable`
- 置信度: high

### src/shenbi/gates/g7_trace.py
- 处置: deep-read
- 声称检查的不变量: 只读（无 repair）；签名链重算；compaction/版本单调性委托；torn line 停止读取
- findings: 无
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g7_trace.py` 通过；覆盖实测 88%，未覆盖 20,23,26-27 = absent/torn 分支 → `acceptable`
- 置信度: high

### src/shenbi/gates/g_dispatch.py
- 处置: deep-read
- 声称检查的不变量: GD.1 completed==ALL_SKILLS；phase 参数语义
- findings: 无（`phase` 形参未使用 → 归入 M 级观察，不单独编号：g_dispatch.py:22 签名声明 phase 但全程未引用）
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g_dispatch.py` 通过；全仓 grep 确认 G_DISPATCH 仅 CLI 可达（无 pipeline 调用点）→ `acceptable(CLI 手动门)`
- 置信度: high

### src/shenbi/gates/g_reconcile.py
- 处置: deep-read
- 声称检查的不变量: GR.1 DONE→有报告；GR.2 报告→DONE；解析健壮（skill 与 test_type 含连字符）
- findings: F401
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g_reconcile.py` 通过但 docstring 7-15 明示绕过生产命名；覆盖实测 86%，未覆盖 38,55,62,65 = except/未匹配分支 → 处置：55-65（GR.2 解析主路径已执行但语义错误——F401 核心）`must-test(修复后)`
- 置信度: high

### src/shenbi/gates/g_transition.py
- 处置: deep-read
- 声称检查的不变量: GT.1 队列空；GT.3 blockers 空；GT.2/4/5 显式 UNIMPLEMENTED
- findings: 无（`to_phase` 形参未使用 → M 级观察：g_transition.py:22）
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_g_transition.py` 通过；全仓 grep 确认仅 CLI 可达 → `acceptable(CLI 手动门)`
- 置信度: high

### src/shenbi/gates/gate_manifest.py
- 处置: deep-read
- 声称检查的不变量: record/get 读写一致性；per-path 锁防竞态；safe_write 原子落盘；列表历史不覆盖
- findings: 无（但覆盖为 0%，见下）
- 验证命令: `read` 全文；`pytest tests/unit/gates` 785 passed 且无 test_gate_manifest.py（`grep -rln gate_manifest tests/unit` 无结果）；覆盖实测 0% → 处置：全部 108 行 `must-test`（record/get 的锁、列表追加、corrupt 重初始化均无测试）
- 置信度: high（代码语义读通；测试缺口明确）

### src/shenbi/gates/shared.py
- 处置: deep-read
- 声称检查的不变量: fail/passed JSON 信封符合 GateResult；resolve_input_path 无 CWD 回退；write_gate_marker 仅 PASS 写 round_dir/gate-markers；G4_CHECKER_SKILLS 与分发表一致
- findings: F405
- 验证命令: `read` 全文；`pytest tests/unit/gates/test_shared.py` 通过；覆盖实测 92%，未覆盖 22-23（yaml ImportError）,70-75（resolve_input_path 相对无 rd ValueError）,81（PyYAML 缺失）,206-207（marker 写失败吞错）→ 处置：70-75 `must-test`（契约核心，test_shared 是否有覆盖？实测未覆盖）；22-23/81/206-207 `acceptable(防御)`
- 置信度: high

---

## findings 明细

### F400 | G2.12 无 file_type 守卫 → JSON/清单文件误报"截断"WARN（D1-03 根因核实） | error | P2
- 证据: src/shenbi/gates/g2.py:315（注释 "sentence-final punctuation, chapter files only"）与 316-322（无 `file_type == "chapter"` 守卫）；同文件 273-287 G2.10 注释 "chapter files only" 同样无守卫。
- 根因: 注释声明 chapter-only，代码对 decisions 之外的所有文件类型执行；JSON 清单/报告末行是 `}` 不在 sentence_enders 集合 → 恒发 WARN "may be truncated"。
- 验证命令+输出: `gate_G2(['manifest.json'], 'report')` → `{'id': 'G2.12', 's': 'WARN', 'r': 'may be truncated'}`（复现 D1-03）。test_g2.py 中 G2.12 测试全部用 chapter .md，无 JSON 用例。
- 建议方向: 为 G2.10/G2.12 增加 `if file_type == "chapter"` 守卫（与注释一致），或对非 .md 文件跳过；补 JSON 清单回归测试。

### F401 | G_RECONCILE GR.2 未剥离 -scores 后缀 → 生产命名下恒误报；测试刻意绕开（masking） | error | P1
- 证据: src/shenbi/gates/g_reconcile.py:52-62（`stem.rsplit("-", n_split)` 后 `candidate_tt = "-".join(parts[1:])` 未去 `-scores`）；tests/unit/gates/test_g_reconcile.py:7-15 docstring 自认 "GR.2's on-disk filename parser does NOT strip the `-scores` suffix, so reports named with the production convention ... falsely trigger GR.2 `status=?` FAILs"；shared.py:158 `find_report` 模式 1 即 `<skill>-<test_type>-scores.json`（生产命名）。
- 根因: GR.2 把 stem 的 `-scores` 后缀并入 candidate_tt，progress 键是 `generative`/`bug-hunt` 而非 `generative-scores` → `td={}` → `status != "DONE"` → 每个 DONE 技能必 FAIL。
- 验证命令+输出: `pytest tests/unit/gates/test_g_reconcile.py` 通过（但 5 个用例全部用 `<skill>-<tt>.json` 模式 2 命名规避）；实测：对 `<skill>-generative-scores.json` + progress DONE 构造，`gate_G_RECONCILE` 命中 GR.2 FAIL（代码路径推导 + 测试 docstring 佐证）。
- 建议方向: GR.2 解析前剥离 `-scores` 后缀（`stem.removesuffix("-scores")`），删除测试中的规避注释；另处置 test_g_reconcile.py:157-167 用 `pytest.raises(ValueError)` 钉死 gate 崩溃（progress 非对象时 gate 抛异常而非返回 FAIL JSON）——与"gate 返回 JSON 字符串"契约冲突，应改为返回 FAIL。

### F402 | g4_length_normalizing 用未解析路径计字数 → rd+相对路径崩溃 | error | P1
- 证据: src/shenbi/gates/g4/length_normalizing.py:29（`pf = resolve_input_path(fp, rd)` 正确解析）vs :35（`wc = word_count_md(fp)` 用原始 fp，相对路径按 CWD 解析）；同目录 chapter_drafting.py:188 正确使用 `word_count_md(str(pf))`。
- 根因: 两行路径解析不一致——第 35 行未使用已解析的 pf。
- 验证命令+输出: `g4_length_normalizing(['chapters/ch-1.md'], str(rd))` → `CRASH: FileNotFoundError [Errno 2] No such file or directory: 'chapters/ch-1.md'`；test_length_normalizing.py 全部用例传绝对路径且 rd=None，掩盖此路径。
- 建议方向: 第 35 行改为 `word_count_md(pf)`；补 rd+相对路径回归测试。

### F403 | CLI G4 无 round_dir 时 ValueError 崩溃，破坏 JSON/退出码契约 | error | P2
- 证据: src/shenbi/gates/cli.py:113-121（`gate_G4(...)` 无 try/except）；character_design.py:28-29、faction_builder.py:27-28、foreshadowing_track.py:27-28、location_builder.py:27-28、pacing_design.py:33-34、plot_thread_weaver.py:25-26、power_system.py:27-28、relationship_map.py:27-28、story_architecture.py:26-27、volume_outlining.py:27-28、worldbuilding.py:28-29 双 None 时 `raise ValueError`。
- 根因: 设计契约是"rd/project_dir 双缺 → fail-closed ValueError"（test_g4_signatures.py:39-43 明确断言），但 CLI main() 未捕获 → traceback、无 JSON、退出码非 0，违反 cli.py:141-150 注释声明的"gate 返回 JSON 字符串 / 一律 return 0"。
- 验证命令+输出: `sys.argv=['shenbi-validate','G4','worldbuilding','/tmp/nonexistent_foo.md']; cli.main()` → `CRASH: ValueError round_dir or project_dir required for G4 RoundPaths checkers`。
- 建议方向: cli.py 对 gate 调用包 try/except ValueError → 返回 `fail("G4", ..., [str(e)])` 或 usage 提示；保持 pipeline 侧 fail-closed（dispatch_helper 已把不可解析 stdout 转 FAIL，见 dispatch_helper.py:1906-1910）。

### F404 | DecisionsDoc P2.5 "REQUIRED" 被空字符串 rationale 绕过 | error | P2
- 证据: src/shenbi/contracts/schemas/decisions.py:30-45（`has = rationale is not None`；`rationale: str | None = None`——空串非 None 视为"已提供"）；AGENTS.md:70 契约。
- 根因: presence 判定用 `is not None` 而非非空判定，`"rationale": ""` 同时满足 REQUIRED 且不触发 FORBIDDEN（routine+low 下空串反而会触发 FORBIDDEN，故只影响 REQUIRED 方向）。
- 验证命令+输出: `DecisionsDoc.model_validate({... basis:'manual_override', severity:'low', rationale:''})` → PASS（HOLE）。
- 建议方向: REQUIRED 判定改为 `not rationale`（空串视为缺失）；补空串用例到 test_g4_decisions.py。

### F405 | G4_CHECKER_SKILLS 注册表过期：漏 9 个专用 checker | error | P2
- 证据: src/shenbi/gates/shared.py:247-270（22 项，无 book-spine-init/chapter-revision/escalation-review/memory-distill/score-arc/score-stratum/score-volume）vs g4/generic.py:282-315 分发表 31 项（含上述 9 项）；11 个 skill 目录实测存在。
- 根因: 注册表未随新 checker 同步；g0.py:532 注释仍写 "dedicated checkers for 20 skills"。
- 影响: G0.12 的 dedicated/generic 计数失真；G0.15 只查 `G4_CHECKER_SKILLS - known` 单向漂移，漏报缺失注册；新增 checker 若未进 G4_CHECKER_SKILLS 不会被 G0.15 兜底。
- 验证命令+输出: `for s in ...; [ -d skills/shenbi-$s ]` → 11 个全 EXISTS；`grep -c 'shenbi-' shared.py G4_CHECKER_SKILLS` = 22 vs generic.py checkers 键 31。
- 建议方向: 从 generic.py 分发表派生 G4_CHECKER_SKILLS（单一事实源）或补全 9 项；同步 g0.py:532 注释。

### F406 | G0.3/G0.6/G7.5 使用遗留目录名 skill-output（真实布局 novel-output / project-output） | error | P2
- 证据: g0.py:195（G0.3 `novel_output = PROJECT / "skill-output"`）、g0.py:323（G0.6）、g7.py:66（G7.5 `rd / "skill-output"`）；真实布局 `ls -d novel-output` 存在、`skill-output` 不存在；g0.py:656 G0.cc 已正确用 `novel-output`；phase_runner.py:262 round 输出在 `round_dir/project-output`。
- 根因: 目录名迁移（skill-output → novel-output/project-output）未同步 G0.3/G0.6/G7.5。
- 影响: G0.3 永远读不到 genre-config → expected_chapters 恒用 3000 默认；G7.5 恒 SKIP（占位符检测失效）；test_g0.py:76-90 用 monkeypatch 构造 `skill-output/proj/` 合成布局掩盖真实布局漂移。
- 验证命令+输出: `ls -d skill-output novel-output` → 仅 novel-output；`find . -name genre-config.json` → `./novel-output/*/genre-config.json`；`grep -n "project-output" src/shenbi/phase_runner.py` → :262。
- 建议方向: G0.3/G0.6/G7.5 改用 novel-output/project-output（与 G0.cc 一致）；测试改用真实布局。

### F407 | G0 无 seed 时早退 → 全部环境检查被跳过，gate 空转 PASS | error | P2
- 证据: src/shenbi/gates/g0.py:177-179（无 seed → G0.1 SKIP + `return passed("G0", checks)`），G0.2-G0.16 与 G0.cc 全部未执行。
- 根因: 早退放在 G0.2 之前；`shenbi-validate G0`（无参数）即 PASS。
- 验证命令+输出: `gate_G0(None)` → `status: PASS, checks: [('G0.1','SKIP')]`；test_g0.py:19-22 将该行为钉死为"SKIP"（测试认可但未质疑早退语义）。
- 建议方向: 无 seed 时改为 FAIL 或仅跳过 G0.1-G0.3 而继续执行环境检查（G0.4-G0.16/G0.cc）；至少补注释说明早退是有意设计。

### F408 | G3.4 fail-closed 被调用方伪造 scorer 证据击穿（跨区交互） | error | P2
- 证据: src/shenbi/gates/g3_independence.py:18-27（fail-closed：无 current_scorer_agent → FAIL）vs src/shenbi/pipeline/dispatch_helper.py:1892-1902（`run_gate_g3` 在 progress.json 缺失时 safe_write 写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` 与 scoring_history）。
- 根因: 调用方（g3_independence docstring 声称的"正确 fail-closed 逻辑"接线处）在缺证据时主动制造证据 → G3.4 恒 PASS，独立性保证失效。
- 验证命令+输出: `sed -n '1883,1910p' src/shenbi/pipeline/dispatch_helper.py`（读源码确认伪造写入）。
- 建议方向: run_gate_g3 删除伪造写入，改为缺 progress 时如实 FAIL（与 fail-closed 一致）；或把该伪造显式标记为"pipeline 占位 scorer 策略"并评估其对评分独立性的影响。（dispatch_helper 属他区，仅记录交互。）

### F409 | G1 空输入 → PASS（G1.0 SKIP 空转） | error | P2
- 证据: src/shenbi/gates/g1.py:281-282（`if not fps: c.append(G1.0 SKIP)`）+ 284-291（无 mf 即 PASS）。
- 根因: 空输入不产生任何 FAIL，G1.0 只记录 SKIP；`shenbi-validate G1 <skill> []` 恒 PASS。
- 验证命令+输出: `gate_G1('shenbi-worldbuilding', [])` → `PASS [('G1.5','SKIP'), ('G1.6','SKIP'), ('G1.0','SKIP')]`。
- 建议方向: 空输入且无 SHENBI_G1_SKIP_READS genesis 豁免时返回 FAIL（"no inputs"），或把 G1.0 SKIP 提升为 WARN+说明。

### F410 | cli.py SHORT_MAP 缺 11 个技能的 shorthand | optimization | M
- 证据: src/shenbi/gates/cli.py:32-53（20 项）；`for s in book-spine-init chapter-revision escalation-review memory-distill review-arc-payoff review-resonance score-arc score-stratum score-volume market-radar short-drafting; do [ -d skills/shenbi-$s ]` → 全部存在。
- 根因: SHORT_MAP 未随新技能扩展（注释称 mirror legacy）。
- 影响: 这些技能 CLI 只能用全名 `shenbi-*`；功能不受影响（`SHORT_MAP.get(k, k)` 兜底）。
- 建议方向: 从 skills/ 或 generic.py 分发表派生 SHORT_MAP，或补全 11 项。

### F411 | g0.py G0.4 missing_dirs 死代码 + G0.12 注释 "20 skills" 过期 | optimization | M
- 证据: g0.py:220（`missing_dirs` 初始化后永不 append）、227（`if missing_dirs:` 恒假）；g0.py:532 注释 "dedicated checkers for 20 skills"（实际 G4_CHECKER_SKILLS 22 项）。
- 根因: 早期实现残留；注释未随 F405 的注册表扩展更新。
- 验证命令+输出: `read` g0.py 全文确认；与 F405 的证据交叉。
- 建议方向: 删除 missing_dirs 死分支；注释改为动态 `len(G4_CHECKER_SKILLS)`。

### F412 | g3_independence.py docstring 行引用过期 | docs | M
- 证据: g3_independence.py:3（"gate_G3（g3.py:144-162）现状 fail-open"）；实际 g3.py G3.4 块在 193-206。
- 根因: 文档/代码演进后未更新引用。
- 建议方向: 更新为 g3.py:193-206，并说明已接线 fail-closed。

### F413 | decisions_validator.py:175 注释 "shared.py:113" 错位 | docs | M
- 证据: decisions_validator.py:175（"fail() emits key 'must_fix' (not 'failures') — see shared.py:113"）；实际 fail() 在 shared.py:125-136（113 行在 word_count_md 内部）。
- 建议方向: 改为 shared.py:125（fail 定义行）。

### F414 | g4_chapter_revision 返回 "HARD_FAIL" 状态值不在 GateStatus 词表 | error | M
- 证据: chapter_revision.py:97（`"status": ... else "HARD_FAIL"`）vs src/shenbi/status.py:15-24（GateStatus 仅 PASS/FAIL/SKIP/WARN/UNIMPLEMENTED）。
- 根因: 字面量未用 GateStatus；composite 只检查 must_fix，故功能无碍，但违反状态字符串唯一定义原则（status.py:1-10 声明"single definition of every status string"）。
- 建议方向: 改 `GateStatus.FAIL`，并为 HARD 语义保留在 must_fix/checks 中表达。

### F415 | g4/chapter_drafting.py:73 引用 SKILL.md:125，实际规则在 140（漂移 15 行） | docs | P2
- 证据: chapter_drafting.py:73（"title must not include chapter number (SKILL.md:125)"）；skills/shenbi-chapter-drafting/SKILL.md:140（"章节标题不要包含章节号（如"第一章"），文件名已编码章节号。"）。
- 根因: SKILL.md 编辑后行号未同步（>±5 行）。
- 验证命令+输出: `sed -n '123,127p' skills/shenbi-chapter-drafting/SKILL.md` → 125 行为 `<!--META-END-->`；`grep -n "不要包含章节号" skills/shenbi-chapter-drafting/SKILL.md` → 140。
- 建议方向: 引用改为 SKILL.md:140，或去掉行号只留规则名。

### F416 | docs/framework/decisions-schema.md 严重度枚举缺 medium（代码与示例均有） | docs | M
- 证据: decisions-schema.md:66-68（severity 仅列 low/high）vs src/shenbi/contracts/schemas/decisions.py:12（`VALID_SEVERITY = {"low","medium","high"}`）且同文档示例 :33 用了 "medium"。
- 根因: 文档枚举未含 medium（且未提 medium 也 REQUIRED，比文档更严——见 F404 上下文）。
- 建议方向: 文档补 medium，并写明 medium/high 均 REQUIRED（与代码一致）。

### F417 | 覆盖率缺口处置汇总（对应 d1-06 本区 415 行） | — | —
- 处置总表（每文件明细见上文各节）：
  - `dead-code`: g0.py:220/227（missing_dirs 恒假分支）。
  - `must-test`: cli.py 全 main() 分发（67 行）；gate_manifest.py 全文件（0% 无测试文件）；decisions_validator.py 129-134（相邻章 budget 命中路径，F404 相关）；g3.py 106-125/138-143（G3.2 rubric 回退/acceptance 异常）；shared.py 70-75（resolve_input_path ValueError 契约）；g4/memory_distill.py 全文件（无独立测试）；g6_checks/g5 部分冲突子分支；length_normalizing rd+相对路径（F402 修复后补）。
  - `acceptable(防御/异常分支)`: 各文件 read_text/json 解析的 except 分支、SKIP 分支、`raise ValueError` 守卫（如 g4 各 RoundPaths checker 的 28/29 行）、yaml ImportError、marker 写失败吞错（shared.py:206-207）。
- 验证命令+输出: `COVERAGE_FILE=/tmp/z4_cov3 pytest tests/unit/gates --cov-config=/tmp/z4_cov2.ini --cov-report=term-missing` → cli 0%、g0 85%、g1 91%、g2 87%、g3 77%、g5 84%、g6 95%、g6_checks 91%、g7 88%、g7_trace 88%、g_reconcile 86%、shared 92%、gate_manifest 0%（与 d1-06 逐文件一致，d1-06 数据可信）。

### F418 | g0_config_coherence 的 threshold_mismatch / floor_too_low 检测在真实调用路径永不触发 | error | P2
- 证据: g0.py:665（`check_config_coherence(project_dir)` 未传 `resonance_global_floor`）vs g0_config_coherence.py:65（`if resonance_global_floor is not None:`——生产唯一调用恒为 None）；模块 docstring（:8-14）宣称检测三类缺陷。
- 根因: floor 来源（PipelineState.config）从未接线到调用点；测试直接传 floor（test_g0_config_coherence.py）掩盖接线缺口。
- 影响: E11（threshold_mismatch）与 floor_too_low 两类检测在生产死路，仅 critical_audit_disabled 生效。
- 验证命令+输出: `grep -rn "check_config_coherence" src tests` → 仅 g0.py:665 一个生产调用点，无 floor 实参。
- 建议方向: 在 G0.cc 循环内从 PipelineState/state-config 读取 floor 传入，或明确标注该两项为"待接线"。

---

## 覆盖统计
- deep-read 文件数: **52 / 52**（清单全覆盖）
- 未覆盖文件列表: **空**（无遗漏）

## 低置信度文件列表
- 无 low 置信度文件。全部 52 文件均为 high（全部实际读全文；关键行为均以 python -c / pytest 实测，覆盖数据以 /tmp 采集未污染仓库）。
- 提示：F401/F408 涉及 g_reconcile 的 progress 键约定与 dispatch_helper 的调用语义，属跨区（pipeline 区）交互，复核时建议与 Z5/Z6 交叉确认。

## 验证命令清单（均实际运行）
1. `pytest tests/unit/gates -q -p no:cacheprovider` → **785 passed**（repo venv）
2. `COVERAGE_FILE=/tmp/z4_cov3 pytest tests/unit/gates --cov-config=/tmp/z4_cov2.ini --cov-report=term-missing`（覆盖数据；/tmp 输出）
3. `python -c gate_G2([manifest.json],'report')` → G2.12 WARN（F400）
4. `python -c gate_G0(None)` → PASS [G0.1 SKIP]（F407）
5. `python -c gate_G1(skill,[])` → PASS（F409）
6. `python -c cli.main(['G4','worldbuilding',...])` → ValueError CRASH（F403）
7. `python -c g4_length_normalizing(['chapters/ch-1.md'], rd)` → FileNotFoundError CRASH（F402）
8. `python -c DecisionsDoc.model_validate(manual_override + rationale:'')` → PASS（F404）
9. `python -c gate_G4('shenbi-worldbuilding','generative',['foo.md'])` → ValueError（契约）
10. `ls -d skill-output novel-output` / `find . -name genre-config.json`（F406）
11. `sed -n '123,127p' skills/shenbi-chapter-drafting/SKILL.md`（F415）
12. `grep -rn check_config_coherence / MIRROR_MAP / current_scorer_agent / project-output`（F418/F408/F406/F411）
13. `for s in ...; [ -d skills/shenbi-$s ]`（F410/F405）
14. `sed -n '1883,1910p' src/shenbi/pipeline/dispatch_helper.py`（F408）
15. `git status --short`（确认本次审查未产生跟踪文件变更；tests/coverage 为 gitignored 既有产物）
