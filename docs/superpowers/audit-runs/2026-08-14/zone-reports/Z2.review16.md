# Z2 分区第十六轮独立复核报告（Z2.review16）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）+ `Z2.review9.md`（F253-F255）+ `Z2.review10.md`（F256-F257）+ `Z2.review11.md`（F258）+ `Z2.review12.md`（F259-F261）+ `Z2.review13.md`（F262）+ `Z2.review14.md`（F263-F266）+ `Z2.review15.md`（F267-F268）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，逐文件重读，未依赖前十五轮转述）；另读跨区消费面验证裁决（audit/{_shared,snapshot,write_audit,record}.py、records/parser.py、gates/{cli,g1,g2,g_reconcile}.py、scoring.py、pipeline/dispatch_helper.py 关键段、phase_runner.py、tests/round-exec.sh、AGENTS.md、docs/framework/truth-files.yaml、docs/framework/decisions-schema.md）
- 模式: 只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）。git status 确认 Z2 源码自前轮零改动（工作树无跟踪修改）。
2. **独立验证方式**：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）；十余个 `python -c` 运行时探针（F214/F216/F218/F232/F235/F247/F251/F252/F260/F265 复现 + 新候选 F269 证据 + 反证尝试）。
3. **前十五轮判定**：F201-F268 共 **68 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注（gates/cli.py:148-150 已知门恒返回 0）、F243 机制修正（exit 3 先于 exit 2）、F247 范围注记（中文 prompt → chapter=None 审计空转不 rc=2）均复核通过。**6 条 P1（F214/F216/F218/F227/F235/F247）经独立反证尝试全部维持**（详见第三节）。
4. **本轮新发现 1 条（F269，M 级）**——`_changed_top_keys` 的 JSONDecodeError 路径（F237/F260 家族的第三个触发面）：owned JSON（genre-config.json）被覆写为**非法 JSON** 时 field 级键集校验静默旁路（零违规）。根因在 Z5（audit/snapshot.py:63-76），被 Z2 executor 审计链消费。**实质性（P1/P2）新 finding 已收敛为 0**，本轮仅 1 条 M 级边界遗漏。

---

## 一、复核结论表（F201-F268 判定）

| 编号 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|
| F201 | **成立，P1 维持** | 三模型（chapter_planning/context_composing/volume_outlining）无生产消费者（grep 仅 test_skill_integration.py:188 类名文本）；g4 私有实现规则不同（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer 激活计划；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）。cov 34/38/40% 与报告吻合。 |
| F202 | **成立** | registry.py:40-63 REGISTRY/_discover_skill_models/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | **成立** | deps.py:56-78 DepsDoc/phase_of 无生产消费者；sync_contracts.py:195 仍 json.loads（跨区 read 确认）。 |
| F204 | **成立（附注）** | novel.py NovelConfig 无生产 import；g6.py:51 以 raw dict 读 target_word_count（含 target_words 回退）——死的是模型非行为。 |
| F205 | **成立** | schemas/scores.py 无生产消费者（grep 复证）。 |
| F206 | **成立** | schemas/state.py ProgressDoc/SummaryDoc 无加载点（g3.py 用 jload 直读 dict）。 |
| F207 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | **成立（增补成立）** | enums.py Severity/Verdict 无生产 import（生产仅 CPZone→foreshadowing_resolve、ActorRole→trace）；裸字面量散落点 + chapter_loop.py:1797（"BLOCKING"/"FAIL" in text）、:409-438（G4Severity）本轮 read 复核通过。 |
| F209 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有，read 确认）。 |
| F210 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | **成立（表述修正维持）** | executor.py:167-188 cov missing（复测 74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0（本轮 read 复证）→ 修复方向按"CLI 崩溃"修正，本体成立。 |
| F213 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实（read 复证）。 |
| F214 | **成立，P1 维持** | **本轮 read 复证**：codex.py:20-48 `_record_completion` 不写 current_scorer_agent；dispatch_helper.py:1934-1941 run_gate_g3 在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py fail-closed）以生产方自写证据放行。反证（独立路径不跑 G3 故 P2）不成立——run_gate_g3 在 pipeline 正常路径（chapter_loop/closure/genesis/triggers）被调用，API/IDE 派发不写 progress.json → 伪造为常态。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。 |
| F215 | **成立** | codex.py:75 `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（read 复证）。 |
| F216 | **成立，P1 维持** | **运行时复现**：`GenreConfig.model_validate`（空 customRules + texture:False 禁用维度）→ PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。反证（空规则无真实触发面）不成立——新轮次 stub customRules 可空。P1 维持。 |
| F217 | **成立；支持低置信异议（P2→P1）维持 P2** | SKILL.md 内 7 处 CONSTELLATION 区间 + docstring 20-30 + 模型 [15,35]；权威区间未定（SKILL.md 自相矛盾），维持低置信供协调者裁决。 |
| F218 | **成立，P1 维持** | **运行时复现**：部分匹配（声明 ["当前任务","不存在的字段"]，文件 3 节）→ matched=True、仅命中 "当前任务" 节（世界设定/活跃线索静默丢弃）、无 WARN；全缺 → 全文 + `field_filter_no_match` WARN（fields.py:55-64）。AGENTS.md:86-88 "If a declared field is missing ... returns the full file + logs WARN"（本轮 read 复证，单数 "a declared field"）部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 维持。 |
| F219 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282 read 复证）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，维持 P2（不降 M）。 |
| F221 | **成立** | fixture 9 键含 tropeInventory，`GenreConfig.model_validate` PASS 且 tropeInventory 被静默丢弃（extra=ignore）；SKILL.md:286/:292 声称 8 键；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段 + docstring "8 camelCase keys"。四源漂移确认。 |
| F222 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:44 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（本轮 read 复证，fields 随 dict 条目内联消费）。 |
| F226 | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise SubAgentProtocolError；cli main 无 try/except（read 复证）。 |
| F227 | **成立，P1 维持** | **本轮复证**：dispatch()（executor.py:190-214）在 dispatch_codex（技能执行处，codex.py:61-66）之前跑 G1（:190）/G2（:200）；round-exec.sh create-mode（:87-108 read 复证）只建 t1-reports/t2-reports/t3-reports/skill-output/skill-traces + progress.json（completed_skill_names/skills/tier/expected_chapters）/meta.json/summary.json/.token-hashes.json，**无 novel.json**；first-novel.md:41 文档化流程第一步 G1.1 novel.json not found → rc=1。反证（缺 novel.json 是预置问题）不成立。P1 维持。 |
| F228 | **成立；支持低置信异议（P2→P1）维持 P2** | derive_output_files（audit/_shared.py:38-56）只 resolve_or_skip、不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | **成立（范围低估但严重度维持）** | g2.py:76-84 decisions 分支对非 .json `continue`（:83-84，:164 连 G2.4-G2.12 一并跳过）+ executor 只调一次 run_g2 → .md 散文 G2.6/G2.8/G2.9 静默缺失。 |
| F230 | **成立** | **运行时复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`（无规范化）。 |
| F231 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者（复证）。 |
| F232 | **成立** | **运行时复现**：`Selection(manual_override, severity="high", rationale="")` → ACCEPTED；`Adjustment(rationale="   ")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | **成立，P1 维持** | **本轮复证**：`PROJECT_DIR = REPO_ROOT`（executor.py:26）；`git ls-files truth/` → bridge_tracker.md + character_matrix.md 均 tracked；worldbuilding watch 含 `truth/*.md` → snapshot_tree(REPO_ROOT) 展开命中两文件 → `_matches_declared` 失败（F236）→ 零写入派发也 `未声明写入` → rc=2；round_dir 真实写入不被审计。codex.py:62 cwd 已改 round_dir 而审计根未跟随；test_executor_audit.py:18,40 monkeypatch `ex.PROJECT_DIR = tmp_path`（==round_dir）编码了正确语义。反证（审计面意图为仓库根）不成立。P1 维持。 |
| F236 | **成立** | **运行时复现**：`_matches_declared("truth/bridge_tracker.md", ["truth/*.md"], parametric_globs())` → False（直接 fnmatch → True）；`"truth/*.md" in parametric_globs()` → False（registry glob 非 parametric）。 |
| F237 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | **成立；支持 review6 低置信异议（P2→P1）维持 P2** | audit_writes（write_audit.py:53-56）对每个 post .md 无 try 地 parse_records；records/parser.py:40-44 yaml.safe_load 可抛 ScannerError/非 list 抛 ValueError；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | **成立** | derive_file_type（executor.py:81-85）truth/decisions 分类用精确字符串交集 → `truth/*.md`/`snapshots/chapter-NNN/*` 写者（worldbuilding/sequel-writing/truth-sync/snapshot-manage）误归 "chapter"（read 复证）。 |
| F240 | **成立** | **运行时复现**：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证）。 |
| F242 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`（get_ownership → None）；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"。生产写者 lifecycle 无条目 → 记录级写保护对生产路径空转。 |
| F243 | **成立（机制修正维持）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。机制修正（review8）维持：gate-marker 强制 exit 3（scoring.py:364-377）先于分数校验 exit 2（:422-432）。 |
| F244 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:289-313 G2.11 对 **truth 输出**找 .bak（不存在则静默无检查）；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | **成立** | resolve_chapter_path（paths.py:28-34）对所有 bounded N 一律按章节替换；resolve_volume_path（:37-42）仅 closure.py:158 消费；`_build_skill_prompt`（dispatch_helper.py:656-658）对 chapter=None 抛 UnresolvedPathError（read 复证）。 |
| F246 | **成立** | dispatch_skill（dispatch_helper.py:1864）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | **成立，P1 维持（范围注记维持）** | **运行时复现**：`_declared_patterns("shenbi-chapter-drafting") == []`（write_audit.py:19-26 无 chapter）vs `_audit_watch_paths("shenbi-chapter-drafting", 7) == ['chapters/chapter-7.md','chapters/chapter-7-decisions.json']`（executor.py:220-225 带 chapter）→ N 写技能成功派发恒判「未声明写入」rc=2。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | **成立** | PacingDesign.from_markdown（pacing_design.py:77-130）chapter_sequence 恒空（:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）致误拒/虚增（read 复证）。 |
| F249 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500（read 复证）。 |
| F250 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | **成立；支持 review9 低置信异议（P2→P1）维持 P2** | **本轮 read 复证**：命名面 codex.py:56 `-scores-subagent.json` vs find_report（shared.py:157-166）三模式 / g0.py:440 G0.10 glob；内容面 raw 维度 dict 无 final_score；状态面 codex.py:44 "done" vs g_reconcile.py:40,61 "DONE"（GR.2 恒 FAIL；GR.2 的 rsplit 解析出 candidate_tt 含 "-scores-subagent" 后缀，与 progress test_type 键永不相等）。受 F252 前置掩盖维持低置信。 |
| F252 | **成立** | **本轮 read 复证**：scoring.py:364-377 marker 强制 + :187-224 check_gate_markers（t1-skill 分支要求 `G4-{skill}-{test_type}.json`）→ exit 3 MARKER_MISSING；codex.py:95-110 调 shenbi-score 不传 `--tier` → G3 子进程不触发、marker 检查必然执行；executor 编排（:190-214）只跑 G1/G2 无 G4；grep write_gate_marker dispatcher/ → 空。 |
| F253 | **成立** | codex.py:27-48 `_record_completion` 读-改-写整体无锁（safe_write.py `_acquire_lock` flock 仅覆盖写入周期，读在锁外）；pipeline legacy 并行（parallel_dispatch.py:168-171 Semaphore(4) 共享 project_dir）触发面真实。 |
| F254 | **成立** | run_g1/run_g2（executor.py:114-118/:125-137）与 shenbi-score 子进程（codex.py:95-109）均无 timeout；仅 codex exec 有 600s（codex.py:63）；`grep -n timeout src/shenbi/dispatcher/` → 仅 codex.py:63。 |
| F255 | **成立** | derive_file_type 对 4 个 report-kind truth 写者（score-volume/score-arc/score-stratum/review-arc-payoff）全 → "report"（executor.py:77-78 REPORT 分支先于 truth 交集）→ G2.11（g2.py:289-290 仅 file_type=="truth"）对其静默失效（read 复证）。 |
| F256 | **成立** | **本轮 read 复证**：g2.py:273/:315 注释声明 "chapter files only"，G2.10（:274-287）/G2.12（:316-322）位于 `if file_type == "chapter":` 块（:218-271）**之外**无条件执行——truth/report/json 全部受影响；句末符集合（:317）缺 `}`/`]` → 误归 chapter 的 JSON 恒 G2.12 WARN。 |
| F257 | **成立** | executor.py:156 `def dispatch(..., *, chapter: int | None = None)` 全仓零调用方（grep 仅 cli.py:29、executor.py:250，均不传）；dispatch_with_write_audit（:242）另行独立二次 extract_chapter（read 复证）。 |
| F258 | **成立** | `extract_chapter("Now that chapter 6 is complete, draft chapter 7")` → 6（首匹配）；单引用 → 7。pipeline 生成 prompt 均为单章节引用，仅 standalone 手工 prompt 触发。 |
| F259 | **成立** | `extract_chapter("draft chapter 0")` → 0；`resolve_chapter_path('chapters/chapter-NNN.md', 0)` → `chapters/chapter-000.md`；对照 chapter_planning.py:22 `Field(ge=1)`。 |
| F260 | **成立** | **运行时复现**：`_changed_top_keys('{"version":"1"}', '{"version":"1","hacked":null}')` → `()`（旁路）；对照 `{"hacked": true}` → `('hacked',)`。snapshot.py:63-76 `a.get(k) != b.get(k)` None 哨兵不可区分"缺席"与"null 值"。根因 Z5，被 Z2 executor 审计链消费。 |
| F261 | **成立** | `Selection(arc_relevance+low, rationale="x"*150)` → `Value error, rationale exceeds 100 chars`（应为 FORBIDDEN）；`grep -c VALID_BASIS decisions.py` → 1（仅定义）。decisions.py:11-12 死常量 + `_p25` 错误优先级（:34 长度检查先于 :41-42 FORBIDDEN）。 |
| F262 | **成立** | read_fields 声明与生产者模板系统性漂移（7 文件/5 技能）；book_strata/arc-N 部分匹配静默丢「未解决悬置」（F218 机制实锤）；style_profile/volume_map 等全漏恒 escape hatch。本轮枚举复证全部 18 条 read_fields 声明均为 .md/.json 路径（16+2），"未知扩展名直通"无触发面。 |
| F263 | **成立** | legacy.py:134-135 `split("---", 2)` 非行首锚定（frontmatter 内 "---" 子串静默截断或 ScannerError）；g0_skill_contract.py:71 同款孪生（read 复证）。 |
| F264 | **成立** | fields.py:38-52 重复 H2 标题 dict 键覆盖 → 首段静默丢失（read 复证）。 |
| F265 | **成立** | **运行时复现**：`_diff_records(pre=[{id:'h1',state:'PLANTED'}], post=[{id:'h1',state:'PLANTED',hacked:None}])` → `((), (), ())`（旁路）；真值键 → 检测。snapshot.py:87-91 None 哨兵（F260 的 record 层孪生）。根因 Z5。 |
| F266 | **成立** | legacy.py:85-87 write_semantics mode 值无词表校验（拼写错误静默通过）；append_dedup/merge_prose 声明无框架实现（dispatch_helper.py:1058-1067 显式 NOT branched、g4 无 checker）；create_or_overwrite 零消费者；contract `mode:` 与 SKILL.md `update_mode:` 双词表并行（read 复证）。 |
| F267 | **成立** | cli.py:28 `prompt = sys.argv[4] if len(sys.argv) > 4 else ""`——argv[5:] 静默丢弃（read 复证）。 |
| F268 | **成立** | executor.py:124 `output_files = ",".join(outputs)`（G2 第 1 参逗号拼接）vs gates/cli.py:92 `a0.split(",")`（逗号拆分）——含逗号路径无法还原；truth-files.yaml 路径列实测 0 逗号，零触发面。 |

**误报统计**：整体误报 **0 条**（F201-F268 共 68 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F269）

### F269 | `_changed_top_keys` 的 JSONDecodeError 路径：owned JSON（genre-config.json）被覆写为非法 JSON 时 field 级键集校验静默旁路（F237/F260 家族的第三个触发面） | 漏报（审计盲区，根因 Z5 audit/snapshot.py，被 Z2 executor 审计链消费） | M

- 证据: `src/shenbi/audit/snapshot.py:63-67`（`_changed_top_keys`：`try: a, b = json.loads(pre), json.loads(post) except (json.JSONDecodeError, TypeError): return ()`——pre 或 post 任一非法 JSON 时**提前返回空变更集**，不产出任何 violation/drift）；`src/shenbi/contracts/ownership.py:114-119`（field 分支只查 `changed_top_keys` ⊆ write_keys——空集恒通过）；`src/shenbi/dispatcher/executor.py:244-268`（dispatch_with_write_audit 经 snapshot_tree→compute_file_change→check_write_ownership 消费该函数）；`src/shenbi/audit/write_audit.py:57-64`（有 OWNERSHIP 条目则跳过 file-level 兜底——genre-config.json 恰有 `("shenbi-genre-config","genre-config.json")` field 条目）
- 根因: `_changed_top_keys` 的异常分支把"无法解析"降级为"无变更"，与 F260（`a.get(k) != b.get(k)` 的 None 哨兵使 null 值新增不可见）、F237（added 文件短路不计算键集）同属"field 级键集校验可被静默旁路"家族，但触发面独立：**pre 合法、post 非法 JSON**（LLM 输出截断/拼接/非 JSON 内容覆写 genre-config.json 的正常概率路径）→ JSONDecodeError → `()` → `check_write_ownership` 零违规 → `record_audit_outcome` 记 AUDIT_PASS（blocked=False）。与 F238 不同：F238 是 .md 解析崩溃（loud），本条是 JSON 解析**静默**吞掉（不崩溃、不告警、审计通过）。前十五轮覆盖了该函数的 added（F237）与 null（F260）两条旁路面，但未触及 JSONDecodeError 分支（F260 的探针仅测了合法 JSON 的 null 值）。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c "from shenbi.audit.snapshot import compute_file_change; from shenbi.contracts.ownership import check_write_ownership; ch = compute_file_change('genre-config.json', '{\"version\":\"1\"}', '{not valid json!!'); print(ch); print(check_write_ownership('shenbi-genre-config', ch))"` → `FileChange(relpath='genre-config.json', status='modified', changed_top_keys=(), ...)` / `[]`（零违规）；对照 pre==post 均合法 JSON 时 `_changed_top_keys` 正常检出键差
- 建议方向: `_changed_top_keys` 的 except 分支不应静默返回空集——post 非法 JSON 时返回哨兵/记 violation（如 "genre-config.json: post 内容非法 JSON，无法进行 field 级键集校验"），或由 `compute_file_change`/`audit_writes` 对 `_changed_top_keys` 空集 + pre≠post 的情形显式 WARN；与 F260/F265 同源修复（snapshot.py 归 Z5 审计区），Z2 侧（ownership/executor 消费链）无需改动；补非法 JSON post 用例测试
- 影响注记: 当前被 F227（fresh 轮 G1 先死）/F235/F247（审计 rc=2 先于内容检查）/F252（评分 exit 3）链掩盖；该链修复后，genre-config.json 被覆写为非法 JSON 的 standalone 轮次审计静默通过（写所有权保护的 field 级最后防线对内容损坏无感）。M 级（与 F260 同级；触发需 LLM 产出非法 JSON 覆写 owned 文件，且审计职责边界为"所有权"而非"内容有效性"）

---

## 三、严重度异议（不修改前十五轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1（已入账）**：run_gate_g3（dispatch_helper.py:1934-1941）在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py fail-closed）以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。反证尝试（独立路径不跑 G3 故 P2）不成立——run_gate_g3 在 pipeline 正常路径被调用，伪造为常态。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。
2. **支持 F218 P2 → P1（已入账）**：AGENTS.md:86-88 "If a declared field is missing ... returns the full file + logs WARN"（本轮 read 复证原文，单数 "a declared field"）在部分缺失时两处均不满足（fields.py:55-64 本轮复现：matched=True、仅命中 section、无 WARN）；F262 提供 book_strata/arc-N 真实数据形状实例；truth 文件随剧情演进缺 section 属正常路径。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——SKILL.md 内部 7 处自相矛盾，权威区间未定，维持低置信供协调者裁决。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found 恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内，使 record_audit_outcome 永不执行（审计记录丢失）——"崩溃仍审计"保证（executor.py:245-246）失效；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（chapter-drafting/context-composing/chapter-revision 等）的 .md 散文在 G2 被 continue（g2.py:83-84）整体跳过，G2.6/G2.8/G2.9 对章节正文静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F251 P2 → P1（低置信维持）**：评分产物三重不匹配使 standalone 轮次对 G5.1（no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL）确定性失败；受 F252（exit 3 前置）掩盖维持低置信。
8. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。
9. **F269 严重度自评说明（非异议）**：按 M 报告（与 F260 同级——同函数第三条静默旁路面）；若协调者认为"owned 文件内容损坏且审计零感知"已达 P2 边界（正常概率路径可复现 + 审计职责内），可升 P2，但需注意当前被 F227/F235/F247/F252 链掩盖，且内容有效性属 G2/G4 职责而非所有权审计。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F218 部分匹配 → matched=True、仅命中 section、无 WARN；全缺 → 全文 + WARN
  - F232 Selection(manual_override/high, rationale="") → ACCEPTED
  - F235 `git ls-files truth/` → bridge_tracker.md + character_matrix.md tracked；F247 `_declared_patterns`=[] vs `_audit_watch_paths`=2 项（chapter-7.md/decisions.json）
  - F214 run_gate_g3 伪造点 read 复证（dispatch_helper.py:1934-1941）；F227 round-exec.sh create-mode 产物清单（无 novel.json）+ AGENTS.md:86-88 原文复证
  - F251 g_reconcile.py:40,61 "DONE" vs codex.py:44 "done"；GR.2 rsplit 解析 read 复证；F252 scoring.py:364-377 exit 3 先于 :422-432 exit 2
  - F256 g2.py G2.10/G2.12 位于 chapter 块之外 read 复证；F260 `_changed_top_keys` null 值新增 → ()；F265 `_diff_records` null 值新增 → ((),(),())
  - **F269（新）** `compute_file_change` pre 合法/post 非法 JSON → changed_top_keys=() → `check_write_ownership` → []（零违规）
  - read_fields 全量枚举（18 条：16 .md + 2 .json，无未知扩展名直通触发面——F262 增补面消解）
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git ls-files`/`git status` 仅读；探针均在 /tmp 目录与 python -c。

---

## 五、统计

- 新 finding：**1 条**（F269：M×1）；无新增 P1/P2
- 误报：**0 条整体误报**（F201-F268 共 68 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持）
- 严重度异议：**8 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1（均已入账）、F217→P1（低置信）、F228→P1（低置信）、F238→P1（低置信）、F229→P1（低置信）、F251→P1（低置信）、F220 维持 P2
- 前十五轮 findings 判定：F201-F268 共 68 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否（严格意义上）**——本轮仍有 1 条新发现（F269，M：`_changed_top_keys` JSONDecodeError 静默旁路面，F237/F260 家族第三个触发面），**无新增 P1/P2**；6 条 P1（F214/F216/F218/F227/F235/F247）经独立反证尝试全部维持；**实质性（P1/P2）新 finding 已收敛为 0**，仅剩 1 条 M 级边界遗漏（根因在 Z5，建议随 F260/F265 修复一并处理）。
