# Z1 分区初审报告（agent a）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 范围：`docs/superpowers/audit-runs/2026-08-14/zones/Z1.files` 全部 14 文件（deep-read 14/14）
- 只读约束：仅执行 grep / git / read / python -c 只读分析 / pytest --collect-only（`-p no:cacheprovider`）；未创建/修改/删除任何仓库文件（本段文件除外）；未 git add/commit。
- 编号段：F101–F114

## 0. 总体结论

Z1 顶层文件整体质量较高：状态机（phase_runner 92%）、评分（scoring 97%）、capability_fs/status/error_guidance/recovery/logging/cli_utils/__init__ 均为 100% 行覆盖（依据归档的 D1 ① `d1-01-just-check.log` 真实 85.14% 运行记录）；`pytest --collect-only` 收集到 Z1 直接相关测试 **248 个**，全部可收集。无 P0/P1 级发现。共 13 条 finding：P2×9、M×4。无 P0/P1。

关键横切问题（均 P2）：① `safe_write` 每次写入把目标文件权限压成 0600（仓库 0644 工件已被改写，F101）；② `error_guidance` 的 6 条 doc_url 全部指向不存在的文档、且目录引用的异常类在 src/ 无任何 raise 站点（异常层级 22 类中 17 类从未被 raise，F102/F103）；③ `phase_runner` 不强制 `--project-dir`（缺失时把字符串 `"None"` 传给 G5，F105）；④ `G_TRANSITION/G_DISPATCH/G_RECONCILE` 未接入状态机（F107）。

### 覆盖缺口（d1-06）处置总说明

`d1-06-coverage-gaps.txt` 在会话内被 D1 层两次重生成（D1 基线"更正记录"F508：初版取自被 collect-only 覆写的 16.08% coverage.xml，全部条目失效，已重跑真实 85.14% 并重新提取 7123 行）；会话内 `tests/coverage/coverage.xml` 又被并发运行（含其他 agent 的 collect-only）反复覆写（当前为 0.3289 局部产物）。**本报告不采信 d1-06 的逐行条目**，改用归档的真实运行记录 `docs/superpowers/audit-runs/2026-08-14/d1/d1-01-just-check.log`（`just check` 的 term-missing 输出，85.14% 运行）作为 Z1 逐文件覆盖真相，并按其缺失行逐条处置：

| 文件 | Stmts | Miss | 覆盖率 | 缺失行（真实） |
|---|---|---|---|---|
| __init__.py | 1 | 0 | 100% | — |
| capability_fs.py | 29 | 0 | 100% | — |
| cli_utils.py | 7 | 0 | 100% | — |
| error_guidance.py | 10 | 0 | 100% | — |
| exceptions.py | 49 | 1 | 96% | 25 |
| logging.py | 10 | 0 | 100% | — |
| paths.py | 23 | 1 | 96% | 31 |
| phase_runner.py | 203 | 14 | 92% | 74-75, 153-154, 266-269, 371-372, 374-375, 377-378, 380-381 |
| recovery.py | 9 | 0 | 100% | — |
| safe_write.py | 76 | 25 | 66% | 30-31, 34-35, 71-91, 124-125, 139-140 |
| scoring.py | 297 | 5 | 97% | 331, 362, 391-393, 415 |
| status.py | 45 | 0 | 100% | — |
| sync_contracts.py | 103 | 43 | 56% | 128-134, 144-155, 159-160, 165-203 |

处置结果：must-test = 2 组（safe_write.py:71-91 并发竞态分支；sync_contracts.py:128-134,144-155 OD-1 注入逻辑）；dead-code = 2 处（paths.py:31 `repo()` 无调用方 → F112；error_guidance/recovery 目录整体 dead → F103）；缺陷性缺失行 3 处（scoring.py:362→F106、scoring.py:415→F104、phase_runner.py:153-154→F113）；其余为声明/防御/CLI 边界/CI 覆盖 → acceptable（逐文件见下）。

---

### src/shenbi/__init__.py
- 处置: deep-read
- 声称检查的不变量:
  - `__version__` 与 pyproject.toml 一致（pyproject.toml:3 `version = "0.1.0"` == `__init__.py:12`）✓
  - docstring 子模块清单与 src/shenbi/ 实际结构一致（exceptions/logging/scoring/phase_runner/gates/dispatcher 均存在）✓
  - "forwarder until PR-19/PR-20" 注释不再成立（gates/cli.py、dispatcher/cli.py 已是真实实现）✗ → F111
  - 覆盖缺口：100%（d1-01 记录）✓
- findings: [F111]
- 验证命令: `grep -n "^version" pyproject.toml`（输出 `3:version = "0.1.0"`）；`ls src/shenbi/gates/ src/shenbi/dispatcher/`（gates/cli.py、dispatcher/cli.py、dispatcher/executor.py 存在）；`grep -E "src/shenbi/__init__\.py" d1-01-just-check.log`（100%）
- 置信度: high

### src/shenbi/capability_fs.py
- 处置: deep-read
- 声称检查的不变量:
  - 沙箱边界：`_sandbox` 先 `resolve(strict=False)` 再 `relative_to(root)`（capability_fs.py:22-29）——符号链接逃逸在 resolve 后被拒（resolve 跟随链接 → 链接指向 root 外 → relative_to 抛 ValueError → PermissionError）✓
  - 写侧全拒：write_text/write_bytes/unlink/mkdir 一律 PermissionError（capability_fs.py:44-54）✓
  - 签名一致性：`__init__(allow_root: Path)` 与 tests/unit/test_capability_fs.py:14,23,31 等 `CapabilityFS(tmp_path)` 一致 ✓
  - 错误处理：PermissionError 带原路径信息（:28）✓
  - 覆盖缺口：**100%**（d1-01 记录；test_capability_fs.py 81 行 8 测试 + tests/property/gates/test_capability_fs_properties.py）→ 无缺口
- findings: 无
- 验证命令: `read tests/unit/test_capability_fs.py`（81 行 8 测试）；`grep -E "capability_fs\.py" d1-01-just-check.log`（`29 0 0 0 100%`）
- 置信度: high

### src/shenbi/cli_utils.py
- 处置: deep-read
- 声称检查的不变量:
  - stdout 数据通道与 stderr 日志分离（docstring cli_utils.py:1-6）：emit_json 写 stdout（:20-22），日志走 structlog/stderr ✓
  - 无 print()（AGENTS.md:112 契约）✓（sys.stdout.write 是数据通道，非诊断 print，设计上有意为之）
  - JSON 输出可被下游解析：`json.dumps(ensure_ascii=False)` + 换行 + flush ✓
  - 覆盖缺口：100%（d1-01 记录）✓
- findings: 无
- 验证命令: `grep -rn "sys.stdout" src/shenbi/{cli_utils,phase_runner,scoring}.py`（仅 cli_utils 3 处）；`grep -n "print(" src/shenbi/*.py`（Z1 顶层 0 命中，EXIT=1）；`grep -E "cli_utils\.py" d1-01-just-check.log`（100%）
- 置信度: high

### src/shenbi/error_guidance.py
- 处置: deep-read
- 声称检查的不变量:
  - ERROR_GUIDANCE 键 == exceptions.py 类名（test_exceptions.py:112-115 交叉断言通过）✓
  - get_guidance 精确按类名匹配（:55-57）——子类不匹配（test_error_guidance.py:71-82 验证）✓
  - doc_url 指向存在文档 ✗ → F102（6/6 全断）
  - action 指向存在的脚本 ✗ → F102（tests/build_registry.py 不存在）
  - 目录引用的异常类在框架中有 raise 站点 ✗ → F103（6 类全部无 raise 站点）→ 目录整体 dead-code
  - 覆盖缺口：100%（d1-01 记录；行覆盖无缺口，但功能层面 dead——见 F103）
- findings: [F102, F103]
- 验证命令: 逐锚点 grep（6 个 doc 文件全 FILE MISSING；gates.md/dispatcher.md 实际路径在 docs/framework/ 且无对应锚点）；`find docs -name "registry.md|schemas.md|integrity.md"`（0 命中）；`ls tests/build_registry.py`（No such file）；`grep -E "error_guidance\.py" d1-01-just-check.log`（100%）
- 置信度: high

### src/shenbi/exceptions.py
- 处置: deep-read
- 声称检查的不变量:
  - 层级完整：ShenbiError → FrameworkError/IntegrityError/GateError/ScoringError + 分支（exceptions.py:11-175）✓ 结构正确
  - cause 链：`self.__cause__ = cause`（:25）保留异常上下文 ✓
  - to_dict 序列化（:27-35）含 error_class/message/context/cause_class/cause_message，无 pickle 边界（Z1 全文件无 pickle import）✓
  - RegistryStaleError truncation（:59 mismatches[:5]）+ total_mismatches ✓（test_exceptions.py:68-75 验证）
  - **typed 全链使用 ✗**：22 类中仅 5 类在 src/ 有 raise 站点；17 类（含 ShenbiError/FrameworkError 基类本身、error_guidance/recovery 目录引用的全部 6 类）从未被 raise → F103
  - 跨区一致性 ✗（旁证）：pipeline/chapter_loop.py:529 `DriftEscalationError(Exception)`、config/config_coherence.py:44 `ConfigError(ValueError)`、contracts/paths.py:16 `UnresolvedPathError(ValueError)` 均为层级外裸定义，违反 exceptions.py:3 "All errors inherit from ShenbiError"（这些文件属其他 Z 区，此处仅作为 Z1 层级契约被稀释的证据）
  - 覆盖缺口：缺失行仅 25（`self.__cause__ = cause`，需 cause≠None 才执行，无测试构造带 cause 的异常）→ acceptable（低风险赋值行；可选补测）
- findings: [F103, F110]
- 验证命令: `python3 - <<EOF` AST 扫描 src/ 全部 raise 站点（输出 22 类 × RAISED/NEVER RAISED）；`grep -rn "except ShenbiError|except FrameworkError" src`（仅 contracts/registry.py:32 一处捕获）；`grep -E "exceptions\.py" d1-01-just-check.log`（96%，miss 25）
- 置信度: high

### src/shenbi/logging.py
- 处置: deep-read
- 声称检查的不变量:
  - SHENBI_LOG_FORMAT=json → JSONRenderer；否则 ConsoleRenderer（:21-26）✓
  - 全部输出 stderr（PrintLoggerFactory(file=sys.stderr)，:36）✓
  - cache_logger_on_first_use=False（:41）——capsys 测试环境下可重绑 stderr（docstring :37-41 解释正确）✓
  - processors 链：merge_contextvars/add_log_level/TimeStamper(iso)/StackInfo/format_exc_info ✓
  - 覆盖缺口：100%（d1-01 记录；test_logging.py 覆盖 JSON/console 双渲染器 + stderr/stdout 断言）✓
- findings: 无
- 验证命令: `read tests/unit/test_logging.py`（JSON 渲染 → `json.loads(captured.err)` 断言 event/level/timestamp；console 渲染 → 非 JSON；stderr 非 stdout）；`grep -E "logging\.py" d1-01-just-check.log`（100%）
- 置信度: high

### src/shenbi/paths.py
- 处置: deep-read
- 声称检查的不变量:
  - read() 优先 round_dir、回退 project_dir（:19-24）；write() 恒 round_dir（:26-28）✓
  - backup() 与 write() 同根（:33-35）✓
  - resolve_chapter_path 占位符解析委托 contracts/paths.py（单一实现，替换 4 处旧实现——contracts/paths.py:4-8 声明）✓；签名 `(path: str, chapter: int | None)` 与调用一致 ✓
  - 边界：占位符路径 + chapter=None → UnresolvedPathError 上抛（fail-loud，contracts/paths.py:31）——有意的失败语义 ✓
  - **repo() 无任何调用方 ✗** → F112（paths.py:31 死代码；grep `\.repo(` 在 src/ 0 命中；G4 checkers 只消费 read()）
  - 覆盖缺口：缺失行仅 31（repo() 方法体）→ dead-code（F112），非 must-test
- findings: [F112]
- 验证命令: `grep -rn "\.repo(" src/shenbi --include="*.py" | grep -v paths.py`（0 命中）；`grep -rn "rp\.read(" src/shenbi/gates/g4/*.py`（17 处 read() 消费，无 repo()）；`grep -E "paths\.py" d1-01-just-check.log`（96%，miss 31）
- 置信度: high

### src/shenbi/phase_runner.py
- 处置: deep-read
- 声称检查的不变量:
  - 状态机：created→started→skills_done→scored→finalized，每步 require_state 守卫（:116,135,187,244,280,306 的 expected 列表与 PhaseState 枚举一致）✓；非法状态 exit 1 不推进 ✓
  - G5 接线：start（:117）与 finalize（:307）双 G5；G2/G4 在 post-skill（:213-217）✓
  - 崩溃恢复：状态每步 save_state 持久化；post-score 先 json.loads 校验再置 SCORED（:290-300）——坏 scores 文件不会静默推进 ✓；post-skill 子进程与 save_state 之间的崩溃可通过重跑恢复（幂等）✓
  - run_gate 错误处理：subprocess 超时/OSError/JSON 解码失败 → 返回 FAIL 字典而非崩溃（:94-99；TimeoutExpired⊂OSError 被捕获）✓
  - gate 结果记录 best-effort：_record_gate_manifest except 带 exc_info 日志（:74-75）✓
  - 签名一致性：load_contract/derive_output_files/derive_file_type/safe_write/emit_json 调用均与定义匹配（grep 验证）✓
  - 无 print ✓；status 字符串 emit 用枚举成员（CommandStatus/PhaseState）✓
  - **--project-dir 缺失时把 "None" 传给 G5 ✗** → F105（main:360 required=False + :117 str(None)；g5.py:115 `if project_dir:` 对真值字符串 "None" 成立 → Path("None") 相对 CWD → 误导性 FAIL；:188 assert 在 python -O 下失效）
  - **G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 ✗** → F107（phase_runner 从不调用；仅 gates/cli.py:126-134 手动入口 + 各自单测）
  - **pre-skill ContractError 静默吞错 ✗** → F113（:153-154 无日志降级为空契约）
  - 覆盖缺口：92%，缺失 14 行——74-75（gate_manifest except，防御，acceptable）、153-154（ContractError 分支，缺陷见 F113）、266-269（pre-score glob 缺输出 BLOCKED，错误路径，acceptable）、371-372/374-375/377-378/380-381（main() 四条命令路由，纯分发行；TestMainCli 已覆盖 start/pre-skill/unknown/missing-round-dir，CLI 分发边界，acceptable）
- findings: [F105, F107, F113]
- 验证命令: `pytest --collect-only -p no:cacheprovider tests/unit/test_phase_runner.py tests/unit/test_phase_runner_property.py tests/unit/phase_runner/`（248 tests collected 含全部 Z1 测试文件）；`grep -n "def test" tests/unit/test_phase_runner.py`（60+ 测试）；`sed -n '656,720p' tests/unit/test_phase_runner.py`（TestMainCli 5 测试）；`grep -rn "G_TRANSITION|G_DISPATCH|G_RECONCILE" src/shenbi --include="*.py" | grep -v gates/`（0 命中）；`grep -E "phase_runner\.py" d1-01-just-check.log`（92%，miss 14 行）
- 置信度: high

### src/shenbi/py.typed
- 处置: deep-read
- 声称检查的不变量:
  - PEP 561 marker 存在：`src/shenbi/py.typed` 0 字节文件存在 ✓（`ls -la` 输出 `-rw-r--r-- 0 ... py.typed`）
  - 打包配置一致：pyproject.toml:74-75 `[tool.hatch.build.targets.wheel] packages = ["src/shenbi"]` —— hatchling 对 packages 目录默认全量打包（含 py.typed），无 exclude 规则 → wheel 含 marker ✓（未实际构建验证——构建会写 dist/，违反只读禁令；配置层面一致）
- findings: 无
- 验证命令: `ls -la src/shenbi/py.typed`（0 字节存在）；`sed -n '/\[tool.hatch.build/,/^\[tool/p' pyproject.toml`（`packages = ["src/shenbi"]`，无 exclude）
- 置信度: medium（未实构建 wheel；仅配置层验证）

### src/shenbi/recovery.py
- 处置: deep-read
- 声称检查的不变量:
  - RecoveryStrategy 四成员值（none/auto_retry/auto_rebuild/halt）与 test_recovery.py 断言一致 ✓
  - RECOVERY_STRATEGIES 键 == exceptions.py 类名（test_exceptions.py:117-120 交叉断言）✓
  - 目录引用的异常类在 src/ 有 raise 站点 ✗ → F103（6 类全部无 raise 站点 → 恢复策略表 dead-code）
  - 覆盖缺口：100%（d1-01 记录；行覆盖无缺口，但功能层面 dead——见 F103）
- findings: [F103]
- 验证命令: `read tests/unit/test_recovery.py`（75 行 13 测试）；AST 扫描（见 F103）；`grep -E "recovery\.py" d1-01-just-check.log`（100%）
- 置信度: high

### src/shenbi/safe_write.py
- 处置: deep-read
- 声称检查的不变量:
  - 原子写链：mkstemp→write→flush→fsync(file)→os.replace→fsync(dir)（:105-112）✓
  - 异常清理：except BaseException unlink tmp + re-raise（:113-116）——连 KeyboardInterrupt 也不留残留 ✓
  - 并发锁：flock(LOCK_EX) 主路径（:54-61）；Windows/flock 不可用 → O_EXCL lockfile 回退（:63-91）；锁跨 os.replace+fsync 持有（docstring :43-47 正确）✓
  - 释放：finally close fd；O_EXCL 回退 unlink lockfile（:117-125）——M5 修复（历史泄漏回归测试 test_safe_write_lockfile_fallback_cleanup_posix 存在）✓
  - trace 追加 best-effort：except 带 exc_info 日志，不覆盖调用方成功信号（:126-140）✓
  - **权限处理 ✗** → F101（mkstemp 0600 + os.replace → 每次写入把目标压成 0600；仓库 0644 工件已被改写）
  - **并发缺陷 ✗** → F108（mkstemp 在 try 外 → mkstemp 失败锁泄漏；stale-takeover unlink 后 O_EXCL 竞态 FileExistsError 未捕获）
  - 覆盖缺口：66%，缺失 25 行——30-31/34-35（目录 fsync 平台/FS 防御，acceptable）、71-91（O_EXCL 退避重试 + stale-takeover 竞态分支 → **must-test**：并发核心路径无测试触达，且 F108 正指出该区域缺陷）、124-125（释放时并发 takeover 已清理的 FileNotFoundError，防御，acceptable）、139-140（trace 失败 warning，防御，acceptable）
- findings: [F101, F108]
- 验证命令: `git ls-files -s docs/framework/dependency-dag.json docs/framework/truth-files.index.json tests/tiers/deps.json`（均 100644）对比 `ls -la`（均为 -rw------- 0600）——权限回归实证；`read tests/unit/test_safe_write.py`（120 行 8 测试）；`grep -E "safe_write\.py" d1-01-just-check.log`（66%，miss 30-31,34-35,71-91,124-125,139-140）
- 置信度: high

### src/shenbi/scoring.py
- 处置: deep-read
- 声称检查的不变量:
  - 加权计算：weighted_sum/total_weight（:168-174），total_weight≠100 时 log.warning 不静默 ✓
  - kill switch → 0（:166-167）✓
  - validate_scores：空/缺维/越界 REJECT，多余键 WARNING 不 REJECT（:139-159）✓
  - 分类阈值：≥90 PASS_EXCELLENT / ≥75 PASS_ACCEPTABLE / ≥60 CONDITIONAL（:177-184，TEST_PASS=90 与 contracts/thresholds.py:10 一致，AGENTS.md "≥90 individual pass"）✓
  - test-type 维度过滤 + 权重重归一（:102-136）✓
  - 门标记检查：t1/t2/t3 三分支（:187-224）✓；G4 标记路径与 phase_runner cmd_pre_score 一致（均 `G4-{skill}-generative.json`）✓
  - 双评分一致性 + 分数塌缩检测（:227-279）由 orchestration/scoring_bridge.py 消费（签名匹配）✓
  - 序列化：结果含 _provenance（scored_by/timestamp/gate_markers_verified）✓
  - **--kill-switch 无 scores 文件死分支 ✗** → F104（:414-415 scores={} → :422 validate_scores 空 → REJECT exit 2，永远到不了 0 分路径）
  - **G3 gate 输出解析异常静默吞掉 ✗** → F106（:361 except Exception: pass 无日志）
  - 覆盖缺口：97%，缺失 5 行——331（--phase 参数行，T2/T3 专用，低风险，acceptable）、362（F106 缺陷行）、391-393（interactive kill-switch 输入，交互路径非 CI，acceptable）、415（F104 缺陷行）
- findings: [F104, F106]
- 验证命令: `grep -n "def test" tests/unit/test_scoring.py | head -30`（validate/compute/kill-switch/CLI 全覆盖）；`sed -n '560,660p' tests/unit/test_scoring.py`（kill-switch 测试恒带 scores.json → :414 分支未被测试触达）；`grep -rn "check_scorer_agreement|flag_score_collapse" src/shenbi/orchestration/scoring_bridge.py`（消费点）；`grep -E "scoring\.py" d1-01-just-check.log`（97%，miss 5 行）
- 置信度: high

### src/shenbi/status.py
- 处置: deep-read
- 声称检查的不变量:
  - 状态字符串单一定义：GateStatus/PhaseState/CommandStatus/ScoringStatus/ScoreClassification 五枚举（:17-64）+ STATUS_STRING_LITERALS frozenset（:91-99）✓
  - 与 on-disk 线格式兼容（PhaseState 小写值，:30-37；test_status.py 断言 wire values）✓
  - lint 强制：tools/lint_status_strings.py 仅豁免 status.py，禁 status/state/classification 键的裸字符串 emit（读侧比较允许，如 phase_runner 的 `== "PASS"` 属 read-site 不违规）✓
  - rubric 第 5 条注记：契约中 "枚举/状态字符串唯一定义在 contracts/enums.py" 与现状不符——contracts/enums.py 存 Severity/Verdict/CPZone/ActorRole（另一词汇域）；status 字符串的单一定义在 status.py（有 lint+wire 测试强制）。两模块词汇域不相交，未发现重复定义或第三方来源。invariant 满足（单定义/词汇域），但 rubric 的文件引用需要修正。
  - 覆盖缺口：100%（d1-01 记录）✓
- findings: 无
- 验证命令: `read tests/unit/test_status.py`（wire 值断言）；`read tools/lint_status_strings.py`（STATUS_KEYS = status/state/classification；读比较豁免设计）；`grep -E "status\.py" d1-01-just-check.log`（100%）
- 置信度: high

### src/shenbi/sync_contracts.py
- 处置: deep-read
- 声称检查的不变量:
  - 单一 loader（contract.load_contract）派生 artifacts，无第三解析器（docstring :1-9 + load_all_contracts :34-48）✓
  - build_dag glob 感知匹配 + skill 去重（:51-73，注释说明按 key 去重）✓
  - derive_expected_outputs 参数化→glob 归一（:76-92）✓
  - 幂等：BODY_BANNER...BODY_END 哨兵包裹（:30-31,124-134），regex 整体替换（:150-155）✓
  - 迁移前保护：无 contracts 时 bail 不写（:167-172）✓
  - CI 幂等检查：ci.yml:80-88 `uv run shenbi-sync-contracts` + `git diff --exit-code` ✓（D4 漂移守卫）
  - **verify_bijection 用 assert ✗** → F109（:121 assert；python -O 下整守卫消失，docstring 宣称 "Raises on mismatch"）
  - 覆盖缺口：56%，缺失 43 行——128-134（render_body_view 字符串生成）与 144-155（render_body_into 注入/替换，含 regex 幂等逻辑）→ **must-test**（OD-1 数据契约视图注入为真实行为且含 regex 替换，无任何单测触达）；159-160（_write_json，随 main 由 CI 覆盖，acceptable）；165-203（main 全链，由 CI 幂等检查 + D1 ⑦ 冒烟 `shenbi-sync-contracts` PASS/phase_synced 实证，acceptable）
- findings: [F109]
- 验证命令: `read tests/unit/test_sync_contracts.py`（166 行：parametric→glob、per-dim literal 回退、glob 优先级、DAG 去重、load_all_contracts/kind——均不含 render_body_*）；`grep -n "sync" .github/workflows/ci.yml`（80-88 行幂等检查）；`grep -E "sync_contracts\.py" d1-01-just-check.log`（56%，miss 128-134,144-155,159-160,165-203）
- 置信度: high

---

## Findings 明细（F1xx）

F101 | safe_write 写入后目标文件权限一律 0600，仓库 0644 工件已被改写 | error | P2
- 证据: src/shenbi/safe_write.py:105（`tempfile.mkstemp` 默认 0600）+ :111（`os.replace(tmp, path)` 继承 0600）；实证：`git ls-files -s docs/framework/dependency-dag.json` → `100644`，但 `ls -la` → `-rw-------@`（同为 docs/framework/truth-files.index.json、tests/tiers/deps.json）
- 根因: safe_write 未保留目标已有 mode；mkstemp 固定 0600。git 不追踪 rw 位 → CI 幂等检查（git diff）看不见，漂移隐形
- 验证命令+输出: `git ls-files -s`（100644 c8000598... 等三文件）+ `ls -la`（-rw-------@ 三文件）——已粘贴于证据
- 建议方向: 写入前 stat 目标（存在时）并 chmod 回原 mode；新文件按用途给默认（如 0644）；补测试断言"已存在文件权限保持不变"

F102 | error_guidance 6 条 doc_url 全指向不存在的文档路径/锚点；2 条 action 指向不存在的脚本 | error | P2
- 证据: src/shenbi/error_guidance.py:25,30,35,40,45,50（docs/registry.md#stale-lockfile、docs/registry.md#missing-lockfile、docs/gates.md#missing-markers、docs/schemas.md#validation-errors、docs/dispatcher.md#timeouts、docs/integrity.md#tool-tamper）；:24,29（`python3 tests/build_registry.py`）
- 根因: 目录编写于早期 docs 布局（registry/gates/schemas/dispatcher/integrity 顶层文件），后文档重组到 docs/framework/ 且 registry 工具链移除；目录未同步
- 验证命令+输出: 逐锚点 grep → 6 个文件全 FILE MISSING；`find docs -name registry.md -o -name schemas.md -o -name integrity.md` → 0 命中；gates.md/dispatcher.md 仅存在于 docs/framework/ 且只有 `#Gates`/`#Dispatcher` 两个 H1；`ls tests/build_registry.py` → No such file
- 建议方向: 修正为 docs/framework/ 实际路径与锚点（或删除不再适用的条目）；doc_url 校验纳入 test_error_guidance.py

F103 | exceptions.py 22 类中 17 类在 src/ 无 raise 站点；error_guidance/recovery 目录引用的全部 6 类均不会被真实错误命中 | error | P2
- 证据: src/shenbi/exceptions.py:52-123（RegistryStale/RegistryMissing/RegistryCorrupt/SchemaValidation/DefectApplication/Migration/SubAgentUnavailable/Configuration/Integrity/ToolTamper/GateError/GateMarkerMissing/ScoringError/ScoringReject 及基类 ShenbiError/FrameworkError）；error_guidance.py:21-52；recovery.py:23-29
- 根因: P-1.E 时期的 registry lockfile / tool-hash 锁定 / gate 标记设计已被 contracts 架构取代（load_registry/load_contract，见 contracts/），异常类未随架构裁剪；ADR-0006 的"全链 typed"契约在 Z1 顶层文件中也未落实（phase_runner/scoring 用 sys.exit + JSON 而非 typed raise；跨区另有 ConfigError(ValueError)/UnresolvedPathError(ValueError)/DriftEscalationError(Exception) 层级外定义）
- 验证命令+输出: AST 扫描 src/ 全部 raise → RAISED: DispatcherError, DispatchWriteFailureError, SubAgentTimeoutError, SubAgentProtocolError, RetryExhaustedError（5/22）；NEVER RAISED: 其余 17 类；`grep -rn "raise RegistryStaleError|...|raise SchemaValidationError" src` → 0 命中
- 建议方向: 裁剪或接线——恢复策略表（recovery.py）与引导目录（error_guidance.py）要么接回真实错误路径（如 scoring 的 REJECT 态改用 ScoringRejectError），要么标记 deprecated；新异常一律走 ShenbiError 子类

F104 | scoring.py `--kill-switch` 无 scores.json 的死分支必然 REJECT，永远到不了 0 分 | error | P2
- 证据: src/shenbi/scoring.py:414-415（`if scores_file == "--kill-switch": scores = {}`）→ :422-432（validate_scores 空 → REJECT → sys.exit(2)）；用法文档 :285-291 只声明 `<scores.json> [--kill-switch]`；d1-01 记录 :415 为缺失行（该分支从未执行）
- 根因: 残留分支意图"无分数文件时 kill-switch 归零"，与 validate_scores 的空字典 REJECT 冲突；该分支无测试触达（test_scoring.py:598-613 恒带 scores.json）
- 验证命令+输出: 代码走查 :413-432；`sed -n '598,613p' tests/unit/test_scoring.py`（test_kill_switch_flag_forces_score_to_zero 传 sample_scores）；`grep -E "scoring\.py" d1-01-just-check.log`（miss 415）
- 建议方向: 删除该分支或改为显式报错（"--kill-switch 需要 scores.json"）；如需"全零"语义，改为构造全 0 scores 而非空 dict

F105 | phase_runner main() 不强制 --project-dir，缺失时把字符串 "None" 传给 G5；assert 守卫在 python -O 下失效 | error | P2
- 证据: src/shenbi/phase_runner.py:360（`find_flag("--project-dir", required=False)`）；:117（`str(project_dir)` → "None"）；:188（`assert project_dir is not None`）；gates/g5.py:115（`if project_dir:` 对真值字符串 "None" 成立 → `Path("None")` 相对 CWD → G5.4/G5.5 误导性 FAIL）；phase_runner.py:5 用法声明 start 需 --project-dir
- 根因: CLI 层未按 usage 校验必填 flag；单测用 mock run_gate 掩盖（test_phase_runner.py:669-681 test_routes_start_command 不带 project-dir 仍 PASS）
- 验证命令+输出: `sed -n '656,720p' tests/unit/test_phase_runner.py`（TestMainCli 均不校验 project_dir）；g5.py:115-116 读取
- 建议方向: find_flag("--project-dir") 在 start/post-skill/finalize 设为必填并 emit_json ERROR + exit 1；用显式校验替换 assert

F106 | scoring.py G3 gate 输出解析异常被 `except Exception: pass` 静默吞掉，无日志，评分继续 | error | P2
- 证据: src/shenbi/scoring.py:356-362（T1 G3 子进程输出 `json.loads` 失败 → pass；子进程崩溃/非 JSON → 门检静默跳过，评分照常）；d1-01 记录 :362 为缺失行
- 根因: 注释"Expected when gate output is not valid JSON"成立，但应区分"预期非 JSON"与"子进程真实失败"；吞掉后 G3 验证缺口不可见
- 验证命令+输出: `sed -n '354,363p' src/shenbi/scoring.py`；`grep -rn "except Exception" src/shenbi/scoring.py`（仅此一处，另两处 phase_runner.py:74 / safe_write.py:139 均带 exc_info 日志）
- 建议方向: 至少 `log.warning("g3_output_unparseable", stdout=..., exc_info=True)`；或把 G3 失败降级为显式 gate FAIL 而非静默继续

F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接入 phase_runner 状态机，仅 CLI 手动入口 + 各自单测 | optimization | P2
- 证据: gates/cli.py:126-134（三个 gate 的 dispatch 入口）；phase_runner.py 全文无引用（grep 0 命中）；command-to-give.md:94-108 T2/T3 协议不含三者；gates/g_transition.py:22 等实现存在且有 tests/unit/gates/test_g_transition.py
- 根因: PR-19 抽取 gate 逻辑时保留了 CLI 入口，但 phase 状态机（phase_runner）从未接线；无文档说明它们由谁在何时运行 → dead-wire
- 验证命令+输出: `grep -rn "G_TRANSITION|G_DISPATCH|G_RECONCILE" src/shenbi --include="*.py" | grep -v "gates/g_transition.py|gates/g_dispatch.py|gates/g_reconcile.py|gates/cli.py"` → 0 命中；`grep -n "G_TRANSITION|..." command-to-give.md` → 无
- 建议方向: 明确三者的执行方（接线进 phase_runner 的 transition/dispatch/finalize 步骤，或文档声明为人工/编排层手动 gate），消除 dead-wire

F108 | safe_write 两处并发缺陷：mkstemp 在锁获取后、try 块外 → mkstemp 失败锁泄漏；stale-takeover unlink 后 O_EXCL 竞态 FileExistsError 未捕获 | error | P2
- 证据: src/shenbi/safe_write.py:104-105（`_acquire_lock` 之后、`try` 之前 `tempfile.mkstemp`——若 mkstemp 抛 OSError，:117-125 的 finally 不执行，flock fd 泄漏或 .lock 永久残留）；:85-91（10 次退避后 `os.unlink(lockfile)` → 立即 O_EXCL 重建，窗口内另一写者先建 → FileExistsError 未捕获直接上抛）
- 根因: 锁生命周期未覆盖 mkstemp 段；stale-takeover 的 unlink+recreate 非原子
- 验证命令+输出: 代码走查 :102-125；test_safe_write_lockfile_fallback_cleanup_posix 只验证成功路径清理，未覆盖 mkstemp 失败/竞态；d1-01 记录 :71-91 为缺失行（竞态分支无测试）
- 建议方向: 把 mkstemp 移入 try 并让 finally 统一释放锁；stale-takeover 捕获 FileExistsError 再退避一次（或循环至成功/超时）

F109 | sync_contracts.verify_bijection 用 assert 做一致性守卫，python -O 下整函数失效 | optimization | M
- 证据: src/shenbi/sync_contracts.py:121（`assert not missing and not spurious, ...`）；docstring :110 宣称 "Raises on mismatch"
- 根因: assert 非运行时守卫；uv run 默认不带 -O 故现状可用，属防御深度问题
- 验证命令+输出: `sed -n '95,121p' src/shenbi/sync_contracts.py`
- 建议方向: 改显式 `if missing or spurious: raise ValueError(...)`（同步 docstring 的 "Raises" 语义）

F110 | exceptions.py RegistryStaleError 消息 "1 source files changed" 语法错误（单数名词 + 复数 files） | M
- 证据: src/shenbi/exceptions.py:57（`f"{len(mismatches)} source files changed since lockfile built at ..."`）；test_exceptions.py:64 断言同款文案（"1 source files changed"）
- 根因: 文案未区分单复数
- 验证命令+输出: `sed -n '55,62p' src/shenbi/exceptions.py`；`sed -n '58,65p' tests/unit/test_exceptions.py`
- 建议方向: `"source file" if n==1 else "source files"`（连带更新测试断言）

F111 | __init__.py docstring "forwarder until PR-19/PR-20" 过期 | M
- 证据: src/shenbi/__init__.py:8-9（"gates: G0-G7 gate enforcement (forwarder until PR-19)"、"dispatcher: sub-agent dispatch (forwarder until PR-20)"）；实际 gates/cli.py、dispatcher/cli.py、dispatcher/executor.py 均为真实实现（PR-19/20 早已落地，git log dd1fc62）
- 根因: 迁移完成后未更新模块 docstring
- 验证命令+输出: `ls src/shenbi/gates/cli.py src/shenbi/dispatcher/cli.py`；`git log --oneline -3 -- src/shenbi/phase_runner.py`（PR-19 迁移已入库）
- 建议方向: 删除 "(forwarder until PR-19/PR-20)" 括注

F112 | RoundPaths.repo() 无任何调用方（死代码） | optimization | P2
- 证据: src/shenbi/paths.py:30-31（`def repo(self, rel: str)` 方法体）；grep `\.repo(` 在 src/ 0 命中；G4 checkers 仅消费 read()（17 处）；d1-01 记录 :31 为缺失行（方法从未执行）
- 根因: RoundPaths 设计含 repo_root 访问器，但迁移后无消费者；保留为 API 未来用途
- 验证命令+输出: `grep -rn "\.repo(" src/shenbi --include="*.py" | grep -v paths.py` → 0 命中
- 建议方向: 删除或补测试并接线（若计划供 G4/其他使用）

F113 | phase_runner cmd_pre_skill 的 ContractError 静默吞错：契约损坏时无日志降级为空 reads/writes | error | P2
- 证据: src/shenbi/phase_runner.py:153-154（`except ContractError: read_files, write_files = [], []` 无任何日志，emit_json 仍报 execute_skill + 空契约）；对比 :74-75 同类 except 带 log.warning(exc_info=True)；d1-01 记录 153-154 为缺失行（分支从未执行/从未测试）
- 根因: 吞错降级设计未记录原因；契约损坏时调用方（shell/编排）只看到空契约，无法区分"技能无契约"与"契约解析失败"
- 验证命令+输出: `sed -n '148,166p' src/shenbi/phase_runner.py`；`grep -E "phase_runner\.py" d1-01-just-check.log`（miss 153-154）
- 建议方向: 与 :74-75 一致补 log.warning("contract_load_failed", skill=..., exc_info=True)；或让 ContractError 上抛（fail-loud）

---

## 返回摘要

### findings 清单（编号/标题/严重度/证据行）
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F101 | safe_write 权限 0600 覆盖已有 0644 工件 | P2 | safe_write.py:105,111 |
| F102 | error_guidance 6 条 doc_url + 2 条 action 全断 | P2 | error_guidance.py:24-25,29-30,35,40,45,50 |
| F103 | exceptions.py 17/22 类无 raise 站点（含引导/恢复目录全部 6 类） | P2 | exceptions.py:52-123; error_guidance.py:21-52; recovery.py:23-29 |
| F104 | scoring --kill-switch 无 scores 死分支必 REJECT | P2 | scoring.py:414-415,422-432 |
| F105 | phase_runner --project-dir 缺失传 "None" 给 G5；assert 在 -O 失效 | P2 | phase_runner.py:117,188,360; g5.py:115 |
| F106 | scoring G3 输出解析异常静默吞掉无日志 | P2 | scoring.py:356-362 |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线进 phase_runner | P2 | gates/cli.py:126-134; phase_runner.py（无引用） |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态未捕获 | P2 | safe_write.py:85-91,104-105 |
| F109 | verify_bijection assert 守卫 -O 失效 | M | sync_contracts.py:121 |
| F110 | "1 source files changed" 语法 | M | exceptions.py:57 |
| F111 | __init__ "forwarder until PR-19/20" 过期 | M | __init__.py:8-9 |
| F112 | RoundPaths.repo() 无调用方（死代码） | P2 | paths.py:30-31 |
| F113 | phase_runner pre-skill ContractError 静默吞错无日志 | P2 | phase_runner.py:153-154 |

### 覆盖统计
- deep-read 文件数：**14 / 14**（清单全部覆盖）
- 未覆盖文件列表：**空**

### 低置信度文件
- `src/shenbi/py.typed`：medium —— marker 存在性与 hatchling 配置一致已确认，但未实际构建 wheel 验证（构建会写 dist/，违反只读禁令）。

### 其他说明
- 覆盖缺口处置汇总（以归档真实运行 `d1-01-just-check.log` 为准）：must-test 2 组——safe_write.py:71-91（O_EXCL 退避/stale-takeover 竞态分支，联动 F108）、sync_contracts.py:128-134,144-155（render_body_view/render_body_into OD-1 注入逻辑，含 regex 幂等，无单测）；dead-code 2 处——paths.py:31（F112）、error_guidance/recovery 目录（F103）；缺陷性缺失行 3 处——scoring.py:362（F106）、scoring.py:415（F104）、phase_runner.py:153-154（F113）；其余缺失行为声明/防御/CLI 分发边界/CI 覆盖 → acceptable。`d1-06-coverage-gaps.txt` 因会话内被 D1 两次重生成且依据的 coverage.xml 反复被并发运行覆写，未采信其逐行条目（D1 基线 F508 更正记录亦确认初版全部失效）。
- rubric 第 5 条注记：状态字符串单一定义在 `status.py`（lint + wire 测试强制），`contracts/enums.py` 承载 Severity/Verdict/CPZone/ActorRole（不同词汇域）；两模块不相交，无重复定义。rubric 中 "唯一定义在 contracts/enums.py" 的文件引用与现状不符，建议审计层修正。
