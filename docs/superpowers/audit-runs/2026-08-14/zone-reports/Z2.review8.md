# Z2 分区第八轮独立复核报告（Z2.review8）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，逐文件重读，未依赖前八轮转述）；另读跨区消费面验证裁决（gates/{cli,shared,g0,g1,g2,g3,g3_independence,g5,g_reconcile,g_dispatch}.py、g4/{genre_config,pacing_design,decisions_validator}.py、audit/{_shared,snapshot,write_audit,record}.py、records/parser.py、pipeline/dispatch_helper.py 关键段、scoring.py、cli_utils.py、status.py、safe_write.py、tests/round-exec.sh、first-novel.md、skills/shenbi-foreshadowing-track/SKILL.md、tests/unit/dispatcher/*、tests/unit/test_dispatcher_*.py、git show 99f91f0:tests/dispatch-subagent.sh）
- 模式: 只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）。
2. **独立验证方式**：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前八轮一致）；十余个 `python -c`/heredoc 运行时探针（F251 端到端、F252 端到端、find_report 命名/内容形状、G0.10 glob、g_reconcile 状态大小写、shenbi-score gate-marker、scoring.py 门序、导入图）。
3. **前八轮判定**：F201-F250 共 **50 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注维持（gates/cli.py:148-150 对已知门恒返回 0）。已有 6 条严重度异议（F214/F218→P1、F217/F228/F229/F238→P1 低置信、F220 维持 P2）经独立反证尝试后**全部支持**；本轮**不新增异议**，但提出 1 条 F247 范围注记与 1 条 F243 机制修正（均非异议）。
4. **本轮新发现 2 条（F251-F252）**，集中于前八轮都未触及的**评分产物消费面**：
   - **F251（P2）**：独立 dispatcher 评分产物与门消费契约三处不匹配 —— `-scores-subagent.json` 文件名对 G0.10 glob（g0.py:446）与 find_report（shared.py:157-166，消费者 g5.py:65 G5.1 / g_reconcile.py:41 GR.1）不可见；即使命名正确，raw 维度 dict 无 `final_score`/`score` 键 → G5.1 读 0；progress.json skill 状态 `"done"`（codex.py:44）与 g_reconcile 的 `"DONE"`（g_reconcile.py:40,61）大小写不匹配 → GR.2 恒 FAIL（端到端复现）。
   - **F252（P2）**：standalone 编排不执行 G4、不写 gate marker → shenbi-score 的 gate-marker 强制（scoring.py:356-370 + check_gate_markers:200 `G4-{skill}-{test_type}.json`）在 codex.py:95-110 的调用参数下必 MISSING → exit 3 → dispatch rc=3（端到端复现）；且该检查先于分数校验 → F243 所述 "REJECT exit 2" 被 exit 3 前置拦截。

---

## 一、复核结论表（F201-F250 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 三模型（chapter_planning/context_composing/volume_outlining）无生产消费者；g4 私有实现规则不同（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）；cov 34/38/40% 与报告吻合。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g4/worldbuilding.py 与 g6.py:51 以 raw dict 读 target_word_count —— 死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无生产加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797、:409-438（G4Severity 自建词表）本轮 read 复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有，read 确认）。 |
| F210 | P2 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing（74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试 —— "零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0 → 修复方向应为"检查 rc!=0（CLI 崩溃）或 stdout 空 → 容错"，本体成立。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | P2 | **成立；支持异议→建议 P1** | codex.py:20-48 不写 current_scorer_agent；dispatch_helper.py:1934-1945 run_gate_g3 在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → g3.py:213-224 G3.5 prior_agents 恒空 → PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行。反证尝试（独立路径不跑 G3 故 P2？）不成立 —— pipeline 主路径真实触发伪造。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。 |
| F215 | P2 | **成立** | codex.py:75 `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（read 复证 + 前轮复现）。 |
| F216 | P1 | **成立，P1 维持** | genre_config.py:94 `if disabled and self.custom_rules:` —— 空 customRules + disabled dim → model_validate PASS（read 复证）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。反证（空规则无真实触发面）不成立（新轮次 stub 可空）。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | SKILL.md 内 6 处 CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :187 15-30 / :192 15-25 / :193 30-40 开卷 / :194 15-25）+ docstring 20-30 + 模型 [15,35]；模型对标准卷过松（25-35 过门违反 :91 上限）对开卷过严（36-40 误拒，:193 允许 30-40）。权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | fields.py:55-64 `_filter_md` 部分匹配 → 仅命中 section、matched=True、无 WARN（read 复证 + 前轮复现）；AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | P2 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证（grep `.read_keys` 仅定义处 + foundation-review 条目）。矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，建议维持 P2（不降 M）。 |
| F221 | P2 | **成立** | 复证：fixture 9 键含 tropeInventory（grep 计数 1）、模型 8 字段无 tropeInventory（grep 0）、SKILL.md:286/:292 声称"8 个/恰好 8"（grep 0 命中 tropeInventory）、OWNERSHIP _GENRE_KEYS 9 键（grep 3 命中）；`extra=ignore` 静默丢弃。四源漂移确认。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except（read 复证）。 |
| F227 | P1 | **成立，P1 维持** | dispatch() 在 dispatch_codex（executor.py:214，技能执行处）之前跑 G1（:190）/G2（:200）；`git show 99f91f0` 原 shell 注释 "the skill execution happened earlier" 证实门序回归；first-novel.md:38-41 round-exec.sh 只建脚手架（无 novel.json）→ 文档化流程第一步必 G1 FAIL。反证不成立。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | derive_output_files（audit/_shared.py:38-56）不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现（test_dispatcher_executor.py:16 编码该结果）；g2.py:83-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | P2 | **成立** | `_filter_json:76` 裸 `k in fields`（无规范化），与 `_filter_md` canonical rule 不一致（read 复证 + 前轮复现）。 |
| F231 | P2 | **成立** | 本轮 grep 复证：povMode/sensitivityFlags 仅 review_checklist.py:183,187 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者。 |
| F232 | P2 | **成立** | decisions.py:33 `has = rationale is not None` —— 空串/纯空白满足 REQUIRED 且 ≤100 字符；routine+low 空串被拒，两侧语义不对称（read 复证）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | P2 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | P1 | **成立，P1 维持** | `PROJECT_DIR = REPO_ROOT`（executor.py:26），`_audit_watch_paths`（:220-225）带 chapter 解析 watch、`snapshot_tree(PROJECT_DIR, watch)`（:244/:264）快照仓库根 → 仓库根 tracked `truth/bridge_tracker.md`/`truth/character_matrix.md` 命中 watch → F236 匹配失败 → `未声明写入` violations → 零写入派发也 rc=2；round_dir 真实写入不被审计。反证（审计面意图为仓库根）不成立 —— codex.py:62 cwd 已改 round_dir + test_executor_audit.py:18,40 编码 PROJECT_DIR==round_dir 正确语义。 |
| F236 | P2 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs（parametric_globs()），registry globs（`truth/*.md`）恒判未声明（read 复证 + 前轮复现）。 |
| F237 | P2 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | P2 | **成立；支持 review6 低置信异议（P2→P1）** | audit_writes（write_audit.py:52-56）对每个 post .md 无 try 地 parse_records/parse_markdown_table；records/parser.py:40-44 yaml.safe_load 可抛 ScannerError/非 list 抛 ValueError；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行（write-audit.jsonl 无 GATE_FAIL）——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | P2 | **成立** | derive_file_type（executor.py:81-85）truth/decisions 分类用精确字符串交集 → `truth/*.md`/`snapshots/chapter-NNN/*` 写者（worldbuilding/sequel-writing/truth-sync/snapshot-manage）误归 "chapter"（read 复证 + 前轮复现）。 |
| F240 | P2 | **成立** | extract_chapter（paths.py:45-47）仅识别英文 "chapter N"；中文 prompt → chapter=None → N/NNN 输出全部丢弃 → G2 静默跳过（read 复证 + 前轮复现）。 |
| F241 | P2 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证 + 前轮复现）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"。生产写者 lifecycle 无条目 → 记录级写保护对生产路径空转。 |
| F243 | P2 | **成立（机制修正见下）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（原第 4 参为独立评分 prompt）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。**机制修正**：F243 称分数"大概率被 shenbi-score REJECT（exit 2）"——本轮实测 scoring.py:356-370 的 gate-marker 强制（exit 3 MARKER_MISSING）**先于**分数校验（scoring.py:422-432 REJECT exit 2）触发，standalone 路径的实际首个评分阻塞是 exit 3 而非 exit 2（见 F252）；F243 本体（评分者=生成者）不受影响。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | P2 | **成立** | resolve_chapter_path（paths.py:28-34）对所有 bounded N 一律按章节替换；resolve_volume_path（:37-42）仅 closure.py:158 消费；`_build_skill_prompt`（dispatch_helper.py:656-658）对 volume-N/arc-N 等 chapter=None 时抛 UnresolvedPathError → 卷/弧技能 pipeline dispatch 崩（read 复证 + 前轮复现）。 |
| F246 | P2 | **成立** | dispatch_skill（dispatch_helper.py:1864）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径）→ pipeline 继承 F227/F235/F247 缺陷（read 复证）。**本轮增补**：legacy 回退还继承 F252（shenbi-score gate-marker exit 3）——见 F252 影响注记。 |
| F247 | P1 | **成立，P1 维持（范围注记见下）** | `_declared_patterns`（write_audit.py:19-26）以 chapter=None 派生声明集（N 路径全丢），watch 面（executor.py:220-225）以提取到的 chapter 解析 → 40 个 N 写技能在 prompt 含可提取章节号时成功派发恒判「未声明写入」rc=2（read 复证 + 前轮端到端复现）。**范围注记（非异议）**："恒"仅对可提取英文 "chapter N" 的 prompt 成立；中文 prompt（F240）→ chapter=None → watch 面同样丢弃 N 路径 → 审计空转但不 rc=2（转为 F240 的 G2 静默跳过 + 审计盲区）。缺陷本体成立，P1 维持（英文 prompt 为正常路径）。 |
| F248 | P2 | **成立** | PacingDesign.from_markdown（pacing_design.py:77-130）chapter_sequence 恒空（:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）致误拒/虚增（read 复证 + 前轮复现）。 |
| F249 | P2 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500 → 4500-10000 字重要章被 G2.7 误拒（read 复证 + 前轮复现）。 |
| F250 | P2 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 pipeline dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |

**误报统计**：整体误报 **0 条**（F201-F250 共 50 条全部实质成立）；部分误报 1 条（F220 计数 5→6，实质成立，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注维持；F243 机制修正 1 条（exit 3 先于 exit 2，见 F252，本体成立）。

---

## 二、新发现（F251-F252）

### F251 | 独立 dispatcher 评分产物与门消费契约三处不匹配：`-scores-subagent.json` 命名对 G0.10/find_report 不可见 + raw 维度 dict 无 final_score + progress 状态 "done" vs "DONE" → G0.10 恒 WARN、G5.1 恒 no_report FAIL、GR.2 恒 FAIL | 漏报（正常路径功能错误，前八轮未覆盖评分产物消费面） | P2
- 证据:
  - 命名面: `src/shenbi/dispatcher/modes/codex.py:56`（唯一 t1-reports 生产者：`f"{skill}-{test_type}-scores-subagent.json"`；grep 确认全仓无其他 t1-reports/*.json 写者）；`src/shenbi/gates/shared.py:157-166`（find_report 三模式：`{skill}-{test_type}-scores.json` / `{skill}-{test_type}.json` / `{skill}.json`，无 `-subagent` 模式）；消费者 `g5.py:65`（G5.1 回退路径）、`g_reconcile.py:41`（GR.1）；`g0.py:446`（G0.10 `glob("*-generative-scores.json")` 同样不匹配 `-scores-subagent.json`）
  - 内容面: codex.py:87 写入的是 codex 原始维度 dict（`{"1":90,"2":95}`），shenbi-score 校验后完整报告（含 final_score）仅走 stdout（codex.py:114-116），不落盘 → 即使命名修正，G5.1（g5.py:68 `rdata.get("final_score", rdata.get("score", 0))`）仍读 0
  - 状态面: codex.py:44 `skill_entry[test_type] = {"score":..., "status": "done"}` vs `g_reconcile.py:40,61` 比较 `"DONE"`（大写）→ GR.1 跳过、GR.2 恒 FAIL
- 根因: PR-20 保留原 shell 的 `-subagent` 命名（`git show 99f91f0` 确认 shell 同款 SCORES_FILE），但未与 gate 消费面（G0.10 glob / find_report / G_RECONCILE 的 "DONE" 契约）对齐；生产（唯一写者）与消费（三处读者）之间无任何一致性检查，且 codex 单测（test_codex_mark_done.py）只验证生产侧命名、从不验证消费侧匹配 → 测试固化了错误契约。
- 验证命令:
  - `PYTHONPATH=src .venv/bin/python` → 建 t1-reports/ 含 `shenbi-worldbuilding-generative-scores-subagent.json`（codex.py:56 命名）→ `find_report(reports, "shenbi-worldbuilding", "generative")` → `None`（producer 文件存在但三模式均不匹配）
  - G0.10 glob: 同目录 `glob("*-generative-scores.json")` → 只匹配 `-scores.json` 不带 subagent 的文件
  - 内容面: 命名改为 `-scores.json` 后 `find_report` 命中，`data.get("final_score", data.get("score", 0))` → `0`
  - 端到端: `python -m shenbi.gates.cli G_RECONCILE <round>`（round 含 codex 产物 + progress "done"）→ `{"gate":"G_RECONCILE","status":"FAIL",...,"must_fix":["GR.2:shenbi-worldbuilding-generative-scores-subagent:status=?"]}`
- 建议方向: 统一评分产物契约：(a) codex.py 改写 `{skill}-{test_type}-scores.json`（去掉 `-subagent` 后缀）并落盘 shenbi-score 的完整报告（含 final_score）而非 raw 维度 dict；(b) progress.json skill 状态改 `"DONE"` 或 g_reconcile 改为大小写不敏感比较；(c) 补一条"dispatcher 产物 → find_report/G0.10/G_RECONCILE 消费"的集成测试（现测试只测生产侧）。
- 影响注记: 当前被 F227（fresh 轮 G1 先死）掩盖；F227/F235/F247 修复后，本缺陷独立阻塞 standalone 轮的 G0.10 计数、G5.1（T2 边界）与 G_RECONCILE——建议届时重评（若 G5.1 阻塞 T2 主路径可升 P1）。

### F252 | standalone 编排不执行 G4、不写 gate marker → shenbi-score gate-marker 强制必 MISSING（exit 3）→ 任何走到评分阶段的独立派发 rc=3；且 exit 3 先于分数校验（F243 所述 REJECT exit 2 被前置拦截） | 漏报（正常路径功能错误/编排缺口，F227 家族但机制独立） | P2
- 证据: `src/shenbi/dispatcher/executor.py:190-214`（dispatch 只编排 G1/G2，无 G4）；`src/shenbi/scoring.py:356-370`（`if test_type: if round_dir: missing = check_gate_markers(...)` → exit 3 MARKER_MISSING）；`scoring.py:187-225`（check_gate_markers 要求 `gate-markers/G4-{skill}-{test_type}.json` 存在，t1-skill rubric 分支 :200）；`src/shenbi/dispatcher/modes/codex.py:95-110`（以 `--test-type {test_type} --round-dir {round_dir} --subagent` 调 shenbi-score，无 `--tier` → scoring.py:338-353 的 G3 子进程不触发，marker 检查必然执行）；`gates/cli.py:121`（write_gate_marker 仅 G4/G6 CLI 分支调用，standalone 从不调用）——全仓无任何路径在 standalone 流程写 `gate-markers/G4-*.json`
- 根因: PR-20 把"技能执行他处完成、门序只做校验"改为"执行并入 dispatch_codex"，但评分侧的 G4 marker 前提（原设计由调用方在技能执行时跑 G4 落 marker）未随迁入——executor 编排 G1/G2 却漏 G4，使自己的评分步骤（shenbi-score）永远因缺 marker 失败；scoring.py 的 marker 强制先于分数校验，standalone 路径实际在 exit 3 处即终止，REJECT（exit 2）不可达。
- 验证命令: `PYTHONPATH=src .venv/bin/python`（round-exec 风格 round：progress.json 无 scorer、t1-reports 含 raw scores 文件）→ `python -m shenbi.scoring tests/tiers/t1-skill/shenbi-worldbuilding/rubric.md <scores> --test-type generative --round-dir <round> --subagent` → `rc=3`、`{"status":"MARKER_MISSING","missing_markers":["G4-shenbi-worldbuilding-generative"],...}`；`grep -rn "write_gate_marker" src/shenbi/dispatcher/` → 空
- 建议方向: standalone 编排补 G4（在 codex exec 后对产出跑 `shenbi-validate G4 <skill> <files> <round_dir>` 并落 marker，与 G5.5 语义一致）；或 codex.py 调 shenbi-score 时跳过 marker 强制（如 `--skip-gate-markers`，需文档明示 standalone 已用 G2 兜底）；与 F243 修复联动（F243 恢复独立评分 subagent 时一并处理 marker）。
- 影响注记: fresh 轮被 F227（G1 先死）掩盖、重试轮被 F235/F247（审计 rc=2 先于评分）掩盖；F227/F235/F247 修复后本缺陷独立阻塞一切走到评分的 standalone 派发（重试轮 codex 跑通、G1/G2/审计通过后死在评分 rc=3）；pipeline legacy 回退（F246）同步继承（dispatch_skill → shenbi-dispatch → rc=3）。F243 的机制描述（exit 2 REJECT）建议修正为 exit 3 MARKER_MISSING 先行。

### M 级观察（随文记录，均非 finding）
- `codex.py:51-66` `dispatch_codex` 的 `agent_id` 参数在函数体内**零使用**（死参数）：`generate_agent_id`（executor.py:59-61）产出的 agent 身份仅进 `dispatch_start` 日志，不落 progress.json / agent_trace / scores 文件——与 F214（不产证）/F243（评分者=生成者）机械互补：即使修好评分角色，agent 身份也无持久化载体（M，F214/F243 增补）
- `contracts/legacy.py:30` + `contracts/registry.py:14` 从 `shenbi.gates.shared` import PROJECT/SKILLS——契约层（自述"门/helpers/文档从此派生"的单一真理之源）反向依赖门包常量；当前无环（gates/shared.py 不 import contracts，实测双向 import 均成功），但若 gates/shared 未来 import 契约即循环——架构倒置 + 潜在脆弱（M）
- F252 关联：codex.py:111-112 返回 shenbi-score 的 rc 原样透传（含 3=MARKER_MISSING、2=REJECT），而 executor.py:232 docstring 声明 "2 = GATE_FAIL (write overreach or drift)"——调用方无法区分评分失败与写越权（M，review6 已列 rc 语义污染，本轮增补 exit 3 站点）
- `g4/pacing_design.py:44-46` `if not model.beats: mf.append("G4.pd.no_beat_data")` 分支为死代码——`from_markdown` 构造时 `_four_beats_present` 先抛 ValidationError，beats 为空永远走 except 分支（M，F248 关联）

---

## 三、严重度异议（不修改前八轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4 fail-closed 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。反证尝试（独立路径不跑 G3 故 P2？）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持 F218 P2 → P1**：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 实测）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——权威区间未定（SKILL.md 内部 6 处自相矛盾），维持低置信。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内，使 record_audit_outcome 永不执行（审计记录丢失）；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（chapter-drafting/context-composing/chapter-revision——主章节写作链路）的 .md 散文在 G2 被 continue 跳过，G2.6/G2.8/G2.9 对章节正文整体静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。
8. **F247 范围注记（非异议）**："40 个 N 写技能恒 rc=2"仅在 prompt 可提取英文 "chapter N" 时成立；中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 不触发 rc=2 但审计空转。缺陷本体成立，P1 维持。
9. **F243 机制修正（非异议）**：standalone 评分路径的实际首个阻塞是 gate-marker 强制（scoring.py:356-370 → exit 3），先于分数校验（REJECT exit 2）；F243 的"大概率 REJECT"表述需按 F252 修正为"exit 3 MARKER_MISSING 先行"。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前八轮一致）。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc / 子进程，零仓库写入）：
  - **F251** find_report 对 `-subagent.json` → None；G0.10 glob 不匹配；命名修正后内容形状读 0；G_RECONCILE 端到端 `GR.2:...:status=?` FAIL
  - **F252** shenbi-score 实跑 → `rc=3 MARKER_MISSING G4-shenbi-worldbuilding-generative`；`grep write_gate_marker src/shenbi/dispatcher/` → 空；scoring.py:338-353 G3 子进程仅 `--tier` 触发（codex.py 不传）→ marker 检查必然执行
  - 导入图：`import shenbi.contracts` 与 `import shenbi.gates.g1` 双向成功（无环）
  - F243 机制修正：scoring.py:356-370 marker 检查在分数校验（:422-432）之前
  - F210 hooks 行号（PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94）；F221 fixture 9 键/模型 8 字段/SKILL.md 8 键/OWNERSHIP 9 键四源计数；F231 povMode/sensitivityFlags 悬空 grep；F247 范围注记（中文 prompt → chapter=None）
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c / heredoc。

---

## 五、统计

- 新 finding：**2 条**（F251-F252：P2×2）；M 级观察 4 条随文
- 误报：**0 条整体误报**（F201-F250 共 50 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注维持；F243 机制修正 1 条——exit 3 先于 exit 2，本体成立）
- 严重度异议：**6 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1、F217→P1（低置信）、F228→P1（低置信）、F238→P1（低置信）、F229→P1（低置信）；另 F220 严重度存疑支持（建议维持 P2）；F247 范围注记 1 条、F243 机制修正 1 条（均非异议）
- 前八轮 findings 判定：F201-F250 共 50 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否**（F251 为评分产物命名/内容/状态三重消费契约不匹配——G0.10/G5.1/G_RECONCILE 三读者与唯一写者 codex.py 从未对齐；F252 为 standalone 编排漏 G4 → gate-marker 强制 exit 3 前置阻塞评分；两者均为独立根因，被 F227/F235/F247 掩盖，F227/F235/F247 修复后需重评严重度）
