# Z3 分区第七轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第七轮独立复核（本文件 `Z3.review7.md`，与初审 `Z3.a.md`、一轮 `Z3.review.md`、二轮 `Z3.review2.md`、三轮 `Z3.review3.md`、四轮 `Z3.review4.md`、五轮 `Z3.review5.md`、六轮 `Z3.review6.md` 隔离）
> 复核范围：34/34 文件全量重读（13,641 行），逐条独立验证 F300-F372；以真实生产项目 `novel-output/xinghuo-ranqiong/` 为格式/行为对照基准
> 本轮发现编号：F373-F378（6 条新发现，其中 1 条 P1（潜伏）、3 条 P2、2 条 M）；**新增误报 0 条**；新增严重度异议 0 条
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证仅在 /tmp 下做临时目录实验）
> 方法：34 文件逐行重读 + grep/uv run python/G4-CLI 实证 + 真实产物对照 + **/tmp 沙箱进程退出实验复证（F364）** + **G4 CLI 直接执行实验（F373/F371）**

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`。生产 volume_map 全中文（`## 第一卷：觉醒之火（第1-15章）`、`**章节范围**: 第1章 - 第15章（共15章）`），`_END_RE`/`_RANGE_RE` 零命中。novel.json 无 total_chapters（本轮实测 keys 无该字段）。影响链（卷边界触发全家 / `if total > 0:` 守卫跳过全部章间触发 / 无 book_closure / pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 章节节点提取误中 `| 1 |` 跨卷钩子表行）全部成立。
2. **F325（误报）维持撤销**：本轮实测 4 个 group skill 契约 `writes:` 即旧式路径（`group-factual → ['audits/chapter-N-continuity.md','audits/chapter-N-world-rules.md','audits/chapter-N-pacing.md']` 等，load_contract 实证）；生产 audits/ 722 文件全为旧式名、group-*.md 数=0；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING（`严重度: BLOCKING`）被 route 正则与文件扫描双重命中 → revision 执行。
3. **F364（P1 贴 P0）维持成立，本轮 /tmp 沙箱复证**：`register_emergency_handlers` → `atexit.register(_emergency_cleanup)`（crash_recovery.py:66），正常进程退出即无条件 `clear_staging`（:144-148）。本轮实测：`staging before exit: True` → 退出后 `STAGING GONE`（无信号、无异常）。消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交 → chapter-planning 无限循环）成立。
4. **F371（P1）维持成立，本轮 G4 CLI 复证**：`final-snapshot/` 目录路径 → generic G4 恒 FAIL（`G4.gen.not_found` / `G4.gen.read_error` 两态实测）。
5. **F353（P1）/F354（P1）/F345（P1）/F340（P1）/F341（P1）/F326（P1）全部维持成立**（逐条独立实证见结论表）。
6. **本轮最重要新发现（F373，P1 潜伏）**：`run_triggered_skills` 的 G4 校验用**未解析的 N 占位路径**（`step.output_path` 字面如 `audits/arc-N-score.md`），而 dispatch 写盘为**解析后的章号路径**（`_build_skill_prompt` 经 `resolve_chapter_path` + `extract_chapter(prompt)`）→ 5 个 N 型触发步骤（memory-distill L2 / score-arc / score-stratum / score-volume / review-arc-payoff）的 G4 恒 `not_found` → **周期（ch%12/ch%36）与卷边界触发一旦可达（F324/F353 修复后）必进 ESCALATION**。G4 CLI 直接实证（同一内容文件：`arc-N-score.md` → FAIL not_found，`arc-5-score.md` → PASS）。
7. **版本偏移注记（沿用三/四/五/六轮）**：生产 pipeline-state 的 steps_done 为 Plan 18 重构前的旧 skill 名（本轮实测 step_index=9 为旧版中间态），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据；staging/ 119 个文件为旧版遗留（旧版无 atexit 清理）。

---

## 一、复核结论总表（F300-F372 逐条判定）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")`——源码字面量求值后为 `replace("<","<")` no-op |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 :2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1` → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析/趋势落盘）不可达（本轮逐行确认无其他调用点）；cs.resonance_score 并行路径恒 None |
| F302 | **成立** | P1 维持（P0 依据存在——决策表 P0 例字面列明 "TokenLedger 少计"；裁定留 stage 4，无异议） | `dispatch_skill(state=)` 仅 chapter_loop.py:2799（串行可及）/ :2970（串行 BLOCKING 环，不可达）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751 均未传；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0（PipelineState 无 chapter 属性，state.py 实证） |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError；ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93） |
| F305 | **成立但框定需修正（同意二/三/四/五/六轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；route==NO_REVISION 时 `_is_revision_skipped`（:2755）先短路。残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 记账漂移 |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方（本轮实测生产该路径 MISSING）；:97 子串匹配缺陷 |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；`_parse_hook_entries` 对生产 plan section 7 的 MH-001/H-N01 行会接受但 raw_op=="advance" 非 plant/open 被跳过（:151）；检索侧断裂见 F346 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state 无生产调用 |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:430-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方 |
| F313 | **成立** | P1 维持，无异议 | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（shenbi-review-long-span 契约 writes 实证）→ G4 校验错位 |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback 无章号绑定、注入下一次串行 dispatch（:2775-2780）；同源缺口见 F365 |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用（grep 实证；generate_review_checklist 本体已在 dispatch_helper.py:767 接线）；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符 |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`；`_END_RE`/`_RANGE_RE` 对生产 `## 第一卷：觉醒之火（第1-15章）`/`**章节范围**: 第1章 - 第15章（共15章）` 零命中；novel.json keys 无 total_chapters |
| **F325** | **不成立（误报，维持二/三/四/五/六轮撤销）** | **P1 裁定撤回维持** | 本轮实测 4 个 group skill 契约 writes 即旧式路径（load_contract 实证）；生产 audits/ 722 文件全旧式名、group-* 数=0；真实 BLOCKING（`严重度: BLOCKING`）被 collect_audit_issues 正则命中 → route≠NO_REVISION → `_any_audit_has_findings` 同文件必 True → revision 执行 |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证）；并行 post-draft（:2405-2419）两线程均以 uses_staging 写 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）；四轮补充成立：`_parse_resonance_score` 三模式均不匹配生产 `**结果**: 通过 (77/100)` 格式 |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代（本轮 grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议 | core_skills（:2551，含 review-resonance + review-sensitivity）与 genre 波（genre-config auditDimensions.sensitivity=true 实证）双发同一 skill 写同一文件 `audits/chapter-N-sensitivity.md` |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库（该路径被 F364 清空后实际"静默不入库"） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless）；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 补零数=0；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；生产 review-summary.md 恒报 0 问题（本轮实测 chapter-1-review-summary.md 内容 "**BLOCKING Issues**: 0"） |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死 |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 配置在全部路径上均无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用 |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:24-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；本轮实测 6 个 audit skill 全部 requires_independent=True；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 `MH-001`/`H-N01` 零匹配（本轮实测）→ master hooks 从 Route A 检索静默缺失；`MH02 → ['H02']` 误截成立 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys 无 `hooks` 键（hook 以 body 表格呈现）；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)"；生产 book_spine.md `| MH` 行数=0 |
| F348 | **成立但机制描述部分不准（维持四/五/六轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入（本轮 load_contract 实证）；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md 生产均 MISSING）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层（:675-696）；非合规输出先失败一次再重试 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout（:1527/:1731/:1855） |
| F353 | **成立** | P1 维持，无异议 | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 writes 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；grep skills/ 契约 reads 0 个引用该文件（本轮实测生产 context/ 下 curated 文件数=0）；drafting 契约 reads `context/chapter-N-context.md` → 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断）每章每审计恒注入（幽灵输入 + token 浪费） |
| F358 | **成立** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError（:2034 raise，docstring "Pipeline paused for human review"）；drift-correction/warning/opening-variation 指令文件无任何消费者 |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra 被丢弃；drafting 契约 reads 不含 characters/*.md、world/rules.md → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存，无写盘副作用）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358（CLI main） |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |
| F364 | **成立** | **P1 维持；严重度异议支持 P1→P0（依据决策表"数据丢失 + 契约被静默违反"；裁定留 stage 4，无新增异议）** | 本轮 /tmp 沙箱复证：`register_emergency_handlers` 后正常退出 → staging 被 atexit 清空（`staging before exit: True` → 退出后 `STAGING GONE`）；且退出后 pipeline-state.json 的 current_step 被改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`（六轮补强面成立） |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交 + 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`——step 2 时当章 plan 尚不存在 → FileNotFoundError → 捕获 → `_write_minimal_context_fallback`；且 chapter-planning 契约 reads 不含 context/chapter-N-context.md → 组装成功也不被消费 |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格 → 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |
| F371 | **成立** | P1 维持，无异议 | 本轮 G4 CLI 实测：`shenbi-snapshot-manage "final-snapshot/" /tmp/nonexistent-project` → `G4.gen.not_found`；目录已存在 → `G4.gen.read_error`——两态恒 FAIL。`_resolve_closure_g4_path`（closure.py:148-159）对无 N 路径原样返回目录 → closure step 10 重试×3 后 ESCALATION → pipeline 永不 COMPLETED（第三条"永不完成"独立根因） |
| F372 | **成立** | P2 维持（latent），无异议 | `_style_profile_is_stale`（triggers.py:103-124）`chapters_dir.glob("chapter-*.md")` 计数把 `chapter-N-pre-rev.md` 计入（F350 备份恰落入通配；对照 `_load_previous_titles` :2251 用 `chapter-(\d+)\.md` 正则排除）→ style-learning 自愈触发提前/误触发 |

**误报统计**：F300-F372 中仅 F325 为误报（二轮撤销、三/四/五/六轮维持，本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：0 条新增。立场声明（均无异议）：F302（P1 维持，P0 依据存在留 stage 4）、F364（P1 维持，P0 依据存在留 stage 4）、F305（同意 P2）、F301（同意贴 P0 校准）、F324（P0）、F340/F341/F345/F353/F354/F371（P1）。

---

## 二、新发现（F373-F378）

### F373 | run_triggered_skills 的 G4 校验用未解析 N 占位路径（dispatch 写盘为解析后章号路径）→ 5 个 N 型触发步骤 G4 恒 not_found，周期/卷边界触发必进 ESCALATION | error | **P1**（潜伏——F324/F353 修复后即激活）

- 证据：
  - `triggers.py:200/205/218/242/247`（TRIGGER_STEPS 的 N 型 output_path：`truth/arcs/arc-N.md`、`audits/arc-N-score.md`、`audits/stratum-N-score.md`、`audits/volume-N-score.md`、`audits/volume-N-payoff.md`）；
  - `triggers.py:584`（`g4_file = step.output_path if step.output_path else ""` → `run_gate_g4(step.skill, [g4_file] if g4_file else [], project_dir)`——**字面 N 路径直接传给 G4**）；
  - 对照 dispatch 侧：`triggers.py:558`（prompt "Execute {skill} for chapter {chapter}..."）→ `dispatch_helper.py:1504`（`chapter = extract_chapter(prompt)` 取到章号）→ `dispatch_helper.py:656`（`resolve_chapter_path(write_path, chapter)` 把 `audits/arc-N-score.md` 解析为 `audits/arc-5-score.md`）→ "Files to create" 列出解析后路径 → LLM 写盘解析后路径；
  - `gates/shared.py:58-75`（`resolve_input_path` 仅 join rd+fp，无任何 N 占位替换）→ G4 checker 对 `audits/arc-N-score.md` 查不存在的字面文件。
- 根因：`run_triggered_skills` 的 G4 侧与 dispatch 侧对 N 占位符的处理不对称（dispatch 解析、G4 不解析）；TRIGGER_STEPS 的 output_path 从未经 `resolve_chapter_path`/`resolve_volume_path` 归一。
- 验证命令+输出：
  ```
  rm -rf /tmp/g4n && mkdir -p /tmp/g4n/audits && printf '## Route C\nx\n## Route A\n锚点\n' > /tmp/g4n/audits/arc-5-score.md
  uv run python -m shenbi.gates.cli G4 shenbi-score-arc "audits/arc-N-score.md" /tmp/g4n
  # → {"gate": "G4-score-arc", "status": "FAIL", "must_fix": ["G4.not_found:audits/arc-N-score.md"]}
  uv run python -m shenbi.gates.cli G4 shenbi-score-arc "audits/arc-5-score.md" /tmp/g4n
  # → {"gate": "G4-score-arc", "status": "PASS"}
  uv run python -c "from shenbi.pipeline.dispatch_helper import extract_chapter; print(extract_chapter('Execute shenbi-score-arc for chapter 5. Project dir: /tmp/x'))"  # → 5
  ```
- 影响：F324/F353 修复后，ch%12（memory-distill L2 + score-arc）、ch%36（score-stratum）、卷边界（score-volume + review-arc-payoff）触发时，N 型步骤 dispatch 成功写盘（arc-{N}.md 等）但 G4 恒查字面 arc-N.md → FAIL → `run_triggered_skills` 返回 False → cli.py:255-262 设 ESCALATION checkpoint → **每个触发周期都停等人审**（正常路径可复现功能错误）；另有次生问题：`volume-N-*` 路径被 `extract_chapter(prompt)` 解析为**章号**而非卷号（语义错位）。与 F313（closure 卷号替换）、F371（closure 目录路径）是同一族"G4 路径解析缺陷"的第三个独立实例，但机制不同（触发侧完全不解析）。
- 建议方向：`run_triggered_skills` 的 G4 路径先经 `resolve_chapter_path(step.output_path, chapter)`（卷型路径用 `resolve_volume_path` + 卷号）再传 `run_gate_g4`；或 TRIGGER_STEPS 改用无 N 的解析后模板；补一条"ch%12 触发全流程通过 G4"的集成测试（F324/F353 修复后必测项）。

### F374 | write_safety 将 shenbi-review-resonance 分类为 READ_ONLY_AUDIT，但其契约 updates 写 truth/audit_drift.md + truth/resonance_trend.md → 并行审计波在无串行保护下并发写 truth 文件（模块自述的 WRITE_SHARED 串行不变量被违反） | error | P2

- 证据：`write_safety.py:45-46`（`if skill.startswith("shenbi-review-"): return WriteSafety.READ_ONLY_AUDIT`）；`shenbi-review-resonance` 契约（本轮 load_contract 实证）`updates: ['truth/audit_drift.md', 'truth/resonance_trend.md']`；`chapter_loop.py:2515-2630`（并行审计波 Wave 1 含 shenbi-review-resonance，经 `dispatch_reviews_parallel` → `assert_parallelizable`（parallel_dispatch.py:131-147）仅按前缀分类放行）；`write_safety.py:3-7` 模块 docstring（"any skill not classified READ_ONLY_AUDIT must run serially"）。
- 根因：write-safety 分类依据 skill 名前缀而非契约 writes/updates；resonance 是写 truth 文件的技能却被标为只读审计。
- 验证：
  ```
  uv run python -c "from shenbi.pipeline.write_safety import classify_skill_write_safety; print(classify_skill_write_safety('shenbi-review-resonance').value)"
  # → read_only_audit
  uv run python -c "from shenbi.contracts.legacy import load_contract; print(load_contract('shenbi-review-resonance').get('updates'))"
  # → ['truth/audit_drift.md', 'truth/resonance_trend.md']
  ```
- 影响：当前各审计写盘文件互不重叠（resonance 写 truth/、其余写 audits/），无实际 lost-update；但模块声明的"WRITE_SHARED 必须串行"不变量对共振审计不成立——任何未来在 wave 中加入写同一 truth 文件的审计 skill 都会静默引入竞态（与 F326 同族但机制不同：F326 是并行 post-draft 未走分类检查，本 finding 是分类本身错误）。属契约脆弱点 + 文档-行为漂移。
- 建议方向：`classify_skill_write_safety` 改为读取契约 `updates`/`writes` 判定（truth/ 前缀 → WRITE_SHARED），或把 resonance 移出并行波（串行执行 + G3）；补一条分类与契约一致性测试。

### F375 | API 路径多文件输出部分缺失仍返回成功（missing 仅 log.error，DispatchResult 恒 True）→ state-settling 等 6 文件技能的残缺输出被静默接受 | error | P2

- 证据：`dispatch_helper.py:1646-1671`（`written = _write_parsed_outputs(...)` → `if not written: return DispatchResult(False, ...)`（仅全缺才失败）→ `missing = [p for p in output_paths if "*" not in p and not (project_dir / p).exists()]` → `log.error("api_missing_outputs", ...)` → **`return DispatchResult(True, 0, output_text, "")`**）；`_write_parsed_outputs`（:1153-1168）对 parsed 中缺失的 rel_path 走 `parsed.get("__stdout__", "")` → 空 → `output_empty` → skip。
- 根因：missing 只做日志不做失败判定；G4 侧（`_resolve_g4_files` 对 state-settling glob `staging/truth/*.md`，chapter_loop.py:577-583）只校验"已存在的文件"，缺失文件无人检查。
- 影响：state-settling 若 LLM 漏写 6 个 truth 文件中的任意一个（如 emotional_arcs.md），dispatch 仍 success → G4 对现存文件通过 → 章节以缺 truth 文件状态推进（下游读到旧/缺数据）；与 F354（整波失败静默）互补：本 finding 是**部分**输出静默接受。
- 建议方向：`missing` 非空时返回 DispatchResult(False)（携带 missing 列表供重试反馈），或对多文件步骤在 G4 前断言 output_paths 全部存在。

### F376 | `pipeline-truth-embed update` 无 `--text` 时静默报 OK（不嵌入任何内容） | error | M

- 证据：`truth_embed.py:243-251`（`if args.command == "update" and args.text:` 不满足 → 落 `store.close(); emit_json({"status": CommandStatus.OK, "command": args.command})`）——update 命令无 --text 时不做任何嵌入却返回 OK。
- 根因：main() 的 update 分支缺少"无 --text 即报错"的守卫（初审 per-file 报告已注记 "M/P2 未单列"，从未编号）。
- 影响：维护命令误用时静默 no-op，机器可读 stdout 误导。
- 建议方向：`update` 无 `--text` 时 `emit_json({"status": CommandStatus.ERROR, ...})` + return 1。

### F377 | check_audit_completeness 的 VERDICT_MARKERS 含通用词 "通过"，no_verdict 检查几乎永不触发 | error | M

- 证据：`llm_output_integrity.py:51`（`VERDICT_MARKERS = ["判定", "结论", "verdict", "通过", "阻断", "PASS", "BLOCK"]`）；`:149`（`has_verdict = any(marker in text for marker in VERDICT_MARKERS)`）——"通过"在中文审计文本中高频出现（"通过审阅章节…"等普通用法），`has_verdict` 恒 True → `G4.ac.no_verdict`（:150-153）几乎不触发（初审 per-file 报告已注记 "检查偏弱，P2 边界，未单列"，从未编号）。
- 根因：判定标记用裸子串而非"判定/verdict 语义键"正则（对照 revision_router._SEVERITY_BLOCKING_RE 的语义键做法）。
- 影响：审计完整性检查形同虚设（no_verdict 只对纯 preamble 且不含"通过"字样的文件触发）；弱检查仅影响审计产物质量告警（不阻断），M 级。
- 建议方向：VERDICT_MARKERS 改为 `(?:判定|verdict|结论)\s*[:：]\s*(通过|阻断|PASS|BLOCK)` 语义正则，剔除裸 "通过"。

### F378 | context/volume-N-complete.json 全仓无写入方 → `_check_volume_completion` 恒 False → 软失败升级链恒触发 volume_objective_missed 信号；`last_trigger_failure` 写后无消费者 | error | P2

- 证据：
  - `chapter_loop.py:955-964`（`_check_volume_completion` 唯一读取 `context/volume-{n}-complete.json`）；grep 全 src/ + skills/ **0 个写入方**（本轮实测）；
  - `chapter_loop.py:978-1005`（`_check_soft_fail_escalation`：任一 SoftFailTracker ≥ 阈值即把 `volume_objective_met=_check_volume_completion(...)` 传入 `check_escalation`）；`skill_utils/escalation/check.py:70-74`（`if not volume_objective_met: signals.append(volume_objective_missed)`——**无条件触发，无阈值**）；
  - `chapter_drafting.py:206/227`（G4 可产出 `G4.transition`/`G4.fatigue` 软失败项 → SoftFailTracker.record 可达）；
  - `triggers.py:575/593`（`state.last_trigger_failure` 唯一写入方），grep 无任何消费者（仅 state.py 序列化）。
- 根因：volume-objective marker 的写入方从未落地（与 F308/F315 同族"半接线"）；last_trigger_failure 只写不读。
- 验证：
  ```
  grep -rn "volume-.*complete" src/shenbi/ --include="*.py" | grep -v "def \|#"   # → 仅 chapter_loop.py:959(docstring)/963(读)
  grep -rn "last_trigger_failure" src/shenbi/ --include="*.py" | grep -v "triggers.py\|state.py"   # → 无消费者
  ```
- 影响：一旦任何章节的 drafting G4 软失败 3 次（5 章窗口）触发 `_check_soft_fail_escalation`，`check_escalation` 因 `volume_objective_met` 恒 False 必发 `volume_objective_missed` 信号 → 误派 escalation-review + 写 signals sidecar（与真实软失败原因无关的误导信号）；`last_trigger_failure` 记录无任何消费/展示路径（状态里查不到）。
- 建议方向：为 volume objective 建立真实写入方（closure/volume-consolidation 落 marker），或将 `_check_volume_completion` 改为"未知即视为 met"（与 check_resonance(None)=True 同款保守语义）；接线或删除 last_trigger_failure。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件全量 deep-read，共 13,641 行；`__init__.py` 1 行至 chapter_loop.py 3053 行全部逐行）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries` 返回 set()；audits/ 722 文件 13 类后缀全旧式名、group-* 数=0；chapters/ 145 文件补零数=0、pre-rev 数=0；novel.json keys 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0；context/ curated 文件数=0；staging/ 119 个旧版遗留文件；style/ 有 style_profile.md 而 truth/style_profile.md、truth/world_rules.md、truth/volume_map.md 均缺失）；`uv run python` 实证 `read_volume_boundaries/requires_independent/classify_skill_write_safety/extract_chapter`、load_contract 契约 reads/writes/updates（group-*/resonance/lifecycle/state-settling/volume-outlining）；**G4 CLI 直接执行实验**（F373 唯一新验证手段 + F371 复证）；**/tmp 沙箱 atexit 退出实验**（F364 复证）。
- 前几轮覆盖空洞（本轮已补）：(1) **触发路径的 G4 N 占位解析缺口**——前几轮只审了 closure 的 N 替换（F313）与目录路径（F371），从未把 TRIGGER_STEPS 的 output_path 与 dispatch 侧 resolve_chapter_path 对照 → 漏掉 F373；(2) **write_safety 分类与实际写路径脱节**（resonance 写 truth 仍标 READ_ONLY_AUDIT）；(3) **API 多文件部分输出静默接受**（前几轮只覆盖整波失败 F354 与 G4 失败 F331）；(4) **volume-complete marker 无写入方**（`_check_volume_completion` 恒 False → 软失败升级链恒发 volume_objective_missed 信号）。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 路径（codex/zcode）本环境不可执行，仅静态分析；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内）。

---

## 四、汇总

- **新 finding 数**：6（F373 = P1（潜伏）、F374/F375/F378 = P2、F376/F377 = M）
- **误报数**：0 新增（F300-F372 全部成立/维持；F325 维持二/三/四/五/六轮误报撤销，本轮独立复证——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名；F348 维持"成立但机制描述部分不准"）
- **严重度异议数**：0 新增（立场声明：F302 与 F364 的 P1→P0 依据均维持留 stage 4；F305 同意 P2；其余无异议）
- **0 新 finding 判定**：**否** —— 本轮发现 1 条 P1 漏报（F373 触发路径 G4 N 占位不解析，G4 CLI 实证）与 3 条 P2 漏报（F374 write_safety 分类脱节、F375 API 部分输出静默接受、F378 volume-complete marker 无写入方 + last_trigger_failure 无消费者）及 2 条 M（F376/F377，均为初审 per-file 注记过但从未编号的条目）。F324（P0）、F340/F341/F345/F353/F354/F364/F371（P1）维持成立、F325（误报）维持撤销。
- **校准升级提示**：F373 建议与 F324/F353/F371 一并纳入"pipeline 永不完成/触发全失效"根因族处置（触发路径修复时必须同步解析 G4 路径占位符）；F374 建议并入 F326/F345 的并行波正确性族；F378 建议并入 F308/F315 的"半接线死信号"清理族。

---

## 五、独立复核声明

本报告为 fresh-context 第七轮独立复核：34/34 文件逐行重读；对 F300-F372 的判定全部基于本轮独立 read/grep/uv run python/G4-CLI/沙箱实证与真实产物对照（未复用前七轮结论）；新增 6 条发现（F373-F378）均为前七轮报告未以编号形式出现的观察，其中 F373 的验证手段（G4 CLI 对字面 N 路径与解析后路径的对照）为前几轮未使用的新证据路径，F364/F371 的沙箱与 G4 CLI 实验为本轮独立复证。
