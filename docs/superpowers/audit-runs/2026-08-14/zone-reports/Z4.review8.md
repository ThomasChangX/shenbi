# Z4.review8 复核报告 — src/shenbi/gates/（含 g4 checkers）第八轮独立复核

- 复核 agent：第八轮独立复核（fresh-context，全量重读，不依赖前八轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`（F419-F430）+ `Z4.review2.md`（F431-F439）+ `Z4.review3.md`（F440-F445）+ `Z4.review4.md`（F446-F455）+ `Z4.review5.md`（F456-F461）+ `Z4.review6.md`（F462-F467）+ `Z4.review7.md`（F468-F472）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 checker 逐个通读；`src/shenbi/paths.py`、`src/shenbi/contracts/skills/genre_config.py`、`src/shenbi/contracts/schemas/decisions.py`、`src/shenbi/pipeline/dispatch_helper.py`、`skills/shenbi-chapter-planning/SKILL.md` 作交叉证据核实；真实项目 `novel-output/xinghuo-ranqiong` 实证）
- 实测基线：`.venv/bin/python` 只读验证 20+ 组（/tmp 临时目录构造 + 真实项目 `novel-output/xinghuo-ranqiong` 实证 + 真实 SKILL.md/volume_map/pending_hooks/chapter-7/chapter-15 内容核对；未修改任何仓库文件，未 git add/commit）
- 复核结论：**误报 0**；**新增严重度异议 0**（确认前几轮 F404 P2→P1、F408 P2→P1 两条异议成立，本轮独立反证无推翻理由）；**新发现 11**（F473-F483：2×P2 强证据 + 3×P2 + 6×M，全部实测或代码路径验证）
- 结论：**0 新 finding 判定 = 否**（本轮发现 11 个前七轮漏报，未收敛到 0）

---

## 一、复核结论表（F400-F472 逐条判定）

| 编号 | 初审严重度 | 前几轮判定 | 本轮判定 | 理由（本轮独立证据） |
|---|---|---|---|---|
| F400 | P2 | 成立 | **成立** | 读码确认 g2.py:315-322（G2.12）/273-287（G2.10）无 file_type 守卫；JSON 末行 `}` 不在 sentence_enders → 恒 WARN。 |
| F401 | P1 | 成立 | **成立** | 反证核查：GR.2（g_reconcile.py:52-62）`stem.rsplit("-", n_split)` 后 candidate_tt 含 `-scores`/`-scores-subagent`，progress 键为 `generative` → `td={}` → 恒 FAIL。实测 `<skill>-generative-scores.json` + DONE → FAIL。无推翻理由。 |
| F402 | P1 | 成立 | **成立** | length_normalizing.py:29 解析 pf 而 :35 `word_count_md(fp)` 用原始 fp（读码确认）。 |
| F403 | P2 | 成立 | **成立** | cli.py:113-121 无 try/except；11 个 RoundPaths checker 双 None raise ValueError（读码确认）。 |
| F404 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 反证核查（直接读 contracts/schemas/decisions.py:30-45）：`has = rationale is not None`；空串在 manual_override/high 下 PASS（REQUIRED 被绕过）。AGENTS.md:70 契约在正常路径被静默违反 → P1 无推翻理由。 |
| F405 | P2 | 成立 | **成立** | G4_CHECKER_SKILLS 22 项（shared.py:247-270）vs generic.py checkers 分发表 31 项（读码确认）。 |
| F406 | P2 | 成立（限定） | **成立**（限定） | g0.py:195/323 用 `PROJECT/skill-output`、g7.py:66 用 `rd/skill-output`；`ls -d` 实测仅 `novel-output` 存在；round-exec 测试布局有 skill-output 的限定维持。 |
| F407 | P2 | 成立 | **成立** | g0.py:177-179 无 seed → `return passed("G0", [G0.1 SKIP])`（读码确认）。 |
| F408 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 反证核查（直接读 dispatch_helper.py:1925-1936）：run_gate_g3 在 progress.json 缺失时 safe_write 伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history → G3.4 fail-closed 被击穿并制造虚假审计证据。P1 无推翻理由。 |
| F409 | P2 | 成立 | **成立** | g1.py:281-282 空输入仅记 G1.0 SKIP → PASS（读码确认）。 |
| F410 | M | 成立 | **成立** | SHORT_MAP 20 项 vs 11 个缺 shorthand 技能目录存在（前几轮实测）。 |
| F411 | M | 成立 | **成立** | g0.py:220 `missing_dirs` 永不 append、:227 恒假；:532 注释 "20 skills" vs 22（读码确认）。 |
| F412 | M | 成立 | **成立** | g3_independence.py:3 引 g3.py:144-162，实际 G3.4 在 193-206（读码确认）。 |
| F413 | M | 成立 | **成立** | decisions_validator.py:175 注释 "shared.py:113"；fail() 在 125-136（读码确认）。 |
| F414 | M | 成立 | **成立** | chapter_revision.py:97 `"HARD_FAIL"` 不在 GateStatus 词表（status.py 仅 5 值；读码确认）。 |
| F415 | P2 | 成立 | **成立** | SKILL.md:125=`<!--META-END-->`，规则在 :140；chapter_drafting.py:73 引 :125，漂移 15 行（grep 复验）。 |
| F416 | M | 成立 | **成立** | decisions-schema.md:66-68 仅 low/high；decisions.py:12 VALID_SEVERITY 含 medium（读码确认）。 |
| F417 | — | 成立 | **成立** | tests/unit/gates 无 test_gate_manifest.py、g4 无 test_memory_distill.py（ls 复验）；gate_manifest 0% 覆盖属实。 |
| F418 | P2 | 成立 | **成立** | g0.py:665 `check_config_coherence(project_dir)` 唯一生产调用点，无 floor 实参（读码确认）。 |
| F419 | P2 | 成立 | **成立** | g1.py:210-213 / g2.py:169-172 except 元组缺 ValueError（读码确认）。 |
| F420 | P2 | 成立 | **成立** | g6.py:50/56 novel.json/genre-config.json jload 无保护（读码确认）。 |
| F421 | P2 | 成立 | **成立** | g_reconcile.py:33 无 try；g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError（读码确认）。 |
| F422 | P2 | 成立 | **成立** | cli.py:83-87 JSON 解析失败 → `input_files=[]`（读码确认）。 |
| F423 | P2 | 成立 | **成立** | `ls tests/scoring.py` → 不存在，实现在 src/shenbi/scoring.py；g0.py:358 引用旧路径 → 恒 WARN。 |
| F424 | P2 | 成立（限定） | **成立**（限定） | chapter_drafting.py:219-222 skill-output 爬升；pipeline 布局下 `read_genre_config(project_root)` 读不到 → FATIGUE_BASE 兜底。 |
| F425 | P2 | 成立（限定） | **成立**（限定） | g7.py:66+86-91 G7.6 truth 搜索嵌在 `no_dir.exists()`（skill-output）之下；pipeline 布局恒 SKIP。 |
| F426 | P2 | 成立 | **成立** | genre_config.py:28 `str(Path(rd) / fps[0]) if rd and fps else fps[0]` 未走 resolve_input_path（读码确认）。 |
| F427 | P2 | 成立 | **成立** | chapter_revision.py:45 `Path(fp)` 原始路径，OSError 被 :51 捕获 → 误报 invalid_json（读码确认）。 |
| F428 | P2 | 成立 | **成立**（根因补充→F468） | g3.py:98/103 直读路径用 acceptance.t1=94 vs :113/:125 回退硬编码 90（读码确认分叉）。 |
| F429 | P2 | 成立 | **成立** | g1.py:228-232 gate 内 safe_write .bak（FAIL 判定前已落盘）；AGENTS.md 纯验证契约偏离（读码确认）。 |
| F430 | P2 | 成立 | **成立** | foreshadowing_track.py:41-46 裸词 `操作` 命中即 PASS（读码确认）。 |
| F431 | P2 | 成立（范围不全→多轮补充） | **成立**（家族仍可扩展，见 F477） | 前几轮枚举 8+g0 2+deps+worldbuilding 2+G3.2/G5.1 3 处；本轮补充 g_reconcile.py:60 的 skills[skill] 非 dict 实例（见新 F477）。 |
| F432 | P1 | 成立 | **成立** | 反证核查：ALL_SKILLS=74 vs t1-skill 70 目录（68×shenbi-* + _template + using-shenbi），差 5（foreshadowing-lifecycle + review-group-* 4，实测名单一致）；G7.1b（g7.py:59）以 74 为全集 → 完整 round 恒 FAIL。无推翻理由。 |
| F433 | P2 | 成立 | **成立** | chapter_drafting.py:331-334 条件判 project_dir、取值用 project_root（读码确认）。 |
| F434 | P2 | 成立 | **成立** | state_settling.py:14/46-53 单字符子串匹配（读码确认）。 |
| F435 | P2 | 成立 | **成立** | g0.py:521-529 exempt_skills 读后零消费（grep 仅 522/527）；:530-533 注释声称的校验未实现（读码确认）。 |
| F436 | M | 成立 | **成立** | g6.py:125/142 planted_chapters 零消费（grep 复验）。 |
| F437 | P2 | 成立 | **成立** | g5.py:274-296 except Exception → WARN；对照 G6.3/G7.13 fail-closed（读码确认）。 |
| F438 | M | 成立 | **成立** | SKILL.md:85/134 均 ≤1/3000；chapter_drafting.py:204 `max(5, wc//1000)` 放宽 3 倍未同步（grep 复验）。 |
| F439 | P2 | 成立 | **成立** | `'shenbi-style-polishing' in BACKUP_SKILLS` → False；style_polishing.py:42-43 读 .bak 恒不成立且无 SKIP 记录（实测）。 |
| F440 | P2 | 成立 | **成立** | g0.py:202-207（G0.3）/524-528（G0.12）except 缺 ValueError（读码确认）。 |
| F441 | P2 | 成立 | **成立** | g0.py:98-99 json.loads 直读 + :107 `.get` 无 isinstance 守卫（读码确认）。 |
| F442 | P2 | 成立 | **成立** | length_normalizing.py:61-64 只查 3000/10000 无 25% 双底线（读码确认；SKILL.md:38/51-53/62）。 |
| F443 | P2 | 成立 | **成立** | g1.py:102-144 check_fields_exist 全仓仅测试引用（grep 复验）。 |
| F444 | P1 | 成立 | **成立** | g3.py:151-153 读 skill 层 output_files；生产 progress 形状（codex/materialize/round-exec/run_gate_g3 伪造）均无该键 → G3.3 恒 SKIP（读码确认）。无推翻理由。 |
| F445 | M | 成立 | **成立** | shared.py:210-222 unimplemented() 全仓零调用（grep 复验）。 |
| F446 | P2 | 成立 | **成立** | shared.py:314-331 减法表把契约词"然而"减净、漏减 然后/依然/显然 等（读码确认）。 |
| F447 | P2 | 成立（范围不全→F461） | **成立** | chapter_drafting.py:203/:225 transition/fatigue 全文件计数 vs :241-246 G4.meta 剥离（读码确认）。 |
| F448 | P2 | 成立 | **成立** | chapter_drafting.py:70 `第\d+章` 仅 ASCII 数字；SKILL.md:140 明示"第一章"违规（读码确认）。 |
| F449 | P2 | 成立 | **成立** | g_reconcile.py:40/61-62 `"DONE"` vs 生产小写 done（codex.py:44/materialize）（读码确认）。 |
| F450 | P2 | 成立 | **成立** | g6.py:118-142 按 `## hooks`+`- id:` 解析；真实 pending_hooks.md 为表格格式（head 实证）→ total_hooks=1 恒 low_hook_density FAIL。 |
| F451 | P2 | 成立 | **成立** | g6_checks.py:101-102/:234-240 剥离正则前瞻仅 `## `+`\Z`（缺 `# `/`<!--META`），对比 shared.py:111 `_META_BOUNDARY`（读码确认）。 |
| F452 | P2 | 成立 | **成立** | chapter_drafting.py:293/:316-328 未剥离元区块即取 `paragraphs[-1]`（读码确认）。 |
| F453 | P2 | 成立（限定） | **成立**（限定） | g2.py:237-238 `_is_important_chapter(fp, project_dir or "")`；phase_runner.py:213 与 g3.py:177 无 pd 实参；dispatcher/executor.py:144 传 pd 的限定维持（读码确认）。 |
| F454 | P2 | 成立 | **成立** | g_transition.py:47-64/:70-85 读 `remaining_{phase}`/`gate_blockers` 键；round-exec 形状无此键 → 恒真空 PASS，materialize 形状恒 FAIL（读码确认）。 |
| F455 | M | 成立 | **成立** | generic.py:28 docstring 声称 "has frontmatter"，:44-56 无任何 frontmatter 检查（读码确认）。 |
| F456 | P2 | 成立 | **成立** | g6.py:66 `sorted(ch_dir.glob("chapter-*.md"))` 无 key= 数值排序（字典序；读码确认）。 |
| F457 | P2 | 成立 | **成立** | g6_checks.py:111-113/:245 `len(re.findall(...))` 计段数非字符数 → 分子量纲错配（读码确认）。 |
| F458 | P1 | 成立 | **成立** | 反证核查（直接读 decisions.py:48-59）：`Adjustment.rationale: str` 必填但 `_rationale` validator 仅查 `> _RATIONALE_MAX_CHARS`；`"rationale": ""` → PASS（HOLE）。AGENTS.md P2.5 "rationale REQUIRED on adjustments" 被空串绕过 → P1 无推翻理由。 |
| F459 | P2 | 成立 | **成立** | g4/worldbuilding.py:40-53/:61-64 jload except 缺 ValueError（读码确认）。 |
| F460 | P2 | 成立（范围不全→F465/F471） | **成立**（家族仍可扩展，见 F475） | 4 处实例读码确认；本轮补充 G0.3/G6.1 的 chapter_word.default=0 → ZeroDivisionError（非 AttributeError 家族，见新 F475）。 |
| F461 | P2 | 成立 | **成立** | chapter_drafting.py:334/:293-302 protagonist/scene_concreteness 未剥离 PRE/POST（读码确认）。 |
| F462 | P2 | 成立 | **成立** | g3.py:103 `data.get("total_score", data.get("score", 0))` vs scoring.py 规范输出顶层 `final_score` → 规范形状 score=0 恒 FAIL（grep 确认）。 |
| F463 | P2 | 成立 | **成立** | g3.py:100 遍历全部报告无 skill 过滤、:107 用 gate 技能 rubric 评估其它技能报告（读码确认）。 |
| F464 | P2 | 成立 | **成立** | codex.py:56 `-scores-subagent.json` 唯一自动化 t1-reports 写入方；find_report/G0.10 glob/GR.2 均不匹配（读码+grep 确认）。 |
| F465 | P2 | 成立（范围不全→F471） | **成立**（家族仍可扩展，见 F477） | 4 处实例读码确认；本轮补充 g_reconcile.py:60（GR.2 逐技能值）同族新实例（见 F477）。 |
| F466 | P2 | 成立 | **成立** | chapter_drafting.py:260-261 `Path(rd)/"project-output"/"chapters"`；G6.3（g6.py:86-93）传 rd=pd → 静默跳过（读码确认；review7 补充 G5.5/run_gate_g4 同形状调用方）。 |
| F467 | P2 | 成立 | **成立** | chapter_planning.py:37 sections 接受 `## 5[\.、：:\s]` vs :82/:97 s5/s7 仅认 `## 5\.`（读码确认）。 |
| F468 | P2 | 成立 | **成立** | g3.py:98 循环外取 threshold vs :113/:125 循环内覆写 → 跨报告状态泄漏（读码确认）。 |
| F469 | P2 | 成立 | **成立** | generic.py:313 `make_composite_checker(g4_decisions, g4_chapter_revision)` 接线错位（读码确认；其余 composite 均为 existing 结构 checker + decisions schema checker 的正确顺序）。 |
| F470 | P2 | 成立 | **成立** | g3.py:97/102、g5.py:67 jload except 缺 ValueError（g5.py:67 完全无 try）（读码确认）。 |
| F471 | P2 | 成立 | **成立** | gate_manifest.py:64-66 "gates" 键为列表时 `setdefault` AttributeError（读码确认）。 |
| F472 | P2 | 成立 | **成立** | cli.py:105 解析 rd 但 :107-110 bughunt/clean 分支不传 rd → 相对路径恒 resolve_input_path ValueError（读码确认）。 |

**误报判定：0 条。** F400-F472 全部成立。对任务点名的 6 个 P1（F401/F404/F408/F432/F444/F458）逐一反证核查（直接读 decisions.py/dispatch_helper.py/g_reconcile.py 源码 + 实测），全部确认成立、无推翻证据；不准确处仅限范围不全：F431/F460/F465 家族在本轮仍各发现新实例（F475/F477 补充），F406/F424/F425 "恒 SKIP/永不命中" 仅对 pipeline round 布局成立（维持 review2 限定），F453 有 executor.py 传 pd 的限定（维持 review7 限定）。

**严重度异议：0 新增。** 确认前几轮 2 条异议成立（F404 P2→P1、F408 P2→P1——两者均属"AGENTS.md 显式契约在正常路径被静默违反"，按决策表 P1 恰当）；其余严重度按决策表核对无异议。

---

## 二、新发现（F473-F483）

### F473 | G6.11 卷边界检查在真实项目上恒发 7 条 must_fix：跨卷伏笔表格单元格被解析为"幽灵卷" + no_ending_hook 评估的是文件尾部元区块而非叙事结尾 + 规划中未写卷恒 FAIL | error | P2
- 证据: g6.py:306-351。`vol_pat`（:306 `(?:第\s*(\d+|[一二三四五六七八九十百千]+)\s*卷|Volume\s+(\d+))`）无 `## ` 锚定——真实 volume_map.md 的"伏笔跨卷追踪"表格单元格（novel-output/xinghuo-ranqiong/outline/volume_map.md:66-69 `| 1 | ... | 第2卷 | 第26-28章 | 已种植 |`、:165-168、:250-253、:344-347）含"第2卷/第3卷/第4卷/第5卷"字样，随后 300 字符内 `range_pat`（:308）命中同行的"第N-M章" → 生成幽灵卷 2(26-28)/3(46-48)/4(61-63)/5(78-79)（:326 去重按卷名保留首个，幽灵卷因先出现而保留）。no_ending_hook（:349-351）取 `last_ct[-1000:]`——真实章节以 改写报告/POST 自检元区块收尾（chapter-15 尾部实证为"改写报告 replacement strategy"文本）→ 检查的是元文本而非叙事结尾。
- 根因: (a) 卷头正则无标题锚点，误吞跨卷 hook 表格行；(b) 章末钩子检查未先剥离元区块（F452 同类缺陷在 G6.11 的第二实例）；(c) hook_markers（:349）含裸单字"将"（高频字，正常散文结尾几乎必含 → FAIL 分支近死路）且对"规划中未写卷"（第五卷 76-100 章）恒 FAIL。
- 验证命令+输出: `.venv/bin/python -c "from shenbi.gates.g6 import gate_G6; gate_G6('long-form', None, 'novel-output/xinghuo-ranqiong')"` → `status: FAIL`，7 条 G6.11：`no_ending_hook:vol一:ch=chapter-15.md` / `vol二:ch=chapter-35.md` / `vol三:ch=chapter-55.md` / `vol四:ch=chapter-56.md` + `no_chapters:vol4:range=61-63` / `vol5:range=78-79`（幽灵卷，卷 4/5 在真实项目只写到 56 章）/ `vol五:range=76-100`（规划卷）。**review4 实测同一项目得到 G6.11=7 并逐类归因，但从未把 G6.11 归入任何 finding——本轮补上。**
- 建议方向: vol_pat 锚定 `^##\s*第...卷`（排除表格单元格）；no_ending_hook 用 word_count_md 同款元区块剥离后再取尾部；hook_markers 去掉裸"将"；对"卷范围超出已写章节数"改为 WARN 或按 min_ratio 容忍（与 G6.1 一致）。补真实 volume_map.md 形状回归用例。严重度说明：真实 pipeline round 每轮 G6 均因该条 FAIL，可争 P1；与 F450（G6.7 格式漂移恒 FAIL）同类取 P2。

### F474 | G6.4 时间线日期提取对单章内非时序日期引用（截止日/当前日混排）误报 timeline_regression——真实项目 2 条 FAIL | error | P2
- 证据: g6_checks.py:24-42。timeline 收集把同一章内所有 `第N天`/`N月N日` 按**文本出现顺序**入列；回归判定（:41 `if pt == timeline[i][2] and cv < pv and cc >= pc`）允许同章比较（`cc >= pc`）。真实 chapter-7 第 48 行"宽限截止：铁炉堡标准时 7月21日 晨哨前"（截止日）先于第 57 行"今天是7月16日"（当前日）出现 → 721→716 被判回归；该叙述完全合法（先挂截止日、再揭示今日）。附带机制：日期编码 `month*100+day`（:34）无年份处理，跨年合法推进（12月31日→次年1月5日）同样误报。
- 根因: 日期提取不区分"事件时间"与"引用时间"（截止日/回忆/对话中的日期），且同章条目互比；无年份维度。
- 验证命令+输出: `gate_G6('long-form', None, 'novel-output/xinghuo-ranqiong')` → `G6.4:timeline_regression:ch7->ch7:721->716(date)` ×2（第 57/169 行两次"宽限到7月21日。今天是7月16日"产生两条，无去重）；合成用例 chapter-1="这一年12月31日"/chapter-2="次年1月5日" → `timeline_regression:ch1->ch2:1231->105(date)`（跨年误报）。**review4 实测 G6.4=2 亦未归因。**
- 建议方向: 同章内只保留首个日期或排除引用语境（如"截止/宽限/到"等前置词）；日期编码补年份维度；对同一 (ch, value, type) 去重。补"同章先截止日后当前日 → PASS"与"跨年推进 → PASS"用例。

### F475 | G0.3/G6.1 的 chapter_word.default 读取是死特性（GenreConfig 契约与真实配置均无此键）且 default=0 时 ZeroDivisionError 崩溃 | error | P2
- 证据: g0.py:204（`gc_data.get("chapter_word", {}).get("default", CHAPTER_WORD_FLOOR)`）与 :209（`expected = -(-target_words // default_w)`）；g6.py:56/:60 同型；两处 except 元组（g0.py:206 仅 JSONDecodeError/OSError）不含 ZeroDivisionError。`GenreConfig` 模型（src/shenbi/contracts/skills/genre_config.py:15-38）8 字段无 chapter_word；真实配置 `novel-output/xinghuo-ranqiong/genre-config.json` 与 `test-validation/genre-config.json` 均无 chapter_word 键（实测）；全仓 grep `chapter_word` 仅 g0.py/g6.py 4 处消费。
- 根因: (a) 死读取——配置契约从不产生该键，即使按 F406 修复目录名，G0.3 的 expected_chapters/G6.1 的 default_w 仍恒用 3000 兜底（F406 只覆盖了目录名层面）；(b) 畸形配置 `chapter_word.default=0` 时 `-(-x // 0)` 抛 ZeroDivisionError，不在任何 except 内 → gate 崩溃无 JSON 输出。
- 验证命令+输出: `.venv/bin/python -c`（patch g0.PROJECT + `skill-output/demo/genre-config.json={"chapter_word":{"default":0}}` + seed 目标字数 6000）→ `G0 CRASH: ZeroDivisionError integer division or modulo by zero`；`gate_G6` 同构造 → `G6 CRASH: ZeroDivisionError`。
- 建议方向: 从 GenreConfig 契约删除/迁移 chapter_word 语义（或补字段并接线到 genre-config 输出），G0.3/G6.1 的 default_w 计算加 `if default_w <= 0` 守卫（回退 CHAPTER_WORD_FLOOR）；与 F406/F418 一并处置 G0 配置接线。补 `default=0` 用例。

### F476 | G4.chapter-planning 仅实现 SKILL 声明 8 条"可自动检查"规则中的 2 条（段完整性/chapter_role），缺 6 条（段标题精确性/优先级来源声明/章尾改变数/hook 账列名/hook 操作有效性强制/沉默检测） | error | P2
- 证据: skills/shenbi-chapter-planning/SKILL.md:260-270（"可自动检查的计数规则"表 8 条）vs chapter_planning.py:34-104（实现：sections 存在性 :37-43、chapter_role :46-49；s7_hook_ops :97-104 只查 ≥1 个合法 op 出现，不拒绝非法值；无 段标题精确性/优先级来源/章尾改变数/列名/沉默检测 任何实现）。
- 根因: gate 停留在旧版关键词检查，SKILL 升级后的可检查规则未同步（F442 同族：SKILL 契约 ↔ gate 实现漂移）；F467 只覆盖了分隔符不一致，未覆盖缺失规则本体。
- 验证命令+输出: 构造含非法 op（close/destroy）+ 缺"优先级来源" + 缺列名（仅 ID/操作 两列）的 plan → `g4_chapter_planning` → `status: FAIL` 仅因 `missing_chapter_role`（用例缺 chapter_role）与 `s7_hook_ops`（无任何合法 op）；含 1 个合法 op + 非法 op（close）的 plan → `s7_hook_ops PASS`（非法 op 静默放行）；缺"优先级来源"/列名不匹配均无任何 check 输出。
- 建议方向: 按 SKILL 可检查表补齐：段 1 含"优先级来源"、章尾改变数 ≥1（第 3 章起 ≥1 advance）、hook 账四列名精确匹配、操作值白名单校验、defer+沉默≥4 的激活方案检测；与 F467 一并补 SKILL 对齐用例。

### F477 | G_RECONCILE GR.2 对 skills[skill] 非 dict 值无守卫 → AttributeError 崩溃（GR.1 有守卫、GR.2 遗漏——F465 家族第 6 实例） | error | P2
- 证据: g_reconcile.py:60（`td = skills.get(candidate_skill, {}).get(candidate_tt, {})`——skills[skill] 为 list/str 时 `.get` AttributeError）；GR.1（:36-38）有 `if not isinstance(sd, dict): continue` 守卫，GR.2 无。:33 的 jload 无 try（F421 只覆盖顶层非 dict）。
- 根因: F460/F465 内层形状家族在 GR.2 的具体遗漏——F465 枚举了 GR.1 的 `skills.items()`（skills 为 list），未枚举 GR.2 的逐技能值形状。
- 验证命令+输出: progress.json=`{"skills": {"shenbi-worldbuilding": ["not-a-dict"]}}` + t1-reports 含 `<skill>-generative-scores.json` → `gate_G_RECONCILE(rd)` → `CRASH AttributeError: 'list' object has no attribute 'get'`。
- 建议方向: GR.2 对齐 GR.1 加 `isinstance(skills.get(candidate_skill), dict)` 守卫（非 dict 记 WARN/SKIP 而非崩溃）；补 skills[skill] 非 dict 用例。

### F478 | 12 个 UNIMPLEMENTED 子检查 stub（G0.5/G7.2/7.3/7.4/7.8/GR.3/GR.4/GT.2/GT.4/GT.5/GD.2/GD.3）→ 对应门声明的部分校验目标未实现而门仍 PASS | coverage | M
- 证据: g0.py:246（G0.5 "not yet implemented"）；g7.py:109-114（G7.2 skill-traces / G7.3 t1-reports / G7.4 expected outputs / G7.8 gate_blockers）；g_reconcile.py:75-80（GR.3 cross-file hash / GR.4 agent trace）；g_transition.py:67/88/91（GT.2/4/5）；g_dispatch.py:68-69（GD.2/3）。所有 `passed()` 不因 UNIMPLEMENTED check 降级 → 门以 PASS 通过。
- 根因: 显式声明的 stub（有 note，非静默），但门通过语义未区分"全检查通过"与"含未实现检查"——G7 自称 "Round close validation" 而 4/16 子检查未实现；G_RECONCILE 自称 "filesystem consistency check" 而 GR.3/GR.4 未实现。前八轮未将 UNIMPLEMENTED 存量本身列为 finding（初审仅作覆盖率备注）。
- 验证命令+输出: `grep -rn "UNIMPLEMENTED" src/shenbi/gates/*.py` → 12 处（含 shared.unimplemented 定义 2 行）；`gate_G7(rd)` 对 checks 含 4 条 UNIMPLEMENTED 时仍返回 `status: PASS`。
- 建议方向: 将 UNIMPLEMENTED 作为显式缺口治理：要么在门状态/文档中声明"部分子检查未实现"（如 PASS-with-caveats 字段），要么排期实现 G7.3（t1-reports）/G7.8（gate_blockers，GT.3 已实现同语义）等与其它门重复覆盖的项。

### F479 | g4_worldbuilding story_bible 注释 "prose density < 5%" 与代码方向相反（代码检查 bullet_density > 5% FAIL，SKILL 要求散文） | docs | M
- 证据: worldbuilding.py:68 注释（"story_bible.md: 4 ## sections, prose density < 5%"）vs :74-83（`bullet_lines`/`total_bullets`、`bullet_density = total_bullets / total_lines`、`if bullet_density > 0.05: mf`）；skills/shenbi-worldbuilding/SKILL.md:64（"NO BULLET-POINT WORLDS — 世界观以散文形式输出"）。
- 根因: 注释措辞把"bullet density ≤ 5%（即散文 ≥95%）"误写为"prose density < 5%"（与代码/契约意图相反）。
- 验证命令+输出: read worldbuilding.py:68-85 + grep SKILL.md:64（代码行为与 SKILL 一致，仅注释漂移）。
- 建议方向: 注释改为 "bullet density ≤ 5% (prose required)"。

### F480 | g4_generic_bughunt/clean 的 `if not mf or all(not x.startswith(...))` 死条件（mf 只含本家族前缀条目，all(...) 恒假 → 条件退化为 `not mf`） | dead-wire | M
- 证据: generic.py:102（`if not mf or all(not x.startswith("G4.bh.") for x in mf)`）与 :152（clean 同型）——mf 仅追加 "G4.bh.*"/"G4.cl.*" 字符串，故 mf 非空时 `all(...)` 恒 False。
- 根因: 早期防御式写法残留；文件有任何 FAIL 时该文件的 PASS check 一律不输出（checks 为空）。
- 验证命令+输出: `g4_generic_bughunt([report.md])`（内容空）→ `status: FAIL`、`checks: []`、4 条 G4.bh.* must_fix（PASS 条目未输出；`all(...)` 分支从未生效）。
- 建议方向: 删除 `all(...)` 子条件（保留 `not mf`），或改为逐子检查输出 PASS/FAIL 明细。

### F481 | G2.meta_ratio 在 ratio>0.5 时输出两条同 id 的 WARN check（_check_meta_ratio 返回 WARN check + failures 字符串被重复追加） | error | M
- 证据: g2.py:325-329——`checks.extend(meta_checks)`（_check_meta_ratio :365-374 已追加一条 "G2.meta_ratio" WARN）+ `for f in meta_failures: checks.append({"id": "G2.meta_ratio", "s": "WARN", ...})`（:376-377 的 failures 又被逐条追加成第二条 WARN）。
- 根因: _check_meta_ratio 同时返回 check 与 failure 字符串两种表达，调用方两者都追加 → 重复条目。
- 验证命令+输出: 构造 META 区块占比 >50% 的章节 → `gate_G2([ch], 'chapter')` → checks 中 `G2.meta_ratio` 条目数 = 2。
- 建议方向: 二选一——要么 _check_meta_ratio 只返回 checks（含 WARN），要么调用方只追加 failures。

### F482 | check_chapter_title docstring 声称 "Thematic naming encouraged (1-4 Chinese characters)" 未实现 | docs | M
- 证据: chapter_drafting.py:58-66（docstring 声明主题化命名 1-4 字鼓励）vs :67-93（实现仅：第\d+章 违规、重复标题、星期/日期标签 WARN——无任何标题长度/主题化检查）。
- 根因: docstring 与实现漂移（F455 同族：声明检查未实现）。
- 验证命令+输出: read chapter_drafting.py:58-93（确认无长度/主题化逻辑）。
- 建议方向: 实现长度/主题化 WARN 或删除 docstring 该句。

### F483 | g4_chapter_drafting 主角名单硬编码阳性代词"他"且从不加"她" → 女性主角作品主角在场检查误报 FAIL | error | P2
- 证据: chapter_drafting.py:164（`if "他" not in names: names.append("他")`——只保证"他"；无"她"）；_check_protagonist_presence（:128-133）把 "他" 计入——女性主角章节正文用"她"，"他"计数 ≈0，仅靠姓名出现次数撑阈值 3。
- 根因: 代词白名单为男性单数硬编码，未考虑女性主角（"她"）；叠加 F433 的 project_root 错误后默认名单 ["林烽","他"] 对任何女性主角作品恒误报。
- 验证命令+输出: `_check_protagonist_presence('苏寒推开门。她环视四周。她走到窗前，她深吸一口气。她露出微笑。', ['苏寒','他'])` → `['G4.cd.protagonist_absent: protagonist appears 1 times (threshold: 3)']`（正文 苏寒×1+她×4，男性名单计 1）；同文本 `['苏寒','她']` → `[]`（PASS）。
- 建议方向: 按主角 frontmatter 的性别/代词字段派生名单（或同时计入"他/她/TA"），默认名单补"她"；补女性主角用例。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个 checker 逐个通读，含此前轮次已深读的 chapter_drafting/character_design/generic/g6_checks 全文）。未发现清单外遗漏文件。
- 实测基线：本轮未运行全量 pytest（沿用前七轮 785 passed 基线，本轮未修改任何文件）；新增验证命令 20+ 组（`.venv/bin/python` 只读 + /tmp 临时目录构造 + 真实项目 `novel-output/xinghuo-ranqiong` 的 volume_map.md/chapter-7/chapter-15/pending_hooks.md/genre-config.json/novel.json 实证 + 真实 SKILL.md grep）；`git status` 复查无跟踪文件污染。
- 本轮重点复核了任务点名的三处：30 个 per-skill g4 checker 最后一遍（全部 24 专用 + generic 3 分支 + score 三副本）、G0 各子检查（G0.1-G0.16/G0.cc 全部路径含 G0.5 UNIMPLEMENTED）、g6_checks/g7/g3_independence 剩余路径（含 G6.4 时间线/未来知识、G6.11 卷边界、G6.7/G6.8/G6.9/G6.12 全部分支）。
- 跨区交叉确认建议（与 prior rounds 一致）：F473/F474 的 G6.11/G6.4 在真实项目的恒定 FAIL 建议与 Z5/Z6（pipeline 区）确认 round 失败归因；F475 的 genre-config 契约（chapter_word 键缺失）建议与 Z2/Z3（contracts/skills 区）确认 schema 演进方向；F408 的 run_gate_g3 调用语义 ↔ Z5/Z6。

## 四、最终消息（复核统计）

- **新 finding：11**（F473-F483：F473/F474 为真实项目实证的恒定 FAIL（review4 已统计到 G6.11=7 / G6.4=2 但从未归因）；F475-F477/F483 为崩溃/契约/代词缺陷；F478-F482 为覆盖空洞与文案。全部实测或代码路径验证）
- **误报：0**（F400-F472 全部成立；对任务点名的 F401/F404/F408/F432/F444/F458 六个 P1 逐一读源码 + 实测反证，均无推翻证据；不准确处仅限范围不全——F431/F460/F465 家族各有 1 个新实例由 F475/F477 补充）
- **严重度异议：0 新增**（确认前几轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度按决策表核对无异议；F473/F450/F464 的"真实项目恒 FAIL"可争 P1 的理由已注明，按格式漂移类维持 P2）
- **0 新 finding 判定：否**（本轮发现 11 个前七轮漏报，未收敛到 0）
