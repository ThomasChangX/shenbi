# Z11 分区复查报告（G6 meta-audit 触发，4 处子声称/标注复查）

- 日期：2026-08-14
- 触发：`docs/superpowers/audit-runs/2026-08-14/meta-audit.md` §三-F1/F2 + §四复查清单（F1306 / F1320 / F1300 / 类别 8 标注）
- 复查方式：fresh-context 全量重读 `novel-output/xinghuo-ranqiong/` 相关产物（chapters/*.md、**/decisions.json、staging/、.superpowers/、DEBUG_USE_MANUAL_CREATE.md、pipeline-state.json）+ 独立重跑全部统计命令（python3 json 扫描 / find 计数 / grep 全文检索 / git 历史核时）
- 结论：4 项中 **4 项原声称与磁盘不符，均确认需要修正**；修正后 4 项 finding 的**主结论方向全部保持不变**

---

## 1. 复查结论（原声称 vs 实测 vs 修正）

### 1.1 F1306 — revision-decisions 无效计数与示例清单

| 维度 | 原声称（Z11.md:152） | 实测（本次独立扫描） | 修正 |
|---|---|---|---|
| 无效计数 | 34 中 7 个无效 | **34 中 10 个 parse-invalid** | 10（12/15/18/2/21/22/33/44/5/53） |
| 示例清单 | 12/15/2/21/44/**40**/18-revision 等 | 合法文件 chapter-40 被误列；漏 22/33/5/53 | 移除 chapter-40，补入 22/33/5/53 |
| chapter-40-revision-decisions.json | （列入无效示例） | **合法完整 JSON**：`json.loads` 通过，7 键全齐（$schema=shenbi-decisions-v1 / skill=shenbi-chapter-revision / chapter=40 / produced_at / selections×1 / adjustments×5 / budget），2622 字节 | 判定为合法文件 |
| 失败模式 | （未细分） | 12/2/44 = 空文件（Expecting value）；15/21 = 语法破损（Expecting ','）；18 = control char；22/33/5/53 = Extra data（JSON+markdown 尾缀） | 10 个无效文件横跨 F1304 分类的 3 个失败类别（other 5 / control_char 1 / extra_data 4） |

**机制归因修正**：原声称"兜底写入（`_ensure_revision_decisions_exists`）的 minimal 文件违反 schema"。实测不支持该归因——(a) 磁盘 10 个无效文件的签名（空文件/LLM 摘要文本拼接/` ``` `markdown 围栏/控制字符）与 **F1303/F1304 手动写路径家族一致**，而非 minimal 模板（minimal 模板为可解析 JSON）；(b) 现代码 minimal 模板（chapter_loop.py:1968-1984）缺 `budget` 键，但 **decisions-schema.md:56 标注 budget `Required: no`**，故"缺 budget 违反 schema"本身不成立；(c) git 历史显示 `_ensure_revision_decisions_exists` 于 **dd1fc62（2026-07-20）加入，晚于运行窗口（07-15~17）**，运行期该兜底根本不存在，不可能产出运行期文件。

### 1.2 F1320 — staging 计数

| 维度 | 原声称（Z11.md:235） | 实测（本次） | 修正 |
|---|---|---|---|
| staging 文件数 | 121（文档称 119，因含 2 个 lockfile/多余文件） | **恰 119**（plans 111 + truth 8） | 119，与 DEBUG 文档一致 |
| lockfile | 声称含 2 个 lockfile | **staging 内 0 lockfile、0 隐藏文件**（`find staging` 122 条目 = 119 文件 + 3 目录） | 无 lockfile 解释不成立 |
| 121 的来源 | （未解释） | 119 文件 + 2 子目录（plans/truth）计入 = 121；`find staging` 全条目 = 122 | 121 系把子目录计入的伪计数 |

注：`pipeline-state.json.lockfile` 存在于 xinghuo-ranqiong **顶层**（非 staging 内），是 Z11 "lockfile" 说法的可能混淆源，但与 staging 计数无关。

### 1.3 F1300 — grep "山风裹着铁锈味" 命中文件

| 维度 | 原声称（Z11.md:118） | 实测（本次 `grep -rl`） | 修正 |
|---|---|---|---|
| 命中文件 | 仅 `audits/chapter-2-continuity.md` | **2 个产物文件**：`audits/chapter-2-continuity.md`（:9 场景时间表引用）+ `audits/chapter-2-resonance.md`（:21 感官锚定引用） | 2 文件（+ Z11.md/meta-audit.md 报告自身引用） |

两处均为审计引用正文残留，**chapters/ 内 0 命中**——"正文残留只在审计引用中"的结论不变（甚至增强：2 处引用而非 1 处）。

### 1.4 类别 8 计数标注（.superpowers/sdd）

| 维度 | 原声称（Z11.md:22） | 实测（本次） | 修正 |
|---|---|---|---|
| sdd 计数 | `.superpowers/sdd/*`（15） | sdd 单独 **10 个文件**（9 个 .md：audit-T1..T7 + progress + spec-deviations，+ 1 个 .gitignore） | sdd 单独 10（.md 9） |
| archive 计数 | sdd-archive-issue24/*（5） | 5 ✓（audit-T1..T3 + progress + spec-deviations） | 5 ✓ |
| 合计 | （标注为 sdd=15） | **两目录合计 15**（10 + 5） | "15" 是两目录合计，非 sdd 单独 |

代码修复核实（finish_reason/executor_config/pricing/ledger/parallel_dispatch/_shared/deepseek-v4-pro）全部通过——类别 8 无 findings 的结论不变，仅计数标注修正。

---

## 2. 修正后的 finding 表述

### F1306（修正版）| 10/34 revision-decisions parse-invalid，签名指向 F1303/F1304 手动写路径（非兜底模板）| 边界/漂移 | **P2（维持）**

- 证据: `python3` 全量扫描 34 个 `*revision-decisions.json` → **10 个 parse-invalid**（chapter-12/15/18/2/21/22/33/44/5/53）；失败模式横跨 other 5（空文件/语法破损）/ control_char 1（chapter-18）/ extra_data 4（22/33/5/53，JSON+markdown 尾缀）；`chapter-40-revision-decisions.json` 为**合法完整 JSON**（7 键全齐，2622B，json.loads 通过），从无效清单移除
- 根因修正: 无效文件签名与 F1303/F1304 手动写路径（LLM 摘要文本拼接落盘）一致，**非** `_ensure_revision_decisions_exists` 兜底模板（该模板产可解析 JSON；budget 按 decisions-schema.md:56 为可选键；且函数 dd1fc62/2026-07-20 才加入，晚于运行窗口）
- 验证命令+输出: `python3` 逐文件 json.loads → 24 valid / 10 invalid；`read chapter_loop.py:1968-1984`（minimal 模板）；`grep budget docs/framework/decisions-schema.md` → `Required: no`；`git log -S _ensure_revision_decisions_exists` → dd1fc62（07-20）
- 建议方向: 无效 revision-decisions 并入 F1304 修复链（写路径前置校验 + G4 纳入 .json）；F1306 不再单独归因兜底模板

### F1320（修正版）| DEBUG_USE_MANUAL_CREATE.md 计数漂移（总 1229 vs 1226；marker 22 vs 21）；staging 无漂移（119=119）| 文案 | **M（维持）**

- 证据: `python3`/`find` 全树计数 → 总 **1229**（文档称 1226）；gate-markers **22**（文档称 21，差额=F1312 污染 marker）；snapshots .md **51**（+manifest.json=52，文档称 52 ✓）；**staging 恰 119**（plans 111 + truth 8，0 lockfile）——staging 与文档一致，**非**漂移项；总差额 1229-1226=+3 来自污染 marker +1 与顶层多余文件（含 pipeline-state.json.lockfile 等 9 个顶层文件）
- 根因修正: 文档为人工/运行中快照未随产物同步（原述成立）；"staging 121 因含 2 lockfile" 为伪计数（119+2 子目录），从漂移证据中移除
- 验证命令+输出: `find staging -type f | wc -l` → 119；`find staging -name "*.lockfile"` → 空；`find . -type f | wc -l` → 1229；`ls gate-markers | wc -l` → 22
- 建议方向: 维持 M；文档标注"仅供参考"或归档（原述不变）

### F1300（细节修正，主结论不动）| 5 章正文被 revision 摘要整体覆写 | 数据损坏 | **P0（维持）**

- 证据: chapter-2.md（1035B）/chapter-9.md（642B）/chapter-12.md（1506B）/chapter-44.md（1229B）/chapter-55.md（104B）均为 revision 摘要文本（本次重读逐文件 head 确认）；`grep -rl "山风裹着铁锈味"` → **2 个文件**：`audits/chapter-2-continuity.md` + `audits/chapter-2-resonance.md`（原报告只列前者）——正文残留只在审计引用中，chapters/ 0 命中
- 结论方向: **不变**（P0 保持；修正仅使引用证据 +1）
- 验证命令+输出: 本次 `grep -rl` 全库 → 2 产物文件 + 2 报告文件；逐文件 head 5 个退化章节字节数与 Z11 一致
- 建议方向: 维持 F1300 原建议（正文不可恢复，spec 记录不可逆损失）

### 类别 8（修正标注，无 finding）| .superpowers/sdd 计数标注

- 原标注: `.superpowers/sdd/*（15）`；修正: **sdd 单独 10 文件（9 .md + 1 .gitignore）+ sdd-archive-issue24 5 文件 = 两目录合计 15**；"15" 为合计值
- 结论: 无 findings 不变（代码修复核实全部通过）

---

## 3. 主结论方向判定

| Finding | 修正内容 | 主结论方向 | 判定 |
|---|---|---|---|
| F1306 | 无效计数 7→10；示例清单剔除 chapter-40、补 22/33/5/53；根因从"兜底模板违反 schema"修正为"F1303/F1304 手动写路径家族" | "revision-decisions 大量无效" | **保持**（10/34=29.4%，比原 7/34 更强；P2 维持） |
| F1320 | staging 121→119（无 lockfile）；漂移证据剔除 staging | "存在计数漂移" | **保持**（总 1229 vs 1226、marker 22 vs 21 仍成立；M 维持） |
| F1300 | grep 命中 1→2 文件（+chapter-2-resonance.md） | "5 章被 revision 摘要覆写" | **保持**（P0 维持；细节修正） |
| 类别 8 | sdd 15→10（合计 15）标注修正 | "无 findings" | **保持** |

4/4 主结论方向不变；F1306 修正后证据强度上升（10>7），F1320 修正后漂移证据减少一项（staging）但整体漂移断言仍成立。

---

## 4. 覆盖空洞声明（重读范围）

本次复查全量重读/重算以下范围，无空洞：

- `novel-output/xinghuo-ranqiong/chapters/*.md`（56）— 全量 `python3` META/头部扫描复跑 + 5 个退化章节逐文件 head（字节数 1035/642/1506/1229/104 复现）
- `chapters/*decisions.json`（89）+ `staging/plans/*decisions.json`（55）+ `staging/truth/*decisions.json`（1）= **145 全量** json.loads 分类（复现 62 valid / 67 extra_data / 14 other / 2 control_char）；其中 **34 个 revision-decisions 单独全量**重扫（24 valid / 10 invalid），chapter-40-revision-decisions.json 逐键核对
- `staging/`（plans 111 + truth 8 + 3 目录）— `find` 全条目/文件/隐藏/lockfile 三路计数
- `.superpowers/sdd`（10）+ `sdd-archive-issue24`（5）— 全量 `ls`/`find`
- 全文检索 `grep -rl "山风裹着铁锈味"` 全库（.md）
- 辅助核时：`git log`（dd1fc62 兜底函数加入时间、d4b4e83 修复时间）、`docs/framework/decisions-schema.md`（budget Required 列）、`src/shenbi/pipeline/chapter_loop.py:1936-1984`（minimal 模板）
- 未复跑项（超出 4 项复查范围，引用 G6 meta-audit §三结论）：Z11 其余 17 个 finding（F1301-F1305/F1307-F1319/F1321）与类别级主条目（56 章/98 META/0 头/722 audits/13 维/ch56 7 文件/marker 22/snapshots 51/总 1229 等）——meta-audit 已逐项复现成立，本次不重复

---

## 5. 汇总

- 复查 4 项：原声称不符 **4/4**，全部确认需修正
- 修正后主结论保持 **4/4**（F1306/F1320/F1300 方向不变；类别 8 无 findings 不变）
- 修正净效果：F1306 无效计数上调（7→10）且根因归因转向 F1304 家族（证据增强）；F1320 staging 子项从漂移证据剔除（121→119，无 lockfile）；F1300 grep 引用证据 +1；类别 8 标注改为"两目录合计 15，sdd 单独 10"
- 与 G6 meta-audit §三-F1/F2 判定**完全一致**（计数 10、staging 119、grep 2 文件、sdd 9-10），并新增两条 meta-audit 未展开的实证：(a) F1306 根因归因（兜底模板）不成立——budget 为可选键 + 函数晚于运行窗口加入；(b) "121" 的成因 = 119 文件 + 2 子目录伪计数
