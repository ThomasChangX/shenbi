# Z2 分区第十四轮独立复核报告（Z2.review14）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）+ `Z2.review9.md`（F253-F255）+ `Z2.review10.md`（F256-F257）+ `Z2.review11.md`（F258）+ `Z2.review12.md`（F259-F261）+ `Z2.review13.md`（F262）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，逐文件重读，未依赖前十三轮转述）；另读跨区消费面验证裁决（audit/{_shared,snapshot,write_audit,record}.py、gates/{cli,shared,g0,g0_skill_contract,g2,g5,g_reconcile}.py、scoring.py、safe_write.py、pipeline/{dispatch_helper,truth_io,chapter_loop,closure,genesis,triggers}.py、records/parser.py、tests/round-exec.sh、first-novel.md、skills/*/SKILL.md 契约段、tests/fixtures/genre-config-example.json、git show 99f91f0:tests/dispatch-subagent.sh）
- 模式: 只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）。git log/status 确认 Z2 源码零改动（工作树无跟踪修改）。
2. **独立验证方式**：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**；15+ 个 `python -c`/heredoc 运行时探针（F216/F218/F235/F247 机制复现、F214 伪造点复证、F227 门序复证、F229/F239/F255 file_type 枚举、fixture 有效性、新候选 F263-F266 证据）。
3. **前十三轮判定**：F201-F262 共 **62 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均复核通过。6 条 P1（F214/F216/F218/F227/F235/F247）经**独立反证尝试**全部维持（见第三节）。**本轮不新增严重度异议**（对既有 8 条异议全部独立支持）。
4. **本轮新发现 4 条（F263-F266）**，集中于十三轮都未触及的两类盲区：
   - **契约加载/过滤层的极细碎边界**：`read_frontmatter_contract` 的 `split("---", 2)` 不以行首锚定（frontmatter 含 "---" 子串时静默截断字段或 loud 误报）（F263，M）；`extract_h2_sections` 重复 H2 标题折叠 → 同标题多段内容在字段过滤时静默丢首段（F264，M）；
   - **审计/契约语义层的未覆盖孪生**：`_diff_records` 的 None 哨兵使 null 值新增键旁路 record_field 键集校验（F260 的 record 层孪生，根因 Z5）（F265，M）；**write_semantics 校验面完全未触及**——`mode` 值自由形式无词表校验（拼写错误静默通过）、`append_dedup`/`merge_prose` 声明无框架实现（dispatch_helper 显式 NOT branched、无 g4 checker）、且与 truth 模板 `update_mode:` 词表双轨并行（F266，P2）。

---

## 一、复核结论表（F201-F262 判定）

| 编号 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|
| F201 | **成立，P1 维持** | 三模型（chapter_planning/context_composing/volume_outlining）无生产消费者（grep 仅 test_skill_integration.py:188 类名文本）；g4 私有实现规则不同（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）。cov 34/38/40% 复测吻合。 |
| F202 | **成立** | registry.py:40-63 REGISTRY/_discover_skill_models/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | **成立** | deps.py:56-78 DepsDoc/phase_of 无生产消费者；sync_contracts.py:195 仍 json.loads（read 复证）。 |
| F204 | **成立（附注）** | novel.py NovelConfig 无生产 import；g6.py:51 与 g4/worldbuilding.py 以 raw dict 读 target_word_count/target_words——死的是模型非行为。 |
| F205 | **成立** | schemas/scores.py 无生产消费者（grep 复证；g4/score_*.py 用 RouteC/RouteA 正则）。 |
| F206 | **成立** | schemas/state.py ProgressDoc/SummaryDoc 无加载点（g3.py 用 jload 直读 dict）。 |
| F207 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | **成立（增补成立）** | enums.py Severity/Verdict 无生产 import（生产仅 CPZone/ActorRole 有消费）；裸字面量散落点 + chapter_loop.py:1797、:409-438 复核通过。 |
| F209 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有）；GateOutcome/PureInput 无生产消费者（过渡类型自述 base.py:4-5）。 |
| F210 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94，Δ32-40。 |
| F211 | **成立（表述修正维持）** | executor.py:167-188 cov missing（74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0 → 修复方向按"CLI 崩溃"修正，本体成立。 |
| F213 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | **成立，P1 维持** | codex.py:20-48 不写 current_scorer_agent；dispatch_helper.py:1934-1945 run_gate_g3 在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行。**反证尝试**（独立路径不跑 G3 故 P2？）不成立——run_gate_g3 在 chapter_loop.py:2909 / closure.py:269 / genesis.py:345 / triggers.py:605 被调用（pipeline 正常路径；API/IDE 派发不写 progress.json → 伪造为常态）。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。 |
| F215 | **成立** | codex.py:75 `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（read 复证）。 |
| F216 | **成立，P1 维持** | **运行时复现**：空 customRules + texture:False 禁用维度 → `GenreConfig.model_validate` PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。**反证尝试**（空规则无真实触发面）不成立——新轮次 stub customRules 可空。 |
| F217 | **成立；支持低置信异议（P2→P1）维持 P2** | SKILL.md 内 7 处 CONSTELLATION 区间 + docstring 20-30 + 模型 [15,35]；模型对标准卷过松（25-35 过门违反 :91 上限）对开卷过严（36-40 误拒，:193 允许 30-40）。权威区间未定，维持低置信。 |
| F218 | **成立，P1 维持** | **运行时复现**：部分匹配（声明 ["当前任务","不存在的字段"]，文件 3 节）→ matched=True、仅命中 "当前任务" 节、无 WARN；全缺 → 全文 + `field_filter_no_match` WARN（fields.py:55-64）。AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 建议成立。 |
| F219 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，维持 P2。 |
| F221 | **成立** | **运行时复现**：fixture 9 键含 tropeInventory，`GenreConfig.model_validate` PASS 且 tropeInventory 被静默丢弃（extra=ignore）；SKILL.md:286/:292 声称 8 键；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段 + docstring "8 camelCase keys"。四源漂移确认。 |
| F222 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP。 |
| F225 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except。 |
| F227 | **成立，P1 维持** | **本轮复证**：round-exec.sh create-mode 只建 progress/meta/summary + 子目录（**无 novel.json**）；first-novel.md:38-41 文档化流程 `shenbi-dispatch shenbi-worldbuilding generative <round_dir> "<prompt>"` 第一步 G1.1 novel.json not found → rc=1；dispatch()（executor.py:190-214）在 dispatch_codex（技能执行处）之前跑 G1/G2；`git show 99f91f0` 原 shell 注释 "the skill execution happened earlier" 证实门序回归。反证不成立。 |
| F228 | **成立；支持低置信异议（P2→P1）维持 P2** | derive_output_files（audit/_shared.py:38-56）只 resolve_or_skip、不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | **成立（范围低估但严重度维持）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现；g2.py:83-84 decisions 分支对非 .json `continue`（:164 连 G2.4-G2.12 一并跳过）+ executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | **成立** | **运行时复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`（无规范化）。 |
| F231 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者。 |
| F232 | **成立** | **运行时复现**：`Selection(manual_override, rationale="")` → ACCEPTED；`Adjustment(rationale="   ")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | **成立** | snapshot.py:99-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | **成立，P1 维持** | **机制复证**：`PROJECT_DIR = REPO_ROOT`（executor.py:26）；worldbuilding watch 含 `truth/*.md` → `_expand_patterns`（snapshot.py:32-48）展开仓库根 tracked `truth/bridge_tracker.md`/`truth/character_matrix.md`（git ls-files 确认）→ `_matches_declared("truth/bridge_tracker.md", ["truth/*.md"], globs)` 失败（F236）→ 零写入派发也 `未声明写入` → rc=2；round_dir 真实写入不被审计。codex.py:62 cwd 已改 round_dir 而审计根未跟随；test_executor_audit.py:18,40 monkeypatch `ex.PROJECT_DIR = tmp_path` 编码了正确语义。反证不成立。 |
| F236 | **成立** | **运行时复现**：`_matches_declared("truth/bridge_tracker.md", ["truth/*.md"], parametric_globs())` → False（直接 fnmatch → True）；`"truth/*.md" in parametric_globs()` → False（registry glob 非 parametric）。 |
| F237 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | **成立；支持 review6 低置信异议（P2→P1）维持 P2** | audit_writes（write_audit.py:52-56）对每个 post .md 无 try 地 parse_records；records/parser.py:40-44 yaml.safe_load 可抛 ScannerError/非 list 抛 ValueError；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | **成立** | **运行时复现**：derive_file_type worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归；executor.py:81-85 精确字符串交集）；state-settling → "truth"、chapter-drafting → "decisions"。 |
| F240 | **成立** | **运行时复现**：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证）。 |
| F242 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`（get_ownership → None）；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"（grep 复证）。 |
| F243 | **成立（机制修正维持）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。机制修正（review8）维持：gate-marker 强制 exit 3 先于分数校验 exit 2（scoring.py:364-377 vs :422-432，本轮 read 复证）。 |
| F244 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak（不存在则静默无检查）；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | **成立** | **运行时复现**：resolve_chapter_path("audits/volume-N-payoff.md", 7) → `audits/volume-7-payoff.md`；arc-N+7 → `arc-7.md`；AC-NNN+7 → `AC-007.md`；resolve_or_skip(volume-N, None) → None；resolve_volume_path 仅 closure.py:20,158 消费（grep 复证）。 |
| F246 | **成立** | dispatch_skill（dispatch_helper.py:1864）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径；rd = round_dir or project_dir）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | **成立，P1 维持（范围注记维持）** | **运行时复现**：`_declared_patterns("shenbi-chapter-drafting") == []` vs `_audit_watch_paths("shenbi-chapter-drafting", 7) == ['chapters/chapter-7.md','chapters/chapter-7-decisions.json']` → N 写技能成功派发恒判「未声明写入」rc=2。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | **成立** | PacingDesign.from_markdown（pacing_design.py:77-130）chapter_sequence 恒空（:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）致误拒/虚增（read 复证）。 |
| F249 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500（read 复证）。 |
| F250 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 pipeline dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | **成立；支持 review9 低置信异议（P2→P1）维持 P2** | 命名面：codex.py:56 `-scores-subagent.json` vs find_report（shared.py:157-166）三模式 / g0.py:440 G0.10 glob `*-generative-scores.json`（本轮 read 复证）；内容面：codex.py:87 落盘 raw 维度 dict（`{"1":90,"2":95}`），完整报告（含 final_score）仅走 stdout → 命名修正后 G5.1（g5.py:68 `rdata.get("final_score", rdata.get("score", 0))`）仍读 0；状态面：codex.py:44 "done" vs g_reconcile.py:40,61 "DONE" → GR.2 恒 FAIL。 |
| F252 | **成立** | **本轮 read 复证**：scoring.py:364-377 marker 强制 + :187-224 check_gate_markers（t1-skill 分支要求 `G4-{skill}-{test_type}.json`）→ exit 3 MARKER_MISSING；codex.py:95-110 调 shenbi-score 不传 `--tier` → marker 检查必然执行；executor 编排（:190-214）只跑 G1/G2 无 G4；grep write_gate_marker dispatcher/ → 空（gates/cli.py:121 仅 G4/G6 CLI 分支写 marker）。 |
| F253 | **成立** | codex.py:27-48 `_record_completion` 读-改-写整体无锁（safe_write.py:40-59 `_acquire_lock` flock 仅覆盖写入周期，读在锁外）；pipeline legacy 并行（parallel_dispatch.py:168-171 Semaphore(4) 共享 project_dir）触发面真实。 |
| F254 | **成立** | run_g1/run_g2（executor.py:114-118/:125-137）与 shenbi-score 子进程（codex.py:95-109）均无 timeout；仅 codex exec 有 600s（codex.py:63）；`grep -n timeout src/shenbi/dispatcher/` → 仅 codex.py:63。 |
| F255 | **成立** | derive_file_type 对 4 个 report-kind truth 写者（score-volume/score-arc/score-stratum/review-arc-payoff）全 → "report"（executor.py:77-78 REPORT 分支先于 truth 交集，本轮复现）→ G2.11（g2.py:290 仅 file_type=="truth"）对其静默失效。 |
| F256 | **成立** | g2.py:273/:315 注释声明 "chapter files only"，G2.10（:274-287）/G2.12（:316-322）在 `if file_type == "chapter":` 块（:218-271）之外无条件执行——truth/report/json 全部受影响；句末符集合（:317）缺 `}`/`]` → 误归 chapter 的 JSON 恒 WARN（read 复证）。 |
| F257 | **成立** | executor.py:156 `def dispatch(..., *, chapter: int | None = None)` 全仓零调用方（grep 仅 cli.py:29、executor.py:250，均不传）；dispatch_with_write_audit（:242）另行独立二次 extract_chapter。 |
| F258 | **成立** | `extract_chapter("Now that chapter 6 is complete, draft chapter 7")` → 6（首匹配）；单引用 → 7。pipeline 生成 prompt 均为单章节引用，仅 standalone 手工 prompt 触发。 |
| F259 | **成立** | `extract_chapter("draft chapter 0")` → 0；`resolve_chapter_path('chapters/chapter-NNN.md', 0)` → `chapters/chapter-000.md`；对照 chapter_planning.py:22 `Field(ge=1)`。 |
| F260 | **成立** | `_changed_top_keys('{"version":"1"}', '{"version":"1","hacked":null}')` → `()`（旁路）；对照 `{"hacked": true}` → `('hacked',)`。snapshot.py:63-76 `a.get(k) != b.get(k)` None 哨兵不可区分"缺席"与"null 值"。根因在 Z5，被 Z2 executor 审计链消费。 |
| F261 | **成立** | `Selection(arc_relevance+low, rationale="x"*150)` → `Value error, rationale exceeds 100 chars`（应为 FORBIDDEN）；`grep -c VALID_BASIS decisions.py` → 1（仅定义）。decisions.py:11-12 死常量 + `_p25` 错误优先级（:34 长度检查先于 :41-42 FORBIDDEN）。 |
| F262 | **成立** | 本轮复证 book_strata/arc-N 部分匹配 → matched=True、`未解决悬置` section 静默丢失（F218 机制实锤）；style_profile/volume_map/pending_hooks/current_state/chapter_summaries 全漏 → 恒 escape hatch；`match_field('cross-volume bridges','跨卷桥接')` → False、`match_field('cross-volume bridges','Cross-Volume Bridges')` → False。 |

**误报统计**：整体误报 **0 条**（F201-F262 共 62 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F263-F266）

### F263 | `read_frontmatter_contract` 的 `split("---", 2)` 不以行首锚定：frontmatter 内出现 "---" 子串（描述/示例文本）时截断 → 静默字段截断或 loud 误报；g0_skill_contract 存在同款孪生 | 漏报（边界解析缺陷，契约加载层） | M
- 证据: `src/shenbi/contracts/legacy.py:134-135`（`parts = text.split("---", 2)`——对**任意位置**的 "---" 子串切分，非行首锚定）；`src/shenbi/gates/g0_skill_contract.py:71`（同款 `text.split("---", 2)` 孪生）；`read_frontmatter_contract`（legacy.py:130-146）把 `parts[1]` 当 YAML 解析
- 根因: frontmatter 分隔符判定使用裸子串切分而非行锚定（应如 `re.match(r'^---\s*$', line)` 逐行判定）。若技能 frontmatter 的 description/示例内含 "---"：(a) 落在**未加引号标量**中（如 `note: a---b`）→ parts[1] 截断后仍是合法 YAML → **静默**截掉 "---" 之后内容（实测 `note: a` 保留、`---b` 丢弃；contract 块若在截断点之前则完好 → 契约层加载"成功"但 frontmatter 字段损坏）；(b) 落在**加引号标量**中 → yaml ScannerError → `read_frontmatter_contract` 抛 ContractError "frontmatter malformed YAML"（loud，阻塞技能契约加载 → G0/derive_file_type 降级链）。
- 验证命令: `python -c "import yaml; raw='---\\ncontract:\\n  kind: artifact\\nnote: a---b\\n---\\nbody'; p=raw.split('---',2); print(repr(p[1])); print(yaml.safe_load(p[1]))"` → `'\ncontract:\n  kind: artifact\nnote: a'` / `{'contract': {'kind': 'artifact'}, 'note': 'a'}`（"---b" 静默丢失）；加引号变体 → `yaml.scanner.ScannerError`；`grep -l '"---"\|---' skills/*/SKILL.md | head` 复核当前 0 触发（sync 加载 72 契约全通过）
- 建议方向: 行锚定分隔符（`text.splitlines()` 逐行找 `^---$` 作为边界）；或至少对切分结果做完整性校验（parts[1] 重新拼接后与原 text 前缀一致）；与 g0_skill_contract.py:71 孪生一并修复

### F264 | `extract_h2_sections` 重复 H2 标题折叠：同标题多段内容在字段过滤时静默丢首段（dict 键覆盖），与 F218 的"未匹配丢弃"机制不同 | 漏报（边界数据丢失，字段过滤层） | M
- 证据: `src/shenbi/contracts/fields.py:38-52`（`sections[current_heading] = ...`——以标题为 dict 键，重复标题后者覆盖前者）；`_filter_md`（:55-64）命中匹配字段时只输出 `sections` 中内容
- 根因: 结构上 `sections` 用 heading 做键假设标题唯一；producer（LLM）输出含重复 `## X` 标题（分段陈述同一主题）时，命中字段 X 的输出只保留**最后**一段，首段内容静默丢失且无任何 WARN——与 F218（未声明字段被丢弃）互补但机制独立（本条是**命中**字段的内容折叠）。
- 验证命令: `python -c "from shenbi.contracts.fields import extract_h2_sections; print(extract_h2_sections('## 当前任务\\nfirst\\n\\n## 当前任务\\nsecond'))"` → `{'当前任务': 'second'}`（first 丢失）；`filter_to_fields` 同构输出仅含 second
- 建议方向: `extract_h2_sections` 对重复标题合并（拼接 body）或保留多段（list 值）；至少在 `_filter_md` 检测到重复标题时 WARN；补重复标题测试

### F265 | `_diff_records` 的 None 哨兵使 null 值新增键旁路 record_field 键集校验（F260 的 record 层孪生）：track/resolve/state-settling 对 null 值键写入零审计 | 漏报（审计盲区，根因 Z5 audit/snapshot.py） | M
- 证据: `src/shenbi/audit/snapshot.py:87-91`（`_diff_records`：`if pre_by[rid].get(k) != rec.get(k)`——键缺席与值为 null 均得 None，`pre.get(k)=None == rec.get(k)=None` → 不入变更集）；`src/shenbi/contracts/ownership.py:125-135`（record_field 分支只查 `modified_record_keys` ⊆ write_keys）；消费链 executor.py:244-268（snapshot_tree→compute_file_change→check_write_ownership）
- 根因: 与 F260（`_changed_top_keys` JSON 顶层键）同一 None 哨兵根因，但发生在 **markdown 记录 diff**（`_diff_records`）：record_field 级 OWNERSHIP（track/resolve/state-settling 的 pending_hooks.md）对"新增 null 值键"的写变更不可见 → `modified_record_keys` 空 → 键集越权检查旁路（如对 hook 记录加 `hacked: null` 键零违规）。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c "from shenbi.audit.snapshot import _diff_records; pre=[{'id':'h1','state':'PLANTED','content':'x'}]; post=[{'id':'h1','state':'PLANTED','content':'x','hacked':None}]; print(_diff_records(pre,post))"` → `((), (), ())`（旁路）；对照 `post[0]['hacked']='y'` → `(('h1', frozenset({'hacked'})),)`（正常检测）
- 建议方向: `_diff_records` 用键存在性判断（`(k in pre) != (k in post) or pre.get(k) != rec.get(k)`）；与 F260 同源修复，补 null 值记录键用例

### F266 | write_semantics 校验面整体未接线：`mode` 值自由形式无词表校验（拼写错误静默通过）、`append_dedup`/`merge_prose` 声明无框架实现（dispatch_helper 显式 NOT branched、g4 无 checker）、且与 truth 模板 `update_mode:` 词表双轨并行 | 漏报（契约声明↔执行漂移，Z2 根因 legacy.py） | P2
- 证据:
  - 值校验缺失: `src/shenbi/contracts/legacy.py:85-87`（`meta = {k: v for k, v in item.items() if k != "file"}`——mode/no_op_behavior/key 任意值接受，无词表/结构校验）；`src/shenbi/gates/g0_skill_contract.py:134`（`if "mode" not in meta`——只查 presence 不查值）；实测 `{"file": "truth/pending_hooks.md", "mode": "appand_dedup"}` 通过 `_normalize_write_item`（无异常）
  - 语义未实现: `src/shenbi/pipeline/dispatch_helper.py:1058-1067`（"For all declared modes — including append_dedup — it writes the whole file via safe_write. Truth-file append/upsert (mode: append_dedup) is NOT routed here"）、`:1165`（"append_dedup is intentionally NOT branched here"）；`merge_prose` 仅 docstring 声称 "enforced by G4 / the caller"（:1067）——grep gates/g4/ 无 merge_prose checker（本轮复证）；`create_or_overwrite` 全仓零消费者（grep 空）
  - 词表双轨: contract `mode:`（create_or_overwrite×100 / append_dedup×18 / merge_prose×4，skills 枚举实测）vs SKILL.md frontmatter `update_mode:`（replace / upsert_yaml / upsert_markdown_row，state-settling 声明，被 pipeline/truth_io.py:81 消费）——两个平行 truth 写语义词表
- 根因: write_semantics（legacy.py:51 Contract 字段）是 Contract TypedDict 中**唯一被生产消费**（g0_skill_contract + dispatch_helper:1082/1129）的语义字段，但其值面从未进过校验面（对比 reads/writes/updates 有 registry resolves 校验、kind 有 OutputKind 枚举）——前十三轮对契约层的所有校验审查（F201-F262）均未触及该字段；且框架写路径对 mode 语义的消费停留在"记录日志/检查 presence"，声明行为（append/merge）依赖技能自实现，声明与实际写入漂移不可检测。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c "from shenbi.contracts.legacy import _normalize_write_item; print(_normalize_write_item({'file':'truth/pending_hooks.md','mode':'appand_dedup'},'updates','x'))"` → `({'mode': 'appand_dedup'},)`（拼写错误静默通过）；`grep -n 'merge_prose\|append_dedup' src/shenbi/gates/g4/*.py` → 空；`grep -rn 'create_or_overwrite' src/shenbi/` → 空；`grep -rln 'append_dedup' skills/*/SKILL.md | wc -l` → 11；`grep -rln 'mode: merge_prose' skills/*/SKILL.md` → anti-detect/chapter-revision/length-normalizing/style-polishing（4）
- 建议方向: (a) legacy.py 增加 mode 词表枚举校验（create_or_overwrite/append_dedup/merge_prose + no_op_behavior: skip_write + key 类型），拼写错误在契约加载期 loud；(b) 裁定 mode 语义归属：要么框架写路径实现 append_dedup（truth_io 接续），要么文档明示"mode 仅供 G0.16 验证、技能自实现"并删冗余声明；(c) 统一 contract `mode:` 与 `update_mode:` 词表（或文档化两词表各自适用范围）；(d) 补 write_semantics 值校验测试

### M 级观察（随文记录，均非 finding）
- `fields.py:34-35` `match_field` 精确相等 + 无大小写折叠（F262 机制面；review13 已列，本轮复证英文 field 声明零命中）
- `executor.py:75` derive_file_type 的 EPHEMERAL 分支死代码（review6 已列，本轮枚举复证 69 技能无 kind=ephemeral）
- `legacy.py:85-87` 同 F266：write_semantics 的 dict-form 条目若含 "file" 外的任意键（含拼写错误的 `no_op_behaviour`）均静默收入 meta（F266 机制面）
- `snapshot.py:103-104` pre==post 报 status="modified"（review4 已列，语义误导但无功能影响，本轮复读通过）
- `g0_skill_contract.py:71` 与 legacy.py:134 同款 `split("---", 2)` 孪生（F263 增补站点）

---

## 三、严重度异议（不修改前十三轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1（已入账）**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。本轮反证尝试（独立路径不跑 G3 故 P2？）不成立——run_gate_g3 在 chapter_loop.py:2909 / closure.py:269 / genesis.py:345 / triggers.py:605 被调用（pipeline 正常路径），API/IDE 派发不写 progress.json → 伪造为常态。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。
2. **支持 F218 P2 → P1（已入账）**：AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 在部分缺失时两处均不满足（fields.py:55-64 本轮复现：matched=True、仅命中 section、无 WARN）；F262 提供 book_strata/arc-N 真实数据形状实例。truth 文件随剧情演进缺 section 属正常路径。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——SKILL.md 内部 7 处自相矛盾，权威区间未定，维持低置信。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内，使 record_audit_outcome 永不执行（审计记录丢失）——"崩溃仍审计"保证（executor.py:245-246）失效；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（chapter-drafting/context-composing/chapter-revision 等）的 .md 散文在 G2 被 continue 整体跳过，G2.6/G2.8/G2.9 对章节正文静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F251 P2 → P1（低置信维持）**：评分产物三重不匹配使 standalone 轮次对 G5.1（no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL）确定性失败；受 F252（exit 3 前置）掩盖维持低置信。
8. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。
9. **F266 严重度自评说明（非异议）**：按 P2 报告（契约声明↔执行漂移 + 校验洞，F203-F207 死线家族同权）；其实际触发需技能声明拼写错误或依赖未实现的 append/merge 语义（当前技能均自实现），故不升 P1。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十三轮一致）。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F218 部分匹配 → matched=True、仅命中 section、无 WARN；全缺 → 全文 + WARN
  - F235 审计链机制：PROJECT_DIR=REPO_ROOT + watch glob 展开命中仓库根 tracked truth 文件 + `_matches_declared` 失败 → rc=2 语义复证
  - F247 `_declared_patterns`=[] vs `_audit_watch_paths(chapter-drafting, 7)`=2 项（chapter-7.md/decisions.json）
  - F214 run_gate_g3 伪造点 read 复证 + 调用点枚举（chapter_loop/closure/genesis/triggers）
  - F227 round-exec.sh create-mode 产物清单（无 novel.json）+ first-novel.md:41 文档化流程 + 原 shell "the skill execution happened earlier"
  - F229/F239/F255 derive_file_type 枚举：worldbuilding→chapter、chapter-drafting→decisions、state-settling→truth、score-volume→report
  - F221 fixture 9 键 model_validate PASS + tropeInventory 静默丢弃；F210 hooks 行号；F217 SKILL.md 7 处区间
  - **F263（新）** `split("---", 2)` 非行锚定：未加引号标量内 "---" → 静默字段截断（note: a---b → note: a）；加引号 → ScannerError；当前 0 技能触发
  - **F264（新）** extract_h2_sections 重复标题 → `{'当前任务': 'second'}`（first 丢失）
  - **F265（新）** `_diff_records` null 值新增键 → `((), (), ())`（旁路）；真值键 → 检测
  - **F266（新）** `_normalize_write_item` 拼写错误 mode 通过；grep gates/g4/ 无 merge_prose/append_dedup；`create_or_overwrite` 零消费者；append_dedup 11 技能、merge_prose 4 技能；update_mode 词表 truth_io 消费
  - git log/status 确认 Z2 源码零改动
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色差异为 git-show 证据 + 代码路径推理）。
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files`/`git log` 仅读；探针均在 /tmp 目录与 python -c / heredoc。

---

## 五、统计

- 新 finding：**4 条**（F263-F266：P2×1、M×3）；M 级观察 5 条随文
- 误报：**0 条整体误报**（F201-F262 共 62 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持）
- 严重度异议：**8 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1（均已入账）、F217→P1（低置信）、F228→P1（低置信）、F238→P1（低置信）、F229→P1（低置信）、F251→P1（低置信）、F220 维持 P2
- 前十三轮 findings 判定：F201-F262 共 62 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否（严格意义上）**——本轮仍有 4 条新发现（F263-F266），其中 F266 为 P2（write_semantics 校验面整体未接线，属契约层"未接线/声明↔执行漂移"家族，十三轮从未触及该字段的校验面），F263-F265 为 M 级边界/孪生缺陷；**无新增 P1**，6 条 P1（F214/F216/F218/F227/F235/F247）经独立反证尝试全部维持。若协调者将 F266 视为 F203-F207"契约模型未接线"家族的既有类别延伸，则实质性新根因收敛成立；F263/F264/F265 均为极细碎边界，建议随对应修复（frontmatter 解析重写 / fields 重构 / F260 修复）一并处理。
