# Z2 分区第十三轮独立复核报告（Z2.review13）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）+ `Z2.review9.md`（F253-F255）+ `Z2.review10.md`（F256-F257）+ `Z2.review11.md`（F258）+ `Z2.review12.md`（F259-F261）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，逐文件重读，未依赖前十二轮转述）；另读跨区消费面验证裁决（audit/{_shared,snapshot,write_audit,record}.py、gates/{cli,g1,g2,g3,g3_independence,shared,g0,g_dispatch,g_reconcile}.py、scoring.py、safe_write.py、pipeline/{dispatch_helper,chapter_loop,context_curation,closure}.py、tests/round-exec.sh、first-novel.md、docs/framework/truth-files.yaml、skills/shenbi-{style-learning,book-spine-init,memory-distill,state-settling,context-composing,chapter-drafting,chapter-planning,foreshadowing-lifecycle}/SKILL.md、tests/fixtures/genre-config-example.json、novel-output/ 真实 truth 文件）
- 模式: 只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）。git log/status 确认 Z2 源码零改动（findings-ledger 已入账 F259-F261）。
2. **独立验证方式**：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**；20+ 个 `python -c` 运行时探针（F214/F216/F218/F221/F227/F232/F235/F236/F240/F245/F247/F249/F251/F252/F258/F259/F260/F261 复现 + **F262 新证据**）+ 跨区文件逐段 read 复证。
3. **前十二轮判定**：F201-F261 共 **61 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注、F243 机制修正（exit 3 先于 exit 2）、F247 范围注记（中文 prompt → chapter=None 审计空转不 rc=2）均复核通过。6 条 P1（F214/F216/F218/F227/F235/F247）经**独立反证尝试**全部维持（见第三节）。
4. **本轮新发现 1 条（F262，P2）+ M 级观察 3 条**，集中于十二轮都未触及的**read_fields 声明与生产者模板系统性漂移**——字段级过滤声明针对旧版/规划中模板书写，与当前 producer 输出 heading 不匹配，形成两类实害：(a) 全漏（0/N 匹配）→ 恒走 escape hatch（过滤失效 + 每次派发 WARN 噪音）；(b) **部分匹配 → F218 机制实锤实例**（book_strata/arc-N 的「未解决悬置」section 被静默丢弃，matched=True、无 WARN）。**实质性（P1/P2）新 finding 已收敛为 0 之后本轮仍发现 1 条 P2**——故严格意义上 0 新 finding 判定为**否**；但该条为「已覆盖机制（F218/F231 同类）的实例级枚举」，不新增独立根因。

---

## 一、复核结论表（F201-F261 判定）

| 编号 | 复核判定 | 理由（本轮 fresh 验证，引用 Z2 内文件行号） |
|---|---|---|
| F201 | **成立，P1 维持** | 三模型（chapter_planning/context_composing/volume_outlining）无生产消费者（grep 仅 test_skill_integration.py:188 类名文本）；g4 私有实现规则不同（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）——本轮 read 复证。 |
| F202 | **成立** | registry.py:40-63 REGISTRY/_discover_skill_models/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | **成立** | deps.py:56-78 DepsDoc/phase_of 无生产消费者；sync_contracts.py:195 仍 json.loads（read 复证）。 |
| F204 | **成立（附注）** | novel.py NovelConfig 无生产 import；g6.py:51 与 g4/worldbuilding.py 以 raw dict 读 target_word_count/target_words——死的是模型非行为。 |
| F205 | **成立** | schemas/scores.py 无生产消费者（grep 复证）。 |
| F206 | **成立** | schemas/state.py ProgressDoc/SummaryDoc 无加载点（g3.py 用 jload 直读 dict）。 |
| F207 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | **成立（增补成立）** | enums.py Severity/Verdict 无生产 import（grep 复证：生产仅 CPZone/ActorRole 有消费）；裸字面量散落点 + chapter_loop.py:1797、:409-438 复核通过。 |
| F209 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有）；GateOutcome/PureInput 无生产消费者（过渡类型自述 base.py:4-5）。 |
| F210 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94，Δ32-40。 |
| F211 | **成立（表述修正维持）** | executor.py:167-188 cov missing（74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0 → 修复方向按"CLI 崩溃"修正，本体成立。 |
| F213 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | **成立，P1 维持** | codex.py:20-48 不写 current_scorer_agent；dispatch_helper.py:1934-1945 run_gate_g3 在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行。**反证尝试**（独立路径不跑 G3 故 P2？）不成立——run_gate_g3 在 chapter_loop.py:2909 与 closure.py 均被调用（pipeline 正常路径；API/IDE 派发不写 progress.json → 伪造为常态）。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。 |
| F215 | **成立** | codex.py:75 `\{[^{}]*\}` 对嵌套 JSON 取内层（`{"final_score":90,"dimensions":[{"num":1,"score":88}]}` → 匹配 `{"num":1,"score":88}`）。 |
| F216 | **成立，P1 维持** | **运行时复现**：空 customRules + texture:False 禁用维度 → `GenreConfig.model_validate` PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。**反证尝试**（空规则无真实触发面）不成立——新轮次 stub customRules 可空（F241 同链佐证 worldbuilding 产 stub）。 |
| F217 | **成立；支持低置信异议（P2→P1）维持 P2** | SKILL.md 内多处 CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :78 铺垫 30-40）+ docstring 20-30 + 模型 [15,35]；权威区间未定（SKILL.md 自相矛盾），维持低置信。 |
| F218 | **成立，P1 维持** | **运行时复现**：部分匹配（声明 ["当前任务","不存在的字段"]，文件 3 节）→ matched=True、仅命中 section、无 WARN；全缺 → 全文 + `field_filter_no_match` WARN（fields.py:55-64）；dispatch_helper.py:592 `_matched` 弃用。AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足。**本轮补强**：F262 提供 book_strata/arc-N 两个真实数据形状的部分匹配实例（见 F262）。 |
| F219 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态（"支柱一续"）；F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，维持 P2。 |
| F221 | **成立** | **运行时复现**：fixture 9 键含 tropeInventory，`GenreConfig.model_validate` PASS 且无 tropeInventory 属性（extra=ignore 静默丢弃）；SKILL.md:286/:292 "恰好 8 个"；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段 + docstring "8 camelCase keys"。四源漂移确认。 |
| F222 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0。 |
| F224 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（本轮复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except。 |
| F227 | **成立，P1 维持** | **本轮复证**：round-exec.sh create-mode 只建 t1-reports/t2-reports/t3-reports/skill-output/skill-traces + progress.json/meta.json/summary.json/enhancement-signals.json/.token-hashes.json（**无 novel.json**）；first-novel.md:41 文档化流程 `uv run shenbi-dispatch shenbi-worldbuilding generative <round_dir> "<prompt>"` 第一步 G1.1 novel.json not found → rc=1；dispatch()（executor.py:190-214）在 dispatch_codex（技能执行处）之前跑 G1/G2。`git show 99f91f0` 原 shell 注释 "the skill execution happened earlier" 证实门序回归。**反证尝试**（缺 novel.json 是预置问题）不成立——round-exec.sh 无任何产出 novel.json 的步骤。 |
| F228 | **成立；支持低置信异议（P2→P1）维持 P2** | derive_output_files（audit/_shared.py:38-56）只 resolve_or_skip、不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | **成立（范围低估但严重度维持）** | g2.py:76-84 decisions 分支对非 .json `continue`（:84，:164 连 G2.4-G2.12 一并跳过）+ executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | **成立** | **运行时复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`（无规范化）。 |
| F231 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者（本轮复证）。 |
| F232 | **成立** | **运行时复现**：`Selection(manual_override, rationale="")` → ACCEPTED；`Adjustment(rationale="   ")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | **成立，P1 维持** | **机制复证**：`PROJECT_DIR = REPO_ROOT`（executor.py:26）；worldbuilding watch 含 `truth/*.md`（glob 有 `*`）→ `_expand_patterns`（snapshot.py:32-48）展开仓库根现存 tracked `truth/bridge_tracker.md`/`truth/character_matrix.md` → audit_writes 对 `set(pre)|set(post)` 每个文件做 file-level 检查（write_audit.py:48-64）→ `_matches_declared("truth/bridge_tracker.md", ["truth/*.md"], globs)` 失败（F236）→ 零写入派发也 `未声明写入` → rc=2；round_dir 真实写入不被审计。test_executor_audit.py:18,40 monkeypatch `ex.PROJECT_DIR = tmp_path` 编码了正确语义。**反证尝试**（审计面意图为仓库根）不成立——codex.py:62 cwd 已改 round_dir，审计根未跟随。 |
| F236 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs（parametric_globs()），registry globs（`truth/*.md`）恒判未声明（read 复证；`"truth/*.md" in parametric_globs()` → False）。 |
| F237 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | **成立；支持 review6 低置信异议（P2→P1）维持 P2** | audit_writes（write_audit.py:52-56）对每个 post .md 无 try 地 parse_records；records/parser.py:40-44 yaml.safe_load 可抛 ScannerError/非 list 抛 ValueError；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | **成立** | derive_file_type（executor.py:81-85）truth/decisions 分类用精确字符串交集 → `truth/*.md`/`snapshots/chapter-NNN/*` 写者（worldbuilding/sequel-writing/truth-sync/snapshot-manage）误归 "chapter"（read 复证）。 |
| F240 | **成立** | **运行时复现**：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证）。 |
| F242 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"。生产写者 lifecycle 无条目 → 记录级写保护对生产路径空转。 |
| F243 | **成立（机制修正维持）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。机制修正（review8）维持：gate-marker 强制 exit 3 先于分数校验 exit 2（scoring.py:365-377 vs :422-432，本轮 read 复证）。 |
| F244 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak（不存在则静默无检查）；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | **成立** | **运行时复现**：resolve_chapter_path("audits/volume-N-payoff.md", 7) → `audits/volume-7-payoff.md`；arc-N+7 → `arc-7.md`；AC-NNN+7 → `AC-007.md`；resolve_or_skip(volume-N, None) → None；resolve_volume_path 仅 closure.py:158 消费；`_build_skill_prompt`（dispatch_helper.py:656-658）以 `resolve_chapter_path` 直接调用（非 resolve_or_skip）→ chapter=None 时抛 UnresolvedPathError。 |
| F246 | **成立** | dispatch_skill（dispatch_helper.py:1864）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径；rd = round_dir or project_dir）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | **成立，P1 维持（范围注记维持）** | **机制复证**：`_declared_patterns`（write_audit.py:19-26）以 `derive_output_files(skill)` **无 chapter** → N 路径全丢 → 声明集为空；watch 面（executor.py:220-225 `_audit_watch_paths(skill, chapter)`）带 chapter 解析出具体路径；`_expand_patterns`（snapshot.py:47）对 exact 模式**不存在也加入快照**（读为 None）→ audit_writes 对 `pre==post==None` 的文件仍跑 file-level 检查（compute_file_change 返回 "modified"）→ 未声明写入违规。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | **成立** | PacingDesign.from_markdown（pacing_design.py:77-130）chapter_sequence 恒空（:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）。 |
| F249 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500（g2.py:237-238 read 复证）。 |
| F250 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 pipeline dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | **成立；支持 review9 低置信异议（P2→P1）维持 P2** | 命名面：codex.py:56 `-scores-subagent.json` vs find_report（shared.py:157-166）三模式 / g0.py:440 G0.10 glob `*-generative-scores.json`（本轮 read 复证）；内容面：raw 维度 dict 无 final_score；状态面：codex.py:44 "done" vs g_reconcile.py:40,61 "DONE" → GR.2 恒 FAIL。 |
| F252 | **成立** | **本轮 read 复证**：scoring.py:365-377 marker 强制 + :187-224 check_gate_markers（t1-skill 分支要求 `G4-{skill}-{test_type}.json`）→ exit 3 MARKER_MISSING；codex.py:95-110 调 shenbi-score 不传 `--tier` → scoring.py:334 的 G3 子进程不触发、marker 检查必然执行；executor 编排（:190-214）只跑 G1/G2 无 G4；grep write_gate_marker dispatcher/ → 空。 |
| F253 | **成立** | codex.py:27-48 `_record_completion` 读-改-写整体无锁（safe_write.py:40-59 `_acquire_lock` flock 仅覆盖写入周期，read 复证；读发生在锁外）；pipeline legacy 并行（parallel_dispatch.py:168-171 Semaphore(4) 共享 project_dir）触发面真实。 |
| F254 | **成立** | run_g1/run_g2（executor.py:114-118/:125-137）与 shenbi-score 子进程（codex.py:95-109）均无 timeout；仅 codex exec 有 600s（codex.py:63）；`grep -n timeout src/shenbi/dispatcher/` → 仅 codex.py:63。 |
| F255 | **成立** | derive_file_type 对 4 个 report-kind truth 写者（score-volume/score-arc/score-stratum/review-arc-payoff）全 → "report"（executor.py:77-78 REPORT 分支先于 truth 交集）→ G2.11（g2.py:290 仅 file_type=="truth"）对其静默失效（read 复证）。 |
| F256 | **成立** | g2.py:273/:315 注释声明 "chapter files only"，G2.10（:274-287）/G2.12（:316-322）在 `if file_type == "chapter":` 块（:218-271）之外无条件执行——truth/report/json 全部受影响；句末符集合（:317）缺 `}`/`]` → 误归 chapter 的 JSON 恒 WARN（read 复证）。 |
| F257 | **成立** | executor.py:156 `def dispatch(..., *, chapter: int | None = None)` 全仓零调用方（grep 仅 cli.py:29、executor.py:250，均不传）；dispatch_with_write_audit（:242）另行独立二次 extract_chapter。 |
| F258 | **成立** | **运行时复现**：`extract_chapter("Now that chapter 6 is complete, draft chapter 7")` → 6（首匹配）；单引用 → 7。pipeline 生成 prompt 均为单章节引用，仅 standalone 手工 prompt 触发。 |
| F259 | **成立** | **运行时复现**：`extract_chapter("draft chapter 0")` → 0；`resolve_chapter_path('chapters/chapter-NNN.md', 0)` → `chapters/chapter-000.md`；对照 chapter_planning.py:22 `Field(ge=1)`。 |
| F260 | **成立** | **运行时复现**：`_changed_top_keys('{"version":"1"}', '{"version":"1","hacked":null}')` → `()`（旁路）；对照 `{"hacked": true}` → `('hacked',)`。snapshot.py:63-76 `a.get(k) != b.get(k)` None 哨兵不可区分"缺席"与"null 值"。根因在 Z5（audit/snapshot.py），被 Z2 executor 审计链消费。 |
| F261 | **成立** | **运行时复现**：`Selection(arc_relevance+low, rationale="x"*150)` → `Value error, rationale exceeds 100 chars`（应为 FORBIDDEN）；`grep -c VALID_BASIS decisions.py` → 1（仅定义）。decisions.py:11-12 死常量 + `_p25` 错误优先级（:34 长度检查先于 :41-42 FORBIDDEN）。 |

**误报统计**：整体误报 **0 条**（F201-F261 共 61 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F262）

### F262 | read_fields 声明与生产者模板系统性漂移（7 文件/5 技能）——含 F218 部分丢弃实锤（book_strata/arc-N「未解决悬置」被静默丢弃）+ F231 同类全漏实例（0/N → 恒 escape hatch） | 漏报（契约声明漂移，F218/F231 机制的实例级枚举） | P2

- 证据（declared vs 生产者模板 heading，全部 read + probe 实测）:
  1. **book_strata.md**（producer: shenbi-memory-distill L4 模板 `skills/shenbi-memory-distill/SKILL.md:141,144,152` 用 `## 本弧主题推进`/`## 跨弧伏笔账`/`## 未解决的张力悬置（带入下一大弧）`）vs `skills/shenbi-context-composing/SKILL.md:20-22` 声明 `[本弧主题推进, 跨弧伏笔账, 未解决悬置]` → **2/3 匹配**；`filter_to_fields` 实测：matched=True、无 WARN、`未解决悬置` section 被**静默丢弃**（probe 输出见验证清单）——F218 部分匹配机制的真实数据形状实例。
  2. **truth/arcs/arc-N.md**（producer: memory-distill L2 模板 `:107,110,121` 用 `## 弧内事件链`/`## 弧内伏笔兑现/推进`/`## 未解决悬置（带入下一弧段）`）vs `shenbi-context-composing/SKILL.md:30-32` 声明 `[弧内事件链, 弧内伏笔兑现/推进, 未解决悬置]` → **2/3 匹配**，`未解决悬置` section 静默丢弃（同上）。
  3. **style_profile.md**（producer: shenbi-style-learning 模板 `:215` `## 5. 修辞模式`、`:243` `## 8. 综合画像`，**无"对白占比"节**；真实文件 novel-output/.../style/style_profile.md 实测 `## 8. 综合画像`/`## 5. 修辞模式（推测）`）vs `shenbi-chapter-drafting/SKILL.md:18-19` 声明 `[11. 综合画像, 6. 修辞模式, 9. 对白占比]` → **0/3 匹配**（编号陈旧 + 不存在的节）→ 恒 escape hatch（全文 + `field_filter_no_match` WARN）。
  4. **outline/volume_map.md**（producer: volume-outlining；真实文件 novel-output/.../volume_map.md 实测 `### 跨卷桥接`）vs `shenbi-foreshadowing-lifecycle/SKILL.md:10` 声明 `fields: [cross-volume bridges]` → **0/1 匹配**（英文 vs 中文；且 canonical rule 不做 lowercase——`match_field('cross-volume bridges','Cross-Volume Bridges')` 也 False，probe 实测）→ 恒 escape hatch。
  5. **truth/pending_hooks.md**（真实文件 heading 为 `## 第56章伏笔呈现` 等逐章节）vs chapter-planning/context-composing/plant 声明 `[活跃伏笔, 伏笔统计]`（chapter-planning/SKILL.md:15-16 等）→ **0/2 匹配** → 恒 escape hatch。
  6. **truth/current_state.md**（真实文件 heading 为 `系统演化阶段`/`参数当前位置`/`进行中的情节线`/`世界状态变化`；"主角状态"/"当前世界局势"在**任何 producer 技能中零命中**，grep 复证）vs `shenbi-chapter-planning/SKILL.md:10-11` 声明 `[主角状态, 当前世界局势]` → **0/2 匹配** → 恒 escape hatch。
  7. **truth/chapter_summaries.md**（真实文件 heading 为 `## 第55章：宣告后周一` 等）vs chapter-planning/context-composing/review-continuity/review-group-factual 声明 `[已完成章节]` → **0/1 匹配** → 恒 escape hatch。
- 根因: read_fields 声明针对旧版/规划中模板书写（编号、措辞、语言），与当前 producer 输出 heading 脱节；`fields.py` 匹配语义为**规范化后精确相等**（`match_field` = `_normalize_ws(declared) == _normalize_ws(heading)`，fields.py:34-35），无前缀/子串/大小写折叠 → 生产者模板任何措辞改动（加后缀、改编号、中英切换）都会**静默**打破过滤：全漏 → escape hatch（safe 但有 WARN 噪音 + 过滤失效），部分漏 → **F218 机制静默丢 section**（book_strata/arc-N 的"未解决悬置"是 context-composing 规划时的关键信息）。F231 只覆盖了 genre-config 的 povMode/sensitivityFlags 两个键；本条为 truth 层 7 文件的系统性实例枚举，前十二轮未触及。
- 验证命令（python -c，零仓库写入）:
  - book_strata 模拟过滤 → `matched=True`、输出仅含 2 section、`未解决悬置 in output: False`（F218 部分丢弃实锤）
  - `match_field('cross-volume bridges','跨卷桥接')` → False；`match_field('cross-volume bridges','Cross-Volume Bridges')` → False（case 敏感）
  - `grep -rn "主角状态\|当前世界局势" skills/shenbi-memory-distill/SKILL.md skills/shenbi-truth-sync/SKILL.md skills/shenbi-state-settling/SKILL.md` → 空
  - `grep -n "^## " skills/shenbi-style-learning/SKILL.md` → 1-8 节无 11/6/9 号、无"对白占比"
  - `grep -n "对白占比" skills/shenbi-style-learning/SKILL.md` → 空
- 影响面: chapter-drafting（style_profile）、chapter-planning（current_state/pending_hooks）、foreshadowing-lifecycle（volume_map）为 pipeline 正常路径（dispatch_skill → _build_skill_prompt → filter_to_fields，dispatch_helper.py:592），每次派发触发；context-composing 实例（book_spine/book_strata/arc-N/pending_hooks）当前被确定性 context_curation（chapter_loop.py:2714）取代为休眠，但契约声明仍死线漂移，恢复 LLM 路径即生效。
- 建议方向: (a) 逐文件将声明与 producer 模板对齐（book_spine 改 `核心冲突三层（…）` 全称或模板去后缀；style_profile 改 8/5 号并删"对白占比"；volume_map 改中文"跨卷桥接"）；(b) 匹配语义加**前缀/后缀容忍**（如声明为前缀时命中）或文档明示精确相等契约；(c) 与 F218 修复联动：部分匹配时 WARN 缺失字段清单（当前 matched=True 无任何提示）；(d) 补一条 read_fields ↔ producer 模板一致性 lint（sync_contracts 扩展）。

### M 级观察（随文记录，均非 finding）
- `contracts/schemas/decisions.py:73` `DecisionsDoc.chapter: int` 无 `Field(ge=1)`——与 chapter_planning.py:22 的章节号下界语义不一致（F259 家族同源；G2 消费 DecisionsDoc.model_validate 于 g2.py:160，chapter:0 的 decisions.json 可过）(M)
- `pipeline/dispatch_helper.py:718-721` 对 output_paths 中**含 glob 的写路径**（`truth/*.md` 等 9 技能）直接跳过不写入 prompt 的 "Files to create" 清单——glob 写技能的输出指引整体缺失，靠输出收集 rglob 兜底（跨区观察，与 F228/F239 同链；是否有意为之未文档化）(M)
- `contracts/fields.py:34-35` `match_field` 精确相等 + 无大小写折叠——英文 field 声明（如 volume_map 的 `cross-volume bridges`）对任何大小写变体均不命中（F262 根因机制面；若未来有英文 heading 技能需注意）(M)

---

## 三、严重度异议（不修改前十二轮，仅提理由；本轮不新增独立异议）

1. **支持 F214 P2 → P1（已入账）**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4 fail-closed 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。本轮反证尝试（独立路径不跑 G3 故 P2？）不成立——run_gate_g3 在 chapter_loop.py:2909 / closure.py 被调用，API/IDE 派发不写 progress.json → 伪造为 pipeline 常态。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。
2. **支持 F218 P2 → P1（已入账）**：AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 在部分缺失时两处均不满足（fields.py:55-64 本轮复现）。**本轮新证据**：F262 的 book_strata/arc-N 实例证明部分丢弃在**真实数据形状**上发生（生产 template 实测），非假设场景。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——SKILL.md 内部多处自相矛盾（本轮再证 :78 铺垫 30-40），权威区间未定，维持低置信。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内 → record_audit_outcome 永不执行（审计记录丢失）；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能的 .md 散文在 G2 被 continue 整体跳过，G2.6/G2.8/G2.9 对章节正文静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F251 P2 → P1（低置信维持）**：评分产物三重不匹配使 standalone 轮次对 G5.1（no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL）确定性失败；受 F252（exit 3 前置）掩盖维持低置信。
8. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。
9. **F262 严重度自评说明（非异议）**：F262 按 P2 报告（契约声明漂移 + 过滤失效 + WARN 噪音）；但其 (b) 部分（book_strata/arc-N 静默丢 section）是 F218 争议机制的最强实锤，若协调者按 F218 的 P1 已入账口径评估实例，F262 的 (b) 部分可并入 F218 修复清单（声明对齐即可消解），无需单列升 P。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）。
- 运行时探针清单（全部在 /tmp 与 python -c，零仓库写入）：
  - F214 伪造机制：dispatch_helper.py:1934-1945 read 复证 + run_gate_g3 调用点（chapter_loop.py:2909/closure.py）
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F218 部分匹配 → matched=True、仅命中 section、无 WARN（复现）；F262 补强：book_strata 模拟内容 → 未解决悬置 section 静默丢失
  - F221 fixture 9 键 model_validate PASS + tropeInventory 属性缺失（复现）
  - F232 Selection(manual_override, rationale="") ACCEPTED（复现）
  - F240/F258/F259 extract_chapter 中文→None / 多引用→6 / chapter 0→0（复现）
  - F245 resolve_chapter_path(volume-N, 7) → volume-7-payoff.md（复现）
  - F260 `_changed_top_keys` null 值新增 → ()（复现）
  - F261 routine+low 超长 rationale → 长度错误（复现）
  - F235/F236/F247 审计链机制：audit_writes 对 `set(pre)|set(post)` 全体（含 pre==post==None 的 exact watch 路径）做 file-level 检查（write_audit.py:48-64 + snapshot.py:47）——F247 零写入也 rc=2 的机制级复证
  - F262 新证据：7 文件声明 vs producer 模板 heading 对照（grep + match_field probe）
  - F227 round-exec.sh create-mode 产物清单（无 novel.json）；first-novel.md:41 文档化流程
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files`/`git log` 仅读；探针均在 /tmp 目录与 python -c。

---

## 五、统计

- 新 finding：**1 条**（F262：P2×1）；M 级观察 3 条随文
- 误报：**0 条整体误报**（F201-F261 共 61 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持）
- 严重度异议：**8 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1（均已入账）、F217→P1（低置信）、F228→P1（低置信）、F238→P1（低置信）、F229→P1（低置信）、F251→P1（低置信）、F220 维持 P2
- 前十二轮 findings 判定：F201-F261 共 61 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否（严格意义上）**——本轮仍有 1 条新发现（F262，P2），但为**已覆盖机制（F218 部分丢弃 / F231 声明悬空）的实例级枚举**（7 文件/5 技能 read_fields 漂移，含真实数据形状的部分丢弃实锤），**不新增独立根因**；P1 集（F214/F216/F218/F227/F235/F247）经独立反证尝试全部维持。若协调者将"实例级枚举"视为既有 finding 的证据补充而非新根因，则实质性收敛成立。
