# Z1 分区第五轮独立复核报告（agent f）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（13 findings F101–F113）+ `Z1.review.md`（9 findings F114–F122）+ `Z1.review2.md`（5 findings F123–F127）+ `Z1.review3.md`（6 findings F128–F133）+ `Z1.review4.md`（6 findings F134–F139）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件通读；对跨区依赖 `gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g3.py`、`gates/g4/generic.py`、`gates/g5.py`、`gates/gate_manifest.py`、`audit/_shared.py`、`dispatcher/executor.py`、`dispatcher/modes/codex.py`、`contracts/legacy.py`、`contracts/paths.py`、`contracts/graph.py`、`orchestration/scoring_bridge.py`、`tests/tiers/deps.json`、`.venv/bin/shenbi-score`、82 份 rubric、command-to-give.md 独立核实；并**实跑 `.venv/bin/shenbi-score` 验证 CLI 退出码**）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟；临时文件仅写 /tmp（非仓库文件）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F140–F199（实际产出 F140–F143，共 4 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：4**（F140–F143；**P1×1、P2×1、M×2**）
- **误报数：0**（前五轮 39 条全部成立；无一条被证伪，含 F115 的 P1 判定——独立复跑解析器确认无反证）
- **严重度异议数：2 条维持**（F109：同意 R1–R4 的 M→P2；F115：P1 无反证维持）；**无新增异议**
- **0 新 finding 判定：否**（F140 为新增 P1——`shenbi-score` 成功路径恒 exit 1，前五轮全部漏掉；该 P1 与前五轮 F115 不同站点）

---

## 1. 复核结论表（F101–F139 逐条判定）

| 编号 | 标题 | 前五轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | `git ls-files -s`（100644）vs `ls -la`（0600）实证；git 不追踪 rw 位 → CI `git diff --exit-code` 不可见；sync_contracts render_body_into→safe_write 改写 SKILL.md 亦同源（gates/shared.py:205 同） |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `ls docs/{registry,gates,schemas,dispatcher,integrity}.md` 全 No such file；`ls tests/build_registry.py` No such file；`tests/lock-tool-hashes.sh` **存在**（ToolTamper 的 action 不破，仅 doc_url 破） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立**（计数注记） | 独立 AST 扫描：exceptions.py 22 类中直接 RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentTimeoutError/SubAgentProtocolError/RetryExhaustedError（5/22），NEVER RAISED 17 类（字面计数）；R4 按"捕获可达"折为 16 的注记合理（SubAgentUnavailableError⊂DispatcherError），实质结论不变——error_guidance/recovery 引用的 6 类全部无 raise 站点 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117 `str(None)`；g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **PASS（g5.py:202-203，非 SKIP）**——R3 对 R1"静默 SKIP"的修正独立确认；对无 expected_outputs 的 phase 可假 PASS |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一 `except Exception`（grep 证实）；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | grep 全 src：仅 gates/cli.py:131-136 手动入口；phase_runner/pipeline 0 引用 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——若抛 OSError，:117-125 finally 不执行 → 锁 fd/.lock 泄漏；:89-91 unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert；`-O` 下守卫消失与 docstring :110 "Raises on mismatch" 冲突；工具仍会静默写 deps.json（:201）。**同意 R1–R4：M→P2**（运行时正确性守卫而非文案） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo("` 0 命中；同族 `backup()`（:33-35）亦 0 调用方 |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | :153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `issubclass(TimeoutExpired, OSError)==False`（mro 实测）；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两 subprocess 无 timeout |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **全量复跑 `load_applicability`**：82 rubric 中 38 个返回 `{}`（26 t1 + 9 t2 + 3 t3）；4 个 N/A 豁免技能全部确认（worldbuilding:43 dim4 / faction-builder:46 dim8 / location-builder:43 dim5 / story-architecture:44 dim6，均含 "scoring.py renormalizes weights…"，表头 `\| # \| Dimension \| Bug-hunt Standard \| Clean Standard \|` 非解析器认的 `\| Dimension scope \|`）→ 解析器整体 no-op → 豁免维度（10% 权重）计入 bug-hunt/clean。**补充深化**：即使解析器识别该表，`cell_val.strip().lower().startswith("no")` 对 "N/A — exempted" 单元格返回 True（适用）——N/A 豁免语义在解析器内无任何支持（属 F115 根因族，非独立 finding）。command-to-give.md:64,71 文档化路径可复现 → 按决策表 P1 维持 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；G2 FAIL 只进 step/manifest；executor.py:200-204 同路径阻断 → 双入口语义不一致 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值（实测）；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-110 bug-hunt/clean 分支不写标记（仅 :121 generative 写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 test_type 拼接；goal-prompt.md:86 三类型承诺不符（R2 证据成立） |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `steps.append`；其余 5 命令均记录 |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep CapabilityFS src/ tools/` 0 生产命中（仅自身 + 测试） |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；缺参 → IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"` 0 命中；ADR-0009 已迁移 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | :441 `"subagent" if "--subagent" in sys.argv else "interactive"`；command-to-give.md:49 文档化评分命令不带 --subagent |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用；CommandStatus.EXISTS 由 pipeline/cli.py:411 消费，非死成员） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"` 均通过 → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫；对照 :300 `gate_type` 有 `idx+1 < len` 守卫 |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers 非标准 rubric 路径三分支全不匹配 → `return []` |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子（删新持有者锁） | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立** | :145-159 只查空 scores 不查空 dims；:169-170 total_weight==0 → 0；实测 `validate_scores({1:100,2:95}, [])` → (True, [WARNING…]) + `compute_score([],…)` → 0 FAIL |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；实测 NaN 通过校验并产出 NaN final |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src → 仅 status.py:78 定义 |
| F137 | phase_runner post-skill 静默吞 ContractError（G2 SKIP + G4 空文件 vacuous PASS） | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志；g4/generic.py:57-58 空文件 → `passed(...)`；`load_contract` 全部 raise 站点均为 ContractError（legacy.py 核实）→ except 完整但吞错无日志。**补充**：空 output_files 的 G2-SKIP 路径本身有测试（test_phase_runner.py:385 test_skips_g2_when_no_output_files，fake G4 PASS 掩盖真实 generic vacuous PASS）——"契约损坏"触发面成立，空产出触发面为设计内宽松（test docstring 明示），不重复立 finding |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | `grep -c "log\."` 两文件均 0（排除 import 行） |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块 |

**前五轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115-205 确认 **R3 修正正确**；F101 波及文件数递增记录属实。**无新的前轮事实错误需要修正。**

---

## 2. 新发现（F140–F143）

F140 | `shenbi-score` CLI 成功路径恒 exit 1：main() 返回 dict → 控制台脚本 `sys.exit(dict)` → 违反 command-to-give.md 文档化退出码契约（0=成功），且 codex 模式自动评分路径永远"失败" | error | **P1**
- 证据: src/shenbi/scoring.py:463（`emit_json(result); return result`——成功路径返回 dict 而非退出码）；pyproject.toml:60（`shenbi-score = "shenbi.scoring:main"`）；`.venv/bin/shenbi-score`（标准 hatchling 包装：`sys.exit(main())`）；command-to-give.md:57-61（**文档化退出码契约："0 = 评分计算成功，结果写入 stdout。Dispatcher 将 stdout 保存到 t1-reports/..."、1 = 其他错误**）；src/shenbi/dispatcher/modes/codex.py:105-108（`if result.returncode != 0: return result.returncode`——依赖退出码）
- 根因: `main()` 双用途（库 API 返回 result dict + CLI 入口），dict 不是合法退出码：`sys.exit(dict)` → `SystemExit(dict)` → 解释器打印 dict repr 到 stderr 并以 **exit 1** 退出。成功路径从未 `sys.exit(0)`。测试全部直接调 `main()` 断言返回 dict（test_scoring.py:566 `return main()`），从不经控制台脚本跑成功路径；test_logging.py:162-163 的 `_run_framework_cli(["shenbi-score"])` 只测无参 usage 错误路径；codex 测试 mock `subprocess.run` 返回 `returncode=0`（test_codex_mark_done.py:30）掩盖真实行为
- 验证命令+输出: `./.venv/bin/shenbi-score tests/tiers/t1-skill/shenbi-chapter-drafting/rubric.md /tmp/zb_scores.json > /tmp/zb_out.json 2>/tmp/zb_err.txt; echo $?` → **`EXIT CODE: 1`**（stdout 为合法 JSON，`final_score: 88.3 / PASS (acceptable)`——评分计算完全成功；stderr 含完整 result dict 的 repr，即结果被重复打印到 stderr）；`printf 'import sys\nsys.exit({"a":1})\n' > /tmp/t_exit.py; python3 /tmp/t_exit.py; echo $?` → `1`（+ stderr `{'a': 1}`）。退出码 1 与 usage 错误（test_scoring.py:712 断言 code==1）同码 → 成功与错误不可区分
- 影响: ① command-to-give.md:57-61 文档化 "0=成功" 契约被违反（按决策表"违反 AGENTS.md 显式契约"→P1）；② codex 模式（executor.detect_mode → codex）下每次自动评分 `result.returncode != 0` 恒真 → `_record_completion`（codex.py:110）与最终 `emit_json`（:111）**永不可达**，分数从不写入 progress.json/t1-reports → T1 自动评分管线整体失效（若 G4 marker 缺失则先 exit 3，F120 族；marker 存在时也 exit 1）；③ 成功与错误的退出码冲突（1 同时是 usage 错误码）
- 建议方向: `main()` 末尾改 `sys.exit(0)`（或在 `if __name__ == "__main__": sys.exit(main() or 0)` 处显式归一）；把成功路径退出码纳入 CLI 级测试（子进程跑真实控制台脚本断言 rc==0）；同步核对其余入口（shenbi-phase main 返回 None→exit 0 正确、shenbi-validate/sync-contracts 返回 int 正确，仅 shenbi-score 受影响）

F141 | scoring validate_scores 对 bool 分数放行：scores.json 中 `true`/`false` 静默按 1/0 计分（isinstance 把 bool 当 int） | error | P2
- 证据: src/shenbi/scoring.py:153-157（`isinstance(score, (int, float))`——`bool` 是 `int` 子类 → True；`score < 0 or score > 100` 对 True/False 恒 False → 校验通过）；:419（`json.load` 把 `true` → `True`）；:173-174（`float(True)*weight` → 按 1 计）
- 根因: 类型校验用 `isinstance(x, (int, float))` 而未排除 bool；字符串分数（`"100"`）会被 REJECT，bool 却漏过——类型洞与 F135（NaN）同族但为独立值类（NaN 是 float 边界、bool 是 isinstance 子类边界，修复不同：`isinstance(score, bool)` 排除或 `type(score) in (int, float)`）
- 验证命令+输出: `PYTHONPATH=src python3 -c "from shenbi.scoring import validate_scores, compute_score; dims=[{'num':1,'name':'a','weight':50},{'num':2,'name':'b','weight':50}]; print(validate_scores({1: True, 2: 95}, dims)); print(compute_score(dims, {1: True, 2: 95}, False))"` → `(True, [])` / `48.0`（`true` 静默按 1 计，应 REJECT）
- 建议方向: 校验加 `if isinstance(score, bool): errors.append("REJECT: dimension N score must be a number")`（或 `type(score) not in (int, float)`）；补 `{"1": true}` 单测

F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 `weight_mismatch` 误报 warning；filter docstring 宣称 "renormalize weights" 但函数内未实现（重归一实际隐式发生在 compute_score），且输出 dimensions 权重和不等于 100 | doc↔code drift | M
- 证据: src/shenbi/scoring.py:171-172（`if total_weight != 100: log.warning("weight_mismatch", ...)`）；:107-109（filter_dimensions_by_test_type docstring "Then filter those dimensions out and renormalize weights."）；:135-136（函数仅过滤，无任何权重调整）；:138-174（compute_score 以 `weighted_sum/total_weight` 隐式重归一）
- 根因: 重归一逻辑放在 compute_score（除 total_weight），filter 的 docstring 却宣称在 filter 内完成；对含豁免维度的 rubric（如 chapter-drafting dims 6,7,9，各 10/10/5%）做 bug-hunt/clean 过滤后 total_weight=75 → compute_score 在**每条文档化路径**（command-to-give.md:71）上发 weight_mismatch warning——该 warning 设计用于"rubric 权重异常"，却在正常过滤路径上误报；输出 result 的 dimensions 权重和=75（非 100），依赖"权重和=100"的消费方会算错
- 验证命令+输出: `PYTHONPATH=src python3 -c "from shenbi.scoring import load_rubric, filter_dimensions_by_test_type, compute_score; dims,_=load_rubric('tests/tiers/t1-skill/shenbi-chapter-drafting/rubric.md'); f=filter_dimensions_by_test_type(dims,'tests/tiers/t1-skill/shenbi-chapter-drafting/rubric.md','bug-hunt'); print('filtered weights sum:', sum(d['weight'] for d in f)); print(compute_score(f, {d['num']:100 for d in f}, False))"` → `filtered weights sum: 75` + `[warning ] weight_mismatch expected=100 total_weight=75`（数值正确但每次误报）
- 建议方向: 把 docstring 改为"在 compute_score 中按总权重归一"或让 filter 显式重归一权重至 100；warning 仅在"未经 test-type 过滤"时触发（或降为 debug）；输出 dimensions 附 `total_weight` 字段

F143 | phase_runner cmd_post_skill 回退注释声称 "G2's decisions branch would json.loads() markdown → crash"，与 g2.py 实际行为（decisions 分支对非 .json 直接 continue，不会解析 markdown）不符 | doc↔code drift | M
- 证据: src/shenbi/phase_runner.py:204-207（注释 "CRITICAL: …If derive_file_type returns 'decisions' but rglob only finds .md files, G2's decisions branch would json.loads() markdown → crash. So the fallback must use file_type='chapter'"）；src/shenbi/gates/g2.py:76-84（decisions 分支 `if not fp.endswith(".json"): continue  # skip .md files — decisions branch only validates JSON`——.md 文件被显式跳过，无 json.loads，无 crash）
- 根因: 注释编写时的担忧（或更早版本行为）与现行 g2.py 实现脱节——现行实现专门处理了 .md 跳过（g2.py:78-84 注释同样说明该设计）；回退 override 到 "chapter" 仍有实际价值（对 .md 施加 G2.5-2.9 章节校验），但注释给出的 crash 理由已不成立
- 验证命令+输出: 代码走查 phase_runner.py:203-210 + g2.py:76-84；grep `json.loads` g2.py 内 decisions 分支 → 仅 `:110`（.json 文件路径，.md 在 :84 已 continue）
- 建议方向: 更新注释为实际理由（"decisions 分支会跳过 .md 使 G2.5+ 不执行，故回退用 chapter 以施加章节校验"），消除错误 crash 论断

---

## 3. 严重度异议汇总

| 编号 | 前五轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移（CI 幂等检查为唯一间接兜底）。按决策表 P2"防御深度"而非 M"文案" |
| F115 | P1 | **维持 P1（无反证）** | 独立全量复跑解析器（38/82 no-op、4 技能 N/A 豁免、表头格式、command-to-give.md:64,71 文档化路径）全部确认；并发现 N/A 单元格即使被表头识别也会被 `startswith("no")` 判为适用（F115 根因族内）。按决策表"正常路径可复现功能错误"维持 |

其余 37 条严重度无异议（F101–F108/F110–F114/F116–F137/F140 的 P2 与 F110/F111/F126/F127/F130/F131/F132/F136/F138/F139/F142/F143 的 M 均认可；F140 为本轮新增 P1）。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读；另核实 16 个跨区依赖文件/数据（gates/cli.py、gates/shared.py、gates/g2.py、gates/g3.py、gates/g4/generic.py、gates/g5.py、gates/gate_manifest.py、audit/_shared.py、dispatcher/executor.py、dispatcher/modes/codex.py、contracts/legacy.py、contracts/paths.py、contracts/graph.py、orchestration/scoring_bridge.py、tests/tiers/deps.json、.venv/bin/shenbi-score、82 份 rubric、command-to-give.md）以验证调用契约）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型核实无洞；F123 已覆盖无接线）、`cli_utils.py`（stdout 数据通道/stderr 日志分离自洽）、`logging.py`（双渲染器/stderr/`cache_logger_on_first_use=False` 自洽；SHENBI_LOG_FORMAT 非 "json" 静默降级为前轮已记录低价值项）、`paths.py`（read/write 语义正确；F112 覆盖死代码）、`recovery.py`（F103 覆盖 dead；F138 未用 log）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（除 F127/F136 死成员外词汇表/枚举值自洽）、`exceptions.py`（层级结构正确；F103 覆盖 dead；`load_contract` 全部 raise 站点均为 ContractError——legacy.py 核实，无其他异常类型漏网）
- 新发现分布：`scoring.py`（F140 退出码 / F141 bool 洞 / F142 warning 误报）、`phase_runner.py`（F143 过期注释）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **F140 的 codex 路径还有第二个失效点**：codex.py:99-107 的评分子进程带 `--round-dir`，而 dispatcher 流程从不运行 G4 → `G4-{skill}-generative.json` marker 不存在 → check_gate_markers（scoring.py:196-202）先 exit 3（MARKER_MISSING）。即使 F140 修复，codex 自动评分仍会 exit 3（除非按 T1 协议先手动跑 `shenbi-validate G4`）。写侧在 gates/cli.py（非 Z1）、调用侧在 dispatcher（非 Z1），Z1 侧事实已由 F120/F140 覆盖，未单独立 finding。
2. **`normalize_to_glob` 对具体路径（如 `chapters/chapter-5.md`）返回最宽 glob（`chapters/*.md`）而非窄 glob（`chapters/chapter-*.md`）**（contracts/graph.py:33-38 先 patterns 后 globs 的顺序 + globs 列表 `chapters/*.md` 在 `chapters/chapter-*-*.md` 之前）——expected_outputs/G5.4 检查因此变宽（存在性检查宽松但不错报）；docstring 已声明 glob-aware trade-off，当前 82 技能契约均用 parametric 写法，未触发实际缺陷，未立 finding。
3. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达（`grep render_body_into tests/` 仅 coverage HTML 产物）——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。
4. **`--gate-only` 对 UNIMPLEMENTED 门返回 exit 0**（scoring.py:313 `status != "FAIL"` → 0）：stub 门 G_TRANSITION 等视为成功；低危，未立 finding。
5. **t2/t3 rubric 无 "## Dimension Applicability" 节属良性**（未声明豁免，filter no-op 无害——与 R2 修正一致）；kill-switch 解析对全部 82 rubric 实测无漏（无节的 8 个技能 rubric 确实无 kill switch 节）。

---

## 5. 其他说明

- 前五轮"P1×1（F115）"结论因 F140 修正为"P1×2"；F140 是**成功路径、可复现、有文档化契约与自动消费方**的功能错误，按决策表"违反显式契约 + 正常路径可复现功能错误"定 P1，且其影响（codex 自动评分分数永不落盘）比 F115（4 技能×1 维度权重偏差）的自动化影响面更大，建议合并层优先修复。
- 本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**实证退出码；临时文件仅写 /tmp，未触碰仓库文件。
