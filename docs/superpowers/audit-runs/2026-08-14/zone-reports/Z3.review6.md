# Z3 分区第六轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第六轮独立复核（本文件 `Z3.review6.md`，与初审 `Z3.a.md`、一轮 `Z3.review.md`、二轮 `Z3.review2.md`、三轮 `Z3.review3.md`、四轮 `Z3.review4.md`、五轮 `Z3.review5.md` 隔离）
> 复核范围：34/34 文件全量重读（13,641 行），逐条独立验证 F300-F370；以真实生产项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式、audits 722 文件全旧式名、chapters 56 正文 + 89 decisions、plans/chapter-5-plan.md 含 MH-001/H-N01、truth/pending_hooks.md frontmatter 无 hooks 键、truth/book_spine.md 无 hook_master_list、novel.json 无 total_chapters、pipeline-state.json phase=chapter-loop ch=56、style/ 有 style_profile.md 而 truth/style_profile.md、truth/world_rules.md、truth/volume_map.md 均缺失）为格式/行为对照基准
> 本轮发现编号：F371-F372（2 条新发现，其中 1 条 P1、1 条 P2）；**新增误报 0 条**（F325 维持二轮撤销，本轮独立复证）；严重度异议 1 条（F364 支持 P1→P0，裁定留 stage 4）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证仅在 /tmp 下做临时目录实验）
> 方法：34 文件逐行重读 + grep/uv run python 实证 + 真实产物对照 + **G4 CLI 直接执行实验**（本轮唯一新验证手段，见 F371）+ **atexit 沙箱进程退出实验复证**（F364）

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`。生产 volume_map 全中文（`**章节范围**: 第1章 - 第15章（共15章）`、`| 第1章 | 开篇 | …`），`_END_RE`/`_RANGE_RE`/`章节数` 三个正则全部 0 命中；novel.json 无 total_chapters（本轮实测 keys 无该字段、56 章仍在 chapter-loop、closure_step=0）。
2. **F325（误报）维持撤销**：本轮实测 production audits/ 后缀分布——13 类全为旧式名（continuity/character/world-rules/pacing/dialogue/motivation/pov/memo-compliance/foreshadowing/anti-ai/sensitivity/resonance/review-summary），`group-*` 数量 = 0；4 个 group skill 契约 `writes:` 即旧式路径（load_contract 实证）；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING（`严重度: BLOCKING`）必被 route 与文件扫描双重命中 → revision 执行。
3. **F364（P1 贴 P0）维持成立，本轮沙箱复证 + 补一条同根面**：`register_emergency_handlers` → `atexit.register(_emergency_cleanup)`（crash_recovery.py:66），正常进程退出即无条件 `clear_staging`（:144-148）。本轮 /tmp 沙箱实测：`staging before exit: True` → 退出后 `STAGING GONE (cleared by atexit)`（无信号、无异常）。**补强面**：`_emergency_cleanup` 还无条件把 `cl.current_step` 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>` 并 `save_state`（:113-127）——沙箱退出后 `pipeline-state.json` 实测 `current_step='EMERGENCY_SHUTDOWN_AT_unknown'`——**每次正常退出都把持久化状态标记为"紧急停机"**，靠 cmd_resume 的 healer（cli.py:706-718）事后修复。两处同根（atexit 无门控），一并归入 F364 影响面。
4. **本轮最重要新发现（F371，P1）**：closure step 10（shenbi-snapshot-manage，`output_path="final-snapshot/"`）的 G4 校验**恒失败**——`_resolve_closure_g4_path`（closure.py:148-159）对无 N 占位符的路径原样返回目录路径 `final-snapshot/`，`run_gate_g4(step.skill, ["final-snapshot/"], …)`（:260-261）走 generic checker，对目录返回 `G4.gen.read_error`（目录不存在时 `not_found`）→ FAIL → `_handle_closure_failure` 重试（每次重试重新 dispatch LLM）→ 3 次后 escalation → **pipeline 永不进入 COMPLETED**。G4 CLI 直接执行实证（见 F371 验证节）。这是与 F324、F353 并列的**第三条"pipeline 永不完成"独立根因**（F324 修复后即激活）。
5. **F341（P1）维持成立且补强维持**：CHAPTER_STEPS 中唯一 checkpoint 声明是 :148 的 CHAPTER_MEMO（本轮 grep `checkpoint=CheckpointType` 仅 1 处命中）→ `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 全路径无功能效果。
6. **版本偏移注记（沿用三/四/五轮）**：生产 pipeline-state 的 steps_done 为 Plan 18 重构前的旧 skill 名，state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据（本轮新增：chapters/ 目录为 56 个 .md + 89 个 decisions .json，无 pre-rev/meta 文件——pre-rev 是当前代码 F350 的行为，不在旧产物中）。

---

## 一、复核结论总表（F300-F370 逐条判定）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")`——源码字符串字面量 `"\u003c"` 求值即 `<`，恒 no-op；注释宣称的标签注入缓解未生效 |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 :2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1` → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析）不可达（本轮逐行确认无其他调用点）。三轮修正成立：resonance_trend.md 由 shenbi-review-resonance 契约 updates 自身写入（本轮 load_contract 实证 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`）且生产存在 9 列格式；真正失效的是 cs.resonance_score 并行路径恒 None、boundary circle 不可达、BLOCKING 重审环不可达、审计产物无 G4/G3 |
| F302 | **成立** | P1 维持（P0 依据存在——决策表 P0 例字面列明 "TokenLedger 少计"；裁定留 stage 4，无异议） | `dispatch_skill(state=)` 仅 chapter_loop.py:2799（串行可及）/ :2970（串行 BLOCKING 环，不可达）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751 均未传（本轮 grep 实证）；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0 |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（`_snapshot_chapter_files` 死函数内）；restore_from_snapshot 无 src 调用方；step 14 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转（仅 linguistic-drift-check 有实现体）；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议；三触发路径维持 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep 实证 src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError；ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93）→ crash-resume / escalation-resume / auto 多轮 G4 放行三路径均可触发裸崩 |
| F305 | **成立但框定需修正（同意二/三/四/五轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；route==NO_REVISION 时 `_is_revision_skipped`（:2755）先于 `_should_run_step` 短路。残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 的记账漂移（四轮补充成立） |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方（本轮实测生产该路径 MISSING）；:97 `Chapter {chapter}` 子串缺陷；volume_context 无消费方（_INJECT_FROM_CACHE 只注入 4 字段，:601-621） |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用。注记（三/四/五轮）：`_parse_hook_entries`（hook_planting.py:144）对生产 plan section 7 的 `MH-001`/`H-N01` 行会接受（本轮实测 `| MH-001 | advance | … |` 首格匹配 `^(MH|H)-`），但 `raw_op == "advance"` 非 plant/open 被跳过（:151）；检索侧断裂见 F346 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state 无生产调用（grep 实证） |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop:2133 重复实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:430-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方（本轮 grep：write_truth_file 唯一生产调用 chapter_loop.py:3014 为 upsert_markdown_row） |
| F313 | **成立** | P1 维持，无异议 | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（shenbi-review-long-span 契约 writes 实证）→ G4 校验错位（F324 下 `_current_volume` 恒 1 → 校验 chapter-1-long-span.md） |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op；chapter_loop.py:2425-2426 调用无效果 |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重；consolidate 只提行不去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551，core_skills 实为 6 个）；F318.6 scr_extractor.py:313 三次相同引号判断（`'"' in p or '"' in p or '"' in p`）；F318.7 route_chapter_revision 的 blocking 形参未用（revision_router.py:86，函数体 0 次引用）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（维持 M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback（:552）无章号绑定、注入下一次串行 dispatch（:2775-2780）。同源缺口见 F365（STATE_SETTLE MODIFY 不重设 checkpoint） |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan（review_checklist.py:485-544）无生产调用（grep 实证；注意 generate_review_checklist 本体已在 dispatch_helper.py:767 接线，死的是静态/动态拆分三函数）；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符（双重死） |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`；`_END_RE`/`_RANGE_RE`/`章节数` 全 0 命中；生产 volume_map 头 `**章节范围**: 第1章 - 第15章（共15章）`、`| 第1章 | 开篇 |`。影响链（卷边界触发全家 / total_chapters=None → cli.py:219 `if total > 0:` 跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 章节节点提取裸数字不匹配且误中跨卷钩子表 `| 1 |` 行）全部成立 |
| **F325** | **不成立（误报，维持二/三/四/五轮撤销）** | **P1 裁定撤回维持** | 本轮实测 audits/ 13 类后缀全旧式名、group-* 数=0；4 个 group skill 契约 writes 即旧式路径（load_contract 实证：group-factual→continuity/world-rules/pacing 等）；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物；真实 BLOCKING 双重命中 → revision 执行。残余真值（P2）：清单缺 resonance/review-summary、含生产不产生的 texture/reader-pull |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证）；并行 post-draft（:2405-2419）两线程均以 uses_staging 写 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）。四轮补充成立：`_parse_resonance_score`（:1285-1321）三模式均不匹配生产 `**结果**: 通过 (77/100)` 格式 |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代（grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议 | core_skills（:2551，含 review-resonance + review-sensitivity）与 genre 波双发同一 skill 写同一文件；本轮实测 production genre-config auditDimensions.sensitivity=true → `get_active_genre_audits` 含 shenbi-review-sensitivity（实证输出含该 skill） |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库（注意：此路径被 F364 清空后实际"静默不入库"） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless 落入成功路径 :341-355）；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 补零数=0（本轮实测）；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；**本轮生产实证**：`audits/chapter-1-review-summary.md` 内容为 "**BLOCKING Issues**: 0 / No BLOCKING or CRITICAL issues found across all reviews"（与实际审计文件内容矛盾） |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死（本轮重读代码确认 genesis.py:280-281 与 cli.py:209-210 的短路语义） |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 配置在全部路径上均无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用（grep 实证） |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:24-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；本轮实测 6 个 audit skill 全部 requires_independent=True；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 `MH-001`/`H-N01` 零匹配（本轮实测生产 plan 行 `| MH-001 | advance | … |`、`| H-N01 | advance | … |`）→ master hooks 从 Route A 检索静默缺失；`MH02 → ['H02']` 误截成立 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys=`[title,project,version,last_updated,type,category,status,filled_by,last_chapter,track_chapter]`（无 `hooks` 键，本轮实测），hook 以 body 表格呈现（`| P0-4 (quiet阈结构) | RELEVANT→TRIGGERED … |`）；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)"；生产 book_spine.md 无 `| MH` 行（grep -c "^| MH" = 0）→ `_read_spine_master_hooks` 兜底也空 |
| F348 | **成立但机制描述部分不准（维持四/五轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入（本轮 load_contract 实证 group-character reads 含 truth/character_matrix.md）；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md——本轮实测生产两者均 MISSING）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层（:675-696）；非合规输出先失败一次再重试 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout（:1527/:1731/:1855） |
| F353 | **成立** | P1 维持，无异议 | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 writes 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进至 `_LAST_AUDIT_IDX+1`；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；consolidate 对失败结果 continue（:210-221）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；本轮 grep skills/ 契约 reads 0 个引用该文件；drafting 契约 reads `context/chapter-N-context.md`（load_contract 实证）→ 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断，audit_context_cache.py:65）每章每审计恒注入（幽灵输入 + token 浪费） |
| F358 | **成立** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError（:2034 raise，docstring "Pipeline paused for human review"）；drift-correction/warning/opening-variation 指令文件无任何消费者（grep 实证） |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra（hook content/state、rule content）被丢弃；drafting 契约 reads 不含 characters/*.md、world/rules.md（本轮 load_contract 实证）→ 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存，无写盘副作用）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358（CLI main）→ 每 15 章"重建"只产日志 |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |
| F364 | **成立** | **P1 维持；严重度异议：支持 P1→P0（依据决策表"数据丢失 + 契约被静默违反"；裁定留 stage 4）** | 本轮 /tmp 沙箱复证：`register_emergency_handlers` 后正常退出 → staging 被 atexit 清空（`staging before exit: True` → `STAGING GONE`）；且退出后 pipeline-state.json 的 current_step 被改写为 `EMERGENCY_SHUTDOWN_AT_unknown`（同根第二面，见〇.3）。消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交 → chapter-planning 无限循环）成立 |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交（`_commit_staging_for_checkpoint` 只在 review 时触发）+ 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`（:1136）——step 2 时当章 plan 尚不存在（由本步 dispatch 创建于 staging）→ FileNotFoundError → 捕获 → `_write_minimal_context_fallback`；且 chapter-planning 契约 reads 不含 context/chapter-N-context.md → 组装成功也不被消费 |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格（frontmatter 无 hooks 键，本轮实测）→ 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except（对比 cmd_next/cmd_review/cmd_resume 均捕 FileNotFoundError）；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `_parse_structured_output` → `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |

**误报统计**：F300-F370 中仅 F325 为误报（二轮撤销、三/四/五轮维持，本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：1 条——F364（P1→P0 支持：决策表 P0 触发条件"数据损坏/丢失；契约被静默违反导致错误执行"字面命中——人审产物静默作废 + 每次退出篡改持久化状态为 EMERGENCY_SHUTDOWN，属确定性数据丢失；但 staging 门控机制本身由人工 review 兜底、正文文件不丢，P1 亦可辩护，裁定留 stage 4）。另立场声明（均无异议）：F302（P1 维持，P0 依据存在留 stage 4）、F305（同意 P2）、F301（同意贴 P0 校准）、F324（P0）、F340/F341/F345/F353/F354（P1）。

---

## 二、新发现（F371-F372）

### F371 | closure step 10（shenbi-snapshot-manage）G4 校验恒失败：目录路径 `final-snapshot/` 被 generic G4 判 read_error/not_found → 重试×3 后 escalation → pipeline 永不 COMPLETED | error | **P1**（与 F324/F353 并列的第三条"pipeline 永不完成"独立根因，F324/F353 修复后即激活）

- 证据：
  - `closure.py:113-116`（CLOSURE_STEPS[10] `output_path="final-snapshot/"`——目录而非文件，且该目录由 step 10 自身创建，G4 校验时点其内容未知）；
  - `closure.py:148-159`（`_resolve_closure_g4_path`：`if not step.output_path: return ""` → `if "N" in step.output_path:`（"final-snapshot/" 无大写 N，不命中）→ `return step.output_path` 原样返回 `final-snapshot/`）；
  - `closure.py:260-265`（`g4_file = _resolve_closure_g4_path(...)` → `run_gate_g4(step.skill, [g4_file] if g4_file else [], ...)`——"final-snapshot/" 为 truthy → `["final-snapshot/"]` 传入）；
  - `closure.py:262-265` + `:162-193`（G4 FAIL → `_handle_closure_failure` 返回 True 则 `continue` 重试——**每次重试重新 dispatch shenbi-snapshot-manage LLM**；第 3 次后返回 False → run_closure_step 返回 False）；
  - `cli.py:287-304`（closure 失败 → `dispatch_escalation` + `set_checkpoint(ESCALATION)` → escalation 解决后 resume 重跑 step 10 → 同款失败 → 循环）。
  - gates/g4/ 目录无 snapshot-manage 专属 checker（ls 实证）→ 走 generic。
- 根因：closure 步骤表把 step 10 的输出声明为**目录路径**，但 `_resolve_closure_g4_path` 与 generic G4 checker 都按**单文件**语义处理；`final-snapshot/` 目录存在时 generic checker 按文件读取 → `G4.gen.read_error`，不存在时 → `G4.gen.not_found`，两种状态恒 FAIL。
- 验证命令+输出：
  ```
  uv run python -m shenbi.gates.cli G4 shenbi-snapshot-manage "final-snapshot/" /tmp/nonexistent-project
  # → {"gate": "G4-generic-gen", "status": "FAIL", "must_fix": ["G4.gen.not_found:final-snapshot/"]}
  mkdir -p /tmp/g4test/final-snapshot && touch /tmp/g4test/final-snapshot/snapshot.md
  uv run python -m shenbi.gates.cli G4 shenbi-snapshot-manage "final-snapshot/" /tmp/g4test
  # → {"gate": "G4-generic-gen", "status": "FAIL", "must_fix": ["G4.gen.read_error:final-snapshot/"]}
  # （目录已存在仍 FAIL；对照：G4 空文件列表 → PASS "no files"，但 closure 传的是 ["final-snapshot/"]）
  ```
- 影响：BOOK_CLOSURE checkpoint approve 后（cmd_resume 注释明示 "falling through to _orchestrate_to_checkpoint runs step 10"），closure step 10 每次执行都 dispatch 1 次 LLM（快照技能）+ G4 FAIL + 重试，3 次后 ESCALATION；escalation 解决后 resume 重跑同款循环 → **pipeline 永远停于 CLOSURE 阶段，transition_closure_to_completed（cli.py:283-284）不可达**。当前被 F324/F353 掩盖（CLOSURE 本身不可达），一旦前两者修复即确定性复现——closure 正常路径的功能错误。
- 建议方向：`_resolve_closure_g4_path` 对目录型输出（`output_path` 以 "/" 结尾或 step 10 特判）返回 `""`（跳过 G4）或改为校验 `final-snapshot/` 目录内至少一个产物文件存在（如 `final-snapshot/manifest.json`）；补"closure step 10 完成 → COMPLETED"集成测试。

### F372 | `_style_profile_is_stale` 章节计数被 pre-revision 备份文件污染：`chapter-*.md` glob 计入 `chapter-N-pre-rev.md` → style-learning 自愈触发提前/误触发 | error | P2（latent——当前被 F324/F353 屏蔽，触发块不可达）

- 证据：`triggers.py:103-124`（`_style_profile_is_stale`：`chapters_dir.glob("chapter-*.md")` 统计章节数，`chapter_count < 3` 才返回 False）；`chapter_loop.py:1844-1865`（`_create_pre_revision_backup` 复制 `chapters/chapter-N-pre-rev.md`）；`chapter_loop.py:1875`（该备份在 `_route_revision_after_resonance` 内**每章无条件**调用，F350 已报）；`check_triggers` :450-452（`if not r.style_learning and _style_profile_is_stale(...): r.style_learning = True`）。
- 根因：章节计数用 `chapter-*.md` 通配而非 `chapter-{N}.md` 精确匹配（对照 `_load_previous_titles` chapter_loop.py:2251 用 `chapter-(\d+)\.md` 正则排除了 pre-rev——本函数未做同样过滤）；F350 的备份文件名恰落入通配。
- 验证命令+输出：
  ```
  uv run python -c "…（/tmp 临时目录：2 个真实章 + 1 个 chapter-1-pre-rev.md，style_profile.md 含 bootstrap 标记）…"
  # → _style_profile_is_stale() = True（真实章节仅 2 章 < 3，应为 False）
  ```
- 影响：每章产生 pre-rev 备份后计数膨胀约 50%——真实章节 ≥2 时即触发 style_learning 自愈 → 提前 1 章+ 执行 shenbi-style-learning LLM dispatch（数据不足期跑风格学习，token 浪费 + 学习样本污染）；且与 F324/F353 修复后的周期触发叠加会多跑一次。属边界计数缺陷。
- 建议方向：`_style_profile_is_stale` 的计数改用 `re.match(r"chapter-(\d+)\.md", name)` 过滤（仅正文文件）；或统一抽一个"真实章节数"helper（与 `_load_previous_titles`、`_complete_chapter` 一致）。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件全量 deep-read，共 13,641 行）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries` 返回 set()；audits/ 722 文件 13 类后缀全旧式名、group-* 数=0；chapters/ 56 个 .md + 89 个 decisions .json、补零数=0、pre-rev 数=0；plans/chapter-5-plan.md section 7 为 `MH-001`/`H-N01`/`H-N02` 且操作全为 advance；truth/pending_hooks.md frontmatter 无 hooks 键、body 为 P0-* 表格；truth/book_spine.md `| MH` 行数=0 但 body 含 MH01-MH05 无连字符 id；novel.json keys 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0；style/style_profile.md 存在而 truth/world_rules.md、truth/style_profile.md、truth/volume_map.md 均缺失；genre-config auditDimensions 含 sensitivity/worldRules/dialogue/motivation/texture 等）；`uv run python` 实证 `read_volume_boundaries/requires_independent/get_active_genre_audits`、load_contract 契约 reads/writes/updates（group-*/resonance/lifecycle/state-settling/drafting/volume-outlining）；**G4 CLI 直接执行实验**（F371 的唯一新验证手段）；**/tmp 沙箱 atexit 退出实验**（F364 复证 + 新增 current_step 篡改面）。
- 前几轮覆盖空洞（本轮已补）：(1) **closure step 10 的 G4 目录校验**——前几轮只审了 N 占位替换（F313）与 step 15 空转（F303），从未把 closure 步骤表与 generic G4 的文件语义对照（五轮均以代码阅读为限，未实际执行 G4）→ 漏掉第三条"永不完成"根因；(2) **`_style_profile_is_stale` 计数污染**——前几轮只审了触发周期与配置旋钮（F366/F337），未对照 F350 的备份产物与计数通配；(3) **F364 的 current_step 篡改面**——五轮只报了 staging 清空，未验证 `_emergency_cleanup` 同时无条件改写持久化 current_step（本轮沙箱实证 `EMERGENCY_SHUTDOWN_AT_unknown`）。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 路径（codex/zcode）本环境不可执行，仅静态分析；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内），group skill 契约旧式 writes 是 F325 反证的关键。

---

## 四、汇总

- **新 finding 数**：2（F371 = P1 closure step 10 G4 恒失败——第三条"pipeline 永不完成"独立根因；F372 = P2 `_style_profile_is_stale` 计数被 pre-rev 备份污染，latent）
- **误报数**：0 新增（F300-F370 全部成立/维持；F325 维持二/三/四/五轮误报撤销，本轮独立复证；F348 维持"成立但机制描述部分不准"；F364 补一条同根面）
- **严重度异议数**：1（F364：P1→P0 支持，依据决策表"数据损坏/丢失 + 契约被静默违反"字面命中；裁定留 stage 4）
- **事实修正（不改严重度）**：1（F364 补强——`_emergency_cleanup` 正常退出时还无条件把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`，靠 cmd_resume healer 事后修复；与 staging 清空同根）
- **0 新 finding 判定**：**否** —— 本轮发现 1 条 P1 漏报（F371 closure step 10 G4 恒失败）与 1 条 P2 漏报（F372 计数污染）；F324（P0）维持成立、F325（误报）维持撤销、F340/F341/F345/F353/F354/F364（P1）维持成立。
- **校准升级提示**：F371 建议与 F324/F353 合并处置（三者为相互独立的"pipeline 永不完成"根因：解析器 / total_chapters 写入点 / closure step 10 G4——必须一并修复才能跑通整本书）；F372 建议并入 F350/F366 的清理族合并修复；F364 建议维持阶段 4 复核优先级前列。

---

## 五、独立复核声明

本报告为 fresh-context 第六轮独立复核：34/34 文件逐行重读；对 F300-F370 的判定全部基于本轮独立 read/grep/uv run python 实证与真实产物对照（未复用前五轮结论）；新增 2 条发现（F371/F372）均为前六轮报告未出现的观察，其中 F371 的验证手段（G4 CLI 直接执行）为前几轮未使用的新证据路径。
