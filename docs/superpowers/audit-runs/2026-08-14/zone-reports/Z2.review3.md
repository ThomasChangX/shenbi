# Z2 分区第三轮独立复核报告（Z2.review3）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）
- 模式: fresh-context 全量重读 38 文件；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖）；另读跨区消费面验证裁决（g1.py / g2.py / g3.py / g3_independence.py / dispatch_helper.py / audit/_shared.py / audit/write_audit.py / audit/snapshot.py / audit/record.py / chapter_loop.py / genesis.py / closure.py / triggers.py / round-exec.sh / first-novel.md / truth-files.yaml / tests/round-exec.sh 相关段 / git show 99f91f0:tests/dispatch-subagent.sh）。
2. **独立验证方式**：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前两轮一致）；十余个 `python -c` 运行时探针（F216/F218/F221/F230/F232 复现、F235 审计面错位端到端、F236 glob 匹配、F237 added 文件键集、F228 重试轮 G2、G1/G2 门序、ownership 越权）。
3. **前两轮判定**：F201-F234 共 34 条**全部实质成立，0 条整体误报**；F220 计数修正（5→6）成立；F211 表述修正成立；复核轮严重度异议（F214→P1、F218→P1、F217→P1 低置信）**经反证尝试后全部支持**（F214/F216/F218/F227 的 P1 判定均有运行时复现 + 反证不成立，见第三节）。
4. **本轮新发现 3 条（F235-F237）**，集中于两轮都未核查的审计链与契约剩余面：
   - **F235（P1）**：`dispatch_with_write_audit` 审计面错位 —— 审计快照 PROJECT_DIR（shenbi 仓库根）而非 round_dir/project_dir（技能实际写入处），codex.py:62 已把 cwd 改为 round_dir 而审计根未跟随（PR-20 翻译不一致）；仓库根现存文件命中契约 glob（`truth/*.md` → `truth/bridge_tracker.md`、`truth/character_matrix.md`，均 tracked）→ **任何 worldbuilding 派发（含完全成功的）返回 rc=2 GATE_FAIL**，且 round_dir 真实写入完全不被审计（写越权防护空转）。
   - **F236（P2）**：`audit/write_audit.py:_matches_declared` 对声明为 glob 的写入（`truth/*.md` 等 9 技能）**永不 fnmatch**（只查 exact + parametric_globs 的 patterns，不含 registry globs）→ glob 写入在审计中恒被判"未声明写入"；F235 修复后（审计面改对）此缺陷立即放大为 P1。
   - **F237（P2）**：`snapshot.compute_file_change` 对 **added** JSON 文件返回空 `changed_top_keys` → field 级 OWNERSHIP 键集校验对**新建文件**整体旁路（F233 record_create 的 field 级孪生盲区）。

---

## 一、复核结论表（F201-F234 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮独立验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 重读三模型 + g4 私有实现（g4/chapter_planning.py:99-102、context_composing.py:74-86、volume_outlining.py:59-72）；grep 无生产消费者；cov 34/38/40% 与报告吻合。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of 无消费者；sync_contracts.py:195 仍 json.loads。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无 import；g6.py:51 raw dict 读取 target_word_count（含 target_words 回退）属实 —— 死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797（"BLOCKING"/"FAIL"）、:409-438（G4Severity 自建词表）本轮重读复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 Literal 复制 status.py:17-24 且缺 UNIMPLEMENTED（status.py:24）。 |
| F210 | P2 | **成立** | hooks.py:4-6,26-27,31 引用 SKILL.md:72-73/87/120；实测 40/79-80/94，Δ32-40。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 零测试（cov missing）；g1/dispatch_helper 侧有测试 —— "零测试"仅指 executor 侧。 |
| F212 | P2 | **成立** | run_g1/run_g2（executor.py:111-139）无 returncode 检查；rc!=0 且 stdout 空 → 裸 JSONDecodeError。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | P2 | **成立；支持复核异议→建议 P1** | codex.py:20-48 不写 current_scorer_agent；dispatch_helper.py:1939-1940 唯一写入点为伪造 `pipeline-g3-scorer-<uuid>`（本轮重读 + grep 复证）。**反证尝试**：独立路径不跑 G3 故 P2？——pipeline 主路径（IDE/API）dispatch 不写 progress.json → run_gate_g3 伪造写入 → G3.4/G3.5 以生产方自写证据放行。反证不成立。 |
| F215 | P2 | **成立** | `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（复现）。 |
| F216 | P1 | **成立，P1 维持** | **运行时复现**：空 customRules + disabled dim → `GenreConfig.model_validate` PASS；子串启发 false-accept 复现。**反证尝试**（customRules 空时无真实禁用维度触发面）→ fixture genre-config-example.json 含 texture:False + 28 条规则，但空规则场景可构造且真实（新轮次 customRules 可空）→ 反证不成立。 |
| F217 | P2 | **成立；支持复核轮低置信异议（P2→P1）** | SKILL.md 内 5 处 CONSTELLATION 区间 + docstring 20-30 + 代码 15-35；g4 以模型强制 15-35 → 25-35% 过门但违反 SKILL.md:91 声明上限。权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持复核异议→建议 P1** | **运行时复现**：部分匹配 → 仅命中 section、matched=True、无 WARN；全缺 → 全文+WARN。**反证尝试**（AGENTS.md 只承诺"全部缺失"场景）→ 原文 "If a declared field is missing"（任一）→ 部分缺失亦应全文+WARN。反证不成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入真空 PASS 复现（g1.py:281-282 G1.0 SKIP + 整体 PASS）；executor.py:74-75/:107-108 ContractError 静默降级。增补点（_audit_watch_paths → []、_truth_file_set 缓存固化空集）本轮重读通过。**新增增补**：`_decisions_file_set`（executor.py:46-56）与 `_truth_file_set` 同构的 module 级缓存固化问题（bootstrap 失败 → 空集永久缓存），F219 增补只列了 _truth_file_set；`derive_file_type` 的 decisions 分支同样受影响。 |
| F220 | P2 | **成立（计数修正维持：6 条）** | ownership.py:73-94 实测 6 条目。**新增 M 观察**：foundation-review 条目声明 `read_keys=frozenset({"tropeInventory"})`（ownership.py:78-80），但全仓 grep `.read_keys` 无任何消费者（仅定义处）→ read_keys 为纯死声明（无读侧审计）。 |
| F221 | P2 | **成立** | fixture 9 键含 tropeInventory（复现 `model_validate` PASS 且 tropeInventory 被静默丢弃）；SKILL.md:286/:290 声称 8 键；OWNERSHIP 9 键；模型 8 字段。四源漂移确认。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性）/g1（glob 未匹配）/dispatch_helper:382（设置者）三处语义分叉；executor 对 glob 读路径在宽泛 skip 下整条丢弃的增补本轮重读通过。 |
| F223 | P2 | **成立** | progress.json 损坏 → 裸 JSONDecodeError（codex.py:28-29 复现）；:114/:116 二次解析；缺 final_score 静默 0。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` 唯一命中 g3.py:153（读取）；codex.py:40-46 写 `{score,status}` 无 output_files → G3.3 永 SKIP（g3.py:187）。 |
| F225 | P2 | **成立** | dispatcher/ 无 read_fields/filter_to_fields 引用；消费点仅 dispatch_helper.py:592,1229-1230。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 硬 raise；cli main 无 try/except。 |
| F227 | P1 | **成立，P1 维持** | **运行时复现**：fresh round（tmp 轮次 + 真实 worldbuilding 契约）→ G1 FAIL（novel.json 缺失）、G2 FAIL（6 输出 G2.1 not-found，含 `truth/*.md` 字面路径）；CLI 实跑 `shenbi-dispatch shenbi-worldbuilding generative <tmp> "Build the world"` → g1_failed rc=1。`git show 99f91f0:tests/dispatch-subagent.sh` 注释 "the skill execution happened earlier" 证实原语义为校验**已产出**输出；Python 翻译把技能执行并入 G2 之后的 dispatch_codex（codex.py:61-66）→ 门序回归。**反证尝试**（G1 失败也许只是缺 novel.json 的预置问题）→ first-novel.md:38-41 文档化流程 round-exec.sh 只建脚手架（progress.json/meta.json/summary.json/子目录），不产 novel.json 或任何技能输出 → 第一步必然 G1/G2 失败。反证不成立。P1（文档化流程正常路径第一步失败）维持。 |
| F228 | P2 | **成立（新提低置信严重度异议 P2→P1，见第三节）** | **重试轮复现**：底层文件已存在（novel.json + truth/current_state.md 等），`gate_G2(derive_output_files(...), "chapter", tmp)` 仍 FAIL —— `*.md` 字面路径 G2.1 not-found 与 F229 的 JSON 误套规则叠加；`derive_output_files` 返回 `*.md` 字面（不展开 glob）。9 技能 G2 恒败含重试轮 → 正常路径可复现功能错误，但受 F227 掩盖（fresh 轮先死于 G1），故低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现；`gate_G2([genre-config-example.json], "chapter", tmp)` → FAIL（G2.8/G2.9）；`gate_G2([novel-example.json], "chapter", tmp)` → FAIL；g2.py:76-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文校验静默缺失。 |
| F230 | P2 | **成立** | **复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；md 路径 match_field 规范化命中；`_filter_json:76` 裸 `k in fields`。复核轮"md 与 json 同构"断言证伪成立。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + 两个 SKILL.md 命中；fixture 9 键无两者；模型 8 字段无两者。消费侧引用悬空确认。 |
| F232 | P2 | **成立** | **复现**：`Selection(severity="high", basis="manual_override", rationale="")` → ACCEPTED；`Adjustment(rationale="")` → ACCEPTED；routine+low 空串被拒（`has=True` → FORBIDDEN）——两侧语义不对称。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD` 声明后未接线；复现 `check_write_ownership(plant, added)` → []。 |
| F234 | P2 | **成立** | compute_file_change(pre!=None, post=None) → status="deleted" 无键信息；三 level 分支均不看 status；owned 文件删除零违规复现。 |

**误报统计**：整体误报 0 条；部分误报 1 条（F220 计数 5→6，实质成立）；表述修正 1 条（F211，实质成立）。前两轮 34 条全部实质成立。

---

## 二、新发现（F235-F237）

### F235 | dispatch_with_write_audit 审计面错位：快照 PROJECT_DIR（shenbi 仓库根）而非 round_dir/project_dir —— 成功派发返回 rc=2 GATE_FAIL + 写越权审计对真实写入盲区 | 漏报（功能错误/审计失效） | P1
- 证据: `src/shenbi/dispatcher/executor.py:25-26`（`PROJECT_DIR = REPO_ROOT`，模块常量 = shenbi 仓库根）；`:244/:264`（`snapshot_tree(PROJECT_DIR, watch)`）；`:220-225`（`_audit_watch_paths` → `derive_output_files(skill, chapter)` **不带 round_dir** → 项目相对路径）；`:228-268`（`dispatch_with_write_audit` 对**所有** CLI 派发包裹审计）；`src/shenbi/dispatcher/modes/codex.py:62`（`codex exec -C round_dir` —— 技能写入 round_dir，PR-20 翻译把原 shell 的 `-C PROJECT_DIR` 改为 `-C round_dir`，审计根未跟随）；`tests/unit/dispatcher/test_executor_audit.py:18,40`（测试把 `ex.PROJECT_DIR = tmp_path`（== round_dir）才能通过 → 测试编码了"审计根应为写入处"的正确语义，生产从未赋 round_dir）
- 根因: PR-20 翻译把技能执行 cwd 从 PROJECT_DIR 改为 round_dir（codex.py:62），但审计链（executor.py:244/264）仍快照 PROJECT_DIR = shenbi 仓库根。结果：(a) **round_dir 真实写入完全不被审计**（写越权检测空转——技能写任意文件不触发）；(b) 仓库根现存文件命中契约 glob（`truth/*.md` → `truth/bridge_tracker.md`、`truth/character_matrix.md`，两者均为 git tracked 文件）→ pre/post 均含 → `_matches_declared` 失败（F236）→ "未声明写入" 违规 → **rc=2 GATE_FAIL**。实测任何 worldbuilding 派发（即使 dispatch 返回 0、零写入）→ rc=2。
- 验证命令: `PYTHONPATH=src .venv/bin/python`（PROJECT_DIR 保持真实仓库根，fake dispatch 返回 0）→ `write_audit_gate_fail violations=['未声明写入: truth/bridge_tracker.md（…）', '未声明写入: truth/character_matrix.md（…）']`、`rc = 2`；`git ls-files truth/` → bridge_tracker.md + character_matrix.md 均 tracked；`git show 99f91f0:tests/dispatch-subagent.sh` → 原 shell `codex exec -C "${PROJECT_DIR}"`（写入仓库根，与审计一致）
- 建议方向: `dispatch_with_write_audit` 快照 `round_dir`（round-relative watch，与 G1/G2/derive_output_files 一致）；或把 codex cwd 改回 PROJECT_DIR 并与 G2 对齐（两条路选一，避免三处语义分裂）；补一条真实 round_dir 写入 + 仓库根现存文件的集成测试（现 test_executor_audit 全 mock PROJECT_DIR=round_dir，掩盖了生产差异）

### F236 | audit/write_audit.py `_matches_declared` 不 fnmatch 声明为 glob 的写入（`truth/*.md` 等 9 技能）→ glob 写入恒判"未声明写入" | 漏报（跨区：audit/ 被 executor 审计链消费） | P2
- 证据: `src/shenbi/audit/write_audit.py:29-36`（`_matches_declared`：`pat == relpath` 或 `globs.get(pat)` 且 fnmatch —— `globs = parametric_globs()`（snapshot.py:19-29，仅 registry **patterns** 的 parametric→glob 映射），**不含 registry globs**（truth-files.yaml `globs` 列表如 `truth/*.md`））；`src/shenbi/audit/_shared.py:38-56`（`derive_output_files` 返回契约 writes+updates 字面，含 glob）
- 根因: 契约声明 `truth/*.md`（registry glob）时，`globs.get("truth/*.md")` 为 None（parametric_globs 只含 parametric→glob），fnmatch 分支永不执行 → 任何 glob 命中文件（`truth/current_state.md` 等）均返回 False → file-level 审计违规。与 F228（G2 侧 glob 不展开）同根不同消费面：G2 侧漏校验，审计侧**误拒**。9 技能（worldbuilding/character-design/sequel-writing/truth-sync 等）受影响。
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `_matches_declared("truth/bridge_tracker.md", [...,"truth/*.md"], parametric_globs())` → `False`（直接 `fnmatch("truth/bridge_tracker.md","truth/*.md")` → `True`）；`"truth/*.md" in parametric_globs()` → `False`；对照 `_matches_declared("plans/chapter-5-plan.md", ["plans/chapter-N-plan.md"], globs)` → `True`（parametric 正常）
- 建议方向: `_matches_declared` 增加 `fnmatch.fnmatch(relpath, pat)` 分支（对声明自身是 glob 的情形）；与 F235 联动修复（F235 修面、F236 修匹配，缺一不可；F235 修复后 F236 立即升级为 P1 级误拒）

### F237 | compute_file_change 对 added JSON 文件返回空 changed_top_keys → field 级 OWNERSHIP 键集校验对新建文件整体旁路 | 漏报（审计盲区，F233 field 级孪生） | P2
- 证据: `src/shenbi/audit/snapshot.py:99-100`（`if pre is None and post is not None: return FileChange(relpath=relpath, status="added")` —— 不计算顶层键）；`src/shenbi/contracts/ownership.py:114-119`（field 分支只查 `changed_top_keys` ⊆ write_keys）；`src/shenbi/audit/write_audit.py:57-64`（`get_ownership` 命中则跳过 file-level 兜底）→ 新建 genre-config.json 携带任意顶层键（如 `title`/`hacked`）零违规
- 根因: F233 覆盖 record_create（markdown 记录）新增键集盲区；field 级（JSON）的 added 文件同样不校验键集——`changed_top_keys` 仅在 modified（pre/post 均非 None）时经 `_changed_top_keys` 计算（snapshot.py:105-108），added 直接短路。genre-config.json 首次创建即绕过 9 键集声明（_GENRE_KEYS）。
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `compute_file_change("genre-config.json", None, '{"hacked": true}')` → `FileChange(status='added', changed_top_keys=(), ...)`；`check_write_ownership("shenbi-genre-config", added)` → `[]`（对照 modified + `title` 键 → 违规命中）
- 建议方向: `compute_file_change` 对 added JSON 也计算顶层键（pre 为空 dict 基线）；或 check_write_ownership field 分支对 status=="added" 校验 `post` 键集 ⊆ write_keys；补 added 用例测试

### M 级观察（随文记录）
- `ownership.py:78-80` foundation-review `read_keys={"tropeInventory"}` 全仓零消费者（仅定义处）→ read_keys 死声明（F220 增补）
- `executor.py:46-56` `_decisions_file_set` 与 `_truth_file_set` 同构的 module 级缓存固化（F219 增补仅列 _truth_file_set，decisions 孪生未列）
- `g3_independence.py:3` docstring 称 "g3.py:144-162 现状 fail-open"，实际 g3.py:194-206 已 fail-closed 接线（行号/状态过期，M）
- F235 关联观察：`internal.py:22-23` 错误消息指引 "Set SHENBI_LLM_API_KEY to use API mode"（API mode 不可达，review2 已列）——本轮在 F235 语境下复核：executor.detect_mode 仅 codex/internal，API 路径在 pipeline 侧（dispatch_helper `_dispatch_via_api`），消息对独立 CLI 用户误导（维持 M）

---

## 三、严重度异议（不修改前两轮，仅提理由）

1. **支持复核轮异议 F214 P2 → P1**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent` + `scoring_history` → G3.4（fail-closed）与 G3.5 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。本轮反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径（IDE/API dispatch 不写 progress.json）真实触发伪造。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持复核轮异议 F218 P2 → P1**：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 / _filter_json:76-80 同构）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持复核轮异议 F217 P2 → P1（低置信）**：g4/pacing_design 以模型强制 CONSTELLATION ∈ [15,35]，SKILL.md:91 声明 15-25% → 25-35% 输出过门但违反技能自身契约；SKILL.md 内部另有 :176/:187/:193 多处自相矛盾，权威区间未定，故维持低置信。
4. **新提（低置信）F228 P2 → P1**：重试轮（输出已存在）`derive_output_files` 仍返回 `truth/*.md` 字面 → G2.1 恒 not-found，9 glob 写技能在任何轮次（含重试）G2 恒败；fresh 轮被 F227 掩盖，故降为低置信供协调者裁决（若 F227 修复后 F228 独立阻塞 9 技能正常路径，则升 P1）。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**；运行时探针清单（全部在 /tmp 与 python -c，零仓库写入）：
  - F216 空 customRules+disabled dim → PASS（bug 复现）；子串启发 false-accept 复现
  - F218 md 部分匹配 → 仅命中 section、matched=True、无 WARN；json 同构
  - F230 json 键规范化缺失 → matched=False + 全文 + WARN（U+3000 声明键）
  - F232 Selection/Adjustment 空串 rationale → ACCEPTED；routine+low 空串被拒（不对称）
  - F227 fresh round → G1 FAIL（novel.json）+ G2 FAIL（6 输出 G2.1，含 `*.md` 字面）；CLI 实跑 → g1_failed rc=1
  - F228 重试轮（底层文件已存在）→ G2 仍 FAIL（`*.md` 字面 not-found + JSON 误套 chapter 规则）
  - F235 审计面：真实仓库根 + fake dispatch rc=0 → rc=2（violations 为仓库根 tracked truth 文件）；`git ls-files truth/` 确认 tracked
  - F236 `_matches_declared` glob vs parametric 对照（glob False / parametric True / 直接 fnmatch True）
  - F237 added JSON 文件 → check_write_ownership 零违规（对照 modified 命中）
  - F221 fixture 9 键 model_validate PASS + tropeInventory 静默丢弃
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F227 cwd 差异为 git-show 证据 + 代码路径推理，未实机验证）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 `python -c`。

---

## 五、统计

- 新 finding：**3 条**（F235-F237：P1×1、P2×2）
- 误报：**0 条整体误报**（F201-F234 共 34 条全部实质成立；F220 部分误报/计数修正维持；复核轮"md 与 json 同构"断言经 F230 证伪系复核轮陈述错误，非 finding 误报）
- 严重度异议：**4 条**（支持复核轮 3 条：F214→P1、F218→P1、F217→P1 低置信；新提 1 条低置信：F228 P2→P1）
- 前两轮 findings 判定：F201-F226 26 条 + F227-F234 8 条 = 34 条，全部实质成立、0 条整体不成立
- **0 新 finding 判定：否**（F235-F237 共 3 条新发现；F235 为 P1 级审计失效 + 正常路径 GATE_FAIL，与 F227 同链但独立于门序：F227 修复后 F235 仍在）
