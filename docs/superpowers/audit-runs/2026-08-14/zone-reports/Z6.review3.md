# Z6 分区第三轮独立复核报告（fresh-context 全量重读）

> 复核人：第三轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11 findings（F601-F611）+ 第一轮 8（F612-F619）+ 第二轮 10（F620-F629）逐条独立验证；新发现编号 F630-F636。
> 方式：49/49 文件全文 read + `PYTHONPATH=src python3` 实际运行验证 + grep 全仓调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong）交叉核对 + 计划文档（07-19-04/07-19-07）契约核对。全部运行验证在 /tmp 或纯内存完成，未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未达成**（新发现 7 条，均 P2）。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §3）
- 初审 11 findings：**11/11 成立**（无整条误报，无严重度异议）
- 第一轮 8 findings：**8/8 成立**
- 第二轮 10 findings：**10/10 成立**
- 新发现：**7**（F630-F636，P2 × 7）
- 误报数：**0**；严重度异议数：**0**
- 判定：**非 0 新 finding（7 条），未达收敛目标**
- 关键独立验证结论：
  - 四条 P1（F601/F602/F612/F620）全部**实际运行/代码+计划文档双重复核成立**，无反证（见 §1 判定表）；
  - F604/F605/F606/F608/F610/F613/F614/F615/F619/F621/F622/F623/F625/F627 均实际运行复现；F602/F617/F618/F624/F626/F629 全仓 grep 复现；
  - 本轮新增 7 条 P2，全部集中在**前三轮未覆盖的方向**：跨文件阈值漂移（F630/F632）、config 治理绕过新向量（F631）与治理层整体未接线（F635）、统计口径（F633/F634）、detect_drift 触发语义（F636）。

---

## 1. F601-F629 判定表（逐条独立复核）

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（P1 维持，无反证） | 运行：baseline dialogue=10 → current=1 → `max(…,5.0)=5.0` 不满足 `>5.0` → is_drift=False, severity=NONE（本轮实测输出 `F601: dialogue collapse -> is_drift: False | severity: NONE`）。linguistic_drift.py:215/218 代码确认；docstring :185-188 明确承诺 "<20% baseline" 触发。**反证核查**：无任何路径使纯对话塌陷置 is_drift=True（`max_deviation_ratio` 恒为 5.0）；仅当其他指标已 >5x 时才触发，与"对话塌陷本身触发"的契约无关。P1 成立（当前被 F602 无 baseline 掩盖，属潜伏，不影响本体）。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持，无反证） | `grep establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py` re-export；skills/、justfile 零引用；`find . -name linguistic_baseline.json` 0 命中。计划 07-19-07 Task 5a（:911-930）原文："Wire this into `_run_context_assembly` or chapter_loop so it runs once after chapter 3 completes" —— **显式接线契约未履行**。chapter_loop.py:2014-2019 无文件 → `log.warning("no_linguistic_baseline")` + return None，:2722 每章走此分支。P1 成立。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链确认：audit/write_audit.py:51-56（同文件 YAML vs 派生表）→ record.py:24 `blocked = bool(violations) or bool(drift)` → block ship。test_drift.py 6 用例复核：仅 md-ghost（:33-39）/值不匹配/float 0.80 方向，无 YAML-ghost 反向用例。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'`，:226 `sum(text.count(c) for c in chars)`。`'他说"你好"。'` → count=4（应为 2）。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持；review1 影响修正正确） | 运行（monkeypatch load_drift_config 返回空 system_terms）：`compute_linguistic_metrics('参数系统阈值。'*5)` → `system_term_density: 1028.5714`。`"|".join([])==""` → `re.findall("", text)` 全位置匹配。review1 修正（被 is_drift 门控、无管道暂停）复核同意；且 F620 进一步证明即使 severity 成立也不暂停。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过。下游影响链确认：state.py:360 `PipelineConfig(**{...})` 纯 dataclass 构造**无类型校验**，str "58" 直接进入 int 字段 → chapter_loop.py:1897 `check_resonance(score, "58")` → `score >= "58"` TypeError（revision_router.py:116）；G0 侧 `"58" < 60`（g0_config_coherence.py:75）同样 TypeError。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs detect_drift 读 `dialogue_density`；:296/:335 硬编码术语 vs :107 config 驱动。补充核实：`_load_baseline`/`check_linguistic_drift` 生产零调用（仅 tests/pipeline/test_linguistic_drift.py）——context/ 平行实现本身也是死路径。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | cjk.py:54 桶 token 为相邻双字符 `""`/`''`/`「」`/`『』`，正常文本引号包围内容不相邻 → count 恒 0；:61-62 "whole token" 修复对引号桶反而永久为 0。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py:40-43 两处 break 无任何日志，:47-48 `safe_write(path, raw[:keep_chars])` 改写文件。test_replay.py:18-35 断言截断行为但不断言日志。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无防御 → 单条脏数据整批崩溃。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持；review1 证据细化正确） | config_coherence.py:109 `key.startswith("auditDimensions.")`；整键对象覆盖绕过（review1 实测 ACCEPTED；更深嵌套实测 `_set_nested` TypeError 崩溃而非绕过）。P2 成立。**注意**：本轮发现该规则还有第二个绕过向量（falsy 值，见新 F631）。 |

### 1.2 第一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持，无反证） | 运行：baseline stm=60 → current stm=110 → `is_drift: False | severity: ESCALATE`。chapter_loop.py:2023 `if result.is_drift:` 包裹 log + ESCALATE raise + HARD/WARN 注入全部干预（:2024-2043）。计划 07-19-07:749 "3-tier intervention should fire when system term density exceeds thresholds" —— 绝对阈值触发是契约。P1 成立。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"破折号": "——"`/`"省略号": "……"` 重复字符 + :226 按字符迭代 → 一个 `——` 计 4。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | 运行：`{'some.new.key': 1, 'resonance_global_floor': 58}` + 60 字 rationale → ConfigError；genre-config.json 无 `some.new.key`（config 未落盘）但 config-change-log.jsonl 已有 1 条 `"key"` 记录（幻影条目）。config_coherence.py:105-133 循环内逐 key `_append_audit_trail`，`safe_write` 在循环外 :142。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | 运行：合法行 + 撕裂行（`{"seq": 2, "torn`）→ `TraceWriter(d2)` 抛 JSONDecodeError；同文件 `replay` → 1 事件自愈。writer.py:57 `json.loads(lines[-1])` 无 try。补充核实缓解面：safe_write.py:127-135 try/except 吞掉（事件被静默丢弃——F615 影响面的直接后果）；audit/record.py:42-46 catch (OSError, ValueError, TypeError)；migrate.py 先 replay。P2 成立。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤；权威 YAML 缺行 → drift.py:84 假 drift（md 有行）或静默漏报（md 无行）。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | grep 全仓：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。计划 07-19-07:7 承诺 ">3σ frequency-divergence alarm as a second-tier check"。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` compute_stats.py → 仅 :37 定义一处，:250-281 detect_rhetoric 使用内联另一组 regex。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | 运行 `sys.argv=['compute_stats.py','a.md','--output']` → IndexError 裸崩溃。compute_stats.py:374-377。P2 成立。 |

### 1.3 第二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持，无反证） | 代码精读确认：chapter_loop.py:2722-2727 `try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed")`；函数内 :2031-2036 raise DriftEscalationError（"Pipeline paused for human review"）；:2716 注释自称 "non-blocking"。调用点与函数内契约自相矛盾，且 `except Exception` 连带吞掉其他真实错误。grep `_check_linguistic_drift` 唯一生产调用点即 :2722。P1 成立。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | 运行：`compute_entropy([f"X{i}" for i in range(10)])` → 0.0 → "严重单调"；10 个已知 pattern → 3.3219 → "优秀"。compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | 运行：`last_reinforced='3'` → `TypeError: unsupported operand type(s) for -: 'int' and 'str'`。recall.py:41-46。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持） | 运行：`count_ai_markers('他不是……而是默默走开了。')` → {}（双省略号真实文本 0 命中）；单省略号非标准写法才命中。compute_stats.py:52/288。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 | P2 | **成立**（P2 维持） | 运行核对：skills/ 目录 74 个，master.json skills 59 项，缺失 15 个（shenbi-anchor-curate、book-spine-init、escalation-review、foreshadowing-lifecycle、foreshadowing-recall、memory-distill、review-arc-payoff、review-group-*×4、review-resonance、score-arc、score-stratum、score-volume）。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | 运行：`migrate_to_current(SimpleNamespace(schema_version=0))` 于 1.5s 后线程仍存活（infinite loop 确认）。versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity); e = up(e)`。当前潜伏（CURRENT_VERSION=1、无生产调用方）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py:7,19,29` re-export，src 内无生产调用。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | 运行：分数表(90/85/80) + "## 附录" 第二表(50/40) → `overall` 序列 = [90.0, 85.0, 80.0, 50.0, 40.0]（附录行混入）。compute_drift.py:189-209 无 `## ` 标题/空行终止条件；对比 records/drift.py:38-39 非 `|` 行即 break。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | compute_stats.py:16 SENT_ENDS 含 `；`，:57-75 segment_sentences 只按 `。！？\n` 切句，:87 segment_paragraphs 用 SENT_ENDS 计句 → 含分号文本两函数结果矛盾。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用；test_revision_routing.py:96-153 有测试。P2 成立。 |

**严重度异议：0 条。** 四条 P1（F601/F602/F612/F620）均属"计划文档显式承诺的干预/安全网被实现静默失效"，与决策表 P1 档（违反显式契约）吻合，本轮逐条核查无反证（尤其 F602 的 Task 5a 原文接线指令、F612 的 "should fire when system term density exceeds thresholds"、F620 的 raise 与调用点吞异常的契约矛盾）；其余 P2 均属边界/统计口径/死代码档，无高判或低判。

---

## 2. 新发现（F630-F636）

### F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源阈值 65 漂移（E11 缺陷类复活） | 跨文件状态一致性 | P2
- 证据：`src/shenbi/pipeline/revision_router.py:62-64`（注释 "Default resonance global floor (spec §6.3, config.resonance_global_floor)." 后跟 `DEFAULT_RESONANCE_FLOOR = 50`）；`src/shenbi/config/thresholds.py:30`（`resonance_global_floor: int = 65`）；thresholds.py:5-10 docstring（"previously duplicated … state default 50 vs. the audit skill's internal 65 floor (E11) … every consumer reads the same value"）；计划 07-19-04（"state default floor is raised (50 → 65)"）
- 根因：E11 的根因正是 "state default 50 vs 65"，thresholds.py 建立单源后 chapter_loop.py:1897 已传显式 floor（state.config 默认 65），但 revision_router.py:64 的独立默认常量**仍停留在修复前的 50**，且注释明确声明它就是 "config.resonance_global_floor" —— 单源声明被同仓另一常量违反。当前潜伏：`check_resonance(score, floor=DEFAULT_RESONANCE_FLOOR)`（:108）与 `decide_revision(..., resonance_floor=DEFAULT_RESONANCE_FLOOR)`（:123）在生产中均被显式传参覆盖（decide_revision 甚至无生产调用），但任何未来依赖默认值的调用点会让 50-64 分的章节静默通过，复现 E11。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "from shenbi.pipeline.revision_router import DEFAULT_RESONANCE_FLOOR; from shenbi.config.thresholds import DEFAULT_THRESHOLDS; print(DEFAULT_RESONANCE_FLOOR, DEFAULT_THRESHOLDS.resonance_global_floor)"
  -> 50 65
  ```
- 建议方向：删除 revision_router.py 的独立常量，改为 `from shenbi.config.thresholds import DEFAULT_THRESHOLDS` 引用（或显式别名 + 断言相等）；补单测（无显式 floor 调用 check_resonance 时用 65）。

### F631 | config 治理规则 1 与 G0 均用 `is False` 严格相等：`0`/`null`/`""` 等 falsy 值绕过 rationale 与 G0 检查且实际禁用审计 | 治理绕过 | P2
- 证据：`src/shenbi/config/config_coherence.py:109`（`key.startswith("auditDimensions.") and new_value is False`）；`src/shenbi/gates/g0_config_coherence.py:94`（`audit_dims.get(dim, True) is False`）；`src/shenbi/pipeline/audit_layer.py:112-113`（`audit_dims.get(dim_key, False)` —— 任何 falsy 值即不激活该审计）
- 根因：F611 覆盖"整键绕过"，但**值类型绕过**是独立向量：`is False` 只拦字面 `False`，而 `0`/`None`/`""` 同样让 audit_layer 的激活判断（falsy → 不激活）关闭关键审计维度，却同时通过 config_coherence 的 Rule 1（无 rationale 要求）与 G0 的 `is False` 检查（不报 G0.cc.critical_audit_disabled）——写侧、读侧两道治理对 falsy 值同时失明。
- 验证命令+输出：
  ```
  update_genre_config(d, {'auditDimensions.texture': 0}, rationale='x') -> ACCEPTED（无 ConfigError）
  check_config_coherence(d, resonance_global_floor=65) -> []（G0 不报）
  get_active_genre_audits({"auditDimensions": {"texture": 0}}) -> texture 审计不激活（等效禁用）
  对照组：{'auditDimensions.texture': False} + 短 rationale -> ConfigError（正确拦截）
  ```
- 建议方向：Rule 1 与 G0 改为 `new_value is False or new_value in (0, None, "")`（或统一改为 truthiness 判断并与 audit_layer 激活语义对齐）；补单测（0/null/"" 三种 falsy 值必须拒绝或必须报 G0 issue）。

### F632 | linguistic_drift severity 阶梯硬编码 30/50/100 与 thresholds.py 单源阈值重复且不一致风险（"single source of truth" 声明被违反） | 跨文件状态一致性 | P2
- 证据：`src/shenbi/skill_utils/drift_detection/linguistic_drift.py:222-229`（`stm_density > 100 → ESCALATE`、`> 50 → HARD`、`> 30 → WARN` 字面量）；`src/shenbi/config/thresholds.py:36-39`（`system_term_density_warn: int = 30`、`system_term_density_hard: int = 50`，注释 "warn → G4 WARN, hard → G4 FAIL"）；thresholds.py:5-10 docstring（"Importing thresholds from here is the canonical path; every consumer … reads the same value"）
- 根因：同一概念（system-term 密度阈值）存在两套定义：thresholds.py 是声明中的单源，linguistic_drift.py 用字面量复制（30/50 重复 + 100 仅存在于 linguistic_drift 与计划 07-19-07:569）。当前值恰好一致，但单源承诺形同虚设——任一阈值调整（如 G4 提到 40/60）后 linguistic_drift 不会跟随，WARN/HARD 干预边界与 gate 边界静默分裂。escalation/check.py:31-33/60-63 的 window=5/slope=-2.0/regen_loop_limit=3/arc_threshold=70 同理（无单源）。
- 验证命令+输出：代码精读 + `grep -n "> 100\|> 50\|> 30" src/shenbi/skill_utils/drift_detection/linguistic_drift.py` → :222/:224/:226 三处字面量。
- 建议方向：linguistic_drift 的 severity 阶梯消费 `DEFAULT_THRESHOLDS.system_term_density_warn/hard`（ESCALATE 阈值在 thresholds.py 补一个字段或显式常量），删除字面量；补一致性测试（修改 thresholds 后 severity 阶梯跟随）。

### F633 | segment_paragraphs docstring 声称支持单换行分段，实现只按空行分割 → 单换行章节被当作 1 个巨段落且换行被计为句末 | 文档↔代码漂移 | P2
- 证据：`src/shenbi/skill_utils/style_learning/compute_stats.py:78-80`（docstring "Segment text into paragraphs by double-newline or single-newline boundaries."；实现 `re.split(r"\n\s*\n", text)` 只按双换行/空行分割）；`:87`（`SENT_ENDS.finditer(p)` 中 `\n` ∈ SENT_ENDS :16）
- 根因：实现与 docstring 矛盾——单换行（markdown 每段一行的常见格式）不会触发分割；且由于 `\n` 在 SENT_ENDS 中，未分割的段落内每个换行被计为一句末 → sentences_per_paragraph 虚高。当前真实数据（novel-output/xinghuo-ranqiong/chapters/chapter-1.md：156 行、70 空行）用空行分段所以未触发，但任何单换行分段的章节（LLM 输出常见）会得到 1 个"巨段落"+ 句数虚高的统计。
- 验证命令+输出：
  ```
  segment_paragraphs('第一句。\n第二句。\n第三句。') -> 1 个段落，sentences=5（3 句实句 + 2 个 \n 被计句末）
  segment_paragraphs('第一句。\n\n第二句。\n\n第三句。') -> 3 个段落（空行格式正确）
  ```
- 建议方向：统一分段语义（决定单换行是否分段并修正 docstring，或实现与 docstring 对齐）；句末集合与 segment_sentences（:63 只 `。！？\n`）统一，避免 `；`/`\n` 双口径（与 F628 同修）；补单测（单换行文本两函数结果一致）。

### F634 | compute_linguistic_metrics / compute_stats 把 pipeline 自产的 META 指令块当章节正文统计 → 密度稀释 + 指令文本进计数 | 统计口径 | P2
- 证据：`src/shenbi/skill_utils/drift_detection/linguistic_drift.py:94-131`（对整文件文本计算 5 指标）；`src/shenbi/skill_utils/style_learning/compute_stats.py:311-362`（read_chapters 整文件 + compute_all_stats）；真实章节 `novel-output/xinghuo-ranqiong/chapters/chapter-1.md`（`<!--META-BEGIN-->`~`<!--META-END-->` 指令块约 515 字符 / 全文 4177 字符 ≈ 12%，且 META 内含 1 个 `——` 与 4 个 `"`）
- 根因：两处统计都把 pipeline 预写指令块（META，非 LLM 产出的章节正文）计入分母与计数：分母膨胀 ~12% 稀释所有密度（实测 em_dash_density 全文 5.9852 vs 去 META 6.5538，偏差 8.5%）；META 指令文本中的 `——`/引号直接进计数。系统术语密度、对话密度等告警指标因此不是纯章节正文度量。META 由 chapter_loop 写入（任何章节都存在），故影响普遍存在；当前因 F602（无 baseline）未参与决策，但一旦接线即带病测量。
- 验证命令+输出：
  ```
  compute_linguistic_metrics(全文)  -> system_term_density 0.2394 | em_dash_density 5.9852 | total_chars 4177
  compute_linguistic_metrics(去META) -> system_term_density 0.2731 | em_dash_density 6.5538 | total_chars 3662
  ```
- 建议方向：两个计算入口剥离 `<!--META-BEGIN-->…<!--META-END-->`（或统一用正文提取函数）；补单测（含 META 指令块的章节指标与纯正文一致）。

### F635 | 整个 config 治理层（update_genre_config）零生产调用：docstring "Every change flows through" 为假，genre-config 由 skill 直写，audit trail 仅一次人工修复产物 | 死代码/未接线 | P2
- 证据：`src/shenbi/config/config_coherence.py:6-14`（docstring "Every change to genre-config.json … flows through :func:`update_genre_config`"）；全仓 `grep update_genre_config` → 仅 config_coherence.py 自身 + 测试 + 计划文档（计划 07-19-04 Task 7 曾用于一次性修复 E34）；`skills/shenbi-genre-config/SKILL.md`（:204 "写入 genre-config.json" 直接写，无调用；:300 治理规则为 prompt 层 "false 维度必须在 customRules 有理由"）；`novel-output/xinghuo-ranqiong/config-change-log.jsonl` 仅 1 条 2026-07-19 的 texture re-enable 记录（E34 一次性修复产物）
- 根因：写侧治理（rationale≥50 强制、floor≥60 强制、JSONL 审计追踪）从未接入任何生产路径：genre-config skill 直接写文件（LLM + prompt 规则 + G4 AUTO-CHECK + G0 读侧检查），config_coherence.py 只是被测试与一次人工脚本使用的孤立模块。后果：(a) docstring 承诺的治理路由不存在；(b) config-change-log.jsonl 审计账本在生产中不会随配置变更更新（F614 的幻影条目问题因此实际影响面为零，但账本也从不记录真实变更）。
- 验证命令+输出：
  ```
  grep -rn "update_genre_config" src/ skills/ scripts/ tools/ justfile --include=*.py --include=*.md --include=*.sh --include=justfile
  -> 仅 src/shenbi/config/config_coherence.py（定义+docstring）与 tests/（测试）与 docs/plans（计划）
  ```
- 建议方向：二选一——(a) 将 update_genre_config 接入 genre-config skill 的写入流程（skill 通过它落盘，治理+审计生效）；或 (b) 若设计为"prompt 规则 + G4/G0 检查承担治理"，则删除该模块或将其 docstring 改为"一次性修复工具"，并在计划文档标注审计账本已停用。

### F636 | detect_drift 把任意指标 0→正值映射为 6.0x 偏差 → 基线为 0 的指标首次出现即触发 is_drift=True（severity≥WARN） | 边界触发语义 | P2
- 证据：`src/shenbi/skill_utils/drift_detection/linguistic_drift.py:202`（`ratio = (curr_val / base_val) if base_val > 0 else (6.0 if curr_val > 0 else 1.0)`）；`:218`（`is_drift = max_deviation_ratio > 5.0`）；`:226`（`elif stm_density > 30 or is_drift: severity = "WARN"`）
- 根因：docstring :185 承诺触发条件是"指标偏离 baseline >500%"，但 0→任意正值的实现映射为固定 6.0x——单个 em-dash/单个 pattern 指纹出现（如 3000 字章节中 1 个 `——`，密度 0.33‰）即被当作 6x 偏差 → is_drift=True → severity≥WARN → 3 层干预注入警告。对稀疏指标（em_dash/pattern_fingerprints，真实章节 em_dash 密度常 <7‰）这是**过度敏感触发**；与 F601 的"对话塌陷 5.0 不触发"恰成一对镜像缺陷（该触发的没触发、不该触发的触发）。
- 验证命令+输出：
  ```
  base 全 0 + current em_dash_density=0.33 -> detect_drift -> is_drift: True | severity: WARN
  ```
- 建议方向：0→正值的偏差语义改为"按当前密度与绝对阈值结合判断"或设置下限（如 curr_val ≥ 某最小密度才按 6x 处理），避免单个标点/单词触发干预；补单测（基线 0、当前 0.33‰ 不应 is_drift）。

---

## 3. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:2014-2019 baseline 读、:2023-2043 干预门控、:2716-2727 ESCALATE 调用点吞异常、:1897 check_resonance 显式 floor）、revision_router.py（:64 DEFAULT_RESONANCE_FLOOR、:108/116/123）、state.py（:360 PipelineConfig 无类型校验构造）、audit_layer.py（:100-113 激活语义）、g0_config_coherence.py（:42-95 floor/audit 检查）、audit/write_audit.py + audit/record.py（drift block ship 消费链）、safe_write.py:127-135（TraceWriter 缓解）、plugins/master.json + skills/ 目录（59 vs 74）、novel-output/xinghuo-ranqiong/（genre-config.json 实键、chapters/chapter-1.md META 与空行格式、truth/resonance_trend.md 表头、truth/audit_drift.md 无 CLI marker、config-change-log.jsonl 单条记录）、计划 07-19-04（floor 50→65 修复）、计划 07-19-07（Task 5a 接线指令、:749 绝对阈值契约）、tests/unit/*（test_drift.py 6 用例、test_linguistic_drift.py、test_compute_stats.py 等存在性核对）。
- 与前几轮观察的衔接（不重复立项）：`read_chapters` 静默吞读错误、compute_pattern main 非 dict 崩溃、escalation CLI 空串 ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook AttributeError（F610/F622 同族第三模式）——本轮维持观察，未升 finding。

---

## 4. 返回摘要

**复核统计：新 finding 7（F630-F636，均 P2）/ 误报 0 / 严重度异议 0。**

**判定：非 0 新 finding（7 条），未达收敛目标。**

F601-F629 共 29 条全部成立（初审 11 + 第一轮 8 + 第二轮 10，无整条误报）；四条 P1（F601/F602/F612/F620）本轮逐条以运行验证 + 计划文档原文核查，无反证。本轮 7 条新 finding 全部为 P2，集中在四类前三轮未覆盖的方向：
1. **跨文件阈值漂移**：F630（revision_router 的 `DEFAULT_RESONANCE_FLOOR=50` 与单源 65 漂移，E11 缺陷类复活，且该常量注释明确自称就是 config.resonance_global_floor）、F632（linguistic_drift severity 阶梯 30/50/100 字面量复制 thresholds.py，单源承诺落空）；
2. **config 治理**：F631（`is False` 严格相等被 falsy 值 `0`/`null`/`""` 绕过——写侧 rationale 与 G0 读侧同时失明且审计实际被关闭，运行复现）、F635（update_genre_config 治理层零生产调用，docstring "Every change flows through" 为假，genre-config 由 skill 直写）；
3. **统计口径**：F633（segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 被计句末）、F634（META 指令块被当正文统计，真实章节稀释 ~12% 并直接进计数）；
4. **detect_drift 触发语义**：F636（0→正值映射 6.0x，单标点触发 is_drift，与 F601 的"该触发未触发"成镜像）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | revision_router.py:62-64,108,123; thresholds.py:30 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | config_coherence.py:109; g0_config_coherence.py:94; audit_layer.py:112-113 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | linguistic_drift.py:222-229; thresholds.py:36-39 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | compute_stats.py:78-80,87,16 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | linguistic_drift.py:94-131; compute_stats.py:311-362; chapter-1.md |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | config_coherence.py:6-14; skills/shenbi-genre-config/SKILL.md:204 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | linguistic_drift.py:202,218,226 |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review3.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
