# Z6 分区第十二轮独立复核报告（fresh-context 全量重读）

> 复核人：第十二轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11（F601-F611）+ 一轮 8（F612-F619）+ 二轮 10（F620-F629）+ 三轮 7（F630-F636）+ 四轮 3（F637-F639）+ 五轮 3（F640-F642）+ 六轮 1（F643）+ 七轮 3（F644-F646）+ 八轮 2（F647-F648）+ 九轮 5（F649-F653）+ 十轮 2（F654-F655）+ 十一轮 3（F656-F658）逐条独立验证；新发现编号 F659 起。
> 方式：49/49 文件全文 read（含全部 `__init__.py`/`__main__.py` 薄文件）+ `PYTHONPATH=src python3` 实际运行复现（F601/F606/F612/F623/F628/F633/F636/F641/F642/F644/F645/F647/F648/F652/F654/F655/F656/F657/F659/F660 本轮回放）+ 全仓 grep 调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对（genre-config.json、progress.json、config-change-log.jsonl、resonance_trend.md、pending_hooks.md、audit_drift.md、chapters/chapter-1/45/46.md、style_profile.md、无 trace.jsonl/无 linguistic_baseline.json）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **未完全达成**（新发现 2 条，均 P2、均潜伏；无 P0/P1 新 finding）。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §3）
- 初审 11 findings：**11/11 成立**（F601 严重度软异议支持，见 §4）
- 一轮 8：**8/8 成立**
- 二轮 10：**10/10 成立**
- 三轮 7：**7/7 成立**
- 四轮 3：**3/3 成立**
- 五轮 3：**3/3 成立**
- 六轮 1：**1/1 成立**
- 七轮 3：**3/3 成立**
- 八轮 2：**2/2 成立**
- 九轮 5：**5/5 成立**
- 十轮 2：**2/2 成立**
- 十一轮 3：**3/3 成立**
- 新发现：**2**（F659-F660，均 P2、均潜伏）
- 整条误报：**0**；严重度异议：**1 条（F601 软异议，支持 review6-11）**
- 判定：**非 0 新 finding（2 条），均 P2 潜伏级、无 P0/P1** —— 若按 review10/11 提议的"无 P0/P1 新 finding + 无整条误报"判据，本轮达成收敛；若按字面"0 新 finding"，未完全收敛（连续 5 轮新发现均为 P2 潜伏级小口径缺陷）

---

## 1. F601-F658 判定表（逐条独立复核）

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（支持 review6-11 软异议：可辩 P2） | 本轮直接运行：baseline dialogue=10 → current=1 → `max(…,5.0)=5.0` 不满足 `>5.0` → `is_drift: False, severity: NONE`（实测输出 `False NONE`）。linguistic_drift.py:215/218 代码确认；docstring :185-188 承诺 "<20% of baseline" 触发 → off-by-one 成立。**生产影响全潜伏**：唯一生产入口 `_check_linguistic_drift`（chapter_loop.py:1994-2019）因 F602 无 baseline 提前 return，detect_drift 从未被生产调用；即便接线，F612（is_drift 门控）+ F620（调用点吞异常）使干预仍不可达。P1（显式契约静默失效）与 P2（死路径）均可辩护，不构成整条误报。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持） | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py` re-export，生产零调用；`find . -name linguistic_baseline.json` 0 命中（style/ 仅 style_profile.md、context/ 无）。chapter_loop.py:2014-2019 无 baseline → `log.warning("no_linguistic_baseline")` + return None，:2722 每章走该分支。计划 07-19-07 Task 5a 显式接线指令未履行。P1 成立。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链 write_audit.py:54-56 → record.py:24 block ship。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | 本轮真实数据验证：chapter-1.md `compute_punctuation` 引号 count=72；该章 36 个 ASCII `"`（review8 实测）→ 2× 膨胀吻合。compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'` + :226 按字符迭代。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持；三变体均复核） | `"|".join([])==""` → `re.findall("", text)` 全位置匹配；`system_terms: null` → `list(None)` TypeError、`drift_detection: null` → `None.get` AttributeError 同根（linguistic_drift.py:79-84 无类型守卫）。**真实 config 核实**：`drift_detection` 键不存在（非 null）→ 真实项目走 bootstrap 回退，AttributeError 变体不触发（review11 说法正确）。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | 本轮运行：`59.5` → ACCEPTED 且落盘 59.5；`'58'` → ACCEPTED 且落盘 58；int 58 → REJECTED（正确拦截）。config_coherence.py:121-130 `isinstance(new_value, int)` 才拦。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs detect_drift 读 `dialogue_density`；:296 硬编码术语 vs :107 config 驱动。`_load_baseline`/`check_linguistic_drift` 生产零调用（仅测试）→ context/ 平行实现本身是死路径。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | 本轮真实数据验证：chapter-1.md `count_punctuation` 引号 cjk=3（相邻对 `""` 计数，真实引号包围内容不相邻）vs compute_stats=72。cjk.py:54 桶 token 为相邻双字符 → 真实文本恒 0/极少。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py 全文无 logging/structlog import；:40-43 两处 break 无日志、:47-48 safe_write 改写文件。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无 → 单条脏数据整批崩溃。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:109 `key.startswith("auditDimensions.")` 只拦带点形式；整键对象覆盖绕过。F631（falsy）/F638（snake_case）/F643（键缺失）为独立向量。P2 成立。 |

### 1.2 一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持，无反证） | 本轮直接运行：baseline stm=60 → current stm=110 → `is_drift: False, severity: ESCALATE`（实测输出 `False ESCALATE`）；stm=55 → `False HARD`。chapter_loop.py:2024 `if result.is_drift:` 包裹 log + ESCALATE raise + HARD/WARN 注入（:2024-2043 代码精读）；opening-similarity 检查 :2044-2056 在门**外**。计划 07-19-07:749 绝对阈值触发契约。P1 成立。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | 本轮真实数据验证：chapter-1.md 破折号 cjk=25（`text.count("——")` 正确）vs compute_stats=100（4× 膨胀）；省略号 1 vs 4。compute_stats.py:27 重复字符 + :226 按字符迭代。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | config_coherence.py:105-133 循环内逐 key `_append_audit_trail`，safe_write 在循环外 :142 → 中途 ConfigError 时 trail 已追加、config 未落盘。**补充（F614 同族、不单独立项）**：前缀相关键（如 `{'a': {...}, 'a.b': 2}`）时 `_get_nested(config, "a.b")` 读的是前一个 key 已写入的新值 → trail 的 old 快照错误——该模块生产零调用（F635），影响面为零，并入 F614 判定。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | writer.py:57 `json.loads(lines[-1])` 无 try；replay.py:38-43 同文件可自愈 → 不对称。三个生产调用点均有外部缓解（safe_write.py:126-140 / audit/record.py:42-46 / migrate 先 replay）。P2 成立。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤；权威 YAML 缺行 → drift.py:84 假 drift 或静默漏报。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | 本轮 grep：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` → 仅 compute_stats.py:37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex（语义不同）。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。P2 成立。 |

### 1.3 二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持，无反证） | 本轮代码精读：chapter_loop.py:2719-2728 注释自称 "non-blocking"，`try: _check_linguistic_drift(...) / except Exception: log.warning("linguistic_drift_check_failed", exc_info=True)`——函数内 :2034-2038 raise DriftEscalationError（"Pipeline paused for human review"）被调用点吞掉，且 `except Exception` 连带吞掉其他真实错误。grep `_check_linguistic_drift` 唯一生产调用点即 :2722。P1 成立。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目；:170 main 默认 "未分类" 且词表外字符串原样保留。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | recall.py:41-46 `.get()` 只判 None 不判类型 → `int - str` / `int > str` TypeError。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持） | 本轮运行：`count_ai_markers('他不是……而是默默走开了。')` → {}（双省略号真实文本 0 命中）；`'他不是…而是默默走开了。'` → 1 命中（仅单省略号非标准写法）。compute_stats.py:52 单 U+2026 vs 真实文本双 `……`（与 :23 PUNCT_MAP 一致）。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无 skills 校验 | P2 | **成立**（P2 维持） | 本轮规范化重算：master 59 名、skills/ 目录 74（shenbi-* 73 + using-shenbi 1）、缺失恰好 15 个（名单与前九轮一致）、反向 0。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、无生产调用方）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | 本轮 grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export，src 内无生产调用。**补充**：该函数 `return result.is_drift and severity in ("HARD","ESCALATE")` 与 F612 同病（severity=ESCALATE 而 is_drift=False 时返回 False）——即使接线也带 F612 门控病，与 F612/F620 修复应绑定。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件。真实 resonance_trend.md 单表 → 潜伏。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | 本轮运行：`'他顿了一下；然后说。'` → segment_sentences 1 条、segment_paragraphs sentences=2。compute_stats.py:16 SENT_ENDS 含 `；`，:63 只按 `。！？\n` 切句，:87 用 SENT_ENDS。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | 本轮 grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用（shenbi-chapter-revision/SKILL.md:134-142 仅在 skill prompt 层引用）。P2 成立。 |

### 1.4 三轮 F630-F636

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | **成立**（P2 维持） | revision_router.py:62-64（注释明示 "Default resonance global floor (spec §6.3, config.resonance_global_floor)" 后跟 `= 50`）vs thresholds.py:30 `=65`；chapter_loop.py:1897 显式传值覆盖默认 → 潜伏。P2 成立。 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | **成立**（P2 维持） | config_coherence.py:109、g0_config_coherence.py:92 均 `is False`；audit_layer.py:113 `audit_dims.get(dim_key, False)` 任何 falsy 即不激活。**补充**：空列表 `[]` 同样 falsy → `{'auditDimensions.texture': []}` 绕过治理且停用审计——F631 建议的 truthiness 修复一并覆盖。P2 成立。 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | **成立**（P2 维持） | linguistic_drift.py:222-226 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50；thresholds.py:8-10 docstring 自称单源。P2 成立。 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | **成立**（P2 维持） | 本轮运行：`'第一句。\n第二句。\n第三句。'` → 1 段落、sentences=5（3 实句 + 2 个 `\n` 计句末）。compute_stats.py:79 docstring vs :80 `re.split(r"\n\s*\n")`；`\n` ∈ SENT_ENDS :16。P2 成立。 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | **成立**（P2 维持；**数字随剥离方法而变——F642 同款教训，见 §2**） | 本轮复核：chapter-1.md 现含 **2 个** META 块共 858 字符（全文 4177 字符）；META 内 6 个 `——`（密度 6/858≈7.0‰ > 正文 19/3319≈5.7‰）。剥离 2 块后 em_dash_density 5.9852→5.7246（下降）；review5 报告的去 META 6.5538（上升）与其剥离方法（仅首块/旧版文件）相关——方向随方法而变，但"META 被当正文统计"本体成立（两处统计入口无剥离）。P2 成立。 |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | **成立**（P2 维持） | 本轮 grep：`update_genre_config` 仅 config_coherence.py 自身 + 测试 + 计划文档；真实 config-change-log.jsonl 仅 1 条 2026-07-19 E34 记录；skills/shenbi-genre-config/SKILL.md 直写文件。P2 成立。 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | **成立**（P2 维持） | 本轮直接运行：base 全 0 + current em_dash=0.33 → `is_drift: True, severity: WARN`（实测输出 `True WARN`）。linguistic_drift.py:202。P2 成立。 |

### 1.5 四轮 F637-F639

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转（格式分叉 + 测试掩盖） | P1 | **成立**（P1 维持，无反证） | 本轮全链复核：真实 pending_hooks.md（143 行）标题仅 `# 伏笔追踪`/`## 第56章伏笔呈现`/`## 第56章生命周期状态更新（foreshadowing-track）`，**无 `## hooks`/`## 活跃伏笔`**（`grep -c "## hooks\|## 活跃伏笔"`=0）；ch-025 快照有 `## 活跃伏笔`/`## hooks` → 格式分叉成立。消费链 write_audit.py:54-56（对每个 post .md 跑三件套）→ record.py:24 block-ship 已接线但 drift 恒空。P1（"测试失效掩盖真实缺陷"档）成立。 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过 Rule 1 与 G0） | P2 | **成立**（P2 维持） | audit_layer.py:105-107 snake_case 回退 vs config_coherence.py:109/g0_config_coherence.py:79 只认 camelCase。真实配置 camelCase（本轮核实 `has audit_dimensions: False`）→ 潜伏。P2 成立。 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 真实项目 arc/volume 半边不运行 + audit 追加无协调 | P2 | **成立**（P2 维持） | 本轮运行：`python -m shenbi.skill_utils.drift_detection --resonance <真实文件>` → 无输出 exit 0（真实 resonance_trend.md 单行 → `len(series) < 2` 全跳过）；真实 truth/ 仅 resonance_trend.md，无 arc_payoff_trend.md/volume_score_trend.md；audit_drift.md 为 `## Ch55 —` 章节式（review-resonance 写），CLI `_append_audit` 追加 `## drift findings` 段与之无协调。P2 成立。 |

### 1.6 五轮 F640-F642

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F640 | materialize_progress 输入契约零生产者 → 无条件覆盖真实 progress.json（数据丢失+错误结果） | P1 | **成立**（P1 维持；激活应升级 P0） | 本轮全链复核：`grep '"INIT"\|"MARK_DONE"'` src/ → 仅 materialize.py:49,53（消费方）+ 测试；生产 TraceWriter.append 调用点 = audit/record.py:44（GATE_FAIL/AUDIT_PASS）+ safe_write.py:132（trace_action 生产唯一传参处即 materialize.py:98 自身）+ migrate.py:30（migrate_from_progress 自身零调用）。`_maybe_materialize_progress`（chapter_loop.py:684-699）：`if steps_done % 5 == 0`（steps_done=0 即满足，:692）+ `except Exception: pass`（:698-699 静默）；调用点 :2691/:3028 在活动章节循环内。`_auto_rebuild_progress_if_stale`（:702-743）检查 `project_dir/"trace"` **目录**（:713）而 TraceWriter 写 `trace.jsonl` **文件**（writer.py:42）→ 路径错位使自愈分支永不触发。真实 progress.json 为 `current_scorer_agent`/`scoring_history` schema（本轮核实），与 materialize 输出不同 → 触发即数据丢失。真实项目无 trace.jsonl → 潜伏。P1 成立。 |
| F641 | records `_values_equal` 布尔比较孔 → YAML true vs md "true" 假 drift | P2 | **成立**（P2 维持） | 本轮运行：`_values_equal(True, 'true')` → False（假 drift）；`detect_cross_section_drift([{'id':'h','state':True}], {'h':{'id':'h','state':'true'}})` → 假 drift 1 条。drift.py:57-69 float 兜底对 bool 抛 ValueError 落入 return False。P2 成立。 |
| F642 | check_opening_similarity 比较 META 块非正文开头 | P2 | **成立**（本体成立；**证据裁决支持 review9/10/11，否定 review8**，见 §2） | 本轮独立重放（同法剥离 `re.sub(r'<!--META-BEGIN-->.*?<!--META-END-->', '', t, flags=re.S)`，ch45 2 块/ch46 1 块）：raw (46,45)=**0.6267**；剥离后生产顺序 (46,45)=**0.6367** > 0.6；交换顺序 (45,46)=**0.57**；autojunk=False=0.77 —— 0.57 与 0.637 **均可复现**，差异源于 SequenceMatcher 默认 autojunk 参数顺序不对称（F649）；按生产调用顺序（chapter_loop.py:2049 current 在前）剥离后仍 0.6367 > 0.6 → **剥离 META 不足以消除假阳性**（review6/review9/10/11 结论成立，review8 的"0.637 不可复现"不成立）。P2 维持。 |

### 1.7 六轮 F643

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F643 | 治理链"键缺失"默认语义矛盾：G0 视缺失=启用、audit_layer 视缺失=禁用 → 移除/置空 auditDimensions 静默停用审计 | P2 | **成立**（P2 维持） | g0_config_coherence.py:92 `get(dim, True)` vs audit_layer.py:113 `get(dim_key, False)` vs config_coherence.py:109 只认显式 False——三层缺省语义矛盾，整键替换移除关键维度静默停用审计且 G0 不报。真实配置 10 维全 True → 潜伏。P2 成立。 |

### 1.8 七轮 F644-F646

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F644 | compute_ngrams 未剔除标点 → n-gram 指纹被标点对/标点串主导 | P2 | **成立**（P2 维持） | 本轮实测：真实 chapter-1 top bigram = `('。他', 27)`/`('——', 25)`（min_count=10）。compute_stats.py:209 只滤空白；compute_all_stats 对全语料 min_count=50 时 `——` 仍居首（review8 已证 56 章语料 26371）。P2 成立。 |
| F645 | compute_ttr 排除串缺 `“”`/`"`/`‘’` 引号 → 引号字符计入 TTR token | P2 | **成立**（P2 维持） | compute_stats.py:171 排除串只含两个 ASCII 直单引号 `''`；真实 chapter-1 含 36 个 ASCII `"`（review8 实测）计入 TTR。P2 成立。 |
| F646 | AUDIT_SAFETY_MATRIX 6 维 vs 真实 config 10 维漂移：4 维无 rationale/G0 保护（sensitivity 为 BLOCKING 级） | P2 | **成立**（P2 维持；**支持 review11 的 impact 软异议方向，但维持 P2 定级**） | 本轮实测：真实 genre-config.json auditDimensions 10 维全 True，`real - matrix = ['foreshadowing','motivation','sensitivity','worldRules']`、`matrix - real = []`。sensitivity 激活链核实为活：audit_layer.py:44-45 GENRE_ACTIVATION_MATRIX → chapter_loop.py:2574-2577 `get_active_genre_audits` → 派发 shenbi-review-sensitivity → sensitivity_blocking 喂 escalation/check.py。**但**：真实配置当前全 True（潜伏）、配置写入无治理路由（F635），且矩阵"absent → non-critical"是 thresholds.py:48-49 显式声明的设计 → P2 恰当；若协调者认定"BLOCKING 级安全网可无治理禁用"违反显式契约，可辩 P1。不修改定级。 |

### 1.9 八轮 F647-F648

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F647 | records by_id 重复/缺失 id 静默塌缩 → drift 漏报或误报且无告警 | P2 | **成立**（P2 维持） | 本轮运行：权威 YAML 两条同 id 记录（type 主线/支线）+ md 对齐后者 → drift `[]`（漏报）。drift.py:80 dict 推导后者覆盖前者；缺失 id → `str(None)="None"` 塌缩。P2 成立。 |
| F648 | compute_percentiles n=2 百分位倒挂（P75/P95 < P50） | P2 | **成立**（P2 维持） | compute_stats.py:99-104 `int(n*0.75)-1` 截断 + `values[n//2]` 组合在 n=2 时 P75=P95=values[0] < P50=values[1]。P2 成立。 |

### 1.10 九轮 F649-F653

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F649 | SequenceMatcher 参数顺序不对称（autojunk）→ 相似度非良定义、近阈值翻转；F642 之争根源 | P2 | **成立**（P2 维持） | 本轮独立重放：剥离 META 后 (45,46) 生产顺序 0.6367 vs 交换顺序 0.57、autojunk=False 0.77（输出确认）。linguistic_drift.py:249-253/256-268。P2 成立。 |
| F650 | TraceWriter 信任尾部签名不校验 → 签名无效尾部链上新事件，replay 静默删除新合法事件 | P2 | **成立**（P2 维持） | writer.py:51-57 `_last_sig_existing` 只 `json.loads(lines[-1]).get("signature", GENESIS_PREV)`，不校验签名与链一致；append 以坏签名为 prev 链新事件；replay.py:42-43 从坏行截断 → 新合法事件被连带删除。与 F615（JSON 不可解析尾部）为独立失败模式。P2 成立。 |
| F651 | parser yaml.safe_load 重复键静默 last-wins → 权威记录源静默损坏 | P2 | **成立**（P2 维持） | parser.py:37-45；PyYAML SafeLoader 默认映射重复键 last-wins 无异常（review9 运行复现）。P2 成立。 |
| F652 | detect_rhetoric「反复」按子短语分解计数爆炸（7 字短语×3 → 12） | P2 | **成立**（P2 维持） | 本轮运行：`detect_rhetoric('冷在墙体第五层。'*3)` → `{'反复': 12}`；真实 chapter-1 反复=57。compute_stats.py:266-280 枚举全部 3/4/5-gram 各计 1。P2 成立。 |
| F653 | parse_markdown_table 空行不终止表 → 第二张表并入派生视图 | P2 | **成立**（P2 维持） | drift.py:34-39 `if not s: continue` 空行不 break（review9 运行复现第二张表并入）。P2 成立。 |

### 1.11 十轮 F654-F655

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F654 | `_try_float` 非有限值透传（nan/inf/1e999）→ 趋势序列被静默污染、σ 触发失效 | P2 | **成立**（P2 维持） | 本轮运行：`_try_float('nan')='nan'`、`'inf'`、`'1e999'` 透传（实测输出 `nan inf inf 3.14 None`）；构造含 nan 单元格趋势表 → parse_trend 序列 `[(90.0,False),(nan,False),(80.0,False)]`。compute_drift.py:217-222 docstring 承诺"非有限值返回 None"但实现无 finiteness 检查。P2 成立。 |
| F655 | records drift 缺省值比较孔：`None` vs `''` 恒不等 → 空单元格/多余列假 drift | P2 | **成立**（P2 维持） | 本轮运行：`detect_cross_section_drift([{'id':'H01','type':'主线'}], {'H01':{'id':'H01','type':'主线','subtlety':''}})` → 假 drift 1 条。drift.py:90-91 `rec.get(key)`→None；:57-69 `_values_equal(None, '')` 恒 False。P2 成立。 |

### 1.12 十一轮 F656-F658

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由（本轮） |
|---|---|---|---|---|
| F656 | detect_rhetoric「排比」`[:20]` 截断 → 连续长句恒判平行（真实 chapter-1 排比 53 中 40 为伪影） | P2 | **成立**（P2 维持） | 本轮运行：`detect_rhetoric(真实 chapter-1)` → `{'排比': 53, '设问': 0, '反问': 0, '反复': 57}`。compute_stats.py:256 `sent_texts[i][:20]` → ≥20 字长句截断后长度全 20 → `abs(20-20)/20=0 < 0.3` 恒判平行。P2 成立。 |
| F657 | compute_drift 卷级检测丢弃 human_overridden 排除（章节级排除、卷级不排除） | P2 | **成立**（P2 维持） | 本轮运行：构造 `| 弧情感交付 | 80 | |` / `| 弧情感交付 | 79 | true |` → parse_trend `[(80.0,False),(79.0,True)]`；`detect_volume_drift([80.0,79.0])` → VOLUME_DECLINE 触发（被覆盖的 79 仍触发）。compute_drift.py:277 `[score for score, _ in overall_series]` 丢弃标志。P2 成立。 |
| F658 | parse_markdown_table 重复 id 行 last-wins 静默塌缩（F647 md 侧镜像） | P2 | **成立**（P2 维持） | drift.py:51-53 `if rid: out[rid] = row` dict 赋值后者覆盖前者，无日志（review11 运行复现首行消失）。P2 成立。 |

**严重度异议：1 条（F601 软异议，支持 review6-11，见 §4）。** 六条 P1（F601/F602/F612/F620/F637/F640）本轮全部以直接运行 + 代码精读 + 全仓 grep + 真实数据多重复核，**无反证**；F601 生产影响全潜伏（P1↔P2 可辩，不修改）；F640 确认 `_maybe_materialize_progress` 的 `steps_done % 5 == 0`（含 0）触发 + `except Exception: pass` 静默 + `_auto_rebuild_progress_if_stale` 的 `trace/` 目录 vs `trace.jsonl` 文件路径错位，P1 成立且激活应升级 P0。P2 均无高判或低判。

---

## 2. F642 证据裁决复核与 F634 数字勘误

### 2.1 F642：review9/10/11 终裁独立复现（review8 裁定不成立）

本轮独立重放（同一 `check_opening_similarity`、同一剥离正则 `re.sub(r'<!--META-BEGIN-->.*?<!--META-END-->', '', t, flags=re.S)`，ch45 两个 META 块 / ch46 一个）：

```
raw [:300] ratio(46,45)            = 0.6267   # > 0.6
stripped ratio(46,45)（生产顺序）    = 0.6367   # > 0.6 —— 与 review6/9/10/11 一致
stripped ratio(45,46)（交换顺序）    = 0.5700   # < 0.6 —— 与 review5/7/8 一致
stripped autojunk=False (46,45)     = 0.7700
```

结论：**0.57 与 0.637 均可复现**，差异为 `SequenceMatcher.ratio()` 默认 autojunk 的参数顺序不对称（F649）；生产调用顺序（chapter_loop.py:2049 current 在前）下剥离 META 后仍 0.6367 > 0.6 → **剥离不足以消除假阳性**（review6 原始修正成立，review8 裁定不成立，F642 本体与 P2 定级维持）。

### 2.2 F634：剥离数字随方法而变（F642 同款教训），本体不受影响

本轮实测 chapter-1.md 现含 **2 个** META 块（858 字符，占全文 4177 字符约 20.5%），META 内 6 个 `——`（密度约 7.0‰ > 正文 5.7‰）。剥离两块的 em_dash_density 由 5.9852 降至 5.7246；review5 报告的去 META 6.5538（上升）与其剥离方法（仅首块或旧版文件）相关。**方向随剥离方法而变，但"META 指令块被当作章节正文统计"本体不受影响**（linguistic_drift.py:94-131、compute_stats.py:311-362 均无剥离）——F634 成立，建议方向的"统一正文提取函数"应覆盖全部 META 块。

---

## 3. 新发现（F659-F660）

### F659 | compute_stats.PUNCT_MAP 与 cjk.PUNCTUATION_TOKENS 双标点统计实现口径分叉：同一文本两模块给出系统性不同桶计数（感叹号 0 vs 4、冒号 5 vs 18、引号 72 vs 3、破折号 100 vs 25） | 跨模块双实现口径分叉（F607/F632/F646 同族） | P2（潜伏）

- 证据：`src/shenbi/skill_utils/style_learning/compute_stats.py:17-28`（PUNCT_MAP：感叹号只 `"！"`、问号只 `"？"`、冒号只 `"："`、破折号只 `"——"`、省略号只 `"……"`、引号 `"\"\"''「」『』"` 按字符）；`src/shenbi/text/cjk.py:44-55`（PUNCTUATION_TOKENS：感叹号 `["！","!"]`、问号 `["？","?"]`、冒号 `["：",":"]`、破折号 `["——","──"]`、省略号 `["……","。。。"]`、引号 `['""',"''","「」","『』"]` 按相邻对）；真实数据 `novel-output/xinghuo-ranqiong/chapters/chapter-1.md`。
- 根因：两个模块各自实现了"按桶统计标点"且**桶成员定义不同**（ASCII 变体 `!`/`?`/`:`、`──`/`。。。` 变体、引号 token 语义 per-char vs adjacent-pair）。F604/F608/F613 分别修复各模块自身的重复计数/恒零 bug 后，两实现**仍不收敛**——任何 ASCII 标点文本两模块计数必然不同。cjk.count_punctuation 当前无生产调用方（grep：仅 text/ 自身 + 测试），compute_stats.compute_punctuation 经 shenbi-style-learning/SKILL.md:51 接线 → 分叉的实际影响集中在"同一概念两套定义、修复一侧不修复另一侧"的维护陷阱（与 F607 双 baseline、F632 阈值双源、F646 维度集漂移同族——单源承诺（text/__init__ 声称 "Centralized CJK text operations"）未覆盖标点桶成员）。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.text.cjk import count_punctuation
  from shenbi.skill_utils.style_learning.compute_stats import compute_punctuation
  t = open('novel-output/xinghuo-ranqiong/chapters/chapter-1.md', encoding='utf-8').read()
  cjk = count_punctuation(t); cs = {k: v['count'] for k, v in compute_punctuation(t).items()}
  for k in cjk: print(k, 'cjk=', cjk[k], 'compute_stats=', cs.get(k))
  "
  -> 感叹号 cjk=4 compute_stats=0 | 破折号 cjk=25 compute_stats=100 | 省略号 cjk=1 compute_stats=4
     | 冒号 cjk=18 compute_stats=5 | 引号 cjk=3 compute_stats=72 | 其余桶一致
  ```
- 影响：潜伏（cjk.count_punctuation 无生产调用方；compute_stats 侧自身 bug 已由 F604/F613 覆盖）。一旦 text API 被未来消费者启用（spec pillar 3 "Centralized CJK text operations" 的承诺），同一文本的标点统计随调用模块不同而不同，风格报告与文本 API 互相矛盾。
- 建议方向：两个模块共用同一标点桶定义常量（将 PUNCTUATION_TOKENS 作为唯一源，PUNCT_MAP 改为引用它或反向），并在测试中锁定"同一文本两 API 输出一致"；补单测（含 ASCII `!`/`?`/`:` 与 `──`/`。。。` 变体的文本，两模块桶计数一致）。

### F660 | parse_markdown_table 无表头表静默整表丢行：首行即分隔行（`|---|`）被当表头 → 数据行以 "---" 为键、无 id 键 → 全部静默丢弃，drift 检查对该表空转 | 表解析边界（F653 同族、F616/F647/F651/F658 的"静默数据损失"族） | P2（潜伏）

- 证据：`src/shenbi/records/drift.py:34-53`（`:41-43` 首个 `|` 行无条件被当 header——在 `:44-45` 分隔行检查**之前**；`:49` `_MD_HEADER_TO_KEY.get(header[i], header[i])` 对非表头列原样透传为键；`:51-53` `rid = row.get("id"); if rid:` 无 id 键 → 行静默丢弃）；消费链 write_audit.py:54-56 → record.py:24 block ship。
- 根因：`header is None` 分支先于分隔行判断。当 markdown 表**缺表头行**（LLM/手工写入时首行直接是 `| --- | --- | --- |`，或表头行被误删）时：分隔行被当作 header（`["---","---","---"]`），后续数据行每个单元格键都映射为 `"---"`（dict 重复键 last-wins）→ `row` 无 `id` 键 → `if rid:` False → **整表静默返回 {}**，drift 检查对该表空转（与 F637 的空转方向不同：F637 是整个权威结构缺失，本缺陷是表存在但缺表头）。当前 fixture（tests/fixtures/truth-pending_hooks.md）与 ch-025 快照均带表头、真实项目无权威结构（F637）→ 潜伏。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.records.drift import parse_markdown_table
  print(parse_markdown_table('## 活跃伏笔\n\n| --- | --- | --- |\n| H01 | 主线 | PLANTED |\n'))
  -> {}   # 无表头表：数据行静默全部丢弃（应为 {'H01': {...}}）
  print(parse_markdown_table('## 活跃伏笔\n\n| Hook ID | 类型 | 状态 |\n| --- | --- | --- |\n| H01 | 主线 | PLANTED |\n'))
  -> {'H01': {'id': 'H01', 'type': '主线', 'state': 'PLANTED'}}   # 有表头正常
  ```
- 影响：F637 修复方向 (a)（恢复 `## hooks`+`## 活跃伏笔` 权威结构）后，若派生表出现缺表头形态，判据 12 检查对该表静默空转（漏报 drift），无任何告警。与 F653（空行不终止）同为"表解析终止/起始边界孔"，修复宜统一（起始：首行非表头列集合合法则拒绝或跳过；与 markdown 语义对齐）。
- 建议方向：`parse_markdown_table` 在首行被当 header 前先做分隔行检查（`if all(set(c) <= set("-: ")) for c in cells: continue` 移到 header 赋值前，或对 header 合法性校验——header 必须含 `Hook ID` 或至少一个 `_MD_HEADER_TO_KEY` 已知列，否则 log WARNING + 返回 {}）；补单测（无表头表 → 有告警信号或拒绝，而非静默空）。

---

## 4. 严重度异议

| finding | 现定级 | 异议 | 理由 |
|---|---|---|---|
| F601 | P1 | **软异议（支持 review6/7/8/9/10/11）：可辩 P2** | 缺陷本体成立（对话塌陷 off-by-one），但生产影响全潜伏：detect_drift 的唯一生产入口 `_check_linguistic_drift` 在无 baseline（F602）时于 chapter_loop.py:2017-2019 提前 return，detect_drift 从未被生产调用；即便接线，F612（is_drift 门控）与 F620（调用点吞异常）使干预仍不可达。按决策表"死代码/未接线"（P2）与"违反显式契约"（P1）均可辩护。不构成整条误报，不修改定级（无权限），供协调者裁决。 |

（其余 57 条无严重度异议。六条 P1 中 F602/F612/F620/F637/F640 本轮均经运行/代码/全仓 grep 复核无反证；F646 支持 review11 的 impact 软异议方向但维持 P2（真实配置全 True 潜伏 + "absent → non-critical"为显式声明设计，P1 论证需协调者认定"BLOCKING 级安全网可无治理禁用"违反契约）。F642 证据裁决支持 review9/10/11（0.57/0.637 均可复现，review8 裁定不成立），本轮重放一致，无新争议。）

---

## 5. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2058 漂移检查全链、:2014-2019 baseline 读、:2024-2043 is_drift 门控、:2044-2056 opening-similarity 门外、:2719-2728 ESCALATE 调用点吞异常、:684-743 `_maybe_materialize_progress`/`_auto_rebuild_progress_if_stale` trace 路径错位、:989-1010 check_escalation 接线、:2574-2577 get_active_genre_audits 派发）、revision_router.py（:62-64 DEFAULT_RESONANCE_FLOOR=50）、audit_layer.py（:44-45 GENRE_ACTIVATION_MATRIX、:100-113 激活语义）、g0_config_coherence.py（:37-42 critical 矩阵、:89-97）、audit/write_audit.py（:51-56 records 消费）、audit/record.py（:24,42-46 block + trace seam）、safe_write.py（:90-140 trace_action 唯一生产调用点）、gates/g7_trace.py（:50-51 verify_chain/assert_monotonic 消费）、plugins/master.json（59 skills 规范化对比）、skills/ 目录（74）、skills/shenbi-drift-guidance/SKILL.md（drift CLI 接线）、skills/shenbi-review-resonance/SKILL.md（calibration/routing CLI 接线）、skills/shenbi-style-learning/SKILL.md:51（compute_stats CLI 接线）、skills/shenbi-chapter-revision/SKILL.md:127-142（route_revision/verify_preservation prompt 层引用）、skills/shenbi-escalation-review/SKILL.md（escalation_check 概念引用 vs check_escalation 实际接线）、novel-output/xinghuo-ranqiong/（genre-config.json 10 维 camelCase 无 drift_detection 键、progress.json scoring schema、config-change-log.jsonl 单条、resonance_trend.md 单表单行含 human_overridden 列、pending_hooks.md 143 行章节附录结构无 `## hooks`/`## 活跃伏笔`、audit_drift.md `## Ch55` 格式、chapters/chapter-1/45/46.md META 块结构与引号/破折号统计、style_profile.md 种子指纹、无 trace.jsonl、无 linguistic_baseline.json、无 arc_payoff/volume 趋势文件）、tests/fixtures/truth-pending_hooks.md + tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照）、docs/superpowers/plans/archive/2026-07-19-07-*.md（F602 接线指令原文）。
- 维持观察（不重复立项，供协调者参考，均复核确认仍成立）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）、compute_pattern main 非 dict 章节 `.get` 崩溃、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize `_as_int` str 数值 ValueError 与 round 名解析、compute_drift `_append_audit` 重复追加无幂等、compute_connectives "时间"/"顺序" 类别重叠（"接着"/"然后"双计入类）、cjk.count_words 非法 mode 静默走 mixed、`find_terms` 裸字符串 terms 逐字符迭代、plugins/generate.py `fields["marketplace"]` 缺 fields KeyError、compute_stats.read_chapters 同名文件跨目录覆盖、`_short_chain_chars` 句末符口径、`detect_chapter_drift` 平滑先于排除（review5 已述）、compute_pattern.classify_entropy 恰好等于阈值（h==2.5）时落入下一档（"健康"非"优秀"，measure-zero）、check_window_redundancy docstring "sliding window" vs 实现仅取最后 4 章（死代码，F617 族）、frequency_divergence_alarms docstring ">3σ" vs 实现为 4× 速率启发式（死代码，F617 族）、verify_chain 不校验链首为 COMPACTION/LEGACY_MIGRATION（review8 已述，不可验证性）、escalation 输入含 nan 时 slope 静默不触发（F654 值级同族）。
- 关于"0 新 finding"收敛目标：F601-F658 共 58 条全部成立（含六条 P1），**无整条误报**；本轮新发现 2 条（F659/F660）均为前十二轮未触达的**跨模块双实现口径分叉 / 表解析起始边界**方向，均 P2、潜伏、无 P0/P1。收敛曲线 11→8→10→7→3→3→1→3→2→5→2→3→2，自 F648 起连续 5 轮新发现全部为 P2 潜伏级。**按"无 P0/P1 新 finding + 无整条误报"判据本轮达成收敛**；按字面"0 新 finding"判据未达成（P2 潜伏级小口径缺陷理论上可无限续轮）。建议协调者按 review10/11 提议落定收敛判据并终止轮次，或对剩余 P2 潜伏项按 triage 优先级（修复 F602/F612/F620/F640 接线族优先）排期。

---

## 6. 返回摘要

**复核统计：新 finding 2（F659-F660，均 P2）/ 误报 0 / 严重度异议 1（F601 软异议，支持 review6-11）/ 证据裁决复核 1（F642，支持 review9/10/11，否定 review8）。**

**判定：非 0 新 finding（2 条），均 P2 潜伏级、无 P0/P1——按"无 P0/P1 新 finding + 无整条误报"判据达成收敛；按字面"0 新 finding"判据未完全收敛。**

- F601-F658 共 58 条全部成立，**无整条误报**。六条 P1（F601/F602/F612/F620/F637/F640）本轮以直接运行 + 代码精读 + 全仓 grep + 真实数据 + 计划原文多重复核，**无反证**：
  - F601：baseline dialogue=10→current=1 → is_drift False、severity NONE（实测），off-by-one 成立；生产影响全潜伏（F602 无 baseline → detect_drift 从未被生产调用）→ 支持软异议 P1↔P2 可辩；
  - F602：establish_baseline 全仓零调用、无 linguistic_baseline.json（style/ 仅 style_profile.md）；
  - F612：base stm=60/cur=110 → is_drift False、severity ESCALATE（实测），chapter_loop.py:2024 门控吞掉绝对阈值干预；
  - F620：chapter_loop.py:2719-2728 "non-blocking" 注释 + `except Exception` 吞 DriftEscalationError；
  - F637：真实 pending_hooks.md 无 `## hooks`/`## 活跃伏笔`（grep=0），parse_records/parse_markdown_table/detect_cross_section_drift 全空转；
  - F640：`"INIT"`/`"MARK_DONE"` 全仓仅消费方 materialize.py 与测试命中；`_maybe_materialize_progress` steps_done%5==0（含 0）+ `except Exception: pass`；`_auto_rebuild_progress_if_stale` 检查 `trace/` 目录 vs TraceWriter 写 `trace.jsonl` 文件（路径错位）；真实 progress.json 为 scoring schema（触发即数据丢失，激活应升级 P0）。
- **F642 证据裁决复核**：raw 0.6267 / 剥离后生产顺序 0.6367 / 交换顺序 0.57 / autojunk=False 0.77——0.57 与 0.637 均可复现（SequenceMatcher 参数顺序不对称，F649），review8 裁定不成立，review9/10/11 终裁成立。**F634 数字勘误**：chapter-1 现含 2 个 META 块（858 字符），剥离方向随方法而变（本轮 5.9852→5.7246 下降 vs review5 的 6.5538 上升），本体成立。
- 本轮 2 条新 finding 均为前十二轮未触达方向：
  1. **F659（P2）**：compute_stats.PUNCT_MAP 与 cjk.PUNCTUATION_TOKENS 双标点统计实现口径分叉——真实 chapter-1 同文本两模块计数系统性不同（感叹号 0 vs 4、冒号 5 vs 18、引号 72 vs 3、破折号 100 vs 25）；即使 F604/F608/F613 修复各模块自身 bug，两实现仍不收敛（F607/F632/F646 同族"单源承诺落空/双实现"）。
  2. **F660（P2）**：parse_markdown_table 无表头表静默整表丢行——首行即分隔行被当表头、数据行无 id 键全部静默丢弃、drift 检查对该表空转（F653 同族的"表解析起始边界孔"；F637 修复方向 (a) 恢复权威格式后进入活动路径）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F659 | compute_stats/cjk 双标点统计实现口径分叉（同文本计数系统性不同） | P2 | compute_stats.py:17-28; cjk.py:44-55; chapter-1.md |
| F660 | parse_markdown_table 无表头表静默整表丢行（分隔行被当表头） | P2 | drift.py:34-53 |
| F601（异议） | P1↔P2 可辩（生产影响全潜伏），支持 review6-11 软异议 | 严重度软异议 | linguistic_drift.py:215/218; chapter_loop.py:2017-2019 |
| F642（裁决） | 0.57/0.637 均可复现（参数顺序不对称）；生产顺序剥离后 0.6367 仍 > 0.6；review8 裁定不成立 | 证据裁决 | linguistic_drift.py:249-253; chapter-45/46.md |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review12.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
