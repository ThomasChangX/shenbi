# Z3 分区第四轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第四轮独立复核（本文件 `Z3.review4.md`，与初审 `Z3.a.md`、一轮 `Z3.review.md`、二轮 `Z3.review2.md`、三轮 `Z3.review3.md` 隔离）
> 复核范围：34/34 文件全量重读（13,641 行），逐条独立验证 F300-F352；以真实生产项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式、audits 722 文件全旧式名、chapters 145 文件不补零、plans/chapter-5-plan.md 含 MH-001/H-N01、truth/pending_hooks.md frontmatter 无 hooks 键、truth/resonance_trend.md 9 列格式、pipeline-state.json phase=chapter-loop ch=56、novel.json 无 total_chapters）为格式/行为对照基准
> 本轮发现编号：F353-F363（11 条新发现，其中 2 条 P1）；**新误报 0 条**；严重度异议 0 条（对 F302/F305 立场见结论表）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：`_shared.read_volume_boundaries` 对生产中文 volume_map 返回 `set()`（本轮实测 `boundaries: set() / resolve ch15: None`）；生产 `novel.json` 无 `total_chapters`（56 章仍在 chapter-loop、closure_step=0）。
2. **F325（误报）维持撤销**：生产 audits/ 722 文件全为旧式名（continuity/character/world-rules/…），`group-*.md` 数量=0（本轮实测后缀分布）；4 个 group skill 契约 `writes:` 即旧式路径（load_contract 实证）；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物。
3. **本轮新发现 2 条 P1**：(a) `novel.json.total_chapters` 无任何正常流写入点——两个 `_update_total_chapters` 调用点（cli.py:748、triggers.py:623）全部前置依赖 `total > 0`（cli.py:219 守卫），而 total 的唯一来源就是该字段自身 → **即使修复 F324 解析器，pipeline 仍永不完成**（自锁，独立于 F324 的第二根因）；(b) 并行审计波静默吞掉 audit dispatch 失败——全部审计失败仍 `add_step_done` + 推进，无重试、无升级（对照串行路径 `_handle_failure`），章节可在零审计下"完成"。
4. **对 F348 的事实修正**（不推翻成立性）：F348 声称"缓存注入被跳过（契约 reads 已入 raw_inputs）"仅对 character_matrix/pending_hooks 成立；**style_profile 与 world_rules 的注入键是幽灵路径**（`truth/style_profile.md`/`truth/world_rules.md`，生产均不存在、无任何契约读取）→ 注入**实际发生**而非被跳过，style_profile 被以幽灵路径重复塞进每个审计 prompt（见 F357）。
5. **版本偏移注记（沿用三轮）**：生产 pipeline-state 为 Plan 18 重构前的旧版管线产物（steps_done 含旧 skill 名），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据。

---

## 一、复核结论总表（F300-F352 逐条判定）

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")`，源码字面量求值后为 no-op；注释宣称的标签注入缓解未生效 |
| F301 | **成立** | P1 维持，无异议（贴 P0 校准维持） | 并行波 `chapter_loop.py:2515` 无条件触发 → `:2607` 跳至 `_LAST_AUDIT_IDX+1` → 串行块 `:2924`（run_audit_layer）/`:2993`（resonance 解析）不可达。三轮修正的"resonance_trend.md 由 skill 契约自身写入"成立（本轮 load_contract 实证 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`；生产 9 列格式含 ch55 行）。并行路径 `cs.resonance_score` 恒 None（`:2993-2997` 不可达）→ floor 判定恒过 |
| F302 | **成立** | P1 维持（决策表 P0 例字面列明 "TokenLedger 少计"，升 P0 亦有据；裁定留 stage 4） | `dispatch_skill(state=)` 仅 chapter_loop.py:1250（`_check_conditional_resolve`，foreshadowing-track 死分支）/`:2799`（串行可及）/`:2970`（串行 BLOCKING 环，不可达）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli.py:123/751 均未传（grep 实证）；`getattr(state,"chapter",0)`（:1335）恒 0 |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（`_snapshot_chapter_files` 死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转（仅 linguistic-drift-check 有实现体）；cmd_rollback not_implemented |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（:626/:2882）无 except；cmd_next/cmd_resume 仅捕 FileNotFoundError；machine.py:85-93 ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed → crash-resume 与 escalation-resume 两个触发路径 |
| F305 | **成立但框定需修正（同意二/三轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文（ch10-foreshadowing.md:99 "无 BLOCKING 或 CRITICAL 级别缺陷"实测），`_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；残余真缺口 = CRITICAL-only 路由（route≠NO_REVISION）且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过（`_should_run_step` :1820-1821）。本轮补充：该情形下 `_route_revision_after_resonance`（:1886）已先 `revision_count += 1` → 计数与真实执行不符（M 级记账漂移，并入本条） |
| F306 | **成立** | P2 维持，无异议 | truth/volume_map.md 全仓 0 写入方；`Chapter {chapter}` 子串缺陷；volume_context 无消费方（_INJECT_FROM_CACHE 只注入 4 字段） |
| F307 | **成立** | P2 维持，无异议 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；`_parse_hook_entries` 对生产 plan section 7 的 `MH-001`/`H-N01` 行**会接受**（实测 plan 格式 `| MH-001 | advance | … | 0 |`，首格匹配 `^(MH|H)-`）——真正的检索侧断裂见 F346（维持三轮注记） |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state 无生产调用 |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop:2133 重复实现已接线 |
| F311 | **成立** | P2 维持，无异议 | extract_scr（:428-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | upsert_yaml 无生产调用；_serialize_yaml_records（truth_io.py:273-278）只写 frontmatter 丢 body |
| F313 | **成立** | P1 维持，无异议 | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换，step 6 `audits/chapter-N-long-span.md` 的 N 为章号 → G4 校验错位（F324 下 `_current_volume` 恒 1 → 校验 chapter-1-long-span.md） |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult 无 `.result` 属性（:333-339）→ `_merge_step_result`（state.py:521）恒 no-op；chapter_loop.py:2425-2426 调用无效果 |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重；consolidate 只提行不去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（:93/100）；F318.2/3/4 过期货注（"20 steps"/"Step 17"/"7 skills"）；F318.5 `_get_snapshot_retention` 硬编码 50 不读 config（:1371-1376）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 route_chapter_revision 的 blocking 形参未用（:86）；F318.8 current_step="" 窗口（:2607-2608）——窗口内确有文件 I/O（_route_revision_after_resonance 读审计/写备份），但无 save_state，crash 时磁盘为上一 save 点，自愈语义无实际损失（维持 M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback（:552）无章号绑定，注入下一次串行 dispatch（:2775-2780） |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan（review_checklist.py:485-544）无生产调用；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符 |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测 `read_volume_boundaries(production) → set()`；`_resolve_volume_at_runtime(pd, 15) → None`（ch15 是第 1 卷末章）。生产 volume_map 全中文（`## 第一卷：觉醒之火（第1-15章）`、`**章节范围**: 第1章 - 第15章（共15章）`、`| 第1章 | 开篇 | … |`），与 `_END_RE`/`_RANGE_RE` 零交集。影响链（卷边界触发全家 / total_chapters=None → `if total > 0:` 跳过全部章间触发 / 章节节点提取误中 `| 1 |` 行 / 卷名 "Unknown"）全部成立 |
| **F325** | **不成立（误报，维持二/三轮撤销）** | **P1 裁定撤回维持** | 生产 audits/ 722 文件全旧式名（本轮实测后缀分布 continuity×56、character×56、world-rules×55、pacing×56、dialogue×55、motivation×55、foreshadowing×56、anti-ai×56、texture×0、reader-pull×0、sensitivity×55、resonance×55、review-summary×55…），`group-*` 数量=0；group skill 契约 `writes:` 即旧式路径（load_contract 实证）；真实 BLOCKING（`严重度: BLOCKING`）被 collect_audit_issues 正则命中 → route≠NO_REVISION → `_any_audit_has_findings` 同文件必 True → revision 执行 |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证）；并行 post-draft（:2405-2419）两线程并发写同一 staging 文件，未走 assert_parallelizable/per-path read-merge 锁 → 后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）。**补充事实**：`_parse_resonance_score`（chapter_loop.py:1285-1321）三种模式均不匹配生产共鸣报告格式（`**结果**: 通过 (77/100)`）→ 即使串行路径可及，score 也解析不出 |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读 `state.drift_alerts`，该属性不存在→恒 []）死簇 |
| F329 | **成立** | P2 维持，无异议 | 生产 genre-config auditDimensions.sensitivity=true（本轮实测）+ CHAPTER_STEPS step 14 含 shenbi-review-sensitivity → core 波（:2551）与 genre 波（:2575）双发同一 skill 写同一文件 |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库 |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键；token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关；两条 total_chapters 口径不同（求和 vs max） |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 补零数=0（本轮实测）；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 [] → 级联跳过恒不触发（另：波内 skill 名为 group-factual 等，`_audit_short_name` 后也不在 CASCADABLE_AUDITS 清单——双重失效） |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；review-summary.md 恒报 0 问题 |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死 |
| F341 | **成立** | P1 维持，无异议 | chapter_loop.py:2696-2709 并行块无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；state_settle_review_required 唯一消费点 :1087（串行 _advance）不在并行路径 → --auto 每章必停 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用 |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:24-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；6 个 audit skill 均 requires_independent=True（实证）；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）对生产 plan section 7 的 MH-001/H-N01 零匹配（本轮实测 `MH-001 → []`、`H-N01 → []`、`P0-4 → ['P0-4']`）→ master hooks 从 Route A 检索静默缺失 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys=`[title,project,version,last_updated,type,category,status,filled_by,last_chapter,track_chapter]`（无 `hooks` 键，本轮实测），hook 以 body 表格呈现；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)" |
| F348 | **成立但机制描述部分不准** | P2 维持，无异议 | 核心判断（缓存对预期目标近乎死线：character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入、磁盘读仍全量发生）成立；**但 style_profile/world_rules 的注入键是幽灵路径（truth/style_profile.md、truth/world_rules.md，生产均不存在且无契约读取）→ 这两项注入实际发生**（见 F357），F348 "注入被跳过"的机制描述对这两项不成立 |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层；非合规输出先失败一次再重试 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout |

**误报统计**：F300-F352 中仅 F325 为误报（已在二轮撤销、三轮维持，本轮独立确认）。F348 为"成立但机制描述部分不准"（非误报）。

**严重度异议**：0 条新增。立场声明：F302（P1→P0 依据成立但裁定留 stage 4，同意二/三轮）、F305（同意二/三轮降 P2）、F301（同意贴 P0 校准）、F324（P0）、F340/F341（P1）均无新异议。

---

## 二、新发现（F353-F363）

### F353 | novel.json.total_chapters 无任何正常流写入点（自锁死循环）——F324 修复后 pipeline 仍永不完成 | error | **P1**

- 证据：
  - 全仓 `total_chapters` 写盘仅两处：`cli.py:167`（`_update_total_chapters`，唯一调用点 `cli.py:748`，位于 cmd_resume 的 **VOLUME_BOUNDARY approve 分支**）与 `triggers.py:394`（`_update_total_chapters`，唯一调用点 `triggers.py:623`，位于 `run_triggered_skills` 的 **volume_boundary 分支**）。
  - 到达任一写点的前置条件都是先进入触发块（`cli.py:217-219` `if cl.step_index == 0 and cl.current_chapter > 1: total = _read_total_chapters(project_dir); if total > 0:`）——而 `_read_total_chapters`（cli.py:134-144）读的正是该字段自身（novel.json 缺失→0）。
  - 正常流中 genesis 侧无任何写入：`cmd_init` 不写（seed_parser.py:18-20、129-131 显式注释 "volume-outlining 计算"），`shenbi-volume-outlining` 契约 `updates: ['outline/volume_map.md']`（load_contract 实证，不写 novel.json），genesis 代码 `run_genesis_step`/`_update_indexes` 均不写 novel.json。
- 根因：spec §4.2 "genesis step 6 完成后固化 total_chapters" 的接线缺失——volume-outlining 只产出 volume_map.md，pipeline 代码从未在其后计算/写入 total_chapters；而仅有的两个写点又被 `total > 0` 守卫自锁。
- 验证：
  ```
  grep -n "total_chapters" src/shenbi/pipeline/cli.py   # → :143(读) :167(写) :218(守卫) :748(调用) :759(读)
  grep -n "_update_total_chapters" src/shenbi/pipeline/*.py   # → cli.py:147(定义) :748(调用)；triggers.py:376(定义) :623(调用)
  python3 -c "import json; print(json.load(open('novel-output/xinghuo-ranqiong/novel.json')).get('total_chapters'))"   # → None（56 章完成仍无该字段）
  ```
- 影响：修复 F324（volume_map 解析）后，total_chapters 仍恒缺 → `if total > 0:` 守卫仍跳过全部章间触发（含 ch%12/ch%36 周期与 book_closure）→ pipeline 依旧永不进入 CLOSURE。F324 只解决"解析"，本 finding 是同一症状的**第二个独立根因**，必须与 F324 一并修复。
- 建议方向：genesis step 6 成功后由代码从 volume_map 计算 total_chapters 并写 novel.json（可复用 `_update_total_chapters` 逻辑），或将触发块守卫改为"volume_map 存在即尝试计算"。

### F354 | 并行审计波静默吞掉 dispatch 失败：全部审计失败仍标记完成并推进（对照串行 _handle_failure 重试/升级） | error | **P1**

- 证据：`chapter_loop.py:2596-2630`（波内对 `core_results + genre_results` **无任何 `result.success` 检查**，直接 consolidate → 全部 `add_step_done` → 推进至 `_LAST_AUDIT_IDX+1`）；`parallel_dispatch.py:77-128`（`_dispatch_with_retry` 失败后返回 `DispatchResult(False, …)`，不抛异常）；`parallel_dispatch.py:210-221`（consolidate 对失败结果 `continue` 跳过）；对照串行路径 `_handle_failure`（chapter_loop.py:587-671）对 dispatch 失败重试→升级 ESCALATION。
- 根因：并行重构（Plan 18 Task 7）只迁移了"dispatch + consolidate + 路由"，未迁移串行路径的失败重试/升级语义到波级。
- 验证：
  ```
  sed -n '2596,2630p' src/shenbi/pipeline/chapter_loop.py   # → 无 success 检查、无 _handle_failure
  grep -n "handle_failure\|escalation" src/shenbi/pipeline/parallel_dispatch.py   # → 无命中
  ```
- 影响：API 瞬时故障（超过 MAX_RETRIES=2 次重试）时，6 个审计全部失败 → 磁盘无审计文件 → `collect_audit_issues` 返回 ([], False) → route=NO_REVISION → revision 跳过 → 章节以**零审计**状态"完成"，无任何 checkpoint/escalation（仅 consolidated summary 里 Failed: 6 的文本记录）。属正常路径可复现的质量门静默绕过（对照串行语义应为重试/升级）。
- 建议方向：波后检查 `all_results`，任一审计 `not success` 时复用 `_handle_failure`（或整体重发波/至少对失败项单独重发），持续失败转 ESCALATION checkpoint；补"审计波全部失败"集成测试。

### F355 | 确定性策展层输出死线：context/chapter-N-curated.md 无任何 skill 读取（9 节分层编排生产无效） | error | P2

- 证据：`chapter_loop.py:1200-1227`（`_run_context_curation` 写 `context/chapter-N-curated.md`，注释声称 "FIX: actually persist"）；grep 全 skills/ 契约 `reads` **0 个引用该文件**；drafting 契约读取的是 `context/chapter-N-context.md`（未分层的 flat 三路由文档，load_contract 实证）；`shenbi-context-composing`（唯一读 context.md 的另一个 skill）已被策展替换、不在 CHAPTER_STEPS。
- 根因：Phase 2.1 "确定性策展替换 context-composing" 只实现了"写入侧"，消费侧（drafting 的 reads 改指向 curated.md）从未接线。
- 验证：`grep -rln "chapter-N-curated" skills/` → 0 命中；`grep -rln "chapter-N-context" skills/` → shenbi-chapter-drafting、shenbi-context-composing。
- 影响：9 节 P1-P9 分层文档（含结尾多样性、hook 债务简报 F347 的全部内容）生产**从不进入任何 LLM 上下文**——策展层（含 F356 的分层 bug、F347 的债务简报）整体空转；drafting 仍吃 flat context。
- 建议方向：drafting（及后续 skill）契约 reads 改指 `context/chapter-N-curated.md`，或删除策展层并迁移测试；同时确认 review-checklist 注入（dispatch_helper.py:767）与策展不重复。

### F356 | curate_context P 分层渲染失效：章节 plan 与全部 route 条目落入 P7，P1/P3-P6 恒 "(未产出)" | error | P2

- 证据：`context_curation.py:454-464`（`_render_context_document` 按 `_PRIORITY_ORDER` 前缀子串匹配分桶，不匹配则落入 P7）；`:143-154`（plan 以 `source="P1 章节备忘"` 插入）——"p1 章节备忘" 不含任何 `_PRIORITY_ORDER` 前缀（chapter-plan/book_spine/book_strata/volume_summaries/arcs/chapter_summaries/world_rules/style_profile/audit_drift）→ 落入 P7；`route-a:*`/`route-b:*` 同款 → 全部 P7。
- 根因：分桶用"源名子串"而非"结构化 source 字段/权重映射"；plan 节的 source 与 bucket 键（"chapter-plan"）不一致。
- 验证：
  ```
  uv run python -c "<构造 Section(source='P1 章节备忘',…)+route-a/route-b/route-c 调 _render_context_document>"   # → P1:(未产出) P2:SPINE P3-P6:(未产出) P7:PLAN+HERO+SUM+STYLE
  ```
- 影响：若 F355 接线后，LLM 看到的 9 节文档 P1（章节备忘）恒空、全部检索内容堆在 P7（世界铁律与文风）——分层语义全毁。当前与 F355 叠加为"坏输出 + 无人消费"。
- 建议方向：分桶改按 Section.category/source 前缀（"route-a"→P3/P5、"route-c:book_spine"→P2 等）显式映射；plan 节 source 改为可匹配键（如 "chapter-plan"）或直接按 priority==1.0 分入 P1。

### F357 | SharedAuditContext 的 style_profile 以幽灵路径 truth/style_profile.md 注入（style 内容重复进入每个审计 prompt） | error | P2

- 证据：`dispatch_helper.py:611-614`（`_INJECT_FROM_CACHE[_input_key(project_dir / "truth" / "style_profile.md")] = shared_context.style_profile`）；`audit_context_cache.py:63-65`（style 从 `style/style_profile.md` 读取）；生产 `style/style_profile.md` 存在（bootstrap 模式，实测）、`truth/style_profile.md` 不存在；grep skills/ 无任何契约读 `truth/style_profile.md` → `fname not in raw_inputs` 恒真 → 注入**恒发生**。
- 根因：注入键写错目录（truth/ 而非 style/）；"仅补缺"注入策略对不存在的键形同"恒补"。
- 验证：
  ```
  grep -rln "truth/style_profile" skills/*/SKILL.md   # → 0 命中
  ls novel-output/xinghuo-ranqiong/style/   # → style_profile.md
  ```
- 影响：每章每个审计 dispatch（波 1 的 6 个 + 波 2 的 genre 项）的 prompt 多出一个名为 `truth/style_profile.md` 的幽灵输入（2000 字截断的 style 内容）——重复 style 上下文、幽灵文件名可能误导模型，约 2000 字 × 6-8 次/章 的 token 浪费。**F348 声称"注入被跳过"对 style_profile 不成立**（其机制描述部分不准，见结论表 F348 行）。
- 建议方向：键改为 `style/style_profile.md`（或与 `_input_key(project_dir / "style" / "style_profile.md")` 对齐）后，"仅补缺"策略才会按预期工作。

### F358 | linguistic drift ESCALATE 被吞：DriftEscalationError 在 pipeline-linguistic-drift-check 的 except Exception 中仅 log；drift 指令文件无消费者 | error | P2

- 证据：`chapter_loop.py:2720-2728`（`try: _check_linguistic_drift(...) except Exception: log.warning(...)`）；`_check_linguistic_drift`（:2032-2038）在 ESCALATE 时 `raise DriftEscalationError`（docstring "Pipeline paused for human review"）；`:2061-2096` 写入 `context/drift-correction-{n}.md` / `drift-warning-{n}.md` / `opening-variation-{n}.md`——grep 全仓无任何消费者（drafting 契约 reads 不含这些文件，load_contract 实证）。
- 根因：调用方把"ESCALATE 暂停"异常当普通异常吞掉；指令文件写入后无注入点（spec §3.4 三级干预的 WARN/HARD/ESCALATE 均落空）。
- 验证：`grep -rn "drift-correction\|opening-variation" src/ skills/ --include="*.py" --include="*.md" | grep -v chapter_loop.py` → 无消费方。
- 影响：语言漂移检测（step 6，当前代码唯一有实现体的 pipeline-* 步骤）的 ESCALATE 档只产生一条 warning，管线照常推进；HARD/WARN 档生成的下一章指令从不进入 drafting 输入。三级干预契约静默失效。
- 建议方向：`except DriftEscalationError` 单独捕获 → set_checkpoint(ESCALATION) 暂停；将 drift-correction/warning 文件注入 drafting 输入（或并入 curated context）。

### F359 | Route A 检索条目无实体内容（仅 "[category] id from file" 标签），entity 事实从不进入上下文 | error | P2

- 证据：`context_assemble.py:117-131`（`_route_a` 的 `"text": f"[{e.category}] {e.entity_id} from {e.file}"`）；`query_index` 返回的 `IndexEntry.extra`（hook 的 content_keywords/state、rule 的 content）被完全丢弃；drafting 契约 reads 不含 characters/*.md、world/rules.md（load_contract 实证）→ 实体事实既不在 Route A 文本中、也不经契约 reads 到达 drafting。
- 根因：Route A 只把 entity 标识当作检索结果，未携带 `extra` 载荷；spec §7.1 "P1 direct involvement（权重 1.0）" 的实体内容检索未实现。
- 影响：权重最高的 Route A 条目在上下文里只是元数据行；hook 状态/规则全文/角色档案事实静默缺失（与 F346 hook 检索零命中叠加后 Route A 近乎空转）。
- 建议方向：`_route_a` 的 text 拼接 `extra` 内容（hook 取 content/state、rule 取 content、character 取卡片正文节选），或回退读取源文件片段。

### F360 | chapter_loop._maybe_rebuild_truth_index 只 build 不落盘（周期索引重建无效，日志误导） | optimization | P2

- 证据：`chapter_loop.py:836-844`（`if chapter % 15 == 0 or is_volume_boundary(...): build_index(project_dir); log.info("truth_index_rebuilt")`——返回值丢弃，**不写 truth-index.json**）；`truth_index.build_index` 仅返回 TruthIndex（无写盘副作用）；全仓唯一落盘点是 `genesis.py:127-128`（`safe_write(project_dir / "truth-index.json", ...)`）。
- 根因：周期重建接线只调用 build 侧、漏落盘侧（与 F308/F315 同族的"半接线"）。
- 验证：`grep -n "truth-index.json" src/shenbi/ --include="*.py"` → 仅 genesis.py:128 写、truth_index.py:358（CLI main）写；chapter_loop 不写。
- 影响：每 15 章一次"重建"只产生一条日志，磁盘 truth-index.json 恒为 genesis 版本（陈旧）；日志 "truth_index_rebuilt" 误导。当前 truth-index.json 无 pipeline 内读者（context_assemble 每章 fresh build），故为 P2 非 P1。
- 建议方向：`_maybe_rebuild_truth_index` 补 `safe_write(project_dir/"truth-index.json", index.to_json())`，或删除该调用并修正日志。

### F361 | _call_llm_streaming 的 early_stop_patterns 死参数（无调用方传值） | optimization | M

- 证据：`dispatch_helper.py:1383/1455`（形参 `early_stop_patterns: list[str] | None = None`）；grep 全仓调用 `_call_llm_streaming_with_retry`/`_call_llm_streaming` 处（:1537、:1597 等）均未传该参数 → `:1422-1429` 的 early-stop 分支恒不执行。
- 根因：Task 14 早期停止特性落地后调用方从未接线。
- 建议方向：删除死参数，或按设计在 API 路径传入 chapter-end 停止模式。

### F362 | cmd_backfill_context 使用 print() 违反框架约定（AGENTS.md：No print() in framework code） | M

- 证据：`cli.py:877`（`print(f"  Backfilled context for chapter {chapter}")`）、`:879`（`print(f"  FAILED chapter {ch}: {e}", file=sys.stderr)`）；AGENTS.md Python Conventions "No print() in framework code; use structlog"。
- 根因：维护命令（backfill-context，独立 subcommand）绕过 structlog 约定。
- 建议方向：改用 `log.info/log.warning`（或 emit_json 汇总结果）。

### F363 | run_triggered_skills 中途失败后重试会重发已成功的触发步骤（无 per-skill 进度追踪） | optimization | M

- 证据：`triggers.py:555-619`（按序 dispatch，任一失败即 `return False`，无已成功步骤记录）；`cli.py:248-266`（失败 → ESCALATION checkpoint → resume 后 `_orchestrate_to_checkpoint` 重新 `check_triggers(prev_ch)` → `run_triggered_skills` 从第一个步骤重跑，已成功的 l2_distill/score_arc 等被重复 dispatch）。
- 根因：触发执行无幂等/进度状态（对比 chapter 的 steps_done）。
- 验证：`grep -n "steps_done\|last_trigger" src/shenbi/pipeline/triggers.py` → 无 per-step 进度记录（仅 state.last_trigger_failure）。
- 影响：卷边界触发（若 F324 修复后可达）中途失败重试时，前半成功步骤重复消耗 LLM 调用；当前被 total>0 守卫屏蔽（潜伏）。
- 建议方向：run_triggered_skills 按步骤记录 done（可复用 add_step_done 或专用 trigger_done 集），重试时跳过已完成步骤。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件全量 deep-read，共 13,641 行）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries` 返回 set()；audits/ 722 文件全旧式名、group-* 数=0；chapters/ 145 文件补零数=0；plans/chapter-5-plan.md section 7 为 MH-001/H-N01 且 `chapter_role: 推进/转折` 存在；truth/pending_hooks.md frontmatter 无 hooks 键；truth/resonance_trend.md 9 列含 ch55 行；genre-config auditDimensions.sensitivity=true；novel.json 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56）；`uv run python` 执行 `_render_context_document` 分层实证（F356）、`_parse_assembled_sections`、load_contract 契约实证（resonance/lifecycle/state-settling/drafting/planning/volume-outlining 的 reads/writes/updates）。
- 前几轮覆盖空洞（本轮已补）：(1) **total_chapters 写入点自锁**（前几轮归因于 F324 解析器，未识别"正常流无写入点"这一独立根因）；(2) **审计波 dispatch 失败静默推进**（F331 只覆盖 G4 失败，未覆盖 dispatch 失败）；(3) **策展输出死线**（curated.md 无消费者，前几轮仅验证了写入侧 FIX）；(4) **P 分层渲染失效**（分桶子串匹配，无 prior finding）；(5) **style_profile 幽灵注入**（F348 机制描述错误的一半）；(6) **drift ESCALATE 吞异常 + 指令文件无消费者**；(7) **Route A 标签 stub**；(8) **索引周期重建不落盘**。
- 剩余覆盖洞（沿用三轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 路径（codex/zcode）本环境不可执行，仅静态分析；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内），group skill 契约旧式 writes 是 F325 反证的关键。

---

## 四、汇总

- **新 finding 数**：11（F353-F363，其中 F353/F354 = P1，F355-F360 = P2，F361-F363 = M）
- **误报数**：0（F300-F352 全部成立/维持；F325 维持误报撤销，本轮独立复证未发现新的误报；F348 为"成立但机制描述部分不准"）
- **严重度异议数**：0（F302 的 P1→P0 依据维持留 stage 4；F305 同意 P2；无新增异议）
- **事实修正（不改严重度）**：1（F348 机制描述对 style_profile/world_rules 不成立——注入实际以幽灵路径发生，见 F357；另 F327 补充 `_parse_resonance_score` 不匹配生产 `**结果**: 通过 (77/100)` 格式）
- **0 新 finding 判定**：**否** —— 本轮发现 2 条 P1 漏报（F353 total_chapters 写入点自锁、F354 审计波静默吞 dispatch 失败）与 6 条 P2 漏报（F355 策展输出死线、F356 P 分层失效、F357 style 幽灵注入、F358 drift ESCALATE 被吞、F359 Route A 标签 stub、F360 索引重建不落盘）及 3 条 M；F324（P0）维持成立、F325（误报）维持撤销、F340/F341/F345（P1）维持成立。
- **校准升级提示**：F353 与 F324 同属"pipeline 永不完成"的独立根因，建议阶段 4 复核时与 F324 合并处置（解析器 + 写入点必须一起修）；F354（审计门静默绕过）建议进入复核优先级前列；F355/F356 建议与 F347 的 hook 债务简报修复合并（策展层整体接线或整体废弃）。
