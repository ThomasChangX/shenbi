# Z1 分区第十五轮独立复核报告（agent p）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`~`Z1.review14.md`（F114–F164），共 **64 条**（前十四轮新发现 51 条）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `contracts/legacy.py`、`contracts/thresholds.py`、`gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g4/__init__.py`、`gates/g4/generic.py`、`gates/g4/worldbuilding.py`、`gates/g5.py`、`audit/_shared.py`、`dispatcher/executor.py`、`tools/lint_status_strings.py`、`tests/tiers/deps.json`、`command-to-give.md`、`tests/unit/test_phase_runner.py`、82 份 rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`、`.venv/bin/shenbi-validate`、端到端 phase_runner CLI（写目标仅限 /tmp）、真实 gate 调用模拟（/tmp 内构造输出目录）**）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑、真实控制台脚本实跑与 /tmp 内模拟（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F165–F199（实际产出 F165–F166，共 2 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：2**（F165 M、F166 M；**无新 P0/P1**）
- **误报数：0**（F101–F164 全部 64 条成立；无一条被证伪。F115/F140/F158/F163 四个 P1 经本复核**独立实跑/复跑/端到端实证确认无反证**——反证尝试全部失败）
- **严重度异议数：0 新增**（同意既有全部：F109 M→P2 共识、F150 的 R13 M→P2 升级、F115/F140/F158 的 P1 无反证维持、F134 维持 P2）
- **0 新 finding 判定：否**（2 条 M 级新漏报，均为前十四轮完全未覆盖的站点：F165 phase_runner/`__init__` docstring "T2/T3" 宣称与 G5 只认 t2-phases 的实现不符（T3 在 phase_runner 中不可运行）；F166 cmd_pre_skill 的 skill 参数未净化路径拼接（F158 只覆盖 phase 参数写侧，skill 读侧站点遗漏）。Z1 主要缺陷面经前十四轮 + 本轮收敛，本轮仅剩文案/输入校验级扫尾）

---

## 1. 复核结论表（F101–F164 逐条判定）

本复核对 64 条 finding 全部以 fresh-context 重读源码 + 独立实验验证；**结论：64/64 成立，误报数 0**。P1 四条（F115/F140/F158/F163）的反证尝试与实证输出见 §2，其余按文件族分组简述独立验证要点（每条均与当前源码行号逐一核对，无行号漂移）。

### 1.1 P1 判定（F115/F140/F158/F163）——无反证，全部维持

| 编号 | 标题 | 定级 | 本复核独立验证 |
|---|---|---|---|
| F115 | 维度过滤对 38/82 rubric 静默 no-op | P1 | **实跑复现**：`load_applicability(worldbuilding)` → `{}`（表头 `\| # \| Dimension \| Bug-hunt Standard \| Clean Standard \|` 不匹配解析器认的 `\| Dimension scope \|`，scoring.py:88）；`filter_dimensions_by_test_type(dims, wb, 'bug-hunt')` → 9 维原样返回（**含 N/A 豁免的 dim 4 Prose quality，10% 权重**）。反证尝试（"协议不实际用这些 rubric"）被 4 技能 bug-hunt/clean 测试目录 + command-to-give.md:64,71 文档化路径证伪。维持 P1 |
| F140 | `shenbi-score` 成功路径恒 exit 1 | P1 | **实跑 `.venv/bin/shenbi-score`**：10 维 scores + `--test-type generative` → 评分完全成功（stdout 合法 JSON，`final_score: 81.6 PASS (acceptable)`）但 **EXIT: 1**，stderr 为完整 result dict repr（含 `classification: <ScoreClassification...>` 枚举对象 repr）；`main()` 成功路径 `return result`（scoring.py:463）→ hatchling 包装 `sys.exit(main())` → `SystemExit(dict)` → exit 1；违反 command-to-give.md:57-61 "0=成功" 契约。反证（"返回 dict 是库 API 设计"）被 `scoring.main` 无库调用方证伪。维持 P1 |
| F158 | phase_runner phase 参数路径穿越（写侧） | P1 | **端到端实跑复现**：`shenbi-phase start ../../../../tmp/zb15_escaped --round-dir /tmp/zb15_r` → G5 FAIL（exit 1，`unknown phase`）但 **`/tmp/zb15_escaped.json`（289 字节 state JSON）被创建**、round_dir 内 0 文件（G5-FAIL 分支 :126-129 无条件 save_state → state_file = `Path(round_dir)/"phase-state"/f"{phase}.json"` 的 `..` 逃逸）。维持 P1（触发需操作者主动传路径形态 phase，合并层可议 P0，本复核不新增异议） |
| F163 | phase_runner G4 第 3 参传 round_dir 而非 project-dir → T2 特定 checker 输出查找全 miss | P2 | **真实 gate 调用模拟（对照组实证为目录错位而非 checker 缺陷）**：在 `/tmp/zb15_g4r/project-output/` 构造完整输出（novel.json/genre-config.json/world 三件/truth 四件）后，按 phase_runner 原样调用（3rd 参 = round_dir）→ **`status: FAIL`，must_fix 6 项全 not_found**；对照组（3rd 参 = project-output）→ **`status: PASS`，10 项 check 全过**。根因确认：gates/cli.py:118 `project_dir=rd`（round_dir）而 g4_worldbuilding.py:30-34 RoundPaths 以 rd 为根做 `rp.read("novel.json")` 相对名查找。维持 P2（T2 未实际运行、失败为 loud BLOCKED；若合并层确认 T2 已在运行可议升 P1） |

### 1.2 其余 60 条判定（成立；独立验证要点）

- **safe_write 家族（F101/F108/F119/F133/F154）**：F101 实证 `git ls-files -s`（100644）vs `stat -f %Sp`（`-rw-------`）双文件；F108 mkstemp 在 try（:106）外、F119 stale-takeover 无条件 unlink + flock 异常 fd 泄漏、F133 释放路径按路径 unlink 删新持有者锁、F154 flock↔O_EXCL 双原语互不探测——逐行核对 :54-91/:103-125 全部成立。
- **scoring 校验链（F104/F106/F118/F126/F129/F131/F134/F135/F141/F142/F146/F149/F151/F152/F155/F157）**：F135/F141/F155 实跑复现（NaN/bool 通过校验、agreement NaN 误报 agreed、单维度 collapse）；F134 实测 `validate_scores({1:100,2:95}, [])` → (True, [WARNING]) + compute → 0；F142 实测 chapter-drafting bug-hunt 过滤后权重和 75；F149 模板触发实证；F151 `{"1":90,"dimensin1":100,"notes":"x"}` → `{1:90}`；F152 kill-switch 漏 "→ detection dimension = 0"（worldbuilding 等 12+ 份）；F157 交互 input() 污染 stdout；F126 实跑输出 `"scored_by": "interactive"`；F118 `_phase` 仅 :321/:331 两处赋值。
- **phase_runner 状态机/CLI（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150/F156/F162/F164）**：F105 实跑确认 G5.3 对 `"None"` 走扫描块报 **PASS**（非 R1 所称 SKIP，R3 修正正确）；F114 `issubclass(TimeoutExpired, OSError)==False`（mro 实测）；F150 走查 g2.py:237/382-384 + executor.py:200-204 阻断路径（R13 P2 升级成立）；F156 derive_file_type('shenbi-chapter-drafting')→'decisions' + derive_output_files 混合输出实证；F158/F164 见 §1.1/§1.3 注；F162 :185 与 :337 双 configure_logging 实证；F164 `"g5": "PASS"`（:124）裸字面量 + lint（STATUS_KEYS 仅 status/state/classification）不捕获（实跑 exit 0）。
- **sync_contracts（F109/F117/F130/F147/F153/F159）**：F109 :121 assert；F117 :40-41 `except ContractError: continue` 无日志；F130 :146-147 `if not m: return` 无日志 + :152 count=1；F147 :165/:195 无守卫；F153 render_body_view 体内 `skill` 0 命中；F159 全文无 configure_logging（对照 phase_runner:337/scoring:283）。
- **死代码/词汇表（F103/F110/F112/F123/F127/F136/F138/F139/F161）**：F127/F136 实跑 grep 0 消费（REJECT/MARKER_MISSING 在用、CommandResult 仅 status.py:78 定义）；F161 `rp.write` 0 命中（read 21 处）；F138 两文件 `log.` 0 命中；F103 AST 扫描 5/22 RAISED；F112/F123/F139 均重读确认。
- **文档/断链（F102/F111/F125）**：F102 5 个顶层 docs/*.md 全 MISSING + `ls tests/build_registry.py` No such file（ToolTamper 的 action 存在仅 doc_url 破）；F125 `find . -name "*dispatch-subagent*"` 0 命中。
- **加载边界家族（F144/F145/F146/F147）**：F144/F145/F146 逐行核对无 try/形状守卫；F147 对照同文件 :167-172 bail 有 log.error + return 1。
- **G2 混合输出/回退（F121/F143/F148/F150/F156）**：F121 :208-210 `[:20]`；F143 g2.py:76-84 decisions 分支 `.md` 显式 continue（crash 论断不成立）；F148 :196-200 exists 过滤无日志；F150/F156 见上。

**前十四轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 + 实跑 **确认 R3 修正正确**；F101 波及文件数递增记录属实（本复核时点两文件均 100644/0600 实证）；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致。**无新的前轮事实错误需要修正。**

---

## 2. P1 反证尝试汇总（全部失败）

| 编号 | 反证假设 | 证伪结果 |
|---|---|---|
| F115 | "bug-hunt 协议不实际使用 worldbuilding 等 4 技能 rubric" | 4 技能均有 bug-hunt/clean/generative 测试目录（ls 实证）；command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径原文存在 |
| F140 | "`scoring.main` 返回 dict 是库 API 设计，无 CLI 消费方" | grep 全 src：`scoring.main` 无任何库调用方；`.venv/bin/shenbi-score` 为标准 hatchling 包装 `sys.exit(main())`（实跑 exit 1）；对照 `shenbi-phase`（返回 None→exit 0）与 `shenbi-sync-contracts`（返回 int）均正确，仅 shenbi-score 受影响 |
| F158 | "phase 参数仅用于状态文件名，`..` 会被 Path 拒绝" | 端到端实跑：`/tmp/zb15_escaped.json` 被创建（289 字节）、round_dir 内 0 文件——`..` 在文件名内由 OS 归一化，写侧逃逸实证 |
| F163 | "G4 失败是 checker 缺陷或输出格式问题，非目录错位" | 对照组：同一输出集，3rd 参改传 project-output → **status: PASS 全 check 通过**——目录错位而非 checker 缺陷，铁证 |

---

## 3. 新发现（F165–F166）

F165 | phase_runner/`__init__` docstring 宣称 "T2/T3 phase state machine" 与实现不符：G5 只认 t2-phases（t3 名恒 FAIL "unknown phase"），pre_score/finalize 只读 `deps["t2-phases"]`，command-to-give.md:126 明示 "T3 不使用 phase-runner.py" → phase_runner 实际 T2-only，"T2/T3" 承诺为假 | doc↔code drift | **M**
- 证据: src/shenbi/phase_runner.py:2（"State machine for T2/T3 phase execution"）；src/shenbi/__init__.py:7（"phase_runner: T2/T3 phase state machine"）；src/shenbi/gates/g5.py:41-42（`deps.get("t2-phases", {}).get(phase_name)` → 无则 `fail(... "unknown phase")`）；phase_runner.py:246/:318（均 `deps.get("t2-phases", {})`）；command-to-give.md:126（"T3 不使用 phase-runner.py。G6 gate marker 是评分的前置条件"）；tests/tiers/deps.json `t3-pipelines`（long-form/short-form/import-form，无 expected_outputs）
- 根因: T3 由独立机制执行（pipeline 层 + G6 marker + `shenbi-score` t3 分支），phase_runner 从未被设计/接线为 T3 状态机；模块 docstring（及 `__init__` 子模块清单）未随 T3 分流更新。前十四轮对 "T2/T3" 字面只以 scoring `--tier` 参数（F118）与 R8 备查 #3（"未知 phase → G5 FAIL"）侧面触及，从未指出 docstring 承诺本身与 G5 只认 t2-phases 的矛盾
- 验证命令+输出: `PYTHONPATH=src ./.venv/bin/shenbi-validate G5 long-form /tmp/x /tmp/x/project-output` → `{"status": "FAIL", "must_fix": ["unknown phase: long-form"]}`（t3 pipeline 名在 start/finalize 的 G5 处即被拒，T3 相位在 phase_runner 中完全不可运行）；`grep -n "t2-phases" src/shenbi/phase_runner.py` → :246/:318 两处硬编码
- 建议方向: 修正 phase_runner.py:2 与 __init__.py:7 为 "T2 phase state machine"（或明确 "T2 状态机；T3 见 pipeline 层 + G6 协议"）；若未来确需 T3 相位支持，需 g5.py 增加 t3-pipelines 分支（含 G6 前置语义）。（严重度说明：纯文档/承诺漂移，误触发为 loud FAIL 无静默危害，按 M）

F166 | phase_runner cmd_pre_skill 的 skill 参数未净化直接拼接路径：`PROJECT / "skills" / skill / "SKILL.md"` 与 load_contract 内部 `SKILLS / skill / "SKILL.md"` 均可穿越 skills/ 目录做任意路径存在性探测、并把技能目录外任意位置放置的 SKILL.md 解析为契约（F158 只覆盖了 phase 参数的写侧穿越，skill 参数读侧站点前十四轮完全未提及） | error | **M**
- 证据: src/shenbi/phase_runner.py:137（`skill_path = PROJECT / "skills" / skill / "SKILL.md"`——skill 直接来自 `args[0]` 无白名单/净化）；src/shenbi/contracts/legacy.py:204-205（`_skill_path` 同样 `SKILLS / skill / "SKILL.md"` 拼接，被 load_contract 复用）；:138（exists 探测后继续执行）、:150（load_contract 解析穿越路径文件）；对照 F158（phase 参数 :37/:46-47 写侧）——skill 为同一 CLI 输入校验家族遗漏的第二个参数
- 根因: main() 对 phase/skill 两个位置参数均无内容校验；skill 路径拼接允许 `../` 逃逸 skills/ 目录（读侧）；影响 = 任意路径存在性探测 + 外部 SKILL.md 冒充契约被解析（契约 reads/writes 随 emit 输出、在 post-skill 参与 derive_output_files 解析）——均需操作者主动传恶意名（与 F158 触发面相同），无写侧危害
- 验证命令+输出: `python3 -c "from pathlib import Path; p=Path('/Users/xiaotiac/Documents/GitHub/shenbi')/'skills'/'../../../etc/passwd-relative'/'SKILL.md'; print(p.resolve())"` → `/Users/xiaotiac/Documents/etc/passwd-relative/SKILL.md`（escape skills/ 实证）；`grep -rn 'skills" / skill\|SKILLS / skill' src/shenbi/phase_runner.py src/shenbi/contracts/legacy.py` → :137 / :204
- 建议方向: 与 F158 同族处置——skill 入参白名单（`^[a-z][a-z0-9-]*$` 或 `in ALL_SKILLS`），非法 → emit_json ERROR + exit 1；或 load_state/save_state 式 `resolve()` 后断言 `relative_to(SKILLS)`；补 "path-like skill → 拒绝" 单测。（严重度说明：读侧 + 操作者主动触发，无数据写危害，按 M；若合并层将 F158 的输入校验家族视为整体可随其议升，本复核不主动升）

---

## 4. 严重度异议汇总

| 编号 | 前十四轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有共识）** | assert 为运行时正确性守卫（docstring "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json → 静默数据漂移 |
| F150 | M→P2（R13 升级） | **同意 P2（无异议）** | 独立验证 executor 路径（非 pipeline 模式）G2 FAIL 当下 return 1 阻断，且 volume_map.md 不在 REPO_ROOT/outline/ → `_is_important_chapter` 恒 False → 重要章节（4500-10000 字）经 dispatcher 路径当下误 FAIL |
| F115/F140/F158 | P1 | **维持 P1（无反证）** | 见 §2 反证尝试（全部失败）+ §1.1 独立实证 |
| F134 | P2 | **维持 P2（不议升）** | 触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析），缺"正常路径可复现" |

**新增严重度异议：0。** 其余 58 条严重度无异议。

---

## 5. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 全部独立通读；另核实 12 个跨区依赖文件/数据：contracts/legacy.py、contracts/thresholds.py、gates/cli.py、gates/shared.py、gates/g2.py、gates/g4/__init__.py、gates/g4/generic.py、gates/g4/worldbuilding.py、gates/g5.py、audit/_shared.py、dispatcher/executor.py、tools/lint_status_strings.py、tests/tiers/deps.json、command-to-give.md、tests/unit/test_phase_runner.py、82 份 rubric）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型六方法核实无洞；F123 覆盖无接线）、`cli_utils.py`（F160 覆盖唯一剩余分支；allow_nan 为 R11 备查已记录）、`error_guidance.py`/`recovery.py`（F102/F103/F138 覆盖）、`exceptions.py`（F103/F110 覆盖）、`logging.py`（双渲染器/stderr 自洽；SHENBI_LOG_FORMAT 校验为前轮已记录低价值项）、`paths.py`（F112/F161 覆盖）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（F127/F136 覆盖）、`safe_write.py`（F101/F108/F119/F133/F154 覆盖；剩余分支为防御/平台边界）、`sync_contracts.py`（F109/F117/F130/F147/F153/F159 覆盖；build_dag 重复边为无消费者派生文档噪音——R11 备查已记录）、`scoring.py`（F104/F106/F115/F118/F120/F126/F129/F131/F134/F135/F140/F141/F142/F146/F149/F151/F152/F155/F157 覆盖）、`phase_runner.py`（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150/F156/F158/F162/F163/F164 覆盖，另 F165/F166 本轮新增）
- 新发现分布：`phase_runner.py`（F165——T2/T3 docstring 漂移、F166——skill 参数读侧路径拼接）

### 备查项（前轮已记录、本复核独立确认后同意不立 finding）

1. **filter 全排除回退语义反转**（scoring.py:136 `return filtered if filtered else dimensions`——"全部排除"≡"全部保留"；本复核复现 `== True`；R10 备查 #2 已记录，当前 82 份 rubric 均含 Yes 行无触发，同意不立 finding）。
2. **`--gate-only` G4 参数形状错配**（files 被当 skill 名、ftype 被当文件串；R14 备查 #2 已记录，同意不重复）。
3. **scoring WARNING 类 validation_errors 以 log.error 输出**（R8 备查 #2）；**emit_json allow_nan**（R11 备查 #3）；**`--test-type` 大小写敏感与 marker 名交互**（R14 备查 #5）；**非 dict scores.json → `.items()` AttributeError**（R4 备查 #3）；**缺失 rubric/scores 文件裸 traceback**（R7 备查 #2）；**`--interactive` 必须 argv[2]**（R2 备查 #3）；**`--chapter abc` ValueError**（R3 备查 #5）；**find_flag 值不校验**（R7 备查 #6）；**gate_manifest pipeline-manifest.json 无生产读取方**（R12 备查 #1）；**G2.10 注释与实现不符**（gates 区，R12 备查 #2）；**AGENTS.md 67+2=69 与 74 skills 目录不符**（非 Z1，R7 备查 #7）。
4. **R1 must-test 覆盖空洞仍未落实**（safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径无单测触达；R14 新增 G4 特定 checker × T2 布局端到端空洞）——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133/F163 覆盖。

---

## 6. 其他说明

- 前十四轮"P1×3（F115/F140/F158）+ P2×1（F163 为 R14 新增 P2）"结论：本复核独立实跑/复跑/端到端实证后**无反证，全部维持**；误报数 0（64/64 成立）。
- 新发现 F165–F166 均为 M 级（docstring 漂移 / 读侧输入校验），不改变"无 P0"结论，也不新增 P1。Z1 区十五轮复核累计 66 条 finding（F101–F166）：P1×3（F115/F140/F158）、P2×26（含 F109/F150 议升）、M×37；主要缺陷面（safe_write 并发五站点、scoring 校验全链、phase_runner 状态机/CLI/穿越、sync_contracts 生成链、加载边界家族、G2/G4 目录与类型错位）已完全收敛。
- 跨区一致性核查：F165 与 command-to-give.md:126（T3 独立执行）、scoring t3 分支（check_gate_markers :216-222 支持 t3-pipeline rubric）对照——scoring 支持 T3 rubric 而 phase_runner 不支持 T3 相位，二者共同构成 "T2/T3" 字面的半真半假；F166 与 F158（phase 写侧穿越）同族互补；与 Z5 F500（scoring_bridge dead 链）、Z3 区 pipeline 消费方缺陷无重叠。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140/F126 经**实跑 `.venv/bin/shenbi-score`** 实证退出码与 scored_by；F158 经**端到端实跑 phase_runner CLI（写目标仅限 /tmp）**实证穿越写出；F163 经**真实 gate_G4/g4_worldbuilding 调用模拟（/tmp 内构造输出目录 + 对照组）**实证 FAIL→PASS 反转；F115/F134/F135/F141/F142/F149/F151/F152/F155 经 `PYTHONPATH=src` 纯内存导入复跑；F164 经实跑 lint 工具（exit 0）实证；临时文件仅写 /tmp，未触碰仓库文件。

---

## 7. 返回摘要

- **复核统计：新 finding 2（F165 M / F166 M）/ 误报数 0 / 严重度异议 0 新增（维持 F109 M→P2 共识、F150 的 R13 M→P2 升级、F115/F140/F158 的 P1、F134 的 P2；F163 的 P2 经独立对照组实证维持）**
- **0 新 finding 判定：否**（2 条 M 级新漏报：F165 phase_runner/`__init__` docstring "T2/T3" 宣称与 G5 只认 t2-phases 不符（`shenbi-validate G5 long-form` → FAIL "unknown phase"，T3 相位在 phase_runner 中不可运行；command-to-give.md:126 佐证）；F166 cmd_pre_skill skill 参数未净化路径拼接（`PROJECT/skills/<skill>/SKILL.md` 可穿越 skills/ 目录，读侧存在性探测 + 外部 SKILL.md 冒充契约，F158 家族遗漏的读侧站点）。均无新 P0/P1；Z1 主要缺陷面已收敛）
