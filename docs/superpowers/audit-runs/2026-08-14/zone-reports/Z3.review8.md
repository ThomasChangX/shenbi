# Z3 分区第八轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第八轮独立复核（本文件 `Z3.review8.md`，与初审 `Z3.a.md`、一轮 `Z3.review.md`、二轮 `Z3.review2.md`、三轮 `Z3.review3.md`、四轮 `Z3.review4.md`、五轮 `Z3.review5.md`、六轮 `Z3.review6.md`、七轮 `Z3.review7.md` 隔离）
> 复核范围：34/34 文件全量逐行重读（__init__.py 1 行 → chapter_loop.py 3053 行 → dispatch_helper.py 1986 行 → cli.py 939 行全部读完），逐条独立验证 F300-F378；以真实生产项目 `novel-output/xinghuo-ranqiong/` 为格式/行为对照基准
> 本轮发现编号：F379-F383（5 条新发现，其中 **1 条 P1**、4 条 P2）；**新增误报 0 条**；新增严重度异议 0 条（立场声明见结论表）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证全部在 /tmp 与只读 python 中进行）
> 方法：34 文件逐行重读 + grep/uv run python/契约 load_contract/G4-CLI/路径解析实证 + 真实产物对照 + **closure/genesis 全步骤 prompt-build 直接执行实验**（本轮唯一新验证手段，见 F379/F380）

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`；novel.json keys 无 total_chapters；pipeline-state.json phase=chapter-loop、current_chapter=56、closure_step=0（56 章完成仍未进入 CLOSURE）。
2. **F325（误报）维持撤销**：4 个 group skill 契约 `writes:` 即旧式路径（本轮 load_contract 实证 group-factual → `['audits/chapter-N-continuity.md','audits/chapter-N-world-rules.md','audits/chapter-N-pacing.md']`）；生产 audits/ 722 文件全为旧式名、group-*.md 数=0；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING（`严重度: BLOCKING`）被 route 正则与文件扫描双重命中 → revision 执行。
3. **本轮最重要新发现（F379，P1）**：**closure 步骤 2/4/5/6/10 在 API/IDE 派发路径下于 prompt 构建阶段即确定性失败**——closure prompt 不含 "chapter N"（`extract_chapter` 返回 None），而 5 个步骤的契约 writes 含 N/NNN 占位符（`truth/arcs/arc-N.md`、`audits/volume-N-score.md`、`audits/volume-N-payoff.md`、`audits/chapter-N-long-span.md`、`snapshots/chapter-NNN/*`），`resolve_chapter_path(path, None)` 抛 `UnresolvedPathError` → `_dispatch_via_api`/`_dispatch_via_ide` 捕获后返回 `DispatchResult(False, "Prompt build failed")` → `_handle_closure_failure` 重试×3 → ESCALATION。**10 个 closure 步骤中 5 个永不派发**，比 F313（G4 错位）与 F371（step 10 G4 目录路径）更根本（派发先于 G4 失败）。本轮以实际执行 10 步 prompt-build 实证（见 F379 验证节）。
4. **F364（P1 贴 P0）维持成立，本轮补一条事实注记**：`atexit.register(_emergency_cleanup)` 位于 `_run_chapter_step_impl` 的 `step_idx == 0` 分支（chapter_loop.py:2508-2510，注释声称 "Register emergency handler ONCE" 但无任何去重）；CLI 一次性进程模型（每次 `next`/`resume` 为独立进程、每章必停在 CHAPTER_MEMO/STATE_SETTLE 检查点）下单次调用正常只跨一个章首 → 每进程注册 1 次；但任何单次调用跨章完成（如 --auto 下未来修复 F341 卡点后）会在同一进程内多次注册 → 退出时 `_emergency_cleanup` 重复执行。核心缺陷（正常退出无条件清 staging + 改写持久化 current_step）无需放大即成立，P1→P0 依据维持（裁定留 stage 4）。
5. **F353（P1）/F354（P1）/F345（P1）/F340（P1）/F341（P1）/F326（P1）/F373（P1）全部维持成立**（逐条独立实证见结论表；F371/F313 维持 P1 但注意 F379 使其在 API/IDE 路径下的即时影响被"派发先失败"取代——G4 侧缺陷仍在 legacy 路径与 F379 修复后激活）。
6. **版本偏移注记（沿用三~七轮）**：生产 pipeline-state 为 Plan 18 重构前旧版管线产物（steps_done 含旧 skill 名），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据；staging/ 119 文件为旧版遗留。
7. **环境事实（本轮新确认，影响 F379/F383 定性）**：`codex` 已安装于 /opt/homebrew/bin/codex → `_find_ide_cli()` 返回非空 → 本环境（无 SHENBI_LLM_API_KEY）实际走 **IDE 派发路径**（dispatch_helper.py:1833-1841），IDE 路径同样调用 `_build_skill_prompt`（:1710-1717）→ F379 在本环境即确定性可复现；F383（stdout 全量写每个输出文件）为 IDE 路径的潜伏数据损坏点。

---

## 一、复核结论总表（F300-F378 逐条判定）

> 判定基于本轮独立 read/grep/uv run python/契约实证与真实产物对照。全部 34 文件逐行重读。

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")` 源码字面量求值即 `<`，恒 no-op |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 :2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1` → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析/趋势落盘）不可达（本轮逐行确认无其他调用点）；cs.resonance_score 并行路径恒 None。三轮修正成立：resonance_trend.md 由 shenbi-review-resonance 契约 updates 自身写入（本轮 load_contract 实证） |
| F302 | **成立** | P1 维持（P0 依据存在——决策表 P0 例字面列明 "TokenLedger 少计"；裁定留 stage 4，无异议） | `dispatch_skill(state=)` 仅 chapter_loop.py:2799（串行可及）/ :2970（串行 BLOCKING 环，不可达）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751、revision_router:160、audit_layer:170 均未传；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0（PipelineState 无 chapter 属性，本轮读 state.py 确认） |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 在 :2718-2732 空转（仅 linguistic-drift-check 有实现体）；cmd_rollback not_implemented（cli.py:823-843）；`_get_snapshot_retention` 硬编码 50（:1371-1376） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError；machine.clear_checkpoint 的 ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93）→ crash-resume 与 escalation-resume 两路径 |
| F305 | **成立但框定需修正（同意二~七轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；route==NO_REVISION 时 `_is_revision_skipped`（:2755）先于 `_should_run_step` 短路。残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 记账漂移（本轮重读确认） |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方（本轮实测生产该路径 MISSING）；:97 `Chapter {chapter}` 对 "Chapter 10" 子串误中；volume_context 无消费方（_INJECT_FROM_CACHE 只注入 4 字段，:601-621） |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用；`_parse_hook_entries`（hook_planting.py:144）的 `^(MH|H)-` 对生产 plan section 7 的 `MH-001`/`H-N01` 行会接受但 `raw_op=="advance"` 非 plant/open 被跳过（:151）；检索侧断裂见 F346 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state（state.py:383-428）无生产调用（grep 实证） |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:430-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方（本轮 grep 确认唯一生产 upsert 调用为 chapter_loop.py:3014 upsert_markdown_row） |
| F313 | **成立** | P1 维持，无异议（注意 F379 交互） | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（契约 writes 实证）→ G4 校验错位。**注意**：API/IDE 路径下步骤 6 在派发阶段即失败（F379），F313 的 G4 失效面在 legacy 路径与 F379 修复后激活——两条独立缺陷 |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op；chapter_loop.py:2425-2426 调用无效果 |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback（:552）无章号绑定、注入下一次串行 dispatch（:2775-2780）；同源缺口见 F365 |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用（grep 实证；generate_review_checklist 本体已在 dispatch_helper.py:767 接线）；`P\d+-[a-z]+` 不匹配生产数字 id；`_extract_hooks_from_plan` 还用 `chapter-{chapter:03d}-plan.md` 补零文件名（生产不补零，F335 同族） |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符（双重死） |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`；`_END_RE`/`_RANGE_RE` 对生产 `**章节范围**: 第1章 - 第15章（共15章）` 零命中；novel.json keys 无 total_chapters。影响链（卷边界触发全家 / `if total > 0:` 守卫跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 卷名 "Unknown" / 章节节点提取裸数字 `\|\s*1\s*\|` 误中跨卷钩子表 `| 1 | 梵天铭文金属片… | 物品 |` 行）全部成立 |
| **F325** | **不成立（误报，维持二~七轮撤销）** | **P1 裁定撤回维持** | 本轮实测 4 个 group skill 契约 writes 即旧式路径（load_contract 实证）；生产 audits/ 722 文件全旧式名、group-* 数=0；真实 BLOCKING（`严重度: BLOCKING`）被 collect_audit_issues 正则命中 → route≠NO_REVISION → `_any_audit_has_findings` 同文件必 True → revision 执行。残余真值（P2）：清单缺 resonance/review-summary、含生产不产生的 texture/reader-pull |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证）；并行 post-draft（:2405-2419）两线程均以 uses_staging 写 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用；route_chapter_revision 的 blocking 形参 0 次使用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）。四轮补充成立：`_parse_resonance_score` 三模式均不匹配生产 `**结果**: 通过 (77/100)` 格式（本轮重读 :1285-1321 确认） |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代（grep 仅定义）；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议 | 本轮实测 production genre-config auditDimensions.sensitivity=true → `get_active_genre_audits` 含 shenbi-review-sensitivity；core 波（:2551，含 step 14 shenbi-review-sensitivity）与 genre 波双发同一 skill 写同一文件 `audits/chapter-N-sensitivity.md` |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库（该路径被 F364 清空后实际"静默不入库"） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关（--auto 下 1 次重试后 continue regardless）；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零；生产 chapters/ 补零数=0；其余读章路径全不补零；chapter_text 恒不注入 prompt |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖（本轮重读确认） |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict（本轮 grep 实证硬失败字段无写入方）→ `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；review-summary.md 恒报 0 问题 |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死（本轮重读确认短路语义） |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 全路径无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用（本轮 grep 实证） |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:24-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（本轮 grep parallel_dispatch.py 0 命中）；6 个 audit skill 全部 requires_independent=True（实证）；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 `MH-001`/`H-N01` 零匹配（本轮实测）→ master hooks 从 Route A 检索静默缺失；`MH02 → ['H02']` 误截成立 |
| F347 | **成立** | P2 维持，无异议 | 生产 pending_hooks.md frontmatter keys 无 `hooks` 键（hook 以 body 表格呈现）；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)"；`_read_spine_master_hooks` 的 `| MH` 行兜底对生产 book_spine.md 零命中 |
| F348 | **成立但机制描述部分不准（维持四~七轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入（本轮 load_contract 实证）；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md 生产均 MISSING）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层；非合规输出先失败一次再重试 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout |
| F353 | **成立** | P1 维持，无异议 | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入（cmd_init :423-428 只写 seed 字段；shenbi-volume-outlining 契约 writes 仅 outline/volume_map.md，本轮 load_contract 实证）→ F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；grep skills/ 契约 reads 0 个引用该文件；drafting 契约 reads `context/chapter-N-context.md` → 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断）每章每审计恒注入（幽灵输入 + token 浪费） |
| F358 | **成立** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError（:2034 raise，docstring "Pipeline paused for human review"）；drift-correction/warning/opening-variation 指令文件无任何消费者 |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra 被丢弃；drafting 契约 reads 不含 characters/*.md、world/rules.md → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存，无写盘副作用）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358（CLI main） |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |
| F364 | **成立（本轮补事实注记）** | **P1 维持；严重度异议支持 P1→P0（依据决策表"数据损坏/丢失 + 契约被静默违反"；裁定留 stage 4）** | `atexit.register(_emergency_cleanup)`（crash_recovery.py:66）在 chapter_loop.py:2508-2510 的 `step_idx == 0` 分支注册且无去重——CLI 一次性进程模型下单次调用正常注册 1 次，但任何跨章完成（--auto 未来修复 F341 后）会多次注册；`_emergency_cleanup` 正常退出时无条件 `clear_staging`（:144-148）+ save_state + 写紧急快照 + 把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`（:113-127）；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交 → chapter-planning 无限循环）成立。放大面注记为本轮新确认（无去重注册 + 每进程至少 1 次正常退出清理） |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交（`_commit_staging_for_checkpoint` 只在 review 时触发）+ 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`——step 2 时当章 plan 尚不存在 → FileNotFoundError → 捕获 → `_write_minimal_context_fallback`；且 chapter-planning 契约 reads 不含 context/chapter-N-context.md → 组装成功也不被消费 |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格 → 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |
| F371 | **成立** | P1 维持，无异议（注意 F379 交互） | 本轮 G4 CLI 实测：`shenbi-snapshot-manage "final-snapshot/" /tmp/nonexistent-project` → `G4.gen.not_found`；目录已存在 → `G4.gen.read_error`——两态恒 FAIL。**注意**：API/IDE 路径下 step 10 在派发阶段即失败（F379：`snapshots/chapter-NNN/*` 契约在 chapter=None 时抛 UnresolvedPathError），F371 的 G4 失效面在 legacy 路径与 F379 修复后激活——两条独立缺陷 |
| F372 | **成立** | P2 维持（latent），无异议 | `_style_profile_is_stale`（triggers.py:103-124）`chapters_dir.glob("chapter-*.md")` 计数把 `chapter-N-pre-rev.md` 计入（F350 备份恰落入通配；对照 `_load_previous_titles` :2251 用 `chapter-(\d+)\.md` 正则排除）→ style-learning 自愈触发提前/误触发 |
| F373 | **成立** | P1 维持（潜伏），无异议 | 本轮 G4 CLI 实测：`shenbi-score-arc "audits/arc-N-score.md" /tmp/g4n8` → `G4.not_found:audits/arc-N-score.md`（FAIL）；同内容文件 `audits/arc-5-score.md` → PASS。`run_triggered_skills`（triggers.py:584）把字面 N 路径直接传 G4，dispatch 侧经 `resolve_chapter_path` 解析为章号 → 5 个 N 型触发步骤（memory-distill L2 / score-arc / score-stratum / score-volume / review-arc-payoff）G4 恒 not_found → 周期/卷边界触发可达后必进 ESCALATION |
| F374 | **成立** | P2 维持，无异议 | 本轮实测 `classify_skill_write_safety('shenbi-review-resonance').value → read_only_audit`（write_safety.py:45-46 前缀分类），而契约 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`（load_contract 实证）→ 并行审计波在无串行保护下并发写 truth 文件（模块自述的 WRITE_SHARED 串行不变量被违反，潜在竞态） |
| F375 | **成立** | P2 维持，无异议 | dispatch_helper.py:1667-1671 `missing` 非空仅 log.error 后 `return DispatchResult(True, ...)`；`_write_parsed_outputs`（:1153-1168）对 parsed 缺失路径走空内容 skip → state-settling 6+1 文件漏写其一仍 success，G4 只校验现存文件 |
| F376 | **成立** | M 维持，无异议 | truth_embed.py:243-251 `update` 无 `--text` 时落 `emit_json(OK)` 不做任何嵌入 |
| F377 | **成立** | M 维持，无异议 | llm_output_integrity.py:51 `VERDICT_MARKERS` 含通用词 "通过"，:149 `has_verdict` 恒 True → `G4.ac.no_verdict`（:150-153）几乎不触发 |
| F378 | **成立** | P2 维持，无异议 | 本轮 grep：`context/volume-N-complete.json` 仅 chapter_loop.py:959/963 读取、全仓 0 写入方 → `_check_volume_completion` 恒 False → `_check_soft_fail_escalation`（:997）经 `check_escalation` 恒发 volume_objective_missed 信号；`last_trigger_failure` 仅 triggers.py:575/593 写入、无消费者 |

**误报统计**：F300-F378 中仅 F325 为误报（二轮撤销、三~七轮维持，本轮独立复证维持）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：0 条新增。立场声明（均无新增异议）：F302（P1 维持，P0 依据存在留 stage 4）、F364（P1 维持，**本轮补强 atexit 逐章追加注册后支持 P1→P0**，留 stage 4）、F305（同意 P2）、F301（同意贴 P0 校准）、F324（P0）、F340/F341/F345/F353/F354/F371/F373（P1）。另注：F313/F371 与 F379 的相互作用（API/IDE 路径下派发先失败掩盖 G4 缺陷）不构成降级理由——两条独立缺陷在 legacy 路径或 F379 修复后均激活。

---

## 二、新发现（F379-F383）

### F379 | closure 步骤 2/4/5/6/10 在 API/IDE 派发路径下 prompt 构建即失败（UnresolvedPathError）：10 步中 5 步永不派发——比 F313/F371 更根本的 CLOSURE 阻塞 | error | **P1**（与 F324/F353/F371/F373 并列的"pipeline 永不完成"根因族；F324/F353 修复后即激活）

- 证据：
  - `closure.py:245-247`（prompt 无任何章节上下文：`f"Execute {step.skill} for book closure (step {step.step_num}). Project dir: {project_dir}"`）；`closure.py:253`（`dispatch_skill(step.skill, project_dir, prompt)`）；
  - `dispatch_helper.py:1504/1708`（`chapter = extract_chapter(prompt)`——`contracts/paths.py:45-47` 的 `extract_chapter` 仅匹配 `\bchapter\s+(\d+)\b`，closure prompt 无 "chapter N" → **chapter=None**）；
  - `dispatch_helper.py:654-658`（`output_paths.append(resolve_chapter_path(write_path, chapter))`）；`contracts/paths.py:28-34`（`resolve_chapter_path(path, None)` 对含 N/NNN 路径**抛 UnresolvedPathError**）；`dispatch_helper.py:1515-1516/1718-1719`（API/IDE 路径 `except Exception` → `DispatchResult(False, ..., "Prompt build failed: ...")`）；
  - 5 个失败步骤的 N/NNN 契约 writes（本轮 load_contract 实证）：step 2 shenbi-memory-distill → `truth/arcs/arc-N.md`；step 4 shenbi-score-volume → `audits/volume-N-score.md`；step 5 shenbi-review-arc-payoff → `audits/volume-N-payoff.md`；step 6 shenbi-review-long-span → `audits/chapter-N-long-span.md`；step 10 shenbi-snapshot-manage → `snapshots/chapter-NNN/*`。
- 根因：closure 派发侧与 chapter/trigger 派发侧不对称——chapter/trigger prompt 含 "chapter N" 使 extract_chapter 取到章号，closure prompt 无任何数字上下文而契约占位符按章号语义解析；`UnresolvedPathError` 在 API/IDE 路径被降级为普通派发失败（连 LLM 调用都不会发生）。
- 验证命令+输出（本轮实际运行）：
  ```
  uv run python -c "
  from pathlib import Path
  from shenbi.pipeline.dispatch_helper import _build_skill_prompt, extract_chapter
  from shenbi.pipeline.closure import CLOSURE_STEPS
  for step in CLOSURE_STEPS:
      prompt = f'Execute {step.skill} for book closure (step {step.step_num}). Project dir: /tmp/x'
      ch = extract_chapter(prompt)
      try:
          sp, up, paths = _build_skill_prompt(step.skill, Path('/tmp/x'), prompt, ch, json_mode=True)
          print(f'step {step.step_num}: OK {paths}')
      except Exception as e:
          print(f'step {step.step_num} {step.skill}: EXC {type(e).__name__}: {e}')"
  # → step 2 shenbi-memory-distill: EXC UnresolvedPathError: truth/arcs/arc-N.md
  #   step 4 shenbi-score-volume: EXC UnresolvedPathError: audits/volume-N-score.md
  #   step 5 shenbi-review-arc-payoff: EXC UnresolvedPathError: audits/volume-N-payoff.md
  #   step 6 shenbi-review-long-span: EXC UnresolvedPathError: audits/chapter-N-long-span.md
  #   step 10 shenbi-snapshot-manage: EXC UnresolvedPathError: snapshots/chapter-NNN/*
  #   （步骤 1/3/7/8/9 正常）
  which codex  # → /opt/homebrew/bin/codex  （本环境 IDE 路径激活，IDE 同样调 _build_skill_prompt）
  ```
- 影响：一旦 pipeline 进入 CLOSURE（F324/F353 修复后），**第一步即失败**——step 2 memory-distill 派发失败 → `_handle_closure_failure` 重试×3 → 返回 False → cli.py:287-304 设 ESCALATION checkpoint → resume 重跑同款失败 → **CLOSURE 阶段永久停摆**。比 F313（step 6 G4 错位）与 F371（step 10 G4 目录路径）都更早失败（派发先于 G4，且覆盖 5/10 步）。当前被 F324/F353 掩盖（CLOSURE 本身不可达），为第四条/五条独立"pipeline 永不完成"根因。
- 建议方向：closure prompt 注入当前章节上下文（如 `chapter {last_chapter}`）或 `_dispatch_via_api`/`_dispatch_via_ide` 对 closure 阶段的 N 路径改走 `resolve_volume_path`；更稳妥的做法是给 closure 步骤显式携带 chapter/volume 数值并在 prompt 中体现（与 trigger 侧一致）；补"closure step 2 派发成功 → COMPLETED"集成测试（F324/F353 修复后必测项）。

### F380 | genesis step 16（shenbi-anchor-curate，optional）在 API/IDE 路径永远被当作 optional 跳过：anchors 永不产出（AC-NNN.md 契约占位符在 chapter=None 时抛 UnresolvedPathError） | error | P2

- 证据：`genesis.py:77-79`（GENESIS_STEPS[15] `shenbi-anchor-curate`, `optional=True`, output_path=`benchmarks/anchors/AC-001.md`）；该 skill 契约 writes 含 `benchmarks/anchors/AC-NNN.md`（本轮 load_contract 实证）；`genesis.py:302`（`dispatch_skill(step.skill, project_dir, prompt)`——genesis prompt 无 "chapter N" → chapter=None）→ `_build_skill_prompt` 抛 UnresolvedPathError → `DispatchResult(False)` → `genesis.py:311` → `_handle_failure` → `:222-225`（`step.optional` → "optional_step_skipped" + `_advance` 直接前进）。
- 根因：与 F379 同根——chapter=None 时 N/NNN 契约占位符不可解析，optional 步骤的"首败跳过"设计把确定性失败静默吞掉（本意是容忍 LLM 失败，实际变成永不执行）。
- 验证命令+输出（本轮实际运行）：
  ```
  uv run python -c "<同 F379 脚本遍历 GENESIS_STEPS>"
  # → step 16 shenbi-anchor-curate: EXC UnresolvedPathError: benchmarks/anchors/AC-NNN.md
  #   （其余 16 步全部 OK；步骤 16 为 optional）
  ```
- 影响：每次 genesis 运行 step 16 恒被静默跳过，`benchmarks/anchors/` 永不产出——锚点基准（spec 声称的 anchor curation）从未生效；日志仅一条 `optional_step_skipped`，无任何错误信号。
- 建议方向：anchor-curate 契约改用无 NNN 占位路径（如 `benchmarks/anchors/AC-001.md` 由 skill 自命名并写多文件），或 genesis 为该步骤注入固定 chapter 上下文；与 F379 一并修复路径解析不对称。

### F381 | state_heal._heal_revision_counts 把未修订章节的 revision_count 抬到 ≥1：`_ensure_revision_decisions_exists` 对 NO_REVISION 路由也写回退文件，heal 误将其当作"发生过修订" | error | P2

- 证据：`chapter_loop.py:1928-1933`（`_is_revision_routed` 对任何非 None route 含 **'no-revision'** 返回 True）；`chapter_loop.py:2755-2760`（step 16 因 `_is_revision_skipped` 跳过时无条件调 `_ensure_revision_decisions_exists`——并行波每章都会写 `chapters/chapter-N-revision-decisions.json` 回退文件，其 selections 为 `[{target: no_revision_needed...}]`）；`state_heal.py:45-73`（`_heal_revision_counts` 以该文件存在与否判定 disk_count，`cs.revision_count = max(cs.revision_count, disk_count)` → 0→1）。
- 根因：回退 decisions 文件的语义（"未修订"的审计留存）与 heal 的假设（"文件存在 = 修订过"）冲突；`_is_revision_routed` 对 "no-revision" 的宽松判定使回退文件对全部章节产生。
- 验证命令+输出（本轮实际运行）：
  ```
  uv run python -c "
  import tempfile, json
  from pathlib import Path
  from shenbi.pipeline.state import PipelineState, ChapterState
  from shenbi.pipeline.state_heal import heal_state_counters
  d = tempfile.mkdtemp(); pd = Path(d)
  cs = ChapterState(); cs.revision_count = 0
  st = PipelineState.default(d); st.chapter_loop.chapter_states['3'] = cs
  (pd/'chapters').mkdir(); (pd/'chapters'/'chapter-3-revision-decisions.json').write_text(json.dumps({}))
  print(heal_state_counters(st, pd))"
  # → ['revision_count_healed:ch3:0->1']  （chapter 3 从未修订，heal 后 revision_count=1）
  ```
- 影响：任何一次 resume 后，全部未修订章节的 revision_count 被抬到 ≥1（`cmd_chapters` 展示、审计轨迹误导）；heal 日志 `revision_count_healed:chN:0->1` 与真实修订次数不符。与 F305 的"路由 +1 而未执行"记账漂移叠加后，revision_count 数据不可信。
- 建议方向：`_heal_revision_counts` 改为只读 `chapter-N-revision-decisions.json` 中真实 `route != no_revision`（或检查 `selections` 目标非 no_revision_needed）；或回退文件改放不同文件名（如 `chapter-N-revision-skip.json`）以区分。

### F382 | progress.json 物化层半接线：pipeline 从不写 trace 事件（safe_write 未传 round_dir/trace_action），每 5 步 materialize 把 progress.json 重建为"全 pending"视图，resume 时 staleness 检查空转 | optimization | P2

- 证据：`safe_write.py:94-99,125-137`（`round_dir=None, trace_action=None` 默认，仅两者都提供才写 trace 事件）；pipeline 全部 safe_write 调用均未传这两个参数（本轮 grep `round_dir\|trace_action` 于 src/shenbi/pipeline/*.py 0 命中）；`chapter_loop.py:684-699`（`_maybe_materialize_progress` 每 5 步调 `materialize_progress`）；`trace/materialize.py:31-105`（只处理 INIT/MARK_DONE 动作——pipeline 从未发出，且其自身的 safe_write 以 `trace_action="MATERIALIZE"` 自造 trace 事件）；`chapter_loop.py:702-743`（`_auto_rebuild_progress_if_stale`：trace 目录因 MATERIALIZE 事件存在 → 比较 mtime → 重建同样内容）。
- 根因：Task 12 的"从 trace 物化 progress"只接了读侧与物化侧，写侧（add_step_done 未发 MARK_DONE、pipeline 写盘未发 WRITE/INIT 事件）从未接线——与 F360/F378 同族"半接线"。
- 验证命令+输出：
  ```
  grep -rn "safe_write(" src/shenbi/pipeline/*.py | grep -c "round_dir"   # → 0
  grep -rn "MARK_DONE\|TraceWriter" src/shenbi/pipeline/*.py             # → 0 命中（仅 materialize 内部）
  ```
- 影响：每章约 2 次（steps_done 达 5、15 时）materialize 把 progress.json 覆写为"16 个 chapter 技能全 pending、done_counter=0"的失真视图；trace.jsonl 只含 MATERIALIZE 自造事件，无任何真实管线事件；`_auto_rebuild_progress_if_stale`（resume）因 trace 存在而持续"重建"同一失真内容。监控/审计侧 progress.json 恒显示零进度（pipeline-state.json 才是真实状态源）。
- 建议方向：`add_step_done`（或 `_advance`）时经 TraceWriter 发 MARK_DONE 事件（携带 skill/test_type/status），pipeline 写盘统一传 round_dir+trace_action；或删除物化调用并废弃 progress.json 派生层（保留 pipeline-state.json 为唯一状态源）。

### F383 | IDE 派发路径输出无 `### FILE:` 标记时，整段 stdout 被写入每一个输出文件（codex 已安装 → 当前环境 IDE 路径激活下的潜伏数据损坏点） | error | P2（latent/条件触发）

- 证据：`dispatch_helper.py:903-922`（`_parse_file_outputs`：无 `### FILE:` 匹配时返回 `{"__stdout__": response}`）；`dispatch_helper.py:1152-1168`（`content = parsed.get(rel_path, parsed.get("__stdout__", ""))`——每个字面输出路径都拿到整段 stdout 并 `_write_one` 写盘）；`dispatch_helper.py:1695-1783`（`_dispatch_via_ide` 以 `parsed=None` 调 `_write_parsed_outputs(r.stdout, ...)`）；`dispatch_helper.py:1674-1692` + 本轮 `which codex → /opt/homebrew/bin/codex`（IDE 路径在当前环境激活）。
- 根因：IDE 路径复用 `_write_parsed_outputs` 的 `__stdout__` 兜底，但 IDE agent（codex exec）的输出格式不受 `json_mode` 约束，一旦未按 `### FILE:` 约定输出（prose 汇报、围栏包裹等），解析回退会把同一整段输出写进每个契约文件（含 .json 决策文件），随后仅靠 G4/重试兜底。
- 验证命令+输出：`grep -n "__stdout__" src/shenbi/pipeline/dispatch_helper.py` → 908/922/1159/1172（回退链完整存在）；`which codex` → `/opt/homebrew/bin/codex`（路径激活）。
- 影响：条件触发——codex 输出无标记时，所有输出文件内容相同（全量 stdout），内容可能非 JSON（决策文件）或非正文（章节），下游 G4 若为 generic 可能误判通过；属错误处理缺陷（回退策略把"无解析"与"内容即输出"混淆）。
- 建议方向：`_dispatch_via_ide` 在 `_parse_file_outputs` 无匹配时返回失败（`DispatchResult(False, "no file markers in IDE output")`）而非用 `__stdout__` 兜底；或对 IDE 路径要求 json_mode 等价的结构化输出（`codex exec --json` 或强制围栏解析）。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件逐行重读；__init__.py 1 行至 chapter_loop.py 3053 行、dispatch_helper.py 1986 行、cli.py 939 行等全部读完）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries` 返回 set()、`_resolve_volume_at_runtime(pd,15) → None`；novel.json keys 无 total_chapters；pipeline-state.json phase=chapter-loop ch=56 closure_step=0；genre-config auditDimensions 含 sensitivity/worldRules/dialogue/motivation/texture 全 true）；`uv run python` 实证：**closure 10 步与 genesis 17 步全量 prompt-build 执行**（F379/F380）、`extract_chapter` 对 closure/trigger prompt 的取值、`_build_skill_prompt` 对 shenbi-review-long-span/score-volume/review-arc-payoff/memory-distill/snapshot-manage/anchor-curate 的 N/NNN 解析、state_heal revision_count 抬升实验（F381）、`classify_skill_write_safety('shenbi-review-resonance')`（F374）；**G4 CLI 直接执行**（F373 复证：arc-N-score.md → not_found FAIL / arc-5-score.md → PASS；F371 复证：final-snapshot/ → not_found FAIL）；load_contract 契约 writes/updates（group-*/resonance/lifecycle/state-settling/drafting/long-span/volume-outlining/snapshot-manage/anchor-curate）；环境事实（`which codex` → /opt/homebrew/bin/codex，IDE 路径激活）。
- 前几轮覆盖空洞（本轮已补）：(1) **closure 派发侧 N 占位解析**——前八轮只审了 G4 侧（F313/F371），从未把 closure prompt（无章节上下文）与 `extract_chapter`/`resolve_chapter_path` 的 None 分支对照 → 漏掉 F379（本轮最重要的新发现，5/10 步派发即失败）；(2) **genesis optional 步骤的确定性失败被吞**（F380）；(3) **state_heal 与回退 decisions 文件的语义冲突**（F381，`_is_revision_routed` 对 no-revision 的宽松判定）；(4) **trace/progress 物化层写侧未接线**（F382，前几轮未审 safe_write 的 trace 参数约定与 materialize 的动作白名单）；(5) **IDE 路径 `__stdout__` 兜底写全量到每个文件**（F383，前几轮将 IDE 路径判为"不可执行"而未审其写路径回退）。
- 剩余覆盖洞（沿用前几轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 路径的实际 codex 输出格式未做端到端执行验证（F383 为静态推演，未真实调用 codex exec）；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内）。

---

## 四、汇总

- **新 finding 数**：5（F379-F383，其中 F379 = P1（"pipeline 永不完成"根因族，API/IDE 路径确定性复现）、F380/F381/F382/F383 = P2）
- **误报数**：0 新增（F300-F378 全部成立/维持；F325 维持二~七轮误报撤销，本轮独立复证——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名；F348 维持"成立但机制描述部分不准"）
- **严重度异议数**：0 新增（立场声明：F364 维持 P1 且本轮补强 atexit 逐章追加注册的放大事实、支持 P1→P0 留 stage 4；F302 的 P1→P0 依据维持留 stage 4；F305 同意 P2；其余无异议）
- **事实修正（不改严重度）**：2（F364 补事实注记——`atexit.register` 在 `step_idx==0` 分支注册且无去重，CLI 一次性进程模型下每进程正常 1 次、跨章完成时多次，每次正常退出都无条件清 staging 并改写持久化 current_step；F313/F371 补充与 F379 的交互注记——API/IDE 路径下派发先失败掩盖 G4 缺陷，两条独立缺陷在 legacy 路径或 F379 修复后仍激活）
- **0 新 finding 判定**：**否** —— 本轮发现 1 条 P1 漏报（F379 closure 5/10 步派发即失败，prompt-build 实证）与 4 条 P2 漏报（F380 genesis anchor-curate 恒被吞、F381 heal 抬升 revision_count、F382 progress 物化层半接线、F383 IDE stdout 全量写每个文件）；F324（P0）、F340/F341/F345/F353/F354/F364/F371/F373（P1）维持成立、F325（误报）维持撤销。
- **校准升级提示**：F379 建议与 F324/F353/F371/F373 一并纳入"pipeline 永不完成/触发全失效"根因族处置（五者相互独立：volume_map 解析器 / total_chapters 写入点 / closure G4 目录路径 / 触发 G4 N 占位 / **closure 派发 N 占位**——必须全部修复才能跑通整本书；其中 F379 是 CLOSURE 阶段第一个触发点，应优先修）；F380 与 F379 同根（chapter=None 的 N 占位解析）建议合并修复；F381 建议并入 state_heal 语义修正；F382 建议并入 F360/F378 的"半接线死信号"清理族。

---

## 五、独立复核声明

本报告为 fresh-context 第八轮独立复核：34/34 文件逐行重读；对 F300-F378 的判定全部基于本轮独立 read/grep/uv run python/G4-CLI/契约实证/路径解析执行与真实产物对照（未复用前八轮结论）；新增 5 条发现（F379-F383）均为前八轮报告未以编号形式出现的观察，其中 F379/F380 的验证手段（closure/genesis 全步骤 prompt-build 直接执行，暴露 `extract_chapter(None) → UnresolvedPathError` 链）为前几轮未使用的新证据路径；F373/F371 的 G4 CLI 实验为本轮独立复证。
