# Z2 分区第十一轮独立复核报告（Z2.review11）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）+ `Z2.review9.md`（F253-F255）+ `Z2.review10.md`（F256-F257）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，逐文件重读，未依赖前十轮转述）；另读跨区消费面验证裁决（gates/{cli,shared,g1,g2,g3,g3_independence,g_dispatch,g_reconcile}.py、audit/{_shared,snapshot,write_audit,record}.py、records/{parser,drift}.py、pipeline/{dispatch_helper,phase_runner,chapter_loop}.py、scoring.py、safe_write.py、first-novel.md、tests/tiers/t1-skill 结构、git show 99f91f0:tests/dispatch-subagent.sh）
- 模式: 只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）。
2. **独立验证方式**：`pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）；运行时探针（F216/F218/F235/F247/F217/F220/F251/F252 复现、F258 新证据、derive_file_type 全技能枚举）。
3. **前十轮判定**：F201-F257 共 **57 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注、F243 机制修正（exit 3 先于 exit 2）、F247 范围注记均复核通过。已有 7 条严重度异议（F214/F218→P1、F217/F228/F229/F238/F251→P1 低置信、F220 维持 P2）经独立反证尝试后**全部支持**；本轮**不新增严重度异议**。
4. **本轮新发现 1 条（F258，M 级）**，集中于十一轮都未触及的 `extract_chapter` 首匹配语义；另附 M 级观察 3 条。**实质（P1/P2）新 finding 已收敛为 0**，剩余仅 1 条文案/边界级遗漏。

---

## 一、复核结论表（F201-F257 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 重读三模型（chapter_planning/context_composing/volume_outlining）+ g4 私有实现（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer 激活计划；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）；grep 无生产消费者（仅 test_skill_integration.py:188 类名文本）。双源已发散。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads（跨区 read 确认）。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g6.py:51 与 g4/worldbuilding.py 以 raw dict 读 target_word_count/target_words——死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证；g4/score_*.py 用 RouteC/RouteA 正则检查）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无生产加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import（grep 复证：生产仅 CPZone/ActorRole 有消费）；裸字面量散落点 + chapter_loop.py:1797、:409-438 复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 GateStatus 实有，read 确认）。 |
| F210 | P2 | **成立** | hooks.py 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing（复测 74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0 → 修复方向按"CLI 崩溃"修正，本体成立。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实（read 复证）。 |
| F214 | P2 | **成立；支持异议→建议 P1** | codex.py:20-48 不写 current_scorer_agent；run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行。反证（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。 |
| F215 | P2 | **成立** | codex.py:75 `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（read 复证 + 前轮复现）。 |
| F216 | P1 | **成立，P1 维持** | **本轮复现**：`GenreConfig.model_validate`（空 customRules + texture:False 等 1 禁用维度，5-10 章类型）→ PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。P1 维持。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | 本轮 grep 复证 SKILL.md 内 **7 处** CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :187 15-30 / :192-195 15-25·30-40·15-25·15-25 / :257 15-30 且 <10 或 >40 才 FAIL）+ docstring 20-30 + 模型 [15,35]。权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | **本轮复现**：部分匹配（声明 ["当前任务","不存在的字段"]，文件 3 节）→ matched=True、仅命中 "当前任务" 节（世界设定/活跃线索静默丢弃）、无 WARN；全缺 → 全文 + `field_filter_no_match` WARN。AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | P2 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，建议维持 P2（不降 M）。 |
| F221 | P2 | **成立** | fixture 9 键含 tropeInventory（本轮 json.loads 复证）；模型 8 字段无 tropeInventory + docstring "8 camelCase keys"；SKILL.md:286/:292 "恰好 8 个"；OWNERSHIP _GENRE_KEYS 9 键。`extra=ignore` 静默丢弃。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except（read 复证）。 |
| F227 | P1 | **成立，P1 维持** | **本轮复证**：first-novel.md:38-41 文档化流程 = round-exec.sh 建脚手架（无 novel.json）后立即 `uv run shenbi-dispatch shenbi-worldbuilding generative <round_dir> "<prompt>"` → G1.1 novel.json not found → rc=1；`git show 99f91f0:tests/dispatch-subagent.sh:81-82` 注释 "the skill execution happened earlier" 证实门序回归（技能执行被并入 G2 之后的 dispatch_codex，codex.py:61-66）。反证不成立。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | derive_output_files（audit/_shared.py:38-56）不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | 本轮全技能枚举：`derive_file_type` decisions 类含 chapter-drafting/revision/context-composing/market-radar/short-drafting 5 技能；g2.py:83-84 decisions 分支对非 .json `continue`（:164 连 G2.4-G2.12 一并跳过）+ executor 只调一次 run_g2 → .md 散文校验静默缺失。 |
| F230 | P2 | **成立** | `_filter_json:76` 裸 `k in fields`（无规范化），与 `_filter_md` canonical rule 不一致（read 复证 + 前轮复现）。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者（复证）。 |
| F232 | P2 | **成立** | decisions.py:33 `has = rationale is not None`——空串/纯空白满足 REQUIRED 且 ≤100 字符；routine+low 空串被拒，两侧语义不对称（read 复证）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | P2 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | P1 | **成立，P1 维持** | **本轮端到端复现**：`PROJECT_DIR = REPO_ROOT`（executor.py:26），worldbuilding watch 含 `truth/*.md` → `snapshot_tree` 展开仓库根 tracked `truth/bridge_tracker.md`/`truth/character_matrix.md` → 零写入派发也产出 `violations=['未声明写入: truth/bridge_tracker.md', '未声明写入: truth/character_matrix.md']` → rc=2；round_dir 真实写入不被审计。反证（审计面意图为仓库根）不成立——codex.py:62 cwd 已改 round_dir + test_executor_audit.py:18,40 编码 PROJECT_DIR==round_dir 正确语义。 |
| F236 | P2 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs（parametric_globs()），registry globs（`truth/*.md`）恒判未声明（read 复证 + F235 探针内联）。 |
| F237 | P2 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | P2 | **成立；支持 review6 低置信异议（P2→P1）** | audit_writes（write_audit.py:53-56）对每个 post .md 无 try 地 parse_records；records/parser.py:40-44 yaml.safe_load 可抛 ScannerError/非 list 抛 ValueError；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | P2 | **成立** | 本轮全技能枚举复证：derive_file_type worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归；executor.py:81-85 精确字符串交集）；10 个 truth 技能名单与报告吻合（lifecycle/memory-distill/state-settling 等）。 |
| F240 | P2 | **成立** | extract_chapter（paths.py:45-47）仅识别英文 "chapter N"；中文 prompt → chapter=None → N/NNN 输出全部丢弃 → G2 静默跳过（read 复证 + 前轮复现）。 |
| F241 | P2 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`（get_ownership → None）；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"（grep 复证）。 |
| F243 | P2 | **成立（机制修正维持）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（原第 4 参为独立评分 prompt）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。机制修正（review8）维持：gate-marker 强制 exit 3 先于分数校验 exit 2。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak（不存在则静默无检查）；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | P2 | **成立** | **本轮复现**：resolve_chapter_path("audits/volume-N-payoff.md", 7) → `audits/volume-7-payoff.md`；arc-N+7 → `arc-7.md`；AC-NNN+7 → `AC-007.md`；resolve_or_skip(volume-N, None) → None；resolve_volume_path 仅 closure.py:20,158 消费（grep 复证）。 |
| F246 | P2 | **成立** | dispatch_skill（dispatch_helper.py:1826-1865）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径；rd = round_dir or project_dir）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | P1 | **成立，P1 维持（范围注记维持）** | **本轮端到端复现**：`_declared_patterns("shenbi-chapter-drafting") == []` vs `_audit_watch_paths("shenbi-chapter-drafting", 7) == ['chapters/chapter-7.md','chapters/chapter-7-decisions.json']` → N 写技能成功派发恒判「未声明写入」rc=2。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | P2 | **成立** | PacingDesign.from_markdown（pacing_design.py:77-130）chapter_sequence 恒空（:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）致误拒/虚增（read 复证）。 |
| F249 | P2 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500；原 shell 同传 PROJECT_DIR 但 codex 以 `-C PROJECT_DIR` 执行（语义自洽），Python 翻译 cwd 改 round_dir 后参数未跟随（git show + read 复证）。 |
| F250 | P2 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 pipeline dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | P2 | **成立；支持 review9 低置信异议（P2→P1）** | 命名面：codex.py:56 `-scores-subagent.json` vs find_report（shared.py:157-166）三模式 / g0.py:440 G0.10 glob `*-generative-scores.json`；内容面：raw 维度 dict 无 final_score；状态面：codex.py:44 "done" vs g_reconcile.py:40,61 "DONE"（本轮 read 复证）→ GR.2 恒 FAIL。受 F252 前置掩盖维持低置信。 |
| F252 | P2 | **成立** | scoring.py:364-377 marker 强制 + :187-224 check_gate_markers（t1-skill 分支要求 `G4-{skill}-{test_type}.json`）→ exit 3 MARKER_MISSING；executor 编排（:190-214）只跑 G1/G2 无 G4；grep write_gate_marker dispatcher/ → 空（read 复证）。 |
| F253 | P2 | **成立** | codex.py:27-48 `_record_completion` 读-改-写整体无锁（safe_write.py `_acquire_lock` flock 仅覆盖写入周期，read 复证）；pipeline legacy 并行（parallel_dispatch.py:168-171 Semaphore(4) 共享 project_dir）触发面真实。 |
| F254 | P2 | **成立** | run_g1/run_g2（executor.py:114-118/:125-137）与 shenbi-score 子进程（codex.py:95-109）均无 timeout；仅 codex exec 有 600s（codex.py:63）；`grep -n timeout src/shenbi/dispatcher/` → 仅 codex.py:63（read 复证）。 |
| F255 | P2 | **成立** | 本轮全技能枚举复证：derive_file_type 对 4 个 report-kind truth 写者（score-volume/score-arc/score-stratum/review-arc-payoff）全 → "report"（executor.py:77-78 REPORT 分支先于 truth 交集）→ G2.11（g2.py:290-313 仅 file_type=="truth"）对其静默失效。 |
| F256 | M | **成立** | g2.py:273/:315 注释声明 "chapter files only"，G2.10（:274-287）/G2.12（:316-322）在 `if file_type == "chapter":` 块（:218-271）之外无条件执行——truth/report/json 全部受影响；句末符集合（:317）缺 `}`/`]` → 误归 chapter 的 JSON 恒 WARN（read 复证）。 |
| F257 | M | **成立** | executor.py:156 `def dispatch(..., *, chapter: int | None = None)` 全仓零调用方（grep 仅 cli.py:29、executor.py:250，均不传）；dispatch_with_write_audit（:242）另行独立二次 extract_chapter（read 复证）。 |

**误报统计**：整体误报 **0 条**（F201-F257 共 57 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F258）

### F258 | extract_chapter 首匹配语义：多章节引用英文 prompt 取**第一个** "chapter N"（引用章），非目标章 → standalone G1/G2/审计面全部解析到错误章节 | 漏报（边界缺陷，F240/F245 家族的新触发面） | M

- 证据: `src/shenbi/contracts/paths.py:45-47`（`re.search(r"\bchapter\s+(\d+)\b", text, re.IGNORECASE)` 取首个匹配）；消费面 `executor.py:161-162`（dispatch 用其解析 N 占位符）、`executor.py:242`（审计 watch 二次提取）、`dispatch_helper.py:1853`（pipeline legacy 路径同用）
- 根因: `extract_chapter` 对多章节引用无目标语义——prompt 若先引用旧章再指定目标章（"Now that chapter 6 is complete, draft chapter 7"），返回 6（首个匹配）→ `derive_input_files`/`derive_output_files`/`_audit_watch_paths` 全部按 chapter=6 解析 → G1 校验 chapter-6 输入、G2 校验 chapter-6 输出、审计 watch chapter-6，与真实目标章 7 错位。与 F240（中文 prompt → None）互补：F240 是"识别不到"，本条是"多引用时取错"。与 F245（占位符语义碰撞）不同：F245 是 arc-N/volume-N 等非章节占位符被按章节填，本条是纯 chapter 占位符被填错号码。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c "from shenbi.contracts.paths import extract_chapter; print(extract_chapter('Now that chapter 6 is complete, draft chapter 7'))"` → `6`；`extract_chapter('Review chapter 3 feedback and fix chapter 5')` → `3`；对照 `extract_chapter('Draft chapter 7')` → `7`
- 建议方向: 提取**最后一个** "chapter N"（常见 prompt 尾置目标）或要求显式目标标记（如 `target chapter`）；与 F240/F245 修复联动（章节解析统一为带上下文参数的单一 helper）；补多引用 prompt 测试
- 影响注记: pipeline 生成 prompt 均为单章节引用（chapter_loop.py:2397/2556/2770 等 `f"...for chapter {chapter}..."`，本轮 grep 复证），故仅 standalone 手工 prompt 触发；且被 F227（fresh 轮 G1 先死）掩盖——F227/F235/F247 修复后若用户多引用 prompt 则章节错位，届时可重评 P2

### M 级观察（随文记录，均非 finding）
- `codex.py:27-32` `_record_completion` 对**非 dict 的合法 JSON** progress.json（如 `[1,2,3]`）读入后置空重建（`loaded if isinstance(loaded, dict) else {}`）→ 原内容整体丢失；与 F223（损坏 JSON 裸崩）互补但为静默数据覆盖，触发需 progress.json 被非对象 JSON 覆写（无真实生产者，极边缘）（M，F223 增补）
- `executor.py:167-188` SHENBI_G1_SKIP_READS 的 `from fnmatch import fnmatch` 位于条件块内（:171）——函数内条件导入（样式，非缺陷）（M）
- `g3.py:100` G3.2 `reports_dir.glob("*.json")` 会把 codex 落盘的 raw 维度文件（`-scores-subagent.json`）计入评分源（经 `_compute_rubric_weighted_score` 兜底）——raw 文件本非完整评分报告，G3.2 对失败派发遗留的 raw 文件也会计算分数（M，F251 关联）

---

## 三、严重度异议（不修改前十轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4 fail-closed（g3_independence.py:18-28）以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。反证（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发伪造。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持 F218 P2 → P1**：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 本轮复现：matched=True、仅命中 section、无 WARN）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40），与 :257 表（15-30%）亦矛盾——SKILL.md 内部 7 处自相矛盾，权威区间未定，维持低置信供协调者裁决。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内，使 record_audit_outcome 永不执行（审计记录丢失）——"崩溃仍审计"保证（executor.py:245-246）失效；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（本轮枚举 5 个：chapter-drafting/context-composing/chapter-revision/market-radar/short-drafting）的 .md 散文在 G2 被 continue（g2.py:83-84,:164）整体跳过，G2.6/G2.8/G2.9 对章节正文静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F251 P2 → P1（低置信维持）**：评分产物三重不匹配使 standalone 轮次对 G5.1（no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL）确定性失败；受 F252（exit 3 前置）掩盖维持低置信。
8. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`COVERAGE_FILE=/tmp/z2r11-cov .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F218 部分匹配 → matched=True、仅命中 section、无 WARN
  - F235 端到端：真实 REPO_ROOT + worldbuilding watch → violations（bridge_tracker/character_matrix）→ rc=2 语义复现；`git ls-files truth/` 确认两文件 tracked
  - F247 `_declared_patterns`=[] vs `_audit_watch_paths`=2 项（chapter-7.md/decisions.json）
  - F217 SKILL.md 7 处 CONSTELLATION 区间 grep 复证
  - F245 resolve_chapter_path(volume-N/arc-N/AC-NNN, 7) 全按章节填；resolve_or_skip(volume-N, None) → None
  - F239/F255/F229 derive_file_type 全技能枚举：chapter 28 / truth 10 / decisions 5 / report 31；4 个 report-kind truth 写者 → "report"；glob 写者 → "chapter"；decisions 5 技能名单
  - **F258（新）**：extract_chapter 多引用 prompt → 首个匹配（6/3），单引用 → 7
  - F227 原 shell 门序证据：`git show 99f91f0:tests/dispatch-subagent.sh:81-82` "the skill execution happened earlier"；first-novel.md:38-41 文档化流程
  - F249 原 shell G2 第 5 参同传 PROJECT_DIR 但 codex `-C PROJECT_DIR`（语义自洽 vs Python 翻译 cwd 改 round_dir）
  - F251 g_reconcile.py:40,61 "DONE" vs codex.py:44 "done"；find_report 三模式 read 复证
  - F252 scoring.py:364-377 exit 3 MARKER_MISSING；grep write_gate_marker dispatcher/ → 空
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c / heredoc。

---

## 五、统计

- 新 finding：**1 条**（F258：M×1）；M 级观察 3 条随文
- 误报：**0 条整体误报**（F201-F257 共 57 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持）
- 严重度异议：**8 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1、F217→P1（低置信）、F228→P1（低置信）、F238→P1（低置信）、F229→P1（低置信）、F251→P1（低置信）、F220 维持 P2
- 前十轮 findings 判定：F201-F257 共 57 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否（严格意义上）**——本轮仍有 1 条新发现（F258，M 级：extract_chapter 首匹配边界语义），无新增 P1/P2；Z2 分区 57 条既有 findings 全部成立，P1 集（F214/F216/F218/F227/F235/F247）经独立运行时复现全部维持。**实质性（P1/P2）新 finding 已收敛为 0**，剩余仅 1 条边界级遗漏。
