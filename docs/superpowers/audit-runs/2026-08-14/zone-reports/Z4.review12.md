# Z4.review12 复核报告 — src/shenbi/gates/（含 g4 checkers）第十二轮独立复核

- 复核 agent：第十二轮独立复核（fresh-context，全量重读，不依赖前十二轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`~`Z4.review11.md`（F419-F492，74 条新 finding）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个专用 checker + generic 3 分支 + score 三副本逐个通读；`src/shenbi/contracts/schemas/decisions.py`、`src/shenbi/safe_write.py`、`src/shenbi/phase_runner.py` 作交叉证据核实；真实项目 `novel-output/xinghuo-ranqiong` 的 chapters/pending_hooks.md 实证）
- 实测基线：`.venv/bin/python` 只读验证 15+ 组（/tmp 临时目录构造 + 真实项目实证 + DecisionsDoc/内层形状/排序/截断/正则 五类专项实测；未修改任何仓库文件，未 git add/commit；`git status` 复查无跟踪文件污染）
- 复核结论：**误报 0**；**新增严重度异议 0**（前几轮 F404→P1、F408→P1 两条异议本轮独立实测确认成立；任务点名的 6 个 P1 逐一反证，均无推翻证据）；**新发现 3**（F493-F495：2×P2 + 1×M，全部实测验证）
- 结论：**0 新 finding 判定 = 否**（本轮发现 3 个前十一轮漏报，未完全收敛到 0；F493 为内层形状家族第 6 批实例，F494 为 G6.4 时间线检查的采样截断漏报，F495 为 review 证据正则 fail-open 启发式）

---

## 一、复核结论表（F400-F492 逐条判定）

本轮判定方法：对 93 条 finding 逐条直接读其证据行 + 必要的独立实测（共 15+ 组，全部 `.venv/bin/python` 只读 + /tmp 构造）；共同依据是本人对 52 个文件的全量重读。按组列出，仅对独立验证过的新证据展开。

**A 组：P1 六条（任务点名，逐一反证核查，全部成立，无推翻证据）**

| 编号 | 本轮判定 | 独立反证（本轮实测） |
|---|---|---|
| F401 | **成立** | 读 g_reconcile.py:52-62：`stem.rsplit("-", n_split)` 后 `candidate_tt = "-".join(parts[1:])` 对 `shenbi-worldbuilding-generative-scores(-subagent).json` 并入 `-scores`/`-scores-subagent`，progress 键为 `generative` → `td={}` → GR.2 恒 FAIL；且 review-group-* 5 连字符技能名在 rsplit 后同样并入后缀（推演）。无推翻理由。 |
| F404 | **成立，确认 P1 异议** | 本轮实测：`DecisionsDoc.model_validate({..., selections:[{basis:'manual_override', severity:'low', rationale:''}]})` → **PASS（HOLE）**；`severity:'high'` 同 PASS；decisions.py:33 `has = rationale is not None`。AGENTS.md:70 契约在正常路径被空串静默违反 → P1 恰当。 |
| F408 | **成立，确认 P1 异议** | 读 dispatch_helper.py run_gate_g3（progress.json 缺失时 safe_write 伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history）→ G3.4 fail-closed 被击穿并制造虚假审计证据。P1 恰当。 |
| F432 | **成立** | 本轮实测：`ls skills/`=74 vs `tests/tiers/t1-skill` 非 `_` 目录=69，`comm` 差 5 = foreshadowing-lifecycle + review-group-character/craft/factual/plan（与名单一致）；g7.py:59 G7.1b 以 74 为全集 → 完整 round 恒 FAIL。P1 无推翻理由。 |
| F444 | **成立** | 读 g3.py:150-153：`skills.get(skill_name, {}).get("output_files", [])` 取 skill 层；生产 progress 形状（codex/materialize/round-exec/run_gate_g3 伪造）均无该键 → G3.3 恒 SKIP。P1 无推翻理由。 |
| F458 | **成立，确认 P1** | 本轮实测：`DecisionsDoc.model_validate({..., adjustments:[{issue_id:'X1', severity:'high', handling:'ignore', rationale:''}]})` → **PASS（HOLE）**；decisions.py:48-59 `Adjustment.rationale: str` 必填但 validator 仅查 `> _RATIONALE_MAX_CHARS`。AGENTS.md P2.5 "rationale REQUIRED on adjustments" 被空串绕过 → P1 恰当。 |

**B 组：核心门逻辑（g0-g7/共享/专用门）— 全部成立（读码确认 + 关键项实测）**

- F400（g2.py:315-322/273-287 无 file_type 守卫）成立；F402（length_normalizing.py:29 vs :35 `word_count_md(fp)` 用原始 fp——word_count_md 内部 `Path(fp).read_text` 按 CWD 解析，rd+相对路径必 FileNotFoundError）成立；F403（cli.py:113-121 无 try/except + 11 个 RoundPaths checker 双 None raise）成立；F406（g0.py:195/323 用 `PROJECT/skill-output`、g7.py:66 用 `rd/skill-output`；本轮 `ls -d skill-output` → 不存在、仅 novel-output；round-exec 测试布局限定维持）成立；F407（g0.py:177-179 无 seed → `return passed("G0", [G0.1 SKIP])`）成立；F409（g1.py:281-282 空输入仅 G1.0 SKIP → PASS）成立；F410（SHORT_MAP 20 项）成立；F411（g0.py:220/227 missing_dirs 恒假）成立；F412（g3_independence.py:3 行号过期）成立；F413（decisions_validator.py:175 注释 shared.py:113）成立；F414（chapter_revision.py:97 "HARD_FAIL"）成立；F415（SKILL.md:125=`<!--META-END-->`、规则在 :140）成立；F416（decisions-schema.md 缺 medium）成立；F417（无 test_gate_manifest.py / test_memory_distill.py；gate_manifest 0% 覆盖）成立；F418（g0.py:665 未传 floor）成立；F419（g1.py:210-213 / g2.py:169-172 except 缺 ValueError）成立；F420（g6.py:50/56 jload 无保护）成立；F421（g_reconcile.py:33 无 try / g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError）成立；F422（cli.py:83-87 JSON 失败→[]）成立；F423（`ls tests/scoring.py` → 不存在，实现在 src/shenbi/scoring.py）成立；F428（g3.py:98 94 vs :113/:125 90 分叉）成立；F429（g1.py:228-232 gate 内 safe_write .bak——safe_write 支持 bytes/str，写 bytes 不崩，契约偏离成立）成立；F435（g0.py:521-529 exempt_skills 零消费）成立；F436（g6.py:125/142 planted_chapters 死变量）成立；F437（g5.py:274-296 except Exception→WARN vs G6.3/G7.13 FAIL）成立；F439（本轮实测 `'shenbi-style-polishing' in BACKUP_SKILLS` → False，BACKUP_SKILLS=16）成立；F440（g0.py:202-207/524-528 缺 ValueError）成立；F441（g0.py:98-99 json.loads 直读 + :107 .get 无守卫）成立；F443（g1.py:102-144 check_fields_exist 仅测试引用）成立；F445（shared.py:210-222 unimplemented() 零调用）成立；F449（g_reconcile.py:40/61-62 "DONE" vs 生产小写 done）成立；F454（g_transition.py:47-64/70-85 键语义漂移）成立；F456（本轮实测真实项目 `sorted(glob)` 前 12 个 = [1,10,...,19,2]）成立；F457（g6_checks.py:111-113/245 len(findall) 计段数非字符数）成立；F462（g3.py:103 total_score/score vs scoring.py final_score）成立；F463（g3.py:100 无 skill 过滤 + :107 跨技能 rubric）成立；F464（codex.py:56 `-scores-subagent.json` 与 find_report/G0.10 glob/GR.2 均不匹配）成立；F466（chapter_drafting.py:260-261 rd+"project-output"/chapters；G6.3 传 rd=pd）成立；F468（g3.py:98 循环外 threshold + :113/:125 循环内覆写泄漏）成立；F469（generic.py:313 `make_composite_checker(g4_decisions, g4_chapter_revision)` 接线错位——本轮 grep 确认其余 composite 均为 existing 结构 checker + decisions schema checker 正确顺序）成立；F471（gate_manifest "gates" 列表 setdefault）成立；F472（cli.py:105 解析 rd 但 :107-110 bughunt/clean 分支不传）成立。

**C 组：jload/内层形状家族（F431/F440/F441/F459/F460/F465/F470/F471/F477/F485/F488/F489 等）— 成立，且本轮仍发现 4 处未枚举实例（F493 补充）**

- F431（8 处 jload ValueError 调用点）成立；F440（g0.py 2 处）成立；F441（deps.json 顶层）成立；F459（worldbuilding.py:40-53/61-64）成立；F460（g0.py:204 chapter_word / g0_config_coherence:89-92 auditDimensions / g5.py:62-63 t1_scores 条目 / decisions_validator:54-55 相邻文件）成立；F465（g7.py:37/58、g_reconcile.py:36、character_design.py:203、foreshadowing_plant.py:69）成立；F470（g3.py:97/102、g5.py:67）成立；F477（g_reconcile.py:60 GR.2 逐技能值）成立；F485（g3.5/g_dispatch/g5/g7/g6 5 处）成立；F488（g_transition.py:48/57 remaining 标量 len()）成立；F489（g6.py novel.json 字符串数值 TypeError）成立。**本轮新实例 4 处**：g3.py:152（skills[skill] 非 dict → `.get` AttributeError，实测崩溃）、g0.py:107（`_calibration_hashes` 非 dict → `.get` AttributeError，实测崩溃）、g5.py:40（`t2-phases` 非 dict → `.get` AttributeError，实测崩溃）、g6.py:39（`t3-pipelines` 非 dict → `.get` AttributeError，实测崩溃）——见新 F493。

**D 组：per-skill g4 checker — 全部成立（本轮逐个通读复核）**

- F405（G4_CHECKER_SKILLS 22 vs generic.py checkers 31）成立；F424（chapter_drafting.py:219-222 skill-output 爬升）成立；F425（g7.py:66+86-91 G7.6 嵌 skill-output）成立；F426（genre_config.py:28 未走 resolve_input_path；pacing_design.py:31 fps[0] 启发式）成立；F427（chapter_revision.py:45 Path(fp) 原始路径）成立；F430（foreshadowing_track.py:41-46 裸词"操作"）成立；F433（chapter_drafting.py:331-334 project_dir 判、project_root 取）成立；F434（state_settling.py:14/46-53 单字符子串）成立；F438（chapter_drafting.py:204 max(5,wc//1000) vs SKILL 1/3000）成立；F442（length_normalizing.py:61-64 无 25% 双底线）成立；F446（shared.py:314-331 减法表双向偏离）成立；F447（chapter_drafting.py:203/225 transition/fatigue 全文件计数）成立；F448（chapter_drafting.py:70 `第\d+章` 漏中文数字）成立；F450（g6.py:118-142 按 `## hooks`+`- id:` 解析；本轮实测真实 pending_hooks.md `grep -c "id:"` = 0、为 `# 伏笔追踪`+`## 第N章伏笔呈现` 表格格式）成立；F451（g6_checks.py:101-102/234-240 剥离正则缺 `# `/`<!--META`）成立；F452（chapter_drafting.py:293/316-328 末段取元文本）成立；F453（g2.py:237-238 `_is_important_chapter`；本轮 grep phase_runner.py:213 `run_gate("G2", [",".join(output_files), file_type, str(round_dir)])` 确认无 pd 实参；executor.py:144 传 pd 的限定维持）成立；F455（generic.py:28 docstring vs 实现）成立；F461（chapter_drafting.py:334/293-302 protagonist/scene 未剥离）成立；F467（chapter_planning.py:37 vs :82/:97 分隔符不一致）成立；F473（g6.py:306-351 vol_pat 无 `## ` 锚定 + no_ending_hook 元文本 + 裸"将"）成立；F474（g6_checks.py:24-42 同章日期互比 + 无年份维度）成立；F475（g0.py:204/209 与 g6.py:56/60 chapter_word 死读取 + default=0 ZeroDivisionError）成立；F476（chapter_planning.py 仅实现 8 条可检查规则中 2 条）成立；F479（worldbuilding.py:68 注释方向相反）成立；F480（generic.py:102/152 死条件）成立；F482（chapter_drafting.py:58-66 docstring 未实现）成立；F483（chapter_drafting.py:164 只加"他"不加"她"）成立；F486（plot_thread_weaver.py:39-46 任一标签即 PASS）成立；F487（chapter_drafting.py:146-148 正则 + :150-158 YAML 双源 append 同一名字）成立；F490（chapter_revision.py:98 checks 槽为 list[str]）成立；F491（worldbuilding.py:129-133 地点计数双重缺陷——本轮实测 SKILL 模板合规输出（5×`## 地点：X`+`## 地点构建汇总`）→ `G4.locations.count:6` 误报 FAIL）成立；F492（g0.py:276-288 evidence 正则漏认"证据"列头——本轮实测真实 rubric `| 5 | Evidence rigor | 15% | File + line citations |` 对 score-arc SKILL.md evidence-pattern 命中 False；端到端 `gate_G0(seed)` 实测 G0.5b WARN 3 条 score-* 技能）成立。

**误报判定：0 条。** F400-F492 全部成立。对任务点名的 6 个 P1（F401/F404/F408/F432/F444/F458）本轮逐一读源码 + 实测反证：F404/F458 空串绕过用**正确 schema 字段**（`$schema`/`produced_at`）实测复现（Selection manual_override low/high + Adjustment 均 PASS），F408 伪造写入读 dispatch_helper.py 确认，F401 GR.2 恒 FAIL 读码确认，F432 74-vs-69 计数 `comm` 实测确认，F444 G3.3 skill 层查找读码确认；无任何推翻证据。不准确处仅限前几轮已注明的范围/限定（F401 masking 按归档 spec、F406/F424/F425 "恒 SKIP"仅 pipeline 布局、F405 名单、F444 形状、F453 executor 限定、F475 非 int default 类型），本轮无新增修正。

**严重度异议：0 新增。** 确认前几轮 2 条异议成立（F404 P2→P1、F408 P2→P1，均属"AGENTS.md 显式契约在正常路径被静默违反"，按决策表 P1 恰当）；其余严重度按决策表核对无异议（F450/F464/F473 的"真实项目恒 FAIL"可争 P1 的理由前几轮已评估，属格式漂移类维持 P2，本轮无推翻依据——G6 层系统性格式漂移（F406/F424/F425/F426/F427/F433/F439/F442/F450/F451/F453/F456/F457/F461/F466/F467/F473/F474/F491）若单独升级任一而其它维持 P2 会不一致，不单独提出）。

---

## 二、新发现（F493-F495）

### F493 | jload/json.loads 内层形状家族第 6 批：4 处未枚举实例 → AttributeError 崩溃（g3.3 skills 值 / g0.14 _calibration_hashes / g5.1 t2-phases / g6.1 t3-pipelines） | error | P2
- 证据（4 处独立实例，均实测崩溃；前几轮 F460 枚举 4 处、F465 枚举 4 处、F471/F477 各 1 处、F485 枚举 5 处、F488/F489 各 1 处，均未覆盖以下调用点）:
  - g3.py:152（G3.3）：`skill_data = skills.get(skill_name, {}) if isinstance(skills, dict) else {}`——isinstance 守卫只查 `skills` 顶层，未查 `skills[skill_name]` 值；`skills={"shenbi-x": ["not-a-dict"]}`（合法 JSON）→ `skill_data.get("output_files", [])` AttributeError（F465 的 g_reconcile.py:36/60 同型但不同文件；G1.6 有 isinstance(scoring_history, list) 守卫、此处无对应守卫）；
  - g0.py:107（G0.14）：`locked = deps.get("_calibration_hashes", {}).get("combined")`——`_calibration_hashes` 为列表/标量时 `.get` AttributeError（F441 只覆盖 deps.json 顶层非 dict，内层未列）；且 check_calibration_integrity 的 `except (json.JSONDecodeError, OSError)`（:100）只包 json.loads，AttributeError 直接传播 → gate_G0 崩溃；
  - g5.py:40（G5.0）：`phase_data = deps.get("t2-phases", {}).get(phase_name)`——`t2-phases` 为列表时 `.get` AttributeError（deps.json 顶层 dict 校验（jload）通过后内层未校验）；
  - g6.py:39（G6.0）：`pipe_data = deps.get("t3-pipelines", {}).get(pipeline_name, {})`——同型。
- 根因: 与 F460/F465/F485/F488/F489 同族——jload/json.loads 只保证顶层是 dict，内层值形状（skills[skill] / _calibration_hashes / t2-phases / t3-pipelines）无 isinstance 守卫；AttributeError 不在各 except 元组内 → 直接传播，CLI/直接调用面崩溃、无 JSON 输出。
- 验证命令+输出（4 组均实测）:
  - `.venv/bin/python -c`（progress.json=`{"skills": {"shenbi-x": ["not-a-dict"]}}`）→ `gate_G3("shenbi-x","generative",rd)` → `CRASH AttributeError: 'list' object has no attribute 'get'`；
  - `check_calibration_integrity(cal_dir, deps.json={"_calibration_hashes": [1,2,3]})` → `CRASH AttributeError: 'list' object has no attribute 'get'`；
  - `gate_G5("genesis",None,None)`（deps.json t2-phases=[1,2,3]）→ `CRASH AttributeError: 'list' object has no attribute 'get'`；
  - `gate_G6("long-form",None,None)`（deps.json t3-pipelines=[1,2,3]）→ `CRASH AttributeError: 'list' object has no attribute 'get'`。
- 建议方向: 与 F460/F465/F485 统一——g3.py:152 对 `skills.get(skill_name)` 加 dict 校验（非 dict 记 SKIP），g0.py:107 对 `_calibration_hashes` 加 dict 校验（畸形走"missing"FAIL 分支或容错），g5.py:40/g6.py:39 对 `t2-phases`/`t3-pipelines` 加 dict 校验（畸形返回 FAIL "malformed deps.json"）；补 4 类畸形形状用例。

### F494 | G6.4 时间线检查只读每章前 5000 字符 → 章节后半段的时间线回归完全漏报（check_pacing 却读全文，采样口径不一致） | error | P2
- 证据: g6_checks.py:30（`ct = ch.read_text(encoding="utf-8")[:5000]`）——check_continuity 对每章只取前 5000 字符扫描 `第N天`/`N月N日`/`阶段N`；对照同文件 check_pacing（:96 `ct = ch.read_text(encoding="utf-8")` 全文）与 check_style_consistency（:230 全文）——同一 G6 下三个检查采样口径不一致；真实章节（novel-output/xinghuo-ranqiong/chapters/chapter-10.md 7435 字）正文约 2/3 在 5000 字符窗口内，尾部 1/3 的时间线事件永不可见；且 [:5000] 原始文件切片含 frontmatter+PRE 元区块（叠加 F474 的元区块日期噪声）。
- 根因: 采样截断（性能优化残留）使 G6.4 时间线单调性检查覆盖不到章节后半段——回归发生在窗口外时既无 FAIL 也无 WARN（漏报方向）；F456（排序）与 F474（同章互比/无年份）覆盖的是其它机制，未覆盖截断本身。
- 验证命令+输出: `.venv/bin/python -c`（chapter-1="第5天"+填充、chapter-2=5203 字符处放"第3天"）→ `check_continuity` → **无任何 regression mf（漏报）**；同回归放到 chapter-2 前 5000 字符内 → `['G6.4:timeline_regression:ch1->ch2:5->3(day)']`（检出）。实测输出确认"第3天 char position in ch2: 5203 (5000 limit = False) → NO REGRESSION DETECTED"。
- 建议方向: 与 check_pacing 对齐读全文（或至少剥离元区块后取正文全文）；若保留截断必须显式 SKIP/WARN 说明采样范围；补"回归在章节 5000 字符之后 → 应 FAIL"用例。

### F495 | review_arc_payoff / review_resonance 的 file+line 证据正则被时间/比例/日期形态满足 → 无文件引用证据的报告 false PASS（fail-open 启发式） | docs | M
- 证据: review_arc_payoff.py:101 与 review_resonance.py:118（`re.search(r"L\d+|line\s+\d+|:\d+(?!\d)", content, re.IGNORECASE)`）——`:\d+(?!\d)` 匹配任何"冒号+数字"形态：`7:30`（时间）、`1:5`（比例）、`2026:08`（类日期）都满足"file+line evidence"；两个 check 的 intent（docstring/注释 "evidence carrying file+line references"）是文件+行号证据，实现却接受任意冒号数字。
- 根因: 证据正则的 `:\d+` 分支过宽（无文件路径前置约束），使报告正文出现任意时间/比例/编号即通过 G4.ap.evidence / G4.rr.evidence——FAIL 分支近乎死路（fail-open 方向，与 F430 同类但影响弱：F430 恒 PASS、此处需正文无任何冒号数字才 FAIL）。
- 验证命令+输出: `.venv/bin/python -c` 正则实测——`'会议持续到 7:30 结束。'` → evidence-found=True；`'比例 1:5 说明。'` → True；`'2026:08 数据。'` → True；`'本章无任何引用。'` → False（唯一 FAIL 路径）。
- 建议方向: `:\d+` 分支加文件路径前缀约束（如 `[\w\-/]+\.\w+:\d+`）或删除该分支只保留 `L\d+|line\s+\d+`；与 review_arc_payoff/review_resonance 两文件同批修复；补"仅含时间/比例但无文件引用 → FAIL"负例。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个专用 checker + generic 3 分支 + score 三副本 + decisions_validator/make_composite_checker 逐个通读）。未发现清单外遗漏文件。
- 实测基线：本轮未运行全量 pytest（沿用前十一轮 785 passed 基线，本轮未修改任何文件）；新增只读验证 15+ 组（`.venv/bin/python` /tmp 临时目录构造 + 真实项目 `novel-output/xinghuo-ranqiong` 的 chapters/pending_hooks.md 实证 + 端到端 `gate_G0(seed)` 运行观察 G0.5b + 交叉区源码 decisions.py/safe_write.py/phase_runner.py 核实）；`git status` 复查无跟踪文件污染。
- 本轮重点复核了任务点名的三处：30 个 per-skill g4 checker 最后一遍（24 专用 + generic 3 分支 + score 三副本，全部通读）、G0 各子检查（G0.1-G0.16/G0.cc 全部路径，含 G0.14 deps.json 内层、G0.5b evidence/threshold 正则）、g6_checks/g7/g3_independence 剩余路径（含 check_continuity 采样截断、check_pacing/check_style_consistency 全文读取对照、G7.13-G7.16 全部分支、G3.4 独立性函数本体）。
- 已排除的候选（非 finding）：g3.py:152 之外的内层形状实例（g6.py:39 的 t3-pipelines 已实测归入 F493；g5.py:63 summary_data[pr] 为 list 与 F460 实例 3 的标量同属一个缺陷的两种非 dict 形态）；G2.dec.4 的 `"$schema"` 计数对字符串值内嵌 `"$schema"` 文本的误报（需 LLM 产出含字面量引号文本，低概率，未录入）；g0.py G0.8 fixture 正则对双扩展名路径的误匹配（无实际双扩展名 fixture，未录入）；G4 空文件列表 → PASS（F409 家族在 G4 的同类，但 CLI/G6.3 调用面恒传实际文件，可达性低，未录入）；g1.py G1.5 锁文件未来 mtime 负年龄（时钟偏移边界，未录入）。
- 跨区交叉确认建议（与 prior rounds 一致）：F493 的 progress.json/deps.json 内层形状 ↔ Z5/Z6（pipeline/dispatcher 区）；F494 的 G6.4 采样口径 ↔ Z5/Z6；F408 的 run_gate_g3 调用语义 ↔ Z5/Z6；F473/F474 的 G6.11/G6.4 真实项目恒定 FAIL 归因 ↔ Z5/Z6。

## 四、最终消息（复核统计）

- **新 finding：3**（F493：jload 内层形状家族第 6 批 4 处实例（g3.py:152/g0.py:107/g5.py:40/g6.py:39），4 组实测崩溃；F494：G6.4 check_continuity 5000 字符采样截断 → 章节后半段时间线回归漏报，实测对照检出/漏报翻转；F495：review_arc_payoff/review_resonance 证据正则 `:\d+` 被时间/比例形态满足 → file+line 证据检查 fail-open，正则实测。全部实测或代码路径验证）
- **误报：0**（F400-F492 全部成立；对任务点名的 F401/F404/F408/F432/F444/F458 六个 P1 逐一读源码 + 实测反证：F404/F458 空串绕过用正确 schema 字段实测复现，F408 伪造写入读 dispatch_helper.py 确认，F401 GR.2 恒 FAIL 读码确认，F432 74-vs-69 计数 comm 实测确认，F444 G3.3 skill 层查找读码确认；无任何推翻证据）
- **严重度异议：0 新增**（确认前几轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度按决策表核对无异议，F450/F464/F473 的"真实项目恒 FAIL 可争 P1"维持前几轮 P2 判定且本轮不单独升级的理由已注明）
- **0 新 finding 判定：否**（本轮发现 3 个前十一轮漏报，未完全收敛到 0；但相比前几轮（12/9/6/10/6/6/5/11/3/4/2 条）已处于低位收敛：F493 是既有 crash 家族的实例补齐（第 6 批），F494 需对照三个检查的采样口径差异才可见，F495 需正则语义词义分析才可见）
