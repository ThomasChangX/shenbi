# Z1 分区第四轮独立复核报告（agent e）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（13 findings F101–F113）+ `Z1.review.md`（9 findings F114–F122）+ `Z1.review2.md`（5 findings F123–F127）+ `Z1.review3.md`（6 findings F128–F133）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件通读；对跨区依赖 `contracts/legacy.py`、`contracts/paths.py`、`contracts/graph.py`、`gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g3.py`、`gates/g5.py`、`gates/g4/generic.py`、`gates/gate_manifest.py`、`audit/_shared.py`、`dispatcher/executor.py`、`orchestration/scoring_bridge.py`、`trace/writer.py`、`tests/tiers/deps.json` 及全部 82 份 rubric 独立核实）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟（`PYTHONPATH=src` 纯内存模拟，无文件写入）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F134–F199（实际产出 F134–F139，共 6 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：6**（F134–F139；P2×3、M×3；**无新 P0/P1**）
- **误报数：0**（前四轮 33 条全部成立；无一条被证伪，含 F115 的 P1 判定——独立复跑解析器后确认无反证）
- **严重度异议数：1**（F109：同意 R1/R2/R3 的 M→P2 异议；无新增异议）
- **0 新 finding 判定：否**（6 条新漏报，均为中/低危；其中 F134/F135 为 validate_scores 两个校验空洞、F137 为 F113 家族的第三个静默吞错站点——均为前四轮未覆盖的独立缺陷站点）

---

## 1. 复核结论表（F101–F133 逐条判定）

| 编号 | 标题 | 前四轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | `git ls-files -s docs/framework/dependency-dag.json tests/tiers/deps.json skills/shenbi-worldbuilding/SKILL.md` → 均 `100644`；`ls -la` → 均 `-rw-------`（0600），实证复现。git 不追踪 rw 位 → CI `git diff --exit-code` 不可见，漂移持续增长（R3 报 126 个，复核时点仍成立） |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `docs/registry.md`/`docs/gates.md`/`docs/schemas.md`/`docs/dispatcher.md`/`docs/integrity.md` 顶层文件全 MISSING；`tests/build_registry.py` No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 的 action 不破，仅 doc_url 破——与 R1 修正一致） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立**（计数微调） | 独立 AST 扫描 src/ 全量 raise：直接 RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentTimeoutError/SubAgentProtocolError/RetryExhaustedError（5/22）；NEVER RAISED **16 类**（SubAgentUnavailableError 因继承自被 raise 的 DispatcherError 属"捕获可达"，初审计 17 系将其计入；实质结论——error_guidance/recovery 目录引用的 6 类全部无 raise 站点——不变）。F103 成立 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 validate_scores 空 → `REJECT: scores is empty` → exit 2；模拟输出一致；test_scoring.py:598-613 恒带 scores.json（分支无测试触达） |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117 `str(None)`；g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **`c.append({"id":"G5.3","s":"PASS",...})`（g5.py:202-203）**——R3 对 R1 的"静默 SKIP"事实修正正确（真实行为是空证据 PASS）；对无 `expected_outputs` 的 phase，G5.4 循环空、G5.5 SKIP → G5 整体可假 PASS |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一 `except Exception`（grep 证实）；G3 subprocess（:343-355）无 timeout → 超时/崩溃/非 JSON → 门检静默跳过 |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | phase_runner 全文 0 引用；仅 gates/cli.py:131-136 手动入口；command-to-give.md T2/T3 协议无三者 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :103-105 mkstemp 在 `try`（:106）外——若抛 OSError，:117-125 finally 不执行 → flock fd/.lock 永久泄漏；:89-91 takeover unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert 实证；`python -O` 下守卫消失与 docstring :110 "Raises on mismatch" 冲突；工具仍会静默写入 deps.json（:201）。**同意 R1/R2/R3：M→P2**（运行时正确性守卫而非文案）。补充：因 main() 内 `generated` 与 member_outputs 同源派生（docstring 自认"NOT a meaningful check"），assert 实际近乎恒真，失效风险主要在未来重构场景 |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证；test_exceptions.py:64 同款文案 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9 实证；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo(" src/shenbi` 0 命中；同族 `backup()`（paths.py:33-35）亦 0 调用方（复核确认） |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `issubclass(subprocess.TimeoutExpired, OSError) == False`（mro: TimeoutExpired→SubprocessError→Exception）；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有（复核确认） |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **全量复跑 `load_applicability`（独立脚本，非转述）**：82 个 rubric 中 38 个返回 `{}`（26 t1-skill + 9 t2-phase + 3 t3-pipeline）；4 个 N/A 豁免技能全部确认——worldbuilding rubric:43（dim4）、faction-builder:46（dim8）、location-builder:43（dim5）、story-architecture:44（dim6）均含 "N/A — exempted … scoring.py renormalizes weights for remaining applicable dimensions"，且其适用性表头为 `\| # \| Dimension \| Bug-hunt Standard \| Clean Standard \|`（非解析器认的 `\| Dimension scope \|`）→ 解析器对它们整体 no-op → 豁免维度（10% 权重）被计入 bug-hunt/clean 评分。command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径可复现；worldbuilding 有 bug-hunt/clean/generative 测试目录。按决策表"正常路径可复现功能错误"维持 P1（合并层若认定影响面仅 4 技能×1 维度可议降 P2——与前四轮一致，本复核不主动降级）。对照组：chapter-drafting（"Dimension scope" 头 + "dims 6,7,9" 数字）过滤**确实生效**（机制在格式匹配时工作） |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | executor.py:200-204 同路径 G2 FAIL → `return 1`（阻断）；phase_runner :211-239 仅 G4 FAIL 阻断（:228-230）；grep `"g2"` 在 phase_runner 外 0 消费 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值；:334-362 tier 块仅 `tier == "T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 `os.open` 成功后 flock 抛 OSError → fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-110 G4 bug-hunt/clean 分支不写标记（仅 :121 generative 分支写）；:128 G6 恒写 "generative"；shared.py:204 写侧命名 `{gate}-{target}-{test_type}`；scoring.py:200/:220 读侧按 test_type 拼接 → 非 generative + `--round-dir` 必 MARKER_MISSING。goal-prompt.md:86 三类型 marker 覆盖承诺与实现不符（R2 补充证据成立） |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关 → G2/G4 校验覆盖非确定 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `state["steps"].append(...)`；其余 5 命令均记录；grep `"action": "pre-score"` 0 命中 |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep CapabilityFS/capability_fs src/ tools/` → 0 生产命中（仅自身 + 测试）；设计文档承诺的"注入门"从未落地 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；`phase-runner pre-score`（缺参）→ IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"` → 0 命中；command-to-give.md:48 原文引用 `bash tests/dispatch-subagent.sh`；ADR-0009 已迁移为 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | :441 `"subagent" if "--subagent" in sys.argv else "interactive"`；command-to-give.md:49 文档化评分命令不带 --subagent |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep `ScoringStatus.OK\|ScoringStatus.UNIMPLEMENTED` src/shenbi → 0 命中（REJECT :158/:425、MARKER_MISSING :371 在用） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :290-301 仅 `json.loads(...)`；`[]`/`"hello"`/`{"foo":1}` 均合法 JSON → state 置 SCORED → finalize 可推进；finalize :304-333 不读 scores 内容 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫；模拟 `['scoring.py','--gate-only','G2','--files','a.md','--type']` → IndexError（实证）；对照 :300 `gate_type` 有 `idx+1 < len` 守卫 |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` 只替换首个 BANNER..END 块 |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers :187-224 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error", "phase"}` 无 status 键；对照 :139-146/:281-289 均有 status |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子（删新持有者锁） | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 后重建 → 交错时序下释放者删除新持有者锁 → 双写者并发 |

**前四轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115-205 确认 **R3 修正正确**（`if project_dir:` 对 `"None"` 为真 → 扫描块 → 无冲突 → PASS）；F101 波及文件数 85→89→126 的递增记录属实（safe_write 持续改写所致）。无新的前轮事实错误需要修正。

---

## 2. 新发现（F134–F139）

F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 不可解析 rubric 产出静默 0 分（FAIL）而非 REJECT | error | P2
- 证据: src/shenbi/scoring.py:145-159（`if not scores:` 只查空 **scores**，不查空 **dimensions**；:151-152 空 expected_nums → 所有键走 extra → **WARNING 不 REJECT**）+ :169-170（`if total_weight == 0: return 0`）
- 根因: F104 是"空 scores + 非空 dims → REJECT"的镜像——**"非空 scores + 空 dims"未设防**。rubric 无可解析的 `| #` 维度表时 `load_rubric` 返回 `[]`，validate_scores 以 WARNING 通过，compute_score 走 `total_weight == 0` 分支静默返回 0 → 最终输出 `final_score: 0` + `FAIL`，无任何错误信号
- 验证命令+输出: `PYTHONPATH=src python3 -c "from shenbi.scoring import validate_scores, compute_score, classify; print(validate_scores({1:100,2:95}, [])); print(compute_score([], {1:100,2:95}, False), classify(0).value)"` → `(True, ['WARNING: unexpected dimension keys (ignored): [1, 2]'])` / `0 FAIL`（无 REJECT、无 exit 2）
- 建议方向: validate_scores 增加 `if not dimensions: errors.append("REJECT: rubric has no parseable dimensions")`（fail-loud，与空 scores 对称）；或 load_rubric 无可解析维度时直接报错

F135 | scoring validate_scores 对 NaN 分数放行：`{"1": NaN}` 通过 0-100 校验 → final_score NaN 静默 | error | P2
- 证据: src/shenbi/scoring.py:153-157（`score < 0 or score > 100`——NaN 与任何数比较均 False；`isinstance(nan, (int,float))` 为 True → 校验通过）；:417-419（`json.load` 默认接受 `NaN` 字面量 → float('nan')）；:173-174（NaN×weight → NaN weighted_sum → round(NaN,2) → NaN）
- 根因: 范围校验用 `x < 0 or x > 100` 而非 `not (0 <= x <= 100)`，NaN 从两侧漏过；json.loads 非 strict 模式接受 NaN/Infinity（Infinity 会被 `> 100` 拦下，NaN 是唯一漏洞）
- 验证命令+输出: `PYTHONPATH=src python3 -c "import json,math; from shenbi.scoring import validate_scores, compute_score, classify; s={int(k):v for k,v in json.loads('{\"1\": NaN, \"2\": 95}').items()}; d=[{'num':1,'name':'a','weight':50},{'num':2,'name':'b','weight':50}]; print(validate_scores(s,d)); print(math.isnan(compute_score(d,s,False)))"` → `(True, [])` / `True`（NaN 静默通过并产出 NaN final）
- 建议方向: 校验改为 `not (0 <= score <= 100)` 或显式 `math.isfinite(score)` 检查；考虑 `json.loads(..., parse_constant=...)` 拒绝 NaN/Infinity

F136 | status.py CommandResult TypedDict 全仓无使用："Emit sites use enum members through typed result structures" 的静态类型保证未落实 | dead-wire | M
- 证据: src/shenbi/status.py:78-86（`class CommandResult(TypedDict)`——docstring 声明 "Typed envelope for a phase_runner command outcome"）；grep `CommandResult` src/ → **仅 status.py 定义处 1 命中，0 消费**；phase_runner.py 全部 emit 用裸 dict（:123-129/:139-146/:166-175 等）；status.py:3-5 docstring 宣称 "Emit sites use enum members through typed result structures, so ``"status": "PASSED"`` is a static type error under mypy AND basedpyright"——phase_runner 的裸 dict 使该保证（spec §5.2 / audit D3）实际未生效
- 根因: F127 同族（status.py 词汇表/类型结构的死成员）——GateResult 至少被 gates/shared.py:127,140 消费（fail/passed 类型标注），CommandResult 则完全未接线；类型强制依赖"emit 站点使用 TypedDict"，而 phase_runner 从未迁移
- 验证命令+输出: `grep -rn "CommandResult" src/shenbi --include="*.py"` → 仅 `status.py:78`（定义）；`grep -rn "CommandResult" src/shenbi/phase_runner.py` → 0 命中
- 建议方向: 与 F127 一并处置——要么 phase_runner 的 emit 信封改用 `CommandResult`（兑现 docstring 的静态类型承诺），要么删除该 TypedDict 并修正 status.py:3-5 的过度声明

F137 | phase_runner cmd_post_skill 经 derive_output_files/derive_file_type 静默吞 ContractError：契约损坏 → G2 SKIP + G4 空文件 vacuous PASS → 相位无校验推进、无日志 | error | P2
- 证据: src/shenbi/phase_runner.py:196-199（`derive_output_files(skill, chapter, proj)`——契约失败返回 `[]` 时 `if output_files:` 不成立 → :211 `g2_status = SKIP`、G2 不运行）+ :216（G4 以空文件串运行）；src/shenbi/audit/_shared.py:57-58（`except ContractError: return []` 无日志）；src/shenbi/dispatcher/executor.py:74-75（`except ContractError: return "chapter"` 无日志）；src/shenbi/gates/g4/generic.py:46-47（`if not fps: c.append({"id":"G4.gen","s":"SKIP","r":"no files"})` → mf 空 → `passed(...)`）——**generic checker 空文件 → vacuous PASS**
- 根因: F113（pre-skill 吞错）与 F117（sync_contracts 吞错）同根因的**第三站点**——post-skill 经 audit/_shared 与 executor 两个 `except ContractError: return 默认值` 静默降级，且 generic G4 对空文件返回 PASS（SKIP note）而非 FAIL；chapter 给定（pipeline 路径）时无 rglob 兜底 → G2 SKIP + G4 vacuous PASS → post-skill emit OK、相位照常推进。audit 相位诸 skill（shenbi-review-* 系列，deps.json prerequisites 证实）走 generic checker，场景可达。前四轮只覆盖了 pre-skill（F113）与 sync_contracts loader（F117），post-skill 站点未提及
- 验证命令+输出: 代码走查 phase_runner.py:196-216 + audit/_shared.py:47-58 + executor.py:72-75 + generic.py:28-47；`grep -rn "except ContractError" src/shenbi/audit/_shared.py src/shenbi/dispatcher/executor.py` → `:57` / `:74`（均无日志）
- 建议方向: 与 F113 对齐——derive_output_files/derive_file_type 的 ContractError 分支补 `log.warning("contract_load_failed", skill=..., exc_info=True)`（或让 phase_runner 自行 try load_contract 并 fail-loud）；generic G4 对空文件返回 FAIL（"no files to validate"）或至少 WARN 而非 vacuous PASS；补"契约损坏 → post-skill 不推进"测试

F138 | error_guidance.py / recovery.py 的 `log = get_logger(__name__)` 从未使用（死代码） | dead-wire | M
- 证据: src/shenbi/error_guidance.py:9、src/shenbi/recovery.py:8（`log = get_logger(__name__)`）；`grep -n "log\." src/shenbi/error_guidance.py src/shenbi/recovery.py` → 0 命中（两模块除 import 行外无任何日志调用）
- 根因: 目录编写时预留日志句柄，后续实现未使用；两模块本身已因 F103 整体 dead，此为 F103 家族内的独立死代码点
- 验证命令+输出: `grep -c "log\." src/shenbi/error_guidance.py src/shenbi/recovery.py` → `0` / `0`（排除 import 行）
- 建议方向: 删除未使用的 log 绑定（或随 F103 的整体裁剪一并处理）

F139 | __init__.py docstring 子模块清单仅列 6/12：缺 capability_fs/cli_utils/error_guidance/paths/recovery/safe_write/status/sync_contracts，与初审"与实际结构一致"断言不符 | doc↔code drift | M
- 证据: src/shenbi/__init__.py:3-10（docstring 列 exceptions/logging/scoring/phase_runner/gates/dispatcher 六项）；`ls src/shenbi/*.py` 实际 12 个模块（含 capability_fs、cli_utils、error_guidance、paths、recovery、safe_write、status、sync_contracts）
- 根因: docstring 编写于早期（F111 同源），后续新增模块未同步清单；Z1.a.md:42 声称"docstring 子模块清单与 src/shenbi/ 实际结构一致 ✓"不成立（只验证了列出的 6 项存在，未验证完整性）
- 验证命令+输出: `python3 -c "import ast,os; ..."` → `missing from docstring: ['capability_fs','cli_utils','error_guidance','paths','recovery','safe_write','status','sync_contracts']`（8 项）
- 建议方向: 补全 docstring 清单（或改为"核心子模块"措辞避免完整性承诺）；与 F111 一并更新

---

## 3. 严重度异议汇总

| 编号 | 前四轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`python -O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移（CI 幂等检查为唯一间接兜底）。按决策表 P2"边界/错误处理缺陷/防御深度"而非 M"错别字/格式" |
| F115 | P1 | **维持 P1（无反证）** | 独立全量复跑解析器（非转述）：38/82 no-op、4 技能 N/A 豁免全部确认、command-to-give.md:64,71 文档化路径可复现。按决策表"正常路径可复现功能错误"维持；合并层可据"影响面仅 4 技能×1 维度（10% 权重）"裁量降 P2，本复核不主动降级 |

其余 31 条严重度无异议（F101-F108/F110-F114/F116-F134/F137 的 P2 与 F110/F111/F126/F127/F130/F131/F132/F136/F138/F139 的 M 均认可）。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读；另核实 13 个跨区依赖文件/数据（contracts/legacy.py、contracts/paths.py、contracts/graph.py、gates/cli.py、gates/shared.py、gates/g2.py、gates/g3.py、gates/g5.py、gates/g4/generic.py、gates/gate_manifest.py、audit/_shared.py、dispatcher/executor.py、orchestration/scoring_bridge.py、trace/writer.py、tests/tiers/deps.json、82 份 rubric）以验证调用契约）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型六方法核实无洞；F123 已覆盖无接线）、`cli_utils.py`（stdout 数据通道/stderr 日志分离自洽）、`logging.py`（双渲染器/stderr/`cache_logger_on_first_use=False` 自洽；SHENBI_LOG_FORMAT 非 "json" 静默降级 console 为前轮已记录的低价值项，同意不立 finding）、`paths.py`（read/write 语义正确；F112 覆盖死代码）、`recovery.py`（F103 覆盖 dead；仅 F138 未用 log）、`py.typed`（0 字节 marker 存在 + hatchling 配置层一致）
- 新发现分布：`scoring.py`（F134/F135——validate_scores 双校验空洞）、`status.py`（F136——CommandResult 死 TypedDict）、`phase_runner.py`（F137——post-skill 静默吞错站点）、`error_guidance.py`/`recovery.py`（F138）、`__init__.py`（F139）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **load_rubric 对全部 82 个真实 rubric 的维度解析已验证无异常**（唯一 total_weight≠100 的是 `_template/rubric.md`，其占位符 `[weight]%` 属模板非缺陷）；适用性表行不会注入幻影维度（`cells[2]` 为标准文本 → int() ValueError → continue）。
2. **`--gate-only` 无 `--files` → vacuous G2 PASS**（gates/cli G2 空文件 → `passed("G2", [])`）——vacuous-pass 源头在 gates/g2.py（非 Z1），scoring.py:296-313 仅中继；低危，未立 finding。
3. **`scoring.py` 对非 dict scores.json（如 list）→ `.items()` AttributeError traceback**；`--interactive` 必须 argv[2]——R2/R3 备查已记录同族（CLI 健壮性），不重复。
4. **G5.4 的 `pd = Path(project_dir) if project_dir else PROJECT` 回退到 repo root 扫描**（g5.py:210）——仅在直接 CLI 调用 G5 且缺 project_dir 时触发（phase_runner 恒传 "None" 真值字符串走不到该分支）；gates 区（非 Z1）潜在问题，未立 finding。
5. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达（本复核独立确认 `grep render_body_into tests/` 0 命中）——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。

---

## 5. 其他说明

- 前四轮"无 P0/P1"结论自 R1 起因 F115 修正为"P1×1"；本复核独立复跑确认 F115 无反驳证据，**无任何前轮 finding 被证伪**（误报数 0）。
- F101 波及面（0600 漂移 126+ 文件且 git 不可见）是全部 39 条 finding 中持续增长最大的一项，建议合并层与 CI 幂等检查盲区一并纳入修复优先级。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成（`PYTHONPATH=src` 纯内存导入，无文件写入；未运行 pytest 避免写 `__pycache__`；未 uv run）。
