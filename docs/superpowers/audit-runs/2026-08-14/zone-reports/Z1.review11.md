# Z1 分区第十一轮独立复核报告（agent l）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`（F114–F122）+ `Z1.review2.md`（F123–F127）+ `Z1.review3.md`（F128–F133）+ `Z1.review4.md`（F134–F139）+ `Z1.review5.md`（F140–F143）+ `Z1.review6.md`（F144）+ `Z1.review7.md`（F145）+ `Z1.review8.md`（F146–F147）+ `Z1.review9.md`（F148–F153）+ `Z1.review10.md`（F154–F155），共 **53 条**（F101–F155，其中前十一轮新发现 42 条）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读，未依赖任何前轮转述；对跨区依赖 `gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g4/generic.py`、`gates/g5.py`、`audit/_shared.py`、`dispatcher/executor.py`、`dispatcher/modes/codex.py`、`.venv/bin/shenbi-*` 控制台脚本、82 份 rubric、skills/*/SKILL.md 契约独立核实）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟、真实控制台脚本实跑（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`，临时文件仅写 /tmp）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F156–F199（实际产出 F156–F157，共 2 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：2**（F156 P2、F157 M；**无新 P0/P1**）
- **误报数：0**（F101–F155 全部 53 条成立；无一条被证伪。F115/F140 两个 P1 经本复核**独立实跑/复跑确认无反证**）
- **严重度异议数：新增 0；维持既有 3**（F109：同意 R1–R10 共识 M→P2；F115：P1 无反证维持；F140：P1 无反证维持）；F134 的"文档契约背离"角度复核后维持 P2（理由见 §3）
- **0 新 finding 判定：否**（2 条新漏报：F156 为 phase_runner/executor 双路径的 G2 混合输出文件类型缺陷——前十一轮只覆盖了 ContractError 家族（F113/F117/F137/F148）与 G2.7 缺参（F150），未覆盖"derive_file_type 单一类型施加于混合输出 → .md 被 decisions 分支整体跳过"站点；F157 为 scoring 交互模式 stdout 数据通道污染——前十一轮仅将交互路径记为"非 CI，acceptable"，未检查其 stdout 通道契约）

---

## 1. 复核结论表（F101–F155 逐条判定）

| 编号 | 标题 | 前十一轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 实证复现：`git ls-files -s docs/framework/dependency-dag.json` → `100644 c8000598...` vs `ls -la` → `-rw-------@`；根因确认 mkstemp 默认 0600（safe_write.py:105）+ os.replace 继承（:111）；git 不追踪 rw 位 → CI 幂等检查不可见 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `ls docs/{registry,gates,schemas,dispatcher,integrity}.md` → 全部 No such file；`ls tests/build_registry.py` → No such file；`tests/lock-tool-hashes.sh` **存在**（ToolTamper 仅 doc_url 破——与 R1 修正一致） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立 AST 全仓 raise 扫描：exceptions.py 层级内直接 RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentTimeoutError/SubAgentProtocolError/RetryExhaustedError（5/22），NEVER RAISED 17 类（字面计数；R4"捕获可达"折 16 注记合理，实质结论不变）；error_guidance/recovery 引用的 6 类全无站点 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | 走查 :414-415 `scores={}` → :422 validate_scores 空 → `REJECT: scores is empty` → exit 2；分支无测试触达（test_scoring.py:598-613 恒带 scores.json） |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117 `str(None)`；独立走查 g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **PASS（g5.py:202-203，非 R1 所称 SKIP）**；对无 expected_outputs 的 phase 可假 PASS。**补充（事实级）**：cmd_finalize :307 的 G5 调用同样 `str(project_dir)` → 同一 "None" 缺陷在 finalize 路径重复，属 F105 家族内而非新 finding |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一无 exc_info 的 `except Exception`（对照 phase_runner:74、safe_write:139 均带 exc_info）；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | `grep phase_runner` 全文 0 引用（实测）；仅 gates/cli.py:131-136 手动入口；command-to-give.md T2/T3 协议无三者 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——抛 OSError 时 :117-125 finally 不执行 → flock fd/.lock 泄漏；:89-91 unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M（R1 起异议 M→P2） | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert 实证；docstring :110 "Raises on mismatch" 与 `-O` 下守卫消失冲突；工具仍会静默写 deps.json（:201）。**同意 R1–R10 共识：M→P2**（运行时正确性守卫而非文案；R4"同源派生 → 近乎恒真"注记成立） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 `f"{len(mismatches)} source files changed..."` 无单复数区分；test_exceptions.py:64 同款 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9 实证；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo(\|\.backup("` src/ → 0 命中（实测）；同族 `backup()`（paths.py:33-35）亦 0 调用方 |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `subprocess.TimeoutExpired.__mro__` 含 SubprocessError 且 `issubclass(TimeoutExpired, OSError)==False`；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有 |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **独立复跑真实解析器**：`load_applicability(worldbuilding)` → `{}`；`filter_dimensions_by_test_type(dims, worldbuilding, 'bug-hunt')` → 返回全部 9 维（**含 N/A 豁免的 dim 4 Prose quality，10% 权重**，worldbuilding rubric:43 明示 "scoring.py renormalizes weights…"）；对照组 chapter-drafting（"Dimension scope" 头）过滤确实生效。command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径。**反证尝试全部失败**。按决策表"正常路径可复现功能错误"维持 P1 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径 G2 FAIL → return 1；command-to-give.md:48 "只在 G2 和 G4 都通过后…评分" 协议与相位机路径不符 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | `grep _phase` → 仅 :321,:331 两处赋值（实测），全文无第二次读取；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 :58 已开 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-110 G4 bug-hunt/clean 分支不写标记（仅 :121 generative 分支写，且 shared.py:195 `result.get("status") != "PASS"` 不写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 `-{test_type}` 拼接 |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 `[...][:20]` 实证；rglob 顺序 OS 相关 → G2/G4 校验覆盖非确定 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `state["steps"].append(...)`（重读逐行确认）；其余 5 命令均记录（:121/:164/:226/:298/:331） |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS/capability_fs src/ tools/` → 仅 capability_fs.py 自身；0 生产消费者（实测） |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；`phase-runner pre-score`（缺参）→ IndexError（代码走查确认） |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"`（排除 .git）→ 0 命中（实测）；ADR-0009 已迁移为 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | **实跑实证**（见 F140 验证输出）：不带 --subagent 的文档化评分命令输出 `"scored_by": "interactive"` |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | `grep ScoringStatus.OK\|ScoringStatus.UNIMPLEMENTED` src/ → 0 命中（实测）；REJECT :158/:425、MARKER_MISSING :371 在用 |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均合法 JSON → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫（对照 :300 `gate_type` 有 `idx+1 < len` 守卫） |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` 只替换首个 BANNER..END 块 |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers :187-224 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 status 键 |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立（维持 P2）** | 实测 `validate_scores({1:100,2:95}, [])` → `(True, ['WARNING: unexpected dimension keys…'])` + `compute_score([],…)` → 0 FAIL（无 REJECT、无 exit 2）。R8 补充的 command-to-give.md:55 "dimensions 为空 → exit 2" 契约证据成立；**严重度讨论**：按决策表"违反显式契约"可议升 P1，但触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析——本复核独立复跑确认仅 `_template/rubric.md` weight≠100 为占位符良性），缺"正常路径可复现"，按 R8/R9/R10 维持 P2 |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 `NaN` 字面量 → NaN 通过校验并产出 NaN final |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | `grep CommandResult` src/ → 仅 status.py:78 定义，0 消费（实测） |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志（重读确认）；g4/generic.py:46-47 空文件 → SKIP 后 `passed(...)`（vacuous PASS，且写 PASS marker → pre-score 假通过） |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | `grep -c "log\."` 两文件均 0（排除 import 行） |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块（ls 实证） |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **独立实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**：完整 10 维 scores + `--test-type generative` → 计算完全成功（stdout 合法 JSON、final_score 正常）但 **EXIT CODE: 1**，stderr 1189 字节为完整 result dict repr；`.venv/bin/shenbi-score` 包装 `sys.exit(main())` → `SystemExit(dict)` → exit 1（cat 脚本内容实证）；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `if result.returncode != 0: return result.returncode` → `_record_completion` 永不可达；`scoring.main` 无库调用方。对照验证 `shenbi-phase`（main 返回 None → exit 0）与 `shenbi-sync-contracts`（返回 int）均正确。**反证尝试全部失败** |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | 实测 `validate_scores({1: True, 2: 95}, dims)` → `(True, [])` 且 `compute_score` → `48.0`（`isinstance(True, (int,float))` → True，True 按 1 计） |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | filter docstring :107-109 宣称 "renormalize weights" 但 :135-136 仅过滤；compute_score :171-172 以 total_weight≠100 发 warning；实测 chapter-drafting bug-hunt 过滤后权重和 = 75 |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过，无 json.loads、无 crash）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/缺 state 键/非法 JSON → 裸 TypeError/KeyError/JSONDecodeError traceback |
| F145 | phase_runner `load_deps()` 无任何错误处理 | M | **成立** | :32-33 `json.loads(...)` 无 try；:245/:318 调用；对照 g5.py:36-39 try/except → 结构化 FAIL、scoring.py:206 exists() 守卫——phase_runner 是 deps.json 的唯一无守卫消费者。定 M 恰当 |
| F146 | scoring.check_gate_markers t2 分支对 deps.json 仅 exists() 守卫 | M | **成立** | :205-207 `if deps_path.exists(): deps = json.loads(...)`——exists() 只防缺失不防损坏；同族三消费者中 scoring 站点漏报 |
| F147 | sync_contracts.main() deps.json 读取（:195）与 load_registry（:165）均无守卫 | M | **成立** | :195 `json.loads(DEPS_PATH.read_text(...))` 无 try；:165 `load_registry()` ContractError 裸上抛；对比同文件 :167-172 的 bail 有 log.error + return 1 |
| F148 | phase_runner post-skill 契约声明但磁盘缺失/为空的输出静默丢弃 | P2 | **成立** | :196-200 `exists()+st_size>0` 静默过滤缺失项无日志；:211-216 空 output_files → G2 SKIP + G4 空文件 vacuous PASS + PASS marker → 相位 OK 推进 |
| F149 | scoring filter 对无维度数字的排除 scope 静默 no-op（模板自身触发） | P2 | **成立** | 独立复跑 `_template/rubric.md`：表头 `\| Dimension scope \|` 正确匹配（属 44 个已解析之一），"Prose/narrative quality" 行标 No 但 scope 无维度数字 → excluded_nums 恒空 → no-op（过滤前后均为 [1,2]，`== True` 实证）；R2 备查 #5 "44 个未触发" 与事实不符 |
| F150 | phase_runner run_gate("G2") 缺第 4 参 project_dir → G2.7 重要章节 ceiling 恒失效 | M | **成立** | :213 3 参；gates/cli.py:60-63 G2 分支支持 `pd = arg(3, None)`；g2.py:237 `_is_important_chapter(fp, project_dir or "")` + :382-384 `if not project_dir: return False`；executor.py:122-134 run_g2 传 `str(PROJECT_DIR)`=REPO_ROOT（:26 实证）亦非真实项目目录。**补充**：对 file_type="decisions" 的混合输出技能（chapter-drafting 等），.md 在 G2.7 之前已被整体跳过（见新 F156）——F150 的 ceiling 失效场景实际只覆盖 file_type="chapter" 的技能，两者机制不同、受害技能集不同，均成立 |
| F151 | scoring.py:419 scores.json 非数字键静默丢弃且无 WARNING | M | **成立** | :417-419 `{int(k): v for k, v in raw.items() if k.lstrip("-").isdigit()}`——非数字键在 validate_scores 之前被静默过滤；实测 `{"1": 90, "dimensin1": 100, "notes": "x"}` → `{1: 90}` |
| F152 | load_rubric kill-switch 解析漏 "→ detection dimension = 0" 类条目 | M | **成立** | :43-47 只匹配 "total score = 0"/"phase = 0"/"pipeline = 0"；实测 worldbuilding 捕获集 3 项、文件 kill-switch 节 5 行 `= 0`（`- Evidence without file+line citation → detection dimension = 0` 未被捕获；12+ 份 rubric 同） |
| F153 | sync_contracts.render_body_view 的 `skill` 参数从未使用（死参数） | M | **成立** | :124 定义 + :149 传参外，函数体内 `skill` 0 命中 |
| F154 | safe_write flock 主路径与 O_EXCL 回退路径互不感知对方锁原语 | M | **成立（同意 R10 定级，触发面窄）** | :54-62 flock 分支从不检查 lockfile 存在性；:63-91 O_EXCL 分支从不检查目录 flock 状态；:61 `except (ImportError, OSError): pass` 为唯一切换点——两原语互斥语义不共享，flock 对单写者间歇失败（EINTR）时混合场景双写者并发 replace 同一目标。F108/F119/F133 均为原语内竞态，本条为跨原语站点。触发需 flock 间歇失败，M 恰当 |
| F155 | scoring.check_scorer_agreement NaN 误报 agreed；flag_score_collapse 单维度误报 all_identical | M | **成立** | 实测：`check_scorer_agreement({1: nan, 2: 90}, {1: 95, 2: 90})` → `{'agreed': True, 'max_diff': 0.0, 'disputed_dimensions': []}`（95 分差距被 NaN 掩盖）；`flag_score_collapse({1: 95})` → `{'collapse_suspected': True, 'signals': ['all_identical']}`（无 `len(values) >= 2` 守卫）。函数当前 dead（Z5 F500），接线前置缺陷 |

**前十一轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 **确认 R3 修正正确**；F101 波及文件数递增记录属实（本复核时点 dependency-dag.json 100644/0600 实证）；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致。**无新的前轮事实错误需要修正。**

---

## 2. 新发现（F156–F157）

F156 | phase_runner/executor 双路径对混合输出施加单一 derive_file_type：file_type="decisions" 时 G2 decisions 分支整体跳过 .md → 主章 .md 仅过 G2.1-2.3（存在/非空/UTF-8），文档化 G2.4-G2.12 内容校验（字数 G2.6、重要章节 ceiling G2.7、格式 G2.8/2.9、frontmatter G2.5）在管线路径静默不执行 | error | **P2**
- 证据: src/shenbi/phase_runner.py:196-213（`output_files` 为契约 writes+updates 全量解析结果 + `file_type = derive_file_type(skill)` 单一类型 → :213 单次 G2 调用）；src/shenbi/dispatcher/executor.py:66-86（derive_file_type：`outputs & _decisions_file_set()` → 返回 "decisions"）+ :200（run_g2 单次调用）；src/shenbi/gates/g2.py:76-84（decisions 分支 `if not fp.endswith(".json"): continue` —— .md 仅 G2.1-2.3 后跳过）+ :120（`continue  # CRITICAL: skip G2.4-G2.10 (word count etc.) for JSON decisions`）；受影响技能契约：skills/shenbi-chapter-drafting/SKILL.md:27-32（writes=[chapters/chapter-N.md, chapters/chapter-N-decisions.json]）、skills/shenbi-chapter-revision/SKILL.md（writes 含 state_snapshot-pre-rev.md + revision-decisions.json，updates 含 chapter-N.md）、skills/shenbi-short-drafting/SKILL.md（writes=[chapters/chapter-N.md, short/short-N-decisions.json]）
- 根因: derive_file_type 按"输出集合是否含 decisions 文件"返回单一类型，但输出文件是异构的（.md + decisions.json 并存）；G2 对全部输出施加该单一类型 → decisions 分支按扩展名跳过 .md。g2.py:83 注释宣称 ".md files are validated by their own file_type gate instead"，但全仓 G2 仅两个调用站点（executor.py:200、phase_runner.py:213）均单次单类型调用，**无任何调用方按文件类型拆分重跑**（grep `run_g2\|gate_G2` 实证）。前十一轮覆盖了 ContractError 家族（F113/F117/F137/F148，契约加载失败/产出缺失 → G2 SKIP）与 G2.7 缺参（F150，chapter 分支内 ceiling 失效），均未覆盖"契约正常、文件类型推导正确但类型与文件异构 → .md 被分支跳过"这一独立站点
- 验证命令+输出: 实跑 `gate_G2("/tmp/zb_g2/chapter-5.md,/tmp/zb_g2/chapter-5-decisions.json", "decisions", ...)` → chapter-5.md 仅 `[{'id':'G2.1','s':'PASS'},{'id':'G2.2','s':'PASS'},{'id':'G2.3','s':'PASS'}]`（无 G2.4-G2.12）；同文件 file_type="chapter" → check ids 含 G2.4/G2.5/G2.7/G2.10/G2.12（字数/重要章节/格式校验确实存在）；`derive_file_type("shenbi-chapter-drafting")` → `decisions`、`derive_output_files("shenbi-chapter-drafting", 5)` → `['chapters/chapter-5.md', 'chapters/chapter-5-decisions.json']`（混合输出实证）；全仓 grep G2 调用站点仅 2 处（无 per-file-type 拆分）
- 建议方向: 按文件拆分 G2 调用（.md 输出以 "chapter"/对应类型跑一次、decisions.json 以 "decisions" 跑一次，如 executor 侧先按 `fp.endswith(".json")` 分组）；或 derive_file_type 改为返回 per-file 类型映射；或 gate_G2 内部对 decisions 分支的 .md 走 chapter 校验（消除 continue）；补"混合输出 → .md 获得 G2.5-2.9 校验"单测。（严重度说明：按决策表 P2"边界/错误处理缺陷 + 正常路径门校验静默降级"，与 F121/F137/F148 同族；若合并层认为管线路径的 G2 结果仅元数据（F116 G2 不阻断）可议降 M，本复核按"文档化 G2 校验在正常路径静默不执行"定 P2）

F157 | scoring 交互模式 `input()` 提示词写入 stdout 数据通道：stdout 整体为单行"提示词+JSON"混合、不可 JSON 解析，违反 cli_utils "stdout=数据通道/stderr=诊断" 契约 | error | **M**
- 证据: src/shenbi/scoring.py:388（`input("Kill switch triggered? (y/n): ")`）、:397（`input(f"  {d['num']}. {d['name']} [...]")`——input() 的 prompt 经 sys.stdout.write 输出）；:462（`emit_json(result)` 在提示词之后写入 stdout）；src/shenbi/cli_utils.py:1-6 docstring（"stdout, parsed by downstream tools" 数据通道声明）
- 根因: 交互模式用 input() 采集分数，Python input() 将 prompt 写 stdout（非 stderr）；最终 emit_json 的 JSON 追加在同一 stdout 上且无换行分隔 → stdout 为 1 行"提示词文本+JSON"混合体。前十一轮仅将交互路径记为"391-393 交互路径非 CI，acceptable"（覆盖缺口角度）与"--interactive 必须 argv[2]"（R2 备查 #3 CLI 健壮性），未检查其 stdout 通道契约；实测完整 stdout `json.loads` → JSONDecodeError
- 验证命令+输出: `printf 'y\n95\n90\n...\n' | PYTHONPATH=src python3 -c "sys.argv=['scoring.py','tests/tiers/t1-skill/shenbi-chapter-drafting/rubric.md','--interactive']; from shenbi.scoring import main; main()"` → stdout 1544 字节、**仅 1 个换行**（提示词与最终 JSON 同处一行，JSON 嵌在行尾）；`json.loads(完整 stdout)` → `JSONDecodeError: Expecting value: line 1 column 1`（首个字符是 "Kill switch triggered?..." 提示文本）
- 建议方向: 交互提示改走 stderr（临时重定向 `sys.stdout→sys.stderr` 或改用 `input()` 前 `sys.stdout.write(..., file=sys.stderr)` 式实现/`getpass` 风格）；或交互模式 final emit 前输出空行并文档声明 stdout 仅末行可解析；补"交互模式 stdout 首字符为 `{` 或整段可 json.loads"断言。（严重度说明：交互模式为人工路径、无自动消费者，按 M"格式"级；若合并层认为通道契约违反应升 P2，本复核按"无正确性影响、人工可见"维持 M）

---

## 3. 严重度异议汇总

| 编号 | 前十一轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有异议）** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移。按决策表 P2"防御深度"而非 M"文案" |
| F115 | P1 | **维持 P1（无反证）** | 独立复跑真实解析器：worldbuilding applicability `{}`、filter 返回全 9 维（含 N/A 豁免 dim 4，10% 权重）、rubric:43 明示 renormalize 承诺、4 技能均有 bug-hunt/clean 测试目录、command-to-give.md:64,71 文档化路径。**反证尝试全部失败**。按决策表"正常路径可复现功能错误"维持 |
| F140 | P1 | **维持 P1（无反证）** | 独立实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr 1189 字节 result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达；`scoring.main` 无库调用方；对照 shenbi-phase/shenbi-sync-contracts 退出码正常。**反证尝试全部失败** |
| F134 | P2 | **维持 P2（不议升，附说明）** | command-to-give.md:55 确文档化 "dimensions 为空 → exit 2"，实现静默 0 分属契约背离——按决策表"违反显式契约"字面可议升 P1；但触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析，仅 `_template` 占位符 weight≠100），缺"正常路径可复现"，按 R8/R9/R10 维持 P2。本复核不新增异议 |

**新增严重度异议：0**。其余 49 条严重度无异议（F101–F108/F110–F114/F116–F121/F123–F125/F128–F129/F133/F135/F137/F141/F144/F148/F149/F156 的 P2 与 F110/F111/F122/F126/F127/F130/F131/F132/F136/F138/F139/F142/F143/F145/F146/F147/F150/F151/F152/F153/F154/F155/F157 的 M 均认可）。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读；另核实跨区依赖：gates/cli.py、gates/shared.py、gates/g2.py、gates/g4/generic.py、gates/g5.py、audit/_shared.py、dispatcher/executor.py、dispatcher/modes/codex.py、.venv/bin/shenbi-score、shenbi-phase、shenbi-sync-contracts、shenbi-validate 控制台脚本、pyproject.toml 入口、82 份 rubric、skills/*/SKILL.md 契约、command-to-give.md、goal-prompt.md）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型六方法逐一核实无洞；F123 覆盖无接线）、`cli_utils.py`（stdout/stderr 分离自洽；emit_json allow_nan 属 F135 NaN 链的序列化环节，不重复立 finding）、`logging.py`（双渲染器/stderr/`cache_logger_on_first_use=False` 自洽；SHENBI_LOG_FORMAT 校验为前轮已记录低价值项）、`paths.py`（read/write 语义正确；F112 覆盖死代码）、`error_guidance.py`/`recovery.py`（F103 覆盖 dead；F138 未用 log）、`exceptions.py`（层级结构正确；F103 覆盖 dead）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（除 F127/F136 外词汇表自洽；GateResult 由 gates/shared.py 消费非死成员）、`sync_contracts.py`（F109/F117/F130/F147/F153 覆盖；build_dag 重复边为无消费者的派生文档噪音，不立 finding）、`phase_runner.py`（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150/F156 覆盖）、`scoring.py`（F104/F106/F115/F118/F120/F126/F129/F131/F134/F135/F140/F141/F142/F146/F149/F151/F152/F155/F157 覆盖）、`safe_write.py`（F101/F108/F119/F133/F154 覆盖）
- 新发现分布：`phase_runner.py`+`dispatcher/executor.py`+`gates/g2.py` 跨文件（F156——G2 混合输出文件类型）、`scoring.py`（F157——交互 stdout 污染）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达（本复核独立确认 `grep render_body_into\|render_body_view tests/` 0 命中）——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。
2. **capability_fs._sandbox 的 `resolve(strict=False)` 在符号链接环时抛 RuntimeError 而非 PermissionError**（:25 仅捕获 ValueError）——模块无生产接线（F123），纯测试设施边界，未立 finding。
3. **cli_utils.emit_json 无 allow_nan=False**：NaN 流入时 stdout 输出 `NaN` 字面量（非严格 JSON）——F135 NaN 链的序列化环节，Python `json.loads` 默认可回读，未单独立 finding。
4. **load_applicability 表头要求 `len(cells) >= 4`**（scoring.py:88）：仅 2 个测试类型的 "Dimension scope" 表（3 cells）整体 no-op——本复核全量扫描 82 份 rubric **0 触发**（44 个已解析表均 ≥3 列），纯潜在格式风险（F115/F149 根因族）。
5. **load_rubric 对数值形非权重表（如 `\| 94 \| Excellent \| 100 \|`）会误解析为维度**——R9 备查 #5 已确认 82 份 rubric 全部正常解析、无触发文件，纯潜在格式风险。
6. **scoring.py:434-436 将 WARNING 类 validation_errors 以 log.error 输出**（R8 备查 #2 已记录，同意不重复）。
7. **shenbi-short-drafting 契约 writes 含 `chapters/chapter-N.md`**（短篇技能写 chapters/ 目录，疑似契约笔误）——属技能 frontmatter（非 Z1 代码），供合并层转交 skills 区。
8. **`python3 src/shenbi/scoring.py` 直跑失败**（sys.path[0]=src/shenbi 使 `shenbi/logging.py` 遮蔽 stdlib logging → structlog 循环导入 AttributeError）——环境路径语义，.venv 控制台脚本为权威入口，非代码缺陷。
9. **交互模式 kill-switch 提示 `input()` 同样污染 stdout**（scoring.py:388）——已含于 F157 修复范围。
10. **AGENTS.md "67 functional + 2 meta = 69" 与现状 74 个 skills 目录不符**（R7 备查 #7 已记录）：非 Z1 区，供合并层转交。

---

## 5. 其他说明

- 前十一轮"P1×2（F115/F140）"结论：本复核独立实跑/复跑后**无反证，维持**；误报数 0（53/53 成立）。
- 新发现 F156–F157 均不改变"无 P0"结论，也不新增 P1。F156 是 G2 校验链（F116/F120/F137/F148/F150）家族中最后一个未被覆盖的独立站点——"文件类型与文件集异构导致分支跳过"，与既有 finding 根因均不同；F157 是交互模式 stdout 通道契约的最后一个未覆盖点。
- 跨区一致性核查：F156 与 executor（dispatcher 区）的 run_g2 共享同一缺陷，站点在 Z1 的 phase_runner 与跨区的 executor/gates，本报告以 Z1 侧 phase_runner.py:196-213 为主证据并标注 executor/g2 协同站点；与 Z5 F500（scoring_bridge dead 链）无重叠；与 Z3 区 safe_write 消费方缺陷无重叠。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140/F126 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**实证退出码与 scored_by；F115/F134/F135/F141/F149/F152/F155 经 `PYTHONPATH=src` 纯内存导入复跑；F156 经 `gate_G2` 真实函数模拟（临时文件仅写 /tmp）；F157 经真实交互子进程捕获 stdout；临时文件仅写 /tmp，未触碰仓库文件。

---

## 6. 返回摘要

- **复核统计：新 finding 2（F156 P2、F157 M）/ 误报数 0 / 新增严重度异议 0（维持 F109 M→P2 共识；F115/F140 的 P1 无反证维持；F134 维持 P2）**
- **0 新 finding 判定：否**（2 条新漏报：F156 phase_runner/executor 双路径对混合 .md+decisions.json 输出施加单一 file_type="decisions" → G2 decisions 分支整体跳过 .md、文档化 G2.4-2.12 内容校验在管线路径静默不执行；F157 scoring 交互模式 input() 提示词污染 stdout 数据通道、stdout 不可 JSON 解析。均无 P0/P1 级新发现）
