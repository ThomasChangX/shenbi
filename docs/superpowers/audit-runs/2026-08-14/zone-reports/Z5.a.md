# Z5 分区初审报告（Z5.a）

- 分区: Z5 = `src/shenbi/audit/` + `src/shenbi/cost/` + `src/shenbi/orchestration/`
- 模式: **只读**（未创建/修改/删除任何仓库文件，除本段文件；未 git add/commit；未运行写仓库命令）
- 清单: `docs/superpowers/audit-runs/2026-08-14/zones/Z5.files`（13 文件）
- 深读: 13/13（100%）
- 时间: 2026-08-14（audit 会话）

---

## 摘要（findings 清单）

| 编号 | 标题 | 类别 | 严重度 | 证据行 |
|---|---|---|---|---|
| F500 | scoring_bridge 双评员一致性/塌缩检测 dead-wire：`validate_dual_scorer`/`check_single_scorer_collapse` 无任何生产调用方 | dead-wire/未接线 | **P2** | scoring_bridge.py:10,21；grep 全仓仅 tests/unit/orchestration/test_bridges.py 引用；specs/archive/2026-06-28-hierarchical-memory-scoring-system-design.md:512-535,858；plans/archive/2026-06-28-hierarchical-system-wave4-loop-approval.md:543 |
| F501 | escalation_bridge dead-wire：`parse_resonance_scores`/`run_escalation_check` 无生产调用方；resonance_trend.md 写而不读（写侧 chapter_loop.py 直连 `check_escalation`，读侧唯一真实消费者是 header 列解析的 compute_drift） | dead-wire/未接线 | **P2** | escalation_bridge.py:10,28；chapter_loop.py:1001,3006-3021；skill_utils/drift_detection/compute_drift.py:170-205,27 |
| F502 | `FileChange.status` 字符串 Literal（"added"/"deleted"/"modified"）定义在 contracts/ownership.py:22 而非 contracts/enums.py 单一词表（enums.py docstring 明示"所有 Literal 必须从此处 import"） | 枚举单一信源违反 | **P2** | contracts/enums.py:1-9；contracts/ownership.py:22；audit/snapshot.py:100-118 |
| F503 | `write_audit._declared_patterns` 宽泛 `except Exception` 吞错：`derive_output_files` 意外异常 → declared=[] → 所有非 OWNERSHIP 已 watch 文件全量"未声明写入"假阳性 → 静默 GATE_FAIL 阻断 | 错误处理 | **P2** | write_audit.py:25-26；executor.py:266-268 |
| F504 | `ledger.record()` 对 usage 值做裸 `int()` 强转，非 int 可强转值会 ValueError 崩 hot path，违反模块自述"must never crash the pipeline"契约；`_safe_estimate_cost` 只护 cost 计算 | 错误处理/边界 | **P2** | ledger.py:73-75,26-28,30-34 |
| F505 | `TokenLedger._write_lock` 每实例一把，dispatch_helper 每次 record 新建实例 → 锁对并发写无效（跨实例/跨进程 append 可交错，iter_records 以跳过损坏行兜底=丢计量） | 并发/优化 | **P2** | ledger.py:57；dispatch_helper.py:1333 |
| F506 | `report._try_avg_g3_score` 把任意 `**/*score*.json` 的任意顶层 0-100 数值当 G3 分求平均，`Cost per quality point` 指标名实不符（可能混入维度分/无关 int） | 正确性(展示指标) | **P2** | report.py:18-35,74-79 |
| F507 | 写所有权审计对"删除已声明输出文件"零拦截：`status="deleted"` 既不触发 check_write_ownership 的 record/field 维度，也不触发 file-level 检查 → 技能删除自身声明写入文件可静默过审（数据丢失路径） | 审计完整性 | **P2** | write_audit.py:48-64；ownership.py:101-136 |
| F508 | d1-06-coverage-gaps.txt 被 F-D1-02 污染：collect-only 覆写 coverage.xml（16.08%），Z5 区全部逐行 gap 条目失效；真实覆盖率以 d1-01 just-check（85.14%）为准 | 审计工件质量 | **P2**（跨区影响，报协调员） | d1-baseline.md:61-62；d1-11-collect-only-full.log 末行 16.08% FAIL；d1-01-just-check.log:318-321 |
| F509 | `compute_file_change` 对 pre==post（未变化/不存在文件 pre=None,post=None）报 `status="modified"`：checked_files 含幻影条目（不影响 violation 判定，语义瑕疵） | 语义/M | **M** | snapshot.py:103-104 |
| F510 | `report.main()` 尾部 `return 2` 不可达（subparsers required 且仅 "report"）；`print()` 出现在框架代码（CLI 入口上下文，AGENTS.md "No print()" 的边缘豁免待 Z1 定夺） | 死代码/M | **M** | report.py:90-97；d1-baseline.md:26 |
| F511 | archived spec 文档↔代码行号漂移（>±5）：write_audit.py:31 lazy import 实际在 :22；inference-control plan 中 ledger.py:62 调用 estimate_cost 实际在 :31（历史态描述，非现行） | 文档漂移 | **M** | specs/archive/2026-08-02-issue24-cyclic-import-refactor-design.md:183；plans/archive/2026-08-02-inference-control-audit-plan.md:962-964 |
| F512 | 写所有权审计（audit/ 包）在主 dispatch 路径被绕过：API 路径（`_dispatch_via_api`）与 IDE 路径不调 write-audit，仅 legacy subprocess 路径（shenbi-dispatch CLI）执行；dispatch_helper 模块 docstring 声称"reuses dispatch_with_write_audit"与 routing 不符 | 审计波调度接线缺口 | **P2** | dispatch_helper.py:3-4,1826-1841；dispatcher/cli.py:11；executor.py:228 |

---

## 逐文件报告

### src/shenbi/audit/__init__.py
- 处置: deep-read
- 声称检查的不变量: 导出的 6 个符号（AuditResult / audit_writes / compute_file_change / parametric_globs / record_audit_outcome / snapshot_tree）在对应模块真实定义且签名一致；无未定义导出。
- findings: 无
- 验证命令: read（18 行）+ grep 各符号定义点（audit/_shared.py:27,38 / write_audit.py:39 / snapshot.py:19,51,97 / record.py:23）——全部命中。
- 置信度: high

### src/shenbi/audit/_shared.py
- 处置: deep-read
- 声称检查的不变量:
  1. `AuditResult` frozen dataclass 4 字段与 record.py:29-35 消费一致（skill/violations/drift/checked_files）；
  2. `derive_output_files(skill, chapter, round_dir)` 语义：writes+updates 拼接、resolve_or_skip 解析 N/NNN、round_dir 时转绝对路径、ContractError → []；
  3. `load_contract` 返回 TypedDict 含 `writes`/`updates`（contracts/legacy.py:45-49）→ `c["writes"]`/`c["updates"]` 合法；
  4. `resolve_or_skip` 签名 (path, chapter) → str|None（contracts/paths.py:50-55）；
  5. 调用方签名一致：executor.py:196,223 / phase_runner.py:198 / write_audit.py:24。
- findings: 无（F503 的根因在 write_audit 的调用方吞错，非本文件）
- 验证命令: read（58 行）；grep `def derive_output_files` 调用点 4 处；read contracts/legacy.py:200-207（load_contract 抛 ContractError 当 SKILL.md 缺失）——分支 57-58 属该防御。
- 置信度: high

### src/shenbi/audit/record.py
- 处置: deep-read
- 声称检查的不变量:
  1. 账本路径 `round_dir/write-audit.jsonl`，append-only，UTF-8，父目录 mkdir；
  2. blocked = violations∨drift 非空；返回 True=可 ship；
  3. trace seam：TraceWriter(round_dir).append(actor,actor_role,action,target,skill,payload) 签名匹配（trace/writer.py:65-76，actor_role="GATE" ∈ ActorRole enums.py:9）；
  4. 异常捕获集 (OSError,ValueError,TypeError) 覆盖 trace 写失败，账本已先行写入 → 绝不静默丢审计结果；
  5. 调用方：仅 dispatcher/executor.py:266。
- findings: [F504 关联——本文件无直接缺陷；trace seam 意外异常类型（非三类的 TraceEvent 构造失败）会使 record_audit_outcome 在账本写入后崩溃，由 executor finally 传播；概率低，不单列]
- 验证命令: read（61 行）；read trace/writer.py:40-97（签名/异常面）；grep record_audit_outcome 调用点（executor.py:237-266 唯一）；read tests/unit/audit/test_record.py（3 测试真实，含 drift 阻断）。
- 置信度: high

### src/shenbi/audit/snapshot.py
- 处置: deep-read
- 声称检查的不变量:
  1. parametric_globs 非缓存（truth-files.yaml 跨轮可变的显式设计，注释 22-25）；
  2. `_expand_patterns`：registry 参数化 → glob 展开实际文件；含 `*` 的非参数化模式 → 直接 glob；exact → 原样加入（不存在则 None）；
  3. `snapshot_tree` pre/post 每次重展开 → dispatch 新写文件进 post → added；
  4. `compute_file_change` 4 分支：added/deleted/JSON field diff/markdown record diff；
  5. `_diff_records` 以 str(id) 键合并且容忍重复 id（后值覆盖）；
  6. `parse_records`/`parse_markdown_table`/`detect_cross_section_drift` 签名匹配（records/parser.py:48、records/drift.py:26,72）；
  7. `FileChange` 构造字段匹配（contracts/ownership.py:17-26）。
- findings: [F502, F509；另 43-45 非参数化 glob 分支现无可达契约写入（grep 确认 contracts 无 `*` 写入），属防御死分支]
- 验证命令: read（118 行）；read tests/unit/audit/test_snapshot.py（6 测试真实，覆盖参数化展开/JSON field/markdown record/added/deleted）；grep contracts 写入模式无通配；grep truth-files.yaml:48-67（参数化模式）。
- 置信度: high

### src/shenbi/audit/write_audit.py
- 处置: deep-read
- 声称检查的不变量:
  1. N1 粒度表分派：JSON→field、markdown truth→record、其余→file；
  2. OWNERSHIP 命中 → check_write_ownership；未命中 → file-level `_matches_declared`（exact + parametric glob fnmatch）；
  3. cross-section drift（pending_hooks.md YAML vs 派生表）判据 12 仅在 post 存在时检测；
  4. 调用链 executor.py:242-268：chapter=extract_chapter(prompt) → watch=derive_output_files(skill,chapter) → pre/post snapshot → audit_writes → record_audit_outcome；
  5. `_matches_declared` 参数化 glob 兜底使 chapter 解析路径与声明路径一致（`chapters/chapter-N.md` → fnmatch `chapters/chapter-*.md`）。
- findings: [F503, F507, F509；另 write_audit.py:22-24 lazy import 在顶层已 import _shared（:12）后冗余（archived cyclic-import spec 的 Phase 2 已把环回边改指 _shared，lazy 残留），F511 关联]
- 验证命令: read（70 行）；read tests/unit/audit/test_write_audit.py（7 测试真实，含越权 field/record_field/drift/未声明写入）；read executor.py:220-271（接线拓扑）；grep 调用方。
- 置信度: high

### src/shenbi/cost/__init__.py
- 处置: deep-read
- 声称检查的不变量: 包 docstring 存在；无导出。
- findings: 无
- 验证命令: read（1 行）。
- 置信度: high

### src/shenbi/cost/estimate.py
- 处置: deep-read
- 声称检查的不变量:
  1. `estimate_prompt_tokens` CJK 字符按 ~1.5 chars/token、其余 ~4 chars/token；空串 → 0；
  2. `warn_if_over_budget` 阈值 = limit×0.8 严格大于才告警；未知模型回落默认 limit 不崩；
  3. MODEL_CONTEXT_LIMITS 默认模型与 pricing.DEFAULT_PRICING_MODEL / dispatch_helper._DEFAULT_MODEL 一致（deepseek-v4-flash）；
  4. 调用方：dispatch_helper.py:1532-1534 已接线（真实调用，非 dead-wire）。
- findings: 无
- 验证命令: read（59 行）；read tests/unit/cost/test_estimate.py（7 测试真实）；grep warn_if_over_budget 调用点（dispatch_helper.py:1534）。
- 置信度: high

### src/shenbi/cost/ledger.py
- 处置: deep-read
- 声称检查的不变量:
  1. **TokenLedger 接线核验（Z5 重点）**：`TokenLedger(project_dir).record(...)` 在 dispatch_helper.py:1333 真实调用，PR #39（commit d4b4e83）修复历史 dead-wire；仅 API dispatch 路径（_dispatch_via_api 的 usage 非 None 时）记录——spec #4 明示"PR #39 已接 API 路径"，IDE/legacy 路径不计量为已知设计缺口（非少计 bug）；
  2. record 返回 TokenUsageRecord 且字段与 dataclass 一致；timestamp ISO8601 UTC；
  3. iter_records 跳过空行/坏 JSON/字段错配行，绝不崩；
  4. summarize 按 skill/chapter 聚合 + totals；
  5. `estimate_cost` 失败（未知模型 ValueError）经 `_safe_estimate_cost` → 0.0 + warning（spec §5.2 I3 fail-loud 在 pricing 层 + ledger 热路径兜底，test_pricing_fail_loud 钉死 fail-loud 侧）。
- findings: [F504, F505]
- 验证命令: read（141 行）；read tests/unit/cost/test_ledger.py（16 测试真实，含 corrupt line/malformed record/空 ledger/聚合）；read dispatch_helper.py:1290-1341（接线现场）；grep resolve_model/estimate_cost 签名；git log 确认 d4b4e83（PR #39 ledger wire-up）。
- 置信度: high

### src/shenbi/cost/pricing.py
- 处置: deep-read
- 声称检查的不变量:
  1. PRICING 键 = DEFAULT_PRICING_MODEL = dispatch_helper._DEFAULT_MODEL = "deepseek-v4-flash"（单一默认，注释 15-16 明示镜像约束）；
  2. resolve_model：显式参数 > env(SHENBI_LLM_MODEL) > default，与 dispatch 路径 env 名一致（dispatch_helper.py:63,67）；
  3. estimate_cost：未知模型 raise ValueError（fail-loud，spec §5.2 I3）；缺失 token 字段按 0；float 截断为 int；
  4. 调用方：ledger._safe_estimate_cost（唯一生产调用）。
- findings: 无
- 验证命令: read（60 行）；read tests/unit/cost/test_pricing.py（13 测试）+ tests/unit/test_pricing_fail_loud.py（fail-loud 2 测试）；grep _ENV_LLM_MODEL 一致（pricing.py:27 vs dispatch_helper.py:63）。
- 置信度: high

### src/shenbi/cost/report.py
- 处置: deep-read
- 声称检查的不变量:
  1. CLI `shenbi-cost report <project_dir>` 注册（pyproject.toml:64 `shenbi-cost = "shenbi.cost.report:main"`）；
  2. render_report：空账本 → "No token usage recorded"；非空 → total/per-skill 百分比（按 cost 降序）/per-chapter 均值/CPQ；
  3. main() 返回 0=成功、2=目录缺失；
  4. 百分比分母 total_cost 为 0 时防御 0.0（report.py:62）。
- findings: [F506, F510]
- 验证命令: read（97 行）；read tests/unit/cost/test_report.py（4 测试真实）；grep pyproject shenbi-cost 入口；grep d1-baseline.md:26（print() 点名）。
- 置信度: high

### src/shenbi/orchestration/__init__.py
- 处置: deep-read
- 声称检查的不变量: 包 docstring 存在；无导出。
- findings: 无
- 验证命令: read（1 行）。
- 置信度: high

### src/shenbi/orchestration/escalation_bridge.py
- 处置: deep-read
- 声称检查的不变量:
  1. `parse_resonance_scores`：`|` 起始行、`overall` 头行跳过、cells>=7、cells[6] float、val>0 过滤、ValueError/IndexError 跳过坏行；
  2. `run_escalation_check` 参数与 `check_escalation` 真实签名逐一匹配（skill_utils/escalation/check.py:53-64，含 window/slope_threshold 等默认参数不传）；
  3. 写入侧格式契约：chapter_loop.py:1324-1343 `_build_resonance_trend_row` 产 `| Ch{N} | - | - | - | - | - | {overall} |` → parse 得 7 cells、cells[6]=overall（**格式匹配成立**）；
  4. **接线核验（Z5 重点）**：生产运行时**不调用**本桥——chapter_loop.py:1001 直连 `check_escalation` 且输入来自 `_get_recent_resonance_scores`（读 audits/chapter-N-resonance.md，chapter_loop.py:1453-1466），**不读 resonance_trend.md**；trend 文件写侧（chapter_loop.py:3006-3021）的现存唯一读者是 compute_drift.parse_trend（header 列解析，compute_drift.py:170-205）——与 `_build_resonance_trend_row` 无 header 的 7 列行可能不兼容（跨区风险，报 Z6/协调员）。
- findings: [F501]
- 验证命令: read（45 行）；read tests/unit/orchestration/test_bridges.py（3 测试真实）；grep 全仓 `run_escalation_check|parse_resonance_scores` 生产调用方=0（仅 tests + archived plan/spec 引用）；read chapter_loop.py:967-1035（直连接线）、1324-1343（写侧）、1453-1466（读侧）、3000-3022（persist）；read compute_drift.py:170-205（真实读者）。
- 置信度: high

### src/shenbi/orchestration/scoring_bridge.py
- 处置: deep-read
- 声称检查的不变量:
  1. `validate_dual_scorer` 参数与 `check_scorer_agreement(scores_a, scores_b, threshold=5.0)` 匹配（scoring.py:227-251），返回 dict 扩展 `needs_arbitration`；
  2. `check_single_scorer_collapse` 与 `flag_score_collapse`（scoring.py:254-279）匹配；
  3. **接线核验（Z5 重点 / G3.4）**：G3.4 独立评分经 gates/g3.py:193-206（agent-id 隔离）+ gates/g3_independence.py（fail-closed）**已强制**——但**双评员一致性（spec §5.5 补丁2：章/弧/卷/书级"总是双评"，archive spec :512-535,858）与塌缩检测（补丁3）在运行时零执行**：check_scorer_agreement/flag_score_collapse 仅被本桥引用，本桥仅被测试引用；wave4 plan（archive 2026-06-28-hierarchical-system-wave4-loop-approval.md:543）的明确目标（"让 helpers 真正接入逐章循环"）未达成——helper 直连了 escalation 侧，scoring 侧未接入。
- findings: [F500]
- 验证命令: read（23 行）；grep 全仓 `check_scorer_agreement|flag_score_collapse|validate_dual_scorer|check_single_scorer_collapse` 生产调用方=0（tests/unit/test_scoring_anti_collapse.py 直接测 scoring.py helper，tests/unit/orchestration/test_bridges.py 测桥）；read gates/g3.py:193-206 + g3_independence.py（G3.4 独立强制）；read spec archive 补丁2/3 段落。
- 置信度: high

---

## 覆盖缺口处置（rubric 维度 8）

**先决说明（F508）**：`d1-06-coverage-gaps.txt` 由 collect-only 覆写后的 coverage.xml（16.08%，3034 collected 0 run，d1-11-collect-only-full.log 末行）生成，Z5 区逐行条目**全部失效**。真实覆盖率以 d1-01-just-check.log（85.14%，2814 passed）为准。以下处置按真实未覆盖行（从 d1-01 表提取）：

| 文件（真实覆盖率） | 真实未覆盖行 | 处置 |
|---|---|---|
| audit/__init__.py (100%) | 无 | — |
| audit/_shared.py (100%) | 无 | — |
| audit/record.py (91%) | 52-53（trace seam except 回退） | acceptable(防御性回退分支：触发需 TraceWriter 故障；账本已先行写入不丢结果；日志可查) |
| audit/snapshot.py (85%) | 40->39,43-45,66-67,69,92->87,118 | 43-45 → dead-code(非参数化 `*` 通配分支，contracts 现无此写入模式)；66-67,69 → acceptable(JSON 解析防御)；118 → acceptable(非 json/md 兜底，现 watch 面全为 .md/.json)；40->39,92->87 → acceptable(控制流部分分支) |
| audit/write_audit.py (78%) | 25-26,31-35,63->48 | 25-26 → **must-test**(宽 except 吞错→假阳性路径，F503，宜加断言"非 ContractError 异常不吞")；31-35 → acceptable(fnmatch 纯函数分支，参数化展开由 test_snapshot 间接覆盖)；63->48 → acceptable(循环控制流) |
| cost/estimate.py (100%) | 无 | — |
| cost/ledger.py (96%) | 32-34（_safe_estimate_cost 未知模型分支） | **must-test**(守卫的恰是 test_pricing_fail_loud 钉死的 fail-loud 热路径，monkeypatch 测试成本极低) |
| cost/pricing.py (100%) | 无 | — |
| cost/report.py (70%) | 25-32,35,69->74,76-77,97 | 25-35 → acceptable(best-effort G3 启发式，标注非致命；F506 关联)；69->74 → acceptable(per-chapter 分支部分覆盖)；76-77 → acceptable(可选 CPQ 指标)；97 → dead-code(main() 尾部不可达 return 2，F510) |
| orchestration/__init__.py (100%) | 无 | — |
| orchestration/escalation_bridge.py (79%) | 17->14,20->14,22-24,37-38 | 22-24 → acceptable(坏行防御解析)；37-38 → dead-code(run_escalation_check 生产零调用，F501) |
| orchestration/scoring_bridge.py (100% 行) | 无行缺口但生产 dead-wire | dead-code(仅测试引用，F500) |

---

## 统计

- deep-read 文件数: **13 / 13**（清单全覆盖）
- 未覆盖文件（清单内未深读）: **无（空）**
- 低置信度文件: **无**（所有文件的签名/接线均以 grep 调用方 + 读依赖模块验证；唯一不确定点——双评员/塌缩检测未接入是否属有意设计——已在 F500 中以 archived spec 文本证伪"有意"，故不降置信度）
- P0: 0 | P1: 0 | P2: 10 | M: 3（F509/F510/F511 为 M；F500-F508,F512 为 P2）

## 给协调员/复核 agent 的要点

1. **TokenLedger 接线结论**：已真实接线（dispatch_helper.py:1333，PR #39），非少计 bug；仅 API 路径计量为 spec 认可的已知缺口。**无 P0/P1**。
2. **scoring_bridge/escalation_bridge 双 dead-wire**（F500/F501）：G3.4 独立性已由 gates 强制，但"双评员一致性 + 塌缩检测"（archived spec §5.5 补丁2/3）运行时零执行、resonance_trend.md 写而不读——建议决策：接入或显式废弃并删档，避免"看似实现"持续误导。
3. **audit/ 写审计主路径绕过**（F512）：pipeline API/IDE dispatch 不触发写审计，仅 legacy CLI 路径触发——若支柱四 spec 要求 pipeline 覆盖则需升 P1。
4. **F508**（coverage-gaps 污染）影响所有 Z 区的维度 8 处置依据，已按真实覆盖率替代处置。
5. 跨区耦合提示：resonance_trend.md 写侧（chapter_loop，Z6 区）与读侧 compute_drift（Z6/skill_utils 区）的格式兼容性未在 Z5 验证范围内，建议跨区复核。
