# Z1 分区独立复核报告（agent b）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`zone-reports/Z1.a.md`（334 行，13 findings F101–F113）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（无一处依赖初审转述）
- 只读约束：仅 grep / read / python3 只读 AST 与代码路径模拟；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F114–F199（实际产出 F114–F122，共 9 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：9**（F114–F122；其中 P1×1、P2×8）
- **误报数：0**（初审 13 条全部成立；无一条被证伪）
- **严重度异议：1**（F109：M → 建议升 P2；另附 1 处初审事实性错误 + 1 处影响描述修正，均不改变严重度）
- **0 新 finding 判定：否**（初审结论"无 P0/P1"需修正——新增 F115 为 P1）

### 复核结论表（逐条判定）

| 编号 | 标题 | 初审严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立**（证据扩充） | `git ls-files -s` 三文件均 100644 vs `ls -la` 均 `-rw-------` 实证复现；且 blast radius 远超初审 3 个示例——仓库内 **85 个** 100644 索引文件工作树为 0600（含全部 sync_contracts 重写的 `skills/*/SKILL.md`，实测 `skills/shenbi-worldbuilding/SKILL.md` 600）；`git status` 不显示（git 不追踪 rw 位）→ CI `git diff --exit-code` 幂等检查看不见漂移 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `find docs -name registry.md -o -name schemas.md -o -name integrity.md` → 0 命中；docs/framework/gates.md、dispatcher.md 仅 H1 `# Gates`/`# Dispatcher`（无 #missing-markers/#timeouts 锚点）；`ls tests/build_registry.py` → No such file；`tests/lock-tool-hashes.sh` **存在**（ToolTamper 的 action 不破，仅其 doc_url 破——初审"2 条 action 断"指 build_registry 两次，准确） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立 AST 扫描 src/ 全部 raise：RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentTimeoutError/SubAgentProtocolError/RetryExhaustedError（5/22），NEVER RAISED 17 类，与初审一致；error_guidance/recovery 生产消费者仅测试（`grep` 证实） |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | 代码走查 :414-415 + :422-432；`validate_scores({}, dims)` 模拟输出 `False ['REJECT: scores is empty — no dimensions scored']`；test_scoring.py:598-613 恒带 scores.json（分支无测试触达，d1-01 亦记 :415 缺失） |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（影响描述修正） | g5.py:115 `if project_dir:` 对字符串 "None" 为真 → `Path("None")`；G5.4（:218-228）glob/字面量检查对 "None" 目录必然 no_matches/not_found → **误导性 FAIL**（初审结论对）；但 G5.3（:115-205）整个项目扫描块因目录不存在**静默 SKIP**（`c.append({"id":"G5.3","s":"SKIP"})`）而非 FAIL——对无 expected_outputs 的相位存在假 PASS 风险；严重度维持 P2 |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :356-362 唯一 `except Exception`（grep 证实）；subprocess 无 timeout（:343-355），超时/崩溃 → 门检静默跳过 |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | 仅 gates/cli.py:126-134 手动入口；phase_runner/pipeline 0 引用；docs 仅归档计划提及（plans/archive/2026-06-11-test-framework.md）；justfile 无对应 target；三 gate 单测存在（tests/unit/gates/test_g_{transition,dispatch,reconcile}.py） |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :103-105 mkstemp 在 try 外（若抛 OSError，:117-125 finally 不执行 → flock fd/锁文件泄漏）；:85-91 unlink 后立即 O_EXCL 重建的 FileExistsError 未捕获；test_safe_write 8 测试仅成功路径（71-91 行缺失，d1-01 确认） |
| F109 | verify_bijection 用 assert | M | **成立但严重度异议** | sync_contracts.py:121 assert 实证；python -O 下整守卫消失，docstring :110 "Raises on mismatch" 契约失效。**异议：M（文案）→ 建议 P2**——assert 守卫是运行时正确性守卫而非文案问题；该工具会**静默写入** deps.json（:199），守卫失效时产生静默数据漂移（虽然 CI 幂等检查兜底），符合 P2"优化机会/防御深度"而非 M"错别字/格式"。中置信度 |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证；test_exceptions.py:64 同款文案 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9 实证；gates/cli.py、dispatcher/cli.py、dispatcher/executor.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立**（补充） | `grep -rn "\.repo(" src/shenbi` 0 命中；**同族 `backup()`（paths.py:33-35）亦 0 调用方**，初审漏列；d1-01 :31 缺失行确认 |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info；d1-01 153-154 缺失行确认 |

**初审陈述事实错误 1 处（非 finding，需修正记录）**：Z1.a.md phase_runner 节（:130）声称 "TimeoutExpired⊂OSError 被捕获"——**实证错误**：`subprocess.TimeoutExpired.__mro__` = (TimeoutExpired, SubprocessError, Exception, BaseException, object)，`issubclass(TimeoutExpired, OSError) == False`。run_gate 的 `except (json.JSONDecodeError, ValueError, OSError)`（phase_runner.py:94）**捕获不到超时**，60s 门超时会让状态机直接 traceback 崩溃（→ 新 finding F114）。

---

## 1. 新发现（F114–F122）

F114 | phase_runner.run_gate 不捕获 subprocess.TimeoutExpired：60s 门超时 → 状态机 traceback 崩溃而非 FAIL 降级 | error | P2
- 证据: src/shenbi/phase_runner.py:87-94（`subprocess.run(..., timeout=60)`，`except (json.JSONDecodeError, ValueError, OSError)`）；scoring.py:304,343（G3/gate-only 的 subprocess.run **连 timeout 都没有**）
- 根因: TimeoutExpired 继承链为 SubprocessError→Exception，不属于 OSError；except 元组漏列；初审基于错误假设（"TimeoutExpired⊂OSError"）将该路径判为已覆盖
- 验证命令+输出: `python3 -c "import subprocess; print(subprocess.TimeoutExpired.__mro__); print(issubclass(subprocess.TimeoutExpired, OSError))"` → `(...SubprocessError, Exception...)` / `False`
- 建议方向: except 元组补 `subprocess.TimeoutExpired`（或改 `except (json.JSONDecodeError, ValueError, OSError, subprocess.TimeoutExpired)`）；scoring 两个 subprocess.run 补 timeout；补一个"gate 超时 → FAIL 字典"单测

F115 | scoring 维度过滤对 38/82 实际 rubric 静默 no-op：worldbuilding 等 4 技能 bug-hunt/clean 评分含 N/A 豁免维度 | error | P1
- 证据: src/shenbi/scoring.py:81（仅识别 `## Dimension Applicability` 前缀）+ :88（头行 `cells[0] == "Dimension scope"`）+ :115-117（`if not applicability: return dimensions` 静默原样返回）；tests/tiers/t1-skill/shenbi-worldbuilding/rubric.md:43（"| 4 | Prose quality | N/A — exempted … scoring.py renormalizes weights for remaining applicable dimensions |"）；82 个 rubric 中仅 44 个含 "Dimension scope" 表，**38 个**（全部 10 个 t2-phase + 3 个 t3-pipeline + 25 个 t1-skill）为 `| # | Dimension |` 格式或无适用性表 → 解析器对它们返回 `{}`
- 根因: rubric 格式漂移——真实 rubric 用 `| # | Dimension | Bug-hunt Standard | Clean Standard |` 表（或根本没有适用性表），解析器只认模板的 "Dimension scope" 头行；`filter_dimensions_by_test_type` 对空 applicability 静默返回原维度，rubric 文本宣称的 "renormalizes weights" 对这些技能不成立；test_scoring.py 的 applicability fixture（:65-90, :234）只覆盖模板格式，从未用真实 rubric 验证
- 验证命令+输出: 模拟 `load_applicability`：worldbuilding/architecture/long-form → `EMPTY({})`，chapter-drafting → `['Bug-hunt','Clean','Generative']`；模拟 `filter_dimensions_by_test_type`（worldbuilding, bug-hunt/clean/generative）→ 均返回全部 9 维度（含豁免的 dim 4，权重 10%）
- 建议方向: 解析器兼容 `| # | Dimension |` 表并支持 "No"/"N/A" 豁免语义；或为 4 个 N/A 豁免技能（worldbuilding/faction-builder/location-builder/story-architecture）补 "Dimension scope" 表；把 ≥1 个真实 rubric 纳入 filter 测试矩阵。（严重度 P1 依据：文档化路径上可复现的错误评分、rubric 明示意图被静默违背；若认为影响面仅 4 技能×1 维度，可降 P2——留待合并层裁量）

F116 | phase_runner cmd_post_skill 对 G2 FAIL 不阻断（仅记录），与 dispatcher/executor G2 FAIL→return 1 及 command-to-give "G2 失败 = 输出不合格" 冲突 | error | P2
- 证据: src/shenbi/phase_runner.py:211-239（g2_status 记录进 step，仅 `if g4_status == "FAIL"` 阻断 :228-230；G2 FAIL 相位照常 OK）；src/shenbi/dispatcher/executor.py:200-204（同路径 G2 FAIL → `return 1`）；command-to-give.md:10（"G2 失败 = 输出不合格"）、:48（"只在 G2 和 G4 都通过后…评分"）
- 根因: 相位机与 executor 双入口门禁语义不一致；相位机路径 G2 结果只进 pipeline-manifest（best-effort）与 state.steps，无任何下游消费（grep `"g2"` 除 phase_runner 外 0 命中）
- 验证命令+输出: 代码走查 :211-239 vs executor.py:200-204；`grep -rn '"g2"\|g2:' src/shenbi --include="*.py" | grep -v phase_runner` → 0 命中
- 建议方向: 与 executor 对齐——G2 FAIL 时 emit_json BLOCKED + exit 1（或在文档显式声明 T2/T3 相位机 G2 为 informational 并解释与 executor/协议差异）

F117 | sync_contracts.load_all_contracts 静默跳过 ContractError 技能 → expected_outputs/DAG/index 静默缺失，verify_bijection 同源盲区无法发现 | error | P2
- 证据: src/shenbi/sync_contracts.py:40-41（`except ContractError: continue` 无日志）；:86-92（derive 用 `contracts.get(skill, {})`——被跳技能产出为空）；:196-200（verify_bijection 只对**同一 contracts dict** 内成员校验 → 被跳技能两处同缺，断言恒过）
- 根因: 单一技能的契约解析失败被当作"无契约"静默跳过；main() 仅在**全部**失败时 bail（:167-172），单技能失败无任何告警；DAG/index/expected_outputs 三者同步缺失且 CI 幂等检查（确定性重生成）无法暴露
- 验证命令+输出: 代码走查 :37-48/:86-92/:196-200；`grep -n "except ContractError" src/shenbi/sync_contracts.py` → :40（无 log）、:18（import 处）
- 建议方向: 单技能 ContractError 至少 `log.warning("contract_load_failed", skill=..., exc_info=True)`；考虑 fail-loud（一个坏契约 = 生成中止）以匹配 main() 的 bail 语义

F118 | scoring `--phase` 参数被解析但从未使用（死参数）；`--tier` 仅 T1 分支有实际逻辑 | optimization | P2
- 证据: src/shenbi/scoring.py:321（`_phase = None`）、:330-331（`if arg == "--phase" ...: _phase = sys.argv[i+1]`）——`_phase` 全文无第二次读取（grep 证实）；:334-362 的 tier 块仅 `if tier == "T1" and test_type` 有 G3 逻辑，`--tier T2/T3` 进入块后无任何行为；usage :289 宣称 `--tier T1|T2|T3 --phase <name>: enable gate checks before scoring`
- 根因: CLI 契约与实现半脱节——相位来自 rubric 路径（check_gate_markers :204-222 从 rubric_p.parts 解析），`--phase` 是无人消费的残留参数
- 验证命令+输出: `grep -n "_phase" src/shenbi/scoring.py` → 仅 :321,:331 两处赋值；`grep -n "\-\-phase" tests/unit/test_scoring.py` → 0 命中（无测试触达）
- 建议方向: 删除 `--phase` 或接入 check_gate_markers 的 phase 选择；文档明确 `--tier T2/T3` 不触发额外 gate

F119 | safe_write stale-takeover 固定 1 秒退避后无条件 unlink，可破坏活跃锁互斥；flock 失败路径 fd 泄漏 | error | P2
- 证据: src/shenbi/safe_write.py:75-91（10×0.1s 退避后 `os.unlink(lockfile)` + O_EXCL 重建——锁内无 pid/heartbeat/时间戳，1s 阈值内**活跃长写者**（大文件 write+fsync+replace+fsync_dir 可超 1s）的锁会被当作 stale 抢走 → 两个写者并发 replace 同一目标）；:54-62（flock 抛 OSError 时 :58 已开的 fd 未 close 即落回 O_EXCL 分支 → fd 泄漏）
- 根因: 陈旧性判定无证据（只凭固定 1s 超时），且 unlink+recreate 非原子；与 F108 同区域但根因不同（F108=竞态窗口未捕获，本条=互斥本身可被破坏）
- 验证命令+输出: 代码走查 :75-91/:54-62；`grep -rn "os.unlink(str(lockfile))" src/shenbi/safe_write.py` → :86（无条件执行，仅 FileNotFoundError 豁免）
- 建议方向: 锁文件写入 pid+时间戳，takeover 前校验陈旧（如持锁进程不存在或锁龄 > N 秒）；flock 异常分支先 close fd；补并发长写者测试

F120 | scoring check_gate_markers 的 G4/G6 标记名用 test_type 后缀，gates/cli.py 只写 "-generative" 标记（G4 bug-hunt/clean 分支不写标记）→ 非 generative 评分 + --round-dir 必误报 MARKER_MISSING | error | P2
- 证据: src/shenbi/scoring.py:200（t1 期待 `G4-{skill}-{test_type}.json`）、:220（t3 期待 `G6-{pipeline}-{test_type}.json`）；src/shenbi/gates/cli.py:110-121（G4 bug-hunt/clean 分支**不写任何标记**，仅 generative 分支写 `write_gate_marker("G4", full_name, "generative", ...)`）、:128（G6 恒写 `"generative"`）
- 根因: 标记写侧固定 generative 后缀，读侧（scoring）按 test_type 拼接——两套命名未对齐；T1 bug-hunt/clean 与 T3 非 generative 场景下标记**永远无法产生**，`--round-dir` 评分必然 exit 3
- 验证命令+输出: `grep -rn "write_gate_marker(" src/shenbi --include="*.py"` → 仅 cli.py:121（G4 generative）与 :128（G6 generative）；scoring.py:200/:220 读侧对照
- 建议方向: 统一命名——读侧改 `-generative`（与写侧一致）或写侧按 test_type 写；G4 bug-hunt/clean 分支补标记写入；补一个带 --round-dir 的非 generative 评分测试

F121 | phase_runner cmd_post_skill rglob 回退 `[:20]` 静默截断 + 不过滤非输出 .md | error | P2
- 证据: src/shenbi/phase_runner.py:208-210（`output_files = [str(f) for f in proj.rglob("*.md") if f.stat().st_size > 0][:20]`）——第 21 个起的 .md（如第 21+ 章）被静默排除在 G2/G4 校验外；rglob 顺序 OS 相关（任意）；且未排除 round-dir 内/非输出 .md
- 根因: 防失控上限写死 20，无告警；回退路径（非 pipeline T2、chapter=None、无契约产出）下的校验覆盖被静默截断
- 验证命令+输出: `sed -n '203,218p' src/shenbi/phase_runner.py`；d1-01 显示 :208-210 有测试覆盖（测试仅验证小文件集，未断言截断）
- 建议方向: 截断时 `log.warning("output_cap_truncated", cap=20, total=N)`；上限提为可配置；过滤 `round_dir` 与已知非输出目录

F122 | phase_runner cmd_pre_score 不追加 step，其余 5 个命令均记录 → phase-state 审计历史不完整 | error | P2
- 证据: src/shenbi/phase_runner.py:242-275（pre-score 置 SKILLS_DONE + save_state + emit，**无 `state["steps"].append(...)`**）；对照 :118-122（start）、:155-165（pre-skill）、:219-227（post-skill）、:293-300（post-score）、:308-332（finalize）均记录 step
- 根因: 实现遗漏；pre-score 是唯一不留下审计轨迹的命令，state.steps 序列出现"started → skills_done"断层
- 验证命令+输出: 代码走查 :242-275 与其余命令对比；`grep -n '"action": "pre-score"' src/shenbi/phase_runner.py` → 0 命中
- 建议方向: 与其余命令一致追加 `{"action": "pre-score", "timestamp": now_iso(), "missing_markers": ..., "missing_outputs": ...}` 后 save_state

---

## 2. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读，无一处依赖初审转述）
- 与初审一致判定无发现的文件：`capability_fs.py`（权限模型核实无洞：resolve 后 relative_to 拒 symlink 逃逸、写侧 4 方法全拒、无 TOCTOU 利用面）、`cli_utils.py`、`logging.py`、`status.py`（wire 值/单一定义核实）、`py.typed`、`__init__.py`（除 F111）
- 新发现集中区：`phase_runner.py`（F114/F116/F121/F122）、`scoring.py`（F115/F118/F120）、`sync_contracts.py`（F117）、`safe_write.py`（F119）
- 低价值未立 finding 项（记录备查）：① logging.py `SHENBI_LOG_FORMAT` 非 "json" 值（如 "JSON"）静默降级 console，无校验（M 级，可并入后续）；② exceptions.py `ShenbiError.to_dict` 无生产调用方（F103 同族 dead-code，并入 F103 注记）；③ paths.py `backup()` 0 调用方（并入 F112 注记）；④ capability_fs 缺 open()/touch()/rename() 等 Path 方法 → AttributeError 而非 PermissionError（文档化有限句柄，非缺陷）

## 3. 其他说明

- 初审 d1-01 覆盖表已对归档日志逐行核对：phase_runner 92%（缺 74-75,153-154,266-269,371-372,374-375,377-378,380-381）、safe_write 66%（缺 30-31,34-35,71-91,124-125,139-140）、scoring 97%（缺 331,362,391-393,415）、sync_contracts 56%（缺 128-134,144-155,159-160,165-203）——全部一致。
- F115 的 P1 判定使初审"无 P0/P1 级发现"的总体结论不再成立，建议合并层在最终报告中修订。
- 环境备注：本会话 ambient python（3.12）加载 pydantic_core 时与 typing_extensions 冲突，无法直接 `uv run` 全量 CLI 实测；F104/F115/F105 均以代码路径模拟 + 单元级证据完成验证（validate_scores 空 dict 输出、load_applicability 对真实 rubric 输出、filter 对真实 rubric 输出），结论不受影响。
