# Z5 分区第六轮独立复核报告（Z5.review6）

- 复核对象: `Z5.a.md`（F500-F512）+ `Z5.review.md`（F513-F516）+ `Z5.review2.md`（F517-F524）+ `Z5.review3.md`（F525-F530）+ `Z5.review4.md`（F531-F536）+ `Z5.review5.md`（F537-F538）
- 复核方式: **fresh-context 全量重读** — 重读 Z5 区全部 13 个清单文件；对 F500-F538 每条独立 grep/read/empirical-verify（不采信任何先前结论）；对四条 P1 裁决（F507/F512/F513/F524）逐一构造反证尝试；对任务重点区（每文件剩余未覆盖分支最后一遍扫描、Z3/Z6 交叉消费最后一致性、d1-06 覆盖率工件现行状态）逐项实证
- 深读: **13/13**（见「覆盖空洞声明」）
- 模式: 只读（未创建/修改/删除任何仓库文件，仅写入本报告文件；未 git add/commit）
- 时间: 2026-08-14（第六轮复核会话）

---

## 一、复核结论表（F500-F538 逐条判定）

本轮对 38 条 findings 逐一独立验证（实证运行 16 项 + 全链代码审读）。**38 条全部成立，0 条误报；4 条 P1 全部成立，反证尝试全部失败。** 逐条判定如下：

| 编号 | 前五轮判定 | 本轮判定 | 理由（本轮独立证据） |
|---|---|---|---|
| F500 | P2 dead-wire | **成立** | grep 全仓 src/：`validate_dual_scorer`/`check_single_scorer_collapse` 生产调用方=0（仅 scoring_bridge.py 自定义 + tests）；`check_scorer_agreement`/`flag_score_collapse`（scoring.py:227,254）在 src/ 内仅被本桥引用（scoring_bridge.py:7,14,23）。本轮复核 scoring.py:227-251 返回 dict 含 "agreed" 键 → `validate_dual_scorer` 的 `needs_arbitration` 扩展可正常工作，但无任何生产调用方。G3.4（gates/g3.py）只强制 agent-id 隔离。archived spec 补丁2/3 运行时零执行成立。 |
| F501 | P2 dead-wire（含 2 处描述修正） | **成立（修正成立）** | grep 全仓 src/：`run_escalation_check`/`parse_resonance_scores` 生产调用方=0（仅 chapter_loop.py:1327,1341 注释引用 + escalation_bridge 自引用）。chapter_loop.py:1001 直连 `check_escalation`（输入 `_get_recent_resonance_scores` :1453-1466 读 audits/chapter-N-resonance.md，**不读 resonance_trend.md**）。修正 (a) 复核成立：escalation-review SKILL.md `reads: truth/resonance_trend.md` → 趋势文件被 LLM prompt 消费（dispatch_helper OPTIONAL_READS 类机制），非"写而不读"；(b) compute_drift 仅 `__main__`，src/ 内无生产调用 → 真正 dead 的是解析桥。F522 的格式不兼容本轮实证复现（parse_trend 全空、parse_resonance_scores 正常，见 F522 行）。 |
| F502 | P2 枚举单一信源违反 | **成立** | contracts/enums.py:1 docstring"所有 Literal 必须从此处 import"；FileChange.status Literal（ownership.py:22）与 FileOwnership.level（:31）不在 enums（enums 词表仅 Severity/Verdict/CPZone/ActorRole）。字面规则违反成立（意图范围存疑已在复核2 注明，维持 P2）。 |
| F503 | P2 宽 except 吞错 | **成立（机制细节维持复核1修正）** | write_audit.py:25-26 `except Exception: return []` 确认。非 ContractError 面：legacy.py:105（REGISTRY_PATH read_text）与 :131（SKILL.md read_text）的 OSError/UnicodeDecodeError 未包 ContractError → 可达 `_declared_patterns` → declared=[] → 非 OWNERSHIP 已 watch 文件全量假阳性。executor `_audit_watch_paths`（:220-225）只捕 ContractError → 吞错面确在 write_audit 侧。 |
| F504 | P2 int() 强转 | **成立（防御性）** | ledger.py:73-75 裸 int()：`int(None)`→TypeError、`int('abc')`→ValueError、`int(1.5)`→1 静默截断。接线处（dispatch_helper.py:1337-1340）值均 SDK int → 防御性风险成立。`_safe_estimate_cost`（:23-34）只捕 ValueError——int(None) 的 TypeError 在 pricing.py:58-59 同样不被捕，但 record() 自身 int() 先行（:73-75）已崩，该 TypeError 面与 F504 合并（非独立缺陷）。 |
| F505 | P2 实例级锁无效 | **成立** | ledger.py:57 `self._write_lock` 每实例一把；dispatch_helper.py:1333 每次调用新建 TokenLedger → 锁零保护。对比佐证：truth_io.py:54-70 的模块级 `_PATH_LOCKS` 注册表（path-keyed singleton lock）恰是正确模式。同进程多线程（parallel_dispatch）与跨进程 append 可交错 → iter_records 跳坏行=丢计量。P2（并发/优化）恰当。 |
| F506 | P2 展示指标名实不符 | **成立** | report.py:22 glob `**/*score*.json`；scoring.py:286 明示 scores.json 为维度 dict。`_try_avg_g3_score`（report.py:18-35）把任意顶层 0-100 数值当 G3 分平均（bool 是 int 子类、{"total":85,"version":1} 混入）。CPQ 分母失真。纯展示不崩，P2 恰当。 |
| F507 | P2→P1（S1 已裁决） | **成立（同意 P1，无反证）** | **反证尝试失败**：grep src/ 无 status=="deleted" 特判；实证 `compute_file_change('truth/pending_hooks.md', '## hooks...', None)` → deleted、detail 全空 → `check_write_ownership('shenbi-foreshadowing-track', …)` → `[]`（violations 空）。OWNERSHIP 命中跳过 file-level（write_audit.py:62）→ 删除 OWNERSHIP 文件零拦截。**补充评估**：非 pipeline 模式下 G2 会检查 output_files 存在性（executor.py:200-204），可部分兜底文件缺失；但 pipeline 模式显式跳过 G2（:205-206，"output validated by G4/G6"），且 G2 不校验删除动作的写权语义——审计对自身 status 词表（deleted）零逻辑仍成立。数据丢失路径门禁静默放行，P1 裁决无反证。 |
| F508 | P2 覆盖污染（历史时点） | **成立（历史时点；F514 已 resolved）** | 本轮复核 on-disk：tests/coverage/coverage.xml（01:55 mtime）line-rate=0.08389（8.39%），cost/* 与 orchestration/* 全 0%——共享 coverage.xml **仍持续被覆写**（D1 跨区流程问题，非 Z5 文件缺陷，不单列）；d1-06-coverage-gaps.txt 无 cost/ 条目（01:23 真实版）。初审核心观察（collect-only 16.08% 覆写）成立。 |
| F509 | M 幻影条目 | **成立** | 实证 `compute_file_change('x.json', None, None)` → `FileChange(status='modified')`——pre==post==None（exact 模式文件不存在）→ "modified" 幻影。checked_files 含幻影；F524 场景下幻影进一步触发"未声明写入"违规（影响面已在 F524 立案）。M 恰当。 |
| F510 | M return 2 不可达 | **成立** | report.py:86 `add_subparsers(dest="cmd", required=True)` 仅注册 "report"；缺子命令 argparse SystemExit(2)；cmd 恒 "report" → :97 尾部 `return 2` 不可达（注：:93 目录缺失的 return 2 可达，F510 专指尾部不可达，表述精确）。print()（:93,95）在 CLI 入口上下文，AGENTS.md 豁免边缘成立。M 恰当。 |
| F511 | M 归档漂移 | **成立** | specs/archive/2026-08-02-issue24:183 称 write_audit.py:31 lazy import（实际 :22）；plans/archive/2026-08-02-inference-control:962-964 称 ledger.py:62 estimate_cost（实际 :31）。write_audit.py:22 lazy import 在顶层已 import _shared（:12）后冗余成立（复核4 补充：plan 有 bootstrap 失败隔离的既定理由，与"冗余"论断并存）。M 恰当。 |
| F512 | P2→P1（S2 已裁决） | **成立（同意 P1，无反证）** | **反证尝试失败**：dispatch_helper.py:1826-1841 路由实证——`SHENBI_LLM_API_KEY` 设置 → `_dispatch_via_api`（:1827-1830，零 audit 调用）；`_find_ide_cli()` 可用 → `_dispatch_via_ide`（:1833-1841，零 audit）；仅 legacy（:1864 `shenbi-dispatch` → cli.py:11 → `dispatch_with_write_audit`）执行写审计。模块 docstring（:3-4 "Reuses the existing dispatch_with_write_audit"）与 routing 矛盾。**本轮复核计划原文**：pillar4-tierB.md:7,17 明示"写侧用 FS 快照 diff，对所有 dispatch 模式可行（含 codex 子进程）"——设计意图为全模式覆盖 → "正常路径可复现的功能缺失"成立。P1 无反证。 |
| F513 | P1 快照根错位 | **成立（维持 P1，无反证）** | **反证尝试失败**：executor.py:25-26 `REPO_ROOT=Path(__file__).resolve().parents[3]`、`PROJECT_DIR=REPO_ROOT`（实证打印 `/Users/xiaotiac/Documents/GitHub/shenbi`）；:244/:264 `snapshot_tree(PROJECT_DIR, watch)`——快照框架仓库根而非 round_dir；codex 模式 `-C round_dir`（modes/codex.py:62）技能写 round_dir → 比对错树。测试掩蔽实证：test_executor_audit.py:17,36 `monkeypatch.setattr(ex,"PROJECT_DIR",tmp_path)` + 文件写 tmp_path → 测试成立恰因掩蔽。P0 论证（审计静默产出 blocked=False）维持 P1 判定理由成立：审计为增量防护、不直接损坏数据、当前仅 legacy 路径执行。 |
| F514 | P2（已 resolved） | **成立（维持 resolved）** | 本轮复核 on-disk：d1-06-coverage-gaps.txt 无 cost/estimate.py 条目（与 100% 真实覆盖一致）、无 cost/ 命中；共享 tests/coverage/coverage.xml（8.39%）污染为 D1 流程残余（F508 行）。非误报，属处置更新。 |
| F515 | P2 glob 不匹配 | **成立** | 实证：`_declared_patterns('shenbi-truth-sync')==['truth/*.md']`；`_matches_declared('truth/pending_hooks.md', ['truth/*.md'], globs) → False`（globs.get('truth/*.md')→None，原生 glob 从不 fnmatch 展开）；与 `_expand_patterns`（snapshot.py:42-45 能展开 `*`）不对称。glob 写技能除 truth-sync/sequel-writing 外还有 worldbuilding/snapshot-manage 等。 |
| F516 | M 初审 dead-code 误判 | **成立** | `_expand_patterns` 的 `"*" in pat` 分支实证可达（`derive_output_files('shenbi-truth-sync')→['truth/*.md']` 展开为实际 truth 文件）。初审"contracts 无 `*` 写入"断言错误成立。 |
| F517 | P2 drift 外溢+崩 | **成立** | write_audit.py:52-56 对每个 watched .md 执行 parse_records/parse_markdown_table/detect_cross_section_drift——判据 12 本只针对 pending_hooks.md（records/drift.py:1-3 docstring）。场景 A（非 hooks md 含 `## 活跃伏笔` 表）假 drift、场景 B（`## hooks` 非列表）ValueError 无 except 兜底在 executor.py:263-268 finally 传播。当前被 F513 空树掩盖（post_content None 不触发）。 |
| F518 | P2 added 零拦截 | **成立** | 实证：`compute_file_change('genre-config.json', None, '{"title":"x","version":1}')` → added、detail 全空 → `check_write_ownership('shenbi-genre-config', …)` → `[]`。首次创建 OWNERSHIP 文件携带未授权键零拦截。F507 的 added 孪生。 |
| F519 | P2 账本写侧无防御 | **成立** | ledger.py:56 mkdir、:78-79 open/write 裸 IO；模块契约（:26-28 "must never crash the pipeline"）只护 cost 侧。接线点 `_dispatch_via_api` :1562/:1618 在 API 成功后、`_write_parsed_outputs`（:1649）落盘前 → OSError 传播丢 LLM 输出。与 F504（int 转换侧）独立。 |
| F520 | P2 重试成本少计 | **成立** | @retry(stop_after_attempt(3), retry_if_exception(_is_retryable))（dispatch_helper.py:1441-1451，429/5xx/timeout）；usage 仅成功返回后记录（:1561-1562/:1617-1618），失败 attempt 的 usage 无落账路径（before_sleep 回调 :1445-1449 只 log 不记账）。 |
| F521 | P2 CJK 低估 | **成立** | 实证 `estimate_prompt_tokens('你好，世界！这是一个测试。')`=7（chars=13, cjk=10, other=3；标点按 CJK 计应为 8，低估 ~12%）。`_CJK_START/_END=0x4E00/0x9FFF`（estimate.py:24-25）不含 0x3000-0x303F 标点/0xFF00-0xFFEF 全角/0x3400-0x4DBF 扩展 A。MODEL_CONTEXT_LIMITS 单条目回落已在其影响段立案。 |
| F522 | P2 格式不兼容 | **成立** | 本轮实证：`_build_resonance_trend_row`（chapter_loop.py:1324-1343）产 `| Ch{N} | - | - | - | - | - | {overall} |`（无 header）；`_upsert_markdown_table_row`（truth_io.py:201-224）只做 key 去重追加、从不写 header；实证 `parse_trend` 对写侧格式返回 `{'overall': [], …}` 全空、`parse_resonance_scores` 正常解析 `[85.0, 90.0]`。读侧 CLI-only → 实际影响限手工运行，P2 恰当。 |
| F523 | P2 id-less 掩盖 | **成立（防御性）** | snapshot.py:82-83 `str(r.get("id"))` — 缺 id 记录坍缩为 "None" 键。parser.py:45 只过滤非 dict 不强制 id。实证 pre 2 无 id → post 1 无 id → new/del/mod 全空。 |
| F524 | P2→P1（复核3 S1 已裁决） | **成立（同意 P1，无反证）** | **反证尝试失败**：实证 `_declared_patterns('shenbi-chapter-drafting')==[]`（chapter=None 时 chapter-N.md 被 resolve_or_skip genesis 过滤）；空目录 `snapshot_tree(['chapters/chapter-1.md','chapters/chapter-1-decisions.json'])` → 幻影键 → `audit_writes('shenbi-chapter-drafting', …)` → `violations=('未声明写入…×2') blocked=True`——**当前已活体触发**（复核3 修正方向正确，本轮复现）。凡 prompt 含 "chapter N" 的 chapter-parametric 技能走 legacy 审计路径即确定性 GATE_FAIL（justfile:66-67 `just dispatch … /tmp/round` 即此路径）。P1（正常路径可复现功能错误）无反证。 |
| F525 | P2 summarize TypeError | **成立（防御性）** | 实证：账本行 `{"prompt_tokens":"abc",…}` → `summarize` CRASH `TypeError: unsupported operand type(s) for +=: 'int' and 'str'`。`TokenUsageRecord(**data)` 构造不校验值类型；模块契约（:3-4,83）读侧容错承诺被违反。生产写侧恒写 int → 需外部/手改账本触发，防御性。 |
| F526 | P2 dict→list 零违规 | **成立** | 实证 `_changed_top_keys('{"approval":"ok"}', '[1,2,3]') → ()`；`check_write_ownership('shenbi-genre-config', …)` → `violations=[]`。非 dict 载荷静默返回 () → field 级审计失明。 |
| F527 | P2 record_create 新记录键零校验 | **成立** | ownership.py:120-124 只查 deleted_record_ids/modified_record_keys；new_record_ids 全量放行；`_HOOK_KEYS_NEW_RECORD`（:52-71，16 键）grep 全仓仅 :52 定义 + :83 赋值（作为 write_keys 传入），record_create 分支逻辑**从不读取** write_keys → 死配置。`_diff_records`（snapshot.py:82-94）只返回 new_ids，结构上无法携带新记录键。 |
| F528 | P2 审计记录写侧无防御 | **成立** | record.py:36-39 mkdir/open/write 裸 IO；executor.py:263-268 finally 中传播 → 替换 dispatch rc（:269-270 永不执行）。与 F519 同族独立文件。 |
| F529 | P2 drift 单向 | **成立** | 实证：YAML 含 h1+h2、md 表仅 h1 → `detect_cross_section_drift` issues 仅含 h1 的值不匹配项，**h2（YAML-only）零报告**。drift.py:80-93 只遍历 md_rows。消费链 write_audit.py:52-56 → record.py:24 blocked。 |
| F530 | M 0 分过滤 | **成立** | escalation_bridge.py:19-21 `val > 0` 丢弃合法 0 分（实证 `parse_resonance_scores` 对 0 分行返回 [85.0]）；桥为 dead-wire → 无现行生产影响，M 恰当。 |
| F531 | P2 state 闸门零落账 | **成立** | dispatch_helper.py:1297-1298 `if state: _record_token_usage(...)`；实证 `_log_token_usage(usage, skill, state=None, project_dir=td)` → 有 project_dir 也不写账本（ledger 文件不存在）。state-less 调用方实证：genesis.py:302、closure.py:253、triggers.py:561、parallel_dispatch.py:86、audit_layer.py:170、revision_router.py:160 全部不传 state（grep 确认）；仅 chapter_loop.py:1246/2794/2966 传 state。docstring（:1307-1312 "appends a record when project_dir is available"）与实际代码矛盾。 |
| F532 | P2 chapter 恒 0 | **成立** | 实证：`hasattr(PipelineState(),'chapter')==False`、`getattr(...,0)==0`；`_record_token_usage(PipelineState(), …)` 后账本记录 `chapter=0`（实证 JSON 行 chapter=0）。state.py:161-179 PipelineState 无顶层 chapter（真实章节号在 `chapter_loop.current_chapter`，:142）。summarize by_chapter 全坍缩 "0" 桶。 |
| F533 | P2 iter_records UTF-8 | **成立** | 实证：账本含 `b'\xff\xfe'` 字节 → `summarize` CRASH UnicodeDecodeError。ledger.py:86 `read_text(encoding="utf-8")` 整文件解码无 try。与 F525（值类型错）独立故障类（字节编码 vs 聚合 `+=`）。 |
| F534 | P2 broken JSON 零违规 | **成立** | 实证 `_changed_top_keys('{"approval":"ok"}', '{broken json') → ()`；`check_write_ownership` → `violations=[]`。snapshot.py:64-67 JSONDecodeError 与"无键变化"同一空信号。与 F526（isinstance 分支）同函数不同分支。 |
| F535 | P2 无 id 列静默一致 | **成立** | drift.py:51-53 `rid = row.get("id"); if rid:` — 无 id 列 → 整表丢弃 → md_rows={} → drift 恒 []。实证 `parse_markdown_table('## 活跃伏笔\n\n| 类型 | 维度 |...')` → {}。判据 12 对畸形表零检测。 |
| F536 | P2 未声明写不可达 | **成立** | 实证：round_dir 写 secret.json（未声明），watch=['truth/pending_hooks.md'] → `audit_writes` → `violations=()` checked 仅声明面。真实接线 watch=`_audit_watch_paths(skill, chapter)`（executor.py:243）=derive_output_files=声明面 → snapshot_tree 只对声明面取快照 → "未声明写入"真阳性路径结构性不存在；test_write_audit.py:79-87 手工构造 post dict（不经 snapshot_tree）掩盖。本轮复核 `_matches_declared` 每次触发均为 F524/F515 假阳性（与"100% 假阳性驱动"结论一致）。 |
| F537 | P2 null↔missing 归并 | **成立** | 实证 `_changed_top_keys('{"approval":"ok","version":1}', '{"approval":"ok","version":1,"x":null}') → ()`（新增 null 值键零信号；对照 `{"x":1}` → ('x',)）；`compute_file_change('genre-config.json', …, '{"…,"evilKey":null}')` → modified changed_top_keys=() → `check_write_ownership` → `violations=[]`。snapshot.py:73-75 `.get(k)` 缺键与 null 同值。 |
| F538 | P2 snapshot 非 UTF-8 崩 | **成立** | 实证：watched 文件含 `b'\xff\xfe'` → `snapshot_tree` CRASH UnicodeDecodeError。snapshot.py:59 read_text 无 try；pre-snapshot（executor.py:244，try 外）崩 → dispatch 未启动即中止；post-snapshot（:264，finally 内）崩 → 异常替换 dispatch 结果。与 F517（write_audit 分析相）独立代码路径/独立相位。 |

**误报判定**：F500-F538 共 **38 条，0 条误报**。事实性修正 2 处（均非误报，前几轮已处理）：F524"被 F513 掩盖"表述（复核3 已修正为"当前已触发"，本轮实证复现确认修正正确）；F514 时点正确已 resolved。F501 两处描述修正（复核1 已给）本轮独立复核成立。

---

## 二、新发现（F539+）

**本轮新发现：0 条。**

对每个文件剩余未覆盖分支的最后一遍扫描结论（全部候选经实证/设计意图核验后否决）：

| 候选 | 文件:行 | 否决理由 |
|---|---|---|
| `_values_equal` 对 YAML null/缺失 vs md "-" 占位符报 drift（假 drift 过拦截） | records/drift.py:57-69（跨区，Z5 write_audit 消费） | **设计意图核验**：spec New-F「检测」模型明示"派生表必须与 YAML 一致；不一致即 drift（YAML 在冲突时胜出 → 报告 drift → ship 失败 → 人工修）"（design.md:14,452；drift.py:1-3）——检测器被设计为**严格**：任何表↔YAML 不一致都上报人工裁决，null-vs-"-" 属"不一致"的合法报告对象（且当前无表列映射到布尔/可空字段，fixture 从不用 "-"）。非缺陷，不立案。 |
| `FileOwnership.read_keys` 零消费（ownership.py:33,79） | contracts/ownership.py（跨区） | 语义是读 provenance 元数据；executor.py:234-235 明示"read provenance in a subprocess is a known blind spot"——字段为已知盲点的显式保留占位，非死代码缺陷。与 F500/F501（dead-wire 生产调用方缺失）不同类。不立案。 |
| `snapshot_tree` 对 watch 中目录路径抛 IsADirectoryError | snapshot.py:59 | 实证 `snapshot_tree(td, ['subdir'])` → IsADirectoryError。但契约 path 需经 registry `resolves()` 校验（legacy.py:116-127），目录路径无法通过校验 → 不可达。不立案。 |
| `_expand_patterns`/`parametric_globs` 的 registry 读错（ContractError/OSError）在 snapshot 侧无防护 → pre-snapshot 崩 | snapshot.py:34,26 | 与 F503（registry read 非 ContractError 面）/F538（快照读侧零容错）同根因家族；且 registry 为框架静态文件（缺/坏则全框架瘫痪），触发面与影响均被既有 finding 覆盖。不重复立案。 |
| `_log_token_usage` 对 dict 形态 usage 静默跳过（无落账） | dispatch_helper.py:1281-1287（Z6 区） | 接线路径统一经 openai SDK（usage 恒为 CompletionUsage 对象）；dict 形态不可达；且属 Z6 区文件。不立案。 |

**Z3/Z6 交叉消费最后一致性结论**：
- Z3（dispatcher/executor）↔ Z5（audit）：`derive_output_files` 消费点 4 处（executor.py:196,223；phase_runner.py:198；write_audit.py:24）签名一致；executor.py:196（dispatch 内，无 try）的非 ContractError 崩点与 F503 家族同根（已覆盖）；`dispatch_with_write_audit` 快照根错位（F513）与 API/IDE 绕过（F512）为 Z3↔Z5 两大接线缺陷，均已立案。
- Z6（pipeline）↔ Z5（cost）：TokenLedger 消费点（dispatch_helper.py:1333）state 闸门（F531）/chapter 恒 0（F532）/写侧裸 IO（F519）/重试少计（F520）均已立案；无新不一致。
- Z6（pipeline）↔ Z5（orchestration）：resonance_trend.md 写侧（chapter_loop.py:3006-3021）↔ 双读侧（escalation_bridge parse_resonance_scores 匹配 / compute_drift parse_trend 不匹配）已由 F501/F522 立案；escalation-review 技能仅声明 `reads: resonance_trend.md`（无技能写它，chapter_loop 直写）→ 趋势文件不在任何 watch 面，不进入写审计（与 F522 影响面一致）。
- 覆盖率工件现行状态：d1-06（01:23 真实版，无 cost/ 条目）与 F514 resolved 一致；共享 tests/coverage/coverage.xml（01:55，8.39%）仍持续被覆写——D1 跨区流程问题（F508 行备注），建议报协调员（非 Z5 文件缺陷，不单列）。

---

## 三、严重度异议

**正式异议数：0。**

对既有四条 P1 裁决（F507→P1、F512→P1、F513 维持 P1、F524→P1）逐一构造反证尝试，**全部失败、全部确认成立**（详见结论表各行）：

- **F507**：唯一可能的反证（非 pipeline 模式下 G2 检查输出文件存在性可部分兜底删除）经核验不成立——pipeline 模式显式跳过 G2（executor.py:205-206），且 G2 不校验删除的写权语义；审计自身对 status="deleted" 零逻辑（实证 violations=[]）。
- **F512**：反证（API/IDE 路径或在他处执行审计）失败——grep 确认 `_dispatch_via_api`/`_dispatch_via_ide` 零 audit 调用；计划原文（pillar4-tierB.md:7,17）确认设计意图为全 dispatch 模式覆盖。
- **F513**：反证（生产 round_dir==REPO_ROOT）失败——executor.py:25-26 常量实证为框架仓库根，生产 round_dir 为 novel-*/tmp 目录；test_executor_audit.py:17,36 monkeypatch PROJECT_DIR=tmp_path 恰为掩蔽源头。P0 论证（审计静默产出 blocked=False）维持 P1 判定理由成立：审计为增量防护、不直接损坏数据、当前仅 legacy 路径执行。
- **F524**：反证（declared 非空）失败——实证 `_declared_patterns('shenbi-chapter-drafting')==[]`，空目录幻影键即触发 violations（当前已活体触发，非"修复后暴露"）。

**对既有 P2 的升/降级评估（无正式异议，记录权衡）**：
- F536（未声明写入检测结构性不可达）曾考虑升 P1——否决：快照按声明面展开是 snapshot.py:3-6 docstring 明示的设计选择（"避免噪音"），"未声明写入"检查的语义冲突属文档↔设计矛盾（P2 档"文档↔代码漂移/死代码/未接线"），且当前被 F513/F512 双重遮蔽。维持 P2。
- F517（drift 外溢+崩）曾考虑升 P1——否决：触发需 watched .md 含字面 `## hooks`/`## 活跃伏笔` 结构（非正常输入），且当前被 F513 空树掩盖。维持 P2（与 F534 的"修复后复审升级"建议一致，供协调员在 F513 修复后复审）。
- F519/F528（写侧裸 IO 崩 hot path）维持 P2：与模块契约（"must never crash"）违反成立，但触发需文件系统故障（只读目录等），属边界错误处理缺陷档。

---

## 四、覆盖空洞声明

- 清单 `zones/Z5.files` 共 **13 文件**，本轮重读 **13/13（100%）**，无空洞：
  `audit/__init__.py`、`audit/_shared.py`、`audit/record.py`、`audit/snapshot.py`、`audit/write_audit.py`、`cost/__init__.py`、`cost/estimate.py`、`cost/ledger.py`、`cost/pricing.py`、`cost/report.py`、`orchestration/__init__.py`、`orchestration/escalation_bridge.py`、`orchestration/scoring_bridge.py`。
- 逐文件复核方式：read 全文 + grep 全仓 src/ 调用方 + 读相邻依赖（contracts/ownership.py、enums.py、legacy.py、paths.py、records/parser.py、drift.py、trace/writer.py、scoring.py、skill_utils/escalation/check.py、compute_drift.py、dispatcher/executor.py、cli.py、pipeline/dispatch_helper.py、chapter_loop.py、truth_io.py、state.py、genesis.py、closure.py、triggers.py、parallel_dispatch.py、audit_layer.py、revision_router.py、phase_runner.py、modes/codex.py）+ 实证运行 16 项（F504 异常类型、F507 删除过审、F509 幻影、F513 PROJECT_DIR 实证、F515 glob 不匹配、F517 场景复现、F518 added 过审、F521 CJK 低估、F522 parse_trend 空、F523 id-less、F524 活体触发、F525 summarize TypeError、F526 dict→list、F529 drift 单向、F531 state 闸门、F532 chapter=0、F533 UnicodeDecodeError、F534 broken JSON、F536 未声明写不可见、F537 null 键、F538 快照 UTF-8）+ 覆盖率工件核验（d1-06 01:23 真实版无 cost/ 条目；共享 coverage.xml 01:55 8.39% 持续污染）+ 计划原文核验（pillar4-tierB.md:7,17 全模式覆盖意图）。
- 前几轮覆盖缺陷修正状态：复核2 的 F524"被 F513 掩盖"表述（复核3 已修正，本轮实证复现确认）；F514 维持 resolved；初审 snapshot.py:43-45 dead-code 误判（F516）维持；四轮/五轮的"0 误报"结论本轮独立复核维持。

---

## 五、复核统计

- **新 finding 数**：**0**（F539+ 无新增）
- **误报数**：**0**（F500-F538 共 38 条全数成立；F524 描述修正方向正确、F514 属处置更新）
- **严重度异议数**：**0**（对既有 F507/F512/F513/F524 四条 P1 裁决全部确认、反证尝试均失败；对 P2 档无正式升/降级异议，仅记录 F536/F517 的权衡）
- **0 新 finding 判定**：**是**（第六轮达成收敛目标）
- 前六轮总体质量评价：六轮迭代后 Z5 区已覆盖"正常输入路径 + 边界面 + 声明面外检测模型 + 接线语义 + 读/写两侧容错 + 变更形态失明"全部维度；本轮对 38 条 findings 的独立实证全部复现（0 误报），四条 P1 反证尝试全部失败（0 严重度异议），剩余未覆盖分支均为不可达/设计意图/同根因家族（已记录否决理由）。**Z5 区复核收敛。** 遗留跨区事项（非 Z5 文件缺陷）2 项供协调员处置：共享 tests/coverage/coverage.xml 持续被 D1 流程覆写（8.39%）；F536 建议在 F513 修复后按"扩面 or 显式降级声明"决策（与 snapshot.py:3-6 设计选择对齐）。
