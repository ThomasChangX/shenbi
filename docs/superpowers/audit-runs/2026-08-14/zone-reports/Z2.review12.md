# Z2 分区第十二轮独立复核报告（Z2.review12）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）+ `Z2.review9.md`（F253-F255）+ `Z2.review10.md`（F256-F257）+ `Z2.review11.md`（F258）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，逐文件重读，未依赖前十一轮转述）；另读跨区消费面验证裁决（gates/{cli,g1,g2,g3,g_reconcile,shared,g0,g0_skill_contract}.py、audit/{_shared,snapshot,write_audit,record}.py、records/parser.py、pipeline/dispatch_helper.py 关键段、scoring.py、safe_write.py、status.py、first-novel.md、round-exec.sh、tests/unit/dispatcher/test_executor_audit.py、tests/fixtures/genre-config-example.json、skills/shenbi-genre-config/SKILL.md、docs/framework/decisions-schema.md、findings-ledger.md、git show 99f91f0:tests/dispatch-subagent.sh）
- 模式: 只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）。git log/status 确认 Z2 源码自报告以来零改动（findings-ledger 已记录 F258 verified，工作树无跟踪修改），行号证据全部有效。
2. **独立验证方式**：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）；20+ 个 `python -c`/heredoc 运行时探针（F216/F218/F236/F238/F240/F245/F258/F232/F235/F247/F251/F252/F253/F254 复现 + 新 M 候选 F259/F260/F261 探针）。
3. **前十一轮判定**：F201-F258 共 **58 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注、F243 机制修正（exit 3 先于 exit 2）、F247 范围注记（中文 prompt → chapter=None 时审计空转不 rc=2）均复核通过。6 条 P1（F214/F216/F218/F227/F235/F247，含两轮异议升级后已入账的 F214/F218）经独立反证尝试全部维持。
4. **本轮新发现 3 条（F259-F261，全部 M 级）**，集中于十二轮都未触及的三处极细碎面：
   - **F259（M）**：`extract_chapter` 接受 `"chapter 0"` → 0，与 chapter_planning 模型 `Field(ge=1)` 的章节号下限语义不一致（边界缺陷）；
   - **F260（M）**：`_changed_top_keys`（Z5 根因，被 executor 审计链消费）对**新增 null 值键**返回空变更集 → field 级 OWNERSHIP 键集校验对 `{"key": null}` 新增旁路；
   - **F261（M）**：decisions.py `VALID_BASIS`/`VALID_SEVERITY` 死常量 + `Selection._p25` 错误优先级（routine+low 超长 rationale 报长度错误而非 FORBIDDEN）。
5. **严重度异议**：不新增；支持已有 8 条（F214→P1、F218→P1 已入账；F217/F228/F229/F238/F251→P1 低置信维持；F220 维持 P2）。**实质性（P1/P2）新 finding 已收敛为 0**。

---

## 一、复核结论表（F201-F258 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 重读三模型 + g4 私有实现（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer 激活计划；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）；grep 无生产消费者（仅 test_skill_integration.py:188 类名文本）。双源已发散。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads（跨区 read 确认）。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g6.py:51 与 g4/worldbuilding.py 以 raw dict 读 target_word_count/target_words——死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无生产加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797、:409-438（G4Severity）本轮 read 复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实测存在，read 确认）。 |
| F210 | P2 | **成立** | hooks.py 引用 SKILL.md:72-73/87/120；本轮 grep 实测 PLANTED:40 / EXPIRED:80 / TRIGGER:94，Δ32-40。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing（复测 74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0（read 复证）→ 修复方向按"CLI 崩溃"修正。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实。 |
| F214 | P1（已升级入账） | **成立，P1 维持** | **反证尝试失败**：codex.py:20-48 不写 current_scorer_agent；run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"。反证（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧本体 P2，P1 由跨区证据支撑。 |
| F215 | P2 | **成立** | `\{[^{}]*\}` 对嵌套 JSON 取内层（`{"final_score":90,"dimensions":[{"num":1,"score":88}]}` → 匹配 `{"num":1,"score":88}`，read 复证）；实际失败多为 loud（shenbi-score REJECT），P2 适当。 |
| F216 | P1 | **成立，P1 维持** | **运行时复现**：空 customRules + texture:False 禁用维度 → `GenreConfig.model_validate` PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制（read 复证）→ G4 校验洞真实可触发。P1 维持。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）维持 P2** | SKILL.md 内 7 处 CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :187 15-30 / :192-195 15-25·30-40·15-25·15-25 / :257 15-30）+ docstring 20-30 + 模型 [15,35]（本轮 grep 复证）。权威区间未定（SKILL.md 自相矛盾），维持 P2 与低置信异议。 |
| F218 | P1（已升级入账） | **成立，P1 维持** | **运行时复现**：部分匹配（声明 ["当前任务","不存在的字段"]，文件 3 节）→ matched=True、仅命中 "当前任务" 节、无 WARN（fields.py:55-64）；dispatch_helper.py:592 `_matched` 弃用（read 复证）。AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py read 复证）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | P2 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，建议维持 P2（不降 M）。 |
| F221 | P2 | **成立（补证维持）** | 本轮复证：fixture 9 键含 tropeInventory（json.loads 实测）；SKILL.md:286/:292 "恰好 8 个"（grep 复证）；OWNERSHIP _GENRE_KEYS 9 键；模型 8 字段无 tropeInventory + docstring "8 camelCase keys"；`extra=ignore` 静默丢弃。review9 补证（match_tropes.py:61 生产读 tropeInventory）维持。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（本轮复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise SubAgentProtocolError；cli main 无 try/except（read 复证）。 |
| F227 | P1 | **成立，P1 维持** | **反证尝试失败**：first-novel.md:38-41 文档化流程 = round-exec.sh 建脚手架（无 novel.json）后立即 `shenbi-dispatch shenbi-worldbuilding generative <round_dir> "<prompt>"`（本轮 read 复证）；`git show 99f91f0:tests/dispatch-subagent.sh:81-82` 注释 "the skill execution happened earlier" 证实门序回归（技能执行被并入 G2 之后的 dispatch_codex，codex.py:61-66）。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）维持 P2** | derive_output_files（audit/_shared.py:38-56）只 resolve_or_skip、不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found。受 F227 掩盖维持低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | `derive_file_type("shenbi-chapter-drafting") == "decisions"` 复现（writes 含 chapter-N-decisions.json）；g2.py:83-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | P2 | **成立** | **运行时复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中（本轮复证）；fixture 9 键与模型 8 字段均无两者。 |
| F232 | P2 | **成立** | **运行时复现**：`Selection(manual_override, rationale="")` → ACCEPTED；`Selection(routine+low, rationale="")` → REJECTED（FORBIDDEN）——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | P2 | **成立** | snapshot.py:101-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | P1 | **成立，P1 维持** | **端到端复现**：`PROJECT_DIR = REPO_ROOT`（executor.py:26），worldbuilding watch 含 `truth/*.md` → `_expand_patterns`（snapshot.py:32-48）展开仓库根 tracked `truth/bridge_tracker.md`/`truth/character_matrix.md`（git ls-files 确认）→ `_matches_declared` 失败（F236）→ `未声明写入` violations → 零写入派发也 rc=2；round_dir 真实写入不被审计。test_executor_audit.py:18,40 monkeypatch `ex.PROJECT_DIR = tmp_path` 编码了正确语义（read 复证）。反证（审计面意图为仓库根）不成立——codex.py:62 cwd 已改 round_dir。 |
| F236 | P2 | **成立** | **运行时复现**：`_matches_declared("truth/bridge_tracker.md", ["truth/*.md"], parametric_globs())` → False（直接 fnmatch → True）；`"truth/*.md" in parametric_globs()` → False（truth/*.md 是 registry glob 非 parametric）。 |
| F237 | P2 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | P2 | **成立；支持 review6 低置信异议（P2→P1）维持 P2** | **运行时复现**：malformed `## hooks` YAML → `audit_writes` 裸 ParserError（write_audit.py:53-56 + records/parser.py:40-44）；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | P2 | **成立** | **运行时复现**：derive_file_type worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归；executor.py:81-85 精确字符串交集）；state-settling → "truth"、chapter-drafting → "decisions"。 |
| F240 | P2 | **成立** | **运行时复现**：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | P2 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`（get_ownership → None）；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"（grep 复证）。 |
| F243 | P2 | **成立（机制修正维持）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（本轮复证）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。机制修正（review8）维持：gate-marker 强制 exit 3 先于分数校验 exit 2。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak（不存在则静默无检查）；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | P2 | **成立** | **运行时复现**：resolve_chapter_path("audits/volume-N-payoff.md", 7) → `audits/volume-7-payoff.md`；arc-N+7 → `arc-7.md`；AC-NNN+7 → `AC-007.md`；resolve_or_skip(volume-N, None) → None；resolve_volume_path 仅 closure.py:20,158 消费（grep 复证）；`_build_skill_prompt`（dispatch_helper.py:656-658）对 chapter=None 抛 UnresolvedPathError。 |
| F246 | P2 | **成立** | dispatch_skill（dispatch_helper.py:1826-1865）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径；rd = round_dir or project_dir）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | P1 | **成立，P1 维持（范围注记维持）** | **端到端复现**：`_declared_patterns("shenbi-chapter-drafting") == []` vs `_audit_watch_paths("shenbi-chapter-drafting", 7) == ['chapters/chapter-7.md','chapters/chapter-7-decisions.json']` → watch 面（executor.py:220-225 带 chapter）与 declared 面（write_audit.py:19-26 不带 chapter）粒度不一致 → N 写技能成功派发恒判「未声明写入」rc=2。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | P2 | **成立** | `PacingDesign.from_markdown`（pacing_design.py:77-130）chapter_sequence 恒空（:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）致误拒/虚增（read 复证）。 |
| F249 | P2 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500（read 复证）。 |
| F250 | P2 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 pipeline dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | P2 | **成立；支持 review9 低置信异议（P2→P1）维持 P2** | 命名面：codex.py:56 `-scores-subagent.json` vs find_report（shared.py:157-166）三模式 / g0.py:440 G0.10 glob `*-generative-scores.json`（本轮 read 复证）；内容面：raw 维度 dict 无 final_score；状态面：codex.py:44 "done" vs g_reconcile.py:40,61 "DONE" → GR.2 恒 FAIL。受 F252 前置掩盖维持低置信。 |
| F252 | P2 | **成立** | scoring.py:364-377 marker 强制 + :187-224 check_gate_markers（t1-skill 分支要求 `G4-{skill}-{test_type}.json`）→ exit 3 MARKER_MISSING；executor 编排（:190-214）只跑 G1/G2 无 G4；grep write_gate_marker dispatcher/ → 空（本轮 read 复证）。 |
| F253 | P2 | **成立** | codex.py:27-48 `_record_completion` 读-改-写整体无锁（safe_write.py:40-59 `_acquire_lock` flock 仅覆盖写入周期，read 复证）；pipeline legacy 并行（parallel_dispatch.py:168-171 Semaphore(4) 共享 project_dir）触发面真实。 |
| F254 | P2 | **成立** | run_g1/run_g2（executor.py:114-118/:125-137）与 shenbi-score 子进程（codex.py:95-109）均无 timeout；仅 codex exec 有 600s（codex.py:63）；`grep -n timeout src/shenbi/dispatcher/` → 仅 codex.py:63（read 复证）。 |
| F255 | P2 | **成立** | derive_file_type 对 4 个 report-kind truth 写者（score-volume/score-arc/score-stratum/review-arc-payoff）全 → "report"（executor.py:77-78 REPORT 分支先于 truth 交集，read 复证）→ G2.11（g2.py:290-313 仅 file_type=="truth"）对其静默失效。 |
| F256 | M | **成立** | g2.py:273/:315 注释声明 "chapter files only"，G2.10（:274-287）/G2.12（:316-322）在 `if file_type == "chapter":` 块（:218-271）之外无条件执行（本轮 read 复证）——truth/report/json 全部受影响；句末符集合（:317）缺 `}`/`]` → 误归 chapter 的 JSON 恒 WARN。 |
| F257 | M | **成立** | executor.py:156 `def dispatch(..., *, chapter: int | None = None)` 全仓零调用方（grep 仅 cli.py:29、executor.py:250，均不传）；dispatch_with_write_audit（:242）另行独立二次 extract_chapter（read 复证）。 |
| F258 | M | **成立** | **运行时复现**：`extract_chapter("Now that chapter 6 is complete, draft chapter 7")` → 6（首匹配）；单引用 `"Draft chapter 7"` → 7。pipeline 生成 prompt 均为单章节引用（review11 grep 复证），仅 standalone 手工 prompt 触发。 |

**误报统计**：整体误报 **0 条**（F201-F258 共 58 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F259-F261）

### F259 | extract_chapter 接受 "chapter 0" → 0：零章节号与模型/门语义不一致（N/NNN 解析出 chapter-000/chapter-0 路径，G1 对不存在文件 loud FAIL） | 漏报（边界缺陷） | M
- 证据: `src/shenbi/contracts/paths.py:45-47`（`re.search(r"\bchapter\s+(\d+)\b", text, re.IGNORECASE)`——`\d+` 无下界，`"chapter 0"` 匹配返回 0）；对照 `src/shenbi/contracts/skills/chapter_planning.py:22`（`chapter_number: int = Field(ge=1)`——契约模型声明章节号 ≥1）；`paths.py:28-34`（`resolve_chapter_path` 对 N 填 0、NNN 填 "000"）；消费面 executor.py:161-162（dispatch 用其解析）、executor.py:242（审计 watch 二次提取）、dispatch_helper.py:1853（pipeline legacy 同用）
- 根因: `extract_chapter` 对章节号无 ≥1 校验，与契约模型（chapter_planning）和 truth-files.yaml 的 chapter 概念语义（章节从 1 起）不一致；用户以序章惯例输入 "chapter 0"（或 "第0章" 场景等价物）时，G1/G2/审计面全部按 chapter=0 解析 → `chapters/chapter-0.md`/`chapters/chapter-000.md` 不存在 → G1.1 not-found（loud rc=1），无静默错误但产生误导性失败（真实意图章无法传达）。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c "from shenbi.contracts.paths import extract_chapter, resolve_chapter_path; print(extract_chapter('draft chapter 0')); print(resolve_chapter_path('chapters/chapter-NNN.md', 0))"` → `0` / `chapters/chapter-000.md`；对照 `extract_chapter('Draft chapter 7')` → `7`
- 建议方向: `extract_chapter` 对匹配值加 `>= 1` 过滤（`if m and int(m.group(1)) >= 1`），或显式文档声明 0 非法；与 F240/F258 修复联动（章节解析统一为单一 helper 时一并加下界校验）；补 "chapter 0" 测试

### F260 | `_changed_top_keys` 对**新增 null 值键**返回空变更集 → field 级 OWNERSHIP 键集校验对 `{"key": null}` 新增旁路 | 漏报（审计盲区，跨区根因：Z5 audit/snapshot.py，被 Z2 executor 审计链消费） | M
- 证据: `src/shenbi/audit/snapshot.py:63-76`（`_changed_top_keys`：`for k in set(a) | set(b): if a.get(k) != b.get(k): keys.add(k)`——键在 pre 缺席、post 值为 null 时 `a.get(k)=None` 与 `b.get(k)=None` 相等 → 不入变更集）；`src/shenbi/contracts/ownership.py:114-119`（field 分支只查 `changed_top_keys` ⊆ write_keys）；`src/shenbi/dispatcher/executor.py:244-268`（dispatch_with_write_audit 经 snapshot_tree→compute_file_change→check_write_ownership 消费该函数）；`tests/fixtures/genre-config-example.json`（genre-config 顶层含多键，null 值新增场景真实可达——LLM 输出 `{"key": null}` 占位属正常概率）
- 根因: `_changed_top_keys` 用 `dict.get` 的 None 哨兵同时表示"键缺席"与"值为 null"，二者不可区分 → 键缺席→null 值的新增在 diff 中不可见；field 级写越权检查（genre-config.json 9 键集、F233/F237 同链）对 null 值新增键静默放行。与 F237（added 文件短路）机制不同：F237 是整文件新建不计算键集，本条是 modified 文件内 null 值键新增漏检。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c "from shenbi.audit.snapshot import _changed_top_keys; print(_changed_top_keys('{\"version\":\"1\"}', '{\"version\":\"1\",\"hacked\":null}'))"` → `()`（旁路）；对照 `{"hacked": true}` → `('hacked',)`（正常检测）；`{"x": 1}` → `{"x": null}`（值改 null）→ `('x',)`（检测）
- 建议方向: `_changed_top_keys` 用键存在性而非 `get` 哨兵判断（`if (k in a) != (k in b) or a.get(k) != b.get(k)`，或 `k not in a and k in b` 直接入集）；补 null 值新增用例测试。根因在 Z5（audit/snapshot.py），修复清单归审计区，Z2 侧（ownership/executor 消费链）无需改动

### F261 | decisions.py `VALID_BASIS`/`VALID_SEVERITY` 死常量；`Selection._p25` 错误优先级：routine+low 超长 rationale 报长度错误而非 FORBIDDEN | 漏报（死代码+误导性错误） | M
- 证据: `src/shenbi/contracts/schemas/decisions.py:11-12`（`VALID_BASIS`/`VALID_SEVERITY` 各仅 1 次出现——定义处，模型实际用 `Basis`/`Severity` Literal 强制）；`:30-45`（`_p25` 先查 `len(rationale) > _RATIONALE_MAX_CHARS`（:34）再查 routine_low+has FORBIDDEN（:41-42）——routine+low 且 rationale 超 100 字符时抛"exceeds 100 chars"而非"FORBIDDEN"）
- 根因: 词表常量在切到 Literal 后未清理（F208/F209 同族残留）；错误优先级使真实违规（routine+low 不应有 rationale）被误导性消息掩盖——producer 收到"超长"会缩短 rationale 仍失败，形成困惑循环。两者均不改变 FAIL 结果，纯质量问题。
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `Selection(target="t", selected=["a"], basis="arc_relevance", severity="low", rationale="x"*150)` → `Value error, rationale exceeds 100 chars`（应为 FORBIDDEN）；`grep -c VALID_BASIS src/shenbi/contracts/schemas/decisions.py` → 1（仅定义）
- 建议方向: 删除 VALID_BASIS/VALID_SEVERITY（或改为从 Literal 派生并加消费断言）；`_p25` 将 routine_low FORBIDDEN 检查移到长度检查之前；补 routine+low 超长用例

### M 级观察（随文记录，均非 finding）
- `dispatcher/executor.py:25` 与 `modes/codex.py:91` 的 `Path(__file__).resolve().parents[N]` 推导 REPO_ROOT/repo_root——在非 editable 安装（site-packages）下 parents[N] 指向 site-packages 而非仓库根，PROJECT_DIR/rubric 默认路径全部错位（F235/F249 的安装布局孪生；当前 uv sync editable 下无实害，M）
- `audit/snapshot.py:59` `snapshot_tree` 对 watched 目录路径（如契约声明裸目录写）`read_text` 裸 IsADirectoryError（当前无契约声明目录写，M，F238 关联）
- `contracts/legacy.py:58-73` `_normalize_read_item` 对重复读路径（一次纯字符串 + 一次 dict-form）`read_fields[path]` 后写覆盖——last-wins，无合并（M）
- `contracts/skills/foreshadowing_resolve.py:53-78` `Report.current_chapter` 与 hooks 内 `HookCP.current_chapter` 无一致性校验（模型无生产消费者，M，F201 家族）

---

## 三、严重度异议（不修改前十一轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1（已入账）**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → G3.5 prior_agents 恒空 → PASS）→ G3.4 fail-closed 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"显式契约。本轮反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发伪造。注：伪造点在 pipeline/（跨区），Z2 侧本体仍 P2。
2. **支持 F218 P2 → P1（已入账）**：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 本轮复现：matched=True、仅命中 section、无 WARN；dispatch_helper.py:592 弃用 _matched）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——SKILL.md 内部 7 处自相矛盾，权威区间未定，维持低置信供协调者裁决。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内，使 record_audit_outcome 永不执行（审计记录丢失）——"崩溃仍审计"保证（executor.py:245-246）失效；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（chapter-drafting/context-composing/chapter-revision 等 5 个）的 .md 散文在 G2 被 continue（g2.py:83-84）整体跳过，G2.6/G2.8/G2.9 对章节正文静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F251 P2 → P1（低置信维持）**：评分产物三重不匹配使 standalone 轮次对 G5.1（no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL）确定性失败；受 F252（exit 3 前置）掩盖维持低置信。
8. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F218 部分匹配 → matched=True、仅命中 section、无 WARN；全缺 → 全文 + WARN
  - F236 `_matches_declared` glob False / 直接 fnmatch True / parametric_globs 无 glob 键
  - F238 audit_writes malformed `## hooks` → ParserError 崩溃
  - F240/F258 extract_chapter 中文 → None / 多引用 → 6（首匹配）/ 单引用 → 7
  - F245 resolve_chapter_path(volume-N/AC-NNN, 7) 全按章节填；resolve_or_skip(volume-N, None) → None
  - F247 `_declared_patterns`=[] vs watch=2 项（chapter-7.md/decisions.json）
  - F232 Selection(manual_override, rationale="") ACCEPTED / routine+low 空串 REJECTED（不对称）
  - F221 fixture 9 键 / SKILL.md:286 "恰好 8 个" / 模型 8 字段四源复证
  - F235/F249 code 链复证（PROJECT_DIR=REPO_ROOT + test_executor_audit monkeypatch 语义）；F252 scoring exit 3 先于分数校验；F251 g_reconcile "DONE" vs "done"
  - **F259（新）** extract_chapter("draft chapter 0") → 0；resolve NNN→chapter-000.md
  - **F260（新）** `_changed_top_keys` null 值新增 → ()；真值新增 → ('hacked',)；值改 null → 检测
  - **F261（新）** VALID_BASIS/VALID_SEVERITY 各仅 1 次出现（死常量）；routine+low 超长 rationale → "exceeds 100 chars"（误报 FORBIDDEN）
  - git show 99f91f0:tests/dispatch-subagent.sh 门序/评分证据复证；git log/status 确认 Z2 源码零改动
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files`/`git log` 仅读；探针均在 /tmp 目录与 python -c / heredoc。

---

## 五、统计

- 新 finding：**3 条**（F259-F261：M×3）；M 级观察 4 条随文
- 误报：**0 条整体误报**（F201-F258 共 58 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持）
- 严重度异议：**8 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1（已入账）、F217→P1（低置信）、F228→P1（低置信）、F238→P1（低置信）、F229→P1（低置信）、F251→P1（低置信）、F220 维持 P2
- 前十一轮 findings 判定：F201-F258 共 58 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否（严格意义上）**——本轮仍有 3 条新发现（F259-F261），但均为 **M 级**（extract_chapter 零章号边界、null 值键审计旁路、decisions 死常量+错误优先级）；**实质性（P1/P2）新 finding 已收敛为 0**，Z2 分区 58 条既有 findings 全部成立，6 条 P1（F214/F216/F218/F227/F235/F247）经独立运行时复现全部维持。剩余仅 3 条文案/边界级遗漏，其中 F260 根因在 Z5（audit/snapshot.py），建议移交审计区修复清单。
