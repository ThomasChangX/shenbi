# Z6 分区第十一轮独立复核报告（fresh-context 全量重读）

> 复核人：第十一轮独立复核 agent（只读，fresh-context）
> 范围：Z6 全部 49 文件（清单 Z6.files）逐文件全文重读；初审 11（F601-F611）+ 一轮 8（F612-F619）+ 二轮 10（F620-F629）+ 三轮 7（F630-F636）+ 四轮 3（F637-F639）+ 五轮 3（F640-F642）+ 六轮 1（F643）+ 七轮 3（F644-F646）+ 八轮 2（F647-F648）+ 九轮 5（F649-F653）+ 十轮 2（F654-F655）逐条独立验证；新发现编号 F656 起。
> 方式：49/49 文件全文 read + `PYTHONPATH=src python3` 实际运行复现（F601/F636/F641/F642/F644/F645/F646/F647/F648/F649/F652/F654 及全部新发现本轮回放）+ 全仓 grep 调用方验证 + 真实项目数据（novel-output/xinghuo-ranqiong/）交叉核对（pending_hooks.md、resonance_trend.md、audit_drift.md、genre-config.json、chapters/chapter-1/45/46.md、master.json、skills/ 目录、plan 07-19-07 Task 5a 原文）。全部运行验证在 /tmp 或纯内存完成；未创建/修改/删除任何仓库文件（除本报告）；未 git add/commit。
> 目标：收敛到 0 新 finding —— **达成（新发现 3 条均 P2 潜伏级，无 P0/P1；误报 0）**。

---

## 0. 复核总览

- 重读文件数：**49 / 49**（覆盖空洞 = 0，见 §3）
- 初审 11 findings：**11/11 成立**（F601 严重度软异议支持）
- 一轮 8：**8/8 成立**
- 二轮 10：**10/10 成立**
- 三轮 7：**7/7 成立**
- 四轮 3：**3/3 成立**
- 五轮 3：**3/3 成立**
- 六轮 1：**1/1 成立**
- 七轮 3：**3/3 成立**
- 八轮 2：**2/2 成立**
- 九轮 5：**5/5 成立**
- 十轮 2：**2/2 成立**
- 新发现：**3**（F656-F658，均 P2、均潜伏）
- 整条误报：**0**；严重度异议：**2 条**（F601 软异议支持前轮；F646 新增软异议）；证据争议：无新争议（F642 支持 review9/10 终裁，本轮回放一致）
- 判定：**本轮 3 条新 finding 全部为前十一轮未触达的统计口径/边界分支，无 P0/P1；自 F637（P1）之后连续 4 轮（F648 起）新发现已全部为 P2 潜伏级**，且本轮的收敛方向为"单文件剩余分支 + 真实数据最后对照"——建议协调者按 review10 提议将收敛判据落定为"无 P0/P1 新 finding + 无整条误报"后终止轮次。

---

## 1. F601-F655 判定表（逐条独立复核）

### 1.1 初审 F601-F611

| 编号 | 标题 | 初审严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F601 | detect_drift 对话塌陷被 `>5.0` 吞掉 | P1 | **成立**（**支持 review6-10 严重度软异议：可辩 P2**） | 本轮直接运行：baseline dialogue=10 → current=1 → `is_drift: False, severity: NONE`。linguistic_drift.py:215 `max(…,5.0)` 与 :218 严格 `>5.0` off-by-one 成立；docstring :185-188 承诺 "<20% of baseline" 触发。**生产影响全潜伏**：唯一生产入口 `_check_linguistic_drift` 因 F602 无 baseline 于 chapter_loop.py:2017-2019 提前 return（本轮代码精读确认），detect_drift 从未被生产调用；即便接线，F612（is_drift 门控）与 F620（调用点吞异常）使干预仍不可达。P1（显式契约静默失效）与 P2（死路径）均可辩护，不构成整条误报。 |
| F602 | establish_baseline 零调用 → 干预静默失效 | P1 | **成立**（P1 维持） | 本轮 grep：`establish_baseline` 全仓仅 baseline.py:24 定义 + `drift_detection/__init__.py:6,28` re-export（+ tests/coverage 静态 HTML），生产零调用；`find . -name linguistic_baseline.json` 0 命中（含 novel-output 全部目录）。计划原文核实：docs/superpowers/plans/archive/2026-07-19-07-*.md:930 "Wire this into `_run_context_assembly` or chapter_loop so it runs once" —— 显式接线指令未履行。P1 成立。 |
| F603 | records drift 只检 md→YAML 方向 | P2 | **成立**（P2 维持） | drift.py:82-93 仅 `for rid, row in md_rows.items()`；YAML 有而 md 无的 id 永不进循环。消费链 write_audit.py:54-56 → record.py:24 block ship。P2 成立。 |
| F604 | compute_stats 引号桶 ASCII 双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"引号": "\"\"''「」『』"` 含重复 `"`/`'`，:226 `sum(text.count(c) for c in chars)` 按字符迭代 → 一对 ASCII 引号 count=4。P2 成立。 |
| F605 | 空 system_terms 正则空串 → 密度 ~1000‰ | P2 | **成立**（P2 维持；三变体均复核） | `"|".join([])==""` → `re.findall("", text)` 全位置匹配；`system_terms: null` → `list(None)` TypeError、`drift_detection: null` → `None.get` AttributeError（linguistic_drift.py:79-84 无类型守卫）同根。真实 config 无 drift_detection 键（本轮核实 `drift_detection: None`）→ 走 bootstrap 回退，空术语仅配置损坏时可达。P2 成立。 |
| F606 | config floor 只认 int，float/str 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:121-130 `isinstance(new_value, int)` 才拦 <60；59.5/`"58"` 绕过。P2 成立。 |
| F607 | 双 baseline 路径（style/ vs context/）+ 术语分裂 | P2 | **成立**（P2 维持） | baseline.py:78 写 `style/`；linguistic_drift.py:278 `_load_baseline` 读 `context/` 且键 `dialogue_ratio` vs detect_drift 读 `dialogue_density`；:296 硬编码术语 vs :107 config 驱动。P2 成立。 |
| F608 | cjk 引号桶只数相邻对 → 恒 0 | P2 | **成立**（P2 维持） | cjk.py:54 桶 token 为相邻双字符 `""`/`''`/`「」`/`『』`，正常文本引号包围内容不相邻 → `text.count` 恒 0；:61-62 "whole token" 修复对引号桶反而永久为 0。P2 成立。 |
| F609 | replay 静默截断无日志 | P2 | **成立**（P2 维持） | replay.py 全文无 logging/structlog import；:40-43 两处 break 无日志、:47-48 safe_write 改写文件。P2 成立。 |
| F610 | recall 缺 id KeyError | P2 | **成立**（P2 维持） | recall.py:47 `hook["id"]` 无防御；:41-44 对 last_reinforced/max_distance 有 `.get()` 判空、唯独 id 无。F622 为独立类型失败模式。P2 成立。 |
| F611 | config 规则 1 整键 auditDimensions 绕过 | P2 | **成立**（P2 维持） | config_coherence.py:109 `key.startswith("auditDimensions.")` 只拦带点形式；整键对象覆盖绕过。F631（falsy）/F638（snake_case）/F643（键缺失）为独立向量。P2 成立。 |

### 1.2 一轮 F612-F619

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F612 | 3 层干预被 `is_drift` 门控吞掉绝对阈值 severity | P1 | **成立**（P1 维持） | 本轮代码精读 chapter_loop.py:2023-2043：`if result.is_drift:` 包裹 log + ESCALATE raise（:2034-2038）+ HARD/WARN 注入全部干预；opening-similarity 检查 :2047-2056 在门外。运行：baseline stm=60 → current stm=110 → `is_drift: False, severity: ESCALATE`。计划 07-19-07:749 绝对阈值触发契约。P1 成立。 |
| F613 | compute_stats 破折号/省略号桶双计 | P2 | **成立**（P2 维持） | compute_stats.py:27 `"破折号": "——"`/`"省略号": "……"` 重复字符 + :226 按字符迭代 → 一个 `——` 计 4。P2 成立。 |
| F614 | update_genre_config 部分失败留 audit-trail 幻影条目 | P2 | **成立**（P2 维持） | config_coherence.py:105-133 循环内逐 key `_set_nested`+`_append_audit_trail`，safe_write 在循环外 :142 → 中途 ConfigError 时 trail 已追加、config 未落盘。P2 成立。 |
| F615 | TraceWriter 撕裂尾部 JSONDecodeError 崩溃 | P2 | **成立**（P2 维持） | writer.py:57 `json.loads(lines[-1])` 无 try；replay.py:38-43 同文件可自愈 → 不对称。三个生产调用点均有外部缓解（safe_write.py:127-135 / audit/record.py:42-46 / migrate 先 replay）。P2 成立。 |
| F616 | parser 静默丢弃非 dict 记录 | P2 | **成立**（P2 维持） | parser.py:45 `[r for r in data if isinstance(r, dict)]` 静默过滤。P2 成立。 |
| F617 | frequency_divergence_alarms/check_window_redundancy 无生产调用方 | P2 | **成立**（P2 维持） | 本轮 grep：两函数仅 linguistic_drift.py:134-179/:256-268 定义 + 测试引用；chapter_loop.py:2001-2005 只 import check_opening_similarity/compute_linguistic_metrics/detect_drift。P2 成立。 |
| F618 | compute_stats RHETORICAL 正则字典死代码 | P2 | **成立**（P2 维持） | grep `RHETORICAL` → 仅 compute_stats.py:37-41 定义一处；detect_rhetoric :250-281 用另一组内联 regex。P2 成立。 |
| F619 | compute_stats CLI --output 缺参 IndexError | P2 | **成立**（P2 维持） | compute_stats.py:374-377 `args[idx + 1]` 无 argc 守卫。P2 成立。 |

### 1.3 二轮 F620-F629

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F620 | 调用点 `except Exception` 吞 DriftEscalationError → ESCALATE 暂停永不生效 | P1 | **成立**（P1 维持） | 本轮代码精读 chapter_loop.py:2719-2727：注释自称 "non-blocking"，`try: _check_linguistic_drift(project_dir, chapter) / except Exception: log.warning("linguistic_drift_check_failed", exc_info=True)`——函数内 raise 的 DriftEscalationError 被调用点吞掉，且 `except Exception` 连带吞掉其他真实错误。grep `_check_linguistic_drift` 唯一生产调用点即 :2722。P1 成立。 |
| F621 | compute_pattern 词表外 pattern 熵只算分母 | P2 | **成立**（P2 维持） | compute_pattern.py:87-104 只 `for pattern in PATTERNS` 求和但 `n = len(patterns)` 含词表外条目；:170 main 默认 "未分类"。P2 成立。 |
| F622 | recall str 类型 last_reinforced/max_distance TypeError | P2 | **成立**（P2 维持） | recall.py:41-46 `.get()` 只判 None 不判类型 → `int - str` / `int > str` TypeError。P2 成立。 |
| F623 | AI_MARKERS "不是…而是" 单省略号字面量恒不命中 | P2 | **成立**（P2 维持） | compute_stats.py:52 单 U+2026 vs 真实文本双 `……`（与 :23 PUNCT_MAP 一致）。P2 成立。 |
| F624 | master.json 59 vs skills/ 74 清单漂移 + generate.py 无 skills 校验 | P2 | **成立**（P2 维持） | 本轮规范化重算（master 条目为 `skills/<name>/SKILL.md` 路径 → 提取 name）：master 59、skills/ 目录 74、缺失恰好 15 个（名单与前八轮完全一致：anchor-curate、book-spine-init、escalation-review、foreshadowing-lifecycle/recall、memory-distill、review-arc-payoff、review-group-*×4、review-resonance、score-arc/stratum/volume）、反向 0。generate.py:24-34/47-53 无 skills 完整性校验。P2 成立。 |
| F625 | migrate_to_current 缺迁移 `_identity` 回退 → 无限循环 | P2 | **成立**（P2 维持） | versioning.py:33-38 `while e.schema_version < CURRENT_VERSION: up = MIGRATIONS.get(..., _identity)` — `_identity` 不改版本 → 恒真循环。当前潜伏（CURRENT_VERSION=1、无生产调用方）。P2 成立。 |
| F626 | check_linguistic_drift_trigger 第 4 触发点死导出 | P2 | **成立**（P2 维持） | 本轮 grep：仅 compute_drift.py:150 定义 + `drift_detection/__init__.py` re-export，src 内无生产调用。P2 成立。 |
| F627 | parse_trend 无表结束条件 | P2 | **成立**（P2 维持） | compute_drift.py:189-209 `for line in lines[header_idx+1:]` 直至文件末尾，无 `## `/空行终止条件；对比 records/drift.py:34-39。真实 resonance_trend.md 单表 → 潜伏。P2 成立。 |
| F628 | segment_sentences/segment_paragraphs 句数口径不一致（；） | P2 | **成立**（P2 维持） | compute_stats.py:16 SENT_ENDS 含 `；`，:63 只按 `。！？\n` 切句，:87 用 SENT_ENDS。P2 成立。 |
| F629 | verify_preservation §5.3 保留校验器未接线 | P2 | **成立**（P2 维持） | 本轮 grep：`verify_preservation` 仅 preserve_check.py:27 定义；src/shenbi/pipeline/ 无调用。P2 成立。 |

### 1.4 三轮 F630-F636

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F630 | revision_router.DEFAULT_RESONANCE_FLOOR=50 与单源 65 漂移（E11 复活） | P2 | **成立**（P2 维持） | 本轮代码确认 revision_router.py:62-64（注释明示 "Default resonance global floor (spec §6.3, config.resonance_global_floor)" 后跟 `= 50`）vs thresholds.py:30 `=65`；chapter_loop.py:1897 显式传值覆盖默认 → 潜伏。P2 成立。 |
| F631 | config 规则 1 与 G0 的 `is False` 被 falsy 值（0/null/""）绕过且审计实际禁用 | P2 | **成立**（P2 维持） | config_coherence.py:109、g0_config_coherence.py:92 均 `is False`；audit_layer.py:113 `audit_dims.get(dim_key, False)` 任何 falsy 即不激活。P2 成立。 |
| F632 | linguistic_drift severity 阶梯硬编码 30/50/100 违反 thresholds.py 单源声明 | P2 | **成立**（P2 维持） | linguistic_drift.py:222-226 字面量 100/50/30 vs thresholds.py:36-39 warn=30/hard=50；thresholds.py:8-10 docstring 自称单源。P2 成立。 |
| F633 | segment_paragraphs docstring 声称单换行分段、实现只按空行分割且 `\n` 计句末 | P2 | **成立**（P2 维持） | compute_stats.py:79 docstring vs :80 `re.split(r"\n\s*\n")`；`\n` ∈ SENT_ENDS :16。P2 成立。 |
| F634 | compute_linguistic_metrics/compute_stats 把 META 指令块当正文统计 | P2 | **成立**（P2 维持） | 真实 chapter-1.md 含 2 个 `<!--META-BEGIN-->`~`<!--META-END-->` 块（本轮核实）；两处统计入口（linguistic_drift.py:94-131、compute_stats.py:311-362）无剥离。P2 成立。 |
| F635 | update_genre_config 治理层零生产调用，docstring 承诺的治理路由不存在 | P2 | **成立**（P2 维持） | 本轮 grep：`update_genre_config` 仅 config_coherence.py 自身 + 测试 + 计划文档；真实 config-change-log.jsonl 仅 1 条 2026-07-19 记录。P2 成立。 |
| F636 | detect_drift 0→正值映射 6.0x → 单标点触发 is_drift=True | P2 | **成立**（P2 维持） | 本轮直接运行：base 全 0 + current em_dash=0.33 → `is_drift: True, severity: WARN`（实测输出 `True WARN`）。linguistic_drift.py:202。P2 成立。 |

### 1.5 四轮 F637-F639

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F637 | records 判据 12 检查在真实 pending_hooks.md 上恒空转（格式分叉 + 测试掩盖） | P1 | **成立**（P1 维持，无反证） | 本轮全链复核：真实 pending_hooks.md 标题仅 `# 伏笔追踪`/`## 第56章伏笔呈现`/`## 第56章生命周期状态更新（foreshadowing-track）` 等章节附录，**无 `## hooks`/`## 活跃伏笔`**（grep 确认）；ch-025 快照有 `## 活跃伏笔`(:12)/`## hooks`(:20) → 格式分叉成立。消费链 write_audit.py:54-56 → record.py:24 block-ship 已接线但 drift 恒空。P1（"测试失效掩盖真实缺陷"档）成立。 |
| F638 | 治理链 auditDimensions 键形分叉（snake_case 绕过 Rule 1 与 G0） | P2 | **成立**（P2 维持） | audit_layer.py:105-107 snake_case 回退 vs config_coherence.py:109/g0_config_coherence.py:79 只认 camelCase。真实配置 camelCase（本轮核实）→ 潜伏。P2 成立。 |
| F639 | compute_drift CLI 缺失趋势文件静默 exit 0 + 真实项目 arc/volume 半边不运行 + audit 追加无协调 | P2 | **成立**（P2 维持） | 真实 truth/ 仅 resonance_trend.md（1 行 ch55，含 human_overridden 列），无 arc_payoff_trend.md/volume_score_trend.md；audit_drift.md 为 `## Ch55 — 推进/转折` 章节式（review-resonance 写），CLI `_append_audit`（compute_drift.py:225-233）追加裸 `## drift findings` 段与之无协调。P2 成立。 |

### 1.6 五轮 F640-F642

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F640 | materialize_progress 输入契约零生产者 → 无条件覆盖真实 progress.json（数据丢失+错误结果） | P1 | **成立**（P1 维持） | 本轮全链复核：`grep '"INIT"\|"MARK_DONE"'` src/ → 仅 materialize.py:49,53（消费方）+ 测试；生产 TraceWriter.append 调用点 = audit/record.py:44 + migrate.py:30（migrate_from_progress 自身零调用）+ safe_write.py:132（trace_action 生产唯一传参处即 materialize.py:98 自身）。`_maybe_materialize_progress`（chapter_loop.py:684-697）：`if steps_done % 5 == 0`（steps_done=0 即满足，:691）+ `except Exception: pass`（:695，本轮代码精读确认）。真实项目无 trace.jsonl、progress.json 未被破坏 → 潜伏。P1 成立（触发即数据丢失，激活应升级 P0）。 |
| F641 | records `_values_equal` 布尔比较孔 → YAML true vs md "true" 假 drift | P2 | **成立**（P2 维持） | 本轮运行：`_values_equal(True, 'true')` → False（假 drift）。drift.py:57-69 float 兜底对 bool 抛 ValueError 落入 return False。P2 成立。 |
| F642 | check_opening_similarity 比较 META 块非正文开头 | P2 | **成立**（本体成立；**证据裁决支持 review9/review10**） | 本轮独立重放：raw [:300] (46,45)=0.6267；剥离 META 后生产顺序 (46,45)=**0.6367** > 0.6、交换顺序 (45,46)=0.57、autojunk=False=0.77（实测输出确认）——0.57 与 0.637 均可复现，根源为 SequenceMatcher autojunk 参数顺序不对称（F649）；按生产调用顺序（chapter_loop.py:2049 current 在前）剥离后仍 0.6367 > 0.6 → 剥离 META 不足以消除假阳性。review9/review10 裁决成立，review8 裁定不成立。P2 维持。 |

### 1.7 六轮 F643

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F643 | 治理链"键缺失"默认语义矛盾：G0 视缺失=启用、audit_layer 视缺失=禁用 → 移除/置空 auditDimensions 静默停用审计 | P2 | **成立**（P2 维持） | g0_config_coherence.py:92 `get(dim, True)` vs audit_layer.py:113 `get(dim_key, False)` vs config_coherence.py:109 只认显式 False——三层缺省语义矛盾，整键替换移除关键维度静默停用审计且 G0 不报。P2 成立。 |

### 1.8 七轮 F644-F646

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F644 | compute_ngrams 未剔除标点 → n-gram 指纹被标点对/标点串主导 | P2 | **成立**（P2 维持） | 本轮实测：真实 chapter-1 top bigram = `('。他', 27)`/`('——', 25)`。compute_stats.py:209 只滤空白。P2 成立。 |
| F645 | compute_ttr 排除串缺 `“”`/`"`/`‘’` 引号 → 引号字符计入 TTR token | P2 | **成立**（P2 维持） | 本轮实测：真实 chapter-1 含 36 个 ASCII `"` 计入 TTR token（`compute_ttr('他说：“你好。”然后离开了。')['total_chars']` = 11 vs 无引号 9）。compute_stats.py:171 排除串只含两个 ASCII 直单引号 `''`。P2 成立。 |
| F646 | AUDIT_SAFETY_MATRIX 6 维 vs 真实 config 10 维漂移：4 维无 rationale/G0 保护（sensitivity 为 BLOCKING 级） | P2 | **成立**（P2 维持；**新增严重度软异议，见 §2**） | 本轮实测：真实 genre-config.json auditDimensions 10 维全 True，`real - matrix = ['foreshadowing','motivation','sensitivity','worldRules']`、`matrix - real = []`。其中 sensitivity 是 BLOCKING 级安全网（chapter_loop.py:989-1003 sensitivity_blocking → escalation/check.py:80-86），禁用它即静默移除可产生 BLOCKING 升级信号的审计，且写侧 rationale 与读侧 G0 均无信号。P2 成立，但 impact 论证支持 P1 考量（见 §2.2）。 |

### 1.9 八轮 F647-F648

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F647 | records by_id 重复/缺失 id 静默塌缩 → drift 漏报或误报且无告警 | P2 | **成立**（P2 维持） | 本轮运行：权威 YAML 两条同 id 记录（type 主线/支线）+ md 对齐后者 → drift `[]`（漏报）；drift.py:80 dict 推导后者覆盖前者。P2 成立。 |
| F648 | compute_percentiles n=2 百分位倒挂（P75/P95 < P50） | P2 | **成立**（P2 维持） | 本轮运行：`compute_percentiles([1,2])` → `{'P25': 1, 'P50': 2, 'P75': 1, 'P95': 1}`（输出确认）。compute_stats.py:99-104 `int(n*0.75)-1` 截断 + `values[n//2]` 组合在 n=2 时倒挂。P2 成立。 |

### 1.10 九轮 F649-F653

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F649 | SequenceMatcher 参数顺序不对称（autojunk）→ 相似度非良定义、近阈值翻转；F642 之争根源 | P2 | **成立**（P2 维持） | 本轮独立重放：剥离 META 后 (45,46) 生产顺序 0.6367 vs 交换顺序 0.57、autojunk=False 0.77（输出确认）。linguistic_drift.py:249-253/256-268。P2 成立。 |
| F650 | TraceWriter 信任尾部签名不校验 → 签名无效尾部链上新事件，replay 静默删除新合法事件 | P2 | **成立**（P2 维持） | writer.py:51-57 `_last_sig_existing` 只 `json.loads(lines[-1]).get("signature", GENESIS_PREV)`，不校验签名与链一致；append 以坏签名为 prev 链新事件；replay.py:42-43 从坏行截断 → 新合法事件被连带删除。与 F615（JSON 不可解析尾部）为独立失败模式。P2 成立。 |
| F651 | parser yaml.safe_load 重复键静默 last-wins → 权威记录源静默损坏 | P2 | **成立**（P2 维持） | parser.py:37-45；PyYAML SafeLoader 默认映射重复键 last-wins 无异常。P2 成立。 |
| F652 | detect_rhetoric「反复」按子短语分解计数爆炸（7 字短语×3 → 12） | P2 | **成立**（P2 维持） | 本轮运行：`detect_rhetoric('冷在墙体第五层。'*3)` → `{'反复': 12}`（输出确认）。compute_stats.py:266-280 枚举全部 3/4/5-gram 各计 1。P2 成立。 |
| F653 | parse_markdown_table 空行不终止表 → 第二张表并入派生视图 | P2 | **成立**（P2 维持） | drift.py:34-39 `if not s: continue` 空行不 break（本轮运行复现第二张表并入）。P2 成立。 |

### 1.11 十轮 F654-F655

| 编号 | 标题 | 严重度 | 复核判定 | 独立验证理由 |
|---|---|---|---|---|
| F654 | `_try_float` 非有限值透传（nan/inf/1e999）→ 趋势序列被静默污染、σ 触发失效 | P2 | **成立**（P2 维持） | compute_drift.py:217-222 docstring 承诺"非有限数值返回 None"但实现仅捕获 TypeError/ValueError，`float("nan")`/`float("inf")`/`float("1e999")` 透传（运行复现）。P2 成立。 |
| F655 | records drift 缺省值比较孔：`None` vs `''` 恒不等 → 空单元格/多余列假 drift | P2 | **成立**（P2 维持） | drift.py:90-91 `rec.get(key)`→None；:57-69 `_values_equal(None, '')` → `str(None)="None"` != `""`、`float(None)` TypeError → False → 假 drift（运行复现）。P2 成立。 |

---

## 2. 严重度异议

| finding | 现定级 | 异议 | 理由 |
|---|---|---|---|
| F601 | P1 | **软异议（支持 review6/7/8/9/10）：可辩 P2** | 缺陷本体成立（对话塌陷 off-by-one），但生产影响全潜伏：detect_drift 的唯一生产入口 `_check_linguistic_drift` 在无 baseline（F602）时于 chapter_loop.py:2017-2019 提前 return，detect_drift 从未被生产调用；即便接线，F612（is_drift 门控）与 F620（调用点吞异常）使干预仍不可达。按决策表"死代码/未接线"（P2）与"违反显式契约"（P1）均可辩护。不构成整条误报，不修改定级（无权限），供协调者裁决。 |
| F646 | P2 | **新增软异议：impact 可辩 P1（BLOCKING 级安全网）** | F646 本体（矩阵 6 维 vs 真实 config 10 维漂移）P2 恰当；但其 4 个无治理维度中含 **sensitivity**——该维度的激活链是活的：audit_layer GENRE_ACTIVATION_MATRIX 把 sensitivity 映射到 shenbi-review-sensitivity → chapter_loop.py:2574-2575 `get_active_genre_audits` 决定派发 → chapter_loop.py:989-1003 `sensitivity_blocking` 喂 escalation/check.py:80-86 的 BLOCKING 触发。**禁用 sensitivity = 静默移除一个能产生 BLOCKING 升级信号的审计**，且（a）写侧 Rule 1 不拦（不在矩阵 critical 集）、（b）G0 不报（_CRITICAL_DIMENSIONS 只含矩阵维）——与 F631/F643 的"安全网静默移除"同档。按决策表"安全网静默失效/违反显式契约"（P1 档）可辩护；但真实配置当前 10 维全 True（潜伏），且 F635 显示配置写入本身无治理路由（skill 直写），故维持 P2 亦可辩护。不构成整条误报，不修改定级（无权限），供协调者裁决。 |

（其余 53 条无严重度异议：六条 P1 中 F602/F612/F620/F637/F640 本轮均经运行/代码/全仓 grep 复核无反证；F601 软异议见上；P2 均无高判或低判。F642 证据争议支持 review9/review10 终裁，本轮重放一致，无新争议。）

---

## 3. 新发现（F656-F658）

### F656 | detect_rhetoric「排比」对句子 `[:20]` 截断后比长度：连续 ≥20 字长句截断后长度全等 → 恒判"平行"；真实 chapter-1 排比=53，其中 40 个（75%）是截断伪影 | 统计口径 | P2

- 证据：`src/shenbi/skill_utils/style_learning/compute_stats.py:253-260`（`a, b, c = sent_texts[i][:20], sent_texts[i + 1][:20], sent_texts[i + 2][:20]` → `abs(la-lb)/max(la,lb) < 0.3` 判定平行）；消费方 `compute_all_stats` :359（`"rhetoric": detect_rhetoric(combined)`）→ skills/shenbi-style-learning/SKILL.md:51（"第一步必须运行 compute_stats.py"）→ LLM 读 JSON 统计写散文描述；真实数据 `novel-output/xinghuo-ranqiong/chapters/chapter-1.md`。
- 根因：`[:20]` 截断使所有 ≥20 字符的句子长度被压成恰好 20 → `abs(20-20)/20 = 0 < 0.3` → **任何三个连续长句（≥20 字）都必然被判"排比"**。真实章节 183 句中 89 句 ≥20 字：`detect_rhetoric(chapter-1)` = 排比 53；剥离截断效应（用原始长度）仅 13 个平行三元组 → **40 个（75%）为截断伪影**。配合 30% 的宽松阈值，该指标实质度量"连续长句三元组数量"而非排比。测试（test_compute_stats.py:147-149 `test_detect_rhetoric_returns_int_counts`、:428-434 反复 ≥1）均未断言排比量级 → 未锁定也未发现。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "detect_rhetoric(open('novel-output/xinghuo-ranqiong/chapters/chapter-1.md').read())"
  -> {'排比': 53, '设问': 0, '反问': 0, '反复': 57}          # 排比=53
  # 剥离 [:20] 截断（用原始句长）重新计数：仅 13 个三元组满足 30% 相似
  -> truncation-only contribution: 40                        # 40/53 为伪影
  # 最小复现：3 句均为 ≥20 字 → 恒判平行
  detect_rhetoric('这是第一句很长的话它超过了二十个字符所以被截断了。这是第二句也很长同样超过二十个字符。这是第三句还是这么长完全一样。')
  -> {'排比': 1, ...}                                        # 与内容无关恒触发
  ```
- 影响：style-learning 报告的「排比」指标在真实数据上虚高 4 倍（53 vs 13），LLM 据此写风格描述时"排比密集"为系统性误判；与 F652（反复子短语爆炸）同属 detect_rhetoric 的量级失真族，但本缺陷是**截断语义错误**（非计数口径选择），修复方向不同。当前 F602 未接线不影响本指标（compute_stats 由 style-learning skill 直接调用，是活动路径）。
- 建议方向：去掉 `[:20]` 截断（直接比较原始句长），或改用"截断后比较"但把截断长度提到足够大（如 60）并注明语义；同时考虑收紧 30% 阈值。补单测（3 句 ≥20 字不同内容不得判排比；真实 chapter-1 排比量级与人工计数一致）。

### F657 | compute_drift.main 卷级检测丢弃 human_overridden 排除标志：章节级排除、卷级不排除 → 被人工覆盖的卷降分仍触发 VOLUME_DECLINE | 边界/统计口径 | P2

- 证据：`src/shenbi/skill_utils/drift_detection/compute_drift.py:268-270`（章节级：`excl = {i for i, (_, e) in enumerate(series) if e}` 传入 `detect_chapter_drift(..., exclude_indices=excl)`）；`:275-278`（卷级：`volume_scores = [score for score, _ in overall_series]` —— **排除标志被丢弃**，`detect_volume_drift(volume_scores)` 只收 float 列表）；`detect_chapter_drift` docstring :76-78（"exclude_indices = chapters flagged human_overridden (excluded from trigger stats so overrides do not poison detection)"）。
- 根因：同一函数 `main()` 内两条检测路径对 `human_overridden` 列的处理不对称——章节级把 excluded 下标传入检测器（受排除保护），卷级 `overall` 序列只取分数、丢掉布尔标志 → 被人工覆盖（`human_overridden=true`）的卷降分**照样触发** `VOLUME_DECLINE`，违背同一文件内排除语义的对称承诺。真实 resonance_trend.md 含 `human_overridden` 列（本轮核实）→ 该列语义在卷级路径被静默忽略；真实项目缺 arc_payoff_trend.md（F639）→ 潜伏。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  import tempfile, pathlib
  from shenbi.skill_utils.drift_detection.compute_drift import parse_trend, detect_volume_drift
  p = pathlib.Path(tempfile.mkdtemp())/'arc.md'
  p.write_text('| 维度 | overall | human_overridden |\\n| --- | --- | --- |\\n| 弧情感交付 | 80 | |\\n| 弧情感交付 | 79 | true |\\n', encoding='utf-8')
  series = parse_trend(p, ['overall'])['overall']
  -> [(80.0, False), (79.0, True)]          # 第二行被解析为 excluded
  # main() 的卷级路径: volume_scores = [score for score, _ in series] -> [80.0, 79.0]
  detect_volume_drift([80.0, 79.0])
  -> [DriftFinding(kind=VOLUME_DECLINE, ...)]   # 被覆盖的 79 仍触发（章节级会排除）
  "
  ```
- 影响：人工覆盖（human review 判定某卷降分非漂移）在卷级不生效 → VOLUME_DECLINE 假阳性进入 drift-guidance 输入面（F639 同链），与章节级排除语义自相矛盾；当前无 arc 文件潜伏，接线后即活动。
- 建议方向：卷级检测同样按 `exclude_indices` 语义排除（`detect_volume_drift` 增加 excluded 参数或复用下标过滤）；补单测（overall 序列含 excluded 行时 VOLUME_DECLINE 不触发）。

### F658 | parse_markdown_table 派生表内重复 id 行 last-wins 静默塌缩：首行被吞且无告警（F647 的 md 侧镜像，独立函数独立行） | 边界/数据视图损失 | P2

- 证据：`src/shenbi/records/drift.py:51-53`（`rid = row.get("id"); if rid: out[rid] = row` —— dict 赋值，重复 id 后者覆盖前者，无日志）；对比 F647 覆盖的是 `detect_cross_section_drift` :80 的 `by_id`（**YAML 侧**）——本缺陷是 **md 派生表侧**，属不同函数（parse_markdown_table）不同行。
- 根因：派生 markdown 表（LLM/手工维护）出现重复 Hook ID 行时，`out[rid] = row` 静默保留最后一行，首行从派生视图中消失。与 F647 相同的"重复 id 静默塌缩"但方向相反（F647=权威 YAML 源塌缩；本缺陷=派生视图塌缩）：若首行与 YAML 一致、末行不一致 → 假 drift（对末行报）；若末行与 YAML 一致、首行才是真值 → 漏报（首行差异不可见）。真实数据无 `## 活跃伏笔`（F637）→ 潜伏；F637 修复方向 (a) 恢复权威格式后进入活动路径。
- 验证命令+输出：
  ```
  PYTHONPATH=src python3 -c "
  from shenbi.records.drift import parse_markdown_table
  t = '## 活跃伏笔\n\n| Hook ID | 类型 | 状态 |\n| --- | --- | --- |\n| H01 | 主线 | PLANTED |\n| H01 | 支线 | RELEVANT |\n'
  parse_markdown_table(t)
  -> {'H01': {'id': 'H01', 'type': '支线', 'state': 'RELEVANT'}}   # 首行（主线/PLANTED）静默消失
  "
  ```
- 影响：与 F647/F651/F616 同属"records 权威/派生视图静默数据损失"族但为第四个独立向量（md 表内重复行）；修复时宜与 F647 统一（检测重复 id → WARN + 保留全部或拒绝）。
- 建议方向：`parse_markdown_table` 检测重复 rid（`len(out) != 行数` 时 log WARNING 并列出重复 id），或按 id 分组保留全部行供比较；补单测（派生表重复 id → 有信号或明确 last-wins 文档化）。

---

## 4. 覆盖空洞声明

- **重读文件数：49 / 49**（Z6.files 清单逐文件全文 read，含全部 `__init__.py`/`__main__.py` 薄文件；清单每文件均有复核记录，未覆盖文件 = 0）。
- 交叉验证（超出 Z6 但用于判定影响面）：chapter_loop.py（:1994-2056 漂移检查全链、:2017-2019 baseline 读、:2023-2043 is_drift 门控、:2047-2056 opening-similarity 门外交互、:2716-2727 ESCALATE 调用点吞异常、:684-697 `_maybe_materialize_progress`、:989-1003 sensitivity_blocking、:2574-2575 get_active_genre_audits）、revision_router.py（:62-64 DEFAULT_RESONANCE_FLOOR=50）、audit_layer.py（:100-113 激活语义）、g0_config_coherence.py（:37-42 critical 矩阵、:89-97）、audit/write_audit.py（:54-56 records 消费）、audit/record.py（:24,42-46 block + trace seam）、safe_write.py（:126-140 trace_action 唯一生产调用点）、plugins/master.json（59 skills 路径规范化对比）、skills/ 目录（74，缺 15 名单与前八轮一致）、skills/shenbi-style-learning/SKILL.md:51（compute_stats CLI 强制接线）、novel-output/xinghuo-ranqiong/（pending_hooks.md 143 行章节附录结构、resonance_trend.md 单表单行 ch55 含 human_overridden 列、audit_drift.md `## Ch55 —` 格式、genre-config.json 10 维 camelCase 无 drift_detection 键、chapters/chapter-1/45/46.md META 结构与引号统计、无 trace.jsonl、无 linguistic_baseline.json、无 arc_payoff/volume 趋势文件）、tests/fixtures/truth-pending_hooks.md + tests/fixtures/snapshots/chapter-025/truth/pending_hooks.md（权威格式对照）、tests/unit/skill_utils/test_compute_stats.py（rhetoric 测试存在性核对：仅断言 int 类型与反复 ≥1，未锁排比量级）、docs/superpowers/plans/archive/2026-07-19-07-*.md:930（F602 接线指令原文）。
- 维持观察（不重复立项，供协调者参考）：`read_chapters` 静默吞读错误（compute_stats.py:318-326）、compute_pattern main 非 dict 章节 `.get` 崩溃、escalation CLI 空串 float ValueError、preserve_check 非 list 逐字符迭代、recall 非 dict hook、materialize round 名解析脆弱性（`name.split("-")[1]`）与 `_as_int` str 数值 ValueError、compute_drift `_append_audit` 重复追加无幂等、compute_connectives "时间"/"顺序" 类别重叠、cjk.count_words 非法 mode 静默走 mixed、`find_terms` 裸字符串 terms 逐字符迭代、plugins/generate.py `fields["marketplace"]` 缺 fields KeyError、**compute_stats.read_chapters 同名文件跨目录覆盖**（`texts[md_file.name]` 按 basename 键控，多目录输入时后者静默覆盖前者——style-learning 单目录调用不触发，输入契约类）、`_short_chain_chars` 对 ≤15 字句链计数含句末符（口径选择）、`detect_chapter_drift` 平滑先于排除（review5 已述）。
- 关于"0 新 finding"收敛目标：F601-F655 共 55 条全部成立（含六条 P1），**无整条误报**；本轮新发现 3 条均为前十一轮未触达的**单文件剩余分支**（detect_rhetoric 排比截断、compute_drift 卷级排除丢失、parse_markdown_table 重复行），均 P2、潜伏、无 P0/P1。收敛曲线 11→8→10→7→3→3→1→3→2→5→2→3，自 F648 起连续 4 轮新发现全部为 P2 潜伏级。**本轮判定：达成收敛目标**（若目标为"无 P0/P1 新 finding + 无整条误报"）；若目标为字面"0 新 finding"，则需协调者接受"纯 P2 潜伏级小口径缺陷"也可无限续轮的事实（F642 教训：多轮同法复现仍可能是伪影），建议按 review10 提议落定收敛判据后终止。

---

## 5. 返回摘要

**复核统计：新 finding 3（F656-F658，均 P2）/ 误报 0 / 严重度异议 2（F601 软异议支持前轮；F646 新增软异议）。**

**判定：3 条新 finding，全部 P2 潜伏级、无 P0/P1——按"无 P0/P1 新 finding + 无整条误报"判据达成收敛；按字面"0 新 finding"判据未完全收敛（连续 4 轮新发现均为 P2 潜伏级小口径缺陷，边际收益递减）。**

- F601-F655 共 55 条全部成立，无整条误报。六条 P1（F601/F602/F612/F620/F637/F640）本轮以直接运行 + 代码精读 + 全仓 grep + 真实数据 + 计划原文五重复核，无反证：
  - F602：plan 07-19-07:930 "Wire this into `_run_context_assembly` or chapter_loop so it runs once" 原文核实，接线契约未履行；
  - F612：chapter_loop.py:2023 `if result.is_drift:` 门控代码精读确认；
  - F620：chapter_loop.py:2719-2727 "non-blocking" 注释 + `except Exception` 吞 DriftEscalationError 确认；
  - F637：真实 pending_hooks.md 无 `## hooks`/`## 活跃伏笔` 确认；
  - F640：INIT/MARK_DONE 全仓仅消费方 materialize.py 与测试命中，`_maybe_materialize_progress` steps_done%5==0 + `except Exception: pass` 确认。
- **F642 证据争议终裁复核**：本轮回放 raw 0.6267 / 剥离后生产顺序 0.6367 / 交换顺序 0.57 / autojunk=False 0.77——与 review9/review10 完全一致，review6 的修正与 review8 的裁定之争以 review9/review10 终裁为准（根源 SequenceMatcher 参数顺序不对称，F649），无新争议。
- 本轮 3 条新 finding 均为前十一轮未触达的**单文件剩余分支**：
  1. **F656（P2）**：detect_rhetoric「排比」对句子 `[:20]` 截断后比长度 → 连续 ≥20 字长句恒判平行；真实 chapter-1 排比=53，其中 40 个（75%）为截断伪影（剥离截断后仅 13 个真实平行三元组）——style-learning 报告的系统性误判（运行复现，测试未锁量级）。
  2. **F657（P2）**：compute_drift.main 卷级检测丢弃 human_overridden 排除标志（章节级排除、卷级不排除）→ 被人工覆盖的卷降分仍触发 VOLUME_DECLINE（运行复现；真实项目缺 arc 文件潜伏）。
  3. **F658（P2）**：parse_markdown_table 派生表内重复 id 行 last-wins 静默塌缩（F647 的 md 侧镜像，独立函数独立行，运行复现）。
- 严重度异议：F601 软异议（支持 review6-10，P1↔P2 可辩）；F646 新增软异议（sensitivity 为 BLOCKING 级安全网且激活链是活的，impact 可辩 P1——供协调者裁决，不修改定级）。

**findings 清单（编号/标题/严重度/证据行）**
| 编号 | 标题 | 严重度 | 证据行 |
|---|---|---|---|
| F656 | detect_rhetoric「排比」`[:20]` 截断 → 连续长句恒判平行（真实 chapter-1 排比 53 中 40 为伪影） | P2 | compute_stats.py:253-260,359; chapter-1.md |
| F657 | compute_drift 卷级检测丢弃 human_overridden 排除（章节级排除、卷级不排除） | P2 | compute_drift.py:268-270,275-278 |
| F658 | parse_markdown_table 重复 id 行 last-wins 静默塌缩（F647 md 侧镜像） | P2 | drift.py:51-53 |
| F646（异议） | sensitivity（BLOCKING 级安全网）在 4 个无治理维度中，impact 可辩 P1 | 严重度软异议 | thresholds.py:50-84; chapter_loop.py:989-1003 |

**只读合规声明**：本报告写入前/后均未创建/修改/删除任何仓库文件（除 Z6.review11.md）；全部运行验证在 /tmp 或纯内存完成；未 git add/commit。
