# Z4.review2 复核报告 — src/shenbi/gates/（含 g4 checkers）第二轮独立复核

- 复核 agent：第二轮独立复核（fresh-context，全量重读，不依赖前两轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`（F419-F430，含 2 条严重度异议）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文，逐 checker 通读）
- 实测基线：`.venv/bin/python -m pytest tests/unit/gates`（沿用前两轮 785 passed 基线；本轮以 `.venv/bin/python -c` 只读验证 15+ 组，含临时目录构造，未污染仓库）
- 复核结论：**误报 0**；**新增严重度异议 0**（确认前轮 2 条异议 F404→P1、F408→P1 均成立）；**新发现 9**（F431-F439，全部实测或代码路径验证）
- 结论：**0 新 finding 判定 = 否**（发现 9 个前两轮漏报）

---

## 一、复核结论表（F400-F430 逐条判定）

| 编号 | 初审严重度 | 前轮复核 | 本轮判定 | 理由（本轮独立证据） |
|---|---|---|---|---|
| F400 | P2 | 成立 | **成立** | 实测 `gate_G2([manifest.json],'report')` → `G2.12 WARN 'may be truncated'`（本次复现）；g2.py:315-322 无 file_type 守卫，G2.10（273-287）同缺。 |
| F401 | P1 | 成立（masking 附条件） | **成立**（masking 措辞限定） | 实测构造 `shenbi-worldbuilding-generative-scores.json` + progress DONE → `GR.2:...:status=?` FAIL（本次复现）。注：测试 docstring 自认规避系归档 spec Non-Goal #3 钉住现状（前轮已注），"隐瞒式 masking" 定性过重，但缺陷本身与 P1 成立。 |
| F402 | P1 | 成立 | **成立** | 代码路径复核：length_normalizing.py:29 解析 pf 而 :35 用原始 fp；test 全部绝对路径+rd=None。与本轮 F431 家族的路径解析缺陷同源。 |
| F403 | P2 | 成立 | **成立** | 实测 `cli.main(['G4','worldbuilding','/tmp/nonexistent_foo.md'])` → ValueError 崩溃（本次复现）；generic 回退路径 resolve_input_path 同样崩溃（`gate_G4('shenbi-review-memo-compliance','generative',['foo.md'])` → ValueError）。 |
| F404 | P2 | 成立，**异议→P1** | **成立，确认 P1 异议** | 实测 `Selection(basis='manual_override', severity='low', rationale='')` → PASS；`severity='high', rationale=''` → 同样 PASS（本次复现）。decisions.py:30-45 `has = rationale is not None`。AGENTS.md:70 显式契约在正常路径被空串绕过 → P1 恰当。 |
| F405 | P2 | 成立（名单不全） | **成立** | 实测 G4_CHECKER_SKILLS=22 vs generic.py checkers 分发表 31 项；缺 book-spine-init/chapter-revision/escalation-review/market-radar/memory-distill/score-arc/score-stratum/score-volume/short-drafting（9 项，前轮已补全名单）。 |
| F406 | P2 | 成立（覆盖不完整） | **成立**（影响面限定） | 实测 `ls -d skill-output` → 不存在、`novel-output` 存在；dispatch_helper/phase_runner 用 project-output。**限定**：G7.5 的 `rd/skill-output`（g7.py:66）对 round-exec.sh 创建的测试 round（其 heredoc 确实 mkdir `skill-output`，round-exec.sh:75）**有效**，仅对 pipeline round 恒 SKIP——"恒 SKIP" 表述过宽，但 pipeline 为现行生产路径，结论成立。 |
| F407 | P2 | 成立 | **成立** | 实测 `gate_G0()` → PASS [G0.1 SKIP]（本次复现），G0.2-G0.16/G0.cc 全跳过。 |
| F408 | P2 | 成立，**异议→P1** | **成立，确认 P1 异议** | 复核 dispatch_helper.py:1921-1945：`run_gate_g3` 在 progress.json 缺失时 safe_write 伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + 伪造 scoring_history（本次读源码确认 :1939）。G3.4/G3.5/G1.6 全链 PASS → 独立性保证失效并制造虚假审计证据 → P1 恰当。 |
| F409 | P2 | 成立 | **成立** | 实测 `gate_G1('shenbi-worldbuilding', [])` → PASS [G1.5/G1.6/G1.0 SKIP]（本次复现）。 |
| F410 | M | 成立 | **成立** | SHORT_MAP 20 项；skills/shenbi-market-radar 等 11 目录实测存在。 |
| F411 | M | 成立 | **成立** | g0.py:220/227 missing_dirs 恒假；:532 注释 "20 skills" vs 22。 |
| F412 | M | 成立 | **成立** | g3_independence.py:3 引 g3.py:144-162，实际 G3.4 在 193-206（本次复核确认）。 |
| F413 | M | 成立 | **成立** | decisions_validator.py:175 引 shared.py:113；fail() 实际在 125-136（本次复核确认）。 |
| F414 | M | 成立 | **成立** | chapter_revision.py:97 `"HARD_FAIL"` 不在 GateStatus 词表（本次复核确认）。 |
| F415 | P2 | 成立 | **成立** | 实测 SKILL.md:125=`<!--META-END-->`，规则在 :140（本次复核确认）。 |
| F416 | M | 成立 | **成立** | decisions-schema.md:66-68 仅 low/high；decisions.py:12 含 medium（本次复核确认）。 |
| F417 | — | 成立 | **成立** | tests/unit/gates 无 test_gate_manifest.py / test_memory_distill.py（抽查确认）；gate_manifest 0% 覆盖属实。 |
| F418 | P2 | 成立 | **成立** | g0.py:665 `check_config_coherence(project_dir)` 未传 floor；g0_config_coherence.py:65 仅 floor 非 None 时执行两类检测。另注：该函数 docstring 宣称 "genre-config + state-config coherence"，但函数体只读 genre-config.json，state-config 从未读取——"state-config 半场"连理论路径都不存在，属 F418 根因的延伸细节。 |
| F419 | P2 | — | **成立** | g1.py:210-213 / g2.py:169-172 except 元组缺 ValueError（本次读码确认）；本轮 F431 扩展了该家族其余实例。 |
| F420 | P2 | — | **成立** | g6.py:49-59 novel.json/genre-config.json jload 无保护（本次读码确认）。 |
| F421 | P2 | — | **成立** | g_reconcile.py:33 无 try；g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError（本次读码确认）。 |
| F422 | P2 | — | **成立** | cli.py:83-87 JSON 解析失败 → `input_files=[]`（本次读码确认），叠加 F409 后 G1 CLI 面零保护。 |
| F423 | P2 | — | **成立** | 实测 `ls tests/scoring.py` → 不存在，实现在 `src/shenbi/scoring.py`（本次复现）。 |
| F424 | P2 | — | **成立**（同根因第 5 处见 F433） | 实测 pipeline 布局下疲劳词表不读 genre-config（hits=0）。根因爬升（chapter_drafting.py:219-222）对 pipeline 布局永不命中；对 round-exec 测试布局可命中——"恒用默认"同 F406 需限定为 pipeline 布局。 |
| F425 | P2 | — | **成立**（影响面限定） | g7.py:66+86-91 G7.6 依赖 skill-output；同 F406 限定：round-exec 测试 round 有 skill-output，G7.6 可工作；pipeline round 恒 SKIP。 |
| F426 | P2 | — | **成立** | genre_config.py:28 未走 resolve_input_path（本次读码确认）；shared.py:58-75 契约 "silent CWD fallback removed"。 |
| F427 | P2 | — | **成立** | chapter_revision.py:45 `Path(fp)` 原始路径（本次读码确认）。 |
| F428 | P2 | — | **成立** | 实测：含 total_score=92 → threshold 94 → FAIL；同分 dims 形状（无 total_score）→ threshold 90 → PASS（本次复现分叉）。 |
| F429 | P2 | — | **成立** | g1.py:228-232 gate 内 safe_write .bak（本次读码确认）；AGENTS.md 纯验证契约偏离，且 FAIL 判定前已落盘。 |
| F430 | P2 | — | **成立** | 实测构造仅含"操作"列（无状态变更）的 pending_hooks.md → `G4.ft.changes PASS`（本次复现）。 |

**误报判定：0 条**。F400-F430 全部成立。不准确处仅限证据/影响面细节：F401 "masking" 定性需按归档 spec 限定（前轮已注）；F406/F424/F425 "恒 SKIP/永不命中" 仅对 pipeline round 布局成立（round-exec 测试 round 有 skill-output，前两轮均未区分两种 round 布局）；F405 名单前轮已补全。

**严重度异议：0 新增**。前轮 2 条异议（F404 P2→P1、F408 P2→P1）经独立复核**确认成立**，无推翻理由；其余严重度均按决策表核对无异议。

---

## 二、新发现（F431-F439）

### F431 | jload 的 ValueError（合法但非 dict 的 JSON）在 G1.6/G3.x/G5.1/G7.x/read_genre_config/gate_manifest 未捕获 → 多门崩溃（F419/F421 家族扩展） | error | P2
- 证据: `shared.jload`（shared.py:52-55）对非 dict JSON 自抛 `ValueError`；以下调用点 except 元组仅含 `(json.JSONDecodeError, OSError)`（JSONDecodeError 是 ValueError 子类但 jload 自抛的 ValueError 不在其中）：
  - g1.py:260-262（G1.6 progress.json；except 在 :274）
  - g3.py:148-149（G3.3）、:195-196（G3.4）、:210-211（G3.5）；except 在 :188/:203/:227
  - g5.py:55-57（G5.1 summary.json；except 在 :58）
  - g7.py:34-35（G7.1）、:56-57（G7.1b）、:239-246（G7.16 summary.json 与 phase-state）；except 在 :49/:62/:258
  - shared.py:229-235（`read_genre_config` 内 jload，except 仅 JSONDecodeError/OSError）
  - gate_manifest.py:30-35（`_load_gate_manifest` 对合法非 dict manifest 直接 `cast(dict, ...)` 返回 list → record_gate_result:65 `setdefault` → AttributeError）
- 根因: 与 F419（G1.2/G2.4）和 F421（G_RECONCILE/G_DISPATCH/G_TRANSITION）同一缺陷家族，前两轮只覆盖了输入文件与三扇门的实例，漏掉了其余 8 处 progress/summary/genre-config/manifest 消费点。
- 验证命令+输出:
  - `.venv/bin/python -c "gate_G3('shenbi-x','generative',rd)"`（progress.json=`[1,2,3]`）→ `ValueError ...: expected JSON object, got list`（CRASH）
  - `gate_G1('shenbi-x', [], rd)`（同 progress）→ 同 CRASH；`gate_G7(rd)`（summary.json=`[1,2,3]`）→ 同 CRASH
  - `gate_G5('genesis', rd, None)`（summary.json=`[1,2,3]`）→ `ValueError`（CRASH）
  - `read_genre_config(rd)`（genre-config.json=`[1,2,3]`）→ `ValueError`（CRASH）
  - `record_gate_result(md,'p1',1,'sk','G4',...)`（manifest=`[1,2,3]`）→ `AttributeError: 'list' object has no attribute 'setdefault'`（CRASH）
- 建议方向: 统一 `except (json.JSONDecodeError, OSError, ValueError)`（与 g5.py:38/g6.py:37 的写法对齐）；`_load_gate_manifest` 增加 `isinstance(data, dict)` 守卫（corrupt→reinit 语义应覆盖非 dict）；`read_genre_config` 的 except 加 ValueError。补非 dict JSON 用例到各对应测试。

### F432 | G7.1b 反向覆盖以 ALL_SKILLS(74) 为全集，5 个无 T1 scaffold 的 group/lifecycle 技能永不可达 → 完整 round 的 G7 恒 FAIL；G0.10/G_DISPATCH 同根因 | error | P1
- 证据: shared.py:240-244（`ALL_SKILLS` = skills/ 下全部含 SKILL.md 的目录 = **74**，含 2 meta + 5 个无 T1 测试脚手架技能）；g7.py:59（`missing_in_summary = set(ALL_SKILLS) - summary_skills`）；`comm skills/ tests/tiers/t1-skill` → 5 个缺失：shenbi-foreshadowing-lifecycle、shenbi-review-group-character/craft/factual/plan（无 t1-skill 目录 → 无 rubric → T1 永不可评分，且全仓无任何 t1_scores 写入方，round-exec.sh:19 的评分宇宙恰为 t1-skill 集合 69 项）；同根因另两处：g0.py:435-441（G0.10 `total_skills=74` → t1-reports 计数永 <74 → 恒 WARN）、g_dispatch.py:45-47（GD.1 `all_skills=74` vs pipeline progress 的 `total_skills=[step.skill for step in CHAPTER_STEPS]` 子集 → 恒 FAIL）。
- 根因: ALL_SKILLS 未过滤 T1 不可测技能（无 t1-skill 脚手架的技能 + meta 技能），而 G7.1b/G0.10/G_DISPATCH 以它为全集；test_g7.py:55-66 只用 `t1_scores={}` 钉死 FAIL 行为，未用真实 summary 形状（69 项）覆盖 → "测试失效掩盖真实缺陷"。
- 验证命令+输出: 构造含全部 69 个可评分技能 t1_scores 的 summary.json → `gate_G7(rd)` → `status: FAIL`，`must_fix: ["G7.1:missing_coverage:['shenbi-foreshadowing-lifecycle', 'shenbi-review-group-character', 'shenbi-review-group-craft', 'shenbi-review-group-factual', 'shenbi-review-group-plan']"]`（满分 round 也 FAIL）。
- 建议方向: ALL_SKILLS 派生时排除 meta 技能与无 t1-skill 脚手架的技能（或 G7.1b/G0.10/G_DISPATCH 改为以 t1-skill 集合为全集）；补"真实 summary 69 项 → PASS"用例。附：G7.1b 的 FAIL 消息 id 写的是 `G7.1:missing_coverage`（g7.py:61），与自身注释 G7.1b 不一致，应一并修正。

### F433 | g4_chapter_drafting 主角在场检查用坏掉的 project_root（skill-output 爬升）而非已传入的 project_dir → 生产布局恒用默认名 ["林烽","他"]，检查形同虚设或误报 | error | P2
- 证据: chapter_drafting.py:219-222（`while proj_dir.name != "skill-output"` 爬升，pipeline 布局下永不命中 → `project_root = pf.parent`（chapters/ 目录））；:331-334（`protagonist_names = _load_protagonist_names(str(project_root)) if project_dir else [...]` —— 条件判 project_dir 但取值用 project_root）；`_load_protagonist_names`（:136-166）在 `chapters/characters/protagonist.md` 不存在时回退默认 `["林烽","他"]`。
- 根因: 与 F424（genre-config 疲劳词表）同一爬升缺陷的第 5 个消费者，但症状不同：不是静默兜底而是**用错文件路径**——即使调用方正确传入 project_dir（pipeline 中 g6.py:91 `project_dir=str(pd)`），代码仍读 chapters/ 下的 characters → 任何主角名非"林烽"的作品，主角在场检测只统计"林烽"+"他"（"他"通常 ≥3 → 虚晃 PASS；短文本/少"他"则误报 FAIL）。
- 验证命令+输出: 构造 `rd/project-output/{characters/protagonist.md(name:苏寒), chapters/ch-1.md(正文含"苏寒"×4)}` → `g4_chapter_drafting([ch], rd, project_dir=proj)` → `status: FAIL`，`G4.cd.protagonist_absent: protagonist appears 1 times (threshold: 3)`（苏寒×4 被计为 0）。
- 建议方向: 改为 `_load_protagonist_names(str(project_dir))`（与 fatigue 修复共用 project_dir）；删除 skill-output 爬升。严重度说明：同 F424 根因类取 P2，但可争 P1（pipeline 每章 G6.3 均过此检查）。

### F434 | g4_state_settling 参数 agent 名单用单字符子串匹配（"冷"/"光"）→ 真实 fixture 误报 FAIL | error | P2
- 证据: state_settling.py:14（`_PARAMETER_AGENT_NAMES = {"冷","光","安静","缺口","在场于","参数","槽位"}`）；:46-53（`if agent in body` / `if agent in state_sections` 纯子串匹配，无词边界）。
- 根因: 单字符"冷"/"光"是高频汉字，作为子串命中"冷漠/冷静/寒冷/光/光线"等普通词；"参数/槽位"也是常用技术词。`_validate_character_matrix` 对角色定义节之外的正文任何命中即 FAIL。
- 验证命令+输出: `g4_state_settling([abs('tests/fixtures/truth-character_matrix.md')], None)` → `status: FAIL`，`mf: ['G4.ss.parameter_agent_in_character_matrix: 冷']`（命中"冷漠"）；隔离用例：状态表含"冷月"/"冷静分析局势"同样 FAIL。该 fixture 是真实技能输出（AGENTS.md 禁手造 mock），正常路径可复现误报 → 阻塞 state-settling gate。
- 建议方向: 对 CJK 增加词边界/长度约束（如仅匹配独立 token 或 ≥2 字符名），或将单字符项改为排除普通词的上下文规则；补真实 fixture 用例。严重度说明：按决策表"正常路径可复现功能错误"可争 P1，考虑其为宽松启发式且方向为 fail-safe（误报而非漏报），取 P2。

### F435 | G0.12 exempt_skills 读取后从未使用 + 注释声称的 "no skill returns UNIMPLEMENTED" 校验未实现（G0.12 恒 PASS 空转） | error | P2
- 证据: g0.py:521-529（读 g4-exemptions.json 填充 `exempt_skills` 后**零消费**——grep 仅 :522/:527 两处）；:530-533 注释 "G0.12 verifies that the fallback exists (no skill returns UNIMPLEMENTED)"；:534-543（实际只算 dedicated/generic 计数并恒 PASS，既未验证 fallback 存在性，也未使用豁免名单）。
- 根因: 豁免机制与声称的校验在实现时被丢弃，注释与行为不符；G0.12 成为无校验力的空转检查（若某专用 checker 被移除/崩溃，G0.12 不会发现）。
- 验证命令+输出: `read` g0.py:519-543 全文 + `grep -n exempt_skills src/shenbi/gates/g0.py` → 仅 522/527。
- 建议方向: 要么实现注释声称的校验（遍历 ALL_SKILLS 验证 checkers 分发表覆盖或豁免），要么删除豁免读取与注释；与 F405/F411 一并处置 G0.12 区域。

### F436 | G6.7 planted_chapters 死变量 | optimization | M
- 证据: g6.py:125（`planted_chapters: list[int] = []`）、:141-142（循环内 append），循环后（:143 起）零消费——grep 仅 :125/:142 两处。
- 根因: 早期"种植章节分布"统计的残留，未删净。
- 验证命令+输出: `grep -n planted_chapters src/shenbi/gates/g6.py` → 仅 125/142。
- 建议方向: 删除该变量及 append（或实现种植分布统计并输出到 check）。

### F437 | G5.5 用 except Exception → WARN 吞掉 G4 重跑的一切异常（含 gate 崩溃）→ G4 回归检查 fail-open | error | P2
- 证据: g5.py:274-296（`try: gate_G4(...) ... except Exception: c.append({"id":"G5.5", "s":"WARN", "r":"G4-{pr} check unavailable"})`）。
- 根因: 对比 G6.3（g6.py:97-98 `except Exception → mf` FAIL）与 G7.13（g7.py:166-167 `except Exception → mf` FAIL），G5.5 是唯一把异常降级为 WARN 的回归点——当 gate_G4 因 F402/F403/F431 类缺陷崩溃（非 JSON 输出、ValueError）时，G5.5 记 WARN 而非 FAIL，G4 回归失效且无 FAIL 信号。
- 验证命令+输出: 代码路径推演（`json.loads(g4_raw)` 对 traceback 输出抛异常 → 进 except → WARN；gate_G4 抛 ValueError 同）。
- 建议方向: 与 G6.3/G7.13 对齐：`except Exception as e: mf.append(f"G5.5:{fp.name}:G4_crash:{e}")`；补 gate 崩溃注入用例。

### F438 | chapter_drafting 转折词阈值 1/1000（≥5 兜底）vs SKILL.md 契约 1/3000（3 倍放宽，代码注释自认但 SKILL 未同步） | docs | M
- 证据: chapter_drafting.py:202-206（`max_t = max(5, wc // 1000)`，注释 "relaxed from 1/3000 for automated gen"）；skills/shenbi-chapter-drafting/SKILL.md:85（"转折词密度 ≤ 1/3000字"）与 :134（自检表 "≤ 1/3000"）。
- 根因: 放宽是代码侧有意为之并注释，但技能契约（SKILL.md）与 gate 阈值持续漂移（3000 字章节契约允许 1 个、gate 允许 5 个；9000 字契约 3 个、gate 9 个）。
- 验证命令+输出: `sed -n '85p;134p' skills/shenbi-chapter-drafting/SKILL.md` → 均为 ≤1/3000；`read` chapter_drafting.py:202-206。
- 建议方向: 要么 SKILL.md 同步为 1/1000（含自检表模板），要么 gate 回到 1/3000；至少交叉引用注明放宽依据。

### F439 | g4_style_polishing 字数比检查死路：其输入 .bak 只由 G1.4 为 BACKUP_SKILLS（truth updaters）创建，style-polishing 不在其中 → G4.sp.word_ratio 永不执行 | error | P2
- 证据: g1.py:97-99（`compute_backup_targets`：`skill_name not in BACKUP_SKILLS → return []`）；实测 `BACKUP_SKILLS` = 16 个 truth-updater，`'shenbi-style-polishing' in BACKUP_SKILLS` → False（其 contract updates 为 `chapters/*.md`，非 truth 概念，derive_backup_skills 按设计排除）；style_polishing.py:42-43（`bak = Path(bak_path(pf)); if bak.exists():` → 恒不成立，且无 bak 时**连 SKIP 记录都不输出**，失败静默）；shared.py:33-41 docstring 声称 "the G1 writer, the G2.11 reader, and the G4 style-polishing word-ratio reader all agree on the same root"——但 G1 writer 只服务 truth-updater。
- 根因: bak_path 三消费方契约的"单源"只统一了文件名，未统一**创建方覆盖范围**：G1 只为 BACKUP_SKILLS 建 .bak，G4.sp 读 chapters 的 .bak → 生产路径上该检查永无输入（RoundPaths.backup()（paths.py:34-36）亦无调用者）。
- 验证命令+输出: `.venv/bin/python -c "from shenbi.gates.g1 import BACKUP_SKILLS; print('shenbi-style-polishing' in BACKUP_SKILLS)"` → `False`；`grep -rn '\.bak\|bak_path' src/shenbi --include='*.py'` → 生产者仅 g1.py:229。
- 建议方向: 将 style-polishing（以及所有读 .bak 的 in-place 章节技能）纳入 BACKUP_SKILLS 派生规则（updates 含 chapters/ 的 in-place 技能），或让 G1.4 对声明 mode: modify 的 writes/updates 建 .bak；至少补"无 .bak 时输出 SKIP 而非静默"。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部实际读全文；g4 24 个 checker 逐个通读）
- 测试基线：`pytest tests/unit/gates` 785 passed（沿用前两轮实测基线）；本轮新增验证命令 15+ 组（全部 `.venv/bin/python -c` 只读 + /tmp 临时目录构造，未污染仓库，未 git add/commit）
- 未重跑项：全量 coverage 采集（前两轮 /tmp 采集结论信任）；dispatch_helper/phase_runner/chapter_loop/round-exec.sh 属他区，仅做交叉证据核实（F408 行号、F432 评分宇宙、F406/F425 的 round 布局限定）
- 跨区交叉确认建议：F432 的 t1_scores 写入方全仓缺失（summary.json 由 round-exec.sh heredoc 初始化为空）→ 建议与 Z1（tests 区）/Z5/Z6（pipeline 区）确认 summary 实际填充路径；F408 的 run_gate_g3 调用语义 ↔ Z5/Z6。

## 四、最终消息（复核统计）

- **新 finding：9**（F431-F439，全部实测或代码路径验证；重点核查了任务点名的 g4 各 checker 业务规则、G0 子检查、G5/G6/G7 剩余路径、门幂等性）
- **误报：0**（F400-F430 全部成立；仅 3 处证据细节需限定：F401 masking 定性按归档 spec 收敛、F406/F424/F425 "恒 SKIP/永不命中" 仅对 pipeline round 布局成立、F405 名单前轮已补全）
- **严重度异议：0 新增**（确认前轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度无异议）
- **0 新 finding 判定：否**（本轮发现 9 个前两轮漏报，未收敛到 0）
