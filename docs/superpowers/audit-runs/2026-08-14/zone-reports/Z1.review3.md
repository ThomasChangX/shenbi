# Z1 分区第三轮独立复核报告（agent d）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`zone-reports/Z1.a.md`（13 findings F101–F113）+ `zone-reports/Z1.review.md`（9 findings F114–F122）+ `zone-reports/Z1.review2.md`（5 findings F123–F127）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（无一处依赖前三轮转述；`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件通读，并对跨文件依赖 `contracts/legacy.py`、`gates/cli.py`、`gates/shared.py`、`gates/g3.py`、`gates/g5.py`、`audit/_shared.py`、`dispatcher/executor.py`、`contracts/paths.py` 独立核实）
- 只读约束：仅 grep / read / python3 只读 AST 与代码路径模拟；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F128–F199（实际产出 F128–F133，共 6 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：6**（F128–F133；P2×3、M×3；**无新 P0/P1**）
- **误报数：0**（前三轮 27 条全部成立；无一条被证伪）
- **严重度异议数：2**（F109：同意 R1/R2 的 M→P2；F115：P1 无反证、独立复核维持）
- **0 新 finding 判定：否**（6 条新漏报，均为中/低危；其中 F128 为 post-score 结构性校验空洞、F133 为 safe_write 释放路径并发缺陷——均为前三轮未覆盖的独立缺陷站点）
- **事实性修正 2 处（非 finding）**：① R1 对 F105 的补充分析称"G5.3 静默 SKIP"——**实证错误**：phase_runner 传 `str(None)="None"`（真值字符串）时 G5.3 走扫描块并**报 PASS**（非 SKIP，见 §3）；② F101 波及文件数实测已增至 **126**（R1:85 → R2:89 → 本轮 126，随 safe_write 持续改写而增长，结论不变）。

---

## 1. 复核结论表（F101–F127 逐条判定）

| 编号 | 标题 | 前三轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 复现：`git ls-files -s docs/framework/dependency-dag.json` → `100644 c8000598...` vs `ls -la` → `-rw-------@`（truth-files.index.json、tests/tiers/deps.json 同）；全仓 100644 索引文件工作树非 644 者实测 **126 个**（含 SKILL.md 族与三轮报告自身——safe_write 每次改写即新增 0600 文件，漂移随运行持续增长；git 不追踪 rw 位 → CI `git diff --exit-code` 不可见） |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `docs/registry.md`/`docs/gates.md`/`docs/schemas.md`/`docs/dispatcher.md`/`docs/integrity.md` 五个顶层文件全部 MISSING（docs/framework/ 下才有 gates.md/dispatcher.md 且无对应锚点）；`ls tests/build_registry.py` → No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 的 action 不破，仅 doc_url 破） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立 AST 扫描 src/ 全量 raise：RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentTimeoutError/SubAgentProtocolError/RetryExhaustedError（5/22），NEVER RAISED 17 类；`grep get_guidance/ERROR_GUIDANCE/RECOVERY_STRATEGIES` 在 src/+tools/ 除定义文件外 **0 消费者** |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 validate_scores 空 → `REJECT: scores is empty` → exit 2；分支无测试触达（test_scoring.py:598-613 恒带 scores.json） |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R1 补充分析中一处事实错误，见 §3） | :360 `required=False` + :117 `str(None)`；test_phase_runner.py:668 `test_routes_start_command` 无 --project-dir 仍断言 state==started（fake_g5_pass mock 掩盖）；g5.py:115 `if project_dir:` 对 "None" 为真 → `Path("None")` 相对 CWD |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :356-362 唯一 `except Exception`（grep 证实）；G3 subprocess（:343-355）无 timeout → 崩溃/非 JSON → 静默跳过 |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | phase_runner 全文 0 引用；仅 gates/cli.py:131-136 手动入口；command-to-give.md T2/T3 协议无三者 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :103-105 mkstemp 在 `try`（:106）外——若 mkstemp 抛 OSError，:117-125 finally 不执行 → flock fd/.lock 永久泄漏；:89-91 takeover unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M | **成立；严重度异议维持** | sync_contracts.py:121 assert 实证；`python -O` 下守卫消失与 docstring :110 "Raises on mismatch" 冲突；该工具会静默写入 deps.json（:201）。**同意 R1/R2：M → P2**（运行时正确性守卫而非文案） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证；test_exceptions.py:64 同款 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9 实证；gates/cli.py、dispatcher/cli.py 均为真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo(" src/shenbi` 0 命中；同族 `backup()`（paths.py:33-35）亦 0 调用方（复核确认） |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | :153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `subprocess.TimeoutExpired.__mro__` 含 SubprocessError 但 `issubclass(TimeoutExpired, OSError) == False`；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有（复核确认） |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | 独立复跑解析器：82 个 rubric 中 38 个零 applicability（9 t2-phase 全无节 + 3 t3-pipeline 全无节 + 26 t1-skill：8 无节、18 有节但表头非 "Dimension scope"——与 R2 修正一致）；4 个 N/A 豁免技能（worldbuilding dim4 / faction-builder dim8 / location-builder dim5 / story-architecture dim6）rubric 正文明示 "scoring.py renormalizes weights for remaining applicable dimensions" 而 scoring 实际不排除；command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径可复现。按决策表"正常路径可复现功能错误"维持 P1 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | executor.py:200-204 同路径 G2 FAIL → `return 1`；phase_runner :211-239 仅 G4 FAIL 阻断（:228-230）；grep `"g2"` 在 phase_runner 外 0 消费 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值；:334-362 tier 块仅 `tier == "T1"` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 `os.open` 成功后 flock 抛 OSError → fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-121 G4 bug-hunt/clean 分支不写标记（仅 :121 generative 分支写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 test_type 拼接 |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关 → G2/G4 校验覆盖非确定 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `state["steps"].append(...)`；其余 5 命令均记录（:121/:127/:164/:226/:298/:314/:331） |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep CapabilityFS/capability_fs src/ tools/` → 0 命中（仅自身 + 测试）；设计文档承诺的"注入门"从未落地 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；`phase-runner pre-score`（缺参）→ IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"` → 0 命中；command-to-give.md:48 原文引用 `bash tests/dispatch-subagent.sh`；ADR-0009 已迁移为 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | :441 `"subagent" if "--subagent" in sys.argv else "interactive"`；command-to-give.md:49 文档化评分命令不带 --subagent |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep `ScoringStatus.OK\|ScoringStatus.UNIMPLEMENTED` src/shenbi → 0 命中（REJECT :158/:425、MARKER_MISSING :371 在用；GateStatus.WARN/UNIMPLEMENTED 与 CommandStatus.EXISTS 均有消费，无异议） |

**前三轮陈述事实错误 1 处（非 finding，需修正记录）**：R1 对 F105 的补充分析（Z1.review.md:26）声称 "G5.3（…:115-205）整个项目扫描块因目录不存在**静默 SKIP**（`c.append({"id":"G5.3","s":"SKIP"})`）"——**实证错误**：g5.py:115 的 SKIP 分支（:204-205）只在 `project_dir` 为 falsy（None/空串）时触发，而 phase_runner 经 `str(None)` 恒传真值字符串 `"None"` → 走完整扫描块 → `Path("None")` 下 char_dir/world_dir/outline_dir 均不存在 → conflicts 恒空 → `c.append({"id":"G5.3","s":"PASS","note":"no cross-skill conflicts detected"})`（g5.py:202-203）。**真实行为是 G5.3 以空证据报 PASS**（比 R1 描述的 SKIP 更糟）；对无 `expected_outputs` 的 phase，G5.4 循环为空、G5.5 走 SKIP（:299-300）→ **G5 整体可假 PASS**。python3 模拟输出见 §3。

---

## 2. 新发现（F128–F133）

F128 | phase_runner cmd_post_score 只校验 JSON 语法不校验结构：垃圾 scores 文件推进状态机到 SCORED/FINALIZED | error | P2
- 证据: src/shenbi/phase_runner.py:290-301（注释 :290-291 "a malformed file must abort here rather than silently advancing state"；实际仅 :292 `json.loads(...)` 校验语法）；usage/协议（command-to-give.md:106）只承诺 scores 文件路径，无下游再校验（finalize :304-333 不读 scores 内容；g5.py 读的是 summary.json/t1-reports，非本文件）
- 根因: 校验范围停留在"可解析 JSON"，未按 usage（scoring.py:285 `{"1": 100, "2": 95, ...}`）校验 shape；`[]`、`"hello"`、`{"foo": 1}` 均为合法 JSON → state 直接置 SCORED → finalize 可推进 FINALIZED，审计轨迹记录垃圾 scores_file
- 验证命令+输出: 代码走查 :278-301；`grep -rn "scores_file" src/shenbi` → 仅 phase_runner 内部记录，无结构消费；cmd_finalize :304-333 无 scores 校验
- 建议方向: `json.loads` 后增加结构断言（dict 且 `all(str(k).lstrip("-").isdigit() for k in v)` 或复用 scoring.validate_scores 语义）；失败 → emit ERROR + exit 1（与注释承诺一致）

F129 | scoring --gate-only 模式 `--type` 尾参 IndexError（无长度守卫；flag 命名与主路径不一致） | error | P2
- 证据: src/shenbi/scoring.py:303（`ftype = sys.argv[sys.argv.index("--type") + 1] if "--type" in sys.argv else "chapter"`——`--type` 为最后参数时 index+1 越界 → IndexError traceback；对照同函数 :300 `gate_type` 有 `idx + 1 < len(sys.argv)` 守卫）；usage :290 只文档化 `--gate-only <GATE> --files <f1,f2>`，`--type` 未文档化且与主评分路径 flag `--test-type`（:324）命名不一致
- 根因: `--type` 分支漏写长度守卫；命名漂移（gate-only 模式沿袭旧 validate-gate 约定，主路径用 --test-type）
- 验证命令+输出: 代码走查 :296-313；`argv=["scoring.py","--gate-only","G2","--files","a.md","--type"]` → `sys.argv[6]` IndexError（len=6）
- 建议方向: `ftype = sys.argv[idx+1] if idx+1 < len(sys.argv) else "chapter"`；统一为 `--test-type` 或至少文档化 `--type`

F130 | sync_contracts render_body_into 无 frontmatter 静默跳过（无日志）且重复块只替换首个 → OD-1 视图静默缺失/残留 | error | M
- 证据: src/shenbi/sync_contracts.py:146-147（`m = re.match(r"^(---\n.*?\n---\n)(.*)$", text, ...)` 失败 → `return` 无任何日志）；:150-152（`pattern.sub(block, body, count=1)` 只替换首个 BANNER..END 块）；main() :202 仍 `log.info("sync_complete", skills=len(contracts))` 无告警
- 根因: render 路径为防御式静默返回——若 SKILL.md 丢失/异常 frontmatter（如 CRLF 行尾使 `---\n` 不匹配），契约视图静默不注入，sync 仍报成功；文件含两个块时第二个残留 → 幂等性破坏
- 验证命令+输出: 代码走查 :137-155；`grep -rn "render_body_into" tests/` → 0 命中（d1-01 记录 :128-134/:144-155 缺失行，该路径无单测触达）
- 建议方向: `if not m:` 分支补 `log.warning("skill_frontmatter_unparsed", skill=...)`；替换用循环或断言恰一个块；补 render 路径单测（R1 must-test 项仍未落实）

F131 | scoring _provenance `gate_markers_verified` 在无匹配 tier 分支时误报 True | error | M
- 证据: src/shenbi/scoring.py:443（`"gate_markers_verified": bool(round_dir and test_type)`——只要两 flag 非空即报 True）；check_gate_markers :187-224（t1 需 path 含 "t1-skill"、t2 需 "t2-phase"、t3 需 "t3-pipeline"；自定义/非标准 rubric 路径三个分支全不匹配 → `return []` 无任何检查）；对照 t2 分支 :206 `if deps_path.exists():` —— deps.json 缺失时检查整段跳过
- 根因: provenance 字段按 flag 存在性赋值，而非按实际执行过的 marker 检查赋值；audit 元数据声称 "verified" 但实际零检查
- 验证命令+输出: 代码走查 :364-377/:443；`grep -n "gate_markers_verified" src/shenbi` → 仅 :443 一处
- 建议方向: 由 check_gate_markers 返回实际检查的 marker 集合（非空才置 True，或改为 `checked: list[str]` 审计字段）

F132 | phase_runner require_state 错误信封缺 "status" 字段，与其余错误路径不一致 | error | M
- 证据: src/shenbi/phase_runner.py:105-111（emit `{"error": ..., "phase": ...}` **无 status 键**）；对照 :139-146（SKILL.md 缺失 → `{"status": CommandStatus.ERROR, ...}`）、:281-289（scores 缺失 → `{"status": CommandStatus.ERROR, ...}`）——同一进程内两套错误信封
- 根因: require_state 沿早期实现未纳入 CommandStatus 枚举；按 `status` 键解析的 shell 消费者（command-to-give.md 协议按 exit code 解析不受影响，但 JSON 消费者取 `status` 会 KeyError）
- 验证命令+输出: `grep -n '"error"\|"status"' src/shenbi/phase_runner.py` → :107 `"error"`（无 status）、:124/:129/:141/:168/:229/:233/:254/:266/:271/:284/:301/:316/:324/:333 均有 status
- 建议方向: require_state 统一 emit `{"status": CommandStatus.ERROR, "error": ..., "phase": ...}`

F133 | safe_write O_EXCL 释放路径 close(fd)→unlink(lockfile) 非原子：stale-takeover 后释放者 unlink 删除新持有者锁 → 双写者并发 | error | P2
- 证据: src/shenbi/safe_write.py:117-125（finally：先 `os.close(lock_fd)` 再 `os.unlink(lockfile)`——unlink 按**路径**操作）；:83-91（stale-takeover：1s 退避后 `os.unlink(lockfile)` + 立即 O_EXCL 重建）；:68/:78/:89（O_EXCL 为存在性锁，无 inode/持有者身份）
- 根因: 释放者 unlink 针对路径而非自己持有的 inode——交错：A 持锁 → 慢写者（>1s）→ C 走 takeover unlink+重建成功持新锁 → A 写完 finally `os.unlink(lockfile)` 删除的是 **C 的新锁文件** → D 的 O_EXCL 立即成功 → C 与 D 同时写同一目标 → 丢失更新。与 F108（takeover create 竞态未捕获）/F119（takeover 破坏活跃持锁）同区域但为**第三个独立缺陷站点**（释放路径，前三轮未覆盖）
- 验证命令+输出: 代码走查 :102-125；`grep -n "os.unlink" src/shenbi/safe_write.py` → :86（takeover）、:123（释放）；d1-01 记录 :71-91/:124-125 均无测试触达
- 建议方向: 释放前校验锁文件 inode（`os.stat(lockfile).st_ino == os.fstat(lock_fd).st_ino` 才 unlink，否则跳过）；或 takeover 改为"重命名旧锁为 .stale + 原子比较"协议

---

## 3. 事实性修正与严重度异议汇总

### 事实修正 1：R1 对 F105 的补充分析中 "G5.3 静默 SKIP" 为错误（真实行为是 PASS）

模拟验证（只读，无文件写入）：
```
project_dir = "None"   # phase_runner str(None) 所传真值
if project_dir:        # g5.py:115 对 "None" 为真
    pd5 = Path("None"); char_dir = pd5/"characters"
    char_dir.exists()  # -> False（"None/characters" 不存在）
    conflicts = []     # 三项扫描全部空结果
    # g5.py:202-203 -> c.append({"id":"G5.3","s":"PASS","note":"no cross-skill conflicts detected"})
```
输出：`G5.3 block entered; char_dir exists: False` + `c.append(... "s": "PASS" ...)`。g5.py:204-205 的 SKIP 分支仅在 `project_dir` 为 None/空串时触发，phase_runner 从不发送该形态。**结论**：F105 成立且影响比 R1 描述的更严重——`--project-dir` 缺失时 G5.3 以空证据报 PASS（而非 SKIP），对无 `expected_outputs` 的 phase，G5.4 循环为空、G5.5 SKIP → **G5 整体可假 PASS**，`start` 命令可错误推进状态机。

### 严重度异议 1：F109（M → P2）——同意 R1/R2
sync_contracts.py:121 assert 是运行时正确性守卫（docstring :110 "Raises on mismatch" 契约），非文案问题；`python -O` 下失效时该工具仍会静默写入 deps.json（:201），产生静默数据漂移（CI 幂等检查是唯一的间接兜底）。按决策表 P2"边界/错误处理缺陷/防御深度"而非 M"错别字/格式"。

### 严重度异议 2：F115（P1）——无反证，维持
独立复跑 `load_applicability`：4 个 N/A 豁免技能全部返回 `{}`（applicability 空 → `filter_dimensions_by_test_type` :116-117 静默原样返回），worldbuilding rubric 第 43 行明示 "scoring.py renormalizes weights for remaining applicable dimensions" 与实现不符；command-to-give.md:64,71 文档化评分路径可复现。按决策表 P1"正常路径可复现功能错误"维持。若合并层认为影响面仅 4 技能×1 维度可议降 P2，但**本复核按决策表维持 P1**，与 R1/R2 一致。

### 其余 25 条严重度无异议
F101/F104/F105/F106/F107/F108/F112/F113/F114/F116/F117/F118/F119/F120/F121/F122/F123/F124/F125（P2）、F110/F111/F126/F127（M）均按前三轮定级。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读；另核实 8 个跨区依赖文件以验证调用契约）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型六方法逐一核实：resolve 后 relative_to 拒 symlink 逃逸、写侧四方法全拒、list_dir 语义正确；模块无生产接线已由 F123 覆盖）、`cli_utils.py`（stdout 数据通道/stderr 日志分离自洽）、`logging.py`（双渲染器/stderr/cache_logger_on_first_use=False 自洽；SHENBI_LOG_FORMAT 非 "json" 静默降级 console 为已知低价值项，同意 R1/R2 不立 finding）、`paths.py`（read/write 语义正确，F112 覆盖死代码）、`recovery.py`（F103 覆盖 dead）、`py.typed`（0 字节 marker + hatchling 默认全量打包，配置层一致）
- 新发现分布：`phase_runner.py`（F128/F132）、`scoring.py`（F129/F131）、`sync_contracts.py`（F130）、`safe_write.py`（F133）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **R1 must-test 项仍未落实**：safe_write.py:71-91 并发竞态分支与 sync_contracts.py:128-134/:144-155 render 路径至今无单测触达（本轮独立确认 grep tests/ 0 命中 render_body_into）。此为测试覆盖空洞而非新代码缺陷（行为缺陷已由 F108/F119/F130 覆盖）。
2. **scoring --interactive 必须是 argv[2]**（:379）：`scoring.py <rubric> --test-type X --interactive` 会把 `--test-type` 当 scores_file 打开 → FileNotFoundError traceback（R2 备查 #3 已记录，同意不重复立 finding）。
3. **scoring G3 skill_name 提取与 check_gate_markers 不一致**：:340 用 `rubric_p.parent.parent.name == "t1-skill"`（精确两级），:196 用 `"t1-skill" in rubric_p.parts`（任意深度）；嵌套 rubric 路径下 G3 静默跳过。潜在格式风险，未触发当前 82 个 rubric。
4. **filter_dimensions_by_test_type 排除解析脆弱性**（:129-132）：`re.findall(r"#?(\d+)", scope)` 会把 "Phase-3 output" 中的 "3" 当维度号（R2 备查 #5 已记录，同意）。
5. **phase_runner `--chapter abc` → ValueError traceback**（:362 `int(chapter_str)` 无 try）——F124 同族 CLI 校验空洞，M 级，未单独立 finding。

---

## 5. 其他说明

- 前四轮（初审+R1+R2）"无 P0/P1"结论自 R1 起因 F115 修正；本轮独立复核确认 F115 无反驳证据（完整复跑解析器 + 4 技能 N/A 豁免逐条核实），P1 维持。
- F101 波及文件数持续增长（85→89→126）且 git 不可见——建议合并层将 F101 与其 CI 幂等检查盲区一并纳入修复优先级（虽仍为 P2，漂移面是 27 条 finding 中最大的）。
- 环境备注：本复核全部验证经 python3 只读 AST/代码路径模拟 + grep/read 完成（未运行 pytest，避免写 __pycache__；未 uv run）；G5.3 行为通过纯逻辑模拟复现（无文件写入）。
