# Z4.review4 复核报告 — src/shenbi/gates/（含 g4 checkers）第四轮独立复核

- 复核 agent：第四轮独立复核（fresh-context，全量重读，不依赖前四轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`（F419-F430）+ `Z4.review2.md`（F431-F439）+ `Z4.review3.md`（F440-F445）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 24 个 checker 逐个通读）
- 实测基线：`pytest tests/unit/gates/g4/test_chapter_drafting.py test_g_reconcile.py test_g3.py` → **42 passed**（子集实测；全量 785 passed 沿用前四轮基线，本轮未改任何文件）；新增验证命令 30+ 组（`.venv/bin/python -c` / /tmp 脚本只读 + 真实项目 `novel-output/xinghuo-ranqiong` 实证）
- 复核结论：**误报 0**；**新增严重度异议 0**（确认前两轮 F404→P1、F408→P1 异议成立，本轮对 F401/F404/F408/F432/F444 五个 P1 逐一实测反证，均无推翻理由）；**新发现 10**（F446-F455，9×P2 + 1×M，全部实测验证）
- 结论：**0 新 finding 判定 = 否**（本轮发现 10 个前四轮漏报，未收敛到 0）

---

## 一、复核结论表（F400-F445 逐条判定）

| 编号 | 初审严重度 | 前几轮判定 | 本轮判定 | 理由（本轮独立证据） |
|---|---|---|---|---|
| F400 | P2 | 成立 | **成立** | 实测 `gate_G2([manifest.json],'report')` → G2.12 WARN；g2.py:315-322 无 file_type 守卫（本轮读码确认）。 |
| F401 | P1 | 成立 | **成立**（修复方向不完整，见 F449） | 实测 materialize 形状 progress + `shenbi-worldbuilding-generative-scores.json` → `GR.2:...:status=?` FAIL（本次复现）。且发现 GR.2 的失败**不止** -scores 后缀一个原因：progress 状态值（materialize/codex 写小写 `done`）与 gate 的 `"DONE"` 字面量不匹配（见 F449）。P1 成立。 |
| F402 | P1 | 成立 | **成立** | length_normalizing.py:29 解析 pf 而 :35 用原始 fp（读码确认）。 |
| F403 | P2 | 成立 | **成立** | cli.py:113-121 无 try/except；RoundPaths checker 双 None raise ValueError（读码确认）。 |
| F404 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 本轮实测：`Selection(basis='manual_override', severity='low', rationale='')` → **PASS**；`severity='high', rationale=''` → **PASS**；缺省 rationale → ValueError（REQUIRED 生效）。空串绕过确认。P1 无推翻理由。 |
| F405 | P2 | 成立 | **成立** | 读码确认 G4_CHECKER_SKILLS 22 项 vs generic.py checkers 31 项。 |
| F406 | P2 | 成立（影响面限定） | **成立**（影响面限定） | `ls -d skill-output` 不存在、`novel-output` 存在；g0.py:195/323、g7.py:66 用 skill-output；round-exec 测试布局有 skill-output 的限定维持。 |
| F407 | P2 | 成立 | **成立** | g0.py:177-179 无 seed → `return passed("G0", [G0.1 SKIP])`（读码确认）。 |
| F408 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 本轮读 dispatch_helper.py:1933-1945：progress.json 缺失时 safe_write 伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history → G3.4 fail-closed 被击穿。P1 无推翻理由。 |
| F409 | P2 | 成立 | **成立** | g1.py:281-282 空输入仅 G1.0 SKIP → PASS（读码确认）。 |
| F410 | M | 成立 | **成立** | SHORT_MAP 20 项 vs 11 个缺 shorthand 技能目录存在（前几轮已实测）。 |
| F411 | M | 成立 | **成立** | g0.py:220/227 missing_dirs 恒假；:532 注释 "20 skills"（读码确认）。 |
| F412 | M | 成立 | **成立** | g3_independence.py:3 引 g3.py:144-162，实际 G3.4 在 193-206（读码确认）。 |
| F413 | M | 成立 | **成立** | decisions_validator.py:175 引 shared.py:113；fail() 在 125-136（读码确认）。 |
| F414 | M | 成立 | **成立** | chapter_revision.py:97 `"HARD_FAIL"` 不在 GateStatus（读码确认）。 |
| F415 | P2 | 成立 | **成立** | SKILL.md:125=`<!--META-END-->`，规则在 :140（grep 确认）。 |
| F416 | M | 成立 | **成立** | decisions-schema.md:66-68 仅 low/high；decisions.py:12 含 medium（读码确认）。 |
| F417 | — | 成立 | **成立** | tests/unit/gates 无 test_gate_manifest.py / g4 无 test_memory_distill.py（抽查确认）。 |
| F418 | P2 | 成立 | **成立** | g0.py:665 未传 floor；g0_config_coherence.py:65 仅 floor 非 None 时执行两类检测（读码确认）。 |
| F419 | P2 | 成立 | **成立** | g1.py:210-213 / g2.py:169-172 except 缺 ValueError（读码确认）。 |
| F420 | P2 | 成立 | **成立** | g6.py:49-59 novel.json/genre-config.json jload 无保护（读码确认）。 |
| F421 | P2 | 成立 | **成立** | g_reconcile.py:33 无 try；g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError（读码确认）。 |
| F422 | P2 | 成立 | **成立** | cli.py:83-87 JSON 解析失败 → `input_files=[]`（读码确认）。 |
| F423 | P2 | 成立 | **成立** | `ls tests/scoring.py` 不存在，实现在 src/shenbi/scoring.py（前几轮已实测）。 |
| F424 | P2 | 成立（影响面限定） | **成立**（影响面限定） | chapter_drafting.py:219-222 skill-output 爬升（读码确认）。 |
| F425 | P2 | 成立（影响面限定） | **成立**（影响面限定） | g7.py:66+86-91 G7.6 依赖 skill-output（读码确认）。 |
| F426 | P2 | 成立 | **成立** | genre_config.py:28 未走 resolve_input_path（读码确认）。 |
| F427 | P2 | 成立 | **成立** | chapter_revision.py:45 `Path(fp)` 原始路径（读码确认）。 |
| F428 | P2 | 成立 | **成立** | g3.py:103 直读 94 vs :113/:125 硬编码 90（读码确认分叉）。 |
| F429 | P2 | 成立 | **成立** | g1.py:228-232 gate 内 safe_write .bak（读码确认）。 |
| F430 | P2 | 成立 | **成立** | foreshadowing_track.py:41-46 裸词 `操作` 命中即 PASS（读码确认）。 |
| F431 | P2 | 成立（范围不全→F440/441 补充） | **成立** | 8 处调用点读码确认；g0.py 2 处由 F440/441 补充（本轮确认）。 |
| F432 | P1 | 成立 | **成立** | 本轮实测：ALL_SKILLS=74 vs t1-skill 69 项（差 5：foreshadowing-lifecycle + review-group-* 4 个）；构造含全部 69 技能 t1_scores 的 summary → `gate_G7` → `FAIL: G7.1:missing_coverage:[5 技能]`（复现）。P1 无推翻理由。 |
| F433 | P2 | 成立 | **成立** | chapter_drafting.py:331-334 条件判 project_dir、取值用 project_root（读码确认）。 |
| F434 | P2 | 成立 | **成立** | state_settling.py:14/46-53 单字符子串匹配（读码确认）。 |
| F435 | P2 | 成立 | **成立** | g0.py:521-529 exempt_skills 零消费（grep 仅 522/527）。 |
| F436 | M | 成立 | **成立** | g6.py:125/142 planted_chapters 零消费（grep 确认）。 |
| F437 | P2 | 成立 | **成立** | g5.py:274-296 except Exception → WARN（读码确认）。 |
| F438 | M | 成立 | **成立** | chapter_drafting.py:202-206 `max(5, wc//1000)` vs SKILL.md:85/124/134 `≤1/3000`（读码+grep 确认）。 |
| F439 | P2 | 成立 | **成立** | style_polishing.py:42-43 读 .bak；全仓 .bak 生产者仅 g1.py:229，BACKUP_SKILLS 不含 style-polishing（读码+grep 确认）。 |
| F440 | P2 | — | **成立** | g0.py:202-207/524-528 except 缺 ValueError（读码确认）。 |
| F441 | P2 | — | **成立** | g0.py:98-99 json.loads 直读 + :107 `.get` 无 isinstance 守卫（读码确认）。 |
| F442 | P2 | — | **成立** | length_normalizing.py:61-64 只查 3000/10000，无 25% 原始长度双底线（读码确认，SKILL.md:38/51-53/62）。 |
| F443 | P2 | — | **成立** | g1.py:102-144 check_fields_exist 仅测试引用（grep 确认）。 |
| F444 | P1 | — | **成立**（证据细节修正） | 结论成立：G3.3 在生产形状下恒 SKIP。**证据修正**：review3 称 output_files 嵌在 `skills[skill][test_type]` 层（codex.py:40-46），实际全部生产写入方均**不含 output_files 键**——codex.py:44 只写 `{"score", "status": "done"}`、materialize.py 只写 status/score、round-exec.sh 写空 skills。test_g3.py:118/220 用 skill 层 output_files 合成形状钉死代码路径（**非生产形状**）属实。P1（测试掩盖真实缺陷）成立，无推翻理由。 |
| F445 | M | — | **成立** | shared.py:210-222 unimplemented() 无调用方（grep 确认）。 |

**误报判定：0 条。** F400-F445 全部成立。不准确处仅限证据细节：F444 的"output_files 在 test_type 层"描述不准（实际任何生产形状都无 output_files 键），结论不受影响；F406/F424/F425 "恒 SKIP/永不命中"仅对 pipeline 布局成立（维持 review2 限定）。

**严重度异议：0 新增。** 对任务点名的 5 个 P1（F401/F404/F408/F432/F444）逐一实测反证：F404 空串绕过实测复现（manual_override low/high 均 PASS）；F408 伪造写入读码确认；F401 GR.2 恒误报实测复现；F432 G7.1b 全 69 技能 summary 恒 FAIL 实测复现；F444 G3.3 恒 SKIP 实测复现。全部确认成立，无推翻证据。

---

## 二、新发现（F446-F455）

### F446 | count_transition_words 转折词计数双向偏离 SKILL 契约：然而→0、然后/显然→计入 | error | P2
- 证据: shared.py:314-331（`tc = content.count("然")` 后减去 虽然/然而/当然/自然/忽然/突然，再加 不过/此时/突然/终于/于是）；SKILL.md:85（"转折词密度 ≤ 1/3000字 — 然/不过/此时/突然/终于/于是"）与 skills/shenbi-chapter-drafting/anti-ai-reference.md:35（"每 3000 字最多 1 次：然而、不过、此时、突然、终于、于是"——**然而**是契约转折词）。
- 根因: 减法表把契约转折词"然而"减掉后未加回（净 0），却漏减非转折的 然-合成词（然后/依然/既然/果然/显然/必然/偶然/居然/猛然/悄然/茫然…）——过度计入。双向偏离契约词表。
- 验证命令+输出: `.venv/bin/python -c "count_transition_words('然而'*10)"` → `0`；`count_transition_words('然后'*10)` → `10`。真实 fixture `tests/fixtures/chapter-draft-example.md`：正文自检"转折词密度: 0"，但 gate 计 12 = meta 5 + body 7（body 的 7 = 然后×6 + 显然×1，全部非契约转折词）→ `G4.transition:12>5` FAIL（真实技能输出被误报）。
- 建议方向: 减法表去掉"然而"（契约转折词应计数）；对 然-合成词改为白名单法（只计 然而/忽然/突然 及 TRANSITION_SPECIFIC），或补全排除表（至少加 然后/依然/既然/果然/显然/必然/偶然/居然/猛然/悄然）；补"然后/然而"对称用例。

### F447 | G4.chapter-drafting 的 transition/fatigue 计数把 PRE/POST 元区块计入（与 G4.meta、word_count_md 剥离行为不一致）→ 真实章节近阈值/越阈值误报 | error | P2
- 证据: chapter_drafting.py:203（`tc = count_transition_words(content)` 全文件含元区块）、:225（`fatigue_hits = sum(content.count(w) for w in fatigue_list)` 全文件）；对比 chapter_drafting.py:241-246（G4.meta 显式剥离 PRE/POST）、shared.py:111-121（word_count_md 的 META_BOUNDARY 剥离）。SKILL/anti-ai-reference.md:27 规则针对正文（fixture 自检亦称"正文"）。
- 根因: 同文件的 G4.meta 剥离元区块而 transition/fatigue 不剥离——真实章节的 PRE_WRITE_CHECK 自检文本会**点名禁用词**（如 chapter-10 第 8 行"零'似乎'"、第 365 行"「心中暗道」「瞳孔猛地一缩」…「微微一笑」"、第 368 行"无一例「似乎/仿佛/不禁…嘴角微扬…」"）→ 全部计入命中。
- 验证命令+输出: 真实章节 `novel-output/xinghuo-ranqiong/chapters/chapter-10.md`：fatigue 总计 7（猛地/嘴角/微微一笑/暗道/似乎×2/仿佛），**正文剥离后 = 0**——7 个命中全部来自元区块；阈值 8，仅差 1 即误报 FAIL。fixture `chapter-draft-example.md`：fatigue 19>8 FAIL（meta 14 + body 5）。transition：fixture 12 = meta 5 + body 7。
- 建议方向: transition/fatigue 计数前用与 G4.meta 相同的 PRE/POST 剥离（或复用 word_count_md 的 META_BOUNDARY）；补"元区块点名禁用词但正文零命中 → PASS"用例。

### F448 | check_chapter_title 只识别阿拉伯数字章节号，中文数字"第一章"漏检（SKILL.md 明示禁止） | error | P2
- 证据: chapter_drafting.py:70（`re.search(r"第\d+章", title)`——`\d` 仅匹配 ASCII 数字）；skills/shenbi-chapter-drafting/SKILL.md:140（"章节标题不要包含章节号（如"第一章"），文件名已编码章节号"——**第一章**是契约明确列举的违规形态）。
- 根因: 正则只覆盖阿拉伯数字；中文数字（一二三…）章节号完全漏检。该函数被 chapter_loop 消费（生产路径）。
- 验证命令+输出: `.venv/bin/python -c "check_chapter_title('第123章 风云', {})"` → `['G4.cd.title:contains_chapter_number...']`；`check_chapter_title('第一章 风云', {})` → `[]`（漏检）。
- 建议方向: 正则扩展中文数字（如 `第[一二三四五六七八九十百千零\d]+章`）或改用反幻觉数字表；补"第一章"负例用例。

### F449 | G_RECONCILE 状态字面量 "DONE" 与全部生产 progress 形状不匹配 → GR.1 恒死；GR.2 在 F401 修复（剥离 -scores）后仍恒 FAIL | error | P2
- 证据: g_reconcile.py:40（GR.1 `td.get("status") == "DONE"`）、:61-62（GR.2 `td.get("status") != "DONE"`）；全部生产 progress 写入方状态值：trace/materialize.py:37/53-59（`"pending"`/payload 默认 `"done"` 小写）、dispatcher/modes/codex.py:44（`skill_entry[test_type] = {"score": score, "status": "done"}` 小写）、round-exec.sh progress heredoc（`"skills": {}` 无任何状态）、run_gate_g3 伪造形状（无 skills 键）。"DONE" 仅存在于测试 fixture（test_g_reconcile.py:57/144 等）。
- 根因: F401 只覆盖了 `-scores` 后缀这一个失败原因；GR.1/GR.2 的状态字面量大小写与生产数据（小写 done / 无状态）不匹配是**独立缺陷**：GR.1 在任何生产形状下都不触发（死检查）；GR.2 即使剥离 -scores，`td={"status":"done"}` 仍 `"done" != "DONE"` → 恒 FAIL。
- 验证命令+输出: 构造 `progress={"skills": {"shenbi-worldbuilding": {"generative": {"status": "done"}}}}` + 报告 `shenbi-worldbuilding-generative-scores.json` → `GR.2:...:status=?` FAIL；**模拟 F401 修复**（stem.removesuffix('-scores') 后解析）→ `candidate_tt='generative'`、`td={'status':'done'}` → 仍 `!= 'DONE'` → 仍 FAIL。
- 建议方向: GR.1/GR.2 改为接受 `{"done","DONE","skip","completed"}` 集合（或统一从 contracts/thresholds 引状态常量）；与 F401 一并修复并补"materialize 小写形状 → PASS"用例；GR.1 增加生产形状测试（当前无任何生产形状用例）。

### F450 | G6.7 hook 解析器与真实 pending_hooks.md 格式不匹配 → 真实项目恒 low_hook_density:0.0 FAIL，生命周期/超距检查死路 | error | P2
- 证据: g6.py:118-142（`hooks_section = hook_text.split("## hooks")[-1]`；`re.split(r"\n- id:", ...)`；`re.search(r"id:\s*(\S+)")` / `state:\s*(\S+)` / `plant_chapter:\s*(\d+)`）；真实 `novel-output/xinghuo-ranqiong/truth/pending_hooks.md`：无 `## hooks` 节、无 `- id:` 块（`grep -c "id:"` = 0），实际格式为 `# 伏笔追踪` + `## 第56章伏笔呈现` 表格（Hook ID/生命周期/文本依据 列）+ `last_reinforced`/`subtlety` 观察段（由 shenbi-foreshadowing-track 维护，文件头 filled_by 字段自证）。
- 根因: G6.7 按旧 "## hooks"+"- id:" 块格式解析；真实格式不匹配 → `hook_blocks` 退化为整文件单块 → `total_hooks=1` → `density = 1/56 = 0.0` → 恒触发 `G6.7:low_hook_density:0.0/chapter` FAIL；`max_distance`/`plant_chapter` 超距检测与 unresolved 统计同样失效（state 正则匹配不到表格形态）。
- 验证命令+输出: `gate_G6('long-form', None, 'novel-output/xinghuo-ranqiong')` → `must_fix` 含 `G6.7:low_hook_density:0.0/chapter`（实际文件含 30+ 伏笔条目）；`grep -c "id:" novel-output/xinghuo-ranqiong/truth/pending_hooks.md` → 0。
- 建议方向: G6.7 解析器兼容表格格式（按 `## 第N章` 节 + 表格行提取 Hook ID/状态），或与 foreshadowing-track 的输出契约对齐（读 frontmatter 或统一 marker）；补真实 pending_hooks.md 形状用例。严重度说明：真实 pipeline round 每轮 G6 均因该条 FAIL，可争 P1，取 P2（检查器-格式漂移类，同 F406/F424 家族）。

### F451 | g6_checks check_pacing / check_style_consistency 元区块剥离正则缺 `# ` 与 `<!--META` 边界 → 正文被吞 → 章节误分类/风格指标失真 | error | P2
- 证据: g6_checks.py:101-102 与 :234-240（`r"## PRE_WRITE_CHECK.*?(?=## |\Z)"` 等，前瞻仅含 `## ` 与 `\Z`）；对比 shared.py:111（`_META_BOUNDARY = r"(?=## |# |<!--META|\Z)"`）与 chapter_drafting.py:243-244（G4.meta 同款含 `# `）。真实章节正文用 H1 标题（`# 废料场`），PRE 块与 POST 块之间无 `## ` 标题 → 非贪婪 `.*?(?=## |\Z)` 从 PRE 一路吃到文件尾，正文全部被剥。
- 根因: 剥离正则边界清单不完整（漏 `# ` 与 `<!--META`），与同仓库其它元区块剥离实现漂移。
- 验证命令+输出: 合成 63.6% 对话章节（H1 正文 + PRE/POST）→ `check_pacing` 报 `narrative, dia% 0`（正文被吞）；正确边界计算 dia% = 63.6 → 应分类 dialogue。真实章节 chapter-10 同样被吞（body_chars 实测 0，word_count_md 计 7435）。
- 建议方向: 三处剥离正则统一改用 `(?=## |# |<!--META|\Z)`（与 word_count_md/G4.meta 对齐）；补"正文含 H1 无 H2 + 高对话占比 → dialogue 分类"用例。

### F452 | G4.cd.chapter_end_hook 评估的是文件末尾 POST_WRITE_SELF_CHECK 元文本而非叙事结尾 | error | P2
- 证据: chapter_drafting.py:293（`paragraphs = [p.strip() for p in content.split("\n\n") if p.strip()]` 基于**原始全文**）、:316-328（`last_p = paragraphs[-1]` 后按 `## `/`>` 前缀跳过、`cjk_last >= 30`、hook 词表判定）。chapter-drafting 输出格式（SKILL 模板 + 真实 fixture）以 `## POST_WRITE_SELF_CHECK` 元区块收尾 → `paragraphs[-1]` 恒为自检 bullet 文本。
- 根因: 未先剥离元区块再取末段；真实 fixture 的 `paragraphs[-1]` 是 "- [x] 转折词密度: 0 …" bullet，其文本含"还"字 → 检查误 PASS；而真实叙事末段（"而在这个世界，知道位置，本身就是一种危险的开始。"）不含词表 → 若按叙事末段检查应 FAIL。检查结果与正文结尾无关（随元文本随机）。
- 验证命令+输出: fixture `chapter-draft-example.md` → `G4.cd.chapter_end_hook PASS`（命中的是 POST bullet 里的"还"）；提取 POST 剥离后的叙事末段 → 无 hook 词 → 应 FAIL。
- 建议方向: 用 word_count_md 相同的元区块剥离后再取末段；补"元区块点名 hook 词但叙事结尾无钩子 → FAIL"用例。

### F453 | G2.7 重要章豁免（volume_map/plan 标注 → ceiling 10000）在全部自动化调用路径死路：project_dir 未接线 → >4500 字重要章恒误报 FAIL | error | P2
- 证据: g2.py:237-238（`is_important = _is_important_chapter(fp, project_dir or "")`；ceiling 10000 vs 4500）、g2.py:382-413（_is_important_chapter 读 `project_dir/outline/volume_map.md` 与 `project_dir/plans/chapter-N-plan.md`）；全部自动化调用点：phase_runner.py:213（`run_gate("G2", [",".join(output_files), file_type, str(round_dir)])`——**无 project_dir 实参**）、g3.py:177（`gate_G2(output_files, ftype, str(rd))` 同缺）；仅 CLI 手动 `shenbi-validate G2 <files> <type> <rd> <pd>` 可传 pd。
- 根因: _is_important_chapter 的 project_dir 形参从未被自动化调用方接线 → `is_important` 恒 False → 重要章豁免为死代码。
- 验证命令+输出: 真实项目 volume_map.md:37（"第10章 | 收官"——正则 `第10章.*(?:爆发|高潮|卷首|卷末|开篇|收官)` 命中）而 chapter-10 实测 7435 字 > 4500；同批 chapter-15(5132)/35(6546)/46(7382)/47(10029)/48(10161)/49(9165) 等 30 章 >4500 字（`word_count_md` 实测）；在 T2 drafting 路径（phase_runner G2 chapter）这些章节均会误报 `G2.7 exceeds ceiling 4500`。
- 建议方向: phase_runner/g3 的 G2 调用补传 project_dir（与 G6/G4 调用一致）；补"volume_map 标注收官章 >4500 → PASS"集成用例。

### F454 | G_TRANSITION GT.1/GT.3 对生产 progress 形状语义漂移：round-exec 形状恒真空 PASS、materialize 形状恒 FAIL | error | P2
- 证据: g_transition.py:47-64（GT.1 读 `remaining_{from_phase}` 键）、:70-85（GT.3 读 `gate_blockers` 键）；生产形状：round-exec.sh progress heredoc 只有 `completed_skill_names/skills/tier/expected_chapters`（**无 remaining_*/gate_blockers 键**）；trace/materialize.py:85-90（写 `remaining_generative/bug_hunt/clean` 与 `gate_blockers: []`）。
- 根因: 键不存在时 `progress.get(key, [])` 静默空转 → GT.1/GT.3 对 round-exec 形状恒 PASS（检查从未真正执行）；对 materialize 形状 GT.1 又因 `remaining_generative` 含全部未完成技能恒 FAIL。同一 gate 的判定完全取决于 progress.json 由哪个生产者创建（与 F432 的 G_DISPATCH 同族，但 G_TRANSITION 未被前几轮点名）。
- 验证命令+输出: round-exec 形状（空 skills）→ `gate_G_TRANSITION('generative','T2',rd)` → `GT.1 PASS / GT.3 PASS / status PASS`（真空）；materialize 形状（remaining_generative=['shenbi-x']）→ `status FAIL, must_fix=['GT.1']`。
- 建议方向: 键缺失应显式 SKIP 并说明（而非静默 PASS），或与 materialize 的形状契约对齐；补两种形状用例。

### F455 | g4_generic_generative docstring 声称校验 frontmatter，代码未实现 | docs | M
- 证据: generic.py:28（"Validates output exists, non-empty, **has frontmatter**"）；:44-56（实际仅 .md 长度 ≥50、.json JSON 合法性、其它按 size 放行——**无任何 frontmatter 检查**）。
- 根因: docstring 与实现漂移（早期版本可能有 frontmatter 检查，重构后未同步）。
- 建议方向: 要么补 frontmatter 校验（与 g1.3/yload 一致），要么 docstring 改为 "validates output exists, non-empty, well-formed"。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部实际读全文；g4 24 个 checker 逐个通读，含此前仅引用未深读的 chapter_drafting 全文 342 行、g6_checks 全文 275 行）。
- 测试基线：`pytest tests/unit/gates/g4/test_chapter_drafting.py test_g_reconcile.py test_g3.py` → **42 passed**（子集实测；全量 785 passed 沿用前四轮基线，本轮未修改任何文件）。覆盖率数据未重采（信任前几轮 /tmp 采集结论）。
- 本轮新增实证基线：对真实项目 `novel-output/xinghuo-ranqiong`（56 章、volume_map.md、truth/pending_hooks.md、character_matrix.md）运行 `gate_G6('long-form', ...)` 得到 122 条 must_fix 并逐类归因（G6.3=56、G6.5=50、G6.10=6、G6.11=7、G6.4=2、G6.7=1）——其中 G6.7（F450）与 G6.10/G6.5 剥离（F451）为门缺陷，G6.3 属 F433 家族，G6.5 的 4_consecutive_narrative 批量 FAIL 经正确剥离复算后**判定为真实信号**（真实小说 dialogue/action 占比确实极低，chapter-10 仅 2.3% 对话），非误报。
- 跨区交叉确认建议：F449/F444 的 progress.json 形状（materialize 小写、codex 小写、round-exec 空 skills、run_gate_g3 伪造）建议与 Z5/Z6（pipeline/dispatcher 区）最终确认；F453 的 phase_runner G2 调用语义 ↔ Z5/Z6。

## 四、最终消息（复核统计）

- **新 finding：10**（F446-F455：9×P2 + 1×M，全部实测验证；重点核查了任务点名的 30 个 per-skill checker 业务规则、G0 各子检查、g6_checks/g7/g3_independence/shared 剩余路径，并对真实项目输出做了端到端门回归）
- **误报：0**（F400-F445 全部成立；对任务点名的 F401/F404/F408/F432/F444 五个 P1 逐一实测反证，均无推翻证据；仅 F444 证据细节需修正——output_files 键在任何生产 progress 形状中都不存在，非"在 test_type 层"）
- **严重度异议：0 新增**（确认前两轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度按决策表核对无异议）
- **0 新 finding 判定：否**（本轮发现 10 个前四轮漏报，未收敛到 0）
