# Z1 分区第十三轮独立复核报告（agent n）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`~`Z1.review12.md`（F114–F160），共 **60 条**（前十二轮新发现 47 条）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `contracts/legacy.py`、`contracts/schemas/deps.py`、`gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g5.py`、`gates/gate_manifest.py`、`audit/_shared.py`、`dispatcher/executor.py`、`tools/lint_status_strings.py`、`tests/tiers/deps.json`（含 `_tool_hashes`/`_out_of_pipeline`/`t3-pipelines` 结构）、82 份 rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`、真实导入复跑解析器、端到端 phase_runner 穿越实证（仅写 /tmp）**）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟、真实控制台脚本实跑（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`，临时文件仅写 /tmp）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F161–F199（实际产出 F161–F162，共 2 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：2**（F161 P2、F162 M；**无新 P0/P1**）
- **误报数：0**（F101–F160 全部 60 条成立；无一条被证伪。F115/F140/F158 三个 P1 经本复核**独立实跑/复跑/端到端实证确认无反证**）
- **严重度异议数：1 新增**（F150：M → 建议 P2，依据见 §3）；维持既有 F109 M→P2 共识；F115/F140/F158 的 P1 无反证维持；F134 维持 P2
- **0 新 finding 判定：否**（2 条新漏报：F161 paths.py `RoundPaths.write()` 死代码——F112 只覆盖了 repo()/backup()，write() 前十二轮全部遗漏；F162 phase_runner cmd_post_skill 冗余 `configure_logging()`——与 main() 重复调用，前十二轮未提及。均为低危扫尾项，Z1 主要缺陷面已收敛）

---

## 1. 复核结论表（F101–F160 逐条判定）

| 编号 | 标题 | 前十二轮严重度 | 复核判定 | 理由（本复核独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 实证：`git ls-files -s` → `100644`，`ls -la` → `-rw-------@`（dependency-dag.json/deps.json 双文件）；根因确认 mkstemp 0600（safe_write.py:105）+ os.replace 继承（:111） |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | 实证：`ls docs/{registry,schemas,integrity}.md tests/build_registry.py` 全 No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 仅 doc_url 破） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | **独立 AST 全仓 raise 扫描**：RAISED 恰为 DispatchWriteFailureError/DispatcherError/RetryExhaustedError/SubAgentProtocolError/SubAgentTimeoutError（5/22），NEVER RAISED 17 类（字面计数；R4"捕获可达"折 16 注记合理）；error_guidance/recovery 引用的 6 类全无站点 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | 走查 :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117/:307 `str(None)`；独立走查 g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **PASS（g5.py:202-203，非 R1 所称 SKIP）**；:188 assert 在 `-O` 下失效。R11 补充"finalize :307 同缺陷"确认 |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一无 exc_info 的 `except Exception`；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | phase_runner 全文 0 引用；仅 gates/cli.py:131-136 手动入口 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——抛 OSError 时 finally 不执行 → flock fd/.lock 泄漏；:89-91 unlink 后 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M（R1 起异议 M→P2） | **成立；同意 M→P2 共识** | sync_contracts.py:121 assert 实证；docstring :110 "Raises on mismatch" 与 `-O` 下守卫消失冲突；工具仍会静默写 deps.json（:201）→ 静默数据漂移。同意 R1–R12 共识：运行时正确性守卫而非文案 → P2 |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 `f"{len(mismatches)} source files changed..."` 无单复数区分 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立**（本复核扩充 F161） | `grep rp.repo/rp.backup` 0 命中；read() 被 11 个 G4 checker 消费（21 处 `rp.read(` 实证）；**write() 亦 0 生产调用方——前十二轮遗漏，见新 F161** |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 `except ContractError: read_files, write_files = [], []` 无日志；对比 :74-75 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `issubclass(subprocess.TimeoutExpired, OSError)==False`（mro: TimeoutExpired→SubprocessError→Exception）；:94 `except (json.JSONDecodeError, ValueError, OSError)` 漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有 |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **独立实跑真实解析器**：`load_applicability(worldbuilding)` → `{}`；`filter_dimensions_by_test_type(dims, p, 'bug-hunt')` → 返回全部 9 维（**含 N/A 豁免的 dim 4 Prose quality**）；worldbuilding rubric:43 明示 "N/A — exempted … scoring.py renormalizes weights for remaining applicable dimensions"；bug-hunt/clean/generative 测试目录存在（ls 实证）；command-to-give.md:64,71 文档化路径。对照组 chapter-drafting 过滤确实生效（[1,2,3,4,5,8,10]）。**反证尝试失败**。按决策表"正常路径可复现功能错误"维持 P1 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径 G2 FAIL → return 1 → 双入口语义不一致 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑（:337） |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 :58 已开 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-110 G4 bug-hunt/clean 分支不写标记（仅 :121 generative 写）；:128 G6 恒写 "generative"；shared.py:195 status≠PASS 不写；scoring.py:200/:220 读侧按 `-{test_type}` 拼接 |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 `[...][:20]` 实证；rglob 顺序 OS 相关 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `state["steps"].append(...)`；其余 5 命令均记录 |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS src/ tools/` → 仅自身 + 测试；0 生产消费者 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；缺参 → IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | 实证：`find . -name "*dispatch-subagent*"`（排除 .git）→ 0 命中；ADR-0009 已迁移 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | **实跑实证**：不带 --subagent 的文档化评分命令输出 `"scored_by": "interactive"`（见 F140 验证输出） |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用；GateStatus.WARN 由 gates/g4 消费、CommandStatus.EXISTS 由 pipeline/cli.py 消费——均非死成员） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均通过 → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫（对照 :300 `gate_type` 有 `idx+1 < len` 守卫） |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers :187-224 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 status |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立（维持 P2）** | :145-159 只查空 scores 不查空 dims；:169-170 total_weight==0 → 0。触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析——本复核全量复跑确认仅 `_template` weight≠100 为占位符良性），缺"正常路径可复现"，维持 P2 |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 NaN 字面量 → NaN 通过校验并产出 NaN final |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src/ → 仅 status.py:78 定义，0 消费（phase_runner 全裸 dict emit） |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志；g4/generic.py:46-47 空文件 → SKIP 后 `passed(...)`（vacuous PASS，且写 PASS marker → pre-score 假通过） |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | 重读两文件：除 import 行外无任何 `log.` 调用 |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块（ls 实证） |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **独立实跑 `.venv/bin/shenbi-score`**：完整 10 维 scores + `--test-type generative` → `final_score: 79.75 / PASS (acceptable)` 完全成功但 **EXIT CODE: 1**，stderr 1191 字节为完整 result dict repr（`sys.exit(main())` → `SystemExit(dict)` → exit 1）；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达。**反证尝试失败**（`scoring.main` 无库调用方——grep 全 src 实证） |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | `isinstance(True, (int, float))` → True；`True < 0 or True > 100` 恒 False → 校验通过 → 按 1 计分 |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | filter docstring :107-109 宣称 "renormalize weights" 但 :135-136 仅过滤；compute_score :171-172 以 total_weight≠100 发 warning；实测 chapter-drafting bug-hunt 过滤后权重和 = 75 |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过，无 json.loads、无 crash）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/缺 state 键/非法 JSON → 裸 TypeError/KeyError/JSONDecodeError traceback |
| F145 | phase_runner `load_deps()` 无任何错误处理 | M | **成立** | :32-33 `json.loads(...)` 无 try；:245/:318 调用；对照 g5.py:36-39 try/except → 结构化 FAIL。定 M 恰当（fail-loud、无部分写入） |
| F146 | scoring.check_gate_markers t2 分支对 deps.json 仅 exists() 守卫 | M | **成立** | :205-207 `if deps_path.exists(): deps = json.loads(...)`——exists() 只防缺失不防损坏 |
| F147 | sync_contracts.main() deps.json 读取（:195）与 load_registry（:165）均无守卫 | M | **成立** | :195 `json.loads(DEPS_PATH.read_text(...))` 无 try；:165 `load_registry()` ContractError 裸上抛；对比同文件 :167-172 bail 有 log.error + return 1 |
| F148 | phase_runner post-skill 契约声明但磁盘缺失/为空的输出静默丢弃 | P2 | **成立** | :196-200 `exists()+st_size>0` 静默过滤缺失项无日志；:211-216 空 output_files → G2 SKIP + G4 空文件 vacuous PASS + PASS marker → 相位 OK 推进 |
| F149 | scoring filter 对无维度数字的排除 scope 静默 no-op（模板自身触发） | P2 | **成立** | **独立复跑**：`_template/rubric.md` "Prose/narrative quality" 行标 No 但 scope 无维度数字 → excluded_nums 恒空 → no-op（过滤前后均 [1,2]）；本复核全量扫描 44 份 Dimension-scope rubric 确认仅 _template 触发 |
| F150 | phase_runner run_gate("G2") 缺第 4 参 project_dir → G2.7 重要章节 ceiling 恒失效 | M | **成立；严重度异议 M→P2（见 §3）** | :213 3 参；gates/cli.py:60-63 G2 支持 `pd = arg(3, None)`；g2.py:237 `_is_important_chapter(fp, project_dir or "")` + :382-384 `if not project_dir: return False`；executor.py:25-26 `PROJECT_DIR = REPO_ROOT`、:134 传 4 参——两路径均非真实项目目录 → `_is_important_chapter` 恒 False → 重要章节 ceiling 恒 4500（floor×1.5）而非 10000 |
| F151 | scoring.py:419 scores.json 非数字键静默丢弃且无 WARNING | M | **成立** | :417-419 `{int(k): v for k, v in raw.items() if k.lstrip("-").isdigit()}`——非数字键在 validate_scores 之前被静默过滤 |
| F152 | load_rubric kill-switch 解析漏 "→ detection dimension = 0" 类条目 | M | **成立** | :43-47 只匹配 "total score = 0"/"phase = 0"/"pipeline = 0"；worldbuilding 等 12+ 份 rubric 的 `- Evidence without file+line citation → detection dimension = 0` 不被捕获 |
| F153 | sync_contracts.render_body_view 的 `skill` 参数从未使用（死参数） | M | **成立** | :124 定义 + :149 传参外，函数体内 `skill` 0 命中 |
| F154 | safe_write flock 主路径与 O_EXCL 回退路径互不感知对方锁原语 | M | **成立**（触发面窄，维持 M） | :54-62 flock 分支从不检查 lockfile 存在性；:63-91 O_EXCL 分支从不检查目录 flock 状态；:61 `except (ImportError, OSError): pass` 为唯一切换点——两原语互斥语义不共享 |
| F155 | scoring.check_scorer_agreement NaN 误报 agreed；flag_score_collapse 单维度误报 all_identical | M | **成立** | `diff = abs(a - b)` 对 NaN 恒 False → NaN 永不进 disputed；`len(set(values)) == 1` 无 `len(values) >= 2` 守卫。函数当前 dead（Z5 F500），接线前置缺陷 |
| F156 | phase_runner/executor 双路径对混合输出施加单一 derive_file_type → G2 decisions 分支整体跳过 .md | P2 | **成立** | **独立实证**：`derive_file_type('shenbi-chapter-drafting')` → `decisions`；`derive_output_files('shenbi-chapter-drafting', 5)` → `['chapters/chapter-5.md', 'chapters/chapter-5-decisions.json']`；全仓 G2 调用站点仅 2 处（executor.py:200、phase_runner.py:213）均单次单类型；g2.py:84 decisions 分支 `if not fp.endswith(".json"): continue` → .md 仅过 G2.1-2.3；受害技能 3 个（chapter-drafting/chapter-revision/short-drafting，本复核全量 derive 实证） |
| F157 | scoring 交互模式 `input()` 提示词写入 stdout 数据通道 | M | **成立** | :388/:397 `input()` prompt 经 sys.stdout 输出；:462 emit_json 追加同一 stdout → 混合体不可 JSON 解析 |
| F158 | phase_runner phase 参数路径穿越 | **P1** | **成立（P1 维持，无反证；决策表张力见 §3）** | **端到端实跑**：`start ../../../../tmp/zb13_escaped --round-dir /tmp/zb13_r` → G5 FAIL（exit 1）但 **`/tmp/zb13_escaped.json` 被创建**（G5-FAIL 分支 :126-129 无条件 save_state；`find /tmp/zb13_r -type f` 0 个——写入完全逃逸 round_dir）。路径拼接 :37/:46-47 + 六命令直接透传 `args[0]` 无白名单 |
| F159 | sync_contracts 从不调用 configure_logging()：日志走 stdout + SHENBI_LOG_FORMAT 失效 | M | **成立** | **独立实证**：真实导入 `log.info('sync_complete', skills=74)` → 捕获 stdout 有日志、stderr 空；`grep configure_logging src/shenbi` → sync_contracts 唯一缺失（对比 phase_runner:337/scoring:283/gates/cli:57/dispatcher/cli:19） |
| F160 | cli_utils.emit_json 不处理 BrokenPipeError | M | **成立** | :20-22 `sys.stdout.write` + `flush()` 无任何 except；emit_json 是全部 CLI stdout 数据通道唯一出口 |

**前十二轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 **确认 R3 修正正确**；F101 波及文件数递增记录属实（本复核时点两文件均 100644/0600 实证）；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致（`ls tests/tiers/t2-phase/` 9 目录实证）。**无新的前轮事实错误需要修正。**

---

## 2. 新发现（F161–F162）

F161 | RoundPaths.write() 无任何生产调用方（死代码）：F112 只覆盖了 repo()/backup()，write() 为同一家族第三个遗漏成员 | dead-wire/未接线 | **P2**（与 F112 同族同级）
- 证据: src/shenbi/paths.py:26-28（`def write(self, rel, chapter=None) -> Path`，docstring 语义 "write() 恒 round_dir"）；全仓 `grep -rn "\.write(" src/ tools/ --include="*.py"` → RoundPaths 实例上 **0 命中**（唯一形似者为 cli_utils/emit_json、pipeline/snapshot_diff 的 `f.write`、skill_utils 的 `sys.stderr.write`，均非 rp.write）；`grep -rhn "rp\.\(read\|write\|repo\|backup\)(" src/shenbi/gates/g4/*.py` → `21 read / 0 write / 0 repo / 0 backup`
- 根因: RoundPaths 设计含读/写/备份/仓库四访问器，但 G4 checkers 只消费 read()（21 处）；write() 仅被同族死方法 backup()（paths.py:33-35，`w = self.write(rel, chapter)`）与单元测试 test_round_paths.py:31-35 触达——生产路径零调用。前十二轮 F112 及其后续复核只核对了 repo()/backup()（R1 起 "同族 backup() 亦 0 调用方"），**write() 从未被点名**
- 验证命令+输出: `grep -rhn "rp\.\(read\|write\|repo\|backup\)(" src/shenbi/gates/g4/*.py | sed -E 's/.*rp\.(read|write|repo|backup)\(.*/\1/' | sort | uniq -c` → `21 read`（write/repo/backup 均 0）；`grep -rn "RoundPaths" src/ tools/ --include="*.py" | grep -v paths.py | wc -l` → 33（11 个 G4 checker 实例化，仅调用 read）；`grep -rn "write()" docs/superpowers/audit-runs/2026-08-14/zone-reports/Z1*.md` → 仅 Z1.a.md:114-115 的不变量描述（"write() 恒 round_dir ✓"），无任何一轮声明 write() 无调用方
- 建议方向: 与 F112 一并处置——删除 write()/repo()/backup() 三死方法（保留 read()），或为 write() 接线（若计划供 phase_runner/executor 写产出使用）并补生产消费测试

F162 | phase_runner cmd_post_skill 冗余调用 configure_logging()：与 main() 重复配置，为六命令中唯一命令内再配置站点 | dead-wire/冗余 | **M**
- 证据: src/shenbi/phase_runner.py:185（`def cmd_post_skill(...):` 首行 `configure_logging()`）+ :337（`main()` 首行 `configure_logging()`——所有命令必经 main 配置后再分发）；对照其余 5 个命令（cmd_start/pre_skill/pre_score/post_score/finalize）均无命令内 configure_logging；grep 全仓 `configure_logging` → 各 CLI 入口仅 main() 一处（phase_runner:337/scoring:283/gates/cli:57/dispatcher/cli:19），cmd_post_skill:185 是唯一函数级重复
- 根因: cmd_post_skill 迁移/重构时残留的入口级配置（可能源于该函数早期可独立调用的历史），main() 分发制下冗余；`cache_logger_on_first_use=False`（logging.py:41）使重复 configure 幂等无行为差异，故无功能危害，属死代码/冗余（与 F138 未用 log 绑定、F153 死参数同族）
- 验证命令+输出: `grep -n "configure_logging" src/shenbi/phase_runner.py` → `:22（import）、:185、:337`；`grep -rn "configure_logging()" src/shenbi/*.py` → 仅 phase_runner.py 有函数内重复（其余各文件均 main/入口一处）；行为模拟：两处调用参数一致（均读同一 SHENBI_LOG_FORMAT），structlog.configure 幂等 → 无输出差异
- 建议方向: 删除 :185 行（main() 已配置）；若保留函数独立可用性意图，改 docstring 注明并保持幂等语义

---

## 3. 严重度异议汇总

| 编号 | 前十二轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F150 | M（R9 定级） | **异议：M → P2** | R9/R11 的影响描述（"当前 G2 FAIL 不阻断（F116）故直接危害为 g2_status 元数据失真；若 F116 修复为阻断，长重要章节将误 FAIL"）**只适用于 phase_runner 路径**——dispatcher/executor.py:200-204 在**非 pipeline 模式当下即阻断**（`if g2.get("status") != "PASS": log.error(...); return 1`），且 executor run_g2（:122-134）传 `str(PROJECT_DIR)`=`REPO_ROOT`（:25-26 实证）→ `_is_important_chapter`（g2.py:237,382-384）查 `REPO_ROOT/outline/volume_map.md` 恒不存在 → **恒 False → 重要章节 ceiling 恒 4500**。即：被 volume_map.md 标注为爆发段/高潮/卷首/卷末（设计上应享 CHAPTER_WORD_CEILING=10000 上限）的章节，一旦 4500-10000 字，经 dispatcher 路径**当下**即 G2.7 FAIL → dispatch return 1（真实功能阻断，非元数据失真）。volume_map 重要章节标注是文档化特性（g2.py:374-377 注释），机制整体失效属"边界/错误处理缺陷 + 文档化特性静默失效"，按决策表 P2；受害触发需 4500-10000 字章节（T1 fixture 通常更短），缺"正常路径可复现"故不议 P1。注：本复核同时确认 4 个 chapter-typed 技能（anti-detect/length-normalizing/sequel-writing/style-polishing）写 chapters/chapter-N.md 可达该分支 |
| F158 | P1（R12 定级） | **维持 P1（附决策表张力说明，不构成降级异议）** | 决策表 P0 例举含"路径穿越"，F158 端到端实证可写出 round_dir 外任意 JSON（含覆盖既有文件）——按字面可议 P0；但触发需调用方**主动传入**路径形态 phase（CLI argv 由操作者控制，无不可信输入边界；正常管线恒用 deps.json 固定 9 相位名），非攻击面穿越。P1 与"合并层可议 P0"的定位合理，本复核不异议 |
| F115 | P1 | **维持 P1（无反证）** | 独立实跑真实解析器：worldbuilding applicability `{}`、filter 返回全 9 维（含 N/A 豁免 dim 4，10% 权重）、rubric:43 明示 renormalize 承诺、4 技能均有 bug-hunt/clean 测试目录、command-to-give.md:64,71 文档化路径。**反证尝试失败** |
| F140 | P1 | **维持 P1（无反证）** | 独立实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr 1191 字节 result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达；`scoring.main` 无库调用方。**反证尝试失败** |
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有共识）** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移。按决策表 P2"防御深度"而非 M"文案" |
| F134 | P2 | **维持 P2（不议升）** | 触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析——本复核全量复跑确认），缺"正常路径可复现" |

其余 54 条严重度无异议（F101–F108/F110–F114/F116–F121/F123–F125/F128–F129/F133/F135/F137/F141/F144/F148/F149/F156/F161 的 P2 与 F110/F111/F122/F126/F127/F130/F131/F132/F136/F138/F139/F142/F143/F145/F146/F147/F151/F152/F153/F154/F155/F157/F159/F160/F162 的 M 均认可）。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 全部独立通读；另核实跨区依赖：contracts/legacy.py、contracts/schemas/deps.py、gates/cli.py、gates/shared.py、gates/g2.py、gates/g5.py、gates/gate_manifest.py、audit/_shared.py、dispatcher/executor.py、tools/lint_status_strings.py、tests/tiers/deps.json（含 `_tool_hashes`/`_out_of_pipeline`/`t3-pipelines`）、82 份 rubric、command-to-give.md）
- 判定无新缺陷的文件：`__init__.py`（F111/F139 覆盖）、`capability_fs.py`（权限模型六方法核实无洞；F123 覆盖无接线；`resolve(strict=False)` 符号链接环 RuntimeError 未捕获为 R11 备查已记录项）、`cli_utils.py`（F160 覆盖唯一剩余分支；allow_nan 为 R11 备查已记录）、`error_guidance.py`/`recovery.py`（F102/F103/F138 覆盖）、`exceptions.py`（F103/F110 覆盖；AST 全仓 raise 扫描独立复核 5/22 RAISED）、`logging.py`（双渲染器/stderr 自洽；SHENBI_LOG_FORMAT 校验为前轮已记录低价值项）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（F127/F136 覆盖；STATUS_STRING_LITERALS 由 tools/lint_status_strings.py:21 消费非死成员——本复核核实）、`safe_write.py`（F101/F108/F119/F133/F154 覆盖；剩余分支为防御/平台边界）、`scoring.py`（F104/F106/F115/F118/F120/F126/F129/F131/F134/F135/F140/F141/F142/F146/F149/F151/F152/F155/F157 覆盖）、`phase_runner.py`（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150/F156/F158 覆盖，另 F162 本轮新增）、`sync_contracts.py`（F109/F117/F130/F147/F153/F159 覆盖）、`paths.py`（F112 + F161 本轮新增）
- 新发现分布：`paths.py`（F161——write() 死代码）、`phase_runner.py`（F162——冗余 configure_logging）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **F156 的 truth/report 混合类型变体未单独立 finding**：report-typed 技能写 truth/*.md（如 shenbi-review-arc-payoff 写 truth/audit_drift.md、truth/arc_payoff_trend.md）→ file_type="report" 时 G2.11（truth .bak 删除比较）不运行；truth-typed 技能写非 truth .md（如 shenbi-relationship-map 写 characters/relationships.md）→ G2.6-2.9（字数/PRE/POST 检查）不运行——与 F156 同一根因（单一 derive_file_type 施加于异构输出），非独立站点，并入 F156 修复范围。
2. **phase_runner/scoring main() 多余位置参数静默忽略**（`start genesis --round-dir /tmp/r extra` / `scoring.py rubric.md scores.json extra`）：F124（缺参 IndexError）的镜像（多参静默忽略），M 级 CLI 校验空洞，前十二轮未点名，本复核并入 F124 家族注记不单独立 finding。
3. **load_rubric 分隔行空格形态**（`| --- |` 而非 `|---`）会静默跳过整个维度表：本复核全量扫描 82 份 rubric 0 触发（均 `|---`），纯潜在格式风险（F149 家族）。
4. **sync_contracts 仅处理 t2-phases**：本复核核实 deps.json 的 t3-pipelines（long-form/short-form/import-form）**无 expected_outputs 字段**（仅 prerequisites/min_chapter_ratio），故非漂移缺口，不立 finding。
5. **`_tool_hashes` 非死结构**：G0.14（gates/g0.py:104-137）校验，与 F102 的 ToolTamper 引导目录互为验证；`_out_of_pipeline` 为 T1 独占技能说明数据，均无新问题。
6. **R1 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖（本复核独立确认）。
7. **AGENTS.md "67 functional + 2 meta = 69" 与现状 74 个 skills 目录不符**（R7 备查 #7）：非 Z1 区，供合并层转交。

---

## 5. 其他说明

- 前十二轮"P1×3（F115/F140/F158）"结论：本复核独立实跑/复跑/端到端实证后**无反证，维持**；误报数 0（60/60 成立）；无任何前轮 finding 被证伪。
- 新发现 F161–F162 均为低危扫尾项（死代码/冗余），不改变"无 P0"结论，也不新增 P1。至此 Z1 区十三轮共 62 条 finding（F101–F162）：P1×3、P2×24、M×35（含 F109 议升 P2、F150 议升 P2 未定）。
- 跨区一致性核查：F161 与 F112（repo/backup 死代码）同族补全；F162 与 F159（sync_contracts 缺 configure）互为镜像（一处多配、一处缺配），均属"CLI 入口日志配置一致性"家族；与 Z5 F500（scoring_bridge dead 链）无重叠；与 Z3 区 safe_write 消费方缺陷无重叠。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140/F126 经**实跑 `.venv/bin/shenbi-score`**实证退出码与 scored_by；F115/F134/F135/F141/F142/F149/F151/F152/F155 经 `PYTHONPATH=src` 纯内存导入复跑；F156 经 `derive_file_type`/`derive_output_files` 真实函数全量实证；F158 经**端到端实跑 phase_runner CLI（写目标仅限 /tmp）**实证穿越写出；F159 经真实模块导入捕获 stdout/stderr 实证；F101/F102/F125 经 git/ls/find 实证；临时文件仅写 /tmp，未触碰仓库文件。

---

## 6. 返回摘要

- **复核统计：新 finding 2（F161 P2 / F162 M）/ 误报数 0 / 严重度异议 1 新增（F150 M→P2；另维持 F109 M→P2 共识与 F115/F140/F158 的 P1）**
- **0 新 finding 判定：否**（2 条新漏报：F161 paths.py `RoundPaths.write()` 无生产调用方（F112 只覆盖 repo()/backup()，write() 为同一家族第三个死方法，全仓 `rp.write` 0 命中实证）；F162 phase_runner cmd_post_skill:185 冗余 `configure_logging()`（与 main():337 重复，六命令中唯一函数级重复配置）。均为低危死代码/冗余扫尾，无新 P0/P1；Z1 主要缺陷面（safe_write 并发家族、scoring 校验全链、phase_runner 状态机/CLI、sync_contracts 生成链、加载边界家族、路径穿越）已由前十二轮 + 本轮收敛）
