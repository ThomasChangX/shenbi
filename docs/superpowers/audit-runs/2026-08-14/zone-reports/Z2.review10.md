# Z2 分区第十轮独立复核报告（Z2.review10）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）+ `Z2.review9.md`（F253-F255）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖，未依赖前九轮转述）；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖；未覆盖列表：空）；另读跨区消费面验证裁决（gates/{cli,g0,g1,g2,g3,g3_independence,shared,g_dispatch,g_reconcile}.py、g4/{genre_config,pacing_design}.py、audit/{_shared,snapshot,write_audit,record}.py、records/{parser,drift}.py、scoring.py、safe_write.py、cli_utils.py、paths.py、phase_runner.py、pipeline/{dispatch_helper,parallel_dispatch,triggers,chapter_loop,closure}.py、round-exec.sh、first-novel.md、AGENTS.md、docs/framework/decisions-schema.md、git show 99f91f0:tests/dispatch-subagent.sh）。
2. **独立验证方式**：`pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）；20+ 个 `python -c`/heredoc 运行时探针（F216/F218/F215/F235/F238/F245/F247/F252/F255 复现、F227 原 shell 门序证据、G2.10/G2.12 范围探针）。
3. **前九轮判定**：F201-F255 共 **55 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均复核通过；已有 8 条严重度异议（F214/F218→P1、F217/F228/F229/F238/F251→P1 低置信、F220 维持 P2）经独立反证尝试后**全部支持**；本轮**不新增严重度异议**。
4. **本轮新发现 2 条（F256-F257）+ M 级观察 4 条**，均为 M 级（注释/死参数级），集中于前九轮未触及的两处极细碎面：
   - **F256（M）**：g2.py G2.10/G2.12 注释声明 "chapter files only"，代码对**所有 file_type**（truth/report/json）无条件生效——truth 文件含 >10% 占位符行被 G2.10 FAIL；被误归 "chapter" 的 JSON（F229 面）系统性收到 G2.12 "may be truncated" WARN（"}" 不在句末符集合）；
   - **F257（M）**：executor.dispatch 的 `chapter` 关键字参数全仓零调用方（死参数），dispatch_with_write_audit 另行独立二次 extract_chapter。

---

## 一、复核结论表（F201-F255 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 重读三模型（chapter_planning/context_composing/volume_outlining）+ g4 私有实现（g4/chapter_planning.py:99-102 hook ops 存在性 vs 模型 typed change/defer 激活计划；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力 vs 模型 entity_hooks>=3 且 types>=2）；grep 无生产消费者（仅 test_skill_integration.py:188 类名文本）。双源已发散。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads（跨区 read 确认）。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g6.py:51 与 g4/worldbuilding.py 以 raw dict 读 target_word_count/target_words——死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证；g4/score_*.py 用 RouteC/RouteA 正则检查）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无生产加载点（g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797（"BLOCKING"/"FAIL"）、:409-438（G4Severity）本轮 read 复核通过。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 GateStatus 实有，read 确认）。 |
| F210 | P2 | **成立** | hooks.py 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing（复测 74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0 → 修复方向按"CLI 崩溃"修正，本体成立。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实（read 复证）。 |
| F214 | P2 | **成立；支持异议→建议 P1** | **本轮 read 复证 + 新机制细节**：run_gate_g3（dispatch_helper.py:1934-1945）仅在 progress.json **缺失**时伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行。**增补细节**：伪造恰在派发失败路径触发（G1/G2/codex/评分任一失败 → _record_completion 未执行 → progress.json 不存在 → 伪造 → G3.4 PASS 掩盖失败）；而派发**成功**路径 _record_completion 已建 progress.json（无 current_scorer_agent）→ run_gate_g3 不再伪造 → G3.4 对成功派发反而 FAIL。反证（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。 |
| F215 | P2 | **成立** | **运行时复现**：`\{[^{}]*\}` 对 `{"final_score":90,"dimensions":[{"num":1,"score":88}]}` 取内层 `{"num":1,"score":88}`。 |
| F216 | P1 | **成立，P1 维持** | **运行时复现**：空 customRules + 1 disabled dim（genre_config.py:94 `if disabled and self.custom_rules:`）→ `GenreConfig.model_validate` PASS；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。P1 维持。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | SKILL.md 内 7 处 CONSTELLATION 区间 + docstring 20-30 + 模型 [15,35]（read 复证）；权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | **运行时复现**：部分匹配（声明 ["当前任务","不存在的字段"]，文件 3 节）→ matched=True、仅返回 "当前任务" 节（世界设定/活跃线索静默丢弃）、无 WARN；全缺 → matched=False + 全文 + `field_filter_no_match` WARN。AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282 read 复证）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | P2 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证。矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，建议维持 P2（不降 M）。 |
| F221 | P2 | **成立** | fixture 9 键含 tropeInventory + SKILL.md:286/:292 "恰好 8 个" + OWNERSHIP _GENRE_KEYS 9 键 + 模型 8 字段（grep 复证）；`extra=ignore` 静默丢弃 tropeInventory。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592（read 复证）。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise；cli main 无 try/except（read 复证）。 |
| F227 | P1 | **成立，P1 维持** | **原 shell 证据复证**：`git show 99f91f0:tests/dispatch-subagent.sh:81-82` 注释 "G2: Output validation — verifies the skill's output files exist... (the skill execution happened earlier)"；Python 翻译（executor.py:190-214）把技能执行并入 G2 之后的 dispatch_codex（codex.py:61-66）→ fresh 轮（round-exec.sh create-mode 只建 progress/meta/summary + 子目录，无 novel.json）G1.1 not found；**运行时复现**：fresh tmp 轮 chapter-drafting → G1 FAIL（6 输入 G1.1）；worldbuilding → G1 FAIL（novel.json G1.1）。反证不成立。P1 维持。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | derive_output_files（audit/_shared.py:38-56）不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found（read 复证 + F227 探针内联）。受 F227 掩盖维持低置信。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | **运行时复现**：`derive_file_type("shenbi-chapter-drafting") == "decisions"`；g2.py:83-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | P2 | **成立** | **运行时复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + WARN；`_filter_json:76` 裸 `k in fields`。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者（复证）。 |
| F232 | P2 | **成立** | **运行时复现**：`Selection(severity="high", basis="manual_override", rationale="")` → ACCEPTED；`Adjustment(rationale="   ")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | P2 | **成立** | snapshot.py:99-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。**补证**：file-level 兜底分支（write_audit.py:62-64）对删除同样不判——`_matches_declared` 只查路径声明不查存在性，删除任何 declared 文件（含非 owned）均零违规（F234 机制面扩展）。 |
| F235 | P1 | **成立，P1 维持** | **运行时端到端复现**：`dispatch_with_write_audit("shenbi-worldbuilding", ..., "Build the world")`（真实 REPO_ROOT、fake dispatch rc=0）→ ledger violations=['未声明写入: truth/bridge_tracker.md', '未声明写入: truth/character_matrix.md']；`git ls-files truth/` 确认两文件 tracked；codex.py:62 cwd=round_dir 而审计根未跟随；test_executor_audit.py:18,40 编码 PROJECT_DIR==round_dir 正确语义。反证不成立。P1 维持。 |
| F236 | P2 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs（parametric_globs()），registry globs（`truth/*.md`）恒判未声明（read 复证 + F235 探针内联）。 |
| F237 | P2 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | P2 | **成立；支持 review6 低置信异议（P2→P1）** | **运行时复现**：`audit_writes` 对 malformed `## hooks` YAML → `ParserError while parsing a flow sequence` 裸崩溃；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行（write-audit.jsonl 无 GATE_FAIL）——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | P2 | **成立** | **运行时复现**：derive_file_type worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"；state-settling → "truth"、chapter-drafting → "decisions"（executor.py:81-85 精确字符串交集）。 |
| F240 | P2 | **成立** | `extract_chapter("写第7章，主角觉醒") == None`（read 复证 + 前轮复现）；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | P2 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`；plant/track SKILL.md 自述 DEPRECATED "Do not dispatch"（grep 复证）。 |
| F243 | P2 | **成立（机制修正维持）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（原第 4 参为独立评分 prompt）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。机制修正（review8）维持：gate-marker 强制 exit 3 先于分数校验 exit 2。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:289-313 G2.11 对 **truth 输出**找 .bak（不存在则静默无检查）；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | P2 | **成立** | **运行时复现**：resolve_chapter_path("audits/volume-N-payoff.md", 7) → `audits/volume-7-payoff.md`；arc-N+7 → `arc-7.md`；AC-NNN+7 → `AC-007.md`；resolve_or_skip(volume-N, None) → None；resolve_volume_path 仅 closure.py:20,158 消费（grep 复证）。 |
| F246 | P2 | **成立** | dispatch_skill（dispatch_helper.py:1826-1865）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径；rd = round_dir or project_dir → project_dir 含 pipeline-state.json → is_pipeline → G2 跳过但 G1 仍跑）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | P1 | **成立，P1 维持（范围注记维持）** | **运行时端到端复现**：`_declared_patterns("shenbi-chapter-drafting") == []` vs `_audit_watch_paths("shenbi-chapter-drafting", 7) == ['chapters/chapter-7.md','chapters/chapter-7-decisions.json']`；`dispatch_with_write_audit(chapter-drafting, "Draft chapter 7")`（fake codex rc=0）→ ledger violations=['未声明写入: chapters/chapter-7-decisions.json', '未声明写入: chapters/chapter-7.md']。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | P2 | **成立** | `PacingDesign.from_markdown` chapter_sequence 恒空（pacing_design.py:129）→ `_no_three_consecutive_same`（:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）致误拒/虚增（read 复证）。 |
| F249 | P2 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500（read 复证 + g2.py:237-238 read）。 |
| F250 | P2 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 pipeline dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | P2 | **成立；支持 review9 低置信异议（P2→P1）** | 命名面：codex.py:56 `-scores-subagent.json` vs find_report（shared.py:157-166）三模式 / g0.py:440 G0.10 glob `*-generative-scores.json`（read 复证）；内容面：raw 维度 dict 无 final_score；状态面：codex.py:44 "done" vs g_reconcile.py:40,61 "DONE" → GR.2 恒 FAIL（read 复证）。受 F252 前置掩盖维持低置信。 |
| F252 | P2 | **成立** | **运行时复现**：`python -m shenbi.scoring tests/tiers/t1-skill/shenbi-worldbuilding/rubric.md <scores> --test-type generative --round-dir <round> --subagent` → `rc=3`、`{"status":"MARKER_MISSING","missing_markers":["G4-shenbi-worldbuilding-generative"]}`；executor 编排（:190-214）只跑 G1/G2 无 G4；grep write_gate_marker dispatcher/ → 空。 |
| F253 | P2 | **成立** | codex.py:27-48 `_record_completion` 读-改-写整体无锁（safe_write flock 仅覆盖写入）；pipeline legacy 并行（parallel_dispatch.py:168-171 Semaphore(4) 共享 project_dir）触发面真实（read 复证 + 前轮交错复现）。 |
| F254 | P2 | **成立** | run_g1/run_g2（executor.py:114-118/:125-137）与 shenbi-score 子进程（codex.py:95-109）均无 timeout；仅 codex exec 有 600s（codex.py:63）；`grep -n timeout src/shenbi/dispatcher/` → 仅 codex.py:63（read 复证）。 |
| F255 | P2 | **成立** | **运行时复现**：derive_file_type 对 4 个 report-kind truth 写者（score-volume/score-arc/score-stratum/review-arc-payoff）全 → "report"（executor.py:77-78 REPORT 分支先于 truth 交集）→ G2.11（g2.py:289-290 仅 file_type=="truth"）对其静默失效。 |

**误报统计**：整体误报 **0 条**（F201-F255 共 55 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F256-F257）

### F256 | g2.py G2.10/G2.12 注释声明 "chapter files only"，代码对全部 file_type 无条件生效——truth 文件占位符行误拒 + JSON 系统性假 WARN | 漏报（注释↔代码漂移） | M
- 证据: `src/shenbi/gates/g2.py:273`（`# G2.10 — template placeholder detection (10% threshold, chapter files only)`）、`:315`（`# G2.12 — file completeness (sentence-final punctuation, chapter files only)`）；但 G2.10（:274-287）与 G2.12（:316-322）位于 `for fp in fps:` 循环内、`if file_type == "chapter":` 块（:218-271）**之外**——对 truth/report/chapter 任一 file_type 的每个非 decisions 文件无条件执行
- 根因: 检查范围注释与实现脱节；且句末符集合（g2.py:317 `("。","！","？","…","」","』",'"',"）",")","---")`）不含 `}`/`]` → 任何被归 "chapter" 的 JSON 输出（genre-config.json/novel.json——F229(a) 误分类面）`last` 为 `}` → `ends_ok=False` → 恒 WARN "may be truncated"（系统性噪音）；truth 文件在含 >10% "待填充" 行的中间态被 G2.10 FAIL（语义或可辩护，但与注释声明范围冲突）
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `gate_G2([truth/state.md(6/11 行待填充)], "truth", tmp)` → `status=FAIL must_fix=['G2.10:.../truth/state.md']`；`gate_G2([cfg.json('{"a": 1}')], "chapter", tmp)` → checks 含 `G2.12 WARN 'may be truncated'`；`gate_G2([audits/x.md], "report", tmp)` → 同 G2.12 WARN
- 建议方向: G2.10/G2.12 移入 `if file_type == "chapter":` 块（与注释一致），或修正注释为全类型并给 G2.12 加 JSON 分支（仅 .md 检查句末符）；与 F229 修复联动（JSON 不再被归 chapter 后噪音自消）

### F257 | executor.dispatch 的 `chapter` 关键字参数全仓零调用方（死参数）；dispatch_with_write_audit 另行独立二次 extract_chapter | 漏报（死参数/未接线） | M
- 证据: `src/shenbi/dispatcher/executor.py:156`（`def dispatch(..., *, chapter: int | None = None)`）；全仓调用点 `cli.py:29` 与 `executor.py:250` 均不传 chapter（`grep -rn "dispatch(skill" src/` 复证）；`dispatch_with_write_audit`（:242）另行 `chapter = extract_chapter(prompt)` 供审计 watch——两条独立提取路径
- 根因: PR-20 翻译时保留了原 shell 未有的显式 chapter 参数面，但无调用方接线；参数语义（显式章节覆盖 prompt 提取）与 audit 侧不一致（audit 永远走 prompt 提取）
- 验证命令: `grep -rn "dispatch(" src/ --include=*.py` → 仅 cli.py:29、executor.py:250（均无 chapter kwarg）；read executor.py:155-162
- 建议方向: 删除死参数（或让 dispatch_with_write_audit 把提取结果传入，消灭双提取）；属 F240/F245 修复时顺手清理项

### M 级观察（随文记录，均非 finding）
- `phase_runner.py:211` `run_gate("G2", [",".join(output_files), file_type, str(round_dir)])` 不传 project_dir（G2 CLI 第 4 参）→ `_is_important_chapter(fp, "")` 恒 False → 重要章上限 4500（F249 家族第三实例；review7 M 只列了 g3.py:177，phase_runner 实例未列）（M，F249 增补）
- `codex.py:58` `raw_out = scores_file.with_suffix(".raw")` 与原 shell `RAW_OUT="${SCORES_FILE}.raw"`（追加式 `...json.raw`）命名不同——Python 用后缀替换（`...subagent.raw`）；且 Python 无 `rm -f "${RAW_OUT}"`（原 shell 有，git show 复证）→ raw 遗留（review4 M 已列遗留，命名差异为新增细节）（M）
- `gates/g2.py:317` 句末符集合缺 `}`/`]` → 任何 JSON 在 chapter/report/truth file_type 下恒 G2.12 WARN（F256 机制面；若 F256 修复为 .md-only 则自然消解）（M，F256 关联）
- `round-exec.sh` create-mode 初始 progress.json 仅 completed_skill_names/skills/tier/expected_chapters，无 current_scorer_agent/scoring_history（review5 M 已列；本轮在 F214 机制细节下复核：成功派发后 _record_completion 仍不补 scorer → G3.4 对 standalone 轮必 FAIL）（M，F214 关联）

---

## 三、严重度异议（不修改前九轮，仅提理由；本轮不新增异议）

1. **支持 F214 P2 → P1**：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → G3.5 恒 PASS）→ G3.4 fail-closed 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"显式契约。**本轮增补机制细节**：伪造恰在派发失败路径触发（_record_completion 未执行 → progress.json 缺失），成功路径反而因真实 progress.json 无 scorer 而 G3.4 FAIL——伪造不仅使独立性门失效，还**掩盖失败派发**。反证（独立路径不跑 G3 故 P2）不成立。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持 F218 P2 → P1**：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 实测：matched=True、仅命中 section、无 WARN）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40）——SKILL.md 内部 7 处自相矛盾，权威区间未定，维持低置信。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛（本轮 ParserError 复现）发生在 finally 内 → record_audit_outcome 永不执行（审计记录丢失）；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（chapter-drafting/context-composing/chapter-revision——主章节写作链路）的 .md 散文在 G2 被 continue 跳过，G2.6/G2.8/G2.9 对章节正文整体静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F251 P2 → P1（低置信维持）**：评分产物三重不匹配使 standalone 轮次对 G5.1（no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL）确定性失败；受 F252（exit 3 前置）掩盖维持低置信。
8. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`COVERAGE_FILE=/tmp/z2r10-cov .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前十轮一致）。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F218 部分匹配 → matched=True、仅命中 section、无 WARN；全缺 → 全文 + WARN
  - F215 嵌套 JSON → 取内层 `{"num":1,"score":88}`
  - F235 dispatch_with_write_audit(worldbuilding, 真实 REPO_ROOT) → violations（bridge_tracker/character_matrix）入 ledger
  - F247 dispatch_with_write_audit(chapter-drafting, "Draft chapter 7") → violations（chapter-7.md/decisions.json）；`_declared_patterns`=[] vs watch=2 项
  - F238 audit_writes malformed ## hooks → ParserError 崩溃
  - F245 resolve_chapter_path(volume-N/arc-N/AC-NNN, 7) 全按章节填；resolve_or_skip(volume-N, None) → None
  - F252 shenbi-score 实跑 → rc=3 MARKER_MISSING
  - F255 derive_file_type 4 个 report-kind truth 写者 → "report"
  - **F256（新）**：gate_G2(truth 文件 6/11 待填充行, "truth") → G2.10 FAIL；gate_G2(cfg.json, "chapter") → G2.12 WARN；gate_G2(audits/x.md, "report") → G2.12 WARN
  - F227 原 shell 门序证据：`git show 99f91f0:tests/dispatch-subagent.sh:81-82` "the skill execution happened earlier"；round-exec.sh create-mode 不产 novel.json
  - F243 原 shell 证据：头注 "Wraps subagent dispatch for scoring independence (G3.4)" + `RAW_OUT="${SCORES_FILE}.raw"` + `rm -f "${RAW_OUT}"`（Python 无）
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色差异为 git-show 证据 + 代码路径推理）。
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c / heredoc。

---

## 五、统计

- 新 finding：**2 条**（F256-F257：M×2）；M 级观察 4 条随文
- 误报：**0 条整体误报**（F201-F255 共 55 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持）
- 严重度异议：**8 条（全部为支持已有异议，无新增）**——F214→P1、F218→P1、F217→P1（低置信）、F228→P1（低置信）、F238→P1（低置信）、F229→P1（低置信）、F251→P1（低置信）、F220 维持 P2
- 前九轮 findings 判定：F201-F255 共 55 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否（严格意义上）**——本轮仍有 2 条新发现（F256/F257），但均为 **M 级**（注释↔代码漂移、死参数），无新增 P1/P2；Z2 分区 55 条既有 findings 全部成立，P1 集（F214/F216/F218/F227/F235/F247）经独立运行时复现全部维持。实质性（P1/P2）新 finding 已收敛为 0，剩余仅 2 条文案级遗漏。
