# Z1 分区第九轮独立复核报告（agent j）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`（F114–F122）+ `Z1.review2.md`（F123–F127）+ `Z1.review3.md`（F128–F133）+ `Z1.review4.md`（F134–F139）+ `Z1.review5.md`（F140–F143）+ `Z1.review6.md`（F144）+ `Z1.review7.md`（F145）+ `Z1.review8.md`（F146–F147），共 **47 条**
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `contracts/paths.py`、`contracts/legacy.py`、`gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g4/generic.py`、`gates/g5.py`、`audit/_shared.py`、`dispatcher/executor.py`、`dispatcher/modes/codex.py`、`tests/tiers/deps.json`、`command-to-give.md`、pyproject.toml、全部 82 份 rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`** 与 `PYTHONPATH=src` 纯内存解析器全量复跑）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟（`PYTHONDONTWRITEBYTECODE=1`，临时文件仅写 /tmp）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F148–F199（实际产出 F148–F153，共 6 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：6**（F148–F153；P2×2、M×4；**无新 P0/P1**）
- **误报数：0**（前九轮 47 条全部成立；无一条被证伪。F115/F140 两个 P1 经本复核**独立实跑/复跑确认无反证**）
- **严重度异议数：新增 0；维持既有 3**（F109：同意 R1–R8 共识 M→P2；F115：P1 无反证维持；F140：P1 无反证维持）
- **0 新 finding 判定：否**（6 条新漏报：F148 为 post-skill 产出缺失静默放行的独立缺陷站点，F149 为 filter 排除机制的第三个独立失效模式——前九轮均未覆盖）

---

## 1. 复核结论表（F101–F147 逐条判定）

| 编号 | 标题 | 前九轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 实证：`git ls-files -s docs/framework/dependency-dag.json tests/tiers/deps.json` → 均 `100644`；`ls -la` → 均 `-rw-------@`（0600）。根因确认：mkstemp 固定 0600 + os.replace 继承（safe_write.py:105,111） |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `ls docs/{registry,gates,schemas,dispatcher,integrity}.md tests/build_registry.py` → 全部 MISSING（独立实证）；ToolTamper 的 action（tests/lock-tool-hashes.sh）存在，仅 doc_url 破 |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立 AST 全仓 raise 扫描：exceptions.py 层级内直接 RAISED 恰为 DispatchWriteFailureError/DispatcherError/RetryExhaustedError/SubAgentProtocolError/SubAgentTimeoutError（5/22，R8 同；ConfigError/ContractError/DriftEscalationError/UnresolvedPathError 为层级外模块定义）；error_guidance/recovery 引用的 6 类全无站点 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立** | :360 `required=False` + :117 `str(None)`；g5.py:115 `if project_dir:` 对真值字符串 "None" 成立 → `Path("None")` → G5.3 三项扫描全空 → PASS（g5.py:202-203）；R3 对 R1"静默 SKIP"的事实修正独立确认 |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一无 exc_info 的 `except Exception`（对照 phase_runner:74、safe_write:139 均带 exc_info）；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | grep phase_runner 全文 0 引用；仅 gates/cli.py 手动入口 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——抛 OSError 时 finally 不执行 → flock fd 泄漏；:89-91 unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert 实证；`-O` 下守卫消失与 docstring :110 "Raises on mismatch" 冲突；工具仍会静默写 deps.json（:201）。**同意 R1–R8 共识：M→P2**（运行时正确性守卫而非文案；R4 注记"同源派生 → 近乎恒真"成立，失效风险在重构场景） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证；test_exceptions.py:64 同款 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo("` src/ 0 命中；同族 `backup()`（paths.py:33-35）亦 0 调用方（重读确认）；read() 被 11 个 G4 checker 消费（grep 实证） |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | 独立验证 `subprocess.TimeoutExpired.__mro__` = (TimeoutExpired, SubprocessError, Exception, BaseException, object)、`issubclass(TimeoutExpired, OSError) == False`；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有 |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **独立全量复跑 `load_applicability`**：82 rubric 中 62 有 "## Dimension Applicability" 节、44 解析非空、18 有节但解析 EMPTY + 20 无节 = 38 no-op（与 R2 分项修正一致）；4 个 N/A 豁免技能全部确认（worldbuilding rubric:43 dim4 / faction-builder:46 dim8 / location-builder:43 dim5 / story-architecture:44 dim6，均 10% 权重、表头 `\| # \| Dimension \| Bug-hunt Standard \| Clean Standard \|` 非解析器认的 `\| Dimension scope \|`）；4 技能均有 bug-hunt/clean/generative 测试目录（ls 实证）；command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径可复现。**反证尝试失败**（协议路径、测试目录、rubric 正文明示均核实）。按决策表"正常路径可复现功能错误"维持 P1 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径 G2 FAIL → return 1（阻断）；command-to-give.md:48 "只在 G2 和 G4 都通过后…评分" 协议与相位机路径不符 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:110-121 G4 仅 generative 分支写标记（bughunt/clean 分支不写）、:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 `-{test_type}` 拼接 |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关 → G2/G4 校验覆盖非确定 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `steps.append`；其余 5 命令均记录（:121/:164/:226/:298/:331） |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS/capability_fs src/ tools/` → 仅自身 + 测试；0 生产消费者 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；缺参 → IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"`（排除 .git）→ 0 命中（独立实证）；command-to-give.md:48 原文引用 `bash tests/dispatch-subagent.sh`；ADR-0009 已迁移 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | **实跑实证**：不带 --subagent 的文档化评分命令输出 `"scored_by": "interactive"`（见 F140 验证输出） |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均合法 JSON → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫；对照 :300 `gate_type` 有 `idx+1 < len` 守卫 |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 status |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立** | :145-159 只查空 scores 不查空 dims；实测 `validate_scores({1:100,2:95}, [])` → (True, [WARNING…]) + `compute_score([],…)` → 0 FAIL；R8 补充的 command-to-give.md:55 "dimensions 为空 → exit 2" 契约证据成立 |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 NaN 字面量 → NaN 通过校验并产出 NaN final |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src/ → 仅 status.py:78 定义，0 消费（独立实证） |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志（重读确认）；g4/generic.py 空文件 → SKIP 后 `passed(...)`（vacuous PASS，且 write_gate_marker 会写 PASS marker → pre-score 假通过） |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | `grep -c "log\."` 两文件均 0（排除 import 行） |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块（ls 实证） |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **独立实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**：完整 10 维 scores + `--test-type generative` → `final_score` 计算完全成功但 **EXIT CODE: 1**，stderr 为完整 result dict repr（`SystemExit(dict)` 打印，`sys.exit(main())` 包装实证）；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 依赖退出码 → `_record_completion` 永不可达；`scoring.main` 无库调用方。按决策表"违反显式契约 + 正常路径可复现"维持 P1 |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | `isinstance(True, (int, float))` → True；`True < 0 or True > 100` 恒 False → 校验通过 → 按 1 计分 |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | filter docstring :107-109 宣称 "renormalize weights" 但 :135-136 仅过滤；compute_score :171-172 以 total_weight≠100 发 warning；实测 chapter-drafting bug-hunt 过滤后权重和 = 75 |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过；json.loads 仅对 .json 路径 :110 执行）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/缺 state 键/非法 JSON → 裸 TypeError/KeyError/JSONDecodeError traceback |
| F145 | phase_runner `load_deps()` 无任何错误处理 | M | **成立** | :32-33 `json.loads(...)` 无 try；:245/:318 调用；对照 g5.py:36-39 try/except → 结构化 FAIL、scoring.py:206 exists() 守卫——phase_runner 是 deps.json 的唯一无守卫消费者。定 M 恰当（fail-loud、无部分写入） |
| F146 | scoring.check_gate_markers t2 分支对 deps.json 仅 exists() 守卫：存在但损坏 → 裸 JSONDecodeError | M | **成立** | :205-207 `if deps_path.exists(): deps = json.loads(...)`——exists() 只防缺失不防损坏；同族三消费者中 scoring 站点漏报 |
| F147 | sync_contracts.main() deps.json 读取（:195）与 load_registry（:165）均无守卫 | M | **成立** | :195 `json.loads(DEPS_PATH.read_text(...))` 无 try；:165 `load_registry()` ContractError 裸上抛；对比同文件 :167-172 的 bail 有 log.error + return 1 |

**前九轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——独立走查 g5.py:115/202-205 确认 **R3 修正正确**；F101 波及文件数递增记录属实（本复核时点 dependency-dag.json/deps.json 均为 0600）；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致。**无新的前轮事实错误需要修正。**

---

## 2. 新发现（F148–F153）

F148 | phase_runner cmd_post_skill 对契约声明但磁盘缺失/为空的输出静默丢弃（无日志）：技能未产出任何文件时 G2 SKIP + G4 空文件 vacuous PASS + PASS marker → 相位以 OK 推进 | error | **P2**
- 证据: src/shenbi/phase_runner.py:196-200（`output_files = [p for p in derive_output_files(skill, chapter, proj) if Path(p).exists() and Path(p).stat().st_size > 0]`——契约正常解析但声明输出不存在/为空时被**静默过滤**，无任何 log）；:208-210（chapter 给定（pipeline 路径）时 rglob 兜底不触发）；:211-216（output_files 空 → G2 SKIP、G4 以 `""` 运行）；gates/g4/generic.py:46-47（空文件 → `c.append({"id":"G4.gen","s":"SKIP"})` → `passed(...)` vacuous PASS）；gates/shared.py:195-198（result.status==PASS → write_gate_marker 写 `G4-{skill}-generative.json` PASS marker → cmd_pre_score :250 的标记检查被假通过）
- 根因: F137 覆盖的是 **ContractError（契约解析失败）→ derive 返回 []** 的吞错站点；本条为**独立站点**——契约解析成功、声明了 writes/updates，但产出未落盘（技能失败/未写全）时，`exists()+st_size>0` 过滤把缺失项静默丢弃。对比 executor.py:197-204（同路径非 pipeline 模式对 `if output_files:` 空则整体跳过 G2）——但 executor 有 G1 输入校验与流水线 state 兜底，phase_runner 无等价告警。部分产出场景（声明 5 个产出 3 个）同样静默按"已存在部分"校验，缺项无告警；pre-score 的 expected_outputs 检查是 phase 级 glob（任一匹配即过），无法捕获单技能缺失
- 验证命令+输出: 模拟（无文件写入）：`declared=[proj/chapters/chapter-5.md, proj/chapters/chapter-5-decisions.json]`（均不存在）→ `output_files = [p for p in declared if Path(p).exists() and Path(p).stat().st_size > 0]` → `[]`；代码走查 :196-216 确认无 log 调用；`sed -n '385,403p' tests/unit/test_phase_runner.py`（test_skips_g2_when_no_output_files 用 fake_run_gate 返回 PASS，掩盖真实 generic vacuous PASS + marker 写入，未覆盖"声明输出缺失"场景）
- 建议方向: 过滤时统计被丢弃项并 `log.warning("declared_outputs_missing", skill=..., missing=[...], empty=[...])`；或 derive 返回后与声明集合比对，缺失即 emit BLOCKED/ERROR（与 pre-score 的 fail-loud 对齐）；补"契约声明输出全部未产出 → post-skill 不推进/至少 WARN"测试

F149 | scoring filter_dimensions_by_test_type 对"无维度数字"的排除 scope 静默 no-op：规范 _template rubric 自身的 `Prose/narrative quality \| No` 行无效（排除机制第三失效模式，R2 备查 #5 "44 个未触发" 与事实不符） | error | **P2**
- 证据: src/shenbi/scoring.py:127-134（排除解析只认 scope 内 `dim\s+(\d+)` 与 `#?(\d+)` 数字；:133-134 `if not excluded_nums: return dimensions` 静默原样返回）；tests/tiers/t1-skill/_template/rubric.md:34-38（规范模板的 `\| Dimension scope \|` 表**正确匹配**表头（属 44 个已解析之一），"Prose/narrative quality" 行标 `No`，但 scope 无任何维度数字 → excluded_nums 恒空 → no-op）；对照组 chapter-drafting 的 scope 写作 `Prose/narrative quality (dims 6,7,9)`（含数字）→ 过滤**确实生效**
- 根因: F115 覆盖的是"表头格式不识别 → applicability 空"与"N/A 单元格语义"两个失效点；本条为**第三失效点**——表头与 No 标记均正确、仅 scope 无数字 → 静默 no-op。R2 备查 #5 声称"当前 44 个 Dimension-scope rubric 未触发错误分支"**不准确**：_template 属 44 个之一且确实触发（其 dims 为占位符故无实害，但模板是 rubric 作者复制粘贴的规范底稿，复制者按模板写作的排除将静默失效）；且排除解析依赖"作者在 scope 文本里手写维度号"这一未文档化约定
- 验证命令+输出: `PYTHONPATH=src python3 -c "from shenbi.scoring import load_rubric, load_applicability, filter_dimensions_by_test_type; ..."` → `template applicability: {'Bug-hunt': {'Universal (1-2)': True, 'Non-prose bespoke ...': True, 'Prose/narrative quality': False}, ...}`；`filter_dimensions_by_test_type(dims, template, 'bug-hunt')` → 返回与过滤前相同的 `[1, 2]`（排除无效果，`== True` 实证）
- 建议方向: 排除解析增加名称→维度映射（如按 `name` 列匹配维度名）或要求 scope 必须含数字并在缺数字时 `log.warning("exclusion_scope_without_dimension_numbers", scope=...)`；修正 _template 的 scope 写法（如 `Prose/narrative quality (dims N)`）以示范可行模式；（严重度说明：当前仅模板触发、无生产 rubric 受害，若合并层认为纯模板影响可议降 M，本复核按"机制静默失效 + 规范文档漂移"定 P2）

F150 | phase_runner run_gate("G2", ...) 不传 gates/cli G2 的第 4 参 project_dir → G2.7 重要章节 ceiling 判定在相位机路径恒失效（与 executor.run_g2 传 REPO_ROOT 不一致，两路径均未传真实项目目录） | error | **M**
- 证据: src/shenbi/phase_runner.py:213（`run_gate("G2", [",".join(output_files), file_type, str(round_dir)])`——3 参）；gates/cli.py:60-63（G2 分支 `pd = arg(3, None)` 支持第 4 参）；gates/g2.py:237（`is_important = _is_important_chapter(fp, project_dir or "")`）+ :382-384（`if not project_dir: return False`）；src/shenbi/dispatcher/executor.py:122-134（`run_g2` 传 4 参 `str(PROJECT_DIR)`，PROJECT_DIR=REPO_ROOT 于 :26）
- 根因: phase_runner 的 cmd_post_skill 已有 `proj = Path(project_dir)`（:189，按协议即 `round_dir/project-output`，真实项目目录）却未作为第 4 参传给 G2；executor 传的 REPO_ROOT 亦非项目目录（volume_map.md/plans/ 在 project-output 下）→ `_is_important_chapter` 两调用路径都查不到文件 → 恒 False → G2.7 对"重要章节"恒按 `floor*1.5=4500` ceiling（而非 10000）。当前 G2 FAIL 不阻断（F116）故直接危害为 g2_status 元数据失真；若 F116 修复为阻断，长重要章节（4500-10000 字）将误 FAIL
- 验证命令+输出: 代码走查 phase_runner.py:213 vs executor.py:122-134 vs g2.py:237,382-384；`grep -n "run_gate(\"G2\"" src/shenbi/phase_runner.py` → `:213`（3 参）；`sed -n '382,400p' src/shenbi/gates/g2.py`（`if not project_dir: return False` 实证）
- 建议方向: `run_gate("G2", [",".join(output_files), file_type, str(round_dir), str(proj)])`（proj 即 --project-dir）；executor 侧改传真实项目目录（非 REPO_ROOT）并补 G2.7 重要章节单测

F151 | scoring.py:419 scores.json 非数字键静默丢弃且无 WARNING（extra-key WARNING 只对数字键生效） | error | **M**
- 证据: src/shenbi/scoring.py:417-419（`scores = {int(k): v for k, v in raw.items() if k.lstrip("-").isdigit()}`——非数字键在 validate_scores 之前被静默过滤）；:151-152（extra-key WARNING 只针对已保留的数字键）
- 根因: 键过滤在解析期静默完成；用户手误的额外键（如 `"dimensin1": 100`）或元数据键被无声丢弃，validate_scores 的 "WARNING: unexpected dimension keys" 永远看不到它们；若该键恰是某维度的唯一分数（如 `"three": 90`），该维度变 missing → REJECT（会被发现）；若与正确键并存则完全不可见
- 验证命令+输出: `PYTHONPATH=src python3 -c "import json; raw=json.loads('{\"1\": 90, \"dimensin1\": 100, \"notes\": \"x\"}'); scores={int(k): v for k, v in raw.items() if k.lstrip('-').isdigit()}; print(scores)"` → `{1: 90}`（dimensin1/notes 静默消失，无任何告警）
- 建议方向: 过滤时收集丢弃键并 `log.warning("non_numeric_score_keys_dropped", keys=[...])`；或对非数字键直接 REJECT

F152 | load_rubric kill-switch 解析漏 "→ detection dimension = 0" 类条目（只认 total score/phase/pipeline = 0）→ 10+ 份 rubric 的 kill_switches 元数据不完整（交互提示与结果 JSON 缺项） | error | **M**
- 证据: src/shenbi/scoring.py:43-47（仅匹配 `"total score = 0"` / `"phase = 0"` / `"pipeline = 0"`）；全仓实测 12+ 份 rubric 的 Bug-Hunt Kill Switches 节含 `- Evidence without file+line citation → detection dimension = 0`（worldbuilding:26、faction-builder:25、location-builder:25、chapter-planning:26、character-design、pacing-design、relationship-map、short-outline、genre-config、foreshadowing-resolve、volume-outlining、foreshadowing-plant 等）——全部不被捕获
- 根因: 解析器只面向"整卷归零"类 kill switch（total/phase/pipeline），对"维度归零"类（检测维度 = 0）静默跳过；`kill_switches` 列表在交互模式被 log 展示（:383-386）并写入结果 JSON（:457），缺项导致评分者看到的 kill-switch 清单不完整（元数据失真，不改变 --kill-switch 旗标行为）
- 验证命令+输出: `PYTHONPATH=src python3` 全量对比（load_rubric 捕获集 vs 文件内 `= 0` 行）→ 每份受影响 rubric 的 captured 集缺失 `Evidence without file+line citation → detection dimension = 0` 项（实测输出见上方证据行）
- 建议方向: 解析增加 `"dimension = 0"` 模式（或按 kill-switch 节内所有 `→ ... = 0` 行捕获）；补 test_scoring.py 的 kill-switch 解析断言

F153 | sync_contracts.render_body_view 的 `skill` 参数从未使用（死参数） | dead-wire | **M**
- 证据: src/shenbi/sync_contracts.py:124（`def render_body_view(skill: str, contract: dict[str, Any]) -> str:`）；:128-134（函数体只用 BODY_BANNER/contract['reads']/contract['writes']/contract['updates']/BODY_END，`skill` 无任何使用）；:149（调用处 `render_body_view(skill_md.parent.name, contract)` 传值亦无消费者）
- 根因: 函数签名预留 skill 名用于块内展示，实现未使用；属 F130 同文件、F138 同族（未使用绑定）的独立死代码点
- 验证命令+输出: `grep -n "skill" src/shenbi/sync_contracts.py | sed -n '1,20p'`（:124 定义、:149 传参外，render_body_view 体内 0 命中）
- 建议方向: 删除 `skill` 参数（同步调用处）或并入块内容（如 `<!-- skill: xxx -->`）

---

## 3. 严重度异议汇总

| 编号 | 前九轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有异议）** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移。按决策表 P2"防御深度"而非 M"文案" |
| F115 | P1 | **维持 P1（无反证）** | 独立全量复跑（38/82 no-op、4 技能 N/A 豁免、表头格式、rubric 行号、4 技能 bug-hunt/clean 测试目录、command-to-give.md:64,71 文档化路径）全部确认；唯一可能反证（"协议不实际使用这些 rubric"）被测试目录+文档路径证伪 |
| F140 | P1 | **维持 P1（无反证）** | 独立实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr 打印 result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达；`scoring.main` 无库调用方（排除"返回 dict 是 API 设计"的抵消理由） |

其余 44 条严重度无异议（F101-F108/F110-F114/F116-F121/F123-F125/F128-F129/F133-F135/F137/F141/F144 的 P2 与 F110/F111/F122/F126/F127/F130/F131/F132/F136/F138/F139/F142/F143/F145/F146/F147 的 M 均认可）。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读；另核实 11 个跨区依赖文件/数据：contracts/paths.py、contracts/legacy.py、gates/cli.py、gates/shared.py、gates/g2.py、gates/g4/generic.py、gates/g5.py、audit/_shared.py、dispatcher/executor.py、dispatcher/modes/codex.py、tests/tiers/deps.json、command-to-give.md、pyproject.toml、82 份 rubric）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型六方法逐一核实无洞；F123 覆盖无接线）、`cli_utils.py`（stdout 数据通道/stderr 日志分离自洽）、`logging.py`（双渲染器/stderr/`cache_logger_on_first_use=False` 自洽；SHENBI_LOG_FORMAT 校验为前轮已记录低价值项）、`paths.py`（read/write 语义正确；F112 覆盖死代码；read() 的绝对路径/父路径逃逸仅对仓库内硬编码相对路径可达，无外部输入面，未立 finding）、`error_guidance.py`/`recovery.py`（F103 覆盖 dead；F138 未用 log）、`exceptions.py`（层级结构正确；F103 覆盖 dead）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（除 F127/F136 外词汇表自洽）、`safe_write.py`（F101/F108/F119/F133 覆盖；剩余分支为防御/平台边界）、`phase_runner.py`（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150 覆盖；剩余 run_gate 非 dict JSON → AttributeError 属不可达防御边界）、`scoring.py`（F104/F106/F115/F118/F120/F126/F129/F131/F134/F135/F140/F141/F142/F149/F151/F152 覆盖；`--gate-only` G3 参数形状错配、无 --files vacuous PASS、UNIMPLEMENTED 门 exit 0 均为 R4/R5/R7 备查已记录项）、`sync_contracts.py`（F109/F117/F130/F147/F153 覆盖）
- 新发现分布：`phase_runner.py`（F148——post-skill 产出缺失静默放行、F150——G2 project_dir 缺参）、`scoring.py`（F149——name-only scope 排除 no-op、F151——非数字键静默丢弃、F152——kill-switch "dimension = 0" 漏解析）、`sync_contracts.py`（F153——render_body_view 死参数）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达（本复核独立确认 `grep render_body_into tests/` 0 命中）——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。
2. **`filter_dimensions_by_test_type` 对"表头含但 type_key 不匹配"静默 no-op**（如 rubric 表无 Generative 列而评 --test-type generative）——当前 44 个已解析 rubric 均含三列，无触发文件，属潜在格式风险（F115/F149 根因族）。
3. **`--gate-only` G3 参数形状错配 / 无 --files vacuous G2 PASS / UNIMPLEMENTED 门 exit 0**——R4/R5/R7 备查已记录，同意不重复。
4. **`--chapter abc` ValueError / `--interactive` 必须 argv[2] / find_flag 值不校验 / scoring 缺失 rubric/scores 文件裸 traceback**——R3/R2/R7/R6 备查已记录，同意不重复。
5. **`load_rubric` 对 `| # | Dimension |` 表（worldbuilding 等 4 技能）的维度解析正常**（weight 表独立存在，cells[2]=Weight 列可解析——本复核全量复跑确认 82 份 rubric 维度全部解析、仅 _template weight≠100 为占位符良性）——F115 的 N/A 失效点不影响 load_rubric 本身。
6. **exceptions.py `to_dict` 无生产调用方**（并入 F103 注记）；**paths.py `backup()` 0 调用方**（并入 F112 注记）——前轮已注明，不重复。

---

## 5. 其他说明

- 前九轮"P1×2（F115/F140）"结论：本复核独立实跑/复跑后**无反证，维持**；误报数 0（47/47 成立）；前九轮共 47 条 finding 无一条被证伪。
- 新发现 F148–F153 均不改变"无 P0"结论，也不新增 P1；其中 F148（post-skill 产出缺失静默放行）与 F149（name-only scope 排除 no-op）为前九轮完全未覆盖的独立缺陷站点（分别补全了 ContractError 家族（F113/F117/F137）与 F115 家族之外的最后失效模式），F150–F153 为 M 级一致性/元数据/死代码扫尾。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器全量复跑/代码路径模拟 + grep/read 完成；F140 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**实证退出码；F126 经实跑输出实证；F115/F134/F135/F141/F142/F149/F151/F152 经 `PYTHONPATH=src` 纯内存导入复跑；F148 经纯内存模拟（无仓库文件写入）；临时文件仅写 /tmp，未触碰仓库文件。
