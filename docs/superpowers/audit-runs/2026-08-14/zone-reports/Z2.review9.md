# Z2 分区第九轮独立复核报告（Z2.review9）— contracts/ + dispatcher/

- 复核对象: `Z2.a.md`（F201-F220）+ `Z2.review.md`（F221-F226）+ `Z2.review2.md`（F227-F234）+ `Z2.review3.md`（F235-F237）+ `Z2.review4.md`（F238-F244）+ `Z2.review5.md`（F245-F246）+ `Z2.review6.md`（F247-F249）+ `Z2.review7.md`（F250）+ `Z2.review8.md`（F251-F252）
- 模式: fresh-context 全量重读 38 文件（清单全覆盖）；只读（未创建/修改/删除仓库文件，除本报告；git 无 add/commit；探针均在 /tmp 与 python -c / heredoc）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖，未依赖前九轮转述）；另读跨区消费面验证裁决（gates/{cli,g0,g1,g2,g3,g3_independence,shared,g_reconcile}.py、g4/{genre_config,chapter_planning,context_composing,volume_outlining}.py、audit/{_shared,snapshot,write_audit,record}.py、records/{parser,drift}.py、scoring.py、logging.py、cli_utils.py、safe_write.py、paths.py、phase_runner.py、pipeline/{dispatch_helper,parallel_dispatch,chapter_loop,closure}.py、tests/round-exec.sh、first-novel.md、truth-files.yaml、相关 SKILL.md、tests/unit/test_dispatcher_executor.py）。
2. **独立验证方式**：`pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与九轮一致）；cov 定向（executor 74% 缺 169-188/251-262、codex 75% 缺 29-30/54/67-68/71-72/77-78/81-85/112）与各轮报告吻合；10+ 个 `python -c`/heredoc 运行时探针（F216/F218/F230/F232/F235/F240/F245/F247/F248/F251/F252 复现 + F253 竞态演示 + F255 分类探针）。
3. **前九轮判定**：F201-F252 共 **52 条全部实质成立，0 条整体误报**；F220 部分误报（计数 5→6）与 F211 表述修正维持；F212 附注（gates/cli.py:148-150 恒返回 0）、F243 机制修正（exit 3 先于 exit 2）、F247 范围注记（中文 prompt → chapter=None 时审计空转不 rc=2）全部复核通过。
4. **本轮新发现 3 条（F253-F255）+ M 级观察 5 条**，集中于前九轮都未触及的三类盲区：
   - **F253（P2）**：`_record_completion` 对 progress.json 做非原子读-改-写，safe_write 的 flock 仅覆盖写入、不覆盖读取周期 → 并发 shenbi-dispatch 进程丢完成记录（模拟交错复现 lost update；被 F227/F235/F247/F252 链掩盖）；
   - **F254（P2）**：executor.run_g1/run_g2 与 codex.dispatch_codex 的 shenbi-score 子进程均无 timeout（仅 codex exec 有 600s）→ standalone 路径门/评分子进程挂起时无限阻塞（pipeline 外层 cli_timeout 仅包住 shenbi-dispatch 整体）；
   - **F255（P2）**：`derive_file_type` 的 REPORT 分支先于 truth 交集返回 → kind=report 且写 truth 概念文件的技能（score-volume/score-arc/score-stratum/review-arc-payoff，实测 4 个）被归 "report" → G2.11 truth 只增不删保护对其静默失效（与 F239 的 glob 写者误归 chapter 是不同触发面：kind 短路 vs glob 不匹配）。

---

## 一、复核结论表（F201-F252 判定）

| 编号 | 初审严重度 | 复核判定 | 理由（本轮 fresh 验证） |
|---|---|---|---|
| F201 | P1 | **成立，P1 维持** | 三模型（chapter_planning/context_composing/volume_outlining）无生产消费者（grep 仅 test_skill_integration.py:188 类名文本）；g4 私有实现规则不同（g4/chapter_planning.py:99-102 hook ops 子串存在性 vs 模型 typed change/defer 激活计划；g4/context_composing.py:74-86 P1-P7/route 标题 vs 模型 section_count==9；g4/volume_outlining.py:59-72 KR/张力曲线/跨卷桥 vs 模型 entity_hooks>=3 且 types>=2）——本轮 read 复证。cov 34/38/40% 与报告吻合。 |
| F202 | P2 | **成立** | REGISTRY/load_skill_contract 生产无消费者（仅 __init__ re-export；g4/genre_config.py:13、g4/pacing_design.py:13 直接 import 模型）。 |
| F203 | P2 | **成立** | DepsDoc/phase_of（deps.py:56-78）无生产消费者；sync_contracts.py:195 仍 json.loads（read 复证）。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无生产 import；g6.py:51 与 g4/worldbuilding.py 以 raw dict 读 target_word_count/target_words —— 死的是模型非行为。 |
| F205 | P2 | **成立** | schemas.scores 无生产消费者（grep 复证；g4/score_*.py 用 RouteC/RouteA 正则检查）。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无生产加载点（grep 仅 tests/unit/contracts/schemas/test_novel_scores_state.py 命中；g3.py 用 jload 直读 dict）。 |
| F207 | P2 | **成立** | _scoring_base/score_* 无生产消费者；cov 100% 系测试达成。 |
| F208 | P2 | **成立（增补成立）** | Severity/Verdict 无生产 import；裸字面量散落点 + chapter_loop.py:1797、:409-438 复核通过；ALL_ENUMS 亦仅测试消费（本轮补证，M）。 |
| F209 | P2 | **成立（增补成立）** | base.py:29 裸 Literal 缺 UNIMPLEMENTED（status.py:27 实有，read 确认）。过渡类型自述（base.py:4-5）减轻但不消除词汇分裂。 |
| F210 | P2 | **成立** | hooks.py 引用 SKILL.md:72-73/87/120；实测 PLANTED:40 / ARCHIVED:79 / EXPIRED:80 / TRIGGER:94，Δ32-40（grep 复证）。 |
| F211 | P2 | **成立（表述修正维持）** | executor.py:167-188 cov missing（复测 74% 缺 169-188）；g1.py:174-190 与 dispatch_helper:382 设置有测试——"零测试"仅指 executor 侧。 |
| F212 | P2 | **成立（附注维持）** | run_g1/run_g2（executor.py:111-139）无 returncode 检查、无 stderr 上下文；gates/cli.py:148-150 对已知门恒返回 0 → 修复方向按"CLI 崩溃"修正；**补证**：三处门/评分子进程亦无 timeout（见 F254）。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec、无重试/429/finish_reason；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 对比属实（read 复证）。 |
| F214 | P2 | **成立；支持异议→建议 P1** | **反证尝试失败**：codex.py:20-48 不写 current_scorer_agent；dispatch_helper.py:1934-1945 run_gate_g3 在 progress.json 缺失时主动写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + `scoring_history:[{"agent":...}]`（本轮 read 复证；键 "agent" 非 "agent_id" → g3.py:213-224 prior_agents 恒空 → G3.5 PASS）→ G3.4（g3_independence.py:18-28 fail-closed）以生产方自写证据放行。反证（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发伪造。 |
| F215 | P2 | **成立** | codex.py:75 `\{[^{}]*\}` 对嵌套 JSON 取内层、多对象取第一个（read 复证 + 前轮复现）。 |
| F216 | P1 | **成立，P1 维持** | **运行时复现**：空 customRules + disabled dim（5 维度 1 禁用）→ `GenreConfig.model_validate` PASS（genre_config.py:94 `if disabled and self.custom_rules:`）；g4/genre_config.py:36 以 model_validate 强制 → G4 校验洞真实可触发。P1 维持。 |
| F217 | P2 | **成立；支持低置信异议（P2→P1）** | 本轮 grep 复证 SKILL.md 内 **7 处** CONSTELLATION 区间（:91 15-25 / :176 20-30 警告 / :187 15-30 / :192 15-25 / :193 30-40 开卷 / :194 15-25 / :257 表 15-30 且 <10 或 >40 才 FAIL）+ docstring 20-30 + 模型 [15,35]——模型连 :257 表也矛盾；权威区间未定，维持低置信。 |
| F218 | P2 | **成立；支持异议→建议 P1** | **运行时复现**：md 部分匹配（声明 ["当前任务","不存在的字段"]，文件含 3 节）→ matched=True、仅命中 section、无 WARN；AGENTS.md:87-89 "If a declared field is missing → full file + WARN" 部分缺失时两处均不满足；truth 文件随剧情演进缺 section 属正常路径。P1 建议成立。 |
| F219 | P2 | **成立（增补成立）** | G1 空输入 → G1.0 SKIP + 整体 PASS（g1.py:281-282）；executor.py:74-75/:107-108 ContractError 静默降级；`_audit_watch_paths`→[]、`_truth_file_set`/`_decisions_file_set` module 级缓存固化空集（executor.py:29-56）复读通过。 |
| F220 | P2 | **成立（计数修正维持：6 条；严重度存疑支持维持 P2）** | ownership.py:73-94 实测 6 条目（genre-config field / foundation-review field read / plant record_create / track / resolve / state-settling record_field）；read_keys 死声明复证（grep `.read_keys` 仅定义处）。矩阵小系 docstring 明示过渡态（"支柱一续"）；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险，维持 P2。 |
| F221 | P2 | **成立（补证加强）** | fixture 9 键含 tropeInventory + SKILL.md:286/:292 "恰好 8 个" + OWNERSHIP _GENRE_KEYS 9 键 + 模型 8 字段（grep 复证）；**本轮补证**：`skill_utils/trope_detection/match_tropes.py:61` 生产读取 `tropeInventory`——被模型（extra=ignore）静默丢弃的恰是唯一有真实生产消费者的字段，模型缺口从"漂移"升级为"活动消费面缺失"，修复优先级应高于普通漂移（严重度维持 P2）。 |
| F222 | P2 | **成立（增补成立）** | executor（basename fnmatch+存在性，executor.py:167-188）/g1（glob 未匹配，g1.py:174-190）/dispatch_helper:382（设置者）三处语义分叉复读通过；executor 对 glob 读路径宽泛 skip 下整条丢弃复读通过。 |
| F223 | P2 | **成立** | codex.py:28-29 progress.json 损坏裸 JSONDecodeError；:114/:116 同一 stdout 二次解析；缺 final_score 静默 0（read 复证）。 |
| F224 | P2 | **成立** | `grep '"output_files"' src/` → 仅 g3.py:153（读取），全仓无写入者（复证）；codex.py:40-46 写 `{score,status}` → G3.3 永 SKIP（g3.py:187）。 |
| F225 | P2 | **成立** | grep dispatcher/ 无 read_fields/filter_to_fields；消费点仅 dispatch_helper.py:592,1229-1230（read 复证）。 |
| F226 | M | **成立** | cli.py:22-28 usage `[prompt]` 可选 vs codex.py:53-54 空 prompt 硬 raise SubAgentProtocolError；cli main 无 try/except。 |
| F227 | P1 | **成立，P1 维持** | **反证尝试失败**：round-exec.sh create-mode（read 复证）只建 t1-reports/skill-output/skill-traces + progress.json/meta.json/summary.json/.token-hashes.json，**无 novel.json** → 文档化 first-novel.md:38-41 流程 `shenbi-dispatch shenbi-worldbuilding generative <round_dir> "Build the world"` 第一步 G1.1 novel.json not found → rc=1；`git show 99f91f0` 原 shell 注释 "the skill execution happened earlier" 证实门序回归（技能执行被并入 G2 之后的 dispatch_codex，codex.py:61-66）。 |
| F228 | P2 | **成立；支持低置信异议（P2→P1）** | derive_output_files（audit/_shared.py:38-56）只 resolve_or_skip、不展开 glob；gate_G2（g2.py:44-49）无 glob 展开 → 重试轮 `truth/*.md` 字面 G2.1 not-found。**本轮增补（跨区）**：phase_runner.py:196-203 亦消费 derive_output_files + `Path(p).exists()` 过滤 → glob 字面路径被过滤 → 空输出 → rglob 兜底——F228 影响面扩至 phase_runner（新消费者，非新根因）。 |
| F229 | P2 | **成立（范围低估但严重度维持）** | 探针复现 `derive_file_type("shenbi-chapter-drafting") == "decisions"`（decisions 概念含 `chapters/chapter-N-decisions.json`，truth-files.yaml 注册为 concept）；g2.py:83-84 decisions 分支对非 .json `continue` + executor 只调一次 run_g2 → .md 散文 G2.6-2.10 静默缺失。 |
| F230 | P2 | **成立** | **运行时复现**：json 路径 `filter_to_fields('{"当前 世界局势":1}', ["当前\u3000世界局势"], "x.json")` → matched=False + 全文 + `field_filter_no_match` WARN；`_filter_json:76` 裸 `k in fields`（无规范化）。 |
| F231 | P2 | **成立** | grep povMode/sensitivityFlags 仅 review_checklist.py:183,186 + review-group-character/review-pov SKILL.md 命中；fixture 9 键与模型 8 字段均无两者（复证）。 |
| F232 | P2 | **成立** | **运行时复现**：`Selection(severity="high", basis="manual_override", rationale="")` → ACCEPTED；`Adjustment(rationale="   ")` → ACCEPTED；routine+low 空串被拒——两侧不对称（decisions.py:33 `has = rationale is not None`）。 |
| F233 | P2 | **成立** | record_create 分支（ownership.py:120-124）只查 deleted/modified_record_keys，从不查 new_record_ids 键集；`_HOOK_KEYS_NEW_RECORD`（:52-71）声明后未接线（复读确认）。 |
| F234 | P2 | **成立** | snapshot.py:99-102 deleted 无键/记录信息；ownership.py 三 level 分支均不看 status；owned 文件删除零违规（复读确认）。 |
| F235 | P1 | **成立，P1 维持** | `PROJECT_DIR = REPO_ROOT`（executor.py:26），`_audit_watch_paths`（:220-225）带 chapter 解析 watch、`snapshot_tree(PROJECT_DIR, watch)`（:244/:264）快照仓库根 → 仓库根 tracked `truth/bridge_tracker.md`/`truth/character_matrix.md` 命中 watch → F236 匹配失败 → `未声明写入` violations → 零写入派发也 rc=2；round_dir 真实写入不被审计。反证（审计面意图为仓库根）不成立——codex.py:62 cwd 已改 round_dir + test_executor_audit.py:18,40 编码 PROJECT_DIR==round_dir 正确语义。 |
| F236 | P2 | **成立** | `_matches_declared`（write_audit.py:29-36）只 fnmatch parametric globs（parametric_globs()），registry globs（`truth/*.md`）恒判未声明（read 复证 + 前轮复现）。 |
| F237 | P2 | **成立** | snapshot.py:99-100 added 短路不计算顶层键 → field 级键集校验对新建 JSON 旁路（复读确认）。 |
| F238 | P2 | **成立；支持 review6 低置信异议（P2→P1）** | audit_writes（write_audit.py:52-56）对每个 post .md 无 try 地 parse_records/parse_markdown_table；records/parser.py:40-44 yaml.safe_load 可抛 ScannerError/非 list 抛 ValueError；executor.py:263-268 finally 无捕获 → record_audit_outcome 永不执行——"崩溃仍审计"保证（executor.py:245-246）失效。崩溃本身 loud，维持低置信。 |
| F239 | P2 | **成立** | 探针复现 derive_file_type：worldbuilding/sequel-writing/truth-sync/snapshot-manage 全 → "chapter"（glob 写者误归；`outputs & _truth_file_set()` 精确字符串交集，executor.py:81-85）；state-settling → "truth"、chapter-drafting → "decisions"；`truth/arcs/arc-N.md` 是 truth concept → memory-distill 归 "truth"（F239 范围外，正常）。 |
| F240 | P2 | **成立** | **运行时复现**：`extract_chapter("写第7章，主角觉醒") == None`、`extract_chapter("Draft chapter 7") == 7`；chapter=None → derive_output_files 丢弃 N 路径 → G2 静默跳过。 |
| F241 | P2 | **成立** | genre_config.py:41-42 `if decision and ...`（缺失/空串跳过）、:50-52/:58-60/:69-71 isinstance 守卫（非 list/dict 跳过对应规则）——规则 1/3/4 静默放行（read 复证 + 前轮复现）。 |
| F242 | P2 | **成立** | ownership.py:73-94 无 `("shenbi-foreshadowing-lifecycle","truth/pending_hooks.md")`；plant/track SKILL.md:29-30/:18-19 自述 DEPRECATED "Do not dispatch"（本轮 grep 复证）。生产写者 lifecycle 无条目 → 记录级写保护对生产路径空转。 |
| F243 | P2 | **成立（机制修正维持）** | `git show 99f91f0` 头注 "Wraps subagent dispatch for scoring independence (G3.4)" + usage `[subagent_prompt]`（原第 4 参为独立评分 prompt）；codex.py:61-87 现以生成 prompt 驱动单一 codex exec → scorer==generator。机制修正（review8）维持：gate-marker 强制 exit 3 先于分数校验 exit 2。 |
| F244 | P2 | **成立** | g1.py:97-99,164 G1.4 对 fps=**输入**建 .bak（g1.py:224-234）；g2.py:290-313 G2.11 对 **truth 输出**找 .bak；state-settling 输出集∉输入集 → G2.11 静默无检查（read 复证）。 |
| F245 | P2 | **成立** | **运行时复现**：resolve_chapter_path("audits/volume-N-payoff.md", 7) → `audits/volume-7-payoff.md`；AC-NNN+7 → `AC-007.md`；resolve_or_skip(volume-N, None) → None；extract_chapter 中文 → None。resolve_volume_path 仅 closure.py:20,158 消费（grep 复证）。 |
| F246 | P2 | **成立** | dispatch_skill（dispatch_helper.py:1864）legacy 回退 `["uv","run","shenbi-dispatch",...]`（无 API key/无 IDE CLI 时默认路径）→ pipeline 继承 F227/F235/F247/F252 缺陷（read 复证）。 |
| F247 | P1 | **成立，P1 维持（范围注记维持）** | `_declared_patterns`（write_audit.py:19-26）以 chapter=None 派生声明集（N 路径全丢），watch 面（executor.py:220-225）以提取到的 chapter 解析 → N 写技能在 prompt 含可提取英文章节号时成功派发恒判「未声明写入」rc=2（read 复证 + 前轮端到端复现）。范围注记维持：中文 prompt（F240）→ chapter=None → watch 面同样丢 N 路径 → 审计空转但不 rc=2。 |
| F248 | P2 | **成立** | **运行时复现**：`PacingDesign.from_markdown(md).chapter_sequence == []`（恒空）→ `_no_three_consecutive_same`（pacing_design.py:66-74）在 g4 门路径（g4/pacing_design.py:47）死代码；scene_types 固定词表子串扫描（:96-123）。 |
| F249 | P2 | **成立** | executor.py:122-139 run_g2 第 4 参 `str(PROJECT_DIR)`（shenbi 仓库根）→ `_is_important_chapter`（g2.py:382-413）恒查仓库根 outline/plans（不存在）→ 重要章上限退化为 4500（read 复证）。 |
| F250 | P2 | **成立** | codex.py:61-62 唯一指令输入是 CLI 裸 prompt（无 system prompt/SKILL.md/契约/rubric）；grep SKILL.md src/shenbi/dispatcher/ → 空；对照 pipeline dispatch_helper.py:554-557 注入 SKILL.md（read 复证）。 |
| F251 | P2 | **成立** | find_report（shared.py:157-166）三模式均不匹配 `-scores-subagent.json`；g0.py:440 G0.10 glob `*-generative-scores.json` 不匹配；raw 维度 dict 无 final_score（codex.py:87 落盘 raw dict，完整报告仅走 stdout）；progress 状态 "done"（codex.py:44）vs g_reconcile.py:40,61 "DONE" → GR.2 恒 FAIL（read 复证：GR.2 的 rsplit 解析出 candidate_tt 含 "-scores-subagent" 后缀，与 progress 的 test_type 键永不相等）。 |
| F252 | P2 | **成立** | scoring.py:364-377 marker 强制 + :187-224 check_gate_markers（t1-skill 分支要求 `G4-{skill}-{test_type}.json`）→ exit 3 MARKER_MISSING；executor 编排（:190-214）只跑 G1/G2 无 G4；全仓无 standalone 写 `gate-markers/G4-*.json` 的路径（grep write_gate_marker dispatcher/ 空；gates/cli.py:121 仅 G4 CLI 分支写 marker，standalone 从不调用）。 |

**误报统计**：整体误报 **0 条**（F201-F252 共 52 条全部实质成立）；部分误报 1 条（F220 计数 5→6，前轮已修正）；表述修正 1 条（F211，前轮已修正）；F212 附注、F243 机制修正、F247 范围注记均维持。

---

## 二、新发现（F253-F255）

### F253 | codex.py `_record_completion` 对 progress.json 做非原子读-改-写：并发 shenbi-dispatch 进程丢失完成记录（safe_write flock 仅覆盖写入不覆盖读取） | 漏报（并发竞态，被 F227/F235/F247/F252 链掩盖） | P2
- 证据: `src/shenbi/dispatcher/modes/codex.py:27-48`（`json.loads(progress_path.read_text())` → 内存修改 → `safe_write(progress_path, ...)` 整体覆盖）；`src/shenbi/safe_write.py`（flock 仅在写入路径 `_acquire_lock(path.parent)` 持有，读-改-写周期无锁）；`src/shenbi/pipeline/dispatch_helper.py:1864`（legacy 回退逐技能 `uv run shenbi-dispatch`）+ `src/shenbi/pipeline/parallel_dispatch.py:168-171`（Semaphore(4) + ThreadPoolExecutor 并发调 dispatch_skill）+ dispatch_helper.py:1857（`rd = round_dir or project_dir`，并发 worker 共享同一 rd/project_dir）→ 两个以上 shenbi-dispatch 进程并发到达 `_record_completion` 时 read-modify-write 交错 → 后写者基于陈旧读取覆盖先写者 → completed_skill_names/skills 丢失。
- 根因: 完成记录写入未以"读-改-写"为原子单元加锁；safe_write 的 flock 语义是"本次写入原子"，不是"读-改-写周期原子"。codex.py 的直写模式（替代 shenbi-progress mark-done 子进程）引入了进程级共享状态竞态。
- 验证命令: `PYTHONPATH=src .venv/bin/python`（/tmp 模拟两进程交错）→ 进程 A 读 → 进程 B 读 → A 写（加 skill A）→ B 写（基于陈旧读取，加 skill B）→ 最终 `completed_skill_names=['shenbi-B']`、`skills keys=['shenbi-B']`、`LOST UPDATE: True`（复现见本报告验证清单）。
- 建议方向: `_record_completion` 改为进程级文件锁（fcntl.flock 包住整个读-改-写）或使用原子合并（读-改-写在锁内完成再 safe_write）；或 legacy 并行路径串行化/禁用；补并发集成测试（两个进程同时 mark-done 不同 skill）。
- 影响注记: 当前被 F227（fresh 轮 G1 先死）/F235/F247（审计 rc=2 先于评分）/F252（评分 exit 3）掩盖——`_record_completion` 仅在 codex exec 成功 + shenbi-score 通过后到达；上述链修复后，pipeline legacy 并行 review 路径（parallel_dispatch 4 并发 → 同一 project_dir）真实触发。

### F254 | executor.run_g1/run_g2 与 codex.dispatch_codex 的 shenbi-score 子进程均无 timeout（仅 codex exec 有 600s）→ standalone 路径门/评分子进程挂起时无限阻塞 | 漏报（错误处理缺陷） | P2
- 证据: `src/shenbi/dispatcher/executor.py:114-118`（`subprocess.run(["uv","run","shenbi-validate","G1",...], capture_output=True, text=True)` 无 timeout）、`:125-137`（run_g2 同）、`src/shenbi/dispatcher/modes/codex.py:95-109`（shenbi-score 子进程同，无 timeout）；对照 `codex.py:61-66`（codex exec 显式 `timeout=600`）与 `dispatch_helper.py:1865`（pipeline legacy 对 shenbi-dispatch 外层有 `timeout=cli_timeout`）。
- 根因: PR-20 只给 LLM exec 设了超时；门 CLI/评分 CLI 子进程（`uv run` 可因 venv 重同步/锁竞争/网络慢）无超时保护 → standalone 直连路径（无外层 timeout）无限挂起，`dispatch_with_write_audit` 的 finally 审计也等不到。
- 验证命令: read executor.py:111-139 + codex.py:60-110（对比 timeout 参数有无）；`grep -n "timeout" src/shenbi/dispatcher/executor.py src/shenbi/dispatcher/modes/codex.py` → 仅 codex.py:63 一处。
- 建议方向: run_g1/run_g2/shenbi-score 子进程加 timeout（如 60s/120s）并捕获 TimeoutExpired → 记 gate/rc/stderr 后返回失败（与 F212 修复联动）；或复用 dispatch_helper 的 cli_timeout 派生逻辑。
- 影响注记: pipeline 路径被外层 cli_timeout（dispatch_helper.py:1865）兜底；仅 standalone 直连（first-novel 流程、CI 单测）暴露。挂起概率低（本地快 CLI），P2 为"边界/错误处理缺陷"。

### F255 | derive_file_type 的 REPORT 分支先于 truth 交集返回 → kind=report 且写 truth 概念文件的技能被归 "report"，G2.11 truth 只增不删保护对其静默失效 | 漏报（分类错位，F239/F244 同链的新触发面） | P2
- 证据: `src/shenbi/dispatcher/executor.py:76-86`（`if kind == OutputKind.REPORT: return "report"` **先于** `outputs & _truth_file_set()` 交集）；`src/shenbi/gates/g2.py:290-313`（G2.11 仅在 `file_type == "truth"` 时触发）；实测 4 个 kind=report 技能契约写 truth 概念：shenbi-score-volume（`truth/volume_score_trend.md`）、shenbi-score-arc（`truth/audit_drift.md`）、shenbi-score-stratum（`truth/book_spine.md`）、shenbi-review-arc-payoff（`truth/audit_drift.md` + `truth/arc_payoff_trend.md`）。
- 根因: 分类优先级把 REPORT 短路放在 truth 判定之前——report-kind 技能即使写 truth 文件也被归 "report" → G2.11（防删 .bak diff）永不运行；即便 G1.4 侧 BACKUP_SKILLS 覆盖了其中 3 个（score-arc/score-stratum/review-arc-payoff 经 updates 命中 truth concept，g1.py:76；score-volume 的 truth/volume_score_trend.md 在 writes 非 updates → 不在 BACKUP_SKILLS，实测），G2.11 仍因 file_type 错位不可达。与 F239（glob 写者误归 chapter）触发面不同：F239 是"glob 与 concept 精确名不相等"，本条是"kind 短路先于 truth 交集"。
- 验证命令: `PYTHONPATH=src .venv/bin/python` → `derive_file_type("shenbi-score-volume") == "report"`、`"shenbi-score-arc"→"report"`、`"shenbi-score-stratum"→"report"`、`"shenbi-review-arc-payoff"→"report"`（均写 truth/ 概念路径）；对照 worldbuilding（kind=artifact）→ "chapter"（F239 面）；read g2.py:290（G2.11 的 file_type 门）。
- 建议方向: derive_file_type 把 truth/decisions 交集判定移到 kind 分支之前（report 技能若写 truth 概念 → "truth"，与 G3.3 的路径启发一致）；或 G2.11 按路径（truth/ 前缀）而非 file_type 触发；补 report-kind truth 写者分类测试。
- 影响注记: 当前被 F227（standalone 门序）/F245（pipeline 卷/弧技能 N 占位符崩溃）掩盖；两者修复后，score-*/review-arc-payoff 的 truth 输出在重试轮可被删除行而不触发任何告警（只增不删铁律失效）。

### M 级观察（随文记录，均非 finding）
- `phase_runner.py:191,196-211`（跨区）是 derive_file_type/derive_output_files 的**新生产消费者**：rglob 兜底 + file_type 覆盖逻辑使 F228（glob 不展开）/F239（分类错位）/F255（report 短路）的影响面扩大到 T2/T3 phase-runner 路径——非新根因，F228/F239 修复清单应含此消费点（M）
- `gates/shared.py:257` 是 F217 的第 7 处 CONSTELLATION 区间源（表 15-30%、<10 或 >40 才 FAIL）——SKILL.md 自相矛盾面增至 7 处（M，F217 增补）
- `skill_utils/trope_detection/match_tropes.py:61` 生产读取 tropeInventory → F221 的四源漂移中"模型缺字段"一面有真实消费方背书（M，F221 增补）
- `contracts/thresholds.py` CONVERGENCE=100 与 T3_PASS=94 全仓零消费者（grep 仅 thresholds.py 定义处）——死常量（M，F207 同类）
- `audit/write_audit.py:48-56` 对 watch 内**未变更** .md 也跑 parse_records/parse_markdown_table/drift 检测——若 pending_hooks.md 在派发前已有 drift，本次派发（即使未触碰该文件）被判 GATE_FAIL，归因错位（M，F238/F242 关联；perf 面 review4 已列）

---

## 三、严重度异议（不修改前九轮，仅提理由）

1. **支持 F214 P2 → P1**（评分独立性证据被生产方伪造）：run_gate_g3（dispatch_helper.py:1934-1945）在 progress.json 缺失时主动写入伪造 `current_scorer_agent`+`scoring_history`（键 "agent" 非 "agent_id" → G3.5 prior_agents 恒空 → PASS）→ G3.4 fail-closed 以生产方自写证据放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"显式契约。本轮反证尝试（独立路径不跑 G3 故 P2）不成立——pipeline 主路径真实触发。注：伪造点在 pipeline/（跨区），Z2 侧（codex.py 不产证）本体仍 P2。
2. **支持 F218 P2 → P1**（部分匹配静默丢字段）：AGENTS.md:87-89 "If a declared field is missing ... returns the full file + logs WARN" 在部分缺失时两处均不满足（fields.py:55-64 实测）；truth 文件随剧情演进缺 section 属正常路径 → 下游静默在信息不全状态决策。
3. **支持 F217 P2 → P1（低置信维持）**：模型 [15,35] 对标准卷过松（25-35 过门违反 SKILL.md:91 15-25）对开卷过严（36-40 误拒，:193 允许 30-40），且与 :257 表（15-30%）亦矛盾——SKILL.md 内部 7 处自相矛盾，权威区间未定，维持低置信供协调者裁决。
4. **支持 F228 P2 → P1（低置信维持）**：重试轮 G2 仍因 glob 字面 not-found + 误套规则恒败，9 glob 写技能任何轮次 G2 不可过；受 F227 掩盖维持低置信。
5. **支持 F238 P2 → P1（低置信维持）**：audit_writes 对 malformed `## hooks` YAML 裸抛发生在 finally 内，使 record_audit_outcome 永不执行（审计记录丢失）——"崩溃仍审计"保证（executor.py:245-246）失效；崩溃本身 loud，维持低置信。
6. **支持 F229 P2 → P1（低置信维持）**：decisions 型技能（chapter-drafting/context-composing/chapter-revision——主章节写作链路）的 .md 散文在 G2 被 continue 跳过，G2.6/G2.8/G2.9 对章节正文整体静默缺失；受 F227/F247 掩盖维持低置信。
7. **支持 F220 维持 P2（不降 M）**：矩阵小系 docstring 明示过渡态；但 F236 使 glob 写者 file-level 兜底失效，过渡态非零风险。
8. **新提（低置信）F251 P2 → P1**：评分产物三重不匹配使 standalone 轮次对 G5.1（T2 边界门，find_report → no_report FAIL）与 G_RECONCILE（GR.2 恒 FAIL）确定性失败——正常路径功能错误；但受 F252（exit 3 前置）掩盖，且 G5/G_RECONCILE 是否在 standalone 流程被评估依赖外部编排，故低置信，供协调者裁决（F227/F235/F247/F252 修复后独立阻塞评分消费面时升 P1）。
9. **F253/F254/F255 严重度自评说明**：三者均被 F227/F235/F247/F252 链掩盖（触发需先通过门序+审计+评分），按"当前不可达/修复后独立阻塞"判 P2；若协调者以决策表"并发竞态→P1"字面执行，F253 可升 P1（需 F227 等修复后重评）。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider -q -o addopts="" tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与前九轮一致）；`COVERAGE_FILE=/tmp/z2r9-cov` 定向 → executor 74%（缺 169-188 = F211/F222 区域、251-262 must-test、151、192-193、202-203、212-214、224-225、270）、codex 75%（缺 29-30、54、67-68、71-72、77-78、81-85、112）——与各轮报告吻合。
- 运行时探针清单（全部在 /tmp 与 python -c / heredoc，零仓库写入）：
  - F216 空 customRules + disabled dim → model_validate PASS（bug 复现）
  - F218 md 部分匹配 → matched=True、仅命中 section、无 WARN
  - F230 json 键规范化缺失 → matched=False + 全文 + WARN（U+3000 声明键）
  - F232 Selection/Adjustment 空串/空白 rationale → ACCEPTED（两侧不对称）
  - F235 审计面端到端推理复证（PROJECT_DIR=REPO_ROOT + watch 命中仓库根 tracked truth 文件 → violations → rc=2）
  - F240 extract_chapter 中文 → None / 英文 → 7
  - F245 resolve_chapter_path(volume-N/AC-NNN, 7) 全按章节填；resolve_or_skip(volume-N, None) → None
  - F247 `_declared_patterns`（无 chapter）vs `_audit_watch_paths`（带 chapter）声明/监视面粒度不一致（read 复证）
  - F248 from_markdown chapter_sequence 恒空 + beats/scene_types 解析
  - F251 find_report 对 `-subagent.json` → None；G0.10 glob 不匹配；GR.2 rsplit 解析 → status='?' FAIL（read 复证）
  - F252 shenbi-score marker 强制 exit 3 先于分数校验（read 复证 + scoring.py:364-377/:187-224）
  - **F253（新）**：模拟交错读-改-写 → LOST UPDATE: True
  - **F255（新）**：derive_file_type 对 4 个 report-kind truth 写者全归 "report"
  - F221 补证：match_tropes.py 生产读 tropeInventory；F210 hooks 行号（40/79-80/94）；F217 SKILL.md 7 处区间；F242 plant/track DEPRECATED
- 未独立复跑项：shenbi-sync-contracts 幂等（初审 claim #1，跨区）；codex exec 实机执行（无 codex CLI 环境，F243/F250 的 cwd/角色/指令差异为 git-show 证据 + 代码路径推理）。
- 已知 must-test 覆盖缺口（初审已列、仍开放，非本轮新发现）：registry.py known_skill_names（73-75）、decisions.py 35/58（rationale>100）、genre_config.py 95-100（F216 路径）、executor.py 169-188（F211/F222 路径）、executor.py 251-262（dispatch 异常重抛路径）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未 git add/commit；`git show`/`git ls-files` 仅读历史；探针均在 /tmp 目录与 python -c / heredoc。

---

## 五、统计

- 新 finding：**3 条**（F253-F255：P2×3）；M 级观察 5 条随文
- 误报：**0 条整体误报**（F201-F252 共 52 条全部实质成立；F220 部分误报/计数修正维持；F211 表述修正维持；F212 附注、F243 机制修正、F247 范围注记均维持）
- 严重度异议：**8 条**（支持已有 7 条：F214→P1、F218→P1、F217→P1 低置信、F228→P1 低置信、F238→P1 低置信、F229→P1 低置信、F220 维持 P2；**新提 1 条低置信：F251 P2→P1**）
- 前九轮 findings 判定：F201-F252 共 52 条，全部实质成立、0 条整体不成立、2 条需修正（F211 表述、F220 计数，均维持）
- **0 新 finding 判定：否**（F253 为 progress.json 并发读-改-写竞态，F254 为门/评分子进程无超时，F255 为 report-kind truth 写者 G2.11 保护静默失效——三者均为独立根因、被 F227/F235/F247/F252 链掩盖；其中 F255 与 F239/F244 同链但触发面独立（kind 短路 vs glob 不匹配），F253 为前九轮从未触及的并发一致性面）
