# Z1 分区第十二轮独立复核报告（agent m）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`~`Z1.review11.md`（F114–F157），共 **57 条**（前十一轮新发现 44 条）
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g5.py`、`gates/gate_manifest.py`、`audit/_shared.py`、`dispatcher/executor.py`、`dispatcher/cli.py`、`.venv/bin/shenbi-score`、`tests/tiers/deps.json`、82 份 rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`、端到端实跑 phase_runner CLI（仅写 /tmp）、真实模块导入**验证）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑、真实控制台脚本实跑与 /tmp 内端到端模拟（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F158–F199（实际产出 F158–F160，共 3 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：3**（F158 P1、F159 M、F160 M；**无新 P0**）
- **误报数：0**（F101–F157 全部 57 条成立；无一条被证伪。F115/F140 两个 P1 经本复核**独立实跑/复跑确认无反证**）
- **严重度异议数：新增 0**（维持既有共识：F109 M→P2；F115/F140 P1 无反证维持；F134 维持 P2）
- **0 新 finding 判定：否**（3 条新漏报：F158 phase 路径穿越——前十二轮全部遗漏且影响面最大的新发现；F159 sync_contracts 日志通道；F160 emit_json BrokenPipe）

---

## 1. 复核结论表（F101–F157 逐条判定）

| 编号 | 标题 | 前十二轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 实证：`git ls-files -s docs/framework/dependency-dag.json tests/tiers/deps.json skills/shenbi-worldbuilding/SKILL.md` → 均 `100644`；`stat -f %Sp` → 均 `-rw-------`（0600）。根因确认 mkstemp 固定 0600（safe_write.py:105）+ os.replace 继承（:111）；git 不追踪 rw 位 → CI `git diff --exit-code` 不可见 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | 重读 error_guidance.py:24-50：5 个顶层 docs/*.md 全 MISSING；`ls tests/build_registry.py` → No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 仅 doc_url 破） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | error_guidance/recovery 引用的 6 类全无 raise 站点；`grep get_guidance/ERROR_GUIDANCE/RECOVERY_STRATEGIES` src/ 除定义文件外 0 消费者（重读确认） |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | 独立走查 g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **PASS（g5.py:202-203，非 R1 所称 SKIP）**；:188 assert 在 `-O` 下失效。R3 对 R1 的事实修正独立确认 |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一无 exc_info 的 `except Exception`；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | phase_runner 全文 0 引用（重读确认）；仅 gates/cli.py:131-136 手动入口 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——抛 OSError 时 finally 不执行 → 锁 fd/.lock 泄漏；:89-91 unlink 后 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M（R1 起异议 M→P2） | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert 实证；docstring :110 "Raises on mismatch" 与 `-O` 下守卫消失冲突；工具仍会静默写 deps.json（:201）。**同意 R1–R11 共识：M→P2**（运行时正确性守卫而非文案） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo(\|\.backup("` src/ 0 命中（重读确认）；同族 `backup()`（paths.py:33-35）亦 0 调用方 |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | `issubclass(subprocess.TimeoutExpired, OSError)==False`（mro: TimeoutExpired→SubprocessError→Exception）；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有（重读确认） |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **独立实跑真实解析器**：`load_applicability('tests/tiers/t1-skill/shenbi-worldbuilding/rubric.md')` → `{}`；`filter_dimensions_by_test_type(dims, p, 'bug-hunt')` → 返回全部 9 维（**含 dim 4 Prose quality**）；worldbuilding rubric:43 明示 "scoring.py renormalizes weights…" 与实现不符。对照组 chapter-drafting 过滤确实生效。command-to-give.md:64,71 文档化路径可复现。按决策表"正常路径可复现功能错误"维持 P1 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径阻断 → 双入口语义不一致 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区 |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | `grep _phase` 仅 :321,:331 两处赋值；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 :58 已开 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-121 G4 bug-hunt/clean 分支不写标记（仅 :121 generative 分支写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 test_type 拼接（重读确认） |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | cmd_pre_score（:242-275）无 `state["steps"].append(...)`（重读逐行确认）；其余 5 命令均记录 |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS/capability_fs src/ tools/` → 仅自身 + 测试；0 生产消费者 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；缺参 → IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"`（排除 .git）→ 0 命中；ADR-0009 已迁移 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | **实跑实证**（F140 验证输出）：不带 --subagent 的文档化评分命令输出 `"scored_by": "interactive"` |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均合法 JSON → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫（对照 :300 `gate_type` 有 `idx+1 < len` 守卫） |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 status |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立（维持 P2）** | :145-159 只查空 scores 不查空 dims；:169-170 total_weight==0 → 0。触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析），缺"正常路径可复现"，维持 P2 |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 `NaN` 字面量 → NaN 通过校验并产出 NaN final |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src/ → 仅 status.py:78 定义，0 消费 |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志（重读确认）；g4/generic.py 空文件 → SKIP 后 vacuous PASS |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | `grep -c "log\."` 两文件均 0（排除 import 行） |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块（ls 实证） |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **独立实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**：完整 10 维 scores + `--test-type generative` → 计算完全成功但 **EXIT CODE: 1**，stderr 1191 字节为完整 result dict repr；`.venv/bin/shenbi-score` 包装 `sys.exit(main())`（cat 脚本实证）→ `SystemExit(dict)` → exit 1；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 依赖退出码 → `_record_completion` 永不可达。按决策表"违反显式契约 + 正常路径可复现"维持 P1 |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | `isinstance(True, (int, float))` → True；`True < 0 or True > 100` 恒 False → 校验通过 → 按 1 计分 |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | filter docstring :107-109 宣称 "renormalize weights" 但 :135-136 仅过滤；compute_score :171-172 以 total_weight≠100 发 warning |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过，无 json.loads、无 crash）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/缺 state 键/非法 JSON → 裸 TypeError/KeyError/JSONDecodeError traceback |
| F145 | phase_runner `load_deps()` 无任何错误处理 | M | **成立** | :32-33 `json.loads(...)` 无 try；:245/:318 调用；对照 g5.py:36-39 try/except → 结构化 FAIL |
| F146 | scoring.check_gate_markers t2 分支对 deps.json 仅 exists() 守卫 | M | **成立** | :205-207 `if deps_path.exists(): deps = json.loads(...)`——exists() 只防缺失不防损坏 |
| F147 | sync_contracts.main() deps.json 读取（:195）与 load_registry（:165）均无守卫 | M | **成立** | :195 `json.loads(DEPS_PATH.read_text(...))` 无 try；:165 `load_registry()` ContractError 裸上抛；对比 :167-172 bail 有 log.error + return 1 |
| F148 | phase_runner post-skill 契约声明但磁盘缺失/为空的输出静默丢弃 | P2 | **成立** | :196-200 `exists()+st_size>0` 静默过滤缺失项无日志；:211-216 空 output_files → G2 SKIP + G4 空文件 vacuous PASS + PASS marker → 相位 OK 推进 |
| F149 | scoring filter 对无维度数字的排除 scope 静默 no-op（模板自身触发） | P2 | **成立** | **独立复跑**：`_template/rubric.md` 表头 `\| Dimension scope \|` 正确匹配，"Prose/narrative quality" 行标 No 但 scope 无维度数字 → excluded_nums 恒空 → 过滤前后均为 [1,2]（no-op 实证）；R2 备查 #5 "44 个未触发" 与事实不符 |
| F150 | phase_runner run_gate("G2") 缺第 4 参 project_dir → G2.7 重要章节 ceiling 恒失效 | M | **成立** | :213 3 参；gates/cli.py:60-63 G2 分支支持 `pd = arg(3, None)`；g2.py:237 `_is_important_chapter(fp, project_dir or "")` + :382-384 `if not project_dir: return False` |
| F151 | scoring.py:419 scores.json 非数字键静默丢弃且无 WARNING | M | **成立** | :417-419 `{int(k): v for k, v in raw.items() if k.lstrip("-").isdigit()}`——非数字键在 validate_scores 之前被静默过滤 |
| F152 | load_rubric kill-switch 解析漏 "→ detection dimension = 0" 类条目 | M | **成立** | :43-47 只匹配 "total score = 0"/"phase = 0"/"pipeline = 0" |
| F153 | sync_contracts.render_body_view 的 `skill` 参数从未使用（死参数） | M | **成立** | :124 定义 + :149 传参外，函数体内 `skill` 0 命中 |
| F154 | safe_write flock 主路径与 O_EXCL 回退路径互不感知对方锁原语 | M | **成立**（触发面窄，维持 M） | :54-62 flock 分支从不检查 lockfile 存在性；:63-91 O_EXCL 分支从不检查目录 flock 状态；:61 `except (ImportError, OSError): pass` 为唯一切换点——两原语互斥语义不共享 |
| F155 | scoring.check_scorer_agreement NaN 误报 agreed；flag_score_collapse 单维度误报 all_identical | M | **成立** | `diff = abs(a - b)` 对 NaN 恒 False → NaN 永不进 disputed；`len(set(values)) == 1` 无 `len(values) >= 2` 守卫。函数当前 dead（Z5 F500），接线前置缺陷 |
| F156 | phase_runner/executor 双路径对混合输出施加单一 derive_file_type → G2 decisions 分支整体跳过 .md | P2 | **成立** | **独立验证**：`derive_file_type('shenbi-chapter-drafting')` → `decisions`；`derive_output_files('shenbi-chapter-drafting', 5)` → `['chapters/chapter-5.md', 'chapters/chapter-5-decisions.json']`（混合输出实证）；g2.py:83-84 decisions 分支 `if not fp.endswith(".json"): continue` → .md 仅过 G2.1-2.3 |
| F157 | scoring 交互模式 `input()` 提示词写入 stdout 数据通道 | M | **成立** | :388/:397 `input()` prompt 经 sys.stdout 输出；:462 emit_json 追加同一 stdout → 混合体不可 JSON 解析 |

**前十二轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——独立走查 g5.py:115/202-205 确认 **R3 修正正确**；F101 波及文件数递增记录属实（本复核时点三文件均 100644/0600 实证）；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致。**无新的前轮事实错误需要修正。** F115/F140 的反证尝试（协议路径不实际使用、`scoring.main` 返回 dict 是 API 设计）全部失败——两 P1 均属实，**误报数 0**。

---

## 2. 新发现（F158–F160）

F158 | phase_runner 的 `phase` 参数未净化直接拼接进状态文件路径：`load_state`/`save_state` 双向路径穿越，可写出 round_dir 外任意位置（含覆盖既有文件），并造成跨 phase 状态文件读写混淆 | security/path-traversal | **P1**（决策表 P0 例举"路径穿越"，合并层可议升 P0）
- 证据: src/shenbi/phase_runner.py:37（`state_file = Path(round_dir) / "phase-state" / f"{phase}.json"`——读侧）；:46-47（`state_file = state_dir / f"{state['phase']}.json"`——写侧，同一构造）；:365/:368/:371/:374/:377/:380（六条命令全部以 `args[0]` 直接作为 phase 传入，无任何白名单/净化）；合法相位名 = tests/tiers/deps.json `t2-phases` 9 键（genesis/architecture/planning/drafting/audit/management/import/foundation/short-story）
- 根因: phase 拼接进 `f"{phase}.json"` 文件名且允许 `..`/`/`；`save_state` 的 `state_dir.mkdir(parents=True)` + `safe_write`（os.replace）把写操作送出 round_dir；`load_state` 读侧同样穿越（可把任意既有 JSON 当状态读取）。cmd_start 的 G5-FAIL 分支（:126-129）仍执行 `save_state` → 即使门禁失败也写出穿越位置。正常管线恒用 deps.json 固定相位名故不触发，但 CLI 契约未做任何约束（与 F124"缺参校验"、F105"None 传参"同族：CLI 层输入校验空洞的第三个、也是最严重的一个站点）
- 验证命令+输出: ① `python3 -c "from pathlib import Path; print(Path('/tmp/r')/'phase-state'/f'{'../../../../etc/evil'}.json')"` → `/tmp/r/phase-state/../../../../etc/evil.json`（resolve 后 `/private/etc/evil.json`）；② **端到端实跑**：`PYTHONPATH=src python3 -c "sys.argv=['shenbi-phase','start','../../../../tmp/zb12_escaped','--round-dir','/tmp/zb12_r']; from shenbi.phase_runner import main; main()"` → exit 1（G5 FAIL，expected）但 **`/tmp/zb12_escaped.json` 被创建**（内容为 state JSON，`find /tmp/zb12_r -type f` 0 个文件——写入完全逃逸 round_dir）；③ 代码走查 :126-129（G5 FAIL 分支无条件 save_state）
- 建议方向: phase 入参白名单校验（`phase in deps["t2-phases"]` 或正则 `^[a-z][a-z0-9-]*$`），非法 → emit_json ERROR + exit 1；或在 load_state/save_state 内对 state_file `resolve()` 后断言 `relative_to(round_dir/phase-state)`（fail-loud）；补"path-like phase → 拒绝"单测。（严重度说明：按决策表"路径穿越"例举属 P0 类，但触发需调用方主动传入路径形态 phase（正常管线不产生），本复核按 P1 上报并留合并层裁量）

F159 | sync_contracts 从不调用 `configure_logging()`：全部日志输出到 stdout（违反 logging.py:8-9 "All output goes to stderr" 与 cli_utils stdout=数据通道 契约），且 `SHENBI_LOG_FORMAT` 环境变量对该工具静默失效 | error | **M**
- 证据: src/shenbi/sync_contracts.py:25（`log = get_logger(__name__)` 模块级绑定）、:163（`def main()` 无 configure_logging 调用）；src/shenbi/logging.py:19-42（configure 定义）、:8-9（stderr 契约）、:21（`SHENBI_LOG_FORMAT` 决定渲染器）；对比 Z1 其余 CLI 入口均配置：phase_runner.py:337、scoring.py:283、gates/cli.py:57（`grep configure_logging src/shenbi` → sync_contracts 唯一缺失）
- 根因: structlog 未 configure 时默认 `PrintLoggerFactory` → `sys.stdout`（logging.py:36 显式 `file=sys.stderr` 仅在 configure 后生效）；sync_contracts 是 Z1 四个 CLI 入口中唯一不调用 configure 的。`shenbi-sync-contracts` 是 CI 幂等检查的执行方（ci.yml:80-88），其 `sync_complete`/`phase_synced`/`no_contracts_loaded` 日志全部落 stdout；CI 虽不解析该 stdout，但通道契约与格式配置（json 渲染器）双重失效
- 验证命令+输出: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -c "import sys; sys.argv=['x']; import shenbi.sync_contracts as sc; print('configure in file:', 'configure_logging' in open('src/shenbi/sync_contracts.py').read()); sc.log.info('sync_complete', skills=74)"` → stdout: `configure in file: False` + `2026-08-14 03:50:11 [info     ] sync_complete skills=74`；stderr: 空
- 建议方向: sync_contracts.main() 首行 `configure_logging()`（与 phase_runner/scoring/gates 对齐）；补"sync_contracts 日志走 stderr"测试（capsys 断言 err 非空、out 为空）

F160 | cli_utils.emit_json 不处理 BrokenPipeError：下游管道消费方提前关闭（`| head` 等）→ 裸 traceback 污染 stderr 且掩盖真实退出码 | error | **M**（触发需消费方提前关闭；低频率）
- 证据: src/shenbi/cli_utils.py:20-22（`sys.stdout.write(...)` + `"\n"` + `flush()`，无任何 except）；emit_json 是 phase_runner/scoring/gates 全部 stdout 数据通道的唯一出口（cli_utils.py:1-6 docstring "parsed by downstream tools"）——上游任何 CLI 都继承该缺陷
- 根因: Python CLI 经典管道鲁棒性缺口——消费者（`| head -c N`、`| jq .x` 读一行即退）关闭读端后，`write`/`flush` 抛 BrokenPipeError 且未捕获 → traceback（含 cli_utils.py:20 帧）到 stderr + 进程以错误码退出；对 scoring 1-2KB 输出通常被 64KB 管道缓冲掩盖，大输出（gate 结果含长 checks）或快速关闭的消费方下触发。前十二轮对 cli_utils 的零 finding 使该唯一剩余未覆盖分支被遗漏
- 验证命令+输出: 模拟（monkeypatch sys.stdout.write 抛 BrokenPipeError(32)）→ `Traceback ... File ".../shenbi/cli_utils.py", line 20, in emit_json ... BrokenPipeError: [Errno 32] Broken pipe`（未捕获，实证）
- 建议方向: emit_json 捕获 BrokenPipeError（`except BrokenPipeError: os.dup2(os.open(os.devnull, os.O_WRONLY), sys.stdout.fileno()); sys.exit(1)` 标准配方，或捕获后静默退出），避免 traceback 与退出码污染；与 F140 的退出码修复一并做 CLI 级管道测试

---

## 3. 严重度异议汇总

| 编号 | 前十二轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2（维持既有异议）** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移。按决策表 P2"防御深度"而非 M"文案" |
| F115 | P1 | **维持 P1（无反证）** | 独立实跑真实解析器：worldbuilding applicability `{}`、filter 返回全 9 维（含 N/A 豁免 dim 4，10% 权重）、rubric:43 明示 renormalize 承诺、command-to-give.md:64,71 文档化路径可复现。**反证尝试失败** |
| F140 | P1 | **维持 P1（无反证）** | 独立实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr 1191 字节 result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 `_record_completion` 永不可达；`scoring.main` 无库调用方。**反证尝试失败** |
| F134 | P2 | **维持 P2（不议升）** | 触发需 rubric 不可解析（82 份真实 rubric 维度全部正常解析），缺"正常路径可复现" |
| F158 | （新）P1 | **本复核定级 P1** | 路径穿越属决策表 P0 例举类，但触发需调用方主动传路径形态 phase（正常管线恒用 deps.json 固定 9 相位名）；按"安全类缺陷 + 非正常路径触发"定 P1，合并层可议 P0 |

其余 52 条严重度无异议。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读；另核实跨区依赖：gates/cli.py、gates/shared.py、gates/g2.py、gates/g5.py、gates/gate_manifest.py、audit/_shared.py、dispatcher/executor.py、dispatcher/cli.py、.venv/bin/shenbi-score、tests/tiers/deps.json、82 份 rubric）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型核实无洞；F123 覆盖无接线）、`logging.py`（双渲染器/stderr 自洽；SHENBI_LOG_FORMAT 校验为前轮已记录低价值项）、`paths.py`（read/write 语义正确；F112 覆盖死代码）、`error_guidance.py`/`recovery.py`（F103 覆盖 dead；F138 未用 log）、`exceptions.py`（F103/F110 覆盖）、`status.py`（F127/F136 覆盖）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`safe_write.py`（F101/F108/F119/F133/F154 覆盖；剩余分支为防御/平台边界）、`scoring.py`（F104/F106/F115/F118/F120/F126/F129/F131/F134/F135/F140/F141/F142/F146/F149/F151/F152/F155/F157 覆盖）、`phase_runner.py`（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145/F148/F150/F156 覆盖，另 F158 为本轮新增）、`sync_contracts.py`（F109/F117/F130/F147/F153 覆盖，另 F159 为本轮新增）、`cli_utils.py`（F160 为本轮新增——前十二轮零 finding 文件的唯一剩余未覆盖分支）
- 新发现分布：`phase_runner.py`（F158——phase 路径穿越）、`sync_contracts.py`（F159——日志通道）、`cli_utils.py`（F160——BrokenPipe）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **gate_manifest pipeline-manifest.json 无生产读取方**：`get_gate_result`（gate_manifest.py:93-108）全仓仅 tests/gates/test_gate_manifest.py 消费；`record_gate_result` 仅 phase_runner `_record_gate_manifest`（:54-75）调用。R1 F116 证据已记录"G2 结果只进 pipeline-manifest（best-effort）…无任何下游消费"，且 plan 文档（plans/archive/2026-07-19-11）显示该结构为设计中的特性——不重复立 finding，供合并层确认是否需接线或声明 dead。
2. **G2.10 注释与实现不符**（gates/g2.py:273 注释 "chapter files only"，实现对所有非 decisions 文件生效）：truth/report 文件含 >10% "待填充" 会 FAIL——gates 区（非 Z1），且 worldbuilding SKILL.md:82 声称初始化 truth 模板 body 含"待填充"（若真实产出触发则为 gates 区缺陷），供合并层转交 gates 区。
3. **R1 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。
4. **AGENTS.md "67 functional + 2 meta = 69" 与现状 74 skills 目录不符**（R7 备查 #7）：非 Z1 区，供合并层转交。
5. **`--gate-only` G3 参数形状错配 / 无 --files vacuous G2 PASS / UNIMPLEMENTED 门 exit 0 / `--chapter abc` ValueError / `--interactive` 必须 argv[2] / find_flag 值不校验 / scoring 缺失 rubric/scores 裸 traceback / `--test-type` 无枚举校验 / emit_json allow_nan**——R2-R11 备查已记录，同意不重复。

---

## 5. 其他说明

- 前十二轮"P1×2（F115/F140）"结论：本复核独立实跑/复跑后**无反证，维持**；误报数 0（57/57 成立）。
- 新发现 F158–F160：F158 为 P1 级路径穿越（本分区复核 12 轮以来首个新 P1，与前两 P1 站点不同：F115=rubric 解析、F140=CLI 退出码、F158=CLI 输入校验路径穿越）；F159/F160 为 M 级（日志通道契约、管道鲁棒性）。三者均不改变"无 P0"结论。
- F158 与既有 finding 的关系：F124（缺参 IndexError）与 F105（"None" 传参）同属"CLI 层输入校验空洞"家族，但 F158 是**路径穿越（任意文件写）**这一独立且最严重的站点，前十二轮对 phase 参数的内容校验从未检查。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140/F126 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本）**实证；F115/F134/F135/F141/F142/F149/F151/F152/F155 经 `PYTHONPATH=src` 纯内存导入复跑；F156 经 `derive_file_type`/`derive_output_files` 真实函数实证；F158 经**端到端实跑 phase_runner CLI（写目标仅限 /tmp）**实证穿越写出；F159 经真实模块导入捕获 stdout/stderr 实证；临时文件仅写 /tmp，未触碰仓库文件。

---

## 6. 返回摘要

- **复核统计：新 finding 3（F158 P1 / F159 M / F160 M）/ 误报数 0 / 新增严重度异议 0（维持 F109 M→P2 共识；F115/F140 的 P1 无反证维持；F134 维持 P2）**
- **0 新 finding 判定：否**（3 条新漏报：F158 phase_runner phase 参数路径穿越（可写出 round_dir 外任意 JSON、覆盖既有文件、跨 phase 状态混淆，端到端实证）；F159 sync_contracts 缺失 configure_logging 致日志走 stdout + SHENBI_LOG_FORMAT 失效；F160 cli_utils.emit_json 不处理 BrokenPipeError。均无新 P0）
