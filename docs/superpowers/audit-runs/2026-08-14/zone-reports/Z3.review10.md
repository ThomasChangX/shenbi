# Z3 分区第十轮独立复核报告 — src/shenbi/pipeline/（fresh-context 全量重读）

> 复核 agent：Z3 第十轮独立复核（本文件 `Z3.review10.md`，与初审 `Z3.a.md`、一轮至九轮 `Z3.review.md`~`Z3.review9.md` 全部隔离）
> 复核范围：34/34 文件全量逐行重读（`__init__.py` 1 行 → `chapter_loop.py` 3053 行 → `dispatch_helper.py` 1986 行 → `cli.py` 939 行 → `triggers.py` 656 行全部读完），逐条独立验证 F300-F390；以真实生产项目 `novel-output/xinghuo-ranqiong/` 为格式/行为对照基准
> 本轮发现编号：F391-F395（5 条新发现，其中 3 条 P2、2 条 M；**无新增 P0/P1**）；**新增误报 0 条**（F325 维持误报撤销，本轮独立复证）；严重度异议 2 条（F302/F364 的 P1→P0 依据，均为此前已提出、本轮独立复证支持）
> 只读禁令遵守：除本报告外未创建/修改/删除任何仓库文件（验证全部在 /tmp 临时目录与只读 python/G4-CLI 中完成）

---

## 〇、本轮最重要结论（先读）

1. **F324（P0）维持成立，无反证**：本轮实测 `read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`。生产 volume_map 全中文（实测 `volume_map.md:3` `## 第一卷：觉醒之火（第1-15章）`、`:7` `**章节范围**: 第1章 - 第15章（共15章）`），`_END_RE`/`_RANGE_RE` 零命中。影响链（卷边界触发全家 / `novel.json` 无 total_chapters → `cli.py:219 if total > 0:` 跳过全部章间触发 → 无 book_closure → pipeline 永不进入 CLOSURE / 章节节点提取裸数字 `\|\s*1\s*\|` 误中跨卷钩子表 `| 1 | 梵天铭文金属片… | 物品 |` 行）全部成立。
2. **F325（误报）维持撤销**：本轮 `load_contract` 实证 4 个 group skill 契约 `writes:` 即旧式路径（`group-factual → ['audits/chapter-N-continuity.md','audits/chapter-N-world-rules.md','audits/chapter-N-pacing.md']` 等）；`_any_audit_has_findings`（chapter_loop.py:1779-1793）扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING 被 route 正则与文件扫描双重命中 → revision 执行。F325 原判"并行波写 group-*.md、旧式名永不产生"与磁盘/契约事实相反，维持误报裁定。
3. **F379（P1）本轮全步骤 prompt-build 实证**：closure 10 步中 step 2/4/5/6/10 抛 `UnresolvedPathError`（`truth/arcs/arc-N.md`、`audits/volume-N-score.md`、`audits/volume-N-payoff.md`、`audits/chapter-N-long-span.md`、`snapshots/chapter-NNN/*`）——closure 正常路径 5/10 步永不派发。
4. **F371（P1）本轮 G4 CLI 实证**：`final-snapshot/` 目录 → `G4.gen.not_found`（不存在）/`G4.gen.read_error`（存在）两态恒 FAIL。
5. **F364（P1 贴 P0）维持成立**：`crash_recovery.py:66` `atexit.register(_emergency_cleanup)`，`:144-148` 正常退出无条件 `clear_staging`；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交）成立。
6. **本轮新发现（F391-F395）均为 P2/M 级**：G1 门禁在 API/IDE 派发路径缺失（F391）、decisions JSON 恢复失败 ValueError 穿透 CLI（F392）、review_checklist hook deliverables 恒空（F393）、`reset_emergency_state` 注释漂移（F394）、`_handle_timeout_gracefully` 误导性日志（F395）。前九轮对主路径正确性（P0/P1 级）的覆盖已收敛——本轮无新增 P0/P1。
7. **版本偏移注记（沿用三~九轮）**：生产 pipeline-state 为 Plan 18 重构前旧版管线产物（steps_done 含旧 skill 名），state 级行为证据不可外推当前代码；volume_map/chapters/audits/plans/truth 文件格式为稳定跨版本证据；`which codex → /opt/homebrew/bin/codex`（本环境 IDE 路径激活）。

---

## 一、复核结论总表（F300-F390 逐条判定）

> 判定基于本轮独立 read/grep/uv run python/G4-CLI/契约 load_contract 实证与真实产物对照。全部 34 文件逐行重读。**误报 1 条（F325，已撤销维持）**；**事实修正 0 条新增**；严重度异议 2 条（F302/F364，维持既有异议）。

| 编号 | 判定 | 严重度裁定 | 理由（本轮独立验证） |
|---|---|---|---|
| F300 | **成立** | P1 维持，无异议 | dispatch_helper.py:734 `content.replace("<", "\u003c")` 源码字面量求值即 `<`，恒 no-op（Python 语义实证） |
| F301 | **成立** | P1 维持（贴 P0 校准维持），无异议 | 并行波 chapter_loop.py:2515 无条件触发 → :2607 跳至 `_LAST_AUDIT_IDX+1` → 串行块 :2924（run_audit_layer）/ :2993（resonance 解析/趋势落盘）不可达（本轮逐行确认无其他调用点）；cs.resonance_score 并行路径恒 None；resonance_trend.md 由契约 updates 自身写入（本轮 load_contract 实证） |
| F302 | **成立** | P1 维持（**严重度异议支持 P1→P0：决策表 P0 例字面列明 "TokenLedger 少计"；裁定留 stage 4**） | `dispatch_skill(state=)` 仅 chapter_loop.py:2799（串行可及）/ :2966（串行 BLOCKING 环，不可达）；genesis:302、closure:253、triggers:561、parallel_dispatch:86-91、cli:123/751 均未传（本轮 grep 实证）；`getattr(state,"chapter",0)`（dispatch_helper.py:1335）恒 0 |
| F303 | **成立** | P1 维持，无异议 | create_differential_snapshot 唯一调用点 chapter_loop.py:1720（死函数内）；restore_from_snapshot 无 src 调用方；step 15 `pipeline-pre-revision-snapshot` 空转；cmd_rollback not_implemented（cli.py:823-843） |
| F304 | **成立** | P1 维持，无异议 | RetryExhaustedError 仅 raise（chapter_loop.py:626/:2882）无 except（本轮 grep src/ 0 个 except）；cmd_next/cmd_resume 仅捕 FileNotFoundError；ESCALATION 解决只清 retry_counts 不清 retry_budget_consumed（machine.py:85-93） |
| F305 | **成立但框定需修正（同意二~九轮 P2）** | P2 维持，无异议 | 生产审计"BLOCKING"全为否定式散文 → `_SEVERITY_BLOCKING_RE` 不命中 → route 判定正确；route==NO_REVISION 时 `_is_revision_skipped`（:2755）先于 `_should_run_step` 短路。残余真缺口 = CRITICAL-only 路由且文件无字面 "BLOCKING"/"FAIL" 时 `_any_audit_has_findings`（:1797）为 False → revision 被门控跳过 + :1886 revision_count 先 +1 记账漂移 |
| F306 | **成立** | P2 维持，无异议 | audit_context_cache.py:71 `truth/volume_map.md` 全仓 0 写入方（本轮实测生产该路径 MISSING、volume_context len=0）；:97 子串匹配缺陷 |
| F307 | **成立（核心）** | P2 维持，无异议；注记维持 | `step.skill == "shenbi-foreshadowing-plant"` 分支（:2742-2749）不可达（CHAPTER_STEPS 无该 skill）；hook_planting 无生产调用；`_parse_hook_entries`（hook_planting.py:144）对 MH-001/H-N01 行会接受但 raw_op=="advance" 非 plant/open 被跳过 |
| F308 | **成立** | P2 维持，无异议 | compact_pipeline_state/_archive_chapter_state（state.py:383-428）无生产调用 |
| F309 | **成立** | P2 维持，无异议 | cli.py 无 _validate_state_consistency/_heal_current_step 引用（注释 "called from cli.py on resume" 过期） |
| F310 | **成立** | P2 维持，无异议 | check_volume_alignment 仅测试调用；chapter_loop.py:2133 独立实现已接线（:3036） |
| F311 | **成立** | P2 维持，无异议 | extract_scr（scr_extractor.py:428-433）缓存存在即返回，无 mtime/内容校验；revision 改写正文后不失效 |
| F312 | **成立** | P2 维持（latent），无异议 | `_serialize_yaml_records`（truth_io.py:273-278）只写 frontmatter 丢 body；upsert_yaml 无生产调用方（本轮 grep：唯一生产 upsert 调用 chapter_loop.py:3014 为 upsert_markdown_row） |
| F313 | **成立** | P1 维持，无异议（注意 F379 交互） | `_resolve_closure_g4_path`（closure.py:148-159）对含 N 路径一律按卷号替换；step 6 `audits/chapter-N-long-span.md` 的 N 为章号（契约 writes 实证）→ G4 校验错位；API/IDE 路径下步骤 6 在派发阶段即失败（F379），本 finding 的 G4 失效面在 legacy 路径与 F379 修复后激活 |
| F314 | **成立** | P2 维持，无异议 | plan_skeleton.py:218 / context_assemble.py:264 均无 `chapter < activation_ch` 上界 |
| F315 | **成立** | P2 维持，无异议 | DispatchResult（dispatch_helper.py:332-339）无 `.result` 属性 → `_merge_step_result`（state.py:521）恒 no-op |
| F316 | **成立** | P2 维持，无异议 | revision_router.py:199 原始 glob 无去重（F325 反证后 revision 可达，缺口活跃） |
| F317 | **成立** | P2 维持，无异议 | MAX_DISPATCH_RETRIES/MAX_AUDIT_RETRIES（error_handler.py:36-37）无使用点；dispatch/gate 重试绑定 max_revision_retries（:52） |
| F318 | **成立** | M 维持，无异议 | 逐条核验：F318.1 base=900 vs docstring 300（:93/100）；F318.4 "7 skills" vs 实际 6（:2550-2551）；F318.6 scr_extractor.py:313 三次相同引号判断；F318.7 blocking 形参未用（revision_router.py:86）；F318.8 同意前几轮"窗口内无 save_state、crash 时磁盘为上一 save 点"（M） |
| F319 | **成立** | P2 维持，无异议 | cli.py:541-554 MODIFY 无 PER_CHAPTER 分支；modify_feedback（:552）无章号绑定、注入下一次串行 dispatch（:2775-2780）；同源缺口见 F365 |
| F322 | **成立** | P2 维持，无异议 | get_checklist/generate_static_template/generate_chapter_delta/_extract_hooks_from_plan 无生产调用（grep 实证；generate_review_checklist 本体已在 dispatch_helper.py:767 接线）；`P\d+-[a-z]+` 不匹配生产数字 id |
| F323 | **成立** | P2 维持，无异议 | _load_genre_config_cached（dispatch_helper.py:194-201）无调用方；路径 config/genre-config.json 与生产不符 |
| **F324** | **成立（P0 维持，无反证）** | **P0 维持，无异议** | 本轮实测：`read_volume_boundaries(production) → set()`、`_resolve_volume_at_runtime(pd,15) → None`；生产 volume_map 全中文（`:3` `## 第一卷：觉醒之火（第1-15章）`、`:7` `**章节范围**: 第1章 - 第15章（共15章）`）；novel.json 无 total_chapters。影响链全部成立 |
| **F325** | **不成立（误报，维持二~九轮撤销）** | **P1 裁定撤回维持** | 本轮 load_contract 实证 group skill 契约 writes 即旧式路径（group-factual→continuity/world-rules/pacing 等）；生产 audits/ 722 文件全旧式名、group-* 数=0（前几轮实测）；`_any_audit_has_findings` 扫描的 13 个旧式名正是磁盘真实产物 → 真实 BLOCKING（`严重度: BLOCKING`）被 route 正则与文件扫描双重命中 → revision 执行 |
| F326 | **成立** | P1 维持，无异议 | lifecycle `updates: ['truth/pending_hooks.md']`、state-settling `updates` 含同文件（本轮 load_contract 实证）；并行 post-draft（:2405-2419）两线程均以 uses_staging 写 `staging/truth/pending_hooks.md`，未走 assert_parallelizable/per-path read-merge 锁 → 整文件替换、后写者赢、先写者变更整体丢失 |
| F327 | **成立** | P2 维持，无异议 | decide_revision（revision_router.py:119-140）无生产调用；resonance floor 仅 log（并行路径 cs.resonance_score 恒 None → 恒过）。本轮实测 `_parse_resonance_score` 对生产 chapter-10-resonance.md 返回 None（仅 "通过" 命中，无 Score 键） |
| F328 | **成立** | P2 维持，无异议 | CONDITIONAL_STEPS（:266-290）无迭代；_should_run_recall/_should_run_drift/_check_conditional_resolve/_update_last_recall_manifest/_drift_guidance_triggered（读不存在的 `state.drift_alerts`，:1766）死簇 |
| F329 | **成立** | P2 维持，无异议；注记：完整重复面见 F387 | 本轮实测 production genre-config auditDimensions 含 sensitivity=true → `get_active_genre_audits` 含 shenbi-review-sensitivity；core 波（:2551）与 genre 波双发同一 skill 写同一文件 |
| F330 | **成立** | P2 维持，无异议 | cli.py:723 `_verify_truth_integrity` 返回值未接；函数仅 log.warning（:671-672） |
| F331 | **成立** | P2 维持，无异议 | chapter_loop.py:2664-2675 并行 G4 失败仅 log.warning 后 add_step_done+推进；lifecycle G4 失败输出经 STATE_SETTLE approve 提交路径静默入库（该路径被 F364 清空后实际"静默不入库"） |
| F332 | **成立** | P2 维持，无异议 | to_dict/from_dict（state.py:228-363）无 token_usage 键（dispatch_helper.py:1314 动态挂载）→ token 汇总跨 save 归零 |
| F333 | **成立** | P2 维持，无异议 | genesis.py:328 用 per_chapter_review_enabled 兼任 genesis G4 严格度开关；triggers._update_total_chapters（求和）vs cli._update_total_chapters（max）双实现口径 |
| F334 | **成立** | M 维持，无异议 | Z3.a.md 声称 23 findings 实际 22 条编号（F320/F321 缺失） |
| F335 | **成立** | P2 维持，无异议 | 本轮实测 `build_shared_audit_context(production, 1).chapter_text` len=0（audit_context_cache.py:49 `chapter-{chapter:03d}.md` 补零 vs 生产不补零） |
| F336 | **成立** | P2 维持，无异议 | step_timings 定义于 state.py:158，to_dict/from_dict 无该键 → 每次 save/load 归零 |
| F337 | **成立** | P2 维持，无异议 | triggers.py:443-444 `chapter >= total_chapters` 在 total==0 时恒 True；仅被 cli.py:219 守卫掩盖 |
| F338 | **成立** | P2 维持，无异议 | 并行波对 audit_results 只写 revision_route/blocking_found/audit_reports（:1880/:2615-2616），无 skill-keyed {"passed","hard_failures"} dict → `_get_audit_history` 恒 []；且 `_audit_short_name("shenbi-review-group-factual")="group-factual"` 不在 CASCADABLE_AUDITS → 双重失效 |
| F339 | **成立** | P2 维持，无异议 | `_dispatch_via_api` 返回 DispatchResult(True, 0, output_text, "")（:1671，stdout=LLM JSON 文本）；consolidate（parallel_dispatch.py:210-221）扫 stdout 行首 "BLOCKING"/"CRITICAL" 对 JSON 恒 0 命中（本轮实测真实 BLOCKING 内容在 JSON 内 0 命中）；audit_reports 记录 group-*.md 追踪标签（磁盘不存在）；本轮实测生产 chapter-1-review-summary.md 恒报 "BLOCKING Issues: 0" |
| F340 | **成立** | P1 维持，无异议 | cli.py:526-537 REJECT 仅 clear_staging+clear_checkpoint（对比 MODIFY :541-554 有游标回滚）；cmd_resume 相位转换仅 approve（:732）；genesis-complete reject → `run_genesis_step(17>=17)`（genesis.py:280-281）恒 True → 无 checkpoint、无进展、next/resume 恒报 OK——永久卡死（本轮重读确认短路语义） |
| F341 | **成立（补强维持）** | P1 维持，无异议 | 并行块 :2702-2708 无条件 set_checkpoint(STATE_SETTLE)（无 config 分支）；本轮 grep `checkpoint=CheckpointType` 仅 :148（CHAPTER_MEMO）一处 → `_advance` 的 STATE_SETTLE auto-commit 分支（:1086-1105）为不可达死代码，`state_settle_review_required` 全路径无功能效果 |
| F342 | **成立** | P2 维持，无异议 | volume_snapshot_pending（triggers.py:647-656）/add_audit_result/increment_retry/reset_retry（state.py:200-222）无生产调用 |
| F343 | **成立** | M 维持，无异议 | cli.py:294-296 dispatch_escalation(project_dir, 0, …)；revision_router.py:153 `chapter 0` 分支；对照 genesis.py:244-248 用 None |
| F344 | **成立** | M 维持，无异议 | write_safety.py:23-25 注释 "foreshadowing-track" vs 实际并发 shenbi-foreshadowing-lifecycle |
| F345 | **成立** | P1 维持，无异议 | 并行波（:2515-2630）无任何 run_gate_g3 调用（grep parallel_dispatch.py 0 命中）；6 个 audit skill 全部 requires_independent=True；对照串行路径 :2907-2917 有 G3 → 审计评分独立性在正常路径静默缺失 |
| F346 | **成立** | P2 维持，无异议 | `_HOOK_ID_RE`（truth_index.py:32）`(?:[HM]\d+|P\d*-\d+)` 对生产 plan section 7 的 `MH-001`/`H-N01` 零匹配 → master hooks 从 Route A 检索静默缺失 |
| F347 | **成立** | P2 维持，无异议 | 本轮实测生产 pending_hooks.md frontmatter keys=`[title,project,version,last_updated,type,category,status,filled_by,last_chapter,track_chapter]`（无 `hooks` 键），hook 以 body 表格呈现；`_read_pending_hooks`（context_curation.py:361-384）只读 frontmatter → 债务简报恒 "(无)" |
| F348 | **成立但机制描述部分不准（维持四~九轮修正）** | P2 维持，无异议 | character_matrix/pending_hooks 因契约 reads 已入 raw_inputs 而被跳过注入；style_profile/world_rules 注入键为幽灵路径（truth/style_profile.md、truth/world_rules.md 生产均 MISSING）→ 注入实际发生（F357） |
| F349 | **成立** | P2 维持，无异议 | cli.py:90-131 `_execute_pending_re_dispatches` 失败保留重试，无 attempt 计数/无上限/无 escalation；:200 循环顶部每次迭代重跑 |
| F350 | **成立** | M 维持，无异议 | chapter_loop.py:1875 `_create_pre_revision_backup` 在 `_route_revision_after_resonance` 内无条件调用（早于路由判定）；并行波恒调 :2621 → 每章多一份整章副本 |
| F351 | **成立** | M 维持，无异议 | `_call_llm_streaming_with_retry`（:1537-1547）无 `response_format={"type":"json_object"}`；json_mode 仅存在于提示词层；非合规输出先失败一次再重试 |
| F352 | **成立** | M 维持，无异议 | dispatch_skill 的 `timeout: int = 900` 形参（:1797）函数体内无引用，三条路径分别调 _compute_dispatch_timeout |
| F353 | **成立** | P1 维持，无异议（贴 P0 边界） | 全仓 novel.json.total_chapters 写盘仅 cli.py:167（调用点 :748，VOLUME_BOUNDARY approve 分支）与 triggers.py:394（调用点 :623，volume_boundary 分支）——两者均前置依赖 cli.py:219 `if total > 0:` 守卫，而 total 唯一来源即该字段自身（_read_total_chapters :134-144）→ 自锁；genesis 侧无写入 → F324 修复后 pipeline 仍永不完成 |
| F354 | **成立** | P1 维持，无异议 | 并行波（:2596-2630）对 core_results+genre_results **无任何 result.success 检查**，直接 consolidate → 全部 add_step_done → 推进；`_dispatch_with_retry` 失败返回 DispatchResult(False,…) 不抛异常（parallel_dispatch.py:77-128）；对照串行 `_handle_failure`（:587-671）→ 审计波全失败时章节以零审计"完成" |
| F355 | **成立** | P2 维持，无异议 | `_run_context_curation` 写 `context/chapter-N-curated.md`（:1215-1220）；grep skills/ 契约 reads 0 个引用该文件；drafting 契约 reads `context/chapter-N-context.md` → 9 节分层文档生产从不进入任何 LLM 上下文 |
| F356 | **成立** | P2 维持，无异议 | `_render_context_document`（context_curation.py:456-464）按 `_PRIORITY_ORDER` 前缀子串分桶；plan 节 source="P1 章节备忘"（:148）与 route-a:/route-b: 均不含任何桶键 → 全落 P7 |
| F357 | **成立** | P2 维持，无异议 | dispatch_helper.py:611-614 注入键 `truth/style_profile.md`（本轮实测生产 MISSING）→ `fname not in raw_inputs` 恒真 → style 内容（2000 字截断）每章每审计恒注入 |
| F358 | **成立但机制前置需修正（维持九轮修正）** | P2 维持，无异议 | :2720-2728 `except Exception` 吞掉 DriftEscalationError；drift 指令文件无消费者。机制链在更早处已断（F389：基线缺失 → `_check_linguistic_drift` 恒返回 None） |
| F359 | **成立** | P2 维持，无异议 | `_route_a`（context_assemble.py:117-131）text 为 `f"[{e.category}] {e.entity_id} from {e.file}"` 标签；IndexEntry.extra 被丢弃 → 实体事实既不进 Route A 文本也不经契约 reads |
| F360 | **成立** | P2 维持，无异议 | `_maybe_rebuild_truth_index`（:836-844）调 build_index（纯内存）不落盘 truth-index.json；全仓落盘点仅 genesis.py:128 与 truth_index.py:358 |
| F361 | **成立** | M 维持，无异议 | `_call_llm_streaming`/`_call_llm_streaming_with_retry` 的 early_stop_patterns 形参（:1383/:1455）无调用方传值 → :1422-1429 early-stop 分支恒不执行 |
| F362 | **成立** | M 维持，无异议 | cli.py:877/:879 `print()`；AGENTS.md "No print() in framework code; use structlog" |
| F363 | **成立** | M 维持，无异议 | run_triggered_skills（triggers.py:555-619）按序 dispatch，任一失败即 return False，无 per-skill 进度记录（仅 state.last_trigger_failure）→ 重试重发已成功步骤 |
| F364 | **成立** | **P1 维持；严重度异议支持 P1→P0（依据决策表"数据损坏/丢失 + 契约被静默违反"字面命中；裁定留 stage 4）** | `atexit.register(_emergency_cleanup)`（crash_recovery.py:66）正常进程退出无条件 `clear_staging`（:144-148）+ 把持久化 current_step 改写为 `EMERGENCY_SHUTDOWN_AT_<skill>`（:113-127）；消费链（approve 时 staging 缺失 → cli.py:348-353 捕 FileNotFoundError 后 pass → plan/truth 永不提交）成立（本轮重读代码 + 前几轮 /tmp 沙箱实验） |
| F365 | **成立** | P2 维持，无异议 | cli.py:545-546 MODIFY 对 STATE_SETTLE 回滚 `step_index = 7`；CHAPTER_STEPS[7] checkpoint=None → resume 后串行重跑 → `_advance(7)` 不设 checkpoint → step_index=8 直接进审计波；重跑产物无人提交 + 重审门被跳过 |
| F366 | **成立** | P2 维持，无异议 | 本轮 grep：`genesis_review_required`/`volume_boundary_review_required`/`style_learning_interval`/`context_budget_override` 全 src/ 0 消费点；`snapshot_retention_chapters` 仅 chapter_loop.py:1374 docstring |
| F367 | **成立** | M 维持，无异议 | step 2 chapter-planning `calls_context_assembly=True`（:151）→ `_run_context_assembly`（:1126-1159）读 `plans/chapter-N-plan.md`——step 2 时当章 plan 尚不存在 → FileNotFoundError → 捕获 → `_write_minimal_context_fallback`；且 chapter-planning 契约 reads 不含 context/chapter-N-context.md → 组装成功也不被消费 |
| F368 | **成立** | M 维持（latent），无异议 | hook_planting.py:260-269 `_append_to_pending_hooks` 读 frontmatter hooks → 只回写 `---\n{hooks: all_hooks}\n---` 丢弃 body；生产 pending_hooks.md 的 hook 数据在 body 表格 → 接线即破坏生产格式 |
| F369 | **成立** | M 维持，无异议 | cli.py:421 `parse_seed(args.seed_file)` → seed_parser.py:102-103 对缺失路径 raise FileNotFoundError；cmd_init 无 try/except；main() 无顶层异常处理 |
| F370 | **成立** | M 维持，无异议 | dispatch_helper.py:1646-1656 `parsed_dict = {f.path: f.content for f in output.files}`——`output.decisions`（:329）从未读取/写入；json_mode 提示词（:680-694）却要求模型输出 decisions 键 |
| F371 | **成立** | P1 维持，无异议（注意 F379 交互） | 本轮 G4 CLI 实测：`shenbi-snapshot-manage "final-snapshot/"` → 不存在 `G4.gen.not_found`、存在 `G4.gen.read_error`——两态恒 FAIL；`_resolve_closure_g4_path`（closure.py:148-159）对无 N 路径原样返回目录 → closure step 10 重试×3 后 ESCALATION。API/IDE 路径下 step 10 在派发阶段即失败（F379），本 finding 的 G4 失效面在 legacy 路径与 F379 修复后激活 |
| F372 | **成立** | P2 维持（latent），无异议 | `_style_profile_is_stale`（triggers.py:103-124）`chapters_dir.glob("chapter-*.md")` 计数把 `chapter-N-pre-rev.md` 计入（对照 `_load_previous_titles` :2251 用 `chapter-(\d+)\.md` 正则排除）→ style-learning 自愈触发提前/误触发 |
| F373 | **成立** | P1 维持（潜伏），无异议 | `run_triggered_skills`（triggers.py:584）把字面 N 路径直接传 G4，dispatch 侧经 `resolve_chapter_path` 解析为章号 → 5 个 N 型触发步骤（memory-distill L2 / score-arc / score-stratum / score-volume / review-arc-payoff）G4 恒 not_found（前几轮 G4 CLI 实证 arc-N-score.md FAIL / arc-5-score.md PASS） |
| F374 | **成立** | P2 维持，无异议 | `classify_skill_write_safety('shenbi-review-resonance')` → read_only_audit（write_safety.py:45-46 前缀分类），而契约 `updates: ['truth/audit_drift.md','truth/resonance_trend.md']`（load_contract 实证）→ 并行审计波在无串行保护下并发写 truth 文件 |
| F375 | **成立** | P2 维持，无异议 | dispatch_helper.py:1667-1671 `missing` 非空仅 log.error 后 `return DispatchResult(True, ...)`；`_write_parsed_outputs`（:1153-1168）对 parsed 缺失路径走空内容 skip → state-settling 6+1 文件漏写其一仍 success |
| F376 | **成立** | M 维持，无异议 | truth_embed.py:243-251 `update` 无 `--text` 时落 `emit_json(OK)` 不做任何嵌入 |
| F377 | **成立** | M 维持，无异议 | llm_output_integrity.py:51 `VERDICT_MARKERS` 含通用词 "通过"，:149 `has_verdict` 恒 True → `G4.ac.no_verdict`（:150-153）几乎不触发 |
| F378 | **成立** | P2 维持，无异议 | `context/volume-N-complete.json` 仅 chapter_loop.py:955-964 读取、全仓 0 写入方 → `_check_volume_completion` 恒 False → `_check_soft_fail_escalation`（:997）经 check_escalation 恒发 volume_objective_missed 信号；`last_trigger_failure` 仅 triggers.py:575/593 写入、无消费者 |
| F379 | **成立** | P1 维持，无异议 | 本轮全步骤 prompt-build 执行实证：closure step 2/4/5/6/10 抛 `UnresolvedPathError`；`extract_chapter('Execute ... for book closure (step 2)...') → None`；`_dispatch_via_api`/`_dispatch_via_ide`（:1515-1516/:1718-1719）捕获后返回 DispatchResult(False,"Prompt build failed") → `_handle_closure_failure` 重试×3 → ESCALATION——10 步中 5 步永不派发 |
| F380 | **成立** | P2 维持，无异议 | genesis step 16（shenbi-anchor-curate，optional=True）契约 writes 含 `benchmarks/anchors/AC-NNN.md` → chapter=None 时抛 UnresolvedPathError → `_handle_failure`（genesis.py:222-225）对 optional 步骤直接跳过 → anchors 永不产出且无错误信号 |
| F381 | **成立** | P2 维持，无异议 | 本轮复现：`chapter-3-revision-decisions.json` 存在 + `revision_count=0` → `heal_state_counters` 返回 `['revision_count_healed:ch3:0->1']`。`_ensure_revision_decisions_exists`（:2759 对 NO_REVISION 章节也写回退文件）+ `_is_revision_routed`（:1928-1933 对 'no-revision' 返回 True）→ 全部未修订章节的 revision_count 被 heal 抬到 ≥1 |
| F382 | **成立** | P2 维持，无异议 | 本轮 grep：`safe_write(` 于 src/shenbi/pipeline/*.py 中 `round_dir` 命中数=0；`MARK_DONE`/`TraceWriter` 于 pipeline 无命中 → trace 目录只有 materialize 自造事件 → progress.json 物化为"全 pending"失真视图 |
| F383 | **成立** | P2 维持（latent/条件触发），无异议 | dispatch_helper.py:903-922 `_parse_file_outputs` 无 `### FILE:` 匹配时返回 `{"__stdout__": response}`；:1159 `content = parsed.get(rel_path, parsed.get("__stdout__", ""))` 把整段 stdout 写入每个字面输出路径；`_dispatch_via_ide`（:1753-1759）以 parsed=None 调用（本环境 codex 已安装 → IDE 路径激活） |
| F384 | **成立** | P2 维持，无异议 | truth_embed.py:120-121 `embed_and_store` 函数体内每次 `st.SentenceTransformer("bge-large-zh")` 全新加载；genesis `_update_route_b` 逐 hook/rule 调 embed_and_store（F384 依据代码 + 前几轮生产实体计数） |
| F385 | **成立** | P2 维持，无异议 | dispatch_helper.py:1832-1841 无 API key 且无 IDE CLI → legacy 子进程；`dispatcher/modes/internal.py:22` internal 模式硬拒 → 必失败死路由；uses_staging 被忽略（:1844-1849） |
| F386 | **成立** | P2 维持，无异议 | `_check_soft_fail_escalation`（:967-1035）派发 escalation-review 但无任何 set_checkpoint；调用方随后把步骤正常推进（:2865-2905 落至 advance）→ 升级不暂停、报告成孤儿 |
| F387 | **成立** | P2 维持（贴 P1 边界），无异议 | 本轮实测 production genre-config auditDimensions 的 worldRules/motivation/dialogue/texture 全 true；group 波契约 writes（group-character→dialogue/motivation、group-craft→texture）与 genre 波单维 skill 契约 writes 同名 → 每章重复审计并覆盖 group 波产物（F329 的完整版） |
| F388 | **成立** | P2 维持，无异议 | `_audit_context_coverage`（chapter_loop.py:2104-2125）docstring 声称"pipeline resume 初始化时调用"，cli 从未接线（grep 0 命中） |
| F389 | **成立** | P2 维持，无异议 | `style/linguistic_baseline.json` 唯一写入方 `establish_baseline`（skill_utils/drift_detection/baseline.py:78）全仓 0 调用方（grep 实证）；`_check_linguistic_drift`（chapter_loop.py:2013-2019）缺基线 → `return None` → spec §3.4 三级干预生产不可达 |
| F390 | **成立** | M 维持，无异议 | cli.py:533-535 `Path(args.feedback).read_text(...)` 在 try 块内；:569-571 `except FileNotFoundError: "project not found"` → feedback 文件缺失误报项目不存在 |

**误报统计**：F300-F390 中仅 F325 为误报（二轮撤销、三~九轮维持，本轮独立复证维持——group skill 契约 writes 即旧式路径 + 生产 722 文件全旧式名）。F348 为"成立但机制描述部分不准"（非误报）。**本轮未发现新的误报**。

**严重度异议**：2 条（均为既有异议的独立复证支持，无新增）：
1. **F364（P1→P0）**：决策表 P0 触发条件"数据损坏/丢失 + 契约被静默违反"字面命中——人审通过的 plan/truth 在每次正常退出时被确定性清空、持久化状态被改写为 EMERGENCY_SHUTDOWN；但 staging 门控由人工 review 兜底、正文文件不丢，P1 亦可辩护——裁定留 stage 4。
2. **F302（P1→P0）**：决策表 P0 例字面列明 "TokenLedger 少计"——账本系统性少计 + chapter 恒 0；但账本为成本侧车不影响正文执行，P1 亦可辩护——裁定留 stage 4。

另注记（非正式异议）：F353/F371/F373/F379 均贴 P0 边界（"pipeline 永不完成"根因族，F324 修复后即激活）。

---

## 二、新发现（F391-F395）

### F391 | API/IDE 派发路径完全跳过 G1 input-readiness 门禁：缺失契约 reads 静默丢弃、无失败语义——模块 docstring 声称的 "G1/G2 经 dispatcher 内部执行" 仅 legacy 子进程路径成立 | error | P2

- 证据：
  - `dispatch_helper.py:3-6`（模块 docstring："The dispatcher runs G1 (input readiness) and G2 (output structure) internally; this module adds G3 ... and G4 ... on top"）；
  - `dispatch_helper.py:1827-1841`（路由：API key → `_dispatch_via_api`；IDE CLI → `_dispatch_via_ide`——两条路径均只调 `_build_skill_prompt` + LLM 调用 + `_write_parsed_outputs`，**无任何 G1/G2 门禁调用**）；
  - `dispatch_helper.py:390-407`（`_resolve_read_path` 对缺失文件返回 `[]`——`_build_skill_prompt`（:569-595）将缺失 reads 静默排除出 raw_inputs，无 warning、无失败）；
  - 唯一内建 G1/G2 的是 legacy 子进程路径（`dispatcher/executor.py:112-130` 经 `shenbi-validate G1/G2`，且 G2 在 pipeline 模式还显式跳过 :206 "g2_skipped_pipeline"）；
  - `grep -n "run_gate_g1\|run_gate_g2\|gates.cli.*G1\|gates.cli.*G2" src/shenbi/pipeline/` → 0 命中（仅 run_gate_g3/run_gate_g4 存在）。
- 根因：重构引入 API/IDE 直连路径时未迁移 dispatcher 子进程的 G1（input-readiness）职责；模块 docstring 仍是 legacy-only 描述。
- 验证命令+输出：
  ```
  grep -n "run_gate_g1\|run_gate_g2" src/shenbi/pipeline/*.py   # → 无命中
  grep -rn "G1" src/shenbi/pipeline/dispatch_helper.py           # → 仅 :382 _G1_SKIP_ENV_VAR（legacy 专用）、:1201 注释
  ```
- 影响：API/IDE 路径（本环境 codex 已装 → IDE 路径激活）下，技能的关键契约 reads（如章节正文、plan）缺失时 dispatch 照常进行、prompt 静默缺输入，质量门禁 G1 被绕过——与 F345（并行波缺 G3）同族但独立：G1 缺失影响全部 API/IDE 派发（含串行 drafting/planning），G3 缺失仅限并行波。
- 建议方向：`_build_skill_prompt` 对契约 reads 全部解析失败/缺失时返回显式失败（或 log.error 并携带 missing 清单），与 legacy G1 语义对齐；或 API/IDE 路径在 dispatch 前显式调用 `shenbi-validate G1`；同步修正模块 docstring。

### F392 | decisions JSON 恢复失败的 ValueError 穿透 dispatch→CLI：API/IDE 路径裸 traceback、无 checkpoint、无 JSON 结果（F304 同族、异常源不同） | error | P2

- 证据：
  - `dispatch_helper.py:1113-1119`（`_write_one`：`content = _validate_json_output(...)` 的 ValueError 被 `raise` 重抛，注释 "Pipeline must stop rather than persist corrupt data"）；
  - `dispatch_helper.py:1657-1663`（`_dispatch_via_api` 仅 `except DispatchWriteFailureError`——ValueError 不被捕获）与 `:1760-1766`（`_dispatch_via_ide` 同款）；
  - `dispatch_helper.py:826-848`（`_validate_json_output` 对无法 raw_decode 的内容 raise ValueError；对 recovery 出的 shenbi-decisions-v1 对象缺失必填字段也 raise ValueError）；
  - 传播链：`dispatch_skill`（:1791-1880，无 except）→ 串行 `_run_chapter_step_impl` :2794（无 try）→ `run_chapter_step`（仅 try/finally）→ `_orchestrate_to_checkpoint`（cli.py:175-308，无 try）→ `cmd_next`/`cmd_resume`（仅捕 FileNotFoundError）→ traceback。
- 根因：错误处理只覆盖 DispatchWriteFailureError，未覆盖 `_validate_json_output` 的 ValueError；"stop rather than persist" 以裸崩形式实现而非转 ESCALATION checkpoint（对照 `_handle_failure` 的升级语义）。
- 验证命令+输出：
  ```
  uv run python -c "
  import json
  from pathlib import Path
  from shenbi.pipeline.dispatch_helper import _validate_json_output
  content = '{\"\$schema\": \"shenbi-decisions-v1\", \"skill\": \"shenbi-chapter-revision\"} trailing'
  _validate_json_output(content, Path('chapters/chapter-1-revision-decisions.json'))"
  # → ValueError: Recovered decisions JSON for ... failed schema validation (chapter/produced_at required)
  #   （错误在 _dispatch_via_api 不被捕获 → 冒泡至 CLI 顶层）
  ```
- 影响：API/IDE 路径下，任一 .json 契约输出（chapter-N-decisions.json 等）被模型截断/污染且恢复后 schema 不完整时，`pipeline next`/`resume` 直接抛 traceback——进程退出时叠加 F364（atexit 清 staging），无 checkpoint、无 JSON 结果，且可能已部分落盘先行文件（原子写保证单文件完整，但批次内已写文件保留）。IDE 路径下 codex 输出无 `### FILE:` 标记时（F383 前提），`.json` 目标文件内容为 prose → 同样触发本 ValueError（.md 目标走 F383 的静默污染，.json 目标走本 crash）。
- 建议方向：`_dispatch_via_api`/`_dispatch_via_ide` 增捕 ValueError → 返回 `DispatchResult(False, ..., f"Output validation failed: {exc}")` 走 `_handle_failure` 重试/升级（与 DispatchWriteFailureError 同处理）；或至少在 cmd_next/cmd_resume 顶层加通用异常捕获转 ESCALATION。

### F393 | review_checklist._extract_hook_deliverables 仅读 frontmatter hooks：生产 hook 数据在 body 表格 → 注入每个审计 prompt 的 "hook_deliverables" 恒空（F347 同根、新消费面） | error | P2

- 证据：
  - `review_checklist.py:315-375`（`_extract_hook_deliverables` 只解析 YAML frontmatter `hooks` 列表；无 body 兜底）；
  - `review_checklist.py:184`（`_build_checklist` 调 `_extract_hook_deliverables(project_dir, chapter)` → `hook_deliverables` 字段）；
  - `dispatch_helper.py:760-768`（`_build_skill_prompt` 对全部 review skill 注入 `generate_review_checklist` 结果——并行审计波与串行审计均消费）；
  - 生产格式：`truth/pending_hooks.md` frontmatter keys=`[title,project,version,last_updated,type,category,status,filled_by,last_chapter,track_chapter]`（本轮实测 `has hooks key: False`），hook 数据在 body 表格（`| P0-4 ... |`）。
- 根因：与 F347（context_curation._read_pending_hooks）同根——生产/生命周期 skill 以 body 表格存储 hook 数据，解析器假设 frontmatter `hooks` 列表；review_checklist 是第三个 frontmatter-only 消费点（F347 债务简报、F368 hook_planting、本 finding）。
- 验证命令+输出：
  ```
  uv run python -c "
  from shenbi.pipeline.review_checklist import _extract_hook_deliverables
  from pathlib import Path
  print(_extract_hook_deliverables(Path('novel-output/xinghuo-ranqiong'), 5))"
  # → []   （生产 pending_hooks body 有 79 个 P0-* hook 命中，但 frontmatter 无 hooks 键）
  ```
- 影响：审计 checklist 的 `hook_deliverables` 字段（PLANTED 钩子清单、urgency/attention 分级）在生产恒空——每个审计 prompt 缺失活跃伏笔信息；与 F347（债务简报恒 "(无)"）叠加后，hook 意识层整体 inert（truth_index 检索侧 F346、种植侧 F307 已断，此处为消费侧第三断点）。
- 建议方向：`_extract_hook_deliverables` 增加 body 表格解析兜底（复用 truth_index 的 body 扫描或与 `_read_pending_hooks` 统一为"frontmatter + body 双源"）；与 F346/F347/F368 的 hook 生态修复合并。

### F394 | crash_recovery.reset_emergency_state 注释声称 "remove any atexit hooks registered by earlier tests" 但从未调用 atexit.unregister，且 register 无去重 | error | M

- 证据：`crash_recovery.py:39-42`（注释 "and remove any atexit hooks registered by earlier tests" 之后仅 `signal.signal(...)` 两次，无 `atexit.unregister`）；`grep -rn "atexit.unregister" src/shenbi/` → 0 命中；`crash_recovery.py:66`（`atexit.register(_emergency_cleanup)` 无去重注册）。
- 根因：注释先行、实现未落（与 F318 注释漂移同族）；`reset_emergency_state` 只重置信号与模块标志，未清理 atexit 回调。
- 验证命令+输出：`grep -rn "atexit.unregister" src/ tests/` → 0 命中（read crash_recovery.py:28-43）。
- 影响：xdist 共享模块全局下，测试多次调用 `register_emergency_handlers` 后 atexit 回调累积，进程退出时 `_emergency_cleanup` 重复执行（每次多一次 save_state/snapshot/clear_staging 噪音）；生产单进程下与 F364 的"逐章追加注册"放大面同源（review8 已注记）——本 finding 单列注释漂移（M），F364 已覆盖行为缺陷。
- 建议方向：`reset_emergency_state` 补 `atexit.unregister(_emergency_cleanup)`，或注释删除该句。

### F395 | _dispatch_via_api 对任意异常调用 _handle_timeout_gracefully：非超时错误也打 "dispatch_timeout ... resolution=saving_partial_output" 日志，而该函数不保存任何部分输出 | error | M

- 证据：`dispatch_helper.py:1548-1551`（`except Exception as exc: _handle_timeout_gracefully(skill, chapter); log.error("api_call_failed", ...)`——对**所有** API 异常调用超时处理函数）；`dispatch_helper.py:116-126`（`_handle_timeout_gracefully` 日志 `resolution="saving_partial_output"`，函数体仅 log，无任何保存动作——注释 "actual handling depends on skill"）。
- 根因：异常分类缺失（超时/限流/4xx 混用同一处理）；函数名与日志声称的"保存部分输出"未实现。
- 验证命令+输出：read dispatch_helper.py:116-126 与 :1548-1551（函数体无 safe_write；except 块对任何异常无条件调用）。
- 影响：`pipeline` 日志在 4xx/5xx/网络错误时误报 "dispatch_timeout ... saving_partial_output"，误导排障（以为部分输出已落盘）；不改变执行结果（DispatchResult(False) 照常返回），M 级。
- 建议方向：`except` 分支按异常类型分流（httpx.TimeoutException → `_handle_timeout_gracefully`；其余 → 直接 log.error），或删除 `_handle_timeout_gracefully` 的误导性 resolution 日志。

---

## 三、覆盖空洞声明

- 复核重读文件：**34/34**（`zones/Z3.files` 全部文件逐行重读；`__init__.py` 1 行至 `chapter_loop.py` 3053 行、`dispatch_helper.py` 1986 行、`cli.py` 939 行、`triggers.py` 656 行等全部读完）。
- 本轮新增验证资产（全部实际运行）：真实项目 `novel-output/xinghuo-ranqiong/`（volume_map 473 行中文格式 → `read_volume_boundaries → set()`、`_resolve_volume_at_runtime → None`；`truth/pending_hooks.md` frontmatter 无 hooks 键 → `_extract_hook_deliverables → []`；`audits/chapter-10-resonance.md` → `_parse_resonance_score` None；`audits/chapter-1-review-summary.md` 恒报 0 问题；genre-config auditDimensions 全 true；style/ 仅 style_profile.md、无 linguistic_baseline.json）；`uv run python` 实证：**closure 10 步全量 prompt-build 执行**（F379 复证）、**`_validate_json_output` 恢复失败 ValueError 实证**（F392）、**`heal_state_counters` revision_count 抬升**（F381 复证）、**`build_shared_audit_context` chapter_text/volume_context 恒空**（F335/F306 复证）、**consolidate 对 JSON stdout 0 命中**（F339 复证）；**G4 CLI 直接执行**（F371 复证：final-snapshot/ 两态恒 FAIL）；load_contract 契约 writes/updates（group-*/resonance/lifecycle/state-settling/planning/drafting/revision/character-design）。
- 前九轮覆盖空洞（本轮已补）：(1) **API/IDE 路径 G1 门禁缺失**——前九轮只审了并行波 G3（F345）与 decisions 侧车（F370），从未对照模块 docstring 的 "G1/G2 内部执行" 声明与 API/IDE 直连路径的门禁实现 → F391；(2) **decisions JSON 恢复失败的 ValueError 穿透**——F304 只覆盖 RetryExhaustedError，未覆盖 `_validate_json_output` 的 ValueError 传播链 → F392；(3) **review_checklist hook deliverables 消费面**——F347 只覆盖 context_curation 债务简报，未覆盖注入各审计 prompt 的 checklist 字段 → F393；(4) **reset_emergency_state 注释漂移**（F394）与 **超时处理函数误导性日志**（F395）。
- 剩余覆盖洞（沿用前九轮并确认）：(a) 生产 pipeline-state.json 为旧版管线产物，state 级行为证据不可外推当前代码（文件格式证据不受影响）；(b) IDE CLI 的实际 codex 输出格式未做端到端执行验证（F383/F392 为静态推演 + 单元实证，未真实调用 codex exec）；(c) 测试集未全量重跑；(d) skills/ 契约文件作为证据使用（不在 Z3 清单内），group skill 契约旧式 writes 是 F325 反证的关键。

---

## 四、汇总

- **新 finding 数**：5（F391-F393 = P2、F394-F395 = M；**无新增 P0/P1**）
- **误报数**：0 新增（F300-F390 中仅 F325 为误报，二~九轮已撤销、本轮独立复证维持；F348 维持"成立但机制描述部分不准"）
- **严重度异议数**：2（F302 P1→P0 依据支持、F364 P1→P0 依据支持——均为此前已提出、本轮独立复证，裁定留 stage 4；另注记 F353/F371/F373/F379 贴 P0 边界）
- **事实修正（不改严重度）**：0 条新增（F358 机制前置修正与 F324 修复完整性注记已在九轮完成，本轮复证无新修正）
- **0 新 finding 判定**：**否** —— 本轮仍发现 3 条 P2 漏报（F391 API/IDE 路径 G1 缺失、F392 decisions ValueError 穿透、F393 checklist hook deliverables 恒空）与 2 条 M（F394/F395）；F324（P0）维持成立、F325（误报）维持撤销、F340/F341/F345/F353/F354/F364/F371/F373/F379（P1）全部维持成立。前九轮共 67 条编号 finding（F300-F390，其中 F325 误报撤销），本轮新 5 条（F391-F395，无 P0/P1）。收敛目标（0 新 finding）**未达成**，但连续两轮（九轮 7 条、十轮 5 条）均为 P2/M 级，且本轮 5 条全部属于"门禁覆盖/错误处理/注释日志"类别——主路径正确性（P0/P1 级）已在前九轮收敛，残余为防御性与文案类缺口。
- **校准升级提示**：F391 建议并入 F345 的"gate 覆盖完整性"族（G1 补 API/IDE 路径 + G3 补并行波一并处置）；F392 建议并入 F304 的"未捕获异常转 ESCALATION"族；F393 建议并入 F346/F347/F368 的 hook 生态修复（frontmatter/body 双源统一）；F394/F395 为 M 级注释/日志清理。

---

## 五、独立复核声明

本报告为 fresh-context 第十轮独立复核：34/34 文件逐行重读；对 F300-F390 的判定全部基于本轮独立 read/grep/uv run python/G4-CLI/契约 load_contract 实证与真实产物对照（未复用前十轮结论）；新增 5 条发现（F391-F395）均为前十轮报告未以编号形式出现的观察，其中 F391（API/IDE 路径门禁对照模块 docstring 声明）、F392（`_validate_json_output` ValueError 传播链实证）为前几轮未覆盖的新证据路径；F324/F325/F371/F379/F381/F335/F306/F339/F327 的关键实证为本轮独立复现。
