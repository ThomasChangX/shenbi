# Z1 分区第七轮独立复核报告（agent h）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`（F114–F122）+ `Z1.review2.md`（F123–F127）+ `Z1.review3.md`（F128–F133）+ `Z1.review4.md`（F134–F139）+ `Z1.review5.md`（F140–F143）+ `Z1.review6.md`（F144），共 **43 条**
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g3.py`、`gates/g5.py`、`gates/gate_manifest.py`、`audit/_shared.py`、`dispatcher/executor.py`、`contracts/legacy.py`、`contracts/graph.py`、`orchestration/scoring_bridge.py`、`command-to-give.md`、82 份 rubric、74 个 skills 目录独立核实；并**实跑 `.venv/bin/shenbi-score`** 与 `PYTHONPATH=src` 纯内存解析器全量复跑）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟；临时文件仅写 /tmp（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp` 避免写 `__pycache__`）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F145–F199（实际产出 F145，共 1 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：1**（F145，M——phase_runner `load_deps()` 无守卫的加载边界，前七轮全部遗漏的最后一个加载站点）
- **误报数：0**（前七轮 43 条全部成立；无一条被证伪。F115/F140 两个 P1 经独立复跑/实跑确认无反证）
- **严重度异议数：0 新增**（F109 的 M→P2 异议同意 R1–R6 共识；F115/F140 的 P1 无反证、维持；其余 40 条严重度无异议）
- **0 新 finding 判定：否**（1 条新漏报 F145；前七轮对"加载边界"家族只覆盖了 phase-state（F144），漏掉了同一家族中 deps.json 加载站点）

---

## 1. 复核结论表（F101–F144 逐条判定）

| 编号 | 标题 | 前七轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | `git ls-files -s`（100644）vs `stat -f %Lp`（600）实证复现：dependency-dag.json / truth-files.index.json / tests/tiers/deps.json / skills/shenbi-worldbuilding/SKILL.md 全部 0600；全仓当前 **115 个** 100644 索引文件工作树 mode≠644（R1:85 → R2:89 → R3:126 → 本轮 115，漂移持续但非单调——部分文件可能被 git checkout 恢复；结论不变）；git 不追踪 rw 位 → CI `git diff --exit-code` 不可见 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | `ls docs/{registry,gates,schemas,dispatcher,integrity}.md` 全部 No such file；`ls tests/build_registry.py` No such file；`tests/lock-tool-hashes.sh` 存在（ToolTamper 的 action 不破，仅 doc_url 破——与 R1 修正一致） |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | 独立 AST 扫描 src/ 全量 raise：直接 RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentTimeoutError/SubAgentProtocolError/RetryExhaustedError（5/22），NEVER RAISED **17 类**（字面计数；R4 按"捕获可达"折为 16 的注记合理，实质结论不变）；`grep get_guidance/RECOVERY_STRATEGIES/ERROR_GUIDANCE` 在 src/ 除定义文件外 **0 消费者** |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达 |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117 `str(None)`；独立走查 g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **`c.append({"id":"G5.3","s":"PASS",...})`（g5.py:202-203，非 R1 所称 SKIP）**；对无 expected_outputs 的 phase，G5.4 循环空、G5.5 走 SKIP → G5 整体可假 PASS。R3 对 R1 的事实修正独立确认 |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | :361-362 唯一 `except Exception`（grep 证实）；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | gates/cli.py:131-136 仅手动入口；phase_runner 全文 0 引用 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——抛 OSError 时 :117-125 finally 不执行 → flock fd/.lock 泄漏；:89-91 unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert 实证；docstring :110 "Raises on mismatch" 与 `-O` 下守卫消失冲突；工具仍会静默写 deps.json（:201）。**同意 R1–R6：M→P2**（运行时正确性守卫而非文案；R4 补充"generated 与 member_outputs 同源派生 → assert 近乎恒真"成立，失效风险主要在重构场景） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 实证 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo("` src/ 0 命中；同族 `backup()`（:33-35）亦 0 调用方 |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | :153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | `issubclass(subprocess.TimeoutExpired, OSError)==False`（mro: TimeoutExpired→SubprocessError→Exception）；:94 except 元组漏列 → 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有 |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **全量复跑 `load_applicability`（独立脚本）**：82 个 rubric 中 **38 个返回 `{}`**（26 t1 + 9 t2 + 3 t3，与 R2 修正的分项一致）；4 个 N/A 豁免技能逐条确认——worldbuilding rubric:43（dim4）/ faction-builder:46（dim8）/ location-builder:43（dim5）/ story-architecture:44（dim6）均含 "N/A — exempted … scoring.py renormalizes weights for remaining applicable dimensions"，其适用性表头为 `| # | Dimension | Bug-hunt Standard | Clean Standard |`（非解析器认的 `| Dimension scope |`）→ 解析器对它们整体 no-op → 豁免维度（10% 权重）计入 bug-hunt/clean；4 技能均有 bug-hunt/clean 测试目录；command-to-give.md:64,71 文档化 `--test-type bug-hunt/clean` 路径可复现。按决策表"正常路径可复现功能错误"维持 P1。**反证尝试失败**：唯一可能的反证（"bug-hunt 协议不实际使用这些 rubric"）被 4 技能的测试目录 + 文档路径证伪 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径阻断 → 双入口语义不一致；`grep '"g2"'` 在 phase_runner 外 0 消费 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区（generated 与 member_outputs 同源派生，两处同缺断言恒过） |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 fd 未 close 即落 O_EXCL 分支 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | gates/cli.py:107-110 bug-hunt/clean 分支不写标记（仅 :121 generative 分支写，且 shared.py:195 `result.get("status") != "PASS"` 时也不写）；:128 G6 恒写 "generative"；scoring.py:200/:220 读侧按 test_type 拼接 |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 实证；rglob 顺序 OS 相关 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `steps.append`；其余 5 命令均记录 |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS/capability_fs src/ tools/` → 仅 capability_fs.py 自身；0 生产消费者 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；缺参 → IndexError |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"` 0 命中（实测）；ADR-0009 已迁移 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | **实跑实证**：`shenbi-score <rubric> <scores.json> --test-type generative`（不带 --subagent）→ 输出 `"scored_by": "interactive"` |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均通过 → SCORED → finalize 可推进 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫；对照 :300 `gate_type` 有 `idx+1 < len` 守卫 |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)`；独立扫描 74 个 SKILL.md frontmatter 无 `---` 内嵌/CRLF（正则截断风险当前无触发文件，见备查 #3） |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers :187-224 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立** | **独立复跑**：`validate_scores({1:100,2:95}, [])` → `(True, ['WARNING: unexpected dimension keys (ignored): [1, 2]'])` + `compute_score([],…)` → 0 FAIL；全量复跑 load_rubric 确认仅 `_template/rubric.md` weight≠100（占位符，良性） |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 `NaN` 字面量 |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src/ → 仅 status.py:78 定义，0 消费 |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志（实测）；g4/generic.py 空文件 → SKIP 后 vacuous PASS |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | `grep -c "log\."` 两文件均 0（排除 import 行） |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块 |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **实跑 `.venv/bin/shenbi-score`（独立复现）**：`final_score` 计算完全成功（PASS (acceptable)）但 **EXIT CODE: 1**，stderr 1120 字节为 result dict repr；pyproject.toml:60 `shenbi-score = "shenbi.scoring:main"` → hatchling 包装 `sys.exit(main())` → `SystemExit(dict)` → exit 1；command-to-give.md:57-61 文档化 "0=成功 / 1=其他错误" 契约被违反（成功与错误同码不可区分）；codex.py:105-108 `if result.returncode != 0: return result.returncode` → `_record_completion` 永不可达。**反证尝试失败**：确认 `scoring.main` 无任何库调用方（仅 load_rubric/check_scorer_agreement/flag_score_collapse 被 import），不存在"库调用者依赖返回 dict"的抵消理由；且 `shenbi-phase`（main 返回 None→exit 0）与 `shenbi-sync-contracts`（返回 int）均正确，仅 shenbi-score 受影响 |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | `isinstance(score, (int, float))` 对 bool 为 True；`True < 0 or True > 100` 恒 False → 校验通过 → 按 1 计分 |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | :171-172 warning 与 :107-109 docstring 宣称的 filter 内重归一不符（重归一实际在 compute_score 隐式完成）；chapter-drafting bug-hunt 过滤后权重和 = 75 → 每条文档化路径恒误报 |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过，无 json.loads、无 crash）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/`{"phase":...}`（缺 state 键）/非法 JSON → 裸 TypeError/KeyError/JSONDecodeError traceback |

**前七轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 **确认 R3 修正正确**；F101 波及文件数递增记录属实（当前 115）；**无新的前轮事实错误需要修正**。

---

## 2. 新发现（F145）

F145 | phase_runner `load_deps()` 无任何错误处理：deps.json 损坏/缺失 → cmd_pre_score / cmd_finalize 裸 traceback，而同一文件的两个兄弟消费者（g5.py / scoring.py）均有守卫 | error | **M**（同族 F144 为 P2，合并层可议升）
- 证据: src/shenbi/phase_runner.py:32-33（`load_deps`：`json.loads((TESTS / "tiers" / "deps.json").read_text(...))` 无 try）+ :245（cmd_pre_score 首行调用）+ :318（cmd_finalize 调用）；对照 src/shenbi/gates/g5.py:36-39（`try: deps = jload(...) except (json.JSONDecodeError, OSError, ValueError): return fail("G5", [], "scoring", ["G5.0:deps.json unreadable or malformed"])`——结构化 FAIL）；src/shenbi/scoring.py:205-206（check_gate_markers t2 分支 `if deps_path.exists():` 存在性守卫）
- 根因: phase_runner 是 deps.json 的**唯一无守卫消费者**：g5.py 走 jload+try/except（损坏 → 门 FAIL 信封），scoring.py 走 exists() 守卫（缺失 → 检查段跳过），仅 phase_runner 的 load_deps 直接把 `FileNotFoundError`（文件缺失）/`JSONDecodeError`（文件损坏）抛成裸 traceback。前七轮对"加载边界"家族只覆盖了 phase-state 文件（F144，load_state），未检查同文件内第二个加载站点；cmd_pre_score 中 load_deps 在状态变更（:273 SKILLS_DONE）之前调用、cmd_finalize 中在状态变更（:330 FINALIZED）之前调用 → 无部分写入，影响仅为不优雅崩溃（fail-loud），故定 M
- 验证命令+输出: `grep -n "load_deps\|deps.json" src/shenbi/phase_runner.py` → `:32 def load_deps(): :33 return json.loads(...) :245 deps = load_deps() :318 deps = load_deps()`；`grep -n "jload(TESTS / \"tiers\" / \"deps.json\")" -B2 -A3 src/shenbi/gates/g5.py` → try/except → `return fail("G5", ...)`；`grep -n "deps_path" src/shenbi/scoring.py` → `:206 if deps_path.exists():`；模拟：`PYTHONDONTWRITEBYTECODE=1 python3 -c "import json; json.loads('{bad')"` → JSONDecodeError（与 load_deps 相同的未捕获路径）
- 建议方向: 与 g5.py 对齐——load_deps 内 `try: ... except (json.JSONDecodeError, OSError, ValueError): emit_json({"status": CommandStatus.ERROR, "message": "deps.json unreadable or malformed", ...}) + sys.exit(1)`（或返回 None 让调用方 emit ERROR 信封）；与 F144 一并形成"phase_runner 所有加载边界对称守卫"的修复组

---

## 3. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`、`capability_fs.py`、`cli_utils.py`、`error_guidance.py`、`exceptions.py`、`logging.py`、`paths.py`、`phase_runner.py`、`py.typed`、`recovery.py`、`safe_write.py`、`scoring.py`、`status.py`、`sync_contracts.py`；全部独立通读；另核实跨区依赖 gates/cli.py、gates/shared.py、gates/g2.py、gates/g3.py、gates/g5.py、gates/gate_manifest.py、audit/_shared.py、dispatcher/executor.py、contracts/legacy.py、contracts/graph.py、orchestration/scoring_bridge.py、command-to-give.md、82 份 rubric、74 个 skills 目录）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型核实无洞；F123 覆盖无接线）、`cli_utils.py`（stdout/stderr 分离自洽）、`logging.py`（双渲染器自洽；SHENBI_LOG_FORMAT 校验为前轮已记录低价值项）、`paths.py`（read/write 语义正确；F112 覆盖死代码；`resolve_chapter_path` 委托 contracts/paths.py 单一实现核实无第二解析器）、`recovery.py`/`error_guidance.py`（F103 覆盖 dead；F138 未用 log）、`exceptions.py`（层级结构正确；F103 覆盖 dead；`load_contract` 全 raise 站点均为 ContractError——legacy.py 逐行核实，无其他异常类型漏网）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（除 F127/F136 外词汇表自洽；GateResult 由 gates/shared.py:127,140 消费非死成员）、`sync_contracts.py`（F109/F117/F130 覆盖；dag_key/normalize_to_glob 无 raise 路径——contracts/graph.py 逐行核实）、`scoring.py`（F104/F106/F115/F118/F120/F126/F129/F131/F134/F135/F140/F141/F142 覆盖；`scoring.main` 无库调用方——grep 全 src 核实）
- 新发现分布：`phase_runner.py`（F145——load_deps 加载边界，前七轮唯一遗漏站点）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达（本轮独立确认 `grep render_body_into tests/` 0 命中）——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。
2. **scoring.py 缺失 rubric/scores 文件 → FileNotFoundError traceback**（:317 `load_rubric` 与 :417 `open(scores_file)` 均无守卫，对比 phase_runner cmd_post_score:281-289 有结构化 ERROR 信封）——"坏输入 → 裸 traceback"家族（F124/F129/F144 同族），R2/R6 备查已记录同族项，未重复立 finding。
3. **sync_contracts.render_body_into frontmatter 正则早终止为潜在风险**（:145 `^(---\n.*?\n---\n)` 非贪婪——若 frontmatter 内含行首 `---` 会截断 frontmatter 并把数据契约块注入 YAML 中部）：本轮独立扫描 74 个 SKILL.md **0 个触发**（无内嵌 `---`、无 CRLF），纯潜在格式风险，未立 finding。
4. **`--gate-only` 对 G3 的参数形状错配**（scoring.py:304-305 恒传 `[gate_type, files, ftype]`，而 gates/cli.py G3 期望 `[skill_name, test_type, round_dir]`）——G3 收到逗号拼接文件串当 skill_name；`--gate-only` 为调试/测试模式且 R4 备查 #2 已记录同区脆弱性，未立 finding。
5. **scoring.py `--test-type` 无枚举校验**（任意值如 `--test-type xyz` → filter 静默 no-op 返回原维度，无告警）——F115 根因族（解析器对未知格式静默降级），未立 finding。
6. **`find_flag` 对 flag 值不校验**（phase_runner.py:348-356：`--round-dir --project-dir /tmp/x` 会把 `--project-dir` 当 round_dir 值）——F124 同族 CLI 校验空洞，未立 finding。
7. **AGENTS.md "67 functional + 2 meta = 69" 与现状 74 个 skills 目录不符**（本轮实测 `skills/` 74 目录且 ALL_SKILLS 74 项一一对应）——AGENTS.md 文档漂移，属非 Z1 区，供合并层转交。

---

## 4. 严重度异议汇总

| 编号 | 前七轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移。按决策表 P2"防御深度"而非 M"文案" |
| F115 | P1 | **维持 P1（无反证）** | 独立全量复跑：38/82 no-op、4 技能 N/A 豁免全部确认（rubric 行号/表头/权重 10%）、4 技能 bug-hunt/clean 测试目录存在、command-to-give.md:64,71 文档化路径可复现。唯一可能反证（"协议不实际用这些 rubric"）被证伪。按决策表"正常路径可复现功能错误"维持 |
| F140 | P1 | **维持 P1（无反证）** | 实跑确认 exit 1；command-to-give.md:57-61 显式契约被违反；codex 自动评分路径 `_record_completion` 永不可达；`scoring.main` 无库调用方（排除"返回 dict 是 API 设计"的抵消理由）。按决策表"违反显式契约 + 正常路径可复现功能错误"维持 |

其余 40 条严重度无异议。

---

## 5. 其他说明

- 前七轮"P1×2（F115/F140）"结论：本轮独立复跑/实跑后**无反证，维持**；误报数 0（43/43 成立）；前七轮共 44 条 finding（含 F109 的严重度异议）无一条被证伪。
- 唯一新发现 F145 为 M 级加载边界守卫缺口，不改变"无 P0"结论，也不新增 P1。至此 Z1 区七轮复核共 44 条 finding（F101–F144 + F145）：P1×2、P2×22、M×20（含 F109 议升 P2）。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器全量复跑/代码路径模拟 + grep/read 完成；F140 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本，`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`）**实证退出码；F115/F134/F126 经 `PYTHONPATH=src` 纯内存导入复跑；临时文件仅写 /tmp，未触碰仓库文件。
