# Z4.review10 复核报告 — src/shenbi/gates/（含 g4 checkers）第十轮独立复核

- 复核 agent：第十轮独立复核（fresh-context，全量重读，不依赖前十轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`（F419-F430）+ `Z4.review2.md`（F431-F439）+ `Z4.review3.md`（F440-F445）+ `Z4.review4.md`（F446-F455）+ `Z4.review5.md`（F456-F461）+ `Z4.review6.md`（F462-F467）+ `Z4.review7.md`（F468-F472）+ `Z4.review8.md`（F473-F483）+ `Z4.review9.md`（F484-F486）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个专用 checker + generic 3 分支 + score 三副本逐个通读；`src/shenbi/contracts/schemas/decisions.py`、`src/shenbi/pipeline/dispatch_helper.py`、`src/shenbi/contracts/thresholds.py`、`src/shenbi/contracts/skills/genre_config.py` 作交叉证据核实；真实项目 `novel-output/xinghuo-ranqiong/truth/pending_hooks.md` 与 `outline/volume_map.md` 实证）
- 实测基线：`.venv/bin/python` 只读验证 10+ 组（/tmp 临时目录构造 + 真实项目文件实证；未修改任何仓库文件，未 git add/commit；`git status --short` 复查无跟踪文件污染）
- 复核结论：**误报 0**；**新增严重度异议 0**（前几轮 F404 P2→P1、F408 P2→P1 两条异议本轮独立读源码复测确认成立；任务点名的 6 个 P1 逐一反证，均无推翻证据）；**新发现 4**（F487-F490：3×P2 + 1×M，全部实测验证）
- 结论：**0 新 finding 判定 = 否**（本轮发现 4 个前九轮漏报，未完全收敛到 0）

---

## 一、复核结论表（F400-F486 逐条判定）

本轮判定方法：对每条 finding 直接读其证据行 + 必要的独立实测。全部 87 条判定的共同依据是本人对 52 个文件的全量重读；下面按「已验证结论」分组列出，仅对有更新（新实例/范围修正/证据确认）的条目展开。

**A 组：P1 六条（任务点名，逐一反证核查，全部成立）**

| 编号 | 本轮判定 | 独立反证 |
|---|---|---|
| F401 | **成立** | 读 g_reconcile.py:52-62：`stem.rsplit("-", n_split)` 后 `candidate_tt = "-".join(parts[1:])` 对 `shenbi-worldbuilding-generative-scores(-subagent).json` 并入 `-scores`/`-scores-subagent`，progress 键为 `generative` → `td={}` → GR.2 恒 FAIL。无推翻理由。 |
| F404 | **成立，确认 P1 异议** | 读 decisions.py:33 `has = rationale is not None`；空串 `has=True` → manual_override/high 下 PASS（REQUIRED 被绕过）。AGENTS.md:70 契约在正常路径被空串静默违反 → P1 恰当。 |
| F408 | **成立，确认 P1 异议** | 读 dispatch_helper.py:1921-1935：progress.json 缺失时 safe_write 写入伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history → G3.4 fail-closed 被击穿并制造虚假审计证据。P1 恰当。 |
| F432 | **成立** | 实测：`ALL_SKILLS`=74（skills/ 下含 SKILL.md 的目录）vs `tests/tiers/t1-skill` 非 `_` 目录 69 项，差 5 = foreshadowing-lifecycle + review-group-* 4（实测名单一致）；G7.1b（g7.py:59）以 74 为全集 → 满分 round 也 FAIL。P1 无推翻理由。 |
| F444 | **成立** | 读 g3.py:151-153：`skills.get(skill_name, {}).get("output_files", [])` 取 skill 层；生产 progress 形状（codex/materialize/round-exec/run_gate_g3 伪造）均无该键 → G3.3 恒 SKIP。P1 无推翻理由。 |
| F458 | **成立，确认 P1** | 读 decisions.py:48-59：`Adjustment.rationale: str` 必填但 `_rationale` validator 仅查 `> _RATIONALE_MAX_CHARS`（:57）；`"rationale": ""` → PASS。AGENTS.md P2.5 "rationale REQUIRED on adjustments" 被空串绕过 → P1 恰当。 |

**B 组：核心门逻辑（g0-g7/共享/专用门）— 全部成立（读码确认）**

- F400（g2.py:315-322/273-287 无 file_type 守卫）成立；F402（length_normalizing.py:29 vs :35 路径不一致）成立；F403（cli.py:113-121 无 try/except + 11 个 RoundPaths checker 双 None raise）成立；F406（g0.py:195/323、g7.py:66 用 skill-output，实测仅 novel-output 存在）成立（限定：round-exec 测试 round 布局有 skill-output，维持 review2 限定）；F407（g0.py:177-179 无 seed 早退 PASS）成立；F409（g1.py:281-282 空输入 PASS）成立；F410（SHORT_MAP 20 项 vs 11 缺项目录存在）成立；F411（g0.py:220/227 missing_dirs 恒假）成立；F412（g3_independence.py:3 行号引用过期）成立；F413（decisions_validator.py:175 注释 shared.py:113）成立；F414（chapter_revision.py:97 "HARD_FAIL" 不在 GateStatus）成立；F415（SKILL.md:125=`<!--META-END-->`、规则在 :140）成立；F416（decisions-schema.md 缺 medium）成立；F417（无 test_gate_manifest.py / test_memory_distill.py）成立；F418（g0.py:665 未传 floor）成立；F419（g1.py:210-213 / g2.py:169-172 except 缺 ValueError）成立；F420（g6.py:49-59 novel.json/genre-config jload 无保护）成立；F421（g_reconcile.py:33 无 try / g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError）成立；F422（cli.py:83-87 JSON 失败→[]）成立；F423（g0.py:358 tests/scoring.py 不存在）成立；F428（g3.py:98 94 vs :113/:125 90 分叉）成立；F429（g1.py:228-232 gate 内写 .bak）成立；F435（g0.py:521-529 exempt_skills 零消费）成立；F436（g6.py:125/142 planted_chapters 死变量）成立；F437（g5.py:274-296 except Exception→WARN vs G6.3/G7.13 FAIL）成立；F439（BACKUP_SKILLS 16 个 truth-updater 不含 style-polishing；style_polishing.py:42-43 恒无 .bak）成立；F440（g0.py:202-207/524-528 缺 ValueError）成立；F441（g0.py:98-99 json.loads 直读 + :107 .get 无守卫）成立；F443（g1.py:102-144 check_fields_exist 仅测试引用）成立；F445（shared.py:210-222 unimplemented() 零调用）成立；F449（g_reconcile.py:40/61-62 "DONE" vs 生产小写 done）成立；F454（g_transition.py:47-64/70-85 键语义漂移）成立；F456（g6.py:66 字典序排序）成立；F457（g6_checks.py:111-113/245 len(findall) 计段数）成立；F462（g3.py:103 total_score/score vs scoring.py final_score）成立；F463（g3.py:100 无 skill 过滤 + :107 跨技能 rubric）成立；F464（codex.py:56 `-scores-subagent.json` 与 find_report/G0.10 glob/GR.2 均不匹配）成立；F466（chapter_drafting.py:260-261 rd+"project-output"/chapters；G6.3 传 rd=pd → 静默跳过）成立；F468（g3.py:98 循环外 threshold + :113/:125 循环内覆写 → 跨报告泄漏）成立；F470（g3.py:97/102、g5.py:67 jload 缺 ValueError，g5.py:67 无 try）成立；F471（gate_manifest.py:64-66 "gates" 为列表 setdefault AttributeError）成立；F472（cli.py:105 解析 rd 但 :107-110 bughunt/clean 不传）成立；F478（12 个 UNIMPLEMENTED stub）成立；F481（g2.py:325-329 G2.meta_ratio 双 WARN）成立；F484（g3_independence.py:23-27 agent_trace 非 dict 静默 PASS — 实测 `scoring_independence_status({"current_scorer_agent":"x","agent_trace":["x"]},...)` → PASS）成立。

**C 组：jload/内层形状家族（F431/F440/F441/F459/F460/F465/F471/F477/F485 等）— 成立，且本轮仍发现 2 处未枚举实例（F488/F489 补充）**

- F431（8 处 jload ValueError 调用点）成立；F440（g0.py 2 处）成立；F441（deps.json）成立；F459（worldbuilding.py:40-53/61-64）成立；F460（g0.py:204 chapter_word / g0_config_coherence:89-92 auditDimensions / g5.py:62-63 t1_scores 条目 / decisions_validator:54-55 相邻文件）成立；F465（g7.py:37/58 t1_scores、g_reconcile.py:36 skills、character_design.py:203 archetype_sources 元素、foreshadowing_plant.py:69 hooks 元素）成立；F471（gate_manifest "gates"）成立；F477（g_reconcile.py:60 GR.2 逐技能值）成立；F485（g3.5 scoring_history / g_dispatch completed_skill_names / g5 t1_scores list / g7 t2/t3_scores / g6 chapter_word 5 处）成立。**本轮新实例**：g_transition.py:48/57 `remaining_*` 标量 → `len()` TypeError（F488，实测崩溃）；g6.py:51/60 novel.json `target_word_count`/`target_words` 为字符串 → `-(-x // y)` TypeError（F489，实测崩溃）。

**D 组：per-skill g4 checker — 全部成立（本轮逐个通读复核）**

- F405（G4_CHECKER_SKILLS 22 vs generic.py checkers 31，差 9）成立；F424（chapter_drafting.py:219-222 skill-output 爬升 → fatigue 兜底）成立；F425（g7.py:66+86-91 G7.6 嵌 skill-output）成立；F426（genre_config.py:28 未走 resolve_input_path）成立；F427（chapter_revision.py:45 Path(fp) 原始路径）成立；F430（foreshadowing_track.py:41-46 裸词"操作"）成立；F433（chapter_drafting.py:331-334 project_dir 判、project_root 取）成立；F434（state_settling.py:14/46-53 单字符子串）成立；F438（chapter_drafting.py:204 max(5,wc//1000) vs SKILL 1/3000）成立；F442（length_normalizing.py:61-64 无 25% 双底线）成立；F446（shared.py:314-331 count_transition_words 双向偏离 — 实测 `'然而'*10`→0、`'然后'*10`→10）成立；F447（chapter_drafting.py:203/225 transition/fatigue 全文件计数）成立；F448（chapter_drafting.py:70 `第\d+章` 漏中文数字）成立；F450（g6.py:118-142 按 `## hooks`+`- id:` 解析 vs 真实 pending_hooks.md 表格格式 — 实测真实文件无 `- id:` 块）成立；F451（g6_checks.py:101-102/234-240 剥离正则缺 `# `/`<!--META`）成立；F452（chapter_drafting.py:293/316-328 末段取元文本）成立；F453（g2.py:237-238 `_is_important_chapter(fp, project_dir or "")`；phase_runner.py:213 与 g3.py:177 无 pd）成立（限定：executor.py:144 传 pd）；F455（generic.py:28 docstring vs 实现）成立；F461（chapter_drafting.py:334/293-302 protagonist/scene 未剥离）成立；F467（chapter_planning.py:37 vs :82/:97 分隔符不一致）成立；F469（generic.py:313 `make_composite_checker(g4_decisions, g4_chapter_revision)` 接线错位）成立；F473（g6.py:306-351 vol_pat 无 `## ` 锚定 + no_ending_hook 元文本 + 裸"将" — 实测真实 volume_map.md 跨卷表格行命中）成立；F474（g6_checks.py:24-42 同章日期互比 + 无年份维度）成立；F475（g0.py:204/209 与 g6.py:56/60 chapter_word 死读取；GenreConfig 模型 8 字段无 chapter_word — 读 contracts/skills/genre_config.py 确认）+ default=0 ZeroDivisionError 成立；F476（chapter_planning.py 仅实现 8 条可检查规则中 2 条）成立；F479（worldbuilding.py:68 注释方向相反）成立；F480（generic.py:102/152 死条件）成立；F482（chapter_drafting.py:58-66 docstring 未实现）成立；F483（chapter_drafting.py:164 只加"他"不加"她"）成立；F486（plot_thread_weaver.py:39-46 任一标签即 PASS）成立。

**E 组：其余（F407 等已列；补 3 条）**

- F485 之后的 F484/F486 已在上文（B 组 F484、D 组 F486）。
- 全部 87 条（F400-F486）判定：**误报 0**；证据细节修正均为前几轮已注明的范围/限定（F401 masking 按归档 spec、F406/F424/F425 "恒 SKIP"仅 pipeline 布局、F405 名单、F444 形状、F453 executor 限定、F475 非 int default 类型），本轮无新增修正。

**严重度异议：0 新增。** 确认前几轮 2 条异议成立（F404 P2→P1、F408 P2→P1，均属"AGENTS.md 显式契约在正常路径被静默违反"，按决策表 P1 恰当）。其余严重度按决策表核对无异议（F450/F464/F473 的"真实项目恒 FAIL"可争 P1 的理由前几轮已评估，属格式漂移类维持 P2，本轮无推翻依据；F483 误报方向与 F487 漏报方向同属检查质量退化，维持 P2）。

---

## 二、新发现（F487-F490）

### F487 | g4_chapter_drafting `_load_protagonist_names` 主角名双重追加 → 主角在场计数翻倍（正常路径 fail-open） | error | P2
- 证据: chapter_drafting.py:146-148（`re.search(r"^name:\s*(.+)$", text, re.MULTILINE)` 匹配 YAML frontmatter 的 `name:` 行 → append）与 :150-158（`if text.startswith("---")` 再经 `yaml.safe_load` 取 `fm["name"]` → 再次 append）——**同一名字两次入列**；:164 再加"他"。`_check_protagonist_presence`（:128-133）`sum(text.count(name) for name in protagonist_names)` 逐项计数 → 主角名被计两次。
- 根因: 正则路径与 YAML 路径对同一 frontmatter `name:` 字段双源读取后未去重；标准 protagonist.md（YAML frontmatter 含 `name: 苏寒`）100% 触发。计数翻倍 → 阈值 3 更易达到 → **漏报方向**（正文主角仅出现 2 次也判 PASS）。
- 验证命令+输出: `.venv/bin/python -c`（构造 `characters/protagonist.md` 含 `---\nname: 苏寒\nrole: 主角\n---`）→ `_load_protagonist_names(td)` → `['苏寒', '苏寒', '他']`（duplicate present: True）；`_check_protagonist_presence('苏寒走进大厅。苏寒环视四周。', ['苏寒','苏寒','他'])` → `[]`（PASS），`['苏寒','他']` → `['G4.cd.protagonist_absent: ... appears 2 times (threshold: 3)']`（FAIL）——同正文名单去重前后判定翻转。
- 建议方向: `_load_protagonist_names` 追加前去重（`if name not in names`），或只保留 YAML 单源；补"YAML frontmatter 主角名 → 名单唯一"用例。与 F483（"她"缺失）一并修复主角名单语义。

### F488 | g_transition GT.1 `remaining_{phase}` 为标量时 `len()` TypeError 崩溃（F460/F465/F485 家族新实例） | error | P2
- 证据: g_transition.py:48（`remaining = progress.get(phase_key, [])` 无 isinstance 守卫）、:57（`"remaining": len(remaining)` —— 标量时 len() 抛 TypeError）；:36-38 的 except 仅包 jload（`(json.JSONDecodeError, OSError)`），不覆盖 GT.1 判定段。
- 根因: 与 F460/F465/F485 同族——progress.json 内层键形状未校验；`remaining_generative: 3`（合法 JSON 但标量）时 `if remaining:` 为真进入 FAIL 分支，`len(3)` 崩溃 → CLI 无 JSON 输出。前九轮枚举的实例（F465: g7/g_reconcile/character_design/foreshadowing_plant；F485: g3.5/g_dispatch/g5/g7/g6）未覆盖 g_transition 的 GT.1 段。
- 验证命令+输出: `.venv/bin/python -c`（`progress.json={"remaining_generative": 3}`）→ `gate_G_TRANSITION('generative','T2',rd)` → `CRASH: TypeError object of type 'int' has no len()`。
- 建议方向: 与 F460/F465 统一——`remaining`/`blockers` 取值后加 list 校验（非 list 记 WARN/SKIP 或按空处理）；补"remaining_* 标量"用例。

### F489 | g6.py novel.json `target_word_count`/`target_words` 为字符串 → `-(-x // y)` TypeError 崩溃（F475/F485 家族相邻实例） | error | P2
- 证据: g6.py:51（`target_words = _nj.get("target_word_count") or _nj.get("target_words", 100000)` 无类型校验）、:60（`expected = -(-target_words // default_w)` —— str 参与一元负/整除抛 TypeError）；:49-50 的 jload 调用本身无 try（F420），但即便 jload 成功，内层值为字符串时在 :60 崩溃。F485 的 g6 实例只列了 `chapter_word` 为 list 的 AttributeError（g6.py:56），未覆盖 novel.json 数值键为字符串的 TypeError；F475 的注记只覆盖 chapter_word.default 类型。
- 根因: 与 F475/F485 同族——JSON 内层数值键无 int/float 校验；LLM 产出的 `"target_word_count": "6000"`（带引号）或 `"target_words": "100000"` 直接崩溃。
- 验证命令+输出: `.venv/bin/python -c`（`novel.json={"target_word_count": "6000"}` + 空 genre-config）→ `gate_G6('long-form', None, td)` → `CRASH: TypeError bad operand type for unary -: 'str'`。
- 建议方向: `target_words` 取值后加 `isinstance(x, (int, float))` 校验（非数值回退 100000），或先 `int()` 转换（带异常回退）；与 F475 的 `default_w` 类型守卫一并修复 g6.py:51/56/60 区域；补字符串数值用例。

### F490 | g4_chapter_revision 返回的 `checks` 槽是字符串列表而非 dict 列表（GateResult 契约偏离，F414 同返回语句的第二个键） | contract | M
- 证据: chapter_revision.py:95-102（`return json.dumps({"status": ..., "checks": issues, "must_fix": issues}, ...)` —— `issues` 是 `list[str]`）；对照所有其它 gate/checker 的 checks 槽均为 `list[dict]`（如 fail()/passed() 的 checks 参数、g4 各 checker 的 `c: list[dict[str, Any]]`）；F414 只覆盖了同语句的 `"HARD_FAIL"` 状态值，未覆盖 checks 槽类型。
- 根因: 该 checker 的手工 JSON 构造与标准 checks 形状不一致；经 make_composite_checker（decisions_validator.py:187 `combined_checks = ... + ...`）原样并入最终 G4 结果。当前无消费者对 checks 条目做 dict 操作（pipeline 仅查 status），故为潜伏契约漂移；但 pipeline-manifest.json 会持久化该形状，任何未来按 checks[i]["id"] 消费的调用方会 AttributeError。
- 验证命令+输出: `.venv/bin/python -c`（构造含 2 字符 rationale 的 `chapter-1-revision-decisions.json`）→ `g4_chapter_revision([f], None, td)` → `json.loads(out)['checks']` 首元素类型 `str`（`['G4.rev.adjustment_0_thin_rationale:...']`），非 dict。
- 建议方向: 与 F414 一并修复——`"checks"` 改为 `[{"id": "G4.rev", "file": f, "s": "FAIL", "r": issue} for issue in issues]`（或直接复用 fail() 构造），与 GateResult 形状对齐；补"checks 条目为 dict"断言用例。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个专用 checker + generic 3 分支 + score 三副本 + decisions_validator/make_composite_checker 逐个通读）。未发现清单外遗漏文件。
- 实测基线：本轮未运行全量 pytest（沿用前九轮 785 passed 基线，本轮未修改任何文件）；新增只读验证 12 组（`.venv/bin/python` /tmp 临时目录构造 + 真实项目 `novel-output/xinghuo-ranqiong/truth/pending_hooks.md` 与 `outline/volume_map.md` 格式核对 + 交叉区源码 decisions.py/dispatch_helper.py/thresholds.py/genre_config.py 核实）；`git status --short` 复查无跟踪文件污染（仅 untracked 的既有 .zcode/docs 文件与本文档）。
- 本轮重点复核了任务点名的三处：30 个 per-skill g4 checker 最后一遍（24 专用 + generic 3 分支 + score 三副本，含 chapter_drafting/character_design/plot_thread_weaver/g6_checks 全文）、G0 各子检查（G0.1-G0.16/G0.cc 全部路径）、g6_checks/g7/g3_independence 剩余路径（含 G6.4/G6.11/G6.12 全部分支、G7.13-G7.16 全部分支、G3.4 独立性函数本体）。
- 跨区交叉确认建议（与 prior rounds 一致）：F487 的 protagonist.md 真实 frontmatter 形状 ↔ Z3（skills 区）或真实项目实证；F488/F489 的 progress.json/novel.json 内层形状 ↔ Z5/Z6（pipeline/dispatcher 区）；F408 的 run_gate_g3 调用语义 ↔ Z5/Z6；F473/F474 的 G6.11/G6.4 真实项目恒定 FAIL 归因 ↔ Z5/Z6。

## 四、最终消息（复核统计）

- **新 finding：4**（F487-F490：F487 为正常路径计数翻倍漏报 bug（实测名单重复）；F488/F489 为 F460/F465/F485 内层形状家族 2 处未枚举实例（实测崩溃）；F490 为 F414 同返回语句的 checks 槽类型契约偏离（实测）。全部实测或代码路径验证）
- **误报：0**（F400-F486 全部成立；对任务点名的 F401/F404/F408/F432/F444/F458 六个 P1 逐一读源码 + 实测反证：F401 读 g_reconcile.py:52-62 确认、F404/F458 读 decisions.py:33/:48-59 确认、F408 读 dispatch_helper.py:1921-1935 确认、F432 实测 74-vs-69 计数确认、F444 读 g3.py:151-153 确认；无任何推翻证据）
- **严重度异议：0 新增**（确认前几轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度按决策表核对无异议）
- **0 新 finding 判定：否**（本轮发现 4 个前九轮漏报，未完全收敛到 0）
