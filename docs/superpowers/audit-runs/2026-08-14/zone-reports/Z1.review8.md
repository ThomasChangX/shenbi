# Z1 分区第八轮独立复核报告（agent i）— src/shenbi/ 顶层 14 文件

- 日期：2026-08-14
- 复核对象：`Z1.a.md`（F101–F113）+ `Z1.review.md`（F114–F122）+ `Z1.review2.md`（F123–F127）+ `Z1.review3.md`（F128–F133）+ `Z1.review4.md`（F134–F139）+ `Z1.review5.md`（F140–F143）+ `Z1.review6.md`（F144）+ `Z1.review7.md`（F145），共 **44 条**
- 复核范围：`zones/Z1.files` 全部 14 文件，**fresh-context 全量重读 14/14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 逐文件独立通读；对跨区依赖 `contracts/legacy.py`、`contracts/paths.py`、`gates/cli.py`、`gates/shared.py`、`gates/g2.py`、`gates/g4/generic.py`、`gates/g5.py`、`audit/_shared.py`、`dispatcher/executor.py`、`dispatcher/modes/codex.py`、`orchestration/scoring_bridge.py`、`tests/tiers/deps.json`、`command-to-give.md`、`pyproject.toml`、worldbuilding/chapter-drafting 两份 rubric 独立核实；并**实跑 `.venv/bin/shenbi-score`** 与 `PYTHONPATH=src` 纯内存解析器复跑、AST raise 扫描）
- 只读约束：仅 grep / read / python3 只读 AST、解析器复跑与代码路径模拟（`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`，临时文件仅写 /tmp）；未创建/修改/删除任何仓库文件（本报告文件除外）；未 git add/commit
- 编号段：F146–F199（实际产出 F146–F147，共 2 条）

---

## 0. 复核统计（返回摘要用）

- **新 finding 数：2**（F146–F147，均 M——"加载边界"家族的最后一轮扫尾：scoring 的 t2 deps.json 解析与 sync_contracts 的 deps.json/registry 加载，均无守卫）
- **误报数：0**（前八轮 44 条全部成立；无一条被证伪。F115/F140 两个 P1 经本复核**独立实跑/复跑确认无反证**）
- **严重度异议数：3 条维持**（F109：同意 R1–R7 的 M→P2；F115：P1 无反证维持；F140：P1 无反证维持）；**无新增异议**
- **0 新 finding 判定：否**（2 条 M 级漏报；均为"deps.json 存在但损坏 → 裸 JSONDecodeError traceback"的独立加载站点，R7 的 F145 只覆盖了 phase_runner.load_deps，同族另两个消费者站点未覆盖）

---

## 1. 复核结论表（F101–F145 逐条判定）

| 编号 | 标题 | 前八轮严重度 | 复核判定 | 理由（独立验证） |
|---|---|---|---|---|
| F101 | safe_write 权限 0600 覆写 0644 工件 | P2 | **成立** | 实证复现：`git ls-files -s docs/framework/dependency-dag.json tests/tiers/deps.json` → 均 `100644`；`ls -la` → 均 `-rw-------`（0600）。git 不追踪 rw 位 → CI `git diff --exit-code` 不可见。根因确认：mkstemp 固定 0600 + os.replace 继承 |
| F102 | error_guidance 6 doc_url + 2 action 断链 | P2 | **成立** | 前轮已多次实证（docs/registry.md 等顶层文件全 MISSING；tests/build_registry.py No such file；tests/lock-tool-hashes.sh 存在 → ToolTamper 仅 doc_url 破）。本复核重读 error_guidance.py:24-50 确认 6 条 doc_url 路径/锚点均指向不存在的文档 |
| F103 | exceptions 22 类中 17 类无 raise 站点 | P2 | **成立** | **独立 AST 全仓 raise 扫描**：RAISED 恰为 DispatcherError/DispatchWriteFailureError/SubAgentProtocolError/SubAgentTimeoutError/RetryExhaustedError（另 ConfigError/ContractError/DriftEscalationError/UnresolvedPathError 为层级外模块定义，与 Z1 无关）；exceptions.py 22 类中其余 17 类无 raise 站点。error_guidance/recovery 引用的 6 类全无站点 |
| F104 | scoring --kill-switch 无 scores 必 REJECT | P2 | **成立** | :414-415 `scores={}` → :422 空 → `REJECT: scores is empty` → exit 2；分支无测试触达（test_scoring.py:598-613 恒带 scores.json） |
| F105 | phase_runner --project-dir 缺失传 "None" | P2 | **成立**（R3 修正正确） | :360 `required=False` + :117 `str(None)`；独立走查 g5.py:115 `if project_dir:` 对真值字符串 `"None"` 成立 → `Path("None")` → G5.3 三项扫描全空 → **`c.append({"id":"G5.3","s":"PASS",...})`（g5.py:202-203，非 R1 所称 SKIP）**；:188 assert 在 `-O` 下移除后 `Path(None)` → TypeError。R3 对 R1 的事实修正独立确认 |
| F106 | scoring G3 解析 except Exception: pass | P2 | **成立** | grep 确认 scoring.py:361 为唯一无 exc_info 的 `except Exception`（对照 phase_runner.py:74、safe_write.py:139 均带 exc_info 日志）；G3 subprocess（:343-355）无 timeout |
| F107 | G_TRANSITION/G_DISPATCH/G_RECONCILE 未接线 | P2 | **成立** | `grep -rn "G_TRANSITION\|G_DISPATCH\|G_RECONCILE" src/shenbi/phase_runner.py` → 0 命中（实测）；仅 gates/cli.py:131-136 手动入口 |
| F108 | safe_write mkstemp 锁泄漏 + stale-takeover 竞态 | P2 | **成立** | :105 mkstemp 在 `try`（:106）外——若抛 OSError，:117-125 finally 不执行 → flock fd 泄漏（.lock 文件会经 1s 后 takeover 自愈，fd 泄漏至进程退出为实）；:89-91 unlink 后立即 O_EXCL 重建的 FileExistsError 无捕获 |
| F109 | verify_bijection 用 assert | M | **成立；严重度异议维持（M→P2）** | sync_contracts.py:121 assert 实证；docstring :110 "Raises on mismatch" 与 `-O` 下守卫消失冲突；工具仍会静默写 deps.json（:201）→ 静默数据漂移（CI 幂等检查为唯一间接兜底）。**同意 R1–R7 共识：M→P2**（运行时正确性守卫而非文案） |
| F110 | "1 source files changed" 语法 | M | **成立** | exceptions.py:57 `f"{len(mismatches)} source files changed..."` 无单复数区分 |
| F111 | __init__ forwarder until PR-19/20 过期 | M | **成立** | __init__.py:8-9 实证；gates/cli.py、dispatcher/cli.py 均真实实现 |
| F112 | RoundPaths.repo() 死代码 | P2 | **成立** | `grep "\.repo("` src/ 0 命中；同族 `backup()`（paths.py:33-35）亦 0 调用方（重读确认） |
| F113 | pre-skill ContractError 静默吞错 | P2 | **成立** | phase_runner.py:153-154 无日志降级为空契约；对比 :74-75 同类 except 带 exc_info |
| F114 | run_gate 不捕获 TimeoutExpired | P2 | **成立** | :94 `except (json.JSONDecodeError, ValueError, OSError)`；`issubclass(subprocess.TimeoutExpired, OSError)==False`（mro: TimeoutExpired→SubprocessError→Exception）→ 60s 门超时 traceback；scoring.py:304,343 两个 subprocess.run 连 timeout 都没有 |
| F115 | 维度过滤对 38/82 rubric 静默 no-op | **P1** | **成立（P1 维持，无反证）** | **独立复跑 `load_applicability`（真实解析器）**：worldbuilding → `{}`；chapter-drafting → `{'Bug-hunt','Clean','Generative'}`（对照组格式匹配时过滤确实生效）。worldbuilding 的 "## Dimension Applicability by Test Type" 节被 `startswith("## Dimension Applicability")` 前缀命中进入，但其表头 `\| # \| Dimension \| Bug-hunt Standard \| Clean Standard \|` 非解析器认的 `\| Dimension scope \|` → header_dims 恒空 → applicability 恒 `{}` → filter no-op → 豁免维度（dim4 Prose quality，10% 权重）计入 bug-hunt/clean。worldbuilding 确有 bug-hunt/clean/generative 测试目录（ls 实证）；rubric:43 明示 "scoring.py renormalizes weights for remaining applicable dimensions" 与实现不符。按决策表"正常路径可复现功能错误"维持 P1 |
| F116 | phase_runner G2 FAIL 不阻断 | P2 | **成立** | :211-239 仅 `g4_status=="FAIL"` 阻断（:228-230）；executor.py:200-204 同路径 G2 FAIL → return 1（阻断）→ 双入口语义不一致。**补充证据**：command-to-give.md:48 明示 "只在 G2 和 G4 都通过后…评分"——协议要求 G2 通过，相位机路径不执行该语义 |
| F117 | sync_contracts 静默跳过 ContractError 技能 | P2 | **成立** | :40-41 `except ContractError: continue` 无日志；:86-92 被跳技能产出空；:196-200 verify_bijection 同源盲区（generated 与 member_outputs 同源派生 → 两处同缺断言恒过） |
| F118 | scoring --phase 死参数 / --tier T2/T3 无逻辑 | P2 | **成立** | grep `_phase` 仅 :321,:331 两处赋值（实测），全文无第二次读取；tier 块仅 `tier=="T1" and test_type` 有 G3 逻辑 |
| F119 | safe_write stale-takeover 破坏互斥 + flock fd 泄漏 | P2 | **成立** | :75-91 固定 1s 退避后无条件 unlink（无 pid/锁龄证据）；:54-62 flock 抛 OSError 时 :58 已开 fd 未 close 即落 O_EXCL 分支 → fd 泄漏 |
| F120 | G4/G6 标记名 test_type 后缀与写侧不匹配 | P2 | **成立** | grep 实证：gates/cli.py 仅 :121（G4 generative）与 :128（G6 generative）两处 write_gate_marker，均恒写 "generative"；scoring.py:200/:220 读侧按 `-{test_type}` 拼接 → 非 generative + `--round-dir` 必 MARKER_MISSING |
| F121 | phase_runner rglob `[:20]` 截断 + 不过滤非输出 .md | P2 | **成立** | :208-210 `[...][:20]` 实证；rglob 顺序 OS 相关 → G2/G4 校验覆盖非确定 |
| F122 | phase_runner pre-score 不追加 step | P2 | **成立** | :242-275 无 `state["steps"].append(...)`（重读逐行确认）；其余 5 命令均记录 |
| F123 | capability_fs 无生产接线 | P2 | **成立** | `grep -rln CapabilityFS/capability_fs src/ tools/` → 仅 capability_fs.py 自身；0 生产消费者 |
| F124 | phase_runner CLI 无位置参数校验 | P2 | **成立** | :364-381 `args[0]`/`args[1]` 无长度检查；`phase-runner pre-score`（缺参）→ IndexError（代码走查确认） |
| F125 | command-to-give.md:48 引用不存在的 dispatch-subagent.sh | P2 | **成立** | `find . -name "*dispatch-subagent*"`（排除 .git）→ 0 命中（实测）；command-to-give.md:48 原文引用 `bash tests/dispatch-subagent.sh`；ADR-0009 已迁移为 `shenbi-dispatch` |
| F126 | scoring scored_by 误标 "interactive" | M | **成立** | :441 `"subagent" if "--subagent" in sys.argv else "interactive"`；**实跑实证**（见 F140 验证输出）：不带 --subagent 的文档化评分命令输出 `"scored_by": "interactive"` |
| F127 | status.py ScoringStatus.OK/UNIMPLEMENTED 死成员 | M | **成立** | grep 全 src：OK/UNIMPLEMENTED 0 消费（REJECT :158/:425、MARKER_MISSING :371 在用） |
| F128 | phase_runner post-score 只校验 JSON 语法不校验结构 | P2 | **成立** | :292 仅 `json.loads`；`[]`/`"hello"`/`{"foo":1}` 均合法 JSON → SCORED → finalize 可推进；finalize :304-333 不读 scores 内容 |
| F129 | scoring --gate-only `--type` 尾参 IndexError | P2 | **成立** | :303 `sys.argv[sys.argv.index("--type") + 1]` 无长度守卫（对照 :300 `gate_type` 有 `idx+1 < len` 守卫） |
| F130 | sync_contracts render_body_into 无 frontmatter 静默跳过 + 重复块只替换首个 | M | **成立** | :146-147 `if not m: return` 无日志；:152 `pattern.sub(block, body, count=1)` 只替换首个 BANNER..END 块 |
| F131 | scoring `gate_markers_verified` 误报 True | M | **成立** | :443 `bool(round_dir and test_type)` 按 flag 存在性赋值；check_gate_markers :187-224 非标准 rubric 路径三分支全不匹配 → `return []` 零检查 |
| F132 | phase_runner require_state 错误信封缺 "status" | M | **成立** | :105-111 emit `{"error","phase"}` 无 status；对照 :139-146/:281-289 均有 status 键 |
| F133 | safe_write O_EXCL 释放路径 unlink 非原子 | P2 | **成立** | :117-125 先 `os.close(lock_fd)` 再按路径 `os.unlink(lockfile)`；:83-91 takeover 重建 → 交错时序下释放者删新锁 → 双写者 |
| F134 | scoring validate_scores 对空 dimensions rubric 静默放行 → 0 分 | P2 | **成立** | :145-159 只查空 scores 不查空 dims；:169-170 total_weight==0 → 0。**补充证据（强化）**：command-to-give.md:55 文档化 "2 = 评分验证失败（**dimensions 为空**、缺失维度、分数越界）"——文档明确承诺空 dims → exit 2，实现却静默产出 0 分，属文档契约+行为双背离（仍为 P2：需 rubric 不可解析才触发） |
| F135 | scoring validate_scores 对 NaN 放行 | P2 | **成立** | `score < 0 or score > 100` 对 NaN 恒 False；`json.load` 默认接受 `NaN` 字面量 → NaN 通过校验并产出 NaN final |
| F136 | status.py CommandResult TypedDict 全仓无使用 | M | **成立** | grep `CommandResult` src → 仅 status.py:78 定义，0 消费 |
| F137 | phase_runner post-skill 静默吞 ContractError | P2 | **成立** | audit/_shared.py:57-58、executor.py:74-75 均 `except ContractError: return 默认值` 无日志（重读确认）；g4/generic.py:46-47 空文件 → SKIP 后 `passed(...)`（vacuous PASS，status 恒 PASS → write_gate_marker 还会写 PASS marker → pre-score 标记检查被假通过） |
| F138 | error_guidance/recovery 的 `log = get_logger(__name__)` 未使用 | M | **成立** | `grep -c "log\."` 两文件均 0（排除 import 行） |
| F139 | __init__ docstring 子模块清单仅列 6/12 | M | **成立** | :3-10 列 6 项；实际 12 模块（ls 实证） |
| F140 | `shenbi-score` 成功路径恒 exit 1 | **P1** | **成立（P1 维持，无反证）** | **实跑 `.venv/bin/shenbi-score`（独立复现）**：完整 10 维 scores + `--test-type generative` → `final_score: 87.05 / PASS (acceptable)` 完全成功但 **EXIT CODE: 1**，stderr 为完整 result dict repr（`SystemExit(dict)` 打印）。`.venv/bin/shenbi-score` 标准 hatchling 包装 `sys.exit(main())`（实证 cat 脚本内容）；`main()` 成功路径 `return result`（:463）→ dict 非合法退出码；command-to-give.md:57-61 "0 = 评分计算成功 / 1 = 其他错误" 显式契约被违反；codex.py:105-108 `if result.returncode != 0: return result.returncode` → `_record_completion` 永不可达（重读确认）。`shenbi-phase`（返回 None→exit 0）与 `shenbi-sync-contracts`（返回 int）均正确，仅 shenbi-score 受影响。按决策表"违反显式契约 + 正常路径可复现功能错误"维持 P1 |
| F141 | scoring validate_scores 对 bool 分数放行 | P2 | **成立** | `isinstance(True, (int, float))` → True；`True < 0 or True > 100` 恒 False → 校验通过 → 按 1 计分 |
| F142 | scoring 过滤后评分在文档化 bug-hunt/clean 路径恒发 weight_mismatch 误报 | M | **成立** | filter docstring :107-109 宣称 "renormalize weights" 但 :135-136 仅过滤；compute_score :171-172 以 total_weight≠100 发 warning；chapter-drafting bug-hunt 过滤后权重和 75 → 每条文档化路径恒误报 |
| F143 | phase_runner 回退注释 "G2 decisions 分支 json.loads markdown → crash" 与实现不符 | M | **成立** | 独立走查 g2.py:76-84：decisions 分支 `if not fp.endswith(".json"): continue`（.md 显式跳过，注释明示设计意图；json.loads 仅对 .json 路径 :110 执行）——注释的 crash 论断不成立 |
| F144 | phase_runner `load_state` 对 phase-state 文件无形状/类型校验 | P2 | **成立** | :36-40 `json.loads` 无 try、`cast(dict[str,Any],...)` 无运行时断言；`[]`/缺 state 键/非法 JSON → 裸 TypeError/KeyError/JSONDecodeError traceback |
| F145 | phase_runner `load_deps()` 无任何错误处理 | M | **成立** | :32-33 `json.loads(...)` 无 try；:245/:318 调用；对照 g5.py:36-39 try/except → 结构化 FAIL、scoring.py:206 exists() 守卫——phase_runner 是 deps.json 的唯一无守卫消费者。定 M 恰当（fail-loud、无部分写入；触发需 deps.json 损坏） |

**前八轮陈述事实复核结论**：R1 对 F105 的"G5.3 静默 SKIP"错误、R3 的"真实行为是空证据 PASS"修正——本复核独立走查 g5.py:115/202-205 确认 **R3 修正正确**；F101 波及文件数递增记录属实（本复核时点两文件均为 0600）；F115 组成数（9 t2 + 3 t3 + 26 t1 = 38）与 R2 修正一致（`ls tests/tiers/t2-phase/` 9 目录实证）。**无新的前轮事实错误需要修正。**

---

## 2. 新发现（F146–F147）

F146 | scoring.check_gate_markers t2 分支对 deps.json 仅 exists() 守卫：文件存在但损坏 → 未捕获 JSONDecodeError 裸 traceback（同族 F145 只覆盖了 phase_runner.load_deps，scoring 侧损坏场景漏掉） | error | **M**
- 证据: src/shenbi/scoring.py:205-207（`deps_path = Path(__file__).resolve().parents[2] / "tests" / "tiers" / "deps.json"`；`if deps_path.exists():`；`deps = json.loads(deps_path.read_text(encoding="utf-8"))`——exists() 只防**缺失**，不防**损坏**）；调用点 :368（`if round_dir: missing = check_gate_markers(rubric_path, test_type, round_dir)`，需 t2 rubric 路径 + --test-type + --round-dir）
- 根因: R7 的 F145 证据表将 scoring.py:205-206 列为"存在性守卫"对比项（与 g5.py 的 try/except 对照），但该守卫只覆盖缺失场景；文件**存在且 JSON 损坏**时 json.loads 直接抛 JSONDecodeError，无 g5.py:38 式的 `except (json.JSONDecodeError, OSError, ValueError)` 包络 → scoring 主流程裸 traceback。同族三消费者（g5.py 守卫完整、phase_runner F145 已报、scoring 此站点漏报）
- 验证命令+输出: `PYTHONDONTWRITEBYTECODE=1 python3`（monkeypatch Path.read_text 强制返回 `"{ not valid json"` 后调 `check_gate_markers("tests/tiers/t2-phase/genesis/rubric.md", "generative", "/tmp/x")`）→ `UNCAUGHT JSONDecodeError: Expecting property name enclosed in double quotes: line 1 column 3 (char 2)`；路径可达性实证：`ls tests/tiers/t2-phase/genesis/rubric.md`（存在，parts 含 "t2-phase"）
- 建议方向: 与 g5.py:36-39 对齐——`try: deps = json.loads(...) except (json.JSONDecodeError, OSError, ValueError): return missing`（或 emit `{"status": ScoringStatus.REJECT, "reason": "deps.json unreadable or malformed"}` + exit）；与 F144/F145 一并形成"deps.json/phase-state 全部加载边界对称守卫"修复组

F147 | sync_contracts.main() 的 deps.json 读取（:195）与 load_registry（:165）均无守卫：损坏/缺失 → 裸 traceback（本文件前七轮仅 F109/F117/F130 覆盖，加载边界未覆盖） | error | **M**
- 证据: src/shenbi/sync_contracts.py:165（`registry = load_registry()`——legacy.py:102-113 证实缺失 → `raise ContractError("registry missing")`、YAML 损坏/模式无效 → ContractError，main 无 try → 裸上抛）；:195（`deps = json.loads(DEPS_PATH.read_text(encoding="utf-8"))`——DEPS_PATH 缺失 → FileNotFoundError、损坏 → JSONDecodeError，均无捕获）；对比同文件 :167-172 的 "no contracts → bail" 有显式错误路径（log.error + return 1），而 :165/:195 无等价守卫
- 根因: sync_contracts 是 deps.json 的**写入方**（:201 _write_json 重写），读取侧却无任何防御——正常运行中 deps.json 由本工具确定性重生成（CI 幂等检查保证合法），损坏/缺失仅来自外部篡改或部分克隆；但工具以裸 traceback 崩溃与同文件 :171 的 `log.error + return 1` 结构化 bail 语义不一致（R7 对 sync_contracts 声明"F109/F117/F130 覆盖"时未检查 :165/:195 两个加载站点）
- 验证命令+输出: 代码走查 :163-203 + legacy.py:93-113（load_registry 全 raise 路径均为 ContractError）；`grep -n "json.loads\|load_registry" src/shenbi/sync_contracts.py` → `:18（import）、:165、:195`（:195 无 try 包裹）；模拟：`PYTHONDONTWRITEBYTECODE=1 python3 -c "import json; json.loads('{bad')"` → JSONDecodeError（与 :195 相同未捕获路径）
- 建议方向: :195 加 `try: ... except (json.JSONDecodeError, OSError): log.error("deps_unreadable") + return 1`；:165 的 ContractError 同样捕获为 `log.error("registry_unloadable", exc_info=True) + return 1`（与 :171 的 bail 语义对齐）；或两者复用 g5.py:36-39 的守卫模式

---

## 3. 严重度异议汇总

| 编号 | 前八轮定级 | 本复核 | 依据 |
|---|---|---|---|
| F109 | M（R1 起异议 M→P2） | **同意 M→P2** | assert 为运行时正确性守卫（docstring :110 "Raises on mismatch" 契约）；`-O` 下失效时工具仍静默写 deps.json（:201）→ 静默数据漂移。按决策表 P2"防御深度"而非 M"文案" |
| F115 | P1 | **维持 P1（无反证）** | 独立复跑真实解析器：worldbuilding applicability `{}`、过滤 no-op（9 维应 8 维）、rubric:43 明示 renormalize 承诺、bug-hunt/clean 测试目录存在、command-to-give.md:64,71 文档化路径可复现。按决策表"正常路径可复现功能错误"维持 |
| F140 | P1 | **维持 P1（无反证）** | 实跑 `.venv/bin/shenbi-score`：成功评分 exit 1 + stderr 打印 result dict repr；command-to-give.md:57-61 "0=成功" 契约被违反；codex.py:105-108 依赖退出码 → `_record_completion` 永不可达；`scoring.main` 无库调用方（排除"返回 dict 是 API 设计"的抵消理由）。按决策表"违反显式契约 + 正常路径可复现功能错误"维持 |

其余 41 条严重度无异议。

---

## 4. 覆盖空洞声明

- **重读文件数：14 / 14**（`__init__.py`/`capability_fs.py`/`cli_utils.py`/`error_guidance.py`/`exceptions.py`/`logging.py`/`paths.py`/`phase_runner.py`/`py.typed`/`recovery.py`/`safe_write.py`/`scoring.py`/`status.py`/`sync_contracts.py` 全部独立通读；另核实 13 个跨区依赖文件/数据：contracts/legacy.py、contracts/paths.py、gates/cli.py、gates/shared.py、gates/g2.py、gates/g4/generic.py、gates/g5.py、audit/_shared.py、dispatcher/executor.py、dispatcher/modes/codex.py、orchestration/scoring_bridge.py、tests/tiers/deps.json、command-to-give.md、pyproject.toml、worldbuilding/chapter-drafting rubric）
- 判定无新缺陷的文件：`capability_fs.py`（权限模型六方法逐一核实无洞；F123 覆盖无接线）、`cli_utils.py`（stdout 数据通道/stderr 日志分离自洽）、`logging.py`（双渲染器/stderr/`cache_logger_on_first_use=False` 自洽；SHENBI_LOG_FORMAT 非 "json" 静默降级为前轮已记录低价值项）、`paths.py`（read/write 语义正确；F112 覆盖 repo()/backup() 死代码）、`error_guidance.py`/`recovery.py`（F103 覆盖 dead；F138 未用 log）、`exceptions.py`（层级结构正确；F103 覆盖 dead；to_dict/cause 链无新缺陷维度）、`py.typed`（0 字节 marker + hatchling 配置层一致）、`status.py`（除 F127/F136 外词汇表自洽）、`safe_write.py`（F101/F108/F119/F133 覆盖；剩余分支为防御/平台边界）、`phase_runner.py`（F105/F107/F113/F114/F116/F121/F122/F124/F128/F132/F137/F143/F144/F145 覆盖；剩余 run_gate 非 dict JSON → AttributeError 属不可达防御边界，未立 finding）
- 新发现分布：`scoring.py`（F146——t2 deps.json 损坏加载站点）、`sync_contracts.py`（F147——deps.json/registry 加载站点）

### 备查项（评估后未立 finding，记录供合并层参考）

1. **R1 的 must-test 覆盖空洞仍未落实**：safe_write.py:71-91 并发分支、sync_contracts.py:128-134/:144-155 render 路径至今无单测触达——测试覆盖空洞，行为缺陷已由 F108/F119/F130/F133 覆盖。
2. **scoring.py:434-436 对 WARNING 类 validation_errors 用 `log.error` 记录**：extra-key WARNING（:152）被以 error 级别输出，日志级别与语义不符（纯日志噪音，无正确性影响），未立 finding。
3. **cmd_pre_score/finalize 未知 phase → 空检查 vacuous 放行**（:246 `deps.get("t2-phases",{}).get(phase,{})`）：deps.json 中不存在的 phase → prerequisites/expected_outputs 空 → 无检查推进状态；但正常流程需先过 G5（g5.py:41-42 对未知 phase 显式 FAIL）→ 触发面仅手工伪造 phase-state，未立 finding。
4. **`--gate-only` 对 G3 参数形状错配 / 无 `--files` vacuous G2 PASS / UNIMPLEMENTED 门 exit 0**——R4/R5 备查已记录，同意不重复。
5. **`--chapter abc` ValueError traceback / `--interactive` 必须 argv[2] / find_flag 值不校验**——R3/R2/R7 备查已记录，同意不重复。
6. **F134 强化**：command-to-give.md:55 文档化 "dimensions 为空 → exit 2"，实现静默 0 分——并入 F134 修复方向（已在上表注记），不单独立 finding。

---

## 5. 其他说明

- 前八轮"P1×2（F115/F140）"结论：本复核独立复跑/实跑后**无反证，维持**；误报数 0（44/44 成立）。
- 新发现 F146/F147 均为 M 级"deps.json 存在但损坏 → 裸 traceback"加载站点（scoring t2 分支、sync_contracts main），不改变"无 P0"结论，也不新增 P1。二者与 F144/F145 同属"加载边界守卫"家族——前八轮覆盖了 phase-state（F144）与 phase_runner.load_deps（F145），本复核完成同族剩余两个消费者的扫尾。
- 环境备注：本复核全部验证经 python3 只读 AST/解析器复跑/代码路径模拟 + grep/read 完成；F140 经**实跑 `.venv/bin/shenbi-score`（真实控制台脚本，`PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=/tmp`）**实证退出码；F115/F126 经 `PYTHONPATH=src` 纯内存导入复跑；F146 经 monkeypatch Path.read_text 纯内存复现（无仓库文件写入）；临时文件仅写 /tmp，未触碰仓库文件。
