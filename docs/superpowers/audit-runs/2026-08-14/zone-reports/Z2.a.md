# Z2 分区初审报告（段文件 Z2.a）— contracts/ + dispatcher/

- 范围: `src/shenbi/contracts/` + `src/shenbi/dispatcher/`（清单 38 文件，全部 deep-read）
- 日期: 2026-08-14 · 模式: 只读（除本段文件外未写仓库；唯一副作用见「验证命令」注记）
- 已知相关 finding 引用: D1-03（G2.12 清单 WARN）

## 执行摘要

1. **契约单一信源（核心维度）**: 已实际验证 `shenbi-sync-contracts` 幂等 — 在 /tmp 镜像副本中运行后，`tests/tiers/deps.json`、`docs/framework/dependency-dag.json`、`docs/framework/truth-files.index.json`、`skills/*/SKILL.md` 全部与仓库当前内容**零 diff**（等效 `git diff --exit-code` 通过）。frontmatter 契约加载（legacy.py）解析 72 个技能契约成功。
2. **字段级 reads 过滤与 escape hatch（AGENTS.md:87-89）**: 实现真实存在于 `contracts/fields.py`，消费点 `pipeline/dispatch_helper.py:592`（Z2 外）。实测：无匹配字段 → `matched=False` + 返回全文 + 打出 `field_filter_no_match` WARN；部分匹配 → 只返回命中 section（未命中字段被静默丢弃、无 WARN，见 F218）。
3. **派发协议/重试/429/finish_reason/并行上限**: 在 `dispatcher/`（Z2）内**均不存在**；实现位于 pipeline/（dispatch_helper.py:1367/1441/1564, parallel_dispatch.py:168）。独立 dispatcher 为单次 codex exec + 600s 超时、无重试（见 F213）。
4. **G3.4**: dispatcher 以 `shenbi-score --subagent` 标记独立评分（scoring.py:441 仅为 provenance 字符串），但 `_record_completion` 不写 `current_scorer_agent`（F214）。
5. **覆盖洞（d1-06）**: d1-06 对 Z2 大量行标注未覆盖，与当前套件实测不符（base.py/deps.py/hooks.py/novel.py/scores.py/state.py/foreshadowing_resolve.py/_scoring_base.py/codex_api.py/internal.py 等实测 100%），判定 d1-06 对 Z2 显著过期；逐文件处置见各文件条目与文末汇总。
6. **最大发现**: `contracts/skills/` 中 chapter_planning / context_composing / volume_outlining 三个契约模型**无任何生产消费者**，而 g4 同名 checker 携带**私有且规则不同**的实现（F201, P1）；`contracts/registry.py` REGISTRY/load_skill_contract、schemas/deps/novel/scores/state、_scoring_base 评分模型全部为 dead-wire（F202-F207）。`genre_config._disabled_dimensions_have_rules` 在 customRules 为空时跳过检查 → G4 校验洞（F216, P1）。

---

## Findings 清单（F2xx，编号段 F200-F299）

### F201 | chapter_planning/context_composing/volume_outlining 契约模型 dead-wire，g4 携带私有且**规则不同**的实现 | error | P1
- 证据: `contracts/skills/chapter_planning.py:15`（`_VALID_OPS`）/`context_composing.py:19-21`/`volume_outlining.py:19-22`；g4 私有实现 `src/shenbi/gates/g4/chapter_planning.py:99-102`（hook_ops 列表）、`g4/context_composing.py:74-86`（P1-P7 分层标题，与模型 "section_count==9" 规则不同）、`g4/volume_outlining.py:59-72`（KR/张力/跨卷桥 hook，与模型 "entity_hooks>=3 且 >=2 types" 不同）
- 验证命令: `grep -rn "ChapterPlanning\|ContextComposing\|VolumeOutlining" src/ tests/ tools/ scripts/ --include=*.py | grep -v "contracts/skills/"` → 仅 `tests/unit/pipeline/test_skill_integration.py:188` 命中（类名文本，非 import）；cov 子集实测 chapter_planning.py 34%、context_composing.py 38%、volume_outlining.py 40%（仅 import 覆盖）
- 根因: 契约模型先于门改造声明（"spec workflow A"），g4 门未切换到模型，形成同规则双实现且已发散
- 建议: g4 checker 改为消费契约模型（或删除死模型），消灭双源

### F202 | contracts/registry.py REGISTRY + load_skill_contract 生产无消费者（仅测试用） | optimization | P2
- 证据: `registry.py:40-63`（`_discover_skill_models`/`REGISTRY`/`load_skill_contract`）；`grep load_skill_contract/REGISTRY src/` 仅命中定义与 __init__ re-export；消费方 g4 直接 `from shenbi.contracts.skills.genre_config import GenreConfig`（g4/genre_config.py:13）
- 验证命令: `grep -rn "load_skill_contract\|REGISTRY\[" src/ --include=*.py | grep -v "contracts/"` → 空；`tests/unit/contracts/test_registry.py:31` 等测试消费
- 根因: 自动发现面先行落地，生产接线未完成
- 建议: 门校验面改走 REGISTRY，或删除并保留直接 import

### F203 | schemas/deps.py DepsDoc + phase_of 未接线；sync_contracts 仍用 json.loads 解析 deps.json | optimization | P2
- 证据: `deps.py:56-64`（DepsDoc）、`deps.py:67-78`（phase_of）；`sync_contracts.py:195`（`deps = json.loads(DEPS_PATH.read_text(...))`）
- 验证命令: `grep -rn "schemas.deps\|from shenbi.contracts.schemas import.*Deps" src/ --include=*.py` → 无生产消费者；测试 `tests/unit/contracts/schemas/test_deps.py` 存在且通过
- 根因: D19 决议的模型先声明，同步器未切换
- 建议: sync_contracts 消费 DepsDoc（model_validate）以获 structural-drift 保护

### F204 | schemas/novel.py NovelConfig 声称 g6 consumer，g6 未 import | optimization | P2
- 证据: `novel.py:3`（docstring "matches the shape of the novel/config doc (g6 consumer)"）；`grep NovelConfig src/shenbi/gates/g6.py src/shenbi/gates/*.py` → 无命中
- 验证命令: `grep -rn "NovelConfig" src/ --include=*.py | grep -v "schemas/novel.py"` → 空
- 根因: D26 决议（producer 名 target_word_count）仅落在模型与 docstring，g6 消费未落地
- 建议: g6.py 改为 model_validate(NovelConfig) 或修正 docstring

### F205 | schemas/scores.py ScoreReport 声称 g5 consumer，无生产 import | optimization | P2
- 证据: `scores.py:2`（"shape of a scoring/score-report doc (g5 consumer)"）；`grep ScoreReport src/shenbi/gates/g5.py src/` → 仅 schemas/scores.py 自身
- 验证命令: `grep -rn "from shenbi.contracts.schemas" src/ --include=*.py | grep -v contracts/schemas/` → 仅 adapt/decisions/hooks/registry 命中
- 根因: 模型先行、消费者未切换
- 建议: g5 消费或删除

### F206 | schemas/state.py ProgressDoc/SummaryDoc 从未被加载验证（C3 修复无生效面） | optimization | P2
- 证据: `state.py:19-30`；`grep ProgressDoc|SummaryDoc src/` → 仅 schemas/state.py
- 验证命令: 同上 grep（无命中）
- 根因: "extra: ignore 防运行时失败"的前提是模型被使用；实际从未使用
- 建议: 接入 progress/summary 读取点或删除

### F207 | skills/_scoring_base.py ScoreReport + score_arc/stratum/volume 评分契约 dead-wire（M3 修复未接线） | optimization | P2
- 证据: `_scoring_base.py:37-81`；`score_arc.py:3-5`、`score_stratum.py:3-5`、`score_volume.py:3-5`；`grep _scoring_base src/` → 无生产消费者（仅 REGISTRY 发现路径，而 REGISTRY 本身无消费者）
- 验证命令: `grep -rn "_scoring_base\|skills.score_arc" src/ --include=*.py | grep -v contracts/skills/` → 空；tests/unit/contracts/test_scoring_contracts.py 通过（测试-only 覆盖，实测 _scoring_base 100% 由测试达成）
- 根因: M3 评分标尺显式声明先行，评分消费点未切换
- 建议: 接入 scoring.py / g3 或删除

### F208 | enums.py Severity/Verdict 无生产消费者；严重性字面量仍散落 4 处裸字符串 | error | P2
- 证据: `enums.py:6-7` 定义 Severity/Verdict；裸字面量发射点 `pipeline/audit_layer.py:174,181,188,191,193`、`pipeline/revision_router.py:204,206`、`pipeline/parallel_dispatch.py:218,220`、`skill_utils/revision_routing/route.py:41`；`enums.py:1` 声称"收严重性词汇分裂"
- 验证命令: `grep -rn "Severity\|Verdict" src/ --include=*.py | grep -v "contracts/enums.py\|contracts/__init__.py"` → 无 `from shenbi.contracts.enums import Severity/Verdict` 的生产 import（仅测试 test_enums.py）
- 根因: 词表声明与发射点未统一；lint_status_strings 的 STATUS_KEYS 不含 "severity" 键（tools/lint_status_strings.py:25），覆盖洞使分裂不可检测
- 建议: 发射点 import Severity/Verdict；或 lint 扩展 severity 键

### F209 | base.py GateOutcome.status 用裸 Literal 复制 status.py GateStatus 词表 | error | P2
- 证据: `base.py:29`（`status: Literal["PASS", "FAIL", "SKIP", "WARN"]`）vs `status.py:17-24`（GateStatus 自称 "THE single definition of every status string"）；`enums.py:1` 声称"所有 Literal 必须从此处 import"
- 验证命令: read base.py/status.py；`tools/lint_status_strings.py:29-35` 只查 dict 发射点，类型注解 Literal 不被扫描（覆盖洞）
- 根因: GateOutcome 是支柱二门改造的过渡类型（base.py:4-5 自述），暂以字面量占位
- 建议: 迁移时改 `status: GateStatus`；过渡期加 lint 断言两者一致

### F210 | hooks.py 的 SKILL.md 行号引用漂移（超出 ±5） | error | P2
- 证据: `hooks.py:4`（"foreshadowing-track/SKILL.md:72-73,120"）、`hooks.py:6`（"SKILL.md:87"）、`hooks.py:26-27`（":72"/":73,120"）、`hooks.py:31`（":87"）；实际 `skills/shenbi-foreshadowing-track/SKILL.md:40`（生命周期链）、`:79-80`（ARCHIVED/EXPIRED）、`:94`（裸 TRIGGER）
- 验证命令: `grep -n "PLANTED\|...\|EXPIRED" skills/shenbi-foreshadowing-track/SKILL.md` → 40/79-80/94/121-123/127；偏移 Δ32-40
- 根因: SKILL.md 编辑后 docstring 行号未同步
- 建议: 更新 hooks.py 引用为当前行号（或改节名引用）

### F211 | executor.py SHENBI_G1_SKIP_READS 环境特性零测试覆盖 | optimization | P2
- 证据: `executor.py:167-188`（skip 过滤逻辑）；cov 子集实测 executor.py 缺 169-188 整块
- 验证命令: `COVERAGE_FILE=/tmp/... pytest --cov=shenbi.dispatcher --cov-report=term-missing tests/unit/dispatcher/...` → missing 含 169-188
- 根因: 环境变量驱动的可选 reads 过滤未加测试
- 建议: must-test —— 覆盖 skip/保留两分支（含不存在文件、部分匹配）

### F212 | executor.py run_g1/run_g2 不检查 returncode，stdout 非 JSON 时裸 JSONDecodeError | error | P2
- 证据: `executor.py:114-119`（`json.loads(result.stdout)`）、`executor.py:125-139`；无 `result.returncode` 判断、无 stderr 上下文
- 验证命令: read executor.py（未运行）
- 根因: 直接翻译 shell 脚本，未加门二进制失败防护
- 建议: 检查 rc!=0 → 记 gate/rc/stderr 后返回失败，避免裸异常

### F213 | dispatcher/modes/codex.py 单次 codex exec、无重试/429/finish_reason/并行上限（仅 pipeline 路径有） | optimization | P2
- 证据: `codex.py:61-72`（timeout=600 单次调用、rc 直传）；对比 `pipeline/dispatch_helper.py:1367`（`_RETRYABLE_STATUSES={429,500,502,503,504}`）、`:1441-1449`（tenacity @retry）、`:1564`（finish_reason cap-raise）、`pipeline/parallel_dispatch.py:168`（Semaphore）
- 验证命令: `grep -n "429|finish_reason|retry|Semaphore" src/shenbi/dispatcher/ src/shenbi/pipeline/*.py`（输出摘要如上）
- 根因: 独立 dispatcher 是 T1 直连路径，鲁棒性只实现于 pipeline 路径
- 建议: 独立路径加有限重试（429/5xx/超时），或文档明示 T1 不重试

### F214 | codex.py _record_completion 不记录 current_scorer_agent；G3.4 fail-closed 与 pipeline 伪造 scorer 并存 | error | P2
- 证据: `codex.py:20-48`（只写 completed_skill_names + skills[skill][test_type]）；`gates/g3_independence.py:20-22`（无 current_scorer_agent → FAIL fail-closed）；`gates/g3.py:214-225`（G3.4 调 scoring_independence_status）；`pipeline/dispatch_helper.py:1937-1942`（pipeline 自造 `pipeline-g3-scorer-<uuid>` 写入）
- 验证命令: read 上述文件 + `grep current_scorer_agent src/`（唯一写入点为 dispatch_helper.py:1939）
- 根因: 评分独立性的"证据"由 pipeline 伪造、dispatcher 不产证；独立 dispatcher 产出的 progress.json 若被 gate_G3 评估必 FAIL
- 建议: dispatcher 记录真实评分 subagent id；pipeline 停止伪造（评审跨区）

### F215 | codex.py 用 `\{[^{}]*\}` 提取首个"无嵌套花括号"JSON 片段 | error | P2
- 证据: `codex.py:75`（`re.search(r"\{[^{}]*\}", raw_text, re.DOTALL)`）
- 验证命令: read codex.py；test_codex_mark_done.py 仅用扁平 dict fixture（`'{"1": 90, "2": 95}'`）
- 根因: 扁平分数 dict 假设；若 codex 输出嵌套 JSON，会静默取到内层错误片段
- 建议: 用围栏代码块 + json.loads 校验整体，或解析最后一个顶层对象

### F216 | genre_config.py `_disabled_dimensions_have_rules` 在 customRules 为空时跳过检查 → G4 校验洞 | error | P1
- 证据: `genre_config.py:92-103`：`if disabled and self.custom_rules:` —— customRules 为空列表时整段跳过；且 `dim not in rule_texts` 是拼接文本子串启发（false positive 面）；g4 以 `GenreConfig.model_validate` 强制执行（`g4/genre_config.py:33-41`）；SKILL.md 规则 7 "Every disabled auditDimension must have a customRules reason"（genre_config.py:25 自述）
- 验证命令: cov 子集实测 genre_config.py 缺 95-100（该规则路径零测试）；`grep -n customRules tests/unit/contracts/test_genre_config_contract.py` → 无 disabled-dim 用例（fixture 全部 dim=True）
- 根因: `and self.custom_rules` 多加了非空前置条件；空规则时禁用维度无理由却放行
- 建议: 改为 `if disabled:`，逐维度要求 rule 命中（结构化匹配而非子串）

### F217 | pacing_design.py CONSTELLATION 区间三处不一致（docstring 20-30 / 代码 15-35 / SKILL.md 15-25） | error | P2
- 证据: `pacing_design.py:7`（"CONSTELLATION ratio in [20, 30]"）、`pacing_design.py:55`（`not 15 <= const <= 35`）、`skills/shenbi-pacing-design/SKILL.md:91`（"CONSTELLATION ... 15-25%"）；`pacing_design.py:8`（"Exactly 8 scene types"）vs `:61`（6-12）vs SKILL.md:97（"至少定义 6-8 种"）
- 验证命令: `grep -n "CONSTELLATION\|scene\|8" skills/shenbi-pacing-design/SKILL.md`（输出摘要如上）；g4/pacing_design.py:13,47 确认模型已被门强制
- 根因: SKILL.md 编辑后模型阈值未跟随；门放行 25-35% 超 SKILL.md 上限
- 建议: 与 SKILL.md 对齐（15-25），docstring 同步

### F218 | fields.py 部分匹配时未命中的已声明字段被静默丢弃（escape hatch 仅全缺触发），调用方丢弃 matched 标志 | error | P2
- 证据: `fields.py:55-64`（`_filter_md` 任一命中即 matched=True，未命中字段无声）；`pipeline/dispatch_helper.py:592`（`content, _matched = filter_to_fields(...)` 后 `_matched` 弃用）；AGENTS.md:87-89 声称"If a declared field is missing ... returns the full file + logs WARN"
- 验证命令: 运行时实测（python -c）→ 部分匹配输出仅含命中 section、无 WARN；全缺 → matched=False + 全文 + `field_filter_no_match` WARN
- 根因: 文档契约字面读法（任一缺失→全文+WARN）未实现为"部分缺失也 WARN"；调用方无法据 matched 决策
- 建议: 部分缺失时同样 WARN（含缺失清单）；调用方据 _matched 记录

### F219 | executor.py 对 ContractError 静默 fail-open：registry 缺失 → 空 inputs → G1 空集真空 PASS → 无上下文派发 | error | P2
- 证据: `executor.py:74-75`（derive_file_type 异常→"chapter"）、`executor.py:107-108`（derive_input_files 异常→[] 且无日志）；`gates/g1.py:147-205`（空 fps 零迭代 → 无 must_fix → PASS）
- 验证命令: read executor.py + g1.py:147-215（未运行）
- 根因: 契约层异常被降级为空集，门空集真空通过，链式静默
- 建议: registry/契约加载失败应 loud-fail（记日志并拒绝派发），或 G1 对空输入 FAIL

### F220 | ownership.py OWNERSHIP 矩阵仅 5 条参考条目（docstring 自述「支柱一续」），其余技能落 file-level 检查 | optimization | P2
- 证据: `ownership.py:36-94`（`OWNERSHIP` 仅 genre-config + pending_hooks 4 技能）；`ownership.py:7-8`（"完整 69 技能 OWNERSHIP 迁移是「支柱一续」"）；`audit/write_audit.py:57,62`（无条目 → file-level 声明写入检查）
- 验证命令: read ownership.py + write_audit.py:57-62；test_ownership.py 9 用例通过
- 根因: 审计粒度为 per-skill-per-file（New-H），值正确性不在范围（docstring 声明）
- 建议: 迁移期接受；迁移完成前文档保持声明即可（无安全洞，file-level 兜底）

### M 级观察（不单列 finding，随文件报告记录）
- `chapter_planning.py:4-6` docstring 英文类型名（info/relation/physical/power）与代码 `_VALID_CHANGE_TYPES` 中文（信息/关系/物理/权力）不一致（M）
- `volume_outlining.py:6` 规则 3 ">= 2 types" 代码未把 types 与 hooks 关联（entity_hook_types 独立字段）（M）
- `gates/g4/review_resonance.py:25` 独立判定词表 `("通过","阻断","待人机复核")` 与 enums.py Verdict `("通过","有瑕疵","不通过")` 并行（M，跨区观察）
- `adapt.py:61` `"schema" in msg.lower()` 启发式可能误分类含 "schema" 字段的错误为 G2.dec.2（M）

---

## 逐文件 deep-read 报告

### src/shenbi/contracts/__init__.py
- 处置: deep-read
- 声称检查的不变量: re-export 与 legacy 兼容面完整；无循环 import；`__all__` 与导出一致；criterion 3 统一 import 面成立
- findings: 无
- 验证命令: read；`grep -rn "from shenbi.contract import" src/`（未运行完整 grep，见下）——注: 仅 read 验证 re-export 名称与 legacy.py 定义一致（ContractError/Contract/OutputKind/load_contract/load_registry/requires_independent_agent 均存在）
- 置信度: high

### src/shenbi/contracts/base.py
- 处置: deep-read
- 声称检查的不变量: PureInput 无写能力（仅内存 dict + Path 读路径）；GateOutcome 纯数据 frozen；passed/fail 工厂正确；status 词表唯一
- findings: [F209]（status Literal 复制 status.py GateStatus，P2）；[F202 关联] GateOutcome/PureInput 无生产消费者（死线，docstring 明示过渡期）
- 验证命令: `pytest --cov=shenbi.contracts.base tests/unit/contracts/test_base.py` → 100%（3 passed）；`grep -rn "GateOutcome" src/ | grep -v base.py` → 仅 __init__ re-export
- 置信度: high

### src/shenbi/contracts/enums.py
- 处置: deep-read
- 声称检查的不变量: Severity/Verdict/CPZone/ActorRole 唯一定义；ALL_ENUMS 完整；"所有 Literal 必须从此处 import" 成立
- findings: [F208]（Severity/Verdict 无生产消费者 + 严重性字面量散落，P2）；[F209 关联]（enums.py:1 声称与 base.py:29 裸 Literal 冲突）
- 验证命令: `grep -rn "from shenbi.contracts.enums import" src/ tests/` → 生产仅 CPZone（foreshadowing_resolve）、ActorRole（trace）；tests/unit/contracts/test_enums.py 4 用例通过
- 置信度: high

### src/shenbi/contracts/fields.py
- 处置: deep-read
- 声称检查的不变量: 规范化规则（U+3000→空格、去零宽、NFKC、不 lowercase）实现与 docstring 一致；match_field 精确语义；_filter_md/_filter_json 正确；escape hatch（全缺→全文+WARN）真实
- findings: [F218]（部分匹配静默丢弃缺失字段，P2）
- 验证命令: 运行时验证（python -c）→ 全缺: matched=False+全文+`field_filter_no_match` WARN；部分: 仅命中 section；`pytest tests/unit/contracts/test_fields.py`（含 test_escape_hatch_returns_full_when_no_match）通过；cov 实测 84%（缺 70-72/74-75/78-79/86/91 均错误/平凡分支）
- 置信度: high

### src/shenbi/contracts/graph.py
- 处置: deep-read
- 声称检查的不变量: normalize_to_glob 三序（parametric 精确 > declared glob > passthrough）；dag_key glob-aware；与 sync/lint 共享同一语义
- findings: 无
- 验证命令: `pytest tests/unit/test_sync_contracts.py`（normalize_to_glob 4 用例）+ `tests/unit/contracts/test_graph.py` 通过；cov 实测 93%（仅分支 37->36/57->56）
- 置信度: high

### src/shenbi/contracts/legacy.py
- 处置: deep-read
- 声称检查的不变量: frontmatter contract: 块为唯一可编辑 I/O 面；kind∈OutputKind；reads/writes/updates 规范化（str 或 dict{file,fields?}）；registry 解析（concept/glob/parametric）；错误带 skill/field 上下文
- findings: [F203 关联]；[F219 关联]（registry 缺失 → ContractError → 调用方 fail-open）
- 验证命令: `pytest tests/unit/test_contract.py`（11 passed，含 resolves parametric 分支）+ sync 运行加载 72 契约成功（见幂等验证）；cov 实测 71%（缺失均为错误 raise 路径 66-73/88/103/106-107/109/112-113/133/136/139-140/142/145/151/175 与 requires_independent_agent 216-229 —— 后者由 tests/unit/contract/test_dict_reads.py:30-49 覆盖，全量套件非空）
- 置信度: high

### src/shenbi/contracts/ownership.py
- 处置: deep-read
- 声称检查的不变量: 三种 level（field/record_create/record_field）违规判定正确；无条目→[] 由 write_audit 兜底；参考条目键集与 fixture 一致
- findings: [F220]（矩阵仅 5 条参考条目，P2，docstring 自述）
- 验证命令: `pytest tests/unit/contracts/test_ownership.py`（9 passed，含 genre-config 9 键断言、plant/track 越权）；`audit/write_audit.py:57,62` 确认接线；cov 实测 93%（缺 122/129 防御性边缘）
- 置信度: high

### src/shenbi/contracts/paths.py
- 处置: deep-read
- 声称检查的不变量: 有界 N 替换（`(?<=[-/])N(?=[-./]|$)`）不破坏词内大写 N（N-corruption canary）；NNN→3 位 chapter；无上下文有占位符→UnresolvedPathError；resolve_or_skip 捕获
- findings: [M 观察] resolve_volume_path 不处理 NNN（`paths.py:37-42`），当前 registry 无 volume-NNN 路径（truth-files.yaml 中 NNN 仅 chapter/benchmark）→ 无实害
- 验证命令: `pytest tests/unit/contracts/test_paths.py` + canary 索引（test_canaries.py:17-18 指向 test_paths + test_dispatcher_executor）通过；cov 实测 94%（缺 41 = volume-None raise 镜像分支）
- 置信度: high

### src/shenbi/contracts/registry.py
- 处置: deep-read
- 声称检查的不变量: bootstrap_registry 从 load_registry 派生（不二次解析 YAML）；异常→{} 兼容 pre-bootstrap；REGISTRY 自动发现跳过 `_` 前缀模块；known_skill_names 为技能名单一源
- findings: [F202]（REGISTRY/load_skill_contract 生产 dead-wire，P2）；[F211 关联]（known_skill_names 66-75 实测未覆盖 → must-test）
- 验证命令: `pytest tests/unit/contracts/test_registry.py` 通过（含 REGISTRY 映射、bootstrap 非空）；cov 实测 79%（缺 32-36 防御回退、73-75 known_skill_names）
- 置信度: high

### src/shenbi/contracts/schemas/__init__.py
- 处置: deep-read（1 行 docstring）
- 声称检查的不变量: 子包可导入
- findings: 无
- 验证命令: pytest 导入通过（203 passed 含本包）
- 置信度: high

### src/shenbi/contracts/schemas/adapt.py
- 处置: deep-read
- 声称检查的不变量: ValidationError → {id,file,s,r} 映射；decisions 错误归到 G2.dec.2（schema 版本）/G2.dec.3
- findings: [M 观察]（`adapt.py:61` "schema" in msg 启发式）
- 验证命令: `pytest tests/unit/contracts/schemas/test_decisions.py` + canary 7（test_canaries.py:114-195 双门锁步）通过；cov 实测 100%
- 置信度: high

### src/shenbi/contracts/schemas/decisions.py
- 处置: deep-read
- 声称检查的不变量: 与 docs/framework/decisions-schema.md（本区 schema 信源）逐字段一致：$schema/skill/chapter/selections/adjustments/budget/produced_at；basis/severity/handling/trim 枚举；P2.5 规则（routine+low 禁 rationale、high/manual_override 必填、≤100 字符）；extra=forbid
- findings: [F2xx 无] —— 但覆盖处置: decisions.py:35/58（Selection/Adjustment rationale>100 raise）实测未覆盖 → must-test
- 验证命令: `pytest tests/unit/contracts/schemas/test_decisions.py` 通过（含 routine+low 禁、high 必填、manual_override 必填）；cov 实测 94%（缺 35/58）；决策表对照: schema doc 的 P2.5 表未列 severity=medium —— 代码对 medium 也要求 rationale（较 doc 更严，M 观察）
- 置信度: high

### src/shenbi/contracts/schemas/deps.py
- 处置: deep-read
- 声称检查的不变量: t2-phases 为 dict（非 list）；下划线键用 Field(alias) 防 pydantic v2 私有属性吞键；phase_of 按 member roster 定位
- findings: [F203]（DepsDoc/phase_of 未接线，P2）
- 验证命令: `pytest tests/unit/contracts/schemas/test_deps.py` 通过；cov 实测 100%（d1-06 对 deps.py 22-64 全列未覆盖 → 过期）
- 置信度: high

### src/shenbi/contracts/schemas/hooks.py
- 处置: deep-read
- 声称检查的不变量: 6 规范状态 + TRIGGER 非规范折叠 + 大小写不敏感 + 空输入→None
- findings: [F210]（SKILL.md 行号引用漂移，P2）
- 验证命令: `pytest tests/unit/contracts/schemas/test_hooks.py` 通过；`grep -n` 实测 SKILL.md 行 40/79-80/94（vs docstring 72-73/87/120）；cov 实测 100%（d1-06 过期）
- 置信度: high

### src/shenbi/contracts/schemas/novel.py
- 处置: deep-read
- 声称检查的不变量: producer 权威名 target_word_count；extra=forbid 暴露结构漂移；g6 消费（docstring）
- findings: [F204]（g6 未消费，P2）
- 验证命令: grep 无生产消费者；`pytest tests/unit/contracts/schemas/test_novel_scores_state.py` 通过；cov 实测 100%
- 置信度: high

### src/shenbi/contracts/schemas/registry.py
- 处置: deep-read
- 声称检查的不变量: concepts/patterns/globs 结构；16 kind 值；version 仅接受 1；空 concepts 拒绝（D24）；producer 默认 skill
- findings: 无
- 验证命令: sync 运行 + `pytest tests/unit/contracts/schemas/test_registry.py` 通过；cov 实测 95%（缺 87 = version!=1 raise，防御分支，acceptable）
- 置信度: high

### src/shenbi/contracts/schemas/scores.py
- 处置: deep-read
- 声称检查的不变量: dimensions/score 形状；_provenance alias；extra=forbid
- findings: [F205]（g5 未消费，P2）
- 验证命令: grep 无消费者；test_novel_scores_state.py 通过；cov 实测 100%
- 置信度: high

### src/shenbi/contracts/schemas/state.py
- 处置: deep-read
- 声称检查的不变量: extra=ignore（producer-uncontrolled）；与 forbid 族区分（C3）
- findings: [F206]（模型未被加载验证，P2）
- 验证命令: grep 无消费者；test_novel_scores_state.py 通过；cov 实测 100%
- 置信度: high

### src/shenbi/contracts/skills/__init__.py
- 处置: deep-read（docstring）
- 声称检查的不变量: 自动发现约定（每模块 Report）
- findings: 无
- 验证命令: 导入通过
- 置信度: high

### src/shenbi/contracts/skills/_scoring_base.py
- 处置: deep-read
- 声称检查的不变量: final_score = 0.6*route_c_soft + 0.4*route_a；passed 需 final>=90 且硬二元全过；tier_advance 需 >=94 且硬二元全过；阈值从 thresholds.py 单一源 import
- findings: [F207]（模型 dead-wire，P2）
- 验证命令: `pytest tests/unit/contracts/test_scoring_contracts.py` 通过（computed_field 行为钉死）；cov 实测 100%（d1-06 过期）
- 置信度: high

### src/shenbi/contracts/skills/chapter_planning.py
- 处置: deep-read
- 声称检查的不变量: 规则 1（chapter>3 至少 1 个中文类型 change）、规则 2（hook ops 白名单）、规则 3（defer+silent>=4 需 activation plan/abandon）
- findings: [F201]（dead-wire + g4 双实现，P1）；[M 观察]（docstring 英文类型名 vs 代码中文）
- 验证命令: grep 无消费者；cov 实测 34%（28-57 全缺）；g4/chapter_planning.py:99-102 私有规则
- 置信度: high

### src/shenbi/contracts/skills/context_composing.py
- 处置: deep-read
- 声称检查的不变量: section_count==9；hook_debt 有 source_file；无 3 连续同型结尾
- findings: [F201]（dead-wire + g4 双实现且规则不同，P1）
- 验证命令: grep 无消费者；cov 实测 38%；g4/context_composing.py:74-86 私有 P1-P7 规则
- 置信度: high

### src/shenbi/contracts/skills/foreshadowing_resolve.py
- 处置: deep-read
- 声称检查的不变量: zone 派生（>=100 RED、>=50 ORANGE、else GREEN）；must_resolve_next_chapter（>200）；debt_level 与 hooks 最大 cp 一致；同 hook 单 cp；CP_THRESHOLDS 单一阈值
- findings: [F201 关联]（模型无生产消费者——g4 无 foreshadowing_resolve checker，仅 plant/track）
- 验证命令: `pytest tests/unit/contracts/test_foreshadowing_resolve_contract.py` 通过；cov 实测 100%（d1-06 过期）；`ls src/shenbi/gates/g4/ | grep foreshadow` → 仅 plant/track
- 置信度: high

### src/shenbi/contracts/skills/genre_config.py
- 处置: deep-read
- 声称检查的不变量: 9 条计数规则全部编码；approval.decision∈{approved,rejected}；禁用<=50；禁用/慎用词有替换；chapterTypes 6-10；auditDimensions 5-10；disabled 维度需 customRules 理由
- findings: [F216]（disabled 维度规则在 customRules 空时跳过 → G4 校验洞，P1）；[F218 关联]（子串启发式）
- 验证命令: `pytest tests/unit/contracts/test_genre_config_contract.py` 通过（9 用例）；cov 实测 88%（缺 95-100 = 规则 7 路径零测试）；g4/genre_config.py:33-41 确认 model_validate 强制执行
- 置信度: high

### src/shenbi/contracts/skills/pacing_design.py
- 处置: deep-read
- 声称检查的不变量: 4 beats、sum=100、3 lines、CONSTELLATION 区间、scene types 数量、无 3 连续同型；from_markdown 解析
- findings: [F217]（CONSTELLATION 区间三处不一致 + scene types 数 docstring 漂移，P2）；[M 观察]（from_markdown 取行内首个数字，区间表如 "30-40%" 会取 30 端点）
- 验证命令: `pytest tests/unit/contracts/test_pacing_design_contract.py` 通过；cov 实测 95%（缺分支）；SKILL.md:91 实测 15-25%
- 置信度: high

### src/shenbi/contracts/skills/score_arc.py / score_stratum.py / score_volume.py
- 处置: deep-read
- 声称检查的不变量: Report=ScoreReport 供 REGISTRY 发现
- findings: [F207]（dead-wire，P2）
- 验证命令: test_scoring_contracts.py 通过；cov 实测 100%
- 置信度: high

### src/shenbi/contracts/skills/volume_outlining.py
- 处置: deep-read
- 声称检查的不变量: KR 3-5；tension sum=100；entity hooks>=3 且 types>=2
- findings: [F201]（dead-wire + g4 双实现且规则不同，P1）；[M 观察]（types 未与 hooks 关联）
- 验证命令: grep 无消费者；cov 实测 40%；g4/volume_outlining.py:59-72 私有规则
- 置信度: high

### src/shenbi/contracts/skills/worldbuilding.py
- 处置: deep-read
- 声称检查的不变量: 最小契约（kind 默认 artifact）可加载
- findings: 无
- 验证命令: test_registry.py 断言 REGISTRY 含 shenbi-worldbuilding；cov 实测 100%
- 置信度: high

### src/shenbi/contracts/thresholds.py
- 处置: deep-read
- 声称检查的不变量: 94/94/94/90/100 与 AGENTS.md:95 一致；门 import 具名常量
- findings: 无
- 验证命令: `grep` 确认 g3 T1_PASS、g5 T2_PASS、scoring TEST_PASS、_scoring_base 引用；test_thresholds.py 通过；cov 100%
- 置信度: high

### src/shenbi/dispatcher/__init__.py
- 处置: deep-read（docstring）
- 声称检查的不变量: 包文档与实现一致
- findings: 无
- 验证命令: 导入通过
- 置信度: high

### src/shenbi/dispatcher/cli.py
- 处置: deep-read
- 声称检查的不变量: argv 解析（skill/test_type/round_dir/prompt 可选）；usage 返回 1；转发 dispatch_with_write_audit
- findings: [F2xx 无] —— 覆盖处置: cli.py 6-29 实测 0%（main() 无单测）→ acceptable（薄 CLI 胶水，核心逻辑经 dispatch* 测试覆盖）
- 验证命令: `pytest tests/unit/test_dispatcher_modes.py` + test_executor_audit 通过；cov 实测 0%（仅 main）
- 置信度: high

### src/shenbi/dispatcher/executor.py
- 处置: deep-read
- 声称检查的不变量: G1/G2 门序；pipeline 模式跳过 G2；file_type 派生（report/truth/decisions/chapter）经 truth-files.yaml（spec New-I）；chapter 占位解析；写审计包裹（pre/post snapshot + audit + rc 语义 0/1/2）
- findings: [F211]（SHENBI_G1_SKIP_READS 未测，P2）；[F212]（run_g1/run_g2 无 rc 检查，P2）；[F219]（ContractError fail-open 链，P2）；[F214 关联]（G3.4 不产证）
- 验证命令: `pytest tests/unit/dispatcher/test_executor_audit.py`（rc=0/2 两路径）+ test_executor_no_codex_api.py（codex-api 不可达）+ `tests/unit/test_dispatcher_executor.py`（含 N-corruption canary test_section_path_not_corrupted）通过；cov 实测 74%（缺 169-188、251-262、192-193、202-203、212-214、224-225、151、270）
- 置信度: high

### src/shenbi/dispatcher/modes/__init__.py
- 处置: deep-read（docstring）
- findings: 无
- 验证命令: 导入通过
- 置信度: high

### src/shenbi/dispatcher/modes/codex.py
- 处置: deep-read
- 声称检查的不变量: codex exec 子进程（无 shell）；600s 超时→SubAgentTimeoutError；JSON 提取；shenbi-score --subagent 独立评分；进度直写 progress.json（替代不存在 entry point）
- findings: [F213]（无重试/429/finish_reason，P2）；[F214]（不写 current_scorer_agent，P2）；[F215]（花括号正则脆弱，P2）
- 验证命令: `pytest tests/unit/dispatcher/test_codex_mark_done.py`（2 用例：进度写入 + 不调 shenbi-progress）通过；test_read_provenance_honest.py（codex 子进程 read-provenance 盲点自认）通过；cov 实测 75%（缺 29-30/54/67-68/71-72/77-78/81-85/112 均错误/协议路径，acceptable）
- 置信度: high

### src/shenbi/dispatcher/modes/codex_api.py
- 处置: deep-read
- 声称检查的不变量: 不可达（detect_mode 仅 codex/internal）；占位 raise DispatcherError
- findings: 无（docstring 自述 DEPRECATED、可删除）
- 验证命令: test_executor_no_codex_api.py（行为级证明不可达）+ test_dispatcher_modes.py 通过；cov 实测 100%
- 置信度: high

### src/shenbi/dispatcher/modes/internal.py
- 处置: deep-read
- 声称检查的不变量: 硬拒绝（无 LLM 后端）
- findings: 无
- 验证命令: test_dispatcher_modes.py:15-25（raise DispatcherError 匹配）通过；cov 实测 100%
- 置信度: high

---

## 覆盖缺口处置（维度 8，d1-06 输入）

说明: d1-06（由 just check 的 coverage.xml 解析）对 Z2 大量行标未覆盖；当前套件实测（`pytest --cov=shenbi.contracts --cov=shenbi.dispatcher tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` + test_contract.py/test_dict_reads.py 定向补测）显示 d1-06 对以下文件显著过期（实测 100% 或接近）: base.py、enums.py、deps.py、hooks.py、novel.py、scores.py、state.py、foreshadowing_resolve.py、_scoring_base.py、thresholds.py、worldbuilding.py、score_*.py、codex_api.py、internal.py、schemas/__init__.py、skills/__init__.py、dispatcher/__init__.py、modes/__init__.py。以下按 d1-06 行组 + 实测残余处置:

| 文件:行组 | 处置 |
|---|---|
| base.py:35,39 | acceptable —— 实测 100%（test_base.py:26-27）；d1-06 过期 |
| fields.py:70-72,74-75,78-79（JSON 过滤 WARN 分支）、86,91（平凡守卫/直通） | acceptable —— 错误/防御分支；核心路径 test_fields.py 覆盖 |
| graph.py:35,38 | acceptable —— 实测 93%，test_sync_contracts 覆盖 |
| legacy.py:66-73,88,103,106-107,109,112-113,133,136,139-140,142,145,151,175（错误 raise 路径） | acceptable —— 防御性校验 raise；happy path 全量覆盖（sync 加载 72 契约） |
| legacy.py:126-127（resolves parametric） | acceptable —— test_contract.py:157-167 覆盖（子集外） |
| legacy.py:216-229（requires_independent_agent） | acceptable —— tests/unit/contract/test_dict_reads.py:30-49 覆盖；且 g0.py:551,565 + dispatch_helper.py:344 生产消费（wired） |
| ownership.py:122,129（record_create/field 删除违规 append） | acceptable —— 防御性边缘；核心路径 test_ownership.py 覆盖 |
| paths.py:41（volume-None raise） | acceptable —— 镜像已覆盖分支（31） |
| registry.py:32-36（bootstrap 异常回退 {}） | acceptable —— 防御性 pre-bootstrap 回退 |
| registry.py:73-75（known_skill_names） | **must-test** —— G0.13/G0 覆盖计数单一源（g0.py:602-604），实测未覆盖 |
| schemas/decisions.py:35,58（rationale>100 raise） | **must-test** —— decisions-schema.md:90 的 P2.5 ≤100 字符规则无测试 |
| schemas/registry.py:87（version!=1 raise） | acceptable —— 防御性结构守卫（D24） |
| genre_config.py:95-100（disabled 维度规则） | **must-test** —— 且关联 F216 P1 校验洞，必须先补测试再修 |
| chapter_planning.py:28-57 / context_composing.py:25-43 / volume_outlining.py:26-45 | dead-code —— 模型无生产消费者（F201）；g4 私有实现为准 |
| pacing_design.py 分支（38->42,68->74,70->68,87->85,93->91） | acceptable —— 核心规则 test_pacing_design_contract.py 覆盖（95%） |
| executor.py:169-188（SHENBI_G1_SKIP_READS） | **must-test** —— env 驱动行为零测试（F211） |
| executor.py:251-262（dispatch 异常重抛路径） | **must-test** —— CRITICAL 注释声明的"崩溃仍审计"路径无测试 |
| executor.py:192-193,202-203,212-214,224-225,151,270 | acceptable —— 门失败 rc 返回/模式分支/错误路径 |
| codex.py:29-30,54,67-68,71-72,77-78,81-85,112 | acceptable —— 协议/超时/JSON 失败路径（均有类型化异常或 rc 直传） |
| cli.py:6-29 | acceptable —— 薄 CLI 胶水（argv 解析），核心逻辑经 dispatch 测试覆盖 |

must-test 汇总: registry.py known_skill_names、decisions.py 35/58、genre_config.py 95-100、executor.py 169-188、executor.py 251-262。

---

## 验证命令（实际运行）

1. `pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → `203 passed in 1.46s`
2. 幂等验证（/tmp 镜像，只读仓库）: `rm -rf /tmp/z2sync && mkdir -p /tmp/z2sync/docs /tmp/z2sync/tests && cp -r docs/framework /tmp/z2sync/docs/ && cp -r tests/tiers /tmp/z2sync/tests/ && cp -r src skills /tmp/z2sync/ && cp pyproject.toml uv.lock /tmp/z2sync/`；`cd /tmp/z2sync && PYTHONPATH=/tmp/z2sync/src /Users/xiaotiac/Documents/GitHub/shenbi/.venv/bin/python -m shenbi.sync_contracts` → `exit=0`、`sync_complete skills=72`；diff 结果: `IDENTICAL: tests/tiers/deps.json` / `IDENTICAL: docs/framework/dependency-dag.json` / `IDENTICAL: docs/framework/truth-files.index.json` / `diff -rq /tmp/z2sync/skills skills` → 无差异（exit=0）→ 等效 `git diff --exit-code` 零 diff
3. escape hatch 实测: `PYTHONPATH=src .venv/bin/python -c "...filter_to_fields..."` → 全缺: `[warning] field_filter_no_match available=['1. 当前任务','2. 世界设定'] fields=['不存在的字段']`、`matched=False`、`full text returned=True`；部分: 仅命中 section
4. cov 定向: `COVERAGE_FILE=/tmp/... pytest --cov=shenbi.contracts --cov=shenbi.dispatcher --cov-report=term-missing <Z2 测试子集>` → 各文件 %（见各文件条目）；`--cov=shenbi.contracts.base tests/unit/contracts/test_base.py` → 100%
5. grep 验证: consumers（ChapterPlanning/ContextComposing/VolumeOutlining/NovelConfig/DepsDoc/ScoreReport/ProgressDoc/SummaryDoc/load_skill_contract/REGISTRY/known_skill_names/requires_independent_agent/current_scorer_agent）、字面量（BLOCKING/CRITICAL/MINOR/“通过”…）、行号（SKILL.md 40/79-80/94）、`429|finish_reason|retry|Semaphore` 定位（仅 pipeline/）
6. read: g1.py:147-215（空 fps 真空 PASS）、g3.py:214-225 + g3_independence.py、g4/genre_config.py:33-41、g4/pacing_design.py:13,47、g4/{chapter_planning,context_composing,volume_outlining}.py 私有规则、tools/lint_status_strings.py、status.py、docs/framework/decisions-schema.md

注记（只读合规）: 首次 pytest 调用受 pyproject addopts 的 --cov 影响，在 gitignored 目录 tests/coverage/ 再生了覆盖率产物（git status 无跟踪变更；该目录本就存在 .gitkeep 等）；后续调用均加 `--no-cov`/`COVERAGE_FILE=/tmp`。`git status --porcelain` 中 `M docs/superpowers/audit-runs/2026-08-14/findings-ledger.md` 为并行 agent 写入，非本段产生。

## 统计

- deep-read 文件: 38 / 38（清单全覆盖，未覆盖文件列表: 空）
- findings: F201-F220 共 20 条（P1×2: F201, F216；P2×18）；M 级观察 5 条随文
- 低置信度文件: 无（全部 high）
