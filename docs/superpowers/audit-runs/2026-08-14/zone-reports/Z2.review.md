# Z2 分区独立复核报告（Z2.review）— contracts/ + dispatcher/

- 复核对象: `docs/superpowers/audit-runs/2026-08-14/zone-reports/Z2.a.md`（F201-F220，20 findings）
- 模式: fresh-context 全量重读 38 文件；只读（未创建/修改/删除仓库文件，除本报告；git 无操作）
- 复核日期: 2026-08-14

## 复核执行摘要

1. **全量重读**：38/38 文件逐一重读（清单全覆盖），非依赖初审转述。
2. **独立验证方式**：grep 消费面核查、python -c 运行时实测（F216/F218/F215/escape hatch/GenreConfig fixture）、pytest 只读子集（203 passed，与初审一致）、cov 定向子集（34%/38%/40%/74%/75%/79%/88%/95%/100% 等与初审逐项吻合）。
3. **初审总体可信度**：20 条 findings 中 **18 条实质成立、0 条整体误报、2 条需修正**（F211 表述过度、F220 计数错误）；**2 条严重度异议**（F218 建议 P2→P1、F214 建议 P2→P1）；**6 条新发现**（F221-F226）。
4. **重点核查项结论**：
   - F201（P1）双源漂移：**成立**，g4 私有实现与死模型规则确实不同（见逐条）。
   - F216（P1）空 customRules 跳过：**成立**，运行时实测 `model_validate` 空 customRules + disabled dim → PASS（bug 复现）；且真实 fixture `genre-config-example.json` 含 `texture: False` 禁用维度，证明该路径有真实触发面。
   - 字段级 reads 过滤：**成立**。实测部分匹配 → 仅命中 section、无 WARN、matched=True（F218）；全缺 → 全文 + `field_filter_no_match` WARN。**新增严重度异议**：AGENTS.md:87-89 字面契约（"If a declared field is missing → full file + WARN"）在部分缺失时两处均不满足 → 建议 P1。
   - dispatcher 派发协议与 executor 异常面：初审 F212/F213/F219 均成立；**新发现** executor 的 SHENBI_G1_SKIP_READS 与 g1.py/dispatch_helper 三处实现语义分叉（F222）、codex.py 协议边界 JSON 无防护（F223）、G3.3 全仓无生产者永久 SKIP（F224）、独立 dispatcher 不消费 read_fields（F225）。

---

## 一、复核结论表（初审 findings 判定）

| 编号 | 初审严重度 | 复核判定 | 理由 |
|---|---|---|---|
| F201 | P1 | **成立** | 三模型无生产消费者（仅 test_skill_integration.py:188 类名文本命中）；g4 私有规则实测不同：chapter_planning g4:99-102（s7 hook ops 存在性）vs 模型（typed change/op 白名单/defer 激活计划）；context_composing g4:74-86（P1-P7/route-a/b/c 标题）vs 模型（section_count==9）；volume_outlining g4:59-72（KR3-5/张力曲线/跨卷桥）vs 模型（entity_hooks>=3 且 types>=2）。cov 34/38/40% 实测吻合。P1 合理（契约层宪章即单一信源）。 |
| F202 | P2 | **成立** | `grep load_skill_contract/REGISTRY src/` 无生产消费者（仅 __init__ re-export）；g4 直接 import 模型。 |
| F203 | P2 | **成立** | DepsDoc/phase_of 无生产消费者；sync_contracts.py:195 `deps = json.loads(...)` 实测确认。 |
| F204 | P2 | **成立（附注）** | NovelConfig 无 import；但 g6.py:51 确以 raw dict 读 `target_word_count`（含 target_words 回退），docstring 的"g6 读取"在 dict 层属实——死的是模型而非行为。死线本体成立。 |
| F205 | P2 | **成立** | `grep from shenbi.contracts.schemas src/` 仅 decisions/hooks/registry 命中，无 schemas.scores 消费者。 |
| F206 | P2 | **成立** | ProgressDoc/SummaryDoc 无任何生产加载点。 |
| F207 | P2 | **成立** | _scoring_base 无生产消费者；cov 100% 系测试达成（实测吻合）。 |
| F208 | P2 | **成立（增补）** | Severity/Verdict 无生产 import；裸字面量 4 处文件实测确认（audit_layer:174,181,188,191,193 / revision_router:204,206 / parallel_dispatch:218,220 / route.py:41）；lint_status_strings.py:25 STATUS_KEYS 无 severity 键。**增补站点**：chapter_loop.py:1797 裸 "BLOCKING"/"FAIL" 字符串比较、chapter_loop.py:409 自建 G4Severity(HARD/SOFT/WARN) 并行词表。 |
| F209 | P2 | **成立（增补）** | base.py:29 Literal 复制 status.py:17-24 GateStatus（且缺 UNIMPLEMENTED，status.py:24 有——GateOutcome 无法表达 stub gate 状态）。 |
| F210 | P2 | **成立** | SKILL.md 实测 40/79-80/94（vs docstring 72-73/87/120），Δ32-40 吻合。 |
| F211 | P2 | **成立（表述修正）** | executor.py:169-188 cov 缺整块（实测确认）。但"SHENBI_G1_SKIP_READS 零测试"过度：g1.py:174-190 分支有测试（test_g1.py:245-258）、dispatch_helper 设置有测试（test_dispatch_helper.py:172-223）；零测试的仅是 executor 侧实现。must-test 结论不变。 |
| F212 | P2 | **成立** | run_g1/run_g2 无 returncode 检查、无 stderr 上下文；rc!=0 且 stdout 空时裸 JSONDecodeError。 |
| F213 | P2 | **成立** | codex.py:61-72 单次 exec；pipeline 侧 1367/1441-1449/1564/parallel_dispatch:168 实测确认。 |
| F214 | P2 | **成立（严重度异议→建议 P1）** | codex.py _record_completion 不写 current_scorer_agent；g3.py G3.4 经 scoring_independence_status fail-closed；dispatch_helper.py:1939 是唯一写入点。**异议理由见第三节**：pipeline run_gate_g3（dispatch_helper.py:1935-1942）在 progress.json 缺失时**主动伪造** `current_scorer_agent: pipeline-g3-scorer-<uuid>` 与 `scoring_history` 证据 → 生产方自写证据使 fail-closed 门静默放行，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4)"显式契约。 |
| F215 | P2 | **成立** | 实测 `\{[^{}]*\}` 对嵌套 JSON 取**内层**对象（`{"final_score":90,"dimensions":[{"num":1,"score":88}]}` → 匹配 `{"num":1,"score":88}`），对多对象取第一个。 |
| F216 | P1 | **成立** | 运行时复现：空 customRules + disabled dim → model_validate PASS（bug 确认）；子串启发 false-accept 确认（无关规则 description 含维度名即放行）；真实 fixture 含 texture:False 禁用维度 + 28 条 customRules（mention "texture"），模型对 fixture 通过。P1 合理（G4 校验洞，正常路径）。 |
| F217 | P2 | **成立（范围低估）** | 实测 SKILL.md 内部即有 **5 处** CONSTELLATION 区间：:91/:156/:192/:194-195（15-25%）、:176（"低于 20% 或高于 30% 触发警告"）、:187（15-30%）、:193 开卷（30-40%），加 docstring 20-30 与代码 15-35——非"三处不一致"而是五处以上。场景类型：SKILL.md:97"至少 6-8 种" vs :199"必须定义恰好 8 种"（SKILL.md 自相矛盾）vs 代码 6-12 vs docstring "Exactly 8"。严重度维持 P2。 |
| F218 | P2 | **成立（严重度异议→建议 P1）** | 实测部分匹配 → 仅命中 section、matched=True、无 WARN（md 与 json 同构，json 仅投影命中键）。**异议理由见第三节**：违反 AGENTS.md:87-89 显式契约（"If a declared field is missing → full file + WARN"），部分缺失时既不给全文也无 WARN，正常路径（truth 文件随剧情演进缺 section 是常态）静默信息丢失。 |
| F219 | P2 | **成立（增补）** | executor.py:74-75/:107-108 fail-open 实测确认；g1.py:279-285 空 fps → G1.0 SKIP + 整体 PASS。**增补**：同链还波及写审计——executor.py:223-225 `_audit_watch_paths` 对 ContractError 返回 [] → snapshot_tree 无监视面 → 契约损坏时写越权审计同样盲区；且 `_truth_file_set` 缓存（executor.py:38-43）在 bootstrap 失败时永久固化空集，truth 判定后续不可恢复。P2 维持（触发需 registry/契约损坏，非正常路径）。 |
| F220 | P2 | **部分误报（计数错误）** | OWNERSHIP 实为 **6 条**（含 `("shenbi-foundation-review","genre-config.json")` read 条目），非"5 条"；"仅 genre-config + pending_hooks 4 技能"亦漏 foundation-review。实质结论（矩阵小、file-level 兜底 write_audit.py:60-64）成立，P2 维持。 |

**误报统计**：整体误报 0 条；部分误报 1 条（F220 计数 5→6）；表述修正 1 条（F211）。

---

## 二、新发现（F221-F226）

### F221 | GenreConfig 缺 tropeInventory 字段且未编码 SKILL.md 规则 1「顶层字段数=8」；SKILL.md/fixture/OWNERSHIP 四源冲突 | 漏报（校验洞+文档漂移） | P2
- 证据: `src/shenbi/contracts/skills/genre_config.py:28-37`（8 字段、无 tropeInventory、`extra=ignore`）；`skills/shenbi-genre-config/SKILL.md:286`（"顶层字段数：恰好 8 个（version, updated, fatigueWords, pacing, chapterTypes, auditDimensions, customRules, approval）"）与 `:290` 规则表"顶层字段数 | = 8 | ≠ 8"；`tests/fixtures/genre-config-example.json` 实测 **9 键**（含 tropeInventory）；`src/shenbi/contracts/ownership.py:38-50`（_GENRE_KEYS 9 键）；模型 docstring `genre_config.py:5` 自称"Field set verified against real fixture (8 camelCase keys)"
- 根因: 模型/规则表按 8 键声明（docstring 写"8 camelCase keys"），但真实 fixture 与 OWNERSHIP 均为 9 键含 tropeInventory；`extra=ignore` 使顶层字段数漂移与 tropeInventory 内容完全无结构校验 → g4 `GenreConfig.model_validate` 静默放行。SKILL.md 规则 1 与权威 fixture 直接冲突，三源（+docstring）不一致。
- 验证命令: `PYTHONPATH=src .venv/bin/python -c "import json; d=json.load(open('tests/fixtures/genre-config-example.json')); print(sorted(d))"` → `['approval','auditDimensions','chapterTypes','customRules','fatigueWords','pacing','tropeInventory','updated','version']`（9 键）；`GenreConfig.model_validate(fixture)` → PASS（tropeInventory 静默丢弃）；`grep tropeInventory src/shenbi/gates/g4/genre_config.py src/shenbi/contracts/skills/genre_config.py` → 空
- 建议方向: 判定 SKILL.md 规则 1 与 fixture 谁是权威并统一（fixture 若权威则 SKILL.md:286/:290 与模型 docstring 需改 9 键）；模型补 tropeInventory 字段 + 顶层字段数 validator（把规则 1 编码进模型），消灭四源漂移

### F222 | SHENBI_G1_SKIP_READS 三处实现/消费语义分叉（executor/g1/dispatch_helper），executor 侧零测试 | 漏报（F211 增补） | P2
- 证据: `src/shenbi/dispatcher/executor.py:167-188`（basename fnmatch + **存在性判断**——仅丢不存在的文件）；`src/shenbi/gates/g1.py:174-190`（仅丢**未匹配 glob**，pass 分支）；`src/shenbi/pipeline/dispatch_helper.py:382`（`_G1_SKIP_ENV_VAR = "SHENBI_G1_SKIP_READS"` 设置者）
- 根因: 同一 env 契约在三处独立实现，executor 与 g1 语义不同（存在性过滤 vs glob 未匹配丢弃），未收敛单一 helper；F211 只覆盖 executor 侧零测试，未指出 g1/dispatch_helper 并存。
- 验证命令: `grep -rn "SHENBI_G1_SKIP_READS" src/ --include=*.py` → executor.py:167 / g1.py:174 / dispatch_helper.py:382 三处；`tests/unit/gates/test_g1.py:245-258`（g1 分支有测试）、`tests/unit/pipeline/test_dispatch_helper.py:172-223`（设置有测试）、executor.py 169-188 cov missing
- 建议方向: 收敛为单一实现（如 gate 层或 contracts 层 helper 供三方复用），并补 executor 分支测试（不存在文件/部分匹配两分支）

### F223 | codex.py 协议边界 JSON 处理无防护：shenbi-score stdout 双解析 + final_score 缺省静默 0 + 损坏 progress.json 裸 JSONDecodeError | 漏报 | P2
- 证据: `src/shenbi/dispatcher/modes/codex.py:114`（`json.loads(result.stdout).get("final_score", 0)`——rc=0 但 stdout 非 JSON 时裸 JSONDecodeError；缺 final_score 键时静默记 0 并标 done）；`:116`（`emit_json(json.loads(result.stdout))` 对同一 stdout 再解析，共三次解析）；`:28-29`（`progress_path.exists()` 时 `json.loads(progress_path.read_text())` 无 try——评分**已完成**后若 progress.json 损坏则裸炸，完成记录丢失、dispatch 崩溃）
- 根因: 评分子进程输出无异常/schema 防护（F212 同类问题在 codex.py 侧未被覆盖）；progress.json 读取无容错。
- 验证命令: read codex.py:27-32, 111-117；`grep -n "final_score" src/shenbi/dispatcher/modes/codex.py` → 仅 :114
- 建议方向: 单次解析复用结果；try/except + final_score 存在性校验（缺失则协议错误而非静默 0）；progress.json 读取容错（损坏 → WARN + 重建而非崩溃）

### F224 | G3.3「output files passed G2」全仓无生产者 → 永久 SKIP；codex.py _record_completion 写 progress["skills"][skill][test_type]={score,status} 无 output_files 键 | 漏报（门静默空转） | P2
- 证据: `src/shenbi/gates/g3.py:146-156`（读 `progress["skills"][skill]["output_files"]`，`:187` 空时 SKIP）；`src/shenbi/dispatcher/modes/codex.py:40-46`（写 `skills[skill][test_type] = {"score":..., "status":"done"}`）；`grep -rn '"output_files"' src/ --include=*.py` → 唯一命中 g3.py:153（**读取**，全仓无写入者）
- 根因: progress.json skills 条目形状两处契约未对齐；executor.py:205-206 pipeline 模式跳过 G2（"output validated by G4/G6"）时 G3.3 本应兜底复验 G2，但 output_files 从未被任何生产者写入 → G3.3 永远走 SKIP 分支，pipeline 模式输出实际无 G2 兜底。
- 验证命令: `grep -rn '"output_files"' src/ --include=*.py | grep -v test` → 仅 g3.py:153；read g3.py:146-156 + codex.py:40-46
- 建议方向: `_record_completion` 补写 `output_files`（从 contract writes/updates 派生）或删除 G3.3；至少让 G3.3 从 contract 声明而非 progress.json 取输出面

### F225 | 独立 dispatcher（codex 模式）不消费 contract read_fields：Layer B 字段过滤仅 pipeline 生效 | 漏报 | P2
- 证据: `src/shenbi/dispatcher/executor.py:99-103`（仅用 `load_contract(skill)["reads"]` 纯路径，全文件无 read_fields/filter_to_fields 引用）；`src/shenbi/pipeline/dispatch_helper.py:571-592` 与 `:1229-1230`（read_fields 唯二消费点，均在 pipeline）；AGENTS.md:87-89（"The dispatcher filters file content to only declared fields before the LLM sees it"）
- 根因: read_fields 契约声明与消费面不一致——shenbi-dispatch 独立入口（codex 子代理在 round_dir 直读全量文件）不执行字段过滤 → 声明了 fields 的技能在 T1 直连路径获得全量文件（token 浪费 + 超出声明信息面），与 F218 的 escape hatch 语义在该路径完全不生效。
- 验证命令: `grep -rn "read_fields\|filter_to_fields" src/shenbi/dispatcher/ --include=*.py` → 空
- 建议方向: 文档明示字段过滤仅 pipeline 生效；独立路径无法拦截 codex 直读时至少 WARN 声明未生效；与 F218 修复联动

### F226 | cli.py usage 声明 prompt 可选，codex 模式硬要求非空 → 缺参时裸 SubAgentProtocolError | 漏报 | M
- 证据: `src/shenbi/dispatcher/cli.py:22-28`（usage `[prompt]` 可选、默认 `""`）；`src/shenbi/dispatcher/modes/codex.py:53-54`（空 prompt `raise SubAgentProtocolError`）；`dispatch_with_write_audit` 重抛 → cli main 无 try/except → traceback
- 根因: usage 文案与 codex 前置条件不一致；缺参错误类型化但不友好（无 usage 提示、无 rc 语义）。
- 验证命令: read cli.py:17-29 + codex.py:51-54
- 建议方向: 空 prompt 时返回 usage（rc=1）或修正 usage 文案为必填

### M 级增补观察（随文记录）
- `chapter_loop.py:1797` 裸 `"BLOCKING" in text or "FAIL" in text` 字符串比较 —— F208 裸字面量散落的又一站点（初审未列）
- `chapter_loop.py:409-438` G4Severity(HARD/SOFT/WARN) 自建词表 —— 与 enums.py/status.py 并行的又一套状态词表（F208 同类）
- `base.py:29` Literal 缺 `UNIMPLEMENTED`（status.py:24 定义）—— GateOutcome 无法表达 stub gate 状态（F209 增补）
- F217 实测 SKILL.md 内部即 5 处 CONSTELLATION 区间、场景类型 4 源（详见 F217 判定行）

---

## 三、严重度异议（不修改初审，仅提理由）

1. **F218 P2 → 建议 P1**：AGENTS.md:87-89 显式契约字面语义是"任一 declared field 缺失 → 返回全文 + WARN"；实现仅在**全部**缺失时满足，部分缺失时返回仅命中 section 的子集且无 WARN（`fields.py:55-64`，`_filter_json:76-80` 同构）。truth 文件随剧情演进缺某 section 是正常路径（如早期无"活跃线索"），此时下游规划 skill 静默在信息不全状态下决策——满足决策表 P1"违反 AGENTS.md 显式契约；正常路径可复现功能错误"。
2. **F214 P2 → 建议 P1**：pipeline 侧 `run_gate_g3`（dispatch_helper.py:1935-1942）在 progress.json 缺失时**主动写入伪造**的 `current_scorer_agent: pipeline-g3-scorer-<uuid>` 与 `scoring_history: [{"agent": "pipeline-skill-generator"...}]`，使 G3.4（fail-closed）与 G3.5 由生产方自写证据静默放行——评分独立性证据成为门面，违反 AGENTS.md"Scoring MUST use an independent subagent (G3.4); dispatcher-scored results are invalid"显式契约。注：此异议证据主体在 pipeline/（跨区），Z2 侧（codex.py 不写 scorer）本体仍 P2。

---

## 四、覆盖空洞声明

- 重读文件数：**38 / 38**（清单全覆盖；未覆盖列表：空）。
- 独立复跑验证：`pytest -p no:cacheprovider --no-cov -q tests/unit/contracts/ tests/unit/dispatcher/ tests/unit/test_dispatcher_executor.py tests/unit/test_dispatcher_modes.py tests/unit/test_sync_contracts.py` → **203 passed**（与初审一致）；cov 定向子集（chapter_planning 34% / context_composing 38% / volume_outlining 40% / executor 74% 缺 169-188·251-262 / codex 75% / genre_config 88% 缺 95-100 / registry 79% 缺 32-36·73-75 / pacing 95% / _scoring_base 100%）逐项与初审吻合。
- 未独立复跑项：初审 claim #1（shenbi-sync-contracts 幂等零 diff）——sync_contracts.py 不在 Z2 清单内，属跨区声明，未复跑（不影 Z2 findings 判定）。
- 只读合规：未创建/修改/删除仓库文件（除本报告）；未执行 git add/commit；验证均在 /tmp COVERAGE_FILE + 只读 pytest/python -c。

---

## 五、统计

- 新 finding：**6 条**（F221-F226：P2×5、M×1）
- 误报：**1 条（部分）**（F220 计数 5→6，实质成立）；表述修正 1 条（F211，实质成立）
- 严重度异议：**2 条**（F218 P2→P1、F214 P2→P1）
- 初审 findings 判定：20 条中 18 条实质成立、0 条整体不成立、2 条需修正（F211/F220）
- **0 新 finding 判定：否**（F221-F226 共 6 条新发现）
