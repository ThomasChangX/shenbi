# Z4.review7 复核报告 — src/shenbi/gates/（含 g4 checkers）第七轮独立复核

- 复核 agent：第七轮独立复核（fresh-context，全量重读，不依赖前七轮结论）
- 复核对象：`Z4.a.md`（F400-F418）+ `Z4.review.md`（F419-F430）+ `Z4.review2.md`（F431-F439）+ `Z4.review3.md`（F440-F445）+ `Z4.review4.md`（F446-F455）+ `Z4.review5.md`（F456-F461）+ `Z4.review6.md`（F462-F467）
- 重读覆盖：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 24 个 checker 逐个通读；`src/shenbi/paths.py`、`src/shenbi/contracts/schemas/decisions.py`、`src/shenbi/scoring.py`、`src/shenbi/dispatcher/executor.py`、`src/shenbi/pipeline/dispatch_helper.py` 作交叉证据核实）
- 实测基线：`.venv/bin/python` 只读验证 15+ 组（/tmp 临时目录构造 + 真实项目 `novel-output/xinghuo-ranqiong` 实证 + 真实 SKILL.md/volume_map/pending_hooks 格式核对；未修改任何仓库文件，未 git add/commit）；`git status` 复查无跟踪文件污染
- 复核结论：**误报 0**；**新增严重度异议 0**（前两轮 F404→P1、F408→P1 异议本轮独立实测复测确认成立；任务点名的 6 个 P1 逐一反证，均无推翻证据）；**新发现 5**（F468-F472，全部实测验证）
- 结论：**0 新 finding 判定 = 否**（本轮发现 5 个前六轮漏报，未收敛到 0）

---

## 一、复核结论表（F400-F467 逐条判定）

| 编号 | 初审严重度 | 前几轮判定 | 本轮判定 | 理由（本轮独立证据） |
|---|---|---|---|---|
| F400 | P2 | 成立 | **成立** | 读码确认 g2.py:315-322（G2.12）/273-287（G2.10）无 file_type 守卫；G2.dec 分支（:164 `continue`）正确跳过，故仅非 decisions 文件受影响。JSON 末行 `}` 不在 sentence_enders → 恒 WARN。 |
| F401 | P1 | 成立 | **成立** | 本轮实测：`progress.skills[wb][generative].status=DONE` + `shenbi-worldbuilding-generative-scores.json` → `GR.2:...:status=?` FAIL（复现）；test_g_reconcile.py:7-15 docstring 自认用 `<skill>-<test_type>.json` 命名规避。P1 无推翻理由。 |
| F402 | P1 | 成立 | **成立** | length_normalizing.py:29 `pf = resolve_input_path(fp, rd)` vs :35 `word_count_md(fp)` 用原始 fp（读码确认）；test 全部绝对路径+rd=None（抽查 test_length_normalizing.py 确认）。 |
| F403 | P2 | 成立 | **成立**（范围不全，见 F472） | cli.py:113-121 无 try/except；RoundPaths checker 双 None raise ValueError（逐一读码确认）。**本轮补充**：bughunt/clean 分支（cli.py:107-110）根本不转发 rd（arg(2) 解析后丢弃）→ 相对路径报告恒 `resolve_input_path` ValueError 崩溃（见新 F472）。 |
| F404 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 本轮实测：`Selection(target='ch3', selected=['a'], basis='manual_override', severity='high', rationale='')` → **PASS（HOLE）**；缺失 rationale → ValueError（REQUIRED 生效）。decisions.py:33 `has = rationale is not None`。P1 无推翻理由。 |
| F405 | P2 | 成立 | **成立** | 读码确认 G4_CHECKER_SKILLS 22 项（shared.py:247-270）vs generic.py checkers 分发表 31 项。 |
| F406 | P2 | 成立（影响面限定） | **成立**（影响面限定） | g0.py:195/323 用 `PROJECT/"skill-output"`、g7.py:66 用 `rd/"skill-output"`；`ls -d` 实测仅 `novel-output` 存在。round-exec 测试 round 有 skill-output 的限定维持。 |
| F407 | P2 | 成立 | **成立** | g0.py:177-179 无 seed → `return passed("G0", [G0.1 SKIP])`，G0.2-G0.16/G0.cc 全跳过（读码确认）。 |
| F408 | P2 | 成立，**异议→P1** | **成立，确认 P1** | 本轮直接读 dispatch_helper.py:1921-1945：progress.json 缺失时 safe_write 伪造 `current_scorer_agent: pipeline-g3-scorer-<uuid>` + scoring_history → G3.4 fail-closed 被击穿并制造虚假审计证据（该伪造形状亦无 `skills` 键，破坏 GR.2/G_TRANSITION 语义）。P1 无推翻理由。 |
| F409 | P2 | 成立 | **成立** | g1.py:281-282 空输入仅记 G1.0 SKIP → PASS（读码确认）。 |
| F410 | M | 成立 | **成立** | SHORT_MAP 20 项（cli.py:32-53）；11 个缺 shorthand 技能目录 EXISTS（`ls skills/shenbi-market-radar` 等实测）。 |
| F411 | M | 成立 | **成立** | g0.py:220 `missing_dirs` 永不 append、:227 恒假；:532 注释 "20 skills" vs 22（读码确认）。 |
| F412 | M | 成立 | **成立** | g3_independence.py:3 引 g3.py:144-162，实际 G3.4 块在 193-206（读码确认）。 |
| F413 | M | 成立 | **成立** | decisions_validator.py:175 注释 "shared.py:113"；fail() 在 shared.py:125-136（读码确认）。 |
| F414 | M | 成立 | **成立** | chapter_revision.py:97 `"HARD_FAIL"` 不在 GateStatus 词表（status.py 仅 5 值；读码确认）。 |
| F415 | P2 | 成立 | **成立** | `grep -n "不要包含章节号" skills/shenbi-chapter-drafting/SKILL.md` → :140；:125=`<!--META-END-->`；chapter_drafting.py:73 引 :125，漂移 15 行（本轮复验）。 |
| F416 | M | 成立 | **成立** | decisions-schema.md:66-68 仅 low/high；decisions.py:12 VALID_SEVERITY 含 medium（读码确认）。 |
| F417 | — | 成立 | **成立** | tests/unit/gates 无 test_gate_manifest.py、g4 无 test_memory_distill.py（ls 复验）；gate_manifest 0% 覆盖属实。 |
| F418 | P2 | 成立 | **成立** | g0.py:665 `check_config_coherence(project_dir)` 唯一生产调用点，无 floor 实参；g0_config_coherence.py:65 仅 floor 非 None 时执行两类检测（读码确认）。 |
| F419 | P2 | 成立 | **成立** | g1.py:210-213 / g2.py:169-172 except 元组缺 ValueError（读码确认）。 |
| F420 | P2 | 成立 | **成立** | g6.py:50/56 novel.json 与 genre-config.json jload 无保护（读码确认）。 |
| F421 | P2 | 成立 | **成立** | g_reconcile.py:33 无 try；g_dispatch.py:37-38 / g_transition.py:38-39 缺 ValueError（读码确认）。 |
| F422 | P2 | 成立 | **成立** | cli.py:83-87 JSON 解析失败 → `input_files=[]`，叠加 F409 后 G1 CLI 面零保护（读码确认）。 |
| F423 | P2 | 成立 | **成立** | `ls tests/scoring.py` → 不存在，实现在 src/shenbi/scoring.py；g0.py:358 引用旧路径 → 恒 WARN（读码+ls 复验）。 |
| F424 | P2 | 成立（影响面限定） | **成立**（影响面限定） | chapter_drafting.py:219-222 skill-output 爬升；pipeline 布局下 `read_genre_config(project_root)` 读不到 → FATIGUE_BASE 兜底（读码确认）。 |
| F425 | P2 | 成立（影响面限定） | **成立**（影响面限定） | g7.py:66+86-91 G7.6 truth 搜索嵌在 `no_dir.exists()`（skill-output）之下；pipeline 布局恒 SKIP（读码确认）。 |
| F426 | P2 | 成立 | **成立**（范围不全，见下方注） | genre_config.py:28 `str(Path(rd) / fps[0]) if rd and fps else (fps[0] if fps else "")` 未走 resolve_input_path（读码确认）。**注**：同根因另有一处未枚举——pacing_design.py:31 `project_dir = rd or (str(Path(fps[0]).parent.parent) if fps else None)`，fps[0] 为相对路径时 `parent.parent` 解析为 `.` → 静默 CWD 回退；建议与 F426 一并处置。 |
| F427 | P2 | 成立 | **成立** | chapter_revision.py:45 `Path(fp)` 原始路径，OSError 被 :51 捕获 → 误报 invalid_json（读码确认）。 |
| F428 | P2 | 成立（根因补充→F462） | **成立**（根因补充，见 F468） | g3.py:98/103 直读路径用 acceptance.t1=94 vs :113/:125 回退硬编码 90（读码确认分叉）；本轮补充：`threshold = 90` 在循环内赋值造成**跨报告泄漏**（见新 F468）。 |
| F429 | P2 | 成立 | **成立** | g1.py:228-232 gate 内 safe_write .bak（FAIL 判定前已落盘）；AGENTS.md 纯验证契约偏离（读码确认）。 |
| F430 | P2 | 成立 | **成立** | foreshadowing_track.py:41-46 裸词 `操作` 命中即 PASS（读码确认；真实 pending_hooks.md 表格含"操作"列 → has_changes 恒真）。 |
| F431 | P2 | 成立（范围不全→多轮补充） | **成立（范围仍不全，见 F470）** | 前几轮枚举 8 处 + g0.py 2 处 + deps.json + worldbuilding 2 处；**本轮发现 g3.py:97（G3.2 acceptance.json）、g3.py:102（G3.2 逐报告）、g5.py:67（G5.1 逐报告）3 处仍漏**（见新 F470，全部实测崩溃）。 |
| F432 | P1 | 成立 | **成立** | 本轮实测：ALL_SKILLS=74（73×shenbi-* + using-shenbi），t1-skill 缺 5 个脚手架（foreshadowing-lifecycle + review-group-* 4 个，comm 实测名单一致）；G7.1b（g7.py:59）以 74 为全集 → 完整 round 恒 FAIL。P1 无推翻理由。 |
| F433 | P2 | 成立 | **成立** | chapter_drafting.py:331-334 条件判 project_dir、取值用 project_root（读码确认）。 |
| F434 | P2 | 成立 | **成立** | state_settling.py:14/46-53 单字符子串匹配（读码确认；真实 fixture 实证前几轮已做）。 |
| F435 | P2 | 成立 | **成立** | g0.py:521-529 exempt_skills 读后零消费（grep 仅 522/527）；:530-533 注释声称的校验未实现（读码确认）。 |
| F436 | M | 成立 | **成立** | g6.py:125/142 planted_chapters 零消费（grep 复验）。 |
| F437 | P2 | 成立 | **成立** | g5.py:274-296 except Exception → WARN；对照 G6.3（:97-98 FAIL）/G7.13（:166-167 FAIL）fail-closed（读码确认）。 |
| F438 | M | 成立 | **成立** | 本轮 grep：SKILL.md:85/134 均 ≤1/3000；chapter_drafting.py:204 `max(5, wc//1000)` 放宽 3 倍未同步 SKILL。 |
| F439 | P2 | 成立 | **成立** | 本轮实测：`'shenbi-style-polishing' in BACKUP_SKILLS` → False（BACKUP_SKILLS=16）；style-polishing updates `chapters/chapter-N.md`（非 truth 概念，derive 按设计排除）；style_polishing.py:42-43 读 .bak 恒不成立且无 SKIP 记录。 |
| F440 | P2 | 成立 | **成立** | g0.py:202-207（G0.3）/524-528（G0.12）except 缺 ValueError（读码确认）。 |
| F441 | P2 | 成立 | **成立** | g0.py:98-99 json.loads 直读 + :107 `.get` 无 isinstance 守卫（读码确认）。 |
| F442 | P2 | 成立 | **成立** | length_normalizing.py:61-64 只查 3000/10000 无 25% 双底线（读码确认；SKILL.md:38/51-53/62）。 |
| F443 | P2 | 成立 | **成立** | 本轮 grep 复验：check_fields_exist 仅 g1.py:102 定义 + dispatch_helper.py:1201 注释 + contracts/fields.py:2 docstring；gate_G1 不调用。 |
| F444 | P1 | 成立 | **成立** | g3.py:151-153 读 skill 层 output_files；生产 progress 形状（codex/materialize/round-exec/run_gate_g3 伪造）均无该键 → G3.3 恒 SKIP（读码确认）。P1 无推翻理由。 |
| F445 | M | 成立 | **成立** | 本轮 grep 复验：shared.py:210-222 unimplemented() 全仓零调用（g_dispatch/g_transition 内联 dict）。 |
| F446 | P2 | 成立 | **成立** | 本轮实测：`count_transition_words('然而'*10)` → `0`、`count_transition_words('然后'*10)` → `10`（双向偏离复现）。 |
| F447 | P2 | 成立（范围不全→F461） | **成立** | chapter_drafting.py:203/:225 transition/fatigue 用全文件计数 vs :241-246 G4.meta 剥离（读码确认；F461 已补 protagonist/scene 消费方）。 |
| F448 | P2 | 成立 | **成立** | chapter_drafting.py:70 `第\d+章` 仅 ASCII 数字；SKILL.md:140 明示"第一章"违规（读码确认）。 |
| F449 | P2 | 成立 | **成立** | 本轮实测：`td={"status":"done"}`（小写）→ GR.2 仍 `status=?` FAIL（GR.1 死、GR.2 修 -scores 后仍 FAIL）；g_reconcile.py:40/61-62 `"DONE"` 字面量 vs 生产小写（codex.py:44）。 |
| F450 | P2 | 成立 | **成立** | g6.py:118-142 按 `## hooks`+`- id:` 解析；真实 pending_hooks.md 为 `# 伏笔追踪`+`## 第N章伏笔呈现` 表格格式（head 实证，无 `- id:` 块）→ total_hooks=1 → 恒 low_hook_density FAIL。 |
| F451 | P2 | 成立 | **成立** | g6_checks.py:101-102/:234-240 剥离正则前瞻仅 `## `+`\Z`（缺 `# `/`<!--META`），对比 shared.py:111 `_META_BOUNDARY`（读码确认）。 |
| F452 | P2 | 成立 | **成立** | chapter_drafting.py:293/:316-328 未剥离元区块即取 `paragraphs[-1]`（读码确认）。 |
| F453 | P2 | 成立（影响面限定） | **成立**（影响面限定） | g2.py:237-238 `_is_important_chapter(fp, project_dir or "")`；phase_runner.py:213 与 g3.py:177 的 G2 调用均无 pd 实参（读码确认）。**限定**：dispatcher/executor.py:144 的 run_g2 传 `str(PROJECT_DIR)` 作 pd——"全部自动化调用路径死路"表述过宽，但列出的 phase_runner/g3 路径确实缺 pd，结论成立。 |
| F454 | P2 | 成立 | **成立** | g_transition.py:47-64/:70-85 读 `remaining_{phase}`/`gate_blockers` 键；round-exec 形状无此键 → 恒真空 PASS，materialize 形状恒 FAIL（读码确认）。 |
| F455 | M | 成立 | **成立** | generic.py:28 docstring 声称 "has frontmatter"，:44-56 无任何 frontmatter 检查（读码确认）。 |
| F456 | P2 | 成立 | **成立** | g6.py:66 `sorted(ch_dir.glob("chapter-*.md"))` 无 key= 数值排序（字典序；读码确认）。 |
| F457 | P2 | 成立 | **成立** | g6_checks.py:111-113/:245 `len(re.findall(...))` 计段数非字符数 → 分子量纲错配（读码确认）。 |
| F458 | P1 | 成立 | **成立** | 本轮实测：`Adjustment(issue_id='X1', severity='high', handling='ignore', rationale='')` → **PASS（HOLE）**；decisions.py:48-59 无非空校验。AGENTS.md P2.5 "rationale REQUIRED on adjustments" 被空串绕过 → P1 无推翻理由。 |
| F459 | P2 | 成立 | **成立** | g4/worldbuilding.py:40-53/:61-64 jload except 缺 ValueError（读码确认）。 |
| F460 | P2 | 成立（范围不全→F465） | **成立**（范围仍不全，见 F471） | 4 处实例读码确认；本轮补充 gate_manifest "gates" 为列表时 setdefault AttributeError（见新 F471，实测崩溃）。 |
| F461 | P2 | 成立 | **成立** | chapter_drafting.py:334/:293-302 protagonist/scene_concreteness 未剥离 PRE/POST（读码确认）。 |
| F462 | P2 | 成立 | **成立** | g3.py:103 `data.get("total_score", data.get("score", 0))`；scoring.py:458 规范输出顶层为 `final_score`（本轮 grep 确认）→ 规范形状 score=0 恒 FAIL。 |
| F463 | P2 | 成立 | **成立** | g3.py:100 遍历全部报告无 skill 过滤、:107 用 gate 技能 rubric 评估其它技能报告（读码确认）。 |
| F464 | P2 | 成立 | **成立** | 本轮实测：codex.py:56 `{skill}-{test_type}-scores-subagent.json` 是唯一自动化 t1-reports 写入方；find_report（shared.py:158-163）与 G0.10 glob（g0.py:440）均不匹配 → G5.1 恒 no_report、G0.10 计数恒 0、GR.2 在 F401 修复后仍 FAIL。 |
| F465 | P2 | 成立 | **成立** | g7.py:37/58 t1_scores `.keys()`、g_reconcile.py:36 skills `.items()`、character_design.py:203 archetype_sources 元素、foreshadowing_plant.py:69 hooks 元素——4 处读码确认。 |
| F466 | P2 | 成立 | **成立**（实例不全，见下方注） | chapter_drafting.py:260-261 `Path(rd)/"project-output"/"chapters"`；G6.3（g6.py:86-93）传 rd=pd → 静默跳过（读码确认）。**注**：同形状调用方至少还有 2 处未枚举——g5.py:281（G5.5 传 `str(project_dir)` 作 rd）与 dispatch_helper.py:1906 `run_gate_g4`（CLI 传 `str(project_dir)` 作 rd）——content_uniqueness 在这些路径同样静默跳过；建议修复时一并覆盖。 |
| F467 | P2 | 成立 | **成立** | chapter_planning.py:37 sections 接受 `## 5[\.、：:\s]` vs :82/:97 s5/s7 提取仅认 `## 5\.`（读码确认）。 |

**误报判定：0 条。** F400-F467 全部成立。对任务点名的 6 个 P1（F401/F404/F408/F432/F444/F458）本轮逐一实测/读码反证，全部确认成立、无推翻证据；F404/F458 空串绕过实测复现（Selection manual_override+high、Adjustment 均 PASS），F408 伪造写入读码确认，F401 GR.2 恒 FAIL 实测复现（含 DONE 与小写 done 两种形状），F432 ALL_SKILLS=74 vs t1-skill 差 5 实测复现，F444 G3.3 skill 层查找读码确认。不准确处仅限范围/表述：F431 枚举仍缺 g3.py:97/102、g5.py:67（F470 补充）；F460 枚举仍缺 gate_manifest "gates" 内层（F471 补充）；F453 "全部自动化调用路径" 过宽（dispatcher executor.py:144 传 pd）；F466 未枚举 G5.5/run_gate_g4 同形状调用方；F426 未枚举 pacing_design.py:31 的 fps[0] 启发式 CWD 回退。

**严重度异议：0 新增。** 确认前两轮 2 条异议成立（F404 P2→P1、F408 P2→P1——两者均属"AGENTS.md 显式契约在正常路径被静默违反"，按决策表 P1 恰当）；其余严重度按决策表核对无异议（F450/F464 的"格式漂移恒 FAIL"可争 P1 的理由前几轮已评估，维持 P2 无异议）。

---

## 二、新发现（F468-F472）

### F468 | G3.2 的 `threshold = 90` 在循环内赋值泄漏到后续报告 → 同一报告因扫描顺序不同得到 PASS/FAIL 不同判定（F428 同根因的第二机制） | error | P2
- 证据: src/shenbi/gates/g3.py:98（`threshold = acceptance.get("t1", T1_PASS)` 在循环外）vs :113/:125（回退分支内 `threshold = 90` **就地覆写循环变量**）；`for rp in reports_dir.glob("*.json")`（:100）遍历全部报告。
- 根因: F428 只描述了"同一报告按形状走不同阈值"的静态分叉；此处是**跨报告状态泄漏**——一旦某报告触发 rubric/维度回退，`threshold` 从 94 永久降为 90，其后所有报告（含带 total_score 的报告）都按 90 判定。报告处理顺序由 `glob` 的目录序决定（非数值/非字母序，实测不稳定），同一组文件两次运行结果可不同：含 total_score=92 的报告在"回退文件先扫"时 PASS(90)、在"回退文件后扫"时 FAIL(94)。
- 验证命令+输出: `.venv/bin/python` 构造 `shenbi-alpha-*-scores.json`（`{"1":95,"2":95}` 触发回退）与 `shenbi-gamma-*-scores.json`（`{"total_score":92}`），确认 glob 序为 alpha→gamma 后 `gate_G3("shenbi-alpha","generative",rd)` → 两报告均 PASS（gamma 的 92 按泄漏的 90 通过）；倒序时 gamma 在 94 下 FAIL。实测输出：`G3.2: {'file': 'shenbi-gamma-generative-scores.json', 's': 'PASS', 'score': 92}`（应为 FAIL）。
- 建议方向: 循环内每报告重新取 `threshold = acceptance.get("t1", T1_PASS)`（或回退分支用局部变量，不覆写循环级 threshold）；与 F428/F462 的阈值统一一并修复；补"回退文件在前、total_score=90-93 在后 → FAIL"的顺序无关用例。
- 严重度说明: 同 F428 家族取 P2（阈值判定不一致，非数据损坏）；但与 F428 相比多一个"结果取决于文件系统枚举顺序"的非确定性维度。

### F469 | shenbi-chapter-revision 的 composite 把 g4_decisions 接进 "existing"（md）槽 → DecisionsDoc schema/P2.5 校验对该技能 decisions.json 永不执行，schema 违规静默 PASS | error | P2
- 证据: generic.py:313（`"shenbi-chapter-revision": make_composite_checker(g4_decisions, g4_chapter_revision)`）；decisions_validator.py:167-172（`make_composite_checker` 内 `existing_checker(md_files + other_files, ...)`、`decisions_checker(json_files + other_files, ...)`——existing 槽收 md，decisions 槽收 json）；g4_decisions 对非 .json 全部 `continue`（decisions_validator.py:95-96）。
- 根因: 其余 composite（chapter-drafting/planning/context-composing/state-settling）都是 `make_composite_checker(g4_xxx, g4_decisions)`——existing 是结构 checker、decisions 是 schema checker，接线正确；唯独 chapter-revision 把 `g4_decisions` 放在 existing 槽、`g4_chapter_revision` 放在 decisions 槽。existing 槽只收 md+other → g4_decisions 对 revision 的 json 文件一个都看不到（只输出 `G4.dec SKIP: no files`），而 g4_chapter_revision 只做 `json.loads` + rationale≥20，不调 `DecisionsDoc.model_validate`。`$schema`/`skill`/`chapter` 缺失、Adjustment 形状错误等 schema 违规全部漏检。
- 验证命令+输出: 构造 `chapter-1-revision-decisions.json`（缺 `$schema`/`skill`/`chapter`，adjustments 为合法 dict + ≥20 字 rationale）→ `gate_G4("shenbi-chapter-revision","generative",[f],rd,project_dir=rd)` → `status: PASS`、`checks: [{'id': 'G4.dec', 's': 'SKIP', 'r': 'no files'}]`（HOLE）；同一文件直接走 `g4_decisions` → `FAIL`（`G4.dec.missing:$schema: Field required` 等 4 条）。
- 建议方向: 改为 `make_composite_checker(g4_chapter_revision, g4_decisions)`（与其它 composite 接线对齐），或让 make_composite_checker 对两个 checker 都按扩展名路由。补"缺 $schema 的 revision decisions → FAIL"用例。
- 严重度说明: G4 层对该技能决策文件的 schema 校验客观缺失；若下游 G2.dec（file_type="decisions"）未覆盖 revision 文件则影响放大，可再评估 P1；取 P2。

### F470 | jload ValueError 家族剩余 3 实例：g3.py:97（acceptance.json）、g3.py:102（逐报告）、g5.py:67（逐报告）→ gate_G3/gate_G5 对合法非 dict JSON 崩溃（F431 枚举仍不全） | error | P2
- 证据（3 处独立实例，均实测崩溃）:
  - g3.py:97 `acceptance = jload(str(accept_path))`，外层 except 在 :140 仅 `(json.JSONDecodeError, OSError)`；
  - g3.py:102 `data = jload(str(rp))`（G3.2 逐报告），内层 except 在 :138 同缺 ValueError；
  - g5.py:67 `rdata = jload(str(report))`（G5.1 逐报告回退路径），**完全无 try**。
- 根因: F431 枚举 8 处、F440 补 g0.py 2 处、F441 补 deps.json、F459 补 worldbuilding 2 处，但 G3.2 的 acceptance 与逐报告、G5.1 的逐报告 3 处仍未列；`shared.jload` 对合法非 dict JSON 抛 `ValueError`（shared.py:52-55），3 处 except 元组均不含 ValueError（g5.py:67 无任何捕获）→ 直接传播，CLI/直接调用面崩溃、无 JSON 输出。
- 验证命令+输出:
  - acceptance.json=`[1,2,3]`（patch g3.TESTS 到临时目录）→ `gate_G3(...)` → `CRASH ValueError ... expected JSON object, got list`；
  - t1-reports 含 `[1,2,3]` 报告 → `gate_G3("shenbi-worldbuilding","generative",rd)` → 同 CRASH；
  - t1-reports 含 `[1,2,3]` 报告（genesis prereq 技能、无 summary.json）→ `gate_G5("genesis",rd,None)` → 同 CRASH。
- 建议方向: 三处统一 `except (json.JSONDecodeError, OSError, ValueError)`（与 g5.py:38/g6.py:37 写法对齐）；g5.py:67 补 try 包裹；补非 dict JSON 用例（acceptance/report 两种形状）。

### F471 | gate_manifest 的 "gates" 键为合法列表时 `setdefault` AttributeError 崩溃（F460/F465 家族内层形状新实例；gate_manifest 0% 覆盖） | error | P2
- 证据: gate_manifest.py:64-66（`phases = data.setdefault("gates", {}); phase_data = phases.setdefault(phase, {}); ...`）——`_load_gate_manifest` 只保证顶层是 dict（:32 `cast(dict, ...)`，非 dict 顶层由 F431 覆盖），"gates" 内层值形状无 isinstance 守卫；`:33` 的 except 只捕获 `(json.JSONDecodeError, OSError)`，`setdefault` 的 AttributeError 不在其中，且发生在 `record_gate_result` 的锁内（:60-89），无任何 except。
- 根因: 与 F441（deps.json 顶层 .get）/F460（内层 .get）/F465（列表元素）同族——json 加载后内层形状不校验；manifest 为 `{"gates": [1,2,3]}` 这类"顶层合法 dict、内层畸形"数据时崩溃。gate_manifest 全文件 0% 覆盖（F417 must-test），该路径无测试兜底。
- 验证命令+输出: 构造 `pipeline-manifest.json={"version":"1.0","gates":[1,2,3]}` → `record_gate_result(rd,"T2",1,"shenbi-x","G4",{...})` → `CRASH AttributeError: 'list' object has no attribute 'setdefault'`（复现；顶层 list 的 F431 已知崩溃亦复现）。
- 建议方向: `_load_gate_manifest` 加载后校验 `isinstance(data, dict) and isinstance(data.get("gates", {}), dict)`，畸形走 reinit（:35 现有语义）；补"gates 为列表/标量"用例；与 F417 的 gate_manifest must-test 一并处置。

### F472 | cli.py bughunt/clean 分支不转发 rd → 相对路径报告恒 resolve_input_path ValueError 崩溃（F403 家族独立实例） | error | P2
- 证据: cli.py:105（`rd = arg(2, None)` 已解析）vs :107-110（`gate_G4_bughunt(file_list)` / `gate_G4_clean(file_list)`——**rd 未传入**）；generic.py:32/74/121（g4_generic_generative/bughunt/clean 内 `resolve_input_path(fp, rd)`，rd=None 时相对路径抛 ValueError，shared.py:70-75）。
- 根因: F403 只覆盖了 generative RoundPaths checker 的双 None raise 与 cli 无 try/except；bughunt/clean 分支是**独立缺陷**——即使用户提供 rd，cli 也丢弃（arg(2) 解析后未传），相对路径报告必然 `ValueError: round_dir required to resolve relative path ...` 崩溃（绝对路径可幸免），CLI 无 JSON 输出、违反 JSON/退出码契约。
- 验证命令+输出: `os.chdir(tmpdir)` 后 `gate_G4_bughunt(["relative-report.md"])` → `CRASH ValueError: round_dir required to resolve relative path 'relative-report.md'; silent CWD fallback removed (Task 15b)`；cli.py:107-110 读码确认 rd 未传。
- 建议方向: `gate_G4_bughunt(file_list, rd)` / `gate_G4_clean(file_list, rd)`（或 CLI 对相对路径先 resolve_input_path）；与 F403 一并处理 CLI 面崩溃；补"bughunt 相对路径+rd"用例。

---

## 三、覆盖空洞声明

- 重读文件数：**52/52**（清单 `zones/Z4.files` 全部文件实际读全文；g4 全部 24 个 checker 逐个通读，含此前轮次已深读的 chapter_drafting/character_design/generic/g6_checks 全文）。未发现清单外遗漏文件。
- 实测基线：本轮未运行全量 pytest（沿用前六轮 785 passed 基线，本轮未修改任何文件）；新增验证命令 15+ 组（`.venv/bin/python` 只读 + /tmp 临时目录构造 + 真实项目 `novel-output/xinghuo-ranqiong` 的 pending_hooks.md/volume_map/SKILL.md 格式核对 + 交叉区源码 dispatch_helper/executor/codex/scoring 核实）；`git status` 复查无跟踪文件污染。
- 本轮重点复核了任务点名的三处：30 个 per-skill g4 checker 全部最后一遍（含 bughunt/clean generic 的 rd 路径）、G0 各子检查（G0.1-G0.16/G0.cc 全部路径）、g6_checks/g7/g3_independence 剩余路径（含 G7.13/G7.14/G7.15/G7.16 全部分支）。
- 跨区交叉确认建议（与 prior rounds 一致）：F469 的 chapter-revision 是否被 G2.dec（file_type="decisions"）兜底 ↔ Z5/Z6（pipeline/dispatcher 区）；F468 的 t1-reports 报告顺序对 G3.2 判定影响 ↔ Z5/Z6；F408 的 run_gate_g3 调用语义 ↔ Z5/Z6；F453/F466 的 phase_runner/g6.py/dispatch_helper G2/G4 调用形状（含本轮补充的 G5.5/run_gate_g4 同形状调用方）↔ Z5/Z6。

## 四、最终消息（复核统计）

- **新 finding：5**（F468-F472：全部 P2，全部实测验证；覆盖 G3.2 阈值跨报告泄漏、chapter-revision composite 接线错位、jload ValueError 家族 3 新实例、gate_manifest 内层形状、cli bughunt/clean rd 不转发）
- **误报：0**（F400-F467 全部成立；对任务点名的 F401/F404/F408/F432/F444/F458 六个 P1 逐一实测/读码反证，均无推翻证据；F404/F458 空串绕过实测复现，F408 伪造写入读码确认，F401 GR.2 恒 FAIL 实测复现，F432 全 74 vs 69 计数实测复现，F444 G3.3 恒 SKIP 读码确认）
- **严重度异议：0 新增**（确认前两轮 2 条异议成立：F404 P2→P1、F408 P2→P1；其余严重度按决策表核对无异议）
- **0 新 finding 判定：否**（本轮发现 5 个前六轮漏报，未收敛到 0）
