# T10 · 历史修复回归核验（跨模块线程）

> **日期**: 2026-08-14
> **范围**: 从 archive specs / INDEX / git log 提取"已修/已合并(PR #N)"声明，逐一 grep 修复签名在当前代码中是否仍存在；消失 = 回归 finding。
> **方法**: 只读核验（grep + read 精确比对），未运行测试/构建。
> **核验基线**: HEAD `192eb87`。

---

## 一、修复声明核验表（9 条已知声明 + 附带声明）

| # | 修复声明（来源） | 证据位置（当前代码） | 状态 |
|---|---|---|---|
| 1 | **TokenLedger 接线**（PR #39 `d4b4e83`；spec §3.1 dead-wire 修复） | `dispatch_helper.py:1333` `TokenLedger(project_dir).record(...)` 位于 `_record_token_usage`，经 `_log_token_usage`（L1297-1298 `if state:` 分支）真实调用；`cost/ledger.py:49` TokenLedger 类、`cost/report.py:40` summarize 消费 | ✅ 存在 |
| 2 | **finish_reason=length 检测**（PR #40 `0be00bb`；spec §2.9/§5.1） | `dispatch_helper.py:1412-1418` streaming 从 final chunk 读 `choice.finish_reason`（getattr 兼容测试 fake）；L1571-1641 `_dispatch_via_api` 分支：`length` → cap-raise **≤1 次**（`min(cap*2, ceiling*0.9)`，L1573；persistent-length 再触发 → fail-fast L1621-1633）；`content_filter` → hard-fail 不重发（L1565-1569）；cap-raise 循环位于 tenacity `@retry` 之外（L1451+）；`_MODEL_OUTPUT_CEILING=65536`（L75） | ✅ 存在 |
| 3 | **truth_io upsert 调用方全覆盖**（CN3 覆盖 bug 修复） | `chapter_loop.py:3014-3020` `write_truth_file(..., mode="upsert_markdown_row", key_field="chapter")`（resonance_trend.md 行 upsert，任务所列 :3014 吻合）；`dispatch_helper.py:1205-1206` emotional_arcs.md / chapter_summaries.md → `upsert_markdown_row`；`truth_io.py:119-129` 完整 upsert 实现 + 每路径 threading.Lock（L107-108）防 lost-update | ✅ 存在 |
| 4 | **torch-bump 处置 follow-up**（archive `2026-08-02-pr20-torch-bump-disposition-design.md`；INDEX:91"待 #3 follow-up"） | `uv.lock` torch = **2.13.0**（spec §5 验收值）；`src/` 零 `import torch`（grep 0 命中）；`.github/dependabot.yml` `allow: dependency-type: "direct"`（过滤 transitive，Task 2）；`renovate.json` 已删除、workflows 零 renovate 引用；`.github/workflows/embeddings-smoke.yml` 每日推理 smoke（instantiate + encode，非 import-only）——follow-up 已由 **PR #28**（`1eb6e22`）交付并归档 | ✅ 存在（处置完成；INDEX 注记过期见 T1002） |
| 5 | **cyclic-import 重构**（PR #26 `4d228f7`；issue24 spec） | `audit/_shared.py` 叶模块锚点（`AuditResult` + `derive_output_files`，仅依赖 `shenbi.contracts.*` + stdlib）；回边全部改指 `_shared`：`executor.py:16`、`write_audit.py:12,22`、`record.py:16`、`phase_runner.py:190`；Cluster 1 侧 `pipeline/_shared.py`（`read_volume_boundaries`/`VOLUME_MAP_PATH`/`_BRIDGE_ACTIVATION_WINDOW`/`_resolve_volume_at_runtime`）+ `triggers.py:83` re-export；`grep "py/cyclic-import" src/` = **0 条**（V3 验收满足） | ✅ 存在 |
| 6 | **silent CWD fallback 移除**（Task 15b / g4 resolve_input_path） | `gates/shared.py:58-75` `resolve_input_path`：`rd` 缺失时 `raise ValueError(..., "silent CWD fallback removed (Task 15b)")`；20+ 个 g4 checker 消费（generic.py / character_design.py / state_settling.py 等），旧 `Path(rd) if rd else Path.cwd()` 反模式已无 | ✅ 存在 |
| 7 | **G0.16 skill 契约验证 + P2.5 rationale 规则**（AGENTS.md:70） | `g0.py:620-645` G0.16 块调用 `g0_skill_contract.check_skill_contracts()`（≤500 字 trigger-only 描述 / writes·updates 不相交 / mode 声明）；`AGENTS.md:70` P2.5 规则原文在；`g4/decisions_validator.py` schema + P2.5 rationale 校验；`g2.py:71-84` `file_type="decisions"` 分支跳过 word count | ✅ 存在 |
| 8 | **决策 JSON 恢复**（`_validate_json_output` raw_decode；spec §3 Layer 1） | `dispatch_helper.py:785-856`：严格 parse 失败后 `json.JSONDecoder().raw_decode`（L826）截断恢复；恢复对象若 `$schema=="shenbi-decisions-v1"` 必须过 `DecisionsDoc.model_validate`（L834-848，防截断残缺）；接线点 L1116 | ✅ 存在 |
| 9 | **文档行号漂移修复**（sdd spec-deviations D1"✅ 已修（spec 文本订正）"） | 归档 spec `2026-08-01-inference-control-audit-design.md` 确含订正行号（§2.3 `:1387-1393`/`:1522-1524`、§2.9 `:1410-1411`/`:1532-1533`、`:1371-1415`/`:1401-1408`/`:593-613`/`:720-727`）——修复动作已执行；**但**订正值相对当前代码已再次漂移（详见 T1001） | ⚠️ partial（见 T1001） |

### 附带核验（PR #23/#25/#28/#40 声明的其余"已修"项）

| 声明 | 证据 | 状态 |
|---|---|---|
| PR #23 Issue 1：mkdocs 14 死链已修（`1933483`）+ Gap A 本地拦截 | `tools/pre-push-check.sh:48` docs 变更触发 `mkdocs build --strict` + 死链特征串判定块 | ✅ |
| PR #23 Issue 7：G0.11 fixture 漂移已修 + Gap C 守卫 + Task 2.3 单一源 | `g0.py:13` `MIRROR_MAP` 模块级单一源；`tools/check_fixture_mirror.py` 从 `shenbi.gates.g0` import MIRROR_MAP；`.pre-commit-config.yaml:79` `fixture-mirror-sync` 注册；当前 `outline-example.md` 与 `tests/fixtures/outline-example.md` sha256 **一致**（无漂移） | ✅ |
| PR #25 Gap B：pre-push pip-audit 锁 dev 组 | `tools/pre-push-check.sh:37-39` `uv sync --frozen --group dev` 后再 `uv run pip-audit` | ✅ |
| PR #40 audit-T5：pro→flash 文档漂移 | `src/` + `executor_config.toml`：`deepseek-v4-pro` 0 命中 / `deepseek-v4-flash` 13 命中 | ✅ |
| PR #40 audit-T6：RETRY_JITTER 加宽 | `parallel_dispatch.py:31-36` `RETRY_BACKOFF_BASE=2.0`、`RETRY_JITTER=2.0`（注释注明 spec §5.3/§2.8） | ✅ |
| PR #40 audit-T7：定价 fail-loud + ledger 守卫 | `cost/pricing.py:40-56` `estimate_cost` 未知模型 `raise ValueError`；`cost/ledger.py:23` `_safe_estimate_cost` 包裹（0.0 + warning） | ✅ |
| PR #28：renovate.json 移除 + ci.yml renovate step 移除 | `renovate.json` 不存在；`.github/workflows/*.yml` 零 renovate 引用 | ✅ |
| PR #40 audit-T3：drafting max_tokens 提至 32768 | `executor_config.toml:12` `max_tokens = 32768`（L8-10 注释记载 16384→32768 决策） | ✅ |
| PR #16/#17：字段级 reads（dict-form）落地 | `skills/shenbi-chapter-drafting/SKILL.md` 等 `reads: - file: ... fields: [...]` 存在 | ✅ |

---

## 二、回归 findings

### T1001 · [P2] D1 行号订正在归档 spec 中再次漂移（4 处关键引用偏离实际代码 7-22 行）

- **证据**：sdd `spec-deviations.md` D1 记录"✅ 已修（spec 文本订正）"，归档 spec `2026-08-01-inference-control-audit-design.md` 确含订正行号。但订正值基于 **PR #40 实现前**（post-#39）的代码布局；PR #40 自身的 finish_reason 捕获 + cap-raise 块插入使 `dispatch_helper.py` 行号再次移位，归档 spec 未再订正：

| 订正值（归档 spec） | 当前实际 | 偏离 |
|---|---|---|
| §2.3 `:1387-1393`（`client.chat.completions.create`） | L1399 | ~6 行 |
| §2.3 `:1522-1524`（kwargs 注入 temperature/max_tokens/timeout） | L1544-1546 | ~22 行 |
| §2.9 `:1410-1411`（streaming 内 stop_reason log） | L1431-1432 | ~21 行 |
| §2.9 `:1532-1533`（调用方 `api_dispatch_early_stop` log） | L1554-1555 | ~22 行 |

- **性质**：纯文档引用漂移；语义仍可定位（4 处均在受 PR #40 影响的同一函数内），非功能回归。但该修复的**初衷就是行号准确**——修复自身已再次失效，且 D1 偏差文本已预测此系统性脆弱性。归档 spec 引用行号时仍可能误导后续读者。
- **建议**：对归档 spec 的行号引用统一加"基于 commit X 快照"声明，或改为符号引用（函数名）避免再漂。

### T1002 · [M] INDEX:91 "PR #20 torch-bump 处置（待 #3 follow-up）" 注记过期

- **证据**：follow-up（dependabot.yml `dependency-type: direct` + embeddings CI 推理 smoke）已由 **PR #28**（`1eb6e22` "chore(deps): PR #20 follow-up — dependabot.yml + embeddings inference smoke"）交付，对应 follow-up spec `2026-08-02-pr20-followup-dependabot-and-embeddings-ci-design.md` 已归档（commit `7f2ecc9`）。而 INDEX #3（deterministic-skill-replacement-audit）正文与依赖字段**无任何 torch/embeddings/sentence 引用**——"待 #3 follow-up"无对应物，误导读者以为该 follow-up 仍未执行。
- **性质**：文档注记漂移（INDEX 最后更新 2026-08-14，但该行未同步 PR #28 归档状态）。

---

## 三、统计

- **核验声明总数**：9 条已知 + 10 条附带 = **19 条**
- **存在 ✅**：17（含 torch 处置本体）
- **消失 ❌**：**0**（无任何已修签名从当前代码消失）
- **partial ⚠️**：1（T1001 行号订正再漂移——修复动作存在但已失效）
- **回归 finding 数**：**2**（T1001 P2 漂移 ×1、T1002 M 注记过期 ×1）；无 P0/P1 回归

### 交叉备注（供 Z 区线程参考，非 T10 finding）

- 声明 3 的修复签名（upsert_markdown_row 调用点）存在，但 **INDEX #7 活跃 P0 finding F397**（append_dedup no-op，chapter_summaries 仅 2/56 章）落在同一 truth_io 区域——F397 涉及的是 `append_dedup` 路径而非本声明核验的 upsert 路径，两者需在 Z 区线程区分核验，勿将 F397 归因于 CN3 修复回归。
- 声明 2 的 IDE-CLI 路径无 finish_reason 信号为归档 spec §2.9 明确记载的已知限制（API 路径 only），非回归。
