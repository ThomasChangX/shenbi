# T15 线程报告 — Git 历史考古（git log 分析）

**线程**: T15 · 跨模块 git 历史考古
**日期**: 2026-08-14
**范围**: revert 周期 / 未完成迁移 / 孤儿分支 / churn 簇 / 大文件入库 / 凭证残留（转 T12）
**方法**: `git log` / `git rev-list` / `git fsck` / `git cat-file` / blame / branch 合并状态对照

---

## 1. 大文件/二进制入库（96MB blob 定位）

### 1.1 结论：96MB blob **无路径**（孤儿对象，unreachable）

- Blob: `1e19f17122544c6c6b075443b7479bb05b43c247`，96608128 字节（96MB）
- `git rev-list --objects --all | grep 1e19f171` → **0 命中**；`git fsck --unreachable` 列为 unreachable blob
- 内容头部：`commit 8e864467d0cae5e36d8f175c69ded2c51d0cc87b\nAuthor: ThomasChangX...`——是 **git commit 对象原始文本 dump**（636 行 `commit ` 前缀），形如 `git cat-file --batch` 或 `git log --format=raw` 输出被重定向到文件后 `git add` 过但从未被任何 commit 引用
- 磁盘形态：loose 对象 `.git/objects/1e/19f171...` 29.7MB（zlib 压缩后），文件 mtime **2026-08-14 00:43**——即今日审计运行窗口内产生（首条 dump 的 commit 8e86446 为今日 00:17 的 `docs: upgrade full-project-audit prompt to v2`）
- 无 reflog 引用、无任何 branch/tag 可达 → 纯垃圾对象，`git gc --prune=now` 即可清除；**不存在于任何分支历史，不影响推送**，但本地 `.git` 因此膨胀至 75MB

### 1.2 可定位的可达大文件（gh-pages 分支）

| 路径 | 大小 | blob | 状态 |
|---|---|---|---|
| `search/search_index.json`（gh-pages） | 6.9MB | 036d03c7 | **可达**（gh-pages 部署产物） |
| `assets/javascripts/bundle.79ae519e.min.js.map`（gh-pages） | 1.0MB | 5cf02892 | 可达 |
| search_index.json 历史变体 ×4 | 各 6.5-6.9MB | 4db9b104 / 31450399 / 4ba37f68 / 32b8a358 / b063d65a | **孤儿**（gh-pages 历史构建残留） |
| 文件清单 dump（.DS_Store/.cache/... 开头） | 4.1MB | 595f5b05 | 孤儿 |
| uv.lock 旧版本 | 673KB | 65138cea | 孤儿 |

### 1.3 仓库卫生量化

- `.git` 总 75MB：pack 27MB + loose（其中 1e19f171 单项 29MB，另有 4×~1.9MB 的 search_index loose）
- `git cat-file --batch-all-objects` 全部 blob 合计 274MB，其中多数为不可达孤儿
- 建议：`git gc --prune=now`（可回收约 45MB+）；gh-pages 构建产物若长期演进可考虑 `--filter=blob:limit` 或精简部署分支

---

## 2. revert-revert 周期

### 2.1 G5 numeric：fix → revert → **从未 re-apply**（闭环未完成）

- `b74e9ae` (fix): g5 numeric-conflict crash（`m.group(2)` on non-capturing group）——把 `(?:...)` 改回捕获组 `(...)`
- `dc6fc67` (revert): 理由 "pins intentional inert behavior (Non-Goal #3: do not modify source in test PRs)"；revert message 明说 **"The fix belongs in a separate source PR that also flips that pin"**
- **现状核查**：当前 `src/shenbi/gates/g5.py:153` 仍是非捕获组 `(?:个|种|...)`，`:159` 仍读 `m.group(2)` → 运行时 IndexError 被 `except Exception: continue` 吞掉 → **numeric 冲突永不报告**；`tests/unit/gates/test_g5.py:139 test_g5_numeric_inconsistency_not_detected_pins_inert_behavior` 显式 pin 该死路（"source bug"）
- 后续 `git log --all --grep="numeric"` 无任何重新修复提交 → **revert 承诺的 source PR 从未落地**，修复永久悬置
- 与 ledger **F498**（verified：G5.3 数值一致性检测完全死路）一致，本报告提供其 git 历史根因链

### 2.2 ZCode auto-detect：引入 → revert（合理纠错，但残留半迁移）

- `5d531d6` auto-detect ZCode 环境（ZCODE_BASE_URL → 自动配置 API）
- `b8bebe6` revert：**"ZCode API is web app not LLM endpoint"** —— 属合理纠错（错误假设被纠正），非设计波动
- 残留：`dispatch_helper.py:1678` 注释 "zcode support requires separate testing"、`:1680` `for cli_name in ("codex", "zcode")`、`:1776` "future codex --json or zcode usage-report feature"、`:1807` docstring 仍列 zcode → IDE CLI 路径中 zcode 支持是**未测试的半成品**（见 T1506）

### 2.3 其他 "revert" 字样提交（非周期）

- `404ee92` "remove silent CWD fallback… partially reverted the spec's goal"——修复性描述
- `23c0497` / `e99b33c`：revert **Codex 的过早/回归性改动**（ruff per-file-ignores 移除、annotation 重命名）——AI 生成代码反复返工信号
- 无同一功能"引入→revert→再引入→再 revert"的完整周期；真周期风险点是 2.1

---

## 3. 未完成迁移 / 半迁移标记

- **`src/shenbi/contracts/legacy.py`**：docstring 自称 "Despite historical naming (originally a legacy compatibility layer), this is the current single source of truth"——文件名 legacy 实为**当前**契约加载器，命名未迁移（见 T1507）
- **`gates/g4`**：已完全迁移到包（单文件 `gates/g4.py` 已删，`__init__.py` re-export `gate_G4`；`g5.py:275 / g7.py:143 / g6.py:83 / cli.py` 均经 `shenbi.gates.g4` 导入）→ **干净，无残留**
- **`phase_runner.py`**：仍存在 + entry point `shenbi-phase` + 测试引用，非死代码；与 `pipeline` CLI 双轨并存（AGENTS.md 均收录），不算半迁移
- **zcode 残留**：见 2.2 与 T1506

---

## 4. 孤儿分支 / 分支合并状态

### 4.1 本地孤儿分支 `docs/token-efficiency-p2-spec`（T1503）

- tip `5c1a7a4`（2026-08-02，fork 于 `de8481c`=PR #39 archive 提交之后），2 commits：
  1. `f347cb8` docs(specs): P2 token-efficiency spec — cache layer / IDE-CLI split / example externalization
  2. `5c1a7a4` docs(specs): field-level reads coverage spec (3.7)
- **main 上两者均不存在**（`find docs -iname "*cache-ide*" / "*field-level*"` 零命中；specs INDEX 无 #6 条目；orphan 分支 INDEX 的 #6/#3.7 条目指向 main 缺失文件）
- **field-level Layer B 功能已实现**（`ea9575e` 2026-07-08 "implement Layer B field-level filtering" + `b05b9fb`），但 3.7 **spec 文档从未合并也未归档** → 功能与文档漂移
- 建议：将两个 spec 合并回 main（或归档），删除孤儿分支

### 4.2 远程分支残留（T1504）

| 分支 | commits 未合并 | 实际状态 |
|---|---|---|
| `origin/sdd/inference-control-audit` | 13 | 内容已 squash 合入 main（`0be00bb` #40），**分支未删** |
| `origin/docs/archive-inference-control` | 1 | 归档内容已在 main（`2bf7642` #41），**分支未删** |
| `origin/pre-commit-autoupdate` | 1 (`c585556`) | ruff v0.16.0/mypy v2.3.0/hooks v6.0.0 升级**未合入**，main 仍 v0.15.17/v2.1.0/v4.5.0（升级被跳过或 PR 关闭） |
| dependabot ×10 | 各 1 | 全部 NOT-IN-MAIN（actions/cache-6、setup-python-7、upload-artifact-7、create-pull-request-8、action-gh-release-3、basedpyright-1.39.9、cyclonedx-bom-7.3.1、openai-2.50.0、pymdown-extensions-11.0.1、sentence-transformers-5.6.1）→ PR 未合并/关闭未删 |
| `origin/gh-pages` | — | 部署分支（正常，但携带 6.9MB 构建产物，见 T1502） |

---

## 5. churn 簇（全历史 `git log --name-only --all`）

| 文件 | 次数 | findings 关联 |
|---|---|---|
| `pyproject.toml` | 66 | 13 次 dependabot bump + 阈值 89→85 下调（`5b3e0ef`，对应 F0-08 注释漂移）+ ruff/mypy 配置反复 |
| `src/shenbi/pipeline/dispatch_helper.py` | 36 | **F505**（lock 生命周期）/ **F512**（写审计绕过）/ **F300**（转义 no-op）/ **F222**（语义分叉）/ **F266**（write_semantics 未接线）——churn 与 findings 强关联 |
| `src/shenbi/pipeline/chapter_loop.py` | 27 | pipeline 主循环（never-completes / 重试放大簇） |
| `src/shenbi/gates/shared.py` | 24 | gate 共享逻辑（契约/门机漂移簇） |
| `src/shenbi/gates/g5.py` | 18 | revert 周期（T1505）+ F498 |

churn 本身不新增 finding，但确认 dispatch_helper（36）为**跨 5 条 findings 的热点文件**，且 pyproject 高频 churn 中自动化 bump 占 20%。

---

## 6. 历史凭证残留（转 T12）

- `git log -S "api_key" --diff-filter=ACD`：0 命中
- 工作树 `sk-*` / `ghp_*` / `AKIA*` 模式：0 命中
- revert/历史提交 message 无 password/secret/credential 字样
- **与 T12 D1⑧ 零命中结论一致，无新发现**

---

## 7. Findings（T15xx）

| ID | 严重度 | 标题 | 证据 |
|---|---|---|---|
| **T1501** | P2 | 96MB 孤儿 blob（commit 对象 dump）无路径、unreachable；`.git` 膨胀 75MB | `1e19f171...` 96608128B，fsck unreachable，内容=636 commit 文本 dump，mtime 2026-08-14 00:43；建议 `git gc --prune=now` |
| **T1502** | P2 | gh-pages 分支 mkdocs 构建产物入库（search_index.json 6.9MB + bundle js.map 1MB）+ 5 个 6.5-6.9MB 孤儿 search_index 变体 + 4.1MB 文件清单 dump + 673KB uv.lock 孤儿 | `git ls-tree -r -l origin/gh-pages`；batch-all-objects 孤儿清单 |
| **T1503** | P2 | 孤儿分支 `docs/token-efficiency-p2-spec`：P2 效率 spec + field-level 3.7 spec 从未合并 main 也未归档；Layer B 功能已实现（ea9575e）但文档悬空 | 分支 2 commits vs main 零文件；INDEX 条目指向缺失文件 |
| **T1504** | P2 | 远程分支残留：sdd/inference-control-audit（13 commits 已 squash 合入 #40 未删）、docs/archive-inference-control（已合入 #41 未删）、pre-commit-autoupdate 升级未合入（ruff/mypy/hooks 版本滞后）、10 个 dependabot 分支 NOT-IN-MAIN | `git branch -r --contains`；`git merge-base --is-ancestor` 全部 NOT-IN-MAIN |
| **T1505** | P2 | G5 numeric revert 闭环未完成：b74e9ae 修复 → dc6fc67 revert（pin inert）→ 承诺的 source PR 从未落地；`m.group(2)` 非捕获组 IndexError 被吞、numeric 检测死路被测试固化（F498 历史根因） | blame g5.py:153=dc6fc67；`git log --grep=numeric` 无后续修复；test_g5.py:139 |
| **T1506** | M | dispatch_helper zcode 半迁移残留：auto-detect 已 revert 但 IDE CLI 路径仍列 zcode + "requires separate testing"/"future zcode usage-report" 注释 | dispatch_helper.py:1678/1680/1703/1776/1807 |
| **T1507** | M | `contracts/legacy.py` 命名残留：文件名 legacy 实为当前单源契约加载器（docstring 自述） | legacy.py:1-9 |
| **T1508** | M | 归档文档 broken links：2026-06-15-p-1.e-06-enterprise.md → 0001/0002/0009-ADR 链接失效；ci-optimization-design.md → `file.md`；eliminate-existing-warnings-plan.md → `../nonexistent-test-link.md` | 相对路径解析扫描 |

**新 finding 数：8**（P2×5，M×3；T1505 与既有 F498 关联但作为独立 revert-周期 finding 报告）

---

## 8. 建议优先级

1. `git gc --prune=now`（回收 ~45MB+，消除 T1501/T1502 孤儿对象）— 立即可做
2. 合并或归档 `docs/token-efficiency-p2-spec` 两 spec，删本地孤儿分支（T1503）
3. 清理 2 个已合入未删的远程分支 + 关闭/删除 dependabot 残留（T1504）
4. 决策 G5 numeric：要么合入 source PR 翻转 pin（兑现 dc6fc67 承诺），要么正式文档化 Non-Goal（T1505）
5. 低优先级：legacy.py 重命名、zcode 注释清理、归档链接修复（T1506-T1508）
