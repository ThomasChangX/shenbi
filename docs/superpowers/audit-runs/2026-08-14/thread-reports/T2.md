# T2 · 契约单一信源 + sync 幂等（跨模块线程）

> **日期**: 2026-08-14
> **范围**: SKILL.md frontmatter ↔ tests/tiers/deps.json ↔ docs/framework（truth-files.yaml / truth-files.index.json / dependency-dag.json）↔ skills/ 生成物一致性；`shenbi-sync-contracts` 幂等性（/tmp 镜像跑通，`git diff --exit-code` 零 diff）。
> **方法**: 只读 + /tmp 镜像运行（`PYTHONPATH` 覆盖使 `shenbi` 包解析到镜像，避免写真实仓库）；未修改仓库任何 tracked 文件（`git status` 前后一致）。
> **基线**: HEAD `192eb87`（repo 树 clean，仅 2 个无关 untracked 文件）。
> **引用已知 findings（不重复录入）**: F0-02（deps.json 缺 5 skill 登记）、F201（三契约模型 dead-wire）、F624（master.json 59 vs 74）、F0-01（skills 计数文档漂移 69/67 vs 74）。

---

## 一、幂等验证（任务 1）—— 结论：**PASS，零 diff**

### 1.1 方法

- 镜像：`git clone --local` → `/tmp/shenbi-sync-idem`（HEAD `192eb879f`，clean tree，与 repo 树逐字节等价）。
- 运行方式：`PYTHONPATH=/tmp/shenbi-sync-idem/src /Users/xiaotiac/Documents/GitHub/shenbi/.venv/bin/python -m shenbi.sync_contracts`
  - 已验证 `PROJECT` 解析到 `/private/tmp/shenbi-sync-idem`（镜像），所有写入落在镜像，不碰真实仓库。
- 判定：`git -C /tmp/shenbi-sync-idem status --porcelain`（0 变更）+ `git diff --exit-code`（零 diff）+ 与真实仓库 `diff -rq` 逐字节比对。

### 1.2 Run 1 输出（节选）

```
phase_synced outputs=[...] phase=genesis / architecture / planning / drafting / audit / management / import / foundation / short-story
sync_complete skills=72
EXIT=0
```

- Run 1 后：`git status --porcelain` = **0 行**；`git diff --exit-code` = **零 diff**。
- Run 2（再跑一次）：EXIT=0，`git status --porcelain` = 0 行，`git diff --exit-code` = **零 diff**（真正的二次幂等）。
- 与真实仓库字节比对：`tests/tiers/deps.json`、`docs/framework/`（含 dependency-dag.json / truth-files.index.json / truth-files.yaml）、`skills/`（74 个 SKILL.md，含 OD-1 自动 body 视图）—— **全部 IDENTICAL**。

### 1.3 结论

`just check` / pre-push / CI `codegen-idempotency` 的 `git diff --exit-code -- tests/tiers/deps.json docs/framework/ skills/` 判定**成立**：提交态生成物可由单一生成器（frontmatter 契约 + truth-files.yaml 注册表）精确重现，无漂移。**未发现幂等破坏。**

附注（幂等实现细节，均验证一致）：
- sync 对 deps.json 是"读-改 expected_outputs-整写"（`indent=2, ensure_ascii=False`），key 顺序与注释外的组织字段（`_tool_hashes` 99 条、`_calibration_hashes`、`_out_of_pipeline`）全部保留 → 零 diff。
- OD-1 body 视图由 `BODY_BANNER … BODY_END` 哨兵定界，重复运行是确定替换 → 零 diff。
- 72 个契约 skill 全部有 body 视图且与 frontmatter 逐行一致；0 个"有 banner 无契约"、0 个"有契约无 banner"、0 个视图内容不匹配。
- 2 个 meta skill（using-shenbi、shenbi-writing-skills）无契约、无 body 视图——符合设计（load_all_contracts 跳过）。

---

## 二、frontmatter 契约字段完整性（任务 2）—— 通过，无新问题

- 74 个 skill 全部有 SKILL.md、可解析 frontmatter；`name` 与目录名一致（0 失配）、`description` 全存在、契约 `kind` 全存在。
- `load_contract`（schema + registry 校验）加载 **72** 个契约全部通过；`kind ∈ {artifact, report, ephemeral}`。
- G0.16（`check_skill_contracts`）真实树执行 = **0 issues**（≤500 字 trigger-only 描述 / writes·updates 不相交 / 每 write/update 声明 mode）。
- 122 个 write/update 条目声明的 mode 全 ∈ `{create_or_overwrite(100), merge_prose(4), append_dedup(18)}`；no_op_behavior 仅 `skip_write(4)` —— 与 writer 路由（dispatch_helper / truth_io / G4）语义对齐。
- body 视图 ↔ frontmatter 双向一致（见 §一）。

---

## 三、G0.16 依赖 / deps.json t2-phases ↔ skills/ 闭包（任务 3）

- `t2-phases` 9 个 phase，distinct prerequisites **62** 个：全部存在于 skills/ 且全部有可加载契约（0 缺失、0 悬空）。`expected_outputs` 62→由成员 writes/updates 经 `normalize_to_glob` 派生，`verify_bijection` 双侧一致，且全部在注册表中可解析（0 不可解析）。
- 9 个 phase 的 `g4_checker` 全部指向存在的 skill。
- `DepsDoc`（`contracts/schemas/deps.py`，extra=forbid）验证 deps.json 通过。
- **闭包另一侧**：skills/（74）− t2-phases 成员（62）− `_out_of_pipeline` 声明（7）= **5 个 skill 完全未登记**（foreshadowing-lifecycle + review-group-{character,craft,factual,plan}）—— 与 **F0-02 完全一致**，且我验证 F0-02 的表述精确（其余 7 个孤儿 skill 均在 `_out_of_pipeline` 中声明，只有这 5 个连 out-of-pipeline 都没有）。不重复录入，引用即可。
- 4 个 skill（story-architecture / volume-outlining / genre-config / pacing-design）同时是 genesis + architecture 成员（有意复用，`phase_of` 首个命中语义无生产消费者，见 §六）。

---

## 四、docs/framework 生成物 ↔ 代码消费者一致性（任务 4）

| 生成物 | 消费者 | 一致性 |
|---|---|---|
| truth-files.yaml（注册表，输入） | `load_registry` / `bootstrap_registry`（executor `_truth_file_set`/`_decisions_file_set`）/ `snapshot.parametric_globs` / sync | 一致；`bootstrap_registry` 从 `load_registry` 派生，无双解析漂移 |
| truth-files.index.json | `tests/unit/pipeline/test_skill_integration.py`（断言 producers/readers）+ CI idempotency | 91 keys；契约路径 0 缺失、0 死条目；与生成器输出逐字节一致 |
| dependency-dag.json | **无代码/测试/文档消费者**（lint 均内存 `build_dag`）→ **T203** | 与生成器一致（2400 边，0 坏边），但只被 CI idempotency 的 git diff 消费 |

- 契约路径 → 注册表解析：全部可解析（`load_contract` 强制）；index 中每个路径都有 producers 语义（reads/writes/updates 角色列表）。
- 注册表内部：70 concepts / 31 globs / 16 patterns；发现 6 项**死词表** → **T202**。

---

## 五、新 findings（T2xx，共 5 条）

| ID | 标题 | 严重度 | 证据 | 根因 | 验证 | 影响 | 建议方向 |
|---|---|---|---|---|---|---|---|
| **T201** | B.5 字段漂移 lint（`scripts/lint_contract_fields.py`）未接入 CI / pre-push / pre-commit / 测试：docstring 声称 "exit 1 (FAIL, blocks PR)" 在 CI 面不成立 | error | `scripts/lint_contract_fields.py:1-20`（"blocks PR" 声明）; `.github/workflows/ci.yml:55-56`（"Contract + repo-consistency lints" 仅跑 `lint_contracts.py` + `lint_repo_consistency.py`）; `tools/pre-push-check.sh:27-30`（同 2 个）; `.pre-commit-config.yaml:63-65`（lint-contracts hook 仅 `tools/lint_contracts.py`）; tests/ 0 引用 | 该 lint 只在 `just check` → `just lint-contracts` 本地执行；未像 `lint_contract_graph.py` 那样补 pytest 兜底（`test_lint_contract_graph.py` 存在） | `grep -rln "lint_contract_fields" tests/ .github/ tools/pre-push-check.sh` 全部 0 命中；当前树该 lint 通过（"All contract field declarations match truth files"），但无任何门禁强制 | 契约 reads 字段声明 ↔ 真实 truth 文件标题/键漂移可**静默合入**（运行时仅 escape-hatch WARN）；契约单信源层唯一未被 CI 覆盖的 lint | CI/pre-push 增加该 lint 步骤，或补 `tests/unit/tools/test_lint_contract_fields.py`（仿 test_lint_contract_graph 的 clean-repo + injected-drift 双测试） |
| **T202** | truth-files.yaml 死词表 6 项：注册表声明了无任何契约/代码引用的词汇，且无 lint 检查"注册表条目被使用" | doc drift | `docs/framework/truth-files.yaml:89-90`（`short/outline.md`、`short/package.md`）、`:143`（glob `short/chapter-*.md`）、`:145`（glob `chapters/chapter-*-revised.md`）、`:56/:105/:132`（`plans/chapter-N-plan-decisions.json` concept+parametric+glob）、`:135`（glob `snapshots/chapter-*/*`，D20 注释已声明废弃但未删除） | 词表随设计演进添加，未随使用情况清理；无"死条目"lint（F0-02 的 skill↔deps 闭包检查缺失在注册表侧同样成立） | python 统计：6 项不被任何契约路径 fnmatch/verbatim 命中；`grep` src/tests/docs 0 代码引用；D20 测试仅断言 concept 移除、未断言 glob（`test_registry_pipeline_producers.py:test_d20_fictional_dir_concept_deprecated`） | 词表宣称"adding a new file = ONE edit"的治理语义被死条目稀释；`plans/chapter-N-plan-decisions.json` 表明 Layer A decisions 约定未落地到 chapter-planning（计划 sidecar 从未实现） | 删除死条目（保留 `snapshots/chapter-*/*` 的 D20 废弃注释处置：同时删 glob）；为 chapter-planning 补 plan-decisions sidecar 或从注册表移除；lint 增加 unused-registry-entry 检查 |
| **T203** | `docs/framework/dependency-dag.json` 生成但零消费（唯一"消费者"是 CI idempotency 的 git diff 可复现性） | doc drift | `src/shenbi/sync_contracts.py:28,175`（生成）; `tools/lint_contracts.py:51` 与 `tools/lint_contract_graph.py:31` 均内存 `build_dag` 不读 JSON; tests/ 与 docs/（非 superpowers）0 引用 | DAG 生成（spec §5.4 "NEW"）后未接线任何消费方；docstring 自认 2400 边 glob 过度连接是"future impact analysis 噪音" | `grep -rn "dependency-dag" src/ tests/ docs/ mkdocs.yml` 仅 sync_contracts.py 定义与覆盖率 HTML | 死产物 + 维护负担（大文件、无消费者）；若预期是"影响分析/完备性检查"的输入，当前无人使用 | 让 lint_contracts/lint_contract_graph 读该 JSON（避免双实现漂移）并纳入 CI 步骤，或明确降级为文档快照并在 README 注明 |
| **T204** | G0.16 只校验 write mode **存在性**不校验**值合法性**：拼写错误 mode 过门禁，运行时被当默认处理 | error | `src/shenbi/gates/g0_skill_contract.py:128-132`（`if "mode" not in meta` 仅此）; `dispatch_helper.py:1056-1068`（mode 不分支，全按 whole-file 写） | 校验只防"没声明"不防"声明错"；writer 对未知 mode 静默走默认路径 | 当前 122 个 mode 声明全部 ∈ {create_or_overwrite, merge_prose, append_dedup}（无现行违规，latent）；G0.16 0 issues | mode 词表漂移（新 mode 引入或拼写错误）无门禁拦截，语义静默退化 | G0.16 增加 mode ∈ 已知集合校验（与 writer 词表共享单一源） |
| **T205** | sync `derive_expected_outputs`/`verify_bijection` 对"契约加载失败的 phase 成员"静默丢输出：`contracts.get(skill, {})` 空 dict → 该成员 writes 从 expected_outputs 消失且 bijection 两侧同空、自检失效 | error | `src/shenbi/sync_contracts.py:89,113-117`（`contracts.get(skill, {})` + 双侧同源）; `sync_contracts.py:108-110` docstring 自认"not a meaningful check for logic bugs shared" | 生成器跳过 ContractError skill（:40-41）但不在 bijection 中标记"成员无契约" | 当前 62 个 prereq 全有契约（无现行违规）；缓解存在：`lint_contracts.find_load_violations` 在 CI 拦截契约加载失败。latent 防御缺口 | 若未来某 phase 成员契约损坏且 lint 被绕过/未跑，expected_outputs 会静默少输出而 G5.4/cmd_pre_score 匹配失败 | `derive_expected_outputs` 对"成员不在 contracts 中"显式 raise（与 find_load_violations 同源），或 bijection 增加成员存在性断言 |

---

## 六、观察（非 finding）

1. `phase_of`（`contracts/schemas/deps.py:75-85`）无生产调用方（仅 `test_deps.py`）——死函数候选（F201 家族）；其"首个命中 phase"语义在 4 个双 phase 成员（story-architecture 等）上无消费者，当前无影响。
2. DANGLING_WRITE 11 条（lint_contract_graph WARN，非阻塞）：score-*（arc/stratum/volume）、foundation/review_report.md、market-radar-decisions 等——核实这些文件均由 pipeline 代码（triggers.py / closure.py / dispatch_helper.py）消费，仅未走契约 reads 边，**良性**，不升级。
3. `just check`（本地）跑 3 个契约 lint + sync 幂等；CI 只跑 2 个 lint + sync + pytest。闭包 lint 经 `test_lint_contract_graph.py` 在 CI 兜底，但 B.5 无任何兜底（→ T201）。
4. sync 对"单个契约损坏"不报错（仅全缺时报 `no_contracts_loaded`）；该信号完全依赖独立 lint（lint_contracts，CI 有）——设计可接受，但生成器自身无 fail-fast（→ T205 相关）。
5. `shenbi-sync-contracts` 幂等运行写入日志无副作用；72 个契约 skill 全部产出 body 视图，`diff -rq skills/` 与仓库一致。

---

## 七、与已知 findings 的交叉引用（验证而非新增）

- **F0-02**（deps.json 缺 5 skill）：独立复现 —— skills/ 74 − t2-phases 62 − `_out_of_pipeline` 7 = 恰为 foreshadowing-lifecycle + review-group-{character,craft,factual,plan}；`executor_config.toml` 确有 foreshadowing-lifecycle override。**确认成立**，不重复录入。
- **F0-01**（文档计数漂移 69/67 vs 74）：确认 skills/ = 74（72 功能 + 2 meta）。不重复录入。
- **F201**（契约模型 dead-wire）：`contracts/skills/*.py` Pydantic 模型与 frontmatter 契约并存，本次复核未发现模型被 sync/生成物消费（sync 只用 frontmatter）。不重复录入。
- **F624**（master.json 59 vs 74）：plugins/ 侧问题，与契约层正交。不重复录入。

---

## 八、最终消息

**新 finding 数：5**（T201 P1、T202 P2、T203 P2、T204 P2 latent、T205 P2 latent）。
**幂等结论：PASS** —— `/tmp/shenbi-sync-idem` 镜像连跑两次 `shenbi-sync-contracts`，`git diff --exit-code` 零 diff、与真实仓库逐字节一致；CI `codegen-idempotency` 判定成立，未发现幂等破坏。

契约单一信源整体健康：72 契约全部可加载、G0.16 全过、deps.json 闭包（t2-phases 侧）完整、frontmatter ↔ 生成物（body 视图/DAG/index/expected_outputs）双向一致。主要短板在**门禁接线**：B.5 字段漂移 lint 完全游离于 CI/pre-push/pre-commit（T201，本轮唯一 P1），以及注册表死词表与 DAG 死产物的清理缺口（T202/T203）。
