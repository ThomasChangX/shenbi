# round-001-2026-06-11 - Archived

## Phase
Pre-P-1 framework iteration (early T1 testing).

## Skills tested
See summary.json (partial coverage of early T1 skills).

## Archive reason
Produced under pre-P-1 framework version. Outputs are reference data only;
not valid for regression testing per P0 forward-only verification policy
(docs/superpowers/specs/2026-06-15-p-1.e-foundation-completion/08-p0-plan-patches.md).

## Restored from
Originally at `tests/rounds/round-001-2026-06-11/`. Moved to archived/ in
P-1.D (earlier migration), README added in P-1.E PR-21.

## Note on rounds 002-007
Rounds 002-007 exist on disk but are gitignored (.gitignore line 26:
`tests/rounds/round-[0-9][0-9][0-9]-...`). They never entered git history
and cannot be archived via `git mv`. The gitignore policy already prevents
them from polluting the repo.
